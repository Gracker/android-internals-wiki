# AIW 自动 Review 任务报告 (13.9)

## 一、目标发现结果
- **扫描范围**：`src/part3-tools/ch13-perfetto/`
- **候选章节**：
  1. `09-tracing-infrastructure.md` | 技术底层原理，涉及内核与用户态交互，复杂度高，需硬核核验。
- **最终选择**：`09-tracing-infrastructure.md`
- **选择理由**：该章节作为 Perfetto 系列的底层基石，解释了数据如何从内核流向 UI，涉及大量源码路径和协议格式，若出现事实错误会严重误导深度开发者。
- **排除的高频原因**：其他章节已标记为 `reviewed` 或处于 `draft` 状态。

## 二、总体结论
- **总体技术评分**：4.2/5
- **是否建议回炉**：否（建议进行结构化修正）
- **主要风险**：部分 Perfetto 源码路径在 AOSP 主线中已发生迁移，且对 `atrace` 的分类映射描述略显单一。
- **评分理由**：整体原理描述极其扎实，特别是对 Android 16/17 UprobeStats 的前瞻性描述非常出彩。扣分点在于部分源码路径的时效性以及对 `trace_marker` 协议完整性的微小遗漏。
- **闭环建议**：修正 Perfetto 内部组件的源码路径，并补充 `trace_marker` 的 Counter (C) 格式。
- **本轮 review 覆盖范围**：Ftrace 三种模式、tracefs 接口、atrace 分类机制、trace_marker 协议、Perfetto 架构、Android 13+ 启动追踪、UprobeStats、Tracing 开销。
- **本轮未完成部分**：对 GKI 内核下 tracefs 挂载路径的差异（文中标记为待验证）本轮已基本确认，无需额外深度搜索。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4/5 | 1 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 5/5 | 0 |
| 知识盲区 | 4.5/5 | 1 |
| 数据/案例支撑 | 4/5 | 0 |
| 交叉引用一致性 | 4/5 | 0 |

## 四、P0 问题（事实错误）
*暂无 P0 级别错误*

## 五、P1 问题（重要缺失）
- **[P1][源码准确性][Perfetto 架构部分]**
- **原文问题**：`traced_probes` 源码位于 `external/perfetto/src/traced_probes/`。
- **源码 / 一手资料锚点**：AOSP `external/perfetto/src/traced/probes/`
- **关键代码逻辑**：在现代 Perfetto 代码库中，`traced_probes` 的核心实现（如 `ftrace_controller.cc`）已移至 `src/traced/probes/` 目录下。
- **运行原理说明**：虽然 binary 名字仍叫 `traced_probes`，但其源码组织结构已与 `traced` service 归并到同一父目录下以便共享逻辑。
- **核验结论**：路径引用过时。
- **为什么这是重要缺失**：读者若按此路径在 AOSP 检索源码会找不到核心文件。
- **建议补充方向**：修正为 `src/traced/probes/`。

## 六、P2 问题（建议改进）
- **[P2][知识盲区][用户空间 Trace tag 底层实现]**
- **原文问题**：只提到了 `B` (Begin) 和 `E` (End) 格式。
- **证据或观察依据**：`atrace.cpp` 和 `libcutils/Trace.cpp` 广泛使用 `C` (Counter) 格式。
- **问题描述**：基础设施章节应覆盖 `trace_marker` 的完整常用协议。
- **建议**：补充 `C|<pid>|<name>|<value>` 格式说明，因为 Counter 也是 Perfetto 中非常重要的数据维度。

- **[P2][原理链完整性][atrace 的分类机制]**
- **原文问题**：`atrace gfx` → 主线实现里至少启用 `ATRACE_TAG_GRAPHICS`。
- **证据或观察依据**：`atrace.cpp` 中的 `k_categories` 和 `k_ftraceEventMap`。
- **问题描述**：`atrace` 的 category 不仅仅对应用户态 tag，通常还联动特定的 ftrace events。
- **建议**：明确指出 `gfx` 除了 tag，还会联动如 `events/gpu_mem/gpu_mem_total/enable`（如果存在）或特定的 vendor ftrace points。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| GKI 内核下 tracefs 挂载路径 | 中 | 确认在所有主流厂商 Android 13+ 设备中，`/sys/kernel/tracing` 是否已完全取代 `/sys/kernel/debug/tracing`。 |
| Perfetto SDK 的 Java 层封装 | 中 | Android 16+ 的 `ProfilingManager` 是否允许 Java 层直接注册结构化数据源。 |

## 八、外部核验建议
- **搜索关键词**：`site:perfetto.dev perfetto_trace_on_boot`
- **建议查来源**：Perfetto 官方文档中关于 `perfetto_trace_on_boot` 的 SELinux 权限限制，确保在 `user` 版本固件上的行为描述准确。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：13.9
- **严重级别**：P1
- **问题类型**：源码错误
- **位置**：Perfetto traced 守护进程与数据流 -> traced 的架构
- **问题描述**：`traced_probes` 源码路径引用不准确。
- **建议修正方向**：从 `src/traced_probes/` 修正为 `src/traced/probes/`。

### 9.2 知识盲区清单（供后续研究）
- **章节**：13.9
- **盲区描述**：Android 16+ 是否提供了 Java 层直接向 Perfetto 注册 Data Source 的能力。
- **重要程度**：低
- **建议研究方向**：关注 Android 16 `ProfilingManager` 的 API 演进。

### 9.3 一般建议清单（非阻断）
- **章节**：13.9
- **问题类型**：原理缺失
- **位置**：用户空间 Trace tag 的底层实现
- **问题描述**：缺失 `trace_marker` 的 Counter (C) 格式。
- **建议**：增加对 Counter 格式的简要说明。

### 9.4 可复用知识资产（高价值新增知识）
- **章节**：13.9
- **一手资料链接**：[UprobeStats - Android Source](https://source.android.com/docs/core/debug/uprobestats)
- **关键源码路径**：`platform/packages/modules/UprobeStats`
- **关键类 / 方法**：`com.android.uprobestats` Mainline 模块。
- **版本差异摘要**：Android 16 引入，利用 eBPF + uprobe 实现动态插桩，取代部分静态埋点需求。
- **可直接复用的技术结论**：UprobeStats 允许在不发布系统更新的情况下，通过 Config Push 动态下发 BPF 程序，针对生产环境的 Reboot 或恶意应用行为进行毫秒级精度的动态监测。

## 十、下一候选章节
- `src/part3-tools/ch13-perfetto/10-perfetto-sql-advanced.md`（如果存在）或 `src/part4-system/14.10-ebpf-deep-dive.md`。

## 十一、落盘信息
- **已写入文件**：`logs/external-review/2026-04-25-13-ch13.09-external-review.md`
