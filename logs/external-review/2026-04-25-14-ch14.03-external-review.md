# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch14-other-tools/`
- 候选章节：
  1. `03-memory-tools.md` | 本章涵盖了 Android 开发中最重要的内存分析工具链，技术密度高，且涉及多个 Android 版本演进。
- 最终选择：`src/part3-tools/ch14-other-tools/03-memory-tools.md`
- 选择理由：该章节作为内存优化工具的汇总，直接影响读者在实战中的工具选型和数据解读。原文在部分源码路径和新版本特性（如 Android 14+）上存在缺失或陈旧描述。

## 二、总体结论
- 总体技术评分：3.8/5
- 是否建议回炉：是
- 主要风险：源码路径陈旧（针对 Android 11+ ），部分高版本特有的调试信号缺失，且在 Bitmap 内存排查路径上缺乏更具体的 OQL 实战指引。
- 评分理由：文章逻辑清晰，覆盖了主流工具。但在“源码准确性”和“版本差异覆盖”维度存在 P1 级缺失，尤其是 AOSP 目录结构的变更未在 source 引用中同步更新。
- 闭环建议：修正 AOSP 源码路径引用；补充 Android 14+ 的信号量定义；在 MAT 章节增加 OQL 示例。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 3/5 | 2 (P1) |
| 原理链完整性 | 4/5 | 1 (P1) |
| 版本差异覆盖 | 3.5/5 | 1 (P1) |
| 知识盲区 | 4/5 | 1 (P2) |
| 数据/案例支撑 | 4/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
暂无 P0 级逻辑错误。

## 五、P1 问题（重要缺失）

- [P1][源码准确性][Frontmatter Sources]
- **原文问题**：引用的 AOSP 路径为 `system/core/libmeminfo` 和 `system/extras/malloc_debug`。
- **核验结论**：自 Android 11 (R) 起，`libmeminfo` 已迁移至 `system/memory/libmeminfo`。`malloc_debug` 的核心实现始终位于 `bionic/libc/malloc_debug`，`system/extras` 下仅为部分辅助脚本或旧版镜像残留。
- **建议修正方向**：更新 Frontmatter 中的源码路径，并标注 Android 11 这一变更分界线。

- [P1][版本差异覆盖][malloc debug 章节]
- **原文问题**：只提到了 `SIGRTMAX-19` 和 `SIGRTMAX-17`。
- **核验结论**：Android 14 (U) 引入了 `SIGRTMAX-16` (信号值 48)，用于在收到信号时触发 `libmemunreachable` 扫描并报告内存泄漏。这对于 Native 内存泄漏的在线/离线快速诊断非常有价值。
- **建议修正方向**：在 malloc debug 的信号表格或描述中补充 `SIGRTMAX-16`。

- [P1][原理链完整性][MAT 章节]
- **原文问题**：提到 Android 8.0+ 后无法直接看像素，建议用 Profiler。
- **运行原理说明**：虽然无法直接看像素，但通过 OQL 结合 `mWidth`、`mHeight` 和 `Config` 可以精确过滤“疑似大图”。
- **建议补充方向**：提供一段 OQL 示例代码，展示如何计算 Estimated Native Memory，这比单纯依赖 Profiler 更具自动化潜力。

## 六、P2 问题（建议改进）

- [P2][知识盲区][showmap 章节]
- **问题描述**：未提及 `showmap` 对 `profileable` 应用的限制。
- **建议**：补充说明在非 root 设备上，即便应用是 `profileable`，`adb shell showmap <pid>` 依然可能因为权限无法读取 `/proc/<pid>/smaps`，此时 heapprofd 是更好的选择。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|----------|--------------|
| `libmemunreachable` 信号触发 | 中 | 研究 Android 14 信号 48 的具体日志输出格式及限制。 |
| MTE 异步模式实战 | 高 | MTE 在 Android 15+ 上的默认策略及对 `malloc` 性能的影响。 |
| Graphics 内存跨进程分摊 | 中 | 如何在 `dumpsys meminfo` 中区分 SurfaceFlinger 侧和 App 侧的 buffer 占用。 |

## 八、外部核验建议
- **搜索关键词**：`android malloc debug SIGRTMAX-16`
- **建议来源**：AOSP `bionic/libc/malloc_debug/README.md`
- **搜索关键词**：`libmeminfo Android 11 migration`
- **建议来源**：cs.android.com 提交记录。
- **搜索关键词**：`LeakCanary Shark Hprof Indexing`
- **建议来源**：Shark 官方文档，确认其“不加载整堆”的具体内存计算方式。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：14.3 内存分析工具
- **严重级别**：P1
- **问题类型**：源码错误 / 版本差异缺失
- **位置**：Frontmatter Sources 及 malloc debug 信号描述
- **问题描述**：1. `libmeminfo` 路径应更新为 `system/memory/libmeminfo` (Android 11+)。2. 缺失 Android 14 `SIGRTMAX-16` 信号。
- **建议修正方向**：同步最新 AOSP 目录结构，并补全信号量说明。

### 9.2 知识盲区清单（供后续研究）
- **章节**：14.3
- **盲区描述**：MTE (Memory Tagging Extension) 在 Android 15 生产设备上的实际可用性。
- **重要程度**：高
- **建议研究方向**：Pixel 8/9 系列的 MTE 开启状态及对应用层的影响。

### 9.4 可复用知识资产
- **章节**：14.3
- **一手资料**：`bionic/libc/malloc_debug/README.md`
- **关键源码路径**：`bionic/libc/malloc_debug/`
- **关键调用链**：信号 45 -> `DebugData::ToggleBacktrace` -> 切换全局 Hook 状态。
- **版本差异摘要**：Android 14+ 支持信号 48 (SIGRTMAX-16) 进行不可达内存检测。
- **技术结论**：在 Android 11+ 中，查阅 libmeminfo 逻辑应前往 `system/memory/` 目录。

## 十、下一候选章节
- `src/part3-tools/ch14-other-tools/04-cpu-tools.md`

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-14-ch14.03-external-review.md`
