# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part2-performance/ch07-smoothness/01-jank-definition.md`
- 候选章节：
  1. `01-jank-definition.md` | 明确指定 review
- 最终选择：`01-jank-definition.md`
- 选择理由：用户指令指定
- 排除的高频原因：N/A

## 二、总体结论
- 总体技术评分：4.5/5
- 是否建议回炉：否
- 主要风险：AppDeadlineMissed 的边界定义在源码层面上可以更加精确，部分术语缺少源码对应变量名。
- 评分理由：文章对 FrameTimeline 的归因体系分类非常清晰，能准确区分 App、SF 和 HAL 的责任，对 JankStats 的 multiplier 机制也有深入理解。但对于 FrameTimeline.cpp 内部 `actualEndTime` 的具体计算逻辑可以再下沉一层。
- 闭环建议：根据建议补充 P2 细节即可。
- 本轮 review 覆盖范围：Jank 定义、FrameTimeline 分类、FPS/Jank 感知、JankStats 机制。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4.5/5 | 1 |
| 原理链完整性 | 4.5/5 | 0 |
| 版本差异覆盖 | 5/5 | 0 |
| 知识盲区 | 4.5/5 | 1 |
| 数据/案例支撑 | 4.5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
无。

## 五、P1 问题（重要缺失）
无。

## 六、P2 问题（建议改进）
- [P2][源码准确性][Google 的 Jank 分类体系 - AppDeadlineMissed]
- 原文问题：提到 `终点取 max(gpu time, post time)`。
- 证据或观察依据：在 `frameworks/native/services/surfaceflinger/FrameTimeline/FrameTimeline.cpp` 中，App 端的实际结束时间判定逻辑主要依据 `actualQueueBufferTime` 和 GPU fence signaled time。
- 问题描述：虽然概念上 `post time` 和 `gpu time` 是对的，但用 AOSP 里的术语描述会更精准，即 `max(actualQueueBufferTime, actualGpuCompletionTime)`。
- 建议：建议将 `post time` 进一步澄清为 App 调用 `queueBuffer` 将 Buffer 提交给 BufferQueue 的时间，以便读者和 Systrace/Perfetto 中的 `queueBuffer` 动作对应起来。

- [P2][知识盲区][不同刷新率下的帧预算]
- 原文问题：提到了 VSync-app 和 VSync-sf 错峰推进。
- 证据或观察依据：AOSP `DispSync` / `VSyncPredictor` 中的 `appPhase` 和 `sfPhase` 配置。
- 问题描述：读者可能不清楚具体是怎么"错峰"的。
- 建议：可以简单补充一句：这种错峰是通过 `appPhase` 和 `sfPhase`（VSync Offset）来实现的，允许 App 提前被唤醒进行渲染。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|----------|--------------|
| VSync Offset 机制细节 | 中 | 梳理 Android 10+ 之后 DispSync 到 VSyncPredictor 的演进，以及厂商如何调整 appPhase / sfPhase 来优化延迟 |

## 八、外部核验建议
- 搜索关键词 `SurfaceTimeline` / `JankTracker` 源码：建议查 AOSP `frameworks/native/services/surfaceflinger/FrameTimeline/JankTracker.cpp` 中具体的判定条件枚举。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
无

### 9.2 知识盲区清单（供后续研究）
- 章节：7.1 卡顿的定义与分类
- 盲区描述：VSync Offset (appPhase / sfPhase) 在高刷屏下的动态调整策略
- 重要程度：中
- 建议研究方向：AOSP VSyncPredictor 源码及 `dumpsys SurfaceFlinger` 中的 offset 配置
- 可能关联章节：2.3 VSync 机制

### 9.3 一般建议清单（非阻断）
- 章节：7.1 卡顿的定义与分类
- 问题类型：源码准确性
- 位置：AppDeadlineMissed
- 问题描述：`post time` 描述不够底层。
- 建议：标注 `post time` 对应于 BufferQueue 的 `queueBuffer` 时刻。

### 9.4 可复用知识资产（高价值新增知识）
- 章节：7.1 卡顿的定义与分类
- 一手资料链接：`frameworks/native/services/surfaceflinger/FrameTimeline/FrameTimeline.cpp`
- 关键源码路径：`SurfaceFlinger` 的 `FrameTimeline` 模块
- 可直接复用的技术结论：AppDeadlineMissed 的实际结束时间是 `max(actualQueueBufferTime, actualGpuCompletionTime)`。

## 十、下一候选章节
- 建议继续 review `02-jank-causes.md`

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-22-15.1-01-jank-definition-external-review.md`