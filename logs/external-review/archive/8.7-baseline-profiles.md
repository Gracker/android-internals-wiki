# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`logs/external-review/TODO-part1-2.md` 未完成项
- 候选章节：
  1. `src/part2-performance/ch08-responsiveness/07-baseline-profiles.md` | TODO 列表首个未完成项
- 最终选择：`src/part2-performance/ch08-responsiveness/07-baseline-profiles.md`
- 选择理由：按 TODO 列表顺序逐篇处理。
- 排除的高频原因：N/A

## 二、总体结论
- 总体技术评分：5/5
- 是否建议回炉：否
- 主要风险：无技术风险。
- 评分理由：本章展现了极高的系统底层认知深度。它精准地划清了 Baseline Profiles（随包分发）、Cloud Profiles（Play 商店分发）和 Local JIT Profiles（设备端生成）的三大边界，并对 APK/AAB 中的 Profile 路径与设备端安装后的 `/data/misc/profiles/` 及 `/data/app/.../oat/` 目录进行了极为清晰的区分。这种级别的细节正是大多数开发者在排查“Profile 为什么不生效”时最欠缺的。对 Compose 极度依赖 AOT 编译的底层原因分析也非常到位。
- 闭环建议：直接放行。可以微调部分命令行参数建议（P2）。
- 本轮 review 覆盖范围：全篇（ART 编译策略演进、Profile 格式与打包、安装时编译流程、Macrobenchmark 生成、与 AutoFDO 的区别、验证状态、Compose 依赖性）。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 5/5 | 0 |
| 版本差异覆盖 | 5/5 | 0 |
| 知识盲区 | 4.5/5 | 0 |
| 数据/案例支撑 | 5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
无

## 五、P1 问题（重要缺失）
无

## 六、P2 问题（建议改进）
- [P2][源码准确性][验证是否生效：先看编译状态，再看启动收益]
- 原文问题：文中推荐手动触发编译的命令为 `adb shell cmd package compile -r bg-dexopt com.example.app`。
- 证据或观察依据：虽然使用 `-r bg-dexopt`（以后台 dexopt 理由触发）是完全有效的，但在官方的 Baseline Profiles 调试文档中，为了更直接地强制应用按 Profile 编译，通常推荐使用显式指定编译过滤器的命令：`adb shell cmd package compile -m speed-profile -f com.example.app`。
- 建议：建议在文中同时补充 `-m speed-profile` 的显式强制编译命令，这在日常线下验证时行为更明确。

- [P2][源码准确性][扩展 - Profileable 应用与性能分析]
- 原文问题：“`<profileable>` 元素本身是 API 29 加入的，`android:shell` 属性是 API 30 新增的。”
- 证据或观察依据：根据官方文档，`<profileable>` 元素和它的 `android:shell` 属性都是在 Android 10（API 29）中一并引入的。
- 建议：修正此处的 API 版本边界细节。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|---------|-------------|
| dex2oat 编译失败的静默回退机制 | 中 | 当 Baseline Profile 规则文件中存在已经失效的类或方法签名时，ART 的 `dex2oat` 是如何处理的（容错机制），是否会导致整个 Profile 失效。 |

## 八、外部核验建议
- 搜索关键词：`Android dex2oat Baseline Profile mismatch fallback`
- 建议查 AOSP / 官方文档 / Perfetto / blog / issue tracker 哪类来源：`art/dex2oat/dex2oat.cc` 源码中解析 Profile 文件的容错逻辑。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
无

### 9.2 知识盲区清单（供后续研究）
- 章节：8.7 Baseline Profiles 与编译优化实践
- 盲区描述：由于代码混淆（R8）或版本迭代导致 HRF 规则与实际 DEX 方法签名不匹配时，安装端的 `dex2oat` 编译表现。
- 重要程度：中
- 建议研究方向：梳理 R8 插件是如何自动将 HRF 中的人类可读签名重写为混淆后签名的，以及匹配失败时的 ART 日志。
- 可能关联章节：构建优化相关章节。

### 9.3 一般建议清单（非阻断）
- 章节：8.7 Baseline Profiles 与编译优化实践
- 问题类型：实操建议
- 位置：`验证是否生效：先看编译状态，再看启动收益`
- 问题描述：手工触发编译的 ADB 命令可以提供更直接的替代方案。
- 建议：补充 `adb shell cmd package compile -m speed-profile -f <package>`。

### 9.4 可复用知识资产（高价值新增知识）
- 章节：8.7 Baseline Profiles 与编译优化实践
- 为什么这条知识值得保留：文章精准地指出：不要去 APK 里的 `/assets/dexopt/` 下找编译产物，那只是源文件；也不要去 `/data/misc/profiles/` 下找，那只是中间数据；真正的机器码归宿在 `/data/app/.../oat/arm64/base.odex`。这种层次分明的文件流转解释，是解决绝大多数“为什么我的 Profile 没生效”疑问的终极答案。此外，明确 AutoFDO（C/C++ 内核态）与 Baseline Profiles（Java/Kotlin 用户态）的异同，非常见功底。

## 十、下一候选章节
- 下一章建议继续 review 的章节：`src/part2-performance/ch08-responsiveness/08-media-pipeline.md`

## 十一、落盘信息
- 已写入文件：`logs/external-review/15.08-07-baseline-profiles.md`