# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part2-performance/` 及相关章节文件
- 候选章节：
  1. `2.12 Window Manager Service 与窗口管理` | `status: ready-for-review`, `task9_state: pending`
- 最终选择：`2.12 Window Manager Service 与窗口管理`
- 选择理由：该章节作为 Android 窗口系统的核心原理篇，直接影响后续启动优化（8.2）和卡顿分析（7.x）的深度。当前状态为 `ready-for-review` 且涉及大量 Android 14-17 的前瞻性内容，技术核验价值极高。
- 排除的高频原因：排除 `status: draft` 的章节。

## 二、总体结论
- 总体技术评分：4.2/5
- 是否建议回炉：否（建议进入结构化修正队列，补充 P1/P2 项即可）
- 主要风险：部分前瞻性特性（Android 16/17）的源码锚点和具体触发条件尚需通过最新 AOSP 代码进一步核实；Perfetto SQL 示例需根据 Android 14 后的 Slice 变化做精准适配。
- 评分理由：文章结构极其扎实，完美符合“为什么-怎么做-怎么用”模型。源码引用路径准确（如 `ViewRootImpl.java` 的 relayout 逻辑），对版本差异（SplashScreen, Predictive Back, Desktop Windowing）的覆盖非常到位。
- 闭环建议：无需重写，但需补充 `relayoutAsync` 的具体触发规则及 Surface 同步路径的最新变化。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4.5/5 | 1 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 4.8/5 | 1 |
| 知识盲区 | 4.0/5 | 2 |
| 数据/案例支撑 | 3.5/5 | 1 |
| 交叉引用一致性 | 4.5/5 | 0 |

## 四、P0 问题（事实错误）
*本章节未发现 P0 级事实错误。*

## 五、P1 问题（重要缺失）
- **[P1][原理链完整性][relayoutAsync 触发规则]**
  - 原文问题：提及了 Android 14+ 引入 `relayoutAsync()`，但未具体说明哪些属性变更会走异步路径，哪些必须维持同步。
  - 缺失内容：`relayoutAsync` 在 `ViewRootImpl` 中的判定逻辑及 WMS 端的配套处理流程。
  - 建议补充方向：补充基于 `LayoutParams` 的 flags（如 `FLAG_KEEP_SCREEN_ON`）或 `screenBrightness` 等不涉及窗口尺寸变化的属性会走异步 relayout。

- **[P1][版本差异覆盖][Android 17 recreateOnConfigChanges]**
  - 原文问题：文中标记 `[待验证]`，作为技术架构文档，需给出目前 Beta 阶段最接近的结论。
  - 缺失内容：该属性在 `AndroidManifest.xml` 中的具体解析逻辑及它与传统 `android:configChanges` 的优先级关系。
  - 建议补充方向：查阅 Android 17 Beta 源码中 `PackageParser` 或 `ActivityRecord` 处理该属性的片段。

## 六、P2 问题（建议改进）
- **[P2][数据/案例支撑][Perfetto Slice 名变迁]**
  - 问题描述：Android 14 后，部分核心 Slice 名（如 `relayoutWindow`）在某些厂商或特定的 Atrace Category 下可能被重构。
  - 建议：在 Perfetto 章节补充一个“Slice 模糊搜索”的技巧，说明如何通过 `SELECT name FROM slice WHERE name LIKE '%relayout%'` 兼容不同版本。

- **[P2][源码准确性][SurfaceControl.Transaction 同步阻塞]**
  - 原文问题：文中提到“`apply()` 本身不是同步阻塞等待 SF 的”，但在 `mFirst`（首次创建）场景下可能存在特殊的同步栅栏。
  - 建议：核验 `SurfaceControl.Transaction` 是否在包含 `setBuffer` 且伴随 `setSyncTransaction` 时会产生阻塞。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| **Shared Memory Insets** | 中 | Android 15 后 Insets 是否存在通过共享内存减少跨进程通信的优化。 |
| **WMS 锁拆分现状** | 高 | `mGlobalLock` 在 Android 16+ 中是否有进一步拆分（如针对 Display 或 Task 级别）的计划。 |
| **BLASTBufferQueue 资源回收** | 中 | App 侧 `updateBlastSurfaceIfNeeded` 触发时，旧 Buffer 的释放时机对内存瞬时峰值的影响。 |

## 八、外部核验建议
- **搜索关键词**：`relayoutAsync android 14 implementation`, `Android 16 desktop mode windowing architecture`, `recreateOnConfigChanges android 17 beta`.
- **建议查来源**：
  - `cs.android.com`: 搜索 `relayoutAsync` 在 `IWindowSession.aidl` 中的定义。
  - `developer.android.com`: 核对 Android 16/17 的官方 API 变更日志。
  - `perfetto.dev`: 检查 `android.server.wm` category 的最新内置 tracks。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：2.12
- **严重级别**：P1
- **问题类型**：原理断裂 / 版本差异
- **位置**：`relayoutWindow` 节及版本演进表
- **问题描述**：缺乏 `relayoutAsync` 的触发条件细节；Android 17 `recreateOnConfigChanges` 状态不明确。
- **建议修正方向**：在源码调研基础上明确异步 relayout 的场景，并根据最新 Beta 资料补齐 Android 17 章节。

### 9.2 知识盲区清单（供后续研究）
- **章节**：2.12
- **盲区描述**：WMS `mGlobalLock` 的细粒度锁演进。
- **重要程度**：高
- **建议研究方向**：关注 AOSP 中 `WindowManagerService` 内部锁对象的重构。

### 9.3 一般建议清单（非阻断）
- **章节**：2.12
- **问题类型**：案例支撑
- **位置**：Perfetto 综合表现
- **问题描述**：缺少具体的 SQL 示例。
- **建议**：补充一段用于查询 WMS 相关耗时 Slice 的标准 SQL。

### 9.4 可复用知识资产（高价值新增知识）
- **章节**：2.12
- **一手资料链接**：`cs.android.com`
- **关键源码路径**：`frameworks/base/core/java/android/view/ViewRootImpl.java`
- **关键调用链**：`performTraversals` -> `relayoutWindow` -> `mWindowSession.relayout`
- **版本差异摘要**：Android 14 引入异步 relayout，Android 16 将 Desktop Windowing 作为核心特性的 WMS 影响。
- **Trace 观察点**：`relayoutWindow` 在 system_server 的 Binder 线程执行，注意其对 `mGlobalLock` 的竞争。
- **可直接复用的技术结论**：`requestLayout` 不等于必然跨进程 relayout，需命中 6 大阈值。

## 十、下一候选章节
- `2.13 图形缓冲区管理 (BufferQueue)`

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-26-11-2.12-external-review.md`