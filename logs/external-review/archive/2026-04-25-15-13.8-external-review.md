# AIW 自动 Review 任务报告 (13.8)

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch13-perfetto/`
- 候选章节：
  1. 13.8 | Perfetto 输入延迟 SQL 深度分析
- 最终选择：13.8
- 选择理由：输入延迟是交互体验的核心，而 `android.input` 模块是 Perfetto 近年来的重点演进方向，必须核验其 SQL 表结构和延迟计算逻辑的准确性。

## 二、总体结论
- 总体技术评分：4.8/5
- 是否建议回炉：否
- 主要风险：`android_input_events` 表中的 `frame_id` 关联逻辑在 Android 12 以下版本可能不生效，文中已提到 Android 12 基线，但可稍微补充一下低版本的 fallback 现象（即关联帧信息为空）。
- 评分理由：对 stdlib 模块的封装逻辑理解深刻，尤其是对 `iq`/`oq`/`wq` 队列堆积的分析，抓住了 InputDispatcher 性能排查的本质。
- 闭环建议：在“ Choroegrapher 与 Input 的时序关联”部分，补充一条关于“ speculative frame”的提示，因为输入系统有时会预测帧，这在 SQL 关联时会出现多对一或一对多的情况。
- 本轮 review 覆盖范围：android.input 模块表结构、端到端延迟查询、InputDispatcher 队列分析、Choreographer 关联、批量对比脚本。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 5/5 | 0 |
| 版本差异覆盖 | 4.5/5 | 1 |
| 知识盲区 | 4.5/5 | 1 |
| 数据/案例支撑 | 5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
暂无。

## 五、P1 问题（重要缺失）
暂无。

## 六、P2 问题（建议改进）
- [P2][知识盲区][ Choroegrapher 与 Input 的时序关联 - 预测输入]
- 原文问题：提到了 `frame_id` 关联。
- 证据或观察依据：Android 输入系统支持 `Predictive Back` 和 `Resampled Motion Events`。
- 问题描述：如果开启了输入采样（Resampling），一个渲染帧可能对应多个原始输入采样点，`android_input_events` 的一行可能不代表一个完整的物理采样。
- 建议：在解释 `frame_id` 时，注上一句“如果开启了事件重采样，关联关系会变得复杂”。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| `InputDispatcher` 的 `Connection` 生命周期 | 中 | SQL 表 `android_input_connections` 的状态变迁含义 |
| 虚拟输入设备在 Trace 中的表现 | 低 | 如何区分物理触摸和通过 adb 下发的虚拟点击 |

## 八、外部核验建议
- 搜索关键词：`perfetto android_input_events resampled events`
- 建议查：AOSP `frameworks/native/services/inputflinger` 源码中的采样逻辑。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
无。

### 9.4 可复用知识资产
- 章节：13.8
- 核心指标：`dispatch_latency_dur` (IPC 延迟)、`handling_latency_dur` (App 处理延迟)。
- 关键队列：`iq` (Inbound)、`oq` (Outbound)、`wq` (Wait)。
- 为什么这条知识值得保留：这是定位 ANR 和滑动不跟手问题的“标准量化口径”。

## 十、下一候选章节
- 13.9

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-15-13.8-external-review.md`
