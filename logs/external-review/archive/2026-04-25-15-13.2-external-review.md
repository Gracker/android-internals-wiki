# AIW 自动 Review 任务报告 (13.2)

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch13-perfetto/`
- 候选章节：
  1. 13.2 | Trace 抓取
- 最终选择：13.2
- 选择理由：本节涉及大量 `TraceConfig` 配置，直接决定了抓取结果的有效性，且 `perf_event` 相关配置在不同版本 AOSP 中存在 schema 变更，必须严谨核验。

## 二、总体结论
- 总体技术评分：4.2/5
- 是否建议回炉：否
- 主要风险：`PerfEventConfig` 的描述虽然提到了新旧差异，但在示例中混用了旧字段（如 `target_cmdline`），可能导致新版本 AOSP 解析失败。
- 评分理由：涵盖了从命令行到 UI、从同步到异步、从内存到堆剖析的全量抓取场景，实用性极强。
- 闭环建议：修正 `PerfEventConfig` 示例，统一使用 `callstack_sampling` 嵌套结构。
- 本轮 review 覆盖范围：命令行参数、TraceConfig 结构、atrace 分类、Heap Profiling、CPU 采样（perf_event）。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4/5 | 1 |
| 原理链完整性 | 5/5 | 0 |
| 版本差异覆盖 | 4.5/5 | 1 |
| 知识盲区 | 4/5 | 1 |
| 数据/案例支撑 | 5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
暂无。

## 五、P1 问题（重要缺失）
- [P1][源码准确性][CPU Callstack Sampling - PerfEventConfig]
- 原文问题：文中的 `perf_event_config` 示例中提到了“旧资料里常见的顶层 `target_cmdline` 已过时”，但在前面的大段 pbtxt 示例中仍然没有完全体现 `callstack_sampling` 内部的 `scope` 结构。
- 源码 / 一手资料锚点：`external/perfetto/protos/perfetto/config/profiling/perf_event_config.proto`。新版 `PerfEventConfig` 明确将进程过滤逻辑收口到了 `CallstackSampling.Scope` 消息中。
- 为什么这是重要缺失：使用旧字段在 Android 14+ 的 perfetto 版本上会触发 `deprecated` 警告，且可能在未来版本中失效。
- 建议补充方向：提供一个符合当前主干推荐实践的 `perf_event_config` 最小化 pbtxt 示例。

## 六、P2 问题（建议改进）
- [P2][知识盲区][atrace Categories - binder_driver]
- 原文问题：提到了 `binder_driver`，但没说明它对内核版本或 `CONFIG_BINDERFS` 的依赖。
- 证据或观察依据：Android 10+ 引入 binderfs，其 ftrace 挂载点发生了变化。
- 问题描述：部分旧内核设备可能不支持某些 binder 事件。
- 建议：在分类表里注上一句“取决于内核是否开启相应 ftrace event”。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| binderfs 时代的 ftrace 路径 | 低 | 研究 `/sys/kernel/debug/tracing` 与 `/sys/kernel/tracing` 的符号链接关系 |
| DWARF unwinding 性能开销 | 中 | 对比 `linux.perf` 在不同采样频率下对 CPU 的压测数据 |

## 八、外部核验建议
- 搜索关键词：`perfetto perf_event_config callstack_sampling scope`
- 建议查：`perfetto.dev` 的 Config Reference 页面。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- 章节：13.2
- 严重级别：P1
- 问题类型：源码错误 / 接口过时
- 位置：CPU Callstack Sampling 配置示例
- 问题描述：使用的 `perf_event_config` 结构不符合当前推荐的 AOSP proto 定义。
- 建议修正方向：参考 `perf_event_config.proto`，将采样范围和 unwinding 配置移入 `callstack_sampling` 块。

### 9.4 可复用知识资产
- 章节：13.2
- 关键源码路径：`external/perfetto/protos/perfetto/config/trace_config.proto` (TraceConfig 根定义)
- 关键调用链：`android.os.Trace` -> `ATrace_beginSection` -> `trace_marker` (ftrace)
- 为什么这条知识值得保留：这是 Android App 开发者介入系统级追踪的唯一标准路径。

## 十、下一候选章节
- 13.3

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-15-13.2-external-review.md`
