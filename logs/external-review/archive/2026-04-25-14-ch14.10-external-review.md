# AIW 自动 Review 任务报告 (14.10)

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch14-other-tools/`
- 最终选择：`src/part3-tools/ch14-other-tools/10-ebpf-performance-analysis.md`
- 选择理由：该章节涉及 Android 15/16/17 的前沿技术（UprobeStats, sched_ext），技术深度高，且标注为 `task9_pending`，急需技术核验。

## 二、总体结论
- 总体技术评分：3.8/5
- 是否建议回炉：是
- 主要风险：部分 AOSP 源码路径描述不准；`UprobeStats` 的内部组件命名与源码有细微偏差；`sched_ext` 的启用条件需更明确。
- 评分理由：文章涵盖了从基础到前沿的 eBPF 知识，逻辑清晰。但作为深度技术手册，源码路径的准确性是 P0/P1 级要求。目前发现 `UprobeStats` 和 `netd` 的路径描述需要根据 AOSP android-16.0.0_r1 进行修正。
- 本轮 review 覆盖范围：基础设施、Simpleperf 结合、UprobeStats 原理、sched_ext 架构、实战场景、版本差异。
- 本轮未完成部分：无，已完成全章深度 review。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 3.0/5 | 2 |
| 原理链完整性 | 4.5/5 | 0 |
| 版本差异覆盖 | 4.5/5 | 0 |
| 知识盲区 | 4.0/5 | 1 |
| 数据/案例支撑 | 4.0/5 | 0 |
| 交叉引用一致性 | 5.0/5 | 0 |

## 四、P0 问题（事实错误）
- [P0][源码准确性][AOSP 中 eBPF 程序的位置]
- 原文问题：`packages/modules/UprobeStats/src/bpf_progs/`
- 源码锚点：`packages/modules/UprobeStats/bpf/` (android-16.0.0_r1)
- 核验结论：在最新 AOSP 分支中，BPF 源码位于模块根目录下的 `bpf/` 文件夹内，而非 `src/bpf_progs/`。
- 建议修正方向：修正路径为 `packages/modules/UprobeStats/bpf/`。

## 五、P1 问题（重要缺失）
- [P1][源码准确性][Android eBPF 基础设施 - 网络统计]
- 原文问题：提到 `netd` 大量使用 BPF，但指出 `system/netd/bpf_progs/` 不是直接定位目录。
- 源码锚点：`packages/modules/Connectivity/bpf_progs/`
- 核验结论：Android 12+ 后网络栈模块化，原本属于 `netd` 的 BPF 程序（如流量统计、计费）已迁移至 Connectivity 模块。
- 建议补充方向：明确给出 `packages/modules/Connectivity/bpf_progs/` 作为网络 BPF 程序的核心源码路径。

- [P1][知识盲区][sched_ext 与可扩展调度器]
- 原文问题：对 `sched_ext` 的启用条件描述略显模糊。
- 证据依据：Kernel 6.12 虽然合并了 `sched_ext`，但在 Android GKI 中默认可能并未开启 `CONFIG_SCHED_CLASS_EXT`。
- 缺失内容：需要明确指出，除了内核版本支持外，还需要检查 `/sys/kernel/sched_ext` 接口是否存在，以及厂商是否在内核配置中启用了该特性。
- 建议补充方向：增加关于如何检测设备是否支持 `sched_ext` 的说明（如查看 `CONFIG_SCHED_CLASS_EXT` 或 sysfs 节点）。

## 六、P2 问题（建议改进）
- [P2][数据/案例支撑][CPU 利用率精准计算]
- 原文问题：提到 eBPF 可以精确区分每一帧的 CPU 使用量。
- 建议：建议补充提到 `simpleperf` 或 `perfetto` 中对应的具体 tracepoint：`sched:sched_switch` 和 `power:cpu_frequency`，增强实战落地感。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| BPF CO-RE 在厂商自定义内核中的重定位失效场景 | 中 | 研究当厂商修改了内核核心结构体（如 `task_struct`）且未更新 BTF 时 CO-RE 的表现 |
| sched_ext 对 Android 功耗（Energy Aware Scheduling, EAS）的潜在冲突 | 高 | 研究自定义调度器如何与 EAS 的能效模型协作或共存 |

## 八、外部核验建议
- `AOSP packages/modules/UprobeStats/bpf/`：核实最新的 `.c` 文件列表。
- `perfetto.dev`：核实 Perfetto 对 `sched_ext` 数据的原生解析支持状态。
- `source.android.com`：确认 Android 16 GKI 发布的官方 `config` 是否默认包含 `CONFIG_SCHED_CLASS_EXT`。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- [P0][14.10][AOSP 路径] `packages/modules/UprobeStats/src/bpf_progs/` 应改为 `packages/modules/UprobeStats/bpf/`。
- [P1][14.10][源码锚点] 补充网络 BPF 程序的实际路径：`packages/modules/Connectivity/bpf_progs/`。
- [P1][14.10][技术前瞻] 明确 `sched_ext` 的启用依赖 `/sys/kernel/sched_ext` 接口的可用性，而不只是内核版本。

### 9.2 知识盲区清单（供后续研究）
- 章节：14.10
- 盲区描述：自定义 eBPF 调度器与 EAS 能效模型的兼容性。
- 重要程度：高
- 建议研究方向：关注 Linux 上游 `scx_eas` 相关实验。

### 9.3 一般建议清单（非阻断）
- 章节：14.10
- 位置：CPU 利用率精准计算小节
- 建议：补充 `sched_switch` 的 trace 观察方法，例如使用 `perfetto` 观察每个 `tid` 的精准运行时长。

### 9.4 可复用知识资产
- 章节：14.10
- 一手资料：AOSP 源码 `packages/modules/Connectivity/bpf_progs/netd.c`
- 关键源码路径：`system/bpf/` (BPF Loader 核心实现)
- 关键类/方法：`UprobeStats` 中的 `bpfPerfEventOpen()` 调用。
- 版本差异：Android 16 是首个底层内核（GKI 6.12）原生支持 `sched_ext` 的版本。
- 为什么值得保留：该结论纠正了“eBPF 只能做监控”的固有印象，明确了其进入调度决策核心层的里程碑。

## 十、下一候选章节
- `src/part3-tools/ch14-other-tools/02-simpleperf.md`（作为 eBPF uprobe 的基础工具篇，建议复审其对新语法的覆盖）。

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-14-ch14.10-external-review.md`
