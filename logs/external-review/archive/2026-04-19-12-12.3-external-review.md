# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part2-performance/ch12-apk-network/`
- 候选章节：
  1. `03-network-performance-deep.md` | 当前待 review，涉及核心网络性能原理。
- 最终选择：`03-network-performance-deep.md`
- 选择理由：用户明确指定。

## 二、总体结论
- 总体技术评分：4.2/5
- 是否建议回炉：否（建议进行结构化补强，而非推翻重写）
- 主要风险：部分 AOSP 模块路径描述较浅，对 Android 14 `HttpEngine` 与 `Cronet` 的集成细节可更具体。
- 评分理由：文章结构清晰，覆盖了从应用层到内核协议栈的完整链路。技术论点基本准确，对 TLS 1.3、DoH3 等前沿特性有较好的把握。但在源码锚点的精确度上还有提升空间。
- 本轮 review 覆盖范围：全章，重点核验了 OkHttp 5、Android 14 HttpEngine、DoH3 Rust 实现、RRC 状态机。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4.0/5 | 2 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 4.5/5 | 0 |
| 知识盲区 | 4.0/5 | 1 |
| 数据/案例支撑 | 4.0/5 | 1 |
| 交叉引用一致性 | 4.5/5 | 0 |

## 四、P0 问题（事实错误）
*暂无发现 P0 级别的事实错误。*

## 五、P1 问题（重要缺失）
- [P1][源码准确性][Android 网络栈全景]
- **原文问题**：提到 `NetworkOnMainThreadException` 对 `targetSdk >= 11` 生效，但未提及底层 `BlockGuard` 机制。
- **源码 / 一手资料锚点**：`libcore/luni/src/main/java/dalvik/system/BlockGuard.java`；`frameworks/base/core/java/android/os/StrictMode.java` (AndroidBlockGuardPolicy)。
- **关键代码逻辑**：`libcore` 中的网络操作会调用 `BlockGuard.getThreadPolicy().onNetwork()`。`StrictMode` 拦截该调用，若检测到 `PENALTY_DEATH_ON_NETWORK` 标志则抛出异常。
- **为什么这是重要缺失**：理解 `BlockGuard` 是理解 Android 系统如何强制执行线程策略的关键。
- **建议修正方向**：在描述 `NetworkOnMainThreadException` 时，简要提及它是通过 `BlockGuard` 机制在 `StrictMode` 中实现的。

- [P1][源码准确性][DNS 解析性能]
- **原文问题**：提到 `DnsResolver` 是独立模块，但未指出其关键的 DoH3 实现是使用 **Rust** 编写的。
- **源码 / 一手资料锚点**：`packages/modules/DnsResolver/doh`。
- **运行原理说明**：Android 11+ 的 DoH3 采用 Rust + `quiche` (external/quiche) 实现，利用 Rust 的内存安全和异步特性（Tokio）解决了 C++ 在处理多流 QUIC 时的复杂性。
- **为什么这是重要缺失**：这是 Android 系统现代化的重要标志，且解释了为何其性能提升显著。
- **建议补充方向**：在 DoH3 部分补充其 Rust 实现背景。

## 六、P2 问题（建议改进）
- [P2][知识盲区][OkHttp 连接池]
- **问题描述**：OkHttp 5 引入了 `ConnectionPool.setPolicy`，允许针对特定地址配置最小连接数（proactive open）。
- **建议**：在描述连接池参数时，提及这一新特性，这对于极低延迟场景（如冷启动首页请求）很有价值。

- [P2][数据/案例支撑][网络请求对电池的影响]
- **证据或观察依据**：5G 引入了 `RRC_INACTIVE` 状态。
- **问题描述**：原文只提到了 4G 的状态机。
- **建议**：补充 5G `RRC_INACTIVE` 状态对功耗和延迟的优化，这能体现文章的“深度”。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| OkHttp 5 `setPolicy` 预建连 | 中 | 调查其在 App 冷启动网络加速中的实战效果。 |
| DoH3 与运营商防火墙的兼容性 | 低 | 部分运营商可能会拦截 UDP 443 导致 DoH3 回退。 |
| `HttpEngine` 的缓存共享 | 中 | `HttpEngine` 是否与 Chrome 浏览器或其他 App 共享缓存。 |

## 八、外部核验建议
- 搜索关键词：`Android 14 HttpEngine Cronet stack sharing`
- 建议查阅：`packages/modules/Cronet` 下的 `apex` 定义，确认模块打包逻辑。
- 搜索关键词：`RRC_INACTIVE 5G NR power savings`
- 建议查阅：3GPP 相关文档或 Qualcomm 5G 白皮书关于省电特性的描述。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：12.3 网络性能深入
- **严重级别**：P1
- **问题类型**：源码准确性
- **位置**：Android 网络栈全景 / DNS 解析性能
- **问题描述**：缺失 `BlockGuard` 机制描述；缺失 DoH3 Rust 实现细节。
- **建议修正方向**：补充 `BlockGuard` 拦截逻辑说明；补充 `packages/modules/DnsResolver` 中 Rust 引擎的作用。

### 9.2 知识盲区清单（供后续研究）
- **章节**：12.3
- **盲区描述**：5G `RRC_INACTIVE` 状态的详细转换逻辑及 tail time 差异。
- **重要程度**：中
- **建议研究方向**：调研 5G 模块在 Android 系统中的表现。

### 9.4 可复用知识资产（高价值新增知识）
- **章节**：12.3
- **一手资料链接**：[Google Online Security Blog 2022-07](https://security.googleblog.com/2022/07/dns-over-http3-in-android.html)
- **关键源码路径**：`packages/modules/DnsResolver/doh`；`external/quiche`
- **关键技术结论**：Android 11+ 的 DoH3 采用 Rust 实现，P95 延迟降低达 44%。
- **为什么这条知识值得保留**：解释了移动端 DNS 优化的最先进手段及其工程实现。

## 十、下一候选章节
- `src/part2-performance/ch12-apk-network/04-network-monitoring.md`（如果存在），或者继续深入 `12.4` 相关章节。

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-19-12-03-network-performance-deep-external-review.md`
