# AIW 自动 Review 任务报告 (13.10)

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch13-perfetto/`
- 候选章节：
  1. 13.10 | Perfetto SQL 性能分析实战手册
- 最终选择：13.10
- 选择理由：本节是全书的“工具箱”，提供的 SQL 模板直接决定了读者解决实际问题的效率。特别是对 ANR 和锁竞争的量化分析，是高级性能工程师的“护城河”。

## 二、总体结论
- 总体技术评分：4.8/5
- 是否建议回炉：否
- 主要风险：几乎无事实错误。唯一的微小风险是：部分 SQL 示例中的 `GLOB '*GC*'` 依赖 slice 名称的字面匹配，虽然通用，但在某些高度定制的 ART 运行时上可能会漏掉某些特定类型的 GC slice。
- 评分理由：对 `thread_state` 与 `slice` 的时间交集分析（overlap_dur）逻辑极具巧思，解决了“一个状态对应多个嵌套 slice”的归因难题；对 `LEAD()` 窗口函数计算调度延迟的讲解清晰透彻。
- 闭环建议：在“线程调度延迟” SQL 后增加一句话，提醒读者 `delay_ns > 0` 的过滤条件是必要的，以排除那些被由于迁核导致的负值或瞬时状态。
- 本轮 review 覆盖范围：SQL 基础、核心表结构、帧时间分布、调度延迟、线程状态、Binder 利用率、GC 关联、启动阶段、ANR 归因。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 5/5 | 0 |
| 版本差异覆盖 | 4.5/5 | 1 |
| 知识盲区 | 4.5/5 | 1 |
| 数据/案例支撑 | 5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
暂无。

## 五、P1 问题（重要缺失）
暂无。

## 六、P2 问题（建议改进）
- [P2][版本差异覆盖][Java Heap 相关 Counter 名称]
- 原文问题：查询 Java Heap 时提到了几种名称匹配方式。
- 证据或观察依据：在 Android 13+ 的某些版本中，`process_stats` 数据源上报的 heap 字段名可能变为 `mem.java_heap.size_kb`。
- 问题描述：如果读者直接使用固定字符串查询，可能会返回空结果。
- 建议：在 SQL 示例中优先展示“查看可用 counter”的查询，教读者“授人以渔”。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| `CREATE VIRTUAL TABLE` 在 Trace Processor 中的应用 | 中 | 如何自定义 SQL 插件以处理特定的私有 trace 格式 |
| `sqlite_master` 表在 Perfetto 里的可见性限制 | 低 | 为什么某些系统表无法通过普通 SELECT 访问 |

## 八、外部核验建议
- 搜索关键词：`perfetto sql thread_state io_wait v53`
- 建议查：Perfetto 官方 SQL Table Reference。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
无。

### 9.4 可复用知识资产
- 章节：13.10
- 核心算法：`LEAD(ts) OVER (PARTITION BY utid ORDER BY ts) - (ts + dur)` 计算调度延迟。
- 核心逻辑：先裁剪 ANR 时间窗，再匹配重叠时间最长的 slice 进行归因。
- 为什么这条知识值得保留：这是在大规模 Trace 分析中实现自动化 ANR 归因的行业级“标准算法”。

## 十、下一候选章节
- 13.README

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-15-13.10-external-review.md`
