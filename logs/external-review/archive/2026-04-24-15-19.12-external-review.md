# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch19-apm/` 目录
- 候选章节：
  1. `12-framemetrics.md` | 深入帧内部耗时拆解的系统 API
- 最终选择：`12-framemetrics.md`
- 选择理由：按顺序进行。此篇与 JankStats 高度相关，必须明确界定两者在线上监控中的主从关系与成本差异，确保读者不会盲目在线上全量开启 FrameMetrics。
- 排除的高频原因：无。

## 二、总体结论
- 总体技术评分：3.5/5
- 是否建议回炉：是
- 主要风险：文章对 FrameMetrics 的 API 用法和各阶段的归因解释得很好，是一份合格的入门指南。但在“线上工程化”方面有所欠缺，特别是与 JankStats 的横向对比不足，且没有写清降采样策略。
- 评分理由：存在 3 处 P1 级别遗漏。作为线上工具选型的参考书，如果不把 FrameMetrics 与 JankStats 的“易用性、线上聚合成本”对比讲透，读者极易在线上写出 OOM 或高耗电的代码。遗漏部分 API 版本边界也会导致兼容性 Crash。
- 闭环建议：进入回炉。补齐 JankStats 对比表、明确各 API 的版本边界，并完善降采样策略。
- 本轮 review 覆盖范围：全文对齐大纲锚点，重点核对了帧阶段拆解的准确性与 DEADLINE 机制。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 3/5 | 1 |
| 版本差异覆盖 | 3/5 | 1 |
| 知识盲区 | 4/5 | 0 |
| 数据/案例支撑 | 3/5 | 2 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
（无事实错误）

## 五、P1 问题（重要缺失）
- [P1][数据/案例支撑][与 JankStats]
  - 原文问题：正文中只是零散提及 FrameMetrics 是 JankStats 的补充，没有系统对比。
  - 缺失内容：大纲明确要求“对比事件粒度、字段语义、易用性、线上聚合成本和专项诊断价值”。
  - 建议补充方向：新增对比段落或表格。必须点明：JankStats 开箱即用且自带 UI Context，聚合成本低；而 FrameMetrics 字段极多，如果每帧在 Java 层读取 10 个 metric，线上聚合的 CPU 和内存开销（尤其是不小心创建了对象）会显著高于 JankStats。

- [P1][原理链完整性][使用建议]
  - 原文问题：“使用建议”段落只讲了窗口聚合和 Perfetto 配合，未提采样。
  - 缺失内容：大纲要求说明“哪些页面适合采、哪些场景应降采样”。
  - 建议补充方向：必须补充线上防劣化策略。例如：高频滚动的 Feed 流或复杂动画场景不适合对所有用户 100% 开启，应降采样至 1% 甚至万分之一；而对于核心转化页（如支付收银台）或启动阶段可以适当提高采样率。

- [P1][版本差异覆盖][指标表]
  - 原文问题：指标表中只给 `DEADLINE` 标注了 API 31+，其他默认按 API 24 处理。
  - 缺失内容：遗漏了部分指标的实际引入版本。
  - 建议补充方向：核实并补充 `UNKNOWN_DELAY_DURATION` 等指标的版本边界（API 28 加入）。如果应用 `minSdkVersion=24`，读取 API 28 的常量会导致崩溃或异常，这一兼容性风险必须指出。

## 六、P2 问题（建议改进）
- [P2][数据/案例支撑][聚合策略]
  - 原文问题：聚合字段示例中，缺少了大纲要求的 `stage max`（阶段最大耗时）。
  - 建议：在 JSON 或文本示例中加上类似 `draw_max_ms=120` 的字段。因为平均数和 P95 无法体现偶发的单次极度严重卡顿，`stage max` 往往是发现长耗时 GC 或主线程大 IO 的关键线索。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|---|---|---|
| FrameMetrics 的 C++ 层 API 替代方案 | 高 | 在 Android 早期，FrameMetrics Java API 存在一定的 JNI 转换和对象分配开销。是否可以通过底层 Hook `Choreographer` 或直接使用 NDK `AChoreographer` 获取同等数据以降低线上开销。 |

## 八、外部核验建议
- 搜索关键词：`Android FrameMetrics UNKNOWN_DELAY_DURATION API level`
- 建议核验：确认 `FrameMetrics` 中各项常量的具体 `Added in API level xx` 标注，确保书中的兼容性指南 100% 准确。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：19.12
- **严重级别**：P1
- **问题类型**：对比缺失与工程边界未明
- **位置**：主要指标、与 JankStats、使用建议
- **问题描述**：遗漏了 `UNKNOWN_DELAY_DURATION` 等常量的版本边界；未提供与 JankStats 多维度的系统对比；遗漏了针对核心页面/高频场景的降采样策略指导。
- **建议修正方向**：按五、P1 列表逐个修补段落与表格。

### 9.2 知识盲区清单（供后续研究）
- **章节**：19.12
- **盲区描述**：使用 NDK `AChoreographer` 或底层采集替代 Java 层 FrameMetrics 以降低线上性能损耗的可行性。
- **重要程度**：高
- **建议研究方向**：调研微信 Matrix 等 APM 框架底层如何获取帧耗时。

### 9.3 一般建议清单（非阻断）
- **章节**：19.12
- **问题类型**：数据指标丰度
- **位置**：聚合时不要保留所有原始帧
- **问题描述**：示例中没有体现“单帧极值”的威力。
- **建议**：增加 `stage max` 字段并辅以解读。

### 9.4 可复用知识资产（高价值新增知识）
- **章节**：19.12
- **可复用的技术结论**：在线上监控中，数据越细、回调越密（如每秒 60-120 次的 FrameMetrics 回调），其采集代码本身就越容易成为性能瓶颈（尤其是跨线程同步和对象分配）。因此，这类 API 必须伴随“独立 HandlerThread”、“轻量数据结构（避免 Autoboxing）”和“严格降采样”这三大工程手段才能上线。

## 十、下一候选章节
- 建议继续 review：`19.13-tracing-sdk.md`

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-24-15-19.12-external-review.md`