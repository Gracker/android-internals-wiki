# AIW 自动 Review 任务报告 (1.7 ART 编译管线)

## 一、目标发现结果
- **扫描范围**：`src/part1-fundamentals/ch01-architecture/`
- **目标章节**：`07-art-compilation.md` (1.7 ART 编译管线与 dex2oat 优化)
- **选择理由**：该章节处于 `pipeline_stage: task9_pending`，涉及 Android 16/17 最新特性及 2025 年 ART 重大更新，技术时效性极高，需深度核查源码级准确性。

## 二、总体结论
- **总体技术评分**：4.2/5
- **是否建议回炉**：是 (建议针对 Android 17 的 `static final` 约束做更深度的源码级说明，并补强 JIT 计数器逻辑)
- **主要风险**：对 JIT 热度计数器的计算细节描述略显泛泛；Android 17 的 `static final` 优化需明确 Target SDK 触发条件。
- **评分理由**：内容覆盖了 2025 年最新的 ART 性能数据（18% 提速），时效性极佳。但在 JIT 内存分配比例及 deoptimization 的复杂场景（如 Instrumentation）上仍有补强空间。
- **本轮 review 覆盖范围**：ART 演进史、JIT 运行时、dex2oat SSA 优化、PGO/Profile 体系、Android 17 不可变性约束。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4.5/5 | 1 |
| 原理链完整性 | 4.0/5 | 2 |
| 版本差异覆盖 | 4.5/5 | 1 |
| 知识盲区 | 4.0/5 | 1 |
| 数据/案例支撑 | 4.5/5 | 0 |
| 交叉引用一致性 | 4.0/5 | 0 |

## 四、P0 问题（事实错误）
*暂无显性事实错误。*

## 五、P1 问题（重要缺失）
- **[P1][原理链][JIT 热度计算]**
  - **原文位置**：JIT 编译器的工作原理 -> 方法热度追踪
  - **原文问题**：仅提到“观察方法调用和后向分支次数”，未说明两者在触发编译时的权重差异。
  - **源码锚点**：`art/runtime/jit/jit.cc`
  - **关键代码逻辑**：ART 实际上维护了一个 `uint16_t` 的计数器。`HotnessUpdate` 逻辑中，UI 线程的调用可能通过 `dalvik.vm.jitprithreadweight` 获得更高权重。
  - **建议修正方向**：补充说明 `jitprithreadweight`（默认 500）对热度累计的加速作用，这解释了为什么主线程方法更快被 JIT 编译。

- **[P1][版本差异][Android 17 约束]**
  - **原文位置**：版本演进 -> Android 17 `static final` 行为变化
  - **原文问题**：未明确该约束主要针对 Target SDK 37+。
  - **核验结论**：根据 Android 17 Beta 文档，反射修改 `static final` 在 Target SDK 37 下抛出 `IllegalAccessException`，JNI 修改则可能导致 Crash。
  - **建议修正方向**：明确指出 Target SDK 37 的分界线，提醒开发者旧版 Mock 框架或插件化方案的潜在风险。

## 六、P2 问题（建议改进）
- **[P2][知识盲区][JIT Code Cache 分配]**
  - **问题描述**：未提及 JIT Code Cache 的 Data 与 Code 1:1 分配比例。
  - **建议**：补充说明 64MB 的 Max Size 中，Data 部分（ProfilingInfo）和 Code 部分（机器码）各占一半，这有助于读者在分析内存时更精确。
- **[P2][实战建议][profman 验证]**
  - **问题描述**：`profman` 的实战说明较少。
  - **建议**：增加如何利用 `profman --dump-only` 快速核验 Profile 覆盖率的示例，这对启动优化工程实践非常有价值。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| **Instrumentation 导致的去优化** | 中 | 研究 `ClassFileLoadHook` 或 `RedefineClasses` 如何触发全量去优化 |
| **Secondary Dex 优化策略** | 中 | ART Service 如何处理插件化框架动态加载的 DEX |
| **Cloud Compilation 分发细节** | 低 | SDM 文件在不同 OEM 设备间的兼容性校验逻辑 |

## 八、外部核验建议
- **搜索关键词**：`art/runtime/jit/jit_code_cache.cc` `kCodeAndDataCapacityDivider`
- **建议查 AOSP**：核实 `dalvik.vm.jitprithreadweight` 在最新 ART 模块中的实现。
- **建议查博客**：跟踪 2025 年底 ART 更新中关于 $O(n) \to O(1)$ 查找的具体 Pass 名称。

## 九、可闭环输出

### 9.1 回炉问题单
- **章节**：1.7
- **严重级别**：P1
- **问题描述**：需补充 Android 17 `static final` 的 Target SDK 约束详情，并细化 JIT 热度计数的权重逻辑（如 UI 线程加速）。
- **建议修正方向**：在 JIT 章节引入 `jitprithreadweight` 概念；在版本演进部分增加针对 Target SDK 37 的风险提示。

### 9.2 可复用知识资产
- **2025 ART 编译提速**：
  - **源码细节**：`FindReferenceInfoOf` 算法从 $O(n) \to O(1)$。
  - **收益**：编译时间缩减 18%，覆盖 Android 12+ (Mainline)。
- **Android 17 不可变性**：
  - **优化点**：强制 `static final` 不可变 $\to$ 激进常量内联 + 冗余 Load 消除。
- **ART Service (Android 14+)**：
  - **关键点**：由 ART Apex 模块托管，引入 `ArtManagerLocal`，支持智能降级（存储不足时 `speed-profile` $\to$ `verify`）。

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-22-20-1.7-external-review.md`
