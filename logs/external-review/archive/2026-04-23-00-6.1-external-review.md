# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part1-fundamentals/ch06-storage/`
- 最终选择：`src/part1-fundamentals/ch06-storage/01-storage-architecture.md`
- 选择理由：作为存储章节的总纲，该篇涉及大量底层协议（UFS 4.0）、内核机制（dm-linear, VABC）和加密框架（FBE, Metadata Encryption）。这些内容的技术时效性极强且版本差异大，是典型的 Task 9 深度复核目标。
- 排除的高频原因：排除 `ch06-storage` 目录下其他具体性能优化篇，优先审架构总纲以确保后续篇章的理论基石准确。

## 二、总体结论
- 总体技术评分：4.5/5
- 是否建议回炉：否（建议执行 P1/P2 级增量修正）
- 主要风险：该章节对 Android 10+ 的挂载模型描述较为精细，但对 **F2FS 垃圾回收 (GC) 的触发时机** 以及 **Metadata Encryption 与 FBE 的并行性能损耗** 描述略显保守。
- 评分理由：文章深度极佳，准确覆盖了 UFS 4.0 MCQ、VABC 等前沿特性。通过 cs.android.com 核验，其源码锚点（如 vold 密钥安装路径、dm-linear 逻辑）完全正确。扣分项在于对 FUSE Passthrough 在元数据操作上的局限性描述不够突出。
- 闭环建议：根据本报告修正 P1 级缺失（VABC 的上下文切换损耗细节）和 P2 级建议（FUSE Passthrough 元数据局限性）。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 5/5 | 0 |
| 知识盲区 | 4/5 | 1 |
| 数据/案例支撑 | 4.5/5 | 1 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
*暂无 P0 级事实错误。*

## 五、P1 问题（重要缺失）
- **[P1][原理链完整性][Virtual A/B 与 snapuserd]**
- **原文问题**：提到“升级期间的写入性能会受到影响...一次写入会变成两次 I/O”。
- **源码 / 一手资料锚点**：`system/core/fs_mgr/libsnapshot/` 下的 `snapuserd` 实现。
- **关键代码逻辑**：Android 12 的 VABC 依赖 `dm-user` 与 `snapuserd`。每一个块读取都涉及 `Kernel (dm-user) <-> Userspace (snapuserd)` 的上下文切换。
- **运行原理说明**：原文仅提到写入放大，遗漏了最重要的**上下文切换延迟（Context Switch Latency）**。在合并（Merge）期间，CPU 频繁在中短周期 I/O 上进行状态切换，这才是用户感知到“OTA 后重启卡顿”的核心原因，而非简单的双倍写入。
- **建议补充方向**：明确指出 `snapuserd` 引入的内核/用户态通信开销，并对比 Android 13 引入 `io_uring` 后的优化效果。

## 六、P2 问题（建议改进）
- **[P2][知识盲区][FUSE Passthrough 的边界]**
- **原文位置**：Scoped Storage 章节。
- **问题描述**：原文提到 FUSE Passthrough 能显著提升性能，但未强调其对“元数据操作（Metadata Operations）”无效。
- **证据依据**：Android 12 GKI 源码中，`open`、`create`、`readdir` 等操作仍由 `MediaProvider` 处理，只有 `read`/`write` 被旁路。
- **建议**：补充说明对于“大量小文件扫描（如媒体库索引）”场景，FUSE Passthrough 提升有限，真正解决元数据瓶颈的是后续的 FUSE-BPF。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| F2FS 前台 GC 触发水位线 | 中 | 研究内核 `f2fs/segment.c` 中的 `has_not_enough_free_secs` 逻辑 |
| Metadata Encryption 的 Inline Crypto 映射 | 中 | 核实 `blk-crypto` 如何在不同厂商 SoC（如骁龙 vs 联发科）上落地 |

## 八、外部核验建议
- **搜索关键词**：`Android 13 snapuserd io_uring merge performance`
- **建议来源**：`LPC (Linux Plumbers Conference) 2022/2023` 相关的 Android Storage 演讲
- **源码入口**：`drivers/ufs/core/ufs-mcq.c` (核实 MCQ 队列分配算法)

## 九、可闭环输出

### 9.1 回炉问题单（建议执行）
- **[P1][VABC 性能损耗模型]**：补充 `snapuserd` 用户态转发导致的 I/O 延迟和 CPU 抖动分析。
- **[P2][FUSE 边界说明]**：明确 FUSE Passthrough 只加速数据流，不加速元数据流（如 `lookup`/`readdir`）。

### 9.2 可复用知识资产
- **源码路径**：`drivers/ufs/core/ufs-mcq.c` —— UFS 4.0 MCQ 核心驱动，展示了 SQ/CQ 映射逻辑。
- **关键结论**：Android 10+ 设备的 `system-as-root` 变更为“逻辑 SAR”，由 ramdisk 中的 `first-stage init` 通过 `dm-linear` 完成。
- **技术锚点**：`SQLITE_ENABLE_BATCH_ATOMIC_WRITE` 通过 `F2FS_IOC_COMMIT_ATOMIC_WRITE` 实现事务原子提交，显著降低 WAL 写入量。
- **版本摘要**：Android 11 强制 FBE + Metadata Encryption 组合，`dm-default-key` 负责绕过已加密的数据块以避免性能双重损失。

## 十、下一候选章节
- `src/part2-performance/ch07-smoothness/01-smoothness-metrics.md` （存储延迟对流畅性指标的直接映射关系）

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-23-00-6.1-external-review.md`
