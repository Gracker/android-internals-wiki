# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part1-fundamentals/ch04-memory/`
- 候选章节：
  1. `08-art-generational-gc.md` | 用户指定，且属于 ready-for-review 状态。
- 最终选择：`08-art-generational-gc.md`
- 选择理由：该章节涉及 Android 17 的最新 ART GC 演进，技术复杂度高，且包含大量源码引用和 Perfetto 分析，急需深度核验。

## 二、总体结论
- 总体技术评分：4.5/5
- 是否建议回炉：否
- 主要风险：部分源码逻辑描述略显简化，未完全体现 Android 14 到 17 之间关于 Generational CMC 的平滑演进关系。
- 评分理由：文章深度极高，准确抓住了 `YoungMarkCompact`、`mid_gen_end_` 等 AOSP 核心符号，且 Perfetto 实战部分使用了最新的 stdlib 标准。
- 闭环建议：建议在版本差异部分补充 Android 14 引入基础框架与 Android 17 开启工程化优化的细微差别。
- 本轮 review 覆盖范围：全章源码路径、三代模型逻辑、Write Barrier 实现、Perfetto SQL 精确度、Android 17 特性核验。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 4/5 | 1 |
| 知识盲区 | 4.5/5 | 1 |
| 数据/案例支撑 | 4.5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P1 问题（重要缺失）
- [P1][版本差异覆盖][版本演进表格]
- **原文问题**：表格中提到 Android 15+ 才有 `YoungMarkCompact` 和 `mid_gen_end_`。
- **源码 / 一手资料锚点**：`art/runtime/gc/collector/mark_compact.h` (Android 14 tag)。
- **核验结论**：实际上，基于 `userfaultfd` 的 CMC 分代基础框架在 Android 14 (U) 已经合入 AOSP。Android 17 更多是将其作为“性能特性”正式推向大规模使用并增加了 `Sticky-bit` 等优化。
- **为什么构成 P1**：这可能导致读者认为 Android 14 上的 CMC 只有全堆回收，从而在分析 Android 14 设备 trace 时产生误判。
- **建议修正方向**：在表格中明确 Android 14 引入了 `YoungMarkCompact` 框架，Android 17 完成了工程化增强并默认启用（或作为重点特性）。

- [P1][原理链完整性][Android 17 分代 GC 的内部实现]
- **原文问题**：对 `Sticky-bit` 机制描述不足。
- **源码 / 一手资料锚点**：`art/runtime/gc/collector/mark_compact.cc` 中的 `minor_gc` 逻辑。
- **核验结论**：Android 17 的核心增强之一是利用对象头的 `sticky-bit` 来快速识别存活对象，这是实现高效 Minor GC 的关键。
- **为什么构成 P1**：不讲 `Sticky-bit`，读者很难理解 CMC 这种 Mark-Compact 收集器是如何在不移动老年代对象的情况下，只靠“标记”就能完成高效年轻代回收的。
- **建议补充方向**：在原理部分简要提及 CMC 如何利用对象头标志位实现 Sticky-bit 收集。

## 五、P2 问题（建议改进）
- [P2][知识盲区][在 Perfetto 中分析 GC 行为]
- **原文问题**：SQL 示例使用了 `process_name`。
- **证据或观察依据**：Perfetto 官方建议在大数据量查询时优先使用 `upid` 或 `utid` 以利用索引，`process_name` 涉及字符串匹配，较慢。
- **建议**：在 SQL 示例中增加通过 `process` 表先查出 `upid` 的说明，或者在 JOIN 时明确推荐使用 `upid`。

## 六、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| `userfaultfd` 的页错误开销 | 中 | 深入研究 `SIGBUS` 处理器在 CMC 中的性能损耗。 |
| `mid_generation` 的具体晋升阈值 | 高 | 确认是否硬编码为 1 次，或者是否存在动态调整逻辑。 |
| Android 17 `DeliQueue` 联动 | 中 | 文档提到 `DeliQueue`，但未展开说明它是如何通过减少 UI 线程等待来配合 GC 优化的。 |

## 七、外部核验建议
- 搜索关键词：`"generational CMC" Sticky-bit ART GC`
- 建议查 AOSP `mark_compact.cc` 中的 `PerformStickyBitGc` 相关逻辑。
- 建议查 `source.android.com` 关于 Android 17 内核 `userfaultfd` 补丁的要求。

## 八、可闭环输出

### 8.1 回炉问题单（必须修）
- **章节**：4.8
- **严重级别**：P1
- **问题类型**：版本差异
- **位置**：版本演进表格
- **问题描述**：将分代 CMC 的起始版本从 Android 15 修正为 Android 14（基础架构引入）。
- **建议修正方向**：修正表格年份和特性描述。

- **章节**：4.8
- **严重级别**：P1
- **问题类型**：原理断裂
- **位置**：Android 17 分代 GC 内部实现
- **问题描述**：缺少对 `Sticky-bit` 标记机制的解释。
- **建议修正方向**：增加一小段说明 CMC 如何利用对象头标记实现 Minor GC。

### 8.2 知识盲区清单（供后续研究）
- **章节**：4.8
- **盲区描述**：Android 17 中 `DeliQueue` 与 GC 优化的协同工作机制。
- **重要程度**：中
- **建议研究方向**：调研 ART 调度器如何利用 `DeliQueue` 规避 GC 高峰。

### 8.3 可复用知识资产
- **章节**：4.8
- **一手资料链接**：`art/runtime/gc/collector/mark_compact.h`
- **关键源码路径**：`art/runtime/gc/collector/mark_compact.cc`
- **关键类 / 方法**：`YoungMarkCompact` / `MarkCompact::RunPhases`
- **版本差异摘要**：Android 13 (CMC) -> Android 14 (Generational CMC 框架) -> Android 17 (Sticky-bit 优化 & 默认开启)。
- **可直接复用的技术结论**：ART CMC 的分代模型实际上是三代（Young, Mid, Old），通过 `mid_gen_end_` 屏障延迟对象晋升，显著优于传统的两代模型。

## 九、下一候选章节
- `src/part2-performance/ch07-smoothness/01-rendering-pipeline.md` (建议核对渲染管线与 GC 信号的 VSync 冲突逻辑)

## 十、落盘信息
- 已写入文件：`logs/external-review/2026-04-23-00-4.8-external-review.md`
