# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch14-other-tools/04-dumpsys.md`
- 候选章节：
  1. `04-dumpsys.md` | 根据 Task 指示
- 最终选择：`04-dumpsys.md` (标记为 15.4)
- 选择理由：用户指令明确指定
- 排除的高频原因：N/A

## 二、总体结论
- 总体技术评分：3.5/5
- 是否建议回炉：是
- 主要风险：对 Android 15 中 `dumpsys SurfaceFlinger` 从 Legacy 到 FrontEnd 架构切换导致的输出格式破坏描述不够致命，未能指出自动化解析脚本失效的核心痛点。
- 评分理由：文章很好地总结了常用的 dumpsys 命令及性能场景。但对 Android 15 SurfaceFlinger 的架构变更描述过于表面。实际上，`RequestedLayerState` 的包裹直接导致依赖 `--list` 输出然后执行 `--latency` 的常规自动化脚本失效，且旧有的 `HWC/GLES` 标签被弃用，包含 1 个 P1 问题。
- 闭环建议：进入结构化修正队列，着重说明 Android 15 SurfaceFlinger dump 格式的破坏性变化和应对。
- 本轮 review 覆盖范围：activity, meminfo, gfxinfo, window, batterystats, SurfaceFlinger 等常用 dumpsys 子命令。
- 本轮未完成部分：无，已完成全章深度 review。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4/5 | 0 |
| 原理链完整性 | 4/5 | 0 |
| 版本差异覆盖 | 2/5 | 1 |
| 知识盲区 | 4/5 | 0 |
| 数据/案例支撑 | 4/5 | 0 |
| 交叉引用一致性 | 4.5/5 | 0 |

## 四、P0 问题（事实错误）
无。

## 五、P1 问题（重要缺失）
- [P1][版本差异覆盖][dumpsys SurfaceFlinger：Layer 信息与合成状态]
- 原文问题：虽然提到了 Android 15 引入 FrontEnd 架构和输出变化，但未能指出对实战工具链最致命的破坏：图层名称的包装和合成标签的弃用。
- 缺失内容：1. 在 Android 15 中，`--list` 输出的图层名被包裹在 `RequestedLayerState{...}` 等结构中。由于 `--latency` 要求传入完全匹配的图层名，这导致旧的 `grep` 脚本往往获取不到数据。2. 传统的 `HWC` 和 `GLES` 合成方式标签已被弃用，系统转向使用 `CompositionType`。
- 版本差异：Android 15 及以后。
- 为什么这是重要缺失：这是性能自动化测试（尤其是测帧率和流畅度）最容易踩的坑。无数团队测帧率的脚本在 Android 15 上跑出全 0 或获取不到图层，就是因为这层字符串包装。
- 建议补充方向：明确指出自动化脚本必须更新正则解析逻辑以提取被 `RequestedLayerState` 包裹的真实图层名，并指出 HWC/GLES 标签的弃用趋势。

## 六、P2 问题（建议改进）
- [P2][知识更新][dumpsys SurfaceFlinger]
- 原文问题：仍然以“GLES”作为非硬件合成的主要代名词。
- 证据或观察依据：Android 15 大力推动 ANGLE（OpenGL on Vulkan）作为默认驱动，RenderEngine 更多地在使用 Vulkan 后端。
- 问题描述：提到 GLES 合成时，未能指出底层正向 Vulkan/ANGLE 迁移的趋势。
- 建议：在解释 GLES 合成退回时，顺带提及 RenderEngine 的 Vulkan 化趋势。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|---------|-------------|
| Android 15 SurfaceFlinger FrontEnd 架构细节 | 中 | 深入研究 `RequestedLayerState` 引入的原因，以及它是如何优化主合成线程锁争用的。 |

## 八、外部核验建议
- 搜索关键词：`Android 15 dumpsys SurfaceFlinger RequestedLayerState`
  建议查：`cs.android.com` / SurfaceFlinger 源码更新记录
- 搜索关键词：`dumpsys SurfaceFlinger --latency format Android 15`
  建议查：相关 issue tracker 或自动化测试框架的适配代码

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- 章节：14.4 / 15.4 dumpsys 系列命令
- 严重级别：P1
- 问题类型：版本差异
- 位置：dumpsys SurfaceFlinger：Layer 信息与合成状态 -> Android 15+ 的输出变化
- 问题描述：未指出 Android 15 FrontEnd 架构造成的图层名称被 `RequestedLayerState{...}` 包裹，导致经典 `--list` + `--latency` 自动化测帧率脚本大规模失效的问题。
- 建议修正方向：明确警示这一破坏性变更，指导读者如何更新正则匹配逻辑来获取 `--latency` 所需的正确图层参数，同时提及传统 HWC/GLES 标记的弃用。

### 9.2 知识盲区清单（供后续研究）
- 章节：14.4 / 15.4 dumpsys 系列命令
- 盲区描述：SurfaceFlinger FrontEnd 架构对合成管线具体性能瓶颈的转移。
- 重要程度：低
- 建议研究方向：研究 FrontEnd 将原有的状态计算从合成主线程分离后，对总体渲染延迟的影响。

### 9.3 一般建议清单（非阻断）
- 章节：14.4 / 15.4 dumpsys 系列命令
- 问题类型：表述更新
- 位置：dumpsys SurfaceFlinger
- 问题描述：提到 GLES 合成退回时，未体现最新的 Vulkan 趋势。
- 建议：可简要提及 RenderEngine 默认驱动正在转向 ANGLE/Vulkan，Dump 中会反映这一变化。

### 9.4 可复用知识资产（高价值新增知识）
- 章节：14.4 / 15.4 dumpsys 系列命令
- 一手资料链接或来源类型：AOSP Source / Android 15 SurfaceFlinger
- 关键逻辑变化：Android 15 中，获取帧延迟数据 `dumpsys SurfaceFlinger --latency <layer_name>` 严重依赖完整的层名匹配。由于 `--list` 输出如今带有 `RequestedLayerState{...}` 前缀及可能的 ID 后缀，旧脚本必须适配这种拼接格式，否则会直接抓取失败（返回无意义数据）。
- 为什么这条知识值得保留：它是目前 Android 15 升级中最影响性能测试基础设施的具体技术细节。

## 十、下一候选章节
- 暂无，已完成指定任务。

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-22-15.4-external-review.md`