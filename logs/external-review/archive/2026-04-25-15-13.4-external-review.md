# AIW 自动 Review 任务报告 (13.4)

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch13-perfetto/`
- 候选章节：
  1. 13.4 | 命令行打开超大 Trace
- 最终选择：13.4
- 选择理由：本节是处理生产环境 Trace 的核心方法论，由于涉及 SQL 和 Python API，其语法准确性直接影响自动化工具的可靠性。

## 二、总体结论
- 总体技术评分：4.3/5
- 是否建议回炉：否
- 主要风险：参数 `-Q` 的表述可能存在歧义。在 `trace_processor` 的某些版本中，`-q` 指向文件，`-query` 指向语句。应确保文中推荐的 CLI 组合在标准分发版中均能跑通。
- 评分理由：对 `trace_processor` 的内存优势和工作模式（HTTPD）讲解清晰，SQL JOIN 路径模式（slice -> track -> thread -> process）总结得极好，是读者的“避坑指南”。
- 闭环建议：微调 CLI 参数说明，补充 `as_pandas_dataframe()` 在大数据集下的内存风险提示。
- 本轮 review 覆盖范围：trace_processor CLI、SQL 基础表结构、Python API、traceconv 转换、Pipeline 实践。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4/5 | 1 |
| 原理链完整性 | 5/5 | 0 |
| 版本差异覆盖 | 4.5/5 | 0 |
| 知识盲区 | 4.5/5 | 1 |
| 数据/案例支撑 | 5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
暂无。

## 五、P1 问题（重要缺失）
- [P1][源码准确性][trace_processor CLI 参数]
- 原文问题：文中提到 `trace_processor -Q "select 1 as a, 2 as b" /dev/null`。
- 源码 / 一手资料锚点：`external/perfetto/src/trace_processor/main.cc`。标准参数通常是 `--query` 或 `-q` (针对文件)。
- 为什么这是重要缺失：如果读者复制了带 `-Q` 的命令但环境不支持，会直接报错，降低文档可信度。
- 建议补充方向：统一推荐使用全名 `--query` 或明确标注版本兼容性。

## 六、P2 问题（建议改进）
- [P2][知识盲区][Python API - as_pandas_dataframe]
- 原文问题：推荐使用 `as_pandas_dataframe()`。
- 证据或观察依据：在处理 GB 级 Trace 且查询结果集巨大时，一次性转为 DataFrame 会导致 Python 进程 OOM。
- 问题描述：未提及大数据量下的分批处理或迭代器模式的优势。
- 建议：增加一句话提醒：如果查询结果达到百万行级，应优先使用迭代器模式处理。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| Bigtrace 分布式架构 | 中 | 针对数千个 Trace 的分布式 SQL 引擎实现 |
| PerfettoSQL 预编译宏 | 低 | 如何在 `trace_processor` 中预加载自定义 SQL 函数库 |

## 八、外部核验建议
- 搜索关键词：`perfetto trace_processor cli options query`
- 建议查：`perfetto.dev` 官方最新 CLI 帮助文档。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- 章节：13.4
- 严重级别：P1
- 问题类型：CLI 参数描述不准确
- 位置：用 trace_processor 批量跑 SQL 脚本章节
- 问题描述：`-Q` 参数在标准 `trace_processor_shell` 中可能不被支持。
- 建议修正方向：改为 `-q` 并说明它是读取文件，或使用 `--query` 直接执行语句。

### 9.4 可复用知识资产
- 章节：13.4
- 核心模式：`slice` → `thread_track` → `thread` → `process`。
- 为什么这条知识值得保留：这是 PerfettoSQL 80% 查询的通用骨架，极大地降低了读者的上手门槛。

## 十、下一候选章节
- 13.5

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-15-13.4-external-review.md`
