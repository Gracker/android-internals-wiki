# AIW 自动 Review 任务报告 - 11-webview-performance.md

## 一、目标发现结果
- 扫描范围：`src/part2-performance/ch07-smoothness/`
- 最终选择：`11-webview-performance.md`
- 选择理由：用户明确指定。

## 二、总体结论
- 总体技术评分：4.5/5
- 是否建议回炉：否（建议进行 P1 级精度修正）
- 主要风险：对 GPU Service 的运行位置描述准确，但需强化其“一旦崩溃即导致 App 退出”的风险点。
- 评分理由：文章深度极高，特别是对“双层渲染管线”和“in-process vs out-of-process”的边界划分非常清晰。
- 本轮 review 覆盖范围：WebView 架构、进程模型、JS Bridge 线程模型、内存泄漏分析、滚动优化。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5.0/5 | 0 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 4.5/5 | 1 |
| 知识盲区 | 4.5/5 | 0 |
| 数据/案例支撑 | 4.0/5 | 0 |
| 交叉引用一致性 | 5.0/5 | 0 |

## 四、P1 问题（重要缺失）
- [P1][原理链完整性][onRenderProcessGone 的深度分析]
- **原文问题**：提到了 `onRenderProcessGone`，但未深入分析其对 Renderer 崩溃恢复的“非全量性”。
- **源码锚点**：`frameworks/base/core/java/android/webkit/WebViewClient.java`。
- **缺失内容**：需要强调，当多个 WebView 共享同一个 Renderer 进程时，一个页面的崩溃会导致所有关联 WebView 同步收到回调。
- **建议补充方向**：补充一个“正确恢复流程”的代码示例，强调必须 `removeView` 并重新 `new WebView`，不能直接在旧实例上 `loadUrl`。

## 五、P2 问题（建议改进）
- [P2][版本差异][Android 11 内存优化]
- **原文问题**：提到 Android 11+ 全量开启多进程渲染。
- **事实核验**：Android 11 之所以能覆盖低内存设备，是因为 Chromium 团队实现了“内存敏感的沙盒（Memory-aware sandboxing）”，在内存极低时会动态调整子进程优先级。
- **建议**：补充这一技术背景，解释为什么 Android 11 能在“曾经不能跑多进程的 32 位低端机”上跑起来。

## 六、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| GPU 驱动崩溃频率 | 中 | 统计不同 OEM厂商（如 Adreno 驱动）在 WebView 场景下的崩溃特征。 |
| V8 内存快照分析 | 高 | 宿主 App 如何通过 CDP (Chrome DevTools Protocol) 自动化获取 Renderer 进程的 V8 Heap Snapshot。 |

## 七、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：7.11
- **级别**：P1
- **问题描述**：对 Renderer 进程崩溃后的业务侧恢复逻辑描述不完整。
- **修正建议**：增加 `onRenderProcessGone` 的规范处理流程（移除、销毁、重建）。

### 9.2 可复用知识资产
- **技术结论**：**WebView 的 GPU Service 始终为 In-Process（宿主进程线程）**。
- **风险点**：GPU 驱动崩溃会直接导致宿主进程（Host App）死亡，且无法通过 `onRenderProcessGone` 挽救。
- **版本分界线**：Android 11 是多进程渲染全量覆盖的分水岭；在此之前 32 位低内存设备常回退到单进程。

## 八、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-15-ch07.11-external-review.md`
