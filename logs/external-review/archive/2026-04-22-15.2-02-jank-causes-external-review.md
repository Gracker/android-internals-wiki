# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part2-performance/ch07-smoothness/02-jank-causes.md`
- 候选章节：
  1. `02-jank-causes.md` | 明确指定 review
- 最终选择：`02-jank-causes.md`
- 选择理由：用户指令指定
- 排除的高频原因：N/A

## 二、总体结论
- 总体技术评分：4.5/5
- 是否建议回炉：否
- 主要风险：Binder 阻塞中的 BufferQueue 锁竞争描述较弱，可补充 `BufferQueueCore` 的锁竞争；另外 RenderThread 与主线程的 `syncFrameState` 阻塞机制可以稍微强化。
- 评分理由：文章对卡顿原因的分类非常全面，不仅覆盖了 App 层的 MainThread、RenderThread，还深入到了 SurfaceFlinger、CPU 调度、内存 GC 和温控。对 Android 14 lmkd TrimMemory 的分析非常硬核，证据确凿。整体达到 P2 级补充即可。
- 闭环建议：根据建议补充 P2 细节。
- 本轮 review 覆盖范围：全部锚点和扩展分析，包括 MainThread/RenderThread 瓶颈，系统级调度，Binder 卡顿。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4.5/5 | 1 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 4.5/5 | 0 |
| 知识盲区 | 4.5/5 | 0 |
| 数据/案例支撑 | 4.5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
无。

## 五、P1 问题（重要缺失）
无。

## 六、P2 问题（建议改进）
- [P2][源码准确性][Binder 调用导致的主线程阻塞]
- 原文问题：提到了 SurfaceFlinger 的 Binder 瓶颈（dequeueBuffer, queueBuffer）。
- 证据或观察依据：在 AOSP `frameworks/native/libs/gui/BufferQueueCore.cpp` 中，`dequeueBuffer` 和 `queueBuffer` 高频涉及 `mMutex` 锁。如果 App 的 RenderThread 频繁调用，或者多个 App 抢占，容易在 `BufferQueueCore` 这把锁上发生 contention。
- 问题描述：可以进一步点出锁的具体位置，增加技术深度。
- 建议：明确提及 `BufferQueueCore` 的内部 `mMutex` 锁竞争。

- [P2][原理链完整性][误区四：「RenderThread 在独立线程，所以不会影响主线程」]
- 原文问题：提到了 `syncAndDrawFrame` 阶段主线程需要将 DisplayList 同步给 RenderThread。
- 证据或观察依据：在 AOSP `frameworks/base/core/java/android/view/ThreadedRenderer.java` 中，主线程调用 `nSyncAndDrawFrame`，这是一个 blocking JNI 调用，直到 RenderThread 完成 `syncFrameState`。
- 问题描述：解释非常正确，但可以给出对应的 Perfetto slice 名称 `syncFrameState`，这在实战中非常常见。
- 建议：补充提到在 Perfetto trace 中，主线程会因为等待 RenderThread 表现为一个长 `syncFrameState` 的 block/sleeping 状态。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|----------|--------------|
| Hardware Composer 厂商差异 | 中 | 分析高通与联发科 HWC 策略及 dumpsys SurfaceFlinger 中 DEVICE / CLIENT 分配规则的具体厂商实现限制 |

## 八、外部核验建议
- 搜索关键词 `BufferQueueCore::mMutex`：建议查 AOSP 中关于该锁的争用情况和优化历史。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
无

### 9.2 知识盲区清单（供后续研究）
- 章节：7.2 卡顿原因体系
- 盲区描述：BufferQueue 内部的锁竞争机制
- 重要程度：中
- 建议研究方向：AOSP `BufferQueueProducer` / `BufferQueueConsumer` 源码
- 可能关联章节：2.6 SurfaceFlinger 与合成

### 9.3 一般建议清单（非阻断）
- 章节：7.2 卡顿原因体系
- 问题类型：源码准确性
- 位置：Binder 调用导致的主线程阻塞
- 问题描述：缺少 BufferQueue 具体底层锁的指出。
- 建议：补充 `BufferQueueCore` 的 `mMutex`。

### 9.4 可复用知识资产（高价值新增知识）
- 章节：7.2 卡顿原因体系
- 一手资料链接：`frameworks/native/libs/gui/BufferQueueCore.cpp`
- 关键源码路径：`BufferQueueCore.cpp`
- 关键类 / 方法 / 字段：`BufferQueueCore::mMutex`
- 可直接复用的技术结论：`dequeueBuffer` 和 `queueBuffer` 的耗时波动常常由于 `BufferQueueCore` 级别的 Mutex 锁竞争引起。

## 十、下一候选章节
- 建议继续 review `03-jank-methodology.md`

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-22-15.2-02-jank-causes-external-review.md`