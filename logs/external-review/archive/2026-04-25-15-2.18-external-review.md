我将对 `2.18 Adaptive Refresh Rate 与动态帧率控制` 章节进行深度技术核验与审计。

# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part2-performance/`
- 候选章节：
  1. `2.18 Adaptive Refresh Rate` | 处于 `ready-to-publish` 状态，涉及 Android 15/16 最新特性，技术风险点多。
- 最终选择：`2.18 Adaptive Refresh Rate 与动态帧率控制`
- 选择理由：该章节涉及 Android 15-QPR1 和 Android 16 的前沿 API（如 ARR），且直接引用了 AOSP `android-16.0.0_r1` 源码，技术准确性要求极高。
- 排除的高频原因：其他章节状态多为 `draft` 或已完成 Task 9。

## 二、总体结论
- 总体技术评分：4.2/5
- 是否建议回炉：否（建议执行结构化修正）
- 主要风险：部分 Android 16 新增 API 的版本界限描述略显模糊；针对 `VsyncModulator` 的 offset 逻辑描述需要更精准的源码锚点。
- 评分理由：文章对 ARR 的核心 API、版本分界以及 SurfaceFlinger 的调度逻辑有很深的理解，源码路径引用基本准确，但对 Android 16 特有 API 的正式发布状态需要进一步明确。
- 闭环建议：修正 P1 级别的 API 版本标记，补充 `VsyncModulator` 关键方法的源码引用。
- 本轮 review 覆盖范围：ARR 核心 API、AOSP 调度链、Perfetto 分析方法。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 4.5/5 | 1 |
| 原理链完整性 | 4.5/5 | 1 |
| 版本差异覆盖 | 4.0/5 | 1 |
| 知识盲区 | 4.0/5 | 0 |
| 数据/案例支撑 | 4.5/5 | 0 |
| 交叉引用一致性 | 5.0/5 | 0 |

## 四、P0 问题（事实错误）
暂未发现明显的 P0 级事实错误。

## 五、P1 问题（重要缺失）
- [P1][版本差异覆盖][Display 查询 API]
- **原文问题**：文中将 `Display.hasArrSupport()` 等 API 标记为 Android 16，但在“适用范围”中又提到 ARR 主体是 Android 15-QPR1。
- **源码 / 一手资料锚点**：`frameworks/base/core/java/android/view/Display.java` (AOSP `main` / `android-16.0.0_r1`)。
- **关键代码逻辑**：`hasArrSupport()` 和 `getSuggestedFrameRate()` 是在 Android 16 (API 36) 才正式公开的 SDK API。Android 15-QPR1 更多是系统底层对 ARR 的支持。
- **缺失内容**：需要明确区分“系统支持 ARR（15-QPR1）”与“App 公开查询 API（16）”的时间差。
- **建议补充方向**：明确标注这组 API 是 API Level 36 (Android 16) 引入的。

- [P1][源码准确性][VsyncModulator 逻辑]
- **原文位置**：系统里谁在做什么 -> `VsyncModulator`
- **原文问题**：提到 `VsyncModulator` 负责调整 offset，但未给出关键方法名，导致读者难以在源码中定位。
- **源码锚点**：`frameworks/native/services/surfaceflinger/Scheduler/VsyncModulator.cpp`
- **关键代码逻辑**：核心逻辑在 `VsyncModulator::updateVsyncConfig()`，它根据 `Transaction`、`RefreshRate` 变化来决定使用哪组 `Offset`（如 `Early`、`EarlyGl`、`Late`）。
- **建议补充方向**：提及 `updateVsyncConfig` 或 `setVsyncConfigSet` 相关方法名。

## 六、P2 问题（建议改进）
- [P2][数据/案例支撑][Perfetto SQL]
- **原文问题**：SQL 示例使用了 `actual_frame_timeline_slice`。
- **建议**：建议在文中明确说明，如果是在 Android 14+ 使用内置的 Perfetto SQL 引擎，可以直接调用 `android_jank_cuj` 标准库函数，这样比手写 SQL 查 raw table 更高效。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| HAL 层的 ARR 实现 | 中 | 关注 `IComposerClient` 的 `setLayerGenericMetadata` 及其对 ARR 的传参。 |
| 对变帧率视频的支持 | 低 | `Surface.setFrameRate` 的 `FRAME_RATE_COMPATIBILITY_FIXED_SOURCE` 在 ARR 模式下的具体降频策略。 |

## 八、外部核验建议
- 搜索关键词：`Android 16 Display.hasArrSupport API level`
- 建议查：`developer.android.com/reference/android/view/Display` (API 36 预览文档)
- 搜索关键词：`SurfaceFlinger chooseRefreshRateForContent android-16`
- 建议查：AOSP 源码 `frameworks/native/services/surfaceflinger/Scheduler/RefreshRateSelector.cpp`

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：2.18
- **严重级别**：P1
- **问题类型**：版本差异/源码缺失
- **位置**：Display 查询 API 节、VsyncModulator 描述处
- **问题描述**：1. 未明确 API 36 才是 `hasArrSupport` 的起点；2. `VsyncModulator` 缺乏关键方法锚点。
- **建议修正方向**：在代码块旁标注 `@since API 36`；补充 `VsyncModulator::updateVsyncConfig` 方法引用。

### 9.2 知识盲区清单（供后续研究）
- **章节**：2.18
- **盲区描述**：Android 15 HAL 层对 ARR 的 `ISurfaceComposer` 接口变动。
- **建议研究方向**：对比 `android-14` 与 `android-15` 的 `ComposerHal.h`。

### 9.3 一般建议清单（非阻断）
- **章节**：2.18
- **问题类型**：建议改进
- **位置**：Perfetto SQL 部分
- **问题描述**：建议提及 Perfetto 标准库（Standard Library）。

### 9.4 可复用知识资产
- **章节**：2.18
- **关键源码路径**：`frameworks/base/services/core/java/com/android/server/display/mode/DisplayModeDirector.java`
- **关键方法**：`getDesiredDisplayModeSpecs()` —— 这是理解 Android 如何把多方需求（用户、App、系统）折叠成最终刷新率范围的关键入口。
- **版本差异摘要**：Android 16 引入了声明式的 `Display` 能力查询接口，标志着 ARR 从“系统黑盒行为”走向“App 可感知/可适配接口”。

## 十、下一候选章节
- `2.16 渲染流水线中的 VSync 机制`（与 ARR 强相关，可形成闭环）。

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-26-02-2.18-external-review.md`