# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part2-performance/ch18-rendering-pipelines/` 及 `openclaw-tasks`。
- 候选章节：
  1. `11-angle-gles-vulkan.md` | `task9_state: pending` 且明确涉及 AOSP 源码层和 Perfetto 落地，符合深度 review 要求。
- 最终选择：`src/part2-performance/ch18-rendering-pipelines/11-angle-gles-vulkan.md`
- 选择理由：此章节属于图形渲染链路的重要部分，且包含前期 AI 留下的“源码调研”外挂附录，正文与附录存在结构和事实的割裂，急需将其整合并核对 Android 14/15 的最新源码。
- 排除的高频原因：其他章节状态尚未 ready，或无需本轮高优 review。

## 二、总体结论
- 总体技术评分：3.0/5
- 是否建议回炉：是
- 主要风险：正文与附录源码调研内容打架（出现不存在的 `queryAngleChoice` 方法）；缺失 Android 15 将 ANGLE 设为默认驱动的重大版本演进；缺少对 ANGLE 自有 Trace Marker 的说明。
- 评分理由：存在 1 个 P0（AOSP 方法名伪造/过时）和多个 P1（版本差异遗漏、Trace 观察点不足、章节结构割裂），按规范评分不能高于 3.5/5。
- 闭环建议：进入回炉修正队列，合并附录与正文，修正错误 API，补充 Android 15 的系统级策略。
- 本轮 review 覆盖范围：全文核心原理、Driver Selection 源码机制、Perfetto Trace 观察点、版本差异。
- 本轮未完成部分：无，已针对本章知识点全量 review。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 2.5/5 | 2 |
| 原理链完整性 | 4.0/5 | 1 |
| 版本差异覆盖 | 3.0/5 | 1 |
| 知识盲区 | 3.5/5 | 1 |
| 数据/案例支撑 | 3.5/5 | 1 |
| 交叉引用一致性 | 2.0/5 | 1 |

## 四、P0 问题（事实错误）

- [P0][源码准确性][启用条件与运行时选路]
- **原文问题**：“AOSP 里，`GraphicsEnvironment.queryAngleChoice()` 会先决定当前进程该走哪条 GLES driver 路径”
- **源码 / 一手资料锚点**：`frameworks/base/core/java/android/os/GraphicsEnvironment.java`
- **关键代码逻辑**：AOSP 中 `GraphicsEnvironment` 并没有 `queryAngleChoice()` 这个方法。决策的真实入口是 `setupAngle()`，其内部调用 `shouldUseAngleInternal()` 来决定是否启用 ANGLE。
- **运行原理说明**：在进程启动（如 `ActivityThread.attach()`）时，`GraphicsEnvironment.getInstance().setup()` 会被调用，进而通过 `setupAngle()` 评估全局 Settings、按包名的 Settings 和 Game Mode 等条件。
- **为什么错**：无中生有了一个并不存在的 `queryAngleChoice()` 方法，且该错误表述与文末附录的真实源码调用链（`setupAngle -> shouldUseAngleInternal`）直接矛盾。
- **建议修正方向**：删除正文中关于 `queryAngleChoice()` 的表述，将文末“AIW-源码调研”附录中正确的调用链（`setup() -> setupAngle() -> shouldUseAngleInternal()`）融合进正文的“启用条件与运行时选路”小节。

## 五、P1 问题（重要缺失）

- [P1][交叉引用一致性][全文结构]
- **原文问题**：文章末尾直接拼接了一块 `<!-- AIW-源码调研-2026-04-18 -->` 附录，并未融入正文。
- **缺失内容**：附录中的高价值内容（如 Game Mode 优先级、`a4a_rules.json` 机制等）没有被结构化地写入正文，导致读者阅读时正文是一个版本，附录又推翻补充了另一个版本。
- **为什么这是重要缺失**：AIW 的目标是一篇完整的技术文章，不允许把未消化的 raw notes 直接贴在文末。
- **建议补充方向**：必须将附录内容拆解，按逻辑合并到“核心架构”、“启用条件与运行时选路”和“调试可见性”中，然后删除附录块。

- [P1][版本差异覆盖][为什么需要 ANGLE]
- **原文问题**：“Android 15 之后，Google 继续扩大 ANGLE 的覆盖范围”
- **缺失内容**：表述过于保守。实际上，Android 15 已宣布 ANGLE 作为受支持设备的默认 GLES 驱动，并在 Android 16 (2025/2026) 计划逐步废弃原生 GLES 驱动。
- **一手资料锚点**：Android 15 Release Notes (Developer Preview) / ANGLE on Android 官方博文。
- **为什么这是重要缺失**：这是图形子系统近几年最大的架构变动，直接决定了开发者未来的测试基准。只说“扩大覆盖范围”没有点出“成为默认”的里程碑。
- **建议补充方向**：明确指出 Android 15 将 ANGLE 提升为默认 GLES 驱动的战略变化。

- [P1][知识盲区][在 Perfetto 中识别 ANGLE]
- **原文问题**：只提到了看 `vkQueueSubmit` 或 `vkCmdDraw`，以及 `s.name GLOB 'vk*'`。
- **缺失内容**：在 Perfetto 中，如果设备开启了特定的 debug 属性或 ANGLE build 启用了 trace，ANGLE 会抛出自己的 Trace Marker（如 track name 包含 `ANGLE`，或者在 Vulkan API 之前有 `glDraw*` 的 ANGLE 层翻译耗时 slice）。
- **运行原理说明**：ANGLE 内部集成了 perfetto track events。仅靠查 `vk*` 只能说明用了 Vulkan，不能直观区分是 SkiaVK 还是 ANGLE，必须强调通过 `debug.angle.markers` 或查看 GL 层面的耗时。
- **建议补充方向**：补充说明 ANGLE 自身在 Trace 中会输出的 marker，或如何区分 SkiaVK 和 ANGLE 的 Vulkan 提交。

## 六、P2 问题（建议改进）

- [P2][源码准确性][A4A Rules JSON]
- **原文问题**：附录中展示了一个 `a4a_rules.json` 的伪代码或简化版，其描述看起来不够严谨。
- **证据或观察依据**：实际的 AOSP 平台级 rules.json 通常用于控制哪些 GPU/驱动组合黑名单/白名单。
- **建议**：在将其融合到正文时，只需说明“可通过平台的 rules 配置进行设备和驱动的黑白名单控制”，避免使用过于具体的、可能是 AI 脑补的硬编码 JSON 例子。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|---------|-------------|
| ANGLE 的 Shader Cache 机制 | 中 | 原生 GLES 有 driver 层面的 blob cache，ANGLE 的 SPIR-V 编译产物是如何与 Vulkan Pipeline Cache 配合实现持久化的？App 层是否需要调整？ |

## 八、外部核验建议
- **搜索关键词**："Android 15" ANGLE default driver announcement
- **建议查来源**：Android Developer Blog / release notes，确认 Android 15+ 中 ANGLE 默认化的准确口径和生效范围。
- **搜索关键词**：`debug.angle.markers` perfetto trace
- **建议查来源**：AOSP `external/angle` 源码或 Chromium ANGLE doc，确认在 Perfetto 中开启 ANGLE 内部 trace 埋点的确切属性名。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：11-angle-gles-vulkan.md
- **严重级别**：P0
- **问题类型**：源码错误 & 结构割裂
- **位置**：“启用条件与运行时选路” & 文末附录
- **问题描述**：正文中伪造了 `queryAngleChoice()` 方法，且文末挂载了生肉附录，内容发生冲突。
- **建议修正方向**：删除正文错误的 API，将附录中正确的 `GraphicsEnvironment.getInstance().setup()` -> `setupAngle()` -> `shouldUseAngleInternal()` 决策链以及 Game Mode 影响融入正文。
- **建议补充的验证来源**：`frameworks/base/core/java/android/os/GraphicsEnvironment.java`

- **章节**：11-angle-gles-vulkan.md
- **严重级别**：P1
- **问题类型**：版本差异
- **位置**：“为什么需要 ANGLE”
- **问题描述**：对 Android 15 的版本演进表述过弱，未指明 ANGLE 成为默认驱动。
- **建议修正方向**：明确补充 Android 15+ 默认使用 ANGLE 且后续逐步废弃原生 GLES 驱动的平台演进背景。

### 9.2 知识盲区清单（供后续研究）
- **章节**：11-angle-gles-vulkan.md
- **盲区描述**：ANGLE 翻译层的 Shader/Pipeline Cache 是如何落盘并跨进程/重启复用的，与原生 GLES shader cache 的差异。
- **重要程度**：中
- **建议研究方向**：AOSP `external/angle` 中的 cache 管理逻辑及 Vulkan Pipeline Cache 交互。
- **可能关联章节**：第 18 章性能与渲染链路。

### 9.3 一般建议清单（非阻断）
- **章节**：11-angle-gles-vulkan.md
- **问题类型**：追踪调试
- **位置**：“在 Perfetto 中识别 ANGLE”
- **问题描述**：缺乏对 ANGLE 自有 trace marker 开启方式的说明，仅仅通过 `vk*` 过滤容易和 Skia Vulkan 混淆。
- **建议**：补充开启 ANGLE 内部 perfetto events 的方式，或说明如何通过调用栈特征区分 Skia 和 ANGLE 产生的 Vulkan 绘制。

### 9.4 可复用知识资产（高价值新增知识）
- **章节**：11-angle-gles-vulkan.md
- **来源类型**：AOSP 源码 `GraphicsEnvironment.java`
- **关键源码路径**：`frameworks/base/core/java/android/os/GraphicsEnvironment.java`
- **可直接复用的技术结论**：ANGLE 的启用是一个分层决策树（全局设置 > 包级别 opt-in/out > Game Mode > 平台 rules）。特别是 Game Mode (Android 12+) 可以在不修改开发者选项的情况下，针对游戏自动启用 ANGLE。
- **为什么这条知识值得保留**：开发者在排查“为什么我的 App 莫名其妙跑在了 ANGLE 上”时，很容易漏掉 Game Mode 这一层介入因素。

## 十、下一候选章节
- 建议继续 review 本章前后的 `18.8 OpenGL ES 渲染链路` 或 `18.9 Vulkan 原生渲染链路` 以确保上下文一致性。

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-19-14-18.11-external-review.md`
