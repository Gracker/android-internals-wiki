# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part3-tools/ch13-perfetto/`
- 候选章节：
  1. `04-large-traces.md` | 本章专注于大文件处理与命令行工具，是 Perfetto 高级分析的核心。
- 最终选择：`src/part3-tools/ch13-perfetto/04-large-traces.md`
- 选择理由：该章节涉及大量命令行参数、SQL 性能优化以及 Python 自动化接口，技术细节密集，需要严谨核验参数准确性及底层机制。
- 排除的高频原因：其他章节状态多为 `reviewed` 或 `draft`。

## 二、总体结论
- 总体技术评分：4.5/5
- 是否建议回炉：否
- 主要风险：内容高度准确，唯一的微小缺失是对 Perfetto 标准库（Stdlib）现代用法的提及。
- 评分理由：文章对 `trace_processor` 的命令行参数（包括短标志 `-q`, `-Q`, `-e`, `-W`）掌握极其精准，这些参数在官方文档中往往隐藏较深。技术原理（如 `utid/upid` 的必要性、`EXTRACT_ARG` 的子查询实现）解析透彻。
- 闭环建议：无需回炉。建议在“Perfetto SQL 查询基础”部分补充关于 `INCLUDE PERFETTO MODULE` (Stdlib) 的简要说明，以符合现代 Perfetto SQL 开发规范。
- 本轮 review 覆盖范围：整章内容，包括：浏览器内存瓶颈、CLI 工具下载与使用、常用 SQL 表及 JOIN 路径、Python API 自动化、格式转换工具。
- 本轮未完成部分：无。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 5/5 | 0 |
| 原理链完整性 | 5/5 | 0 |
| 版本差异覆盖 | 4/5 | 1 |
| 知识盲区 | 4/5 | 1 |
| 数据/案例支撑 | 5/5 | 0 |
| 交叉引用一致性 | 5/5 | 0 |

## 四、P0 问题（事实错误）
*暂无发现事实性错误。*

## 五、P1 问题（重要缺失）
*暂无发现导致理解偏差的重要缺失。*

## 六、P2 问题（建议改进）
- [P2][知识盲区][Perfetto SQL 查询基础]
- **原文问题**：仅提到了 `CREATE PERFETTO VIEW` 等扩展语法，未提及 Perfetto 标准库（Standard Library / Stdlib）。
- **证据或观察依据**：自 Perfetto v40+ 以来，官方大力推行 Stdlib，大量高级分析函数和预置视图（如 `android.slices`）需通过 `INCLUDE PERFETTO MODULE` 引入。
- **问题描述**：不提及 Stdlib 会导致读者在阅读官方 Metric 源码或进阶 SQL 时感到困惑。
- **建议**：补充一小段关于 `INCLUDE PERFETTO MODULE` 的介绍，说明如何利用官方预置的分析模块。

- [P2][版本差异覆盖][trace_processor 的高级参数]
- **原文问题**：未提及 `--no-ftrace-raw` 标志。
- **证据或观察依据**：在分析包含海量 ftrace 事件的系统级 Trace 时，`raw` 表会占用极大内存。
- **问题描述**：这是分析“超大 Trace”时的关键内存优化手段。
- **建议**：在参数推荐部分增加 `--no-ftrace-raw`，解释其在极端大文件场景下的救命作用。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|----------|--------------|
| Perfetto Stdlib | 中 | 学习如何使用 `INCLUDE PERFETTO MODULE` 及 `android.*` 模块。 |
| Span Join 操作符 | 低 | 了解处理重叠时间段（如 CPU 状态与线程状态交集）的高级 SQL 语法。 |

## 八、外部核验建议
- 搜索关键词：`Perfetto standard library include module`
- 建议查：`perfetto.dev/docs/analysis/stdlib` 了解最新预置模块。
- 搜索关键词：`trace_processor --no-ftrace-raw memory impact`
- 建议查：AOSP 源码中关于 `ftrace` 解析的内存分配逻辑。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
*无（本章质量较高，建议直接合入并做微调）。*

### 9.2 知识盲区清单（供后续研究）
- 章节：13.4
- 盲区描述：读者可能不了解如何利用官方已有的 SQL 模块，重复造轮子（如手动解析 Binder 调用链）。
- 重要程度：中
- 建议研究方向：Perfetto Stdlib 的 `android` 子目录下的视图定义。

### 9.3 一般建议清单（非阻断）
- 章节：13.4
- 问题类型：功能补强
- 位置：Python 自动化分析
- 问题描述：可以提到 `trace_processor` 支持直接加载压缩的 `.gz` 或 `.zip` 文件，无需手动解压。
- 建议：在加载 Trace 部分增加一行提示。

### 9.4 可复用知识资产（高价值新增知识）
- 章节：13.4
- 一手资料链接：`https://perfetto.dev/docs/analysis/trace-processor`
- 关键源码路径：`external/perfetto/src/trace_processor/main.cc`
- 关键类 / 方法 / 字段：`getopt_long` 中的短标志定义：`-q (query-file)`, `-Q (query-string)`, `-e (export)`, `-W (wide)`.
- 关键调用链 / 代码逻辑摘要：`EXTRACT_ARG` 在 SQL 解析阶段被 desugar 成相关子查询（correlated subquery），因此在 `O(N)` 量级下性能较差。
- 版本差异摘要：`traceconv` 现已整合了 `trace_to_text` 的所有功能，且支持 `heapprofd` 的 profile 提取。
- Trace / Perfetto 观察点：`utid` 与 `upid` 是 Trace 处理器在 Ingestion 阶段根据 `thread_state` 和 `process_tree` 生成的自增 ID，能有效规避 PID 复用问题。
- 可直接复用的技术结论：在大 Trace 分析中，`JOIN` 优于 `EXTRACT_ARG`；`--httpd` 模式是浏览器查看 2GB+ 文件的唯一稳定方案。

## 十、下一候选章节
- `src/part3-tools/ch13-perfetto/05-custom-data-sources.md`

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-25-13-ch13.04-external-review.md`
