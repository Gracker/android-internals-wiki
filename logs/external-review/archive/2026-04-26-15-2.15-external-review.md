# AIW 自动 Review 任务报告

## 一、目标发现结果
- 扫描范围：`src/part1-fundamentals/ch02-rendering/`
- 候选章节：
  1. 2.15 DMA-BUF、Gralloc 与跨进程图形内存共享 | 渲染管线物理层核心，涉及 Android 12-16 架构演进。
- 最终选择：2.15 DMA-BUF、Gralloc 与跨进程图形内存共享
- 选择理由：该章节被标记为 `pipeline_stage: ready-to-publish`，但初步扫描发现文中存在严重的占位符残留（ hallucination 痕迹），且对 Android 16 版本的 Gralloc AIDL 演进描述存在准确性风险。
- 排除的高频原因：2.13 已于昨日完成 review；2.16 依赖 2.15 的结论，需先行审定 2.15。

## 二、总体结论
- 总体技术评分：2.5/5
- 是否建议回炉：是
- 主要风险：**文中存在多处非技术性的占位符错误（代码/路径中混入了外部 log 文件名）**，且对 Gralloc Mapper AIDL 的演变描述不准确。
- 评分理由：虽然 16KB 页面的分析和 DMA-BUF 机制的讲解非常透彻，但 P0 级的文本错误和对 Gralloc 最新接口状态的描述偏差严重影响了其作为“Finalized”文档的质量。
- 闭环建议：必须进入回炉流程，修正所有 `@logs/external-review/...` 错误，并重新核验 Android 14+ Mapper AIDL 的普及情况。
- 本轮 review 覆盖范围：DMA-BUF 原理、ION/Heap 迁移、Gralloc Allocator/Mapper 接口、GraphicBuffer 序列化、16KB 页面预算、Perfetto 追踪。
- 本轮未完成部分：厂商私有 Gralloc Usage Flags 的具体透传链路（需结合 SoC 源码进一步验证）。

## 三、六维评分
| 维度 | 评分 | 问题数 |
|------|------|-------|
| 源码准确性 | 2.0/5 | 1 (P0) |
| 原理链完整性 | 4.5/5 | 0 |
| 版本差异覆盖 | 3.5/5 | 1 (P1) |
| 知识盲区 | 4.5/5 | 0 |
| 数据/案例支撑 | 4.0/5 | 1 (P2) |
| 交叉引用一致性 | 5.0/5 | 0 |

## 四、P0 问题（事实错误）
- **[P0][源码准确性][全书多处]**
  - **原文问题**：文中多次出现 `mapper @logs/external-review/2026-04-25-15-2.13-external-review.md`。
  - **核验结论**：这是一个严重的 hallucination 或脚本处理错误。此处原本应指代 `IMapper 4.0` 或 `IMapper AIDL` 版本，却被错误替换为了一个 Log 文件路径。
  - **运行原理说明**：无。属于纯文本生成/替换错误。
  - **为什么错**：这直接导致技术路径描述不可读，且暴露了自动化处理的缺陷。
  - **建议修正方向**：全局替换该错误字符串。根据语境，如果是在讲兼容性，应为 `IMapper 4.0 (HIDL)`；如果是讲最新标准，应为 `IMapper (AIDL)`。

## 五、P1 问题（重要缺失）
- **[P1][版本差异][Gralloc Mapper AIDL 化进度]**
  - **原文问题**：文中称“到 `android-16.0.0_r1` 为止... mapper ... 兼容路径仍然存在... 系统并不是一刀切地彻底 AIDL 化”。
  - **源码锚点**：`hardware/interfaces/graphics/mapper/aidl/android/hardware/graphics/mapper/IMapper.aidl`
  - **核验结论**：虽然 HIDL 兼容路径确实存在，但自 Android 14 起，Google 已正式引入 `graphics.mapper` AIDL 接口并推荐在 Android 15/16 设备上优先使用。文中过分强调“Mapper 仍然是 HIDL”会误导开发者认为 Mapper 还没有 AIDL 版本。
  - **为什么是重要缺失**：Android 16 预研中，AIDL Mapper 1.0/2.0 是核心。只讲 4.0 兼容路径会使文章看起来像是在 2022 年写的。
  - **建议补充方向**：明确指出 Android 14+ 已引入 AIDL Mapper，并解释 AIDL Allocator 与 AIDL Mapper 的配合模式。

## 六、P2 问题（建议改进）
- **[P2][数据/案例支撑][16KB 页面开销]**
  - **原文问题**：提到了 16KB 下“更容易出现尾部空洞”，但没有给出一个具体的对比示例。
  - **建议**：补充一个计算例子。例如：一个 256x256 的 RGBA_8888 纹理占 256KB。在 4KB 下正好 64 页；在 16KB 下也是 16 页，没有浪费。但如果是一个带有 2KB metadata 的 buffer，总大小 258KB，4KB 下占 65 页（浪 3.9KB），16KB 下占 17 页（浪费 14KB）。这种具体数据能让读者直观感受到“空洞”的代价。

## 七、知识盲区清单
| 盲区 | 重要程度 | 建议研究方向 |
|------|------|-------|
| DMA-BUF Sync File vs Fence | 高 | DMA-BUF 本身的 `dma_fence` 机制与 Android Sync Fence 的映射关系，将在 2.16 深挖。 |
| Multi-planar Buffer 分配逻辑 | 中 | 针对 YUV420 等格式，Gralloc 如何在单 fd 或多 fd 之间选择，这对 Camera 场景极重要。 |

## 八、外部核验建议
- **搜索关键词**：`IMapper.aidl` Android 14 15 16
- **搜索关键词**：`BufferDescriptorInfo` reservedSize 16KB alignment
- **建议查 AOSP**：`frameworks/native/libs/ui/GraphicBuffer.cpp` 中对 `GraphicBufferMapper::getInstance()` 的调用逻辑，看它是如何 fallback 的。
- **建议查官方文档**：`source.android.com` 关于 "GKI 2.0 DMA-BUF Heap" 的最新约束。

## 九、可闭环输出

### 9.1 回炉问题单（必须修）
- **章节**：2.15
- **严重级别**：P0
- **问题类型**：源码错误 / 文本错误
- **位置**：文中所有出现 `@logs/external-review/...` 的地方。
- **问题描述**：占位符残留，导致技术路径描述变成了一个文件路径。
- **建议修正方向**：根据 Android 16 现状，统一修正为 `IMapper 4.0` 或 `IMapper AIDL`。

- **章节**：2.15
- **严重级别**：P1
- **问题类型**：版本差异
- **位置**：Gralloc HAL 章节及版本演进章节。
- **问题描述**：低估了 Mapper AIDL 的普及程度，将 Android 16 的接口现状描述得过于守旧。
- **建议修正方向**：加入 AIDL Mapper 的介绍，并说明其在 Android 14+ 的引入背景。

### 9.2 知识盲区清单（供后续研究）
- **章节**：2.15
- **盲区描述**：多平面缓冲区（Multi-planar）的分配策略。
- **重要程度**：中
- **建议研究方向**：调研 `BufferUsage.VIDEO_DECODER` 下 YV12/NV12 格式在现代 Gralloc 里的 fd 数量选择。

### 9.3 一般建议清单（非阻断）
- **建议**：在 16KB 页面章节增加一个量化计算公式或对比表格。

### 9.4 可复用知识资产（高价值新增知识）
- **章节**：2.15
- **关键源码路径**：`hardware/interfaces/graphics/allocator/aidl/android/hardware/graphics/allocator/BufferDescriptorInfo.aidl`
- **版本差异摘要**：Android 15+ 为支持 16KB 页面，在 `BufferDescriptorInfo` 中显式处理了 `reservedSize` 字段，用于确保元数据和像素数据即便在页粒度对齐后也能正确索引。
- **技术结论**：零拷贝的物理基础是 DMA-BUF fd 的引用传递，但业务层的“护照”是 GraphicBuffer。在 Android 12+，这个分配过程已经从 ION 字符设备全面转向了基于 `/dev/dma_heap/` 的独立设备节点。

## 十、下一候选章节
- 2.16 Sync Fence 框架与帧同步机制（逻辑强关联）

## 十一、落盘信息
- 已写入文件：`logs/external-review/2026-04-26-15-2.15-external-review.md`
