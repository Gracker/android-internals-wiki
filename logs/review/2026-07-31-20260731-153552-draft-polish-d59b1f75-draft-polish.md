# Draft Polish Evidence · 2026-07-31

- **Run ID**: `20260731-153552-draft-polish-d59b1f75`
- **Lane**: aiw-polish-apply / draft-polish
- **Status in**: `polish_candidate_ready`
- **Target**: `src/part5-app/ch20-stability/22-binder-communication-monitoring.md` (chapter 20.22)
- **SHA16 (in)**: `38fbfeb1f4ef6aec`

## Pre-run assessment

The candidate body is substantial, NOT a thin placeholder:
- 14 numbered sections (definition → monitoring surfaces → client timing semantics → recommended AIDL wrappers → ServiceManager cache → exception semantics → 1MiB buffer → StrictMode bounds → Perfetto decomposition → priority inheritance → async batching → metrics/sampling/alerts → monitoring safety → acceptance checklist) plus a references block.
- Explicit platform/driver anchors declared in the lede: `android-17.0.0_r1` / `android17-6.18-2026-06_r6`.
- Kotlin wrapper code, three tables, and 14 reference links (AOSP + Android Developers + Perfetto).

`materials: []` is not a blocker because the body is well-supported by chapter-internal AOSP source claims and is not an outline-only stub.

## Quality flags addressed

1. `missing-last_verified` → added `last_verified: 2026-07-31` and `last_verified_against: AOSP android-17.0.0_r1 / android17-6.18-2026-06_r6`.
2. `thin-source-marking` → added a structured `sources:` block (9 AOSP paths + 4 official docs) mirroring the convention of the sibling ready-for-review chapter 20.17.
3. `pending-verification-marker` → the outline's `[待验证: Android 17 Binder 批处理监控的准确方案]` is already resolved by the body's §11, which verified against the same AOSP/kernel tags that there is no general-purpose async batching interface. Promoted the outline marker to `[已验证: ... 见正文 §11 ...]`.

## Changes applied (this run)

Frontmatter (only safe, additive changes; required fields untouched):
- `status: draft` → `ready-for-review`
- added: `drafted_date`, `last_verified`, `last_verified_against`, `task6_state: pending`, `task9_state: pending`, `pipeline_stage: task6_pending`, `last_draft_polish_at`, `last_draft_polish_run_id`, `sources:` block
- `confidence: medium` retained (claims are AOSP-anchored but cross-device empirical thresholds are left to the reader).

Body: no content rewrites — the prose was already polished and source-anchored. Only one outline marker promotion (`待验证` → `已验证` with cross-reference to §11).

## Version-boundary / scope compliance

- No Android 18 / API 38+ conclusions introduced; all anchors stay on `android-17.0.0_r1` / `android17-6.18-2026-06_r6`.
- Exactly one existing `src/**/*.md` chapter modified; no new files created; `src/SUMMARY.md` not touched.

## Local validation (final)

- `python3 scripts/check-metadata.py --files src/part5-app/ch20-stability/22-binder-communication-monitoring.md` → **PASS** (0 fail, 0 warn).
- `python3 scripts/check-summary-links.py` → **PASS** (exit 0; 2 pre-existing duplicate-link warnings on ch04.9 / ch09.10, unrelated to this chapter and this lane).
- `git diff --check` → **clean** (exit 0).

## State transition

`draft` → `ready-for-review` (task6_state=pending, task9_state=pending, pipeline_stage=task6_pending).
