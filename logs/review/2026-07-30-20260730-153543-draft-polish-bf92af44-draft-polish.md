# Draft Polish Evidence Log · 2026-07-30

- **Run ID**: `20260730-153543-draft-polish-bf92af44`
- **Lane**: draft-polish
- **Target**: `src/part1-fundamentals/ch08-startup/8.33-android17-modular-startup-framework-dependency-graph.md`
- **SHA16**: `4112f706ad12e041`

## Pre-run flags

| flag | status |
|---|---|
| thin-source-marking | addressed |
| missing-last_verified | fixed |
| missing-confidence | fixed |

## Assessment

Chapter body was already substantial (12 sections, ~18 KB, 6 references). Body is well-structured with clear version boundaries (Android 17 / App Startup 1.2.0), correct technical claims about `handleBindApplication()` ordering, Provider install semantics, and App Startup discovery + DFS algorithm. No fabricated conclusions detected. Body content was preserved as-is; no expansion needed.

## Changes applied

### Frontmatter (draft → ready-for-review)

- `status`: `draft` → `"ready-for-review"`
- Added `task6_state: "pending"`, `task9_state: "pending"`, `pipeline_stage: "task6_pending"`
- Added `last_verified: "2026-07-30"`
- Added `last_verified_against: "AOSP android-17.0.0_r1 + AndroidX App Startup 1.2.0"`
- Added `last_draft_polish_at`, `last_draft_polish_run_id`
- Added `confidence: "medium"` (claims are source-anchored but some are API-level observations not device-measured)
- Added structured `sources:` block (3 AOSP, 3 JetPack, 2 official docs)
- `related_chapters`: `[]` → `["8.02", "8.37"]` (8.37 references 8.33; 8.02 covers startup optimization)
- Added tags `app-startup`, `androidx`

### Inline source anchors (thin-source-marking fix)

Added `> 源码参照:` blockquotes to 3 sections making source-specific claims:
- §2: `ActivityThread.java` `handleBindApplication()` / `installContentProviders()` — `android-17.0.0_r1`
- §3: `InitializationProvider.java` + `AppInitializer.java` — App Startup 1.2.0 commit `2bbbb9f`
- §9: `AppInitializer.java` `beginStartupSection()`/`endStartupSection()` trace naming

Enriched DFS algorithm sentence in §4 with method/field names (`initializeComponent()`, `mInitialized`, `initializing`).

## Validation

```
check-metadata.py --files <target>: ✅ 1/1 passed, 0 failed, 0 warnings
check-summary-links.py: passed (164 links, 2 pre-existing duplicate warnings)
git diff --check: clean
```

## What downstream Task6/Task9 should verify

- Confirm `InitializationProvider.onCreate()` discovery order vs `discoverAndInitialize()` in 1.2.0 source.
- Confirm trace section name `Startup` and subclass naming in 1.2.0.
- Verify `publishContentProviders` batch claim against `ActivityThread.installContentProviders()` in `android-17.0.0_r1`.
