# Task 6 闲时抽检日志 · 2026-05-24 10:10

## 抽检目标
- 章节：20.11 MTE memtagMode 与 Native 崩溃治理
- 文件：src/part5-app/ch20-stability/11-mte-memtag-native-crash.md
- 模式：闲时抽检（status=finalized, task6_state=reviewed）

## 抽检范围
- L1 禁用词 / 高频词扫描
- frontmatter 完整性
- outline 锚点覆盖
- 不做 L3/L4 深度审查

## 检查结果

### L1 禁用词 / 高频词
- 高频词：`真正` 0 次 / `实际上` 0 次 / `其实` 0 次 / `根本` 0 次 / `彻底` 0 次 / `确实` 0 次。
- 命中：1 处。
  - L308：`源码级闭环` 使用抽象职场词「闭环」。已改为 `源码证据链`。
- 术语一致性：参考资料摘要中的 `SYMM` 已统一为正文使用的 `SYNC`。

### frontmatter 完整性
- 必填元数据齐全：title / chapter / section / status / applicable_versions / sources / pipeline_stage / task6_state / task6_result / task9_state / task9_result / finalized_date 均存在。
- 已补充：`last_task6_audit: "2026-05-24"`。

### outline 锚点覆盖
- 8 个 outline 锚点均有对应正文段落：
  1. MTE 在稳定性治理中的适用场景 → `## MTE 适合放在哪些稳定性场景`
  2. `android:memtagMode` 的 off、sync、async 选择 → `## android:memtagMode 的选择`
  3. ASYMM 模式为何不暴露为应用 API → `## ASYMM 为什么不是应用 API`
  4. Zygote、bionic、Scudo 的生效路径 → `## 从 Zygote 到 bionic allocator 的生效路径`
  5. 线上灰度开启 MTE 的崩溃归因策略 → 同名段落
  6. 性能、兼容性与误报边界 → 同名段落
  7. 与 Native Crash 信号处理器的配合方式 → 同名段落
  8. MTE 报告进入 APM 平台后的聚合字段 → 同名段落

## 队列状态提示
- metadata/queue.json 中仍存在 1 条 section=20.11 的 pending 项：`[DeepResearch 注入] 新增高价值调研材料，建议补充到章节`。
- 本轮为轻量抽检，未裁决该素材是否已完全消费，未新增 queue 条目。

## 修复内容
| 类型 | 位置 | 修改前 | 修改后 | 理由 |
|------|------|--------|--------|------|
| L1 禁用词 | 参考资料价值说明 | `MTE ASYMM/SYMM 启用链路的源码级闭环` | `MTE ASYMM/SYNC 启用路径的源码证据链` | 删除禁用词「闭环」，改成更具体的证据表达。 |
| 术语一致性 | 参考资料摘要 | `ASYMM/SYMM`、`SYMM 同步检查` | `ASYMM/SYNC`、`SYNC 同步检查` | 与正文的 MTE 模式命名保持一致。 |
| 元数据 | frontmatter | 无 `last_task6_audit` | `last_task6_audit: "2026-05-24"` | 记录闲时抽检时间。 |

## 统计
- 小修：2 处
- 元数据更新：1 处
- 新增回炉：0 个
- 自动晋升 finalized：0 个（章节已是 finalized；且 queue 中仍有 pending 提示项）
