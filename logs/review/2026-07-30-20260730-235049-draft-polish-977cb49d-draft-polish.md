# Draft Polish Evidence Log · 2026-07-30

- **Run ID**: `20260730-235049-draft-polish-977cb49d`
- **Lane**: draft-polish
- **Target**: `src/part5-app/ch25-power-size/29-dex-size-optimization.md`
- **SHA16**: `577925c37151d577`

## Pre-run flags

| flag | status |
|---|---|
| pending-verification-marker | addressed (outline bullet rewritten to match verified body content) |
| thin-source-marking | addressed (legacy Clippings markers + inline `[已验证]` tags removed) |
| missing-last_verified | fixed |

## Assessment

This chapter is a substantial, high-quality draft (~645 lines, ~37 KB). The body is well-structured with:

- Clear version discipline: Android 17 / API 37 / `android-17.0.0_r1` is the platform anchor; AGP/R8/Kotlin versions are treated as an independent toolchain axis. No Android 18 / API 38+ conclusions.
- Primary-source anchoring: AOSP source links (`standard_dex_file.cc`, `dex_file.h`, `DexPathList.java` at `android-17.0.0_r1`), official developer.android.com docs (R8, D8, APK Analyzer, bundletool, multidex, baseline/startup profiles), and the DEX format spec.
- Correct and cautious technical framing: the 64K limit is correctly described as a *reference-table* limit, DEX header/ID-table byte sizes are accurate, `LineNumberTable` is correctly identified as a JVM class-file attribute distinct from DEX `debug_info_item`, R8 Full Mode default-since-AGP-8.0 is correct.
- No fabricated conclusions: speculative claims are consistently hedged ("不保证", "不能据此推导", "仍取决于").

No body content changes were needed — the body is already ready-for-review quality. Only outline-marker cleanup and frontmatter completion were applied.

## Changes applied

### Frontmatter (draft → ready-for-review)

- `status`: `draft` → `ready-for-review`
- `confidence`: `medium` → `medium-high`
- Added `last_verified: 2026-07-30`
- Added `task6_state: pending`, `task9_state: pending`, `pipeline_stage: task6_pending`
- Added `last_draft_polish_at: 2026-07-30`, `last_draft_polish_run_id: 20260730-235049-draft-polish-977cb49d`

### Outline-marker cleanup (quality flags)

1. **pending-verification-marker**: The outline bullet "Android 17 Art 加载多 DEX 的并行化优化 [待验证: AOSP android-17.0.0_r1]" claimed a parallelization optimization that the body does *not* support. The Multidex body section explicitly concludes that Android 17 ART supports multi-DEX/container *reading* but offers no app-facing "parallel load N DEX" performance contract. Rewrote the bullet to accurately reflect the verified body conclusion: "Android 17 ART 支持多 DEX / DEX container 读取，但不向应用承诺'并行加载'性能契约 [已核对: AOSP android-17.0.0_r1，见正文 Multidex 节]".

2. **thin-source-marking**: Removed two legacy source-citation tags left in the outline from the Clippings-参考书 generation phase:
   - `[结构参考: Clippings/Android 性能优化 - dex 文件的体积优化实战.md]` — the body now cites the official DEX format spec + AOSP source directly.
   - `[已验证: 官方文档, developer.android.com/build/shrink-code]` — the body already links the canonical R8 docs; the inline tag was redundant.

3. **Typo fix**: `symbolicaton` → `symbolication` (outline Debug-info bullet).

## Validation

```
check-metadata.py --files <target>: ✅ 1/1 passed, 0 failed, 1 warning
  (warning: optional field 'sources' not set — benign; body has 16 inline first-source references)
check-summary-links.py: passed (164 links, 2 pre-existing duplicate warnings on unrelated MTE/ANR chapters)
git diff --check: clean
```

## What downstream Task6/Task9 should verify

- Confirm DEX header byte sizes (112 bytes for ≤040, 120 bytes for 041) against the official DEX format spec.
- Confirm `class_defs` entry size is 32 bytes and `proto_ids` is 12 bytes per the `android-17.0.0_r1` `dex_file.h` definitions.
- Verify that the body's claim "AGP 8.0 起 R8 Full Mode 为默认" matches the current developer.android.com R8 docs.
- Verify that "AGP 9.1 起默认采用更积极的 class repackaging" is accurate against AGP 9.x release notes (this is a toolchain claim, not a platform claim).
- Confirm `DexPathList.java` at `android-17.0.0_r1` does not define an app-facing DEX-count or parallel-load performance contract.
