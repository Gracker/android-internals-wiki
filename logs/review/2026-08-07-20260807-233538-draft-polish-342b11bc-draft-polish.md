# AIW Draft Polish Evidence · 2026-08-07

- lane: draft-polish
- run_id: 20260807-233538-draft-polish-342b11bc
- target: `src/part5-app/ch22-rendering-practice/34-compose-subcompose-layout-performance.md`
- input status: polish_candidate_ready
- quality_flags: thin-source-marking, missing-last_verified
- materials: none supplied by pre-run JSON

## Scope

只处理预运行 JSON 指定的既有章节；未新增 `src/**/*.md`，未修改 `src/SUMMARY.md`。Android 基线保持 `android-17.0.0_r1` / `android17-6.18-2026-06_r6`。

## Applied changes

1. Frontmatter 从 `draft` 推进为 `ready-for-review`，补齐 `sources`、`pipeline_stage`、`task6_state`、`task9_state`、`last_draft_polish_at`、`last_draft_polish_run_id`、`last_verified`，并将 `confidence` 调整为 `medium-high`。
2. 将 outline 中残留的占位/错误风险点改为正文已经支撑的结论：
   - SubcomposeLayout 不默认执行 intrinsic + actual 双趟测量；
   - intrinsic 查询传播到 SubcomposeLayout 可能直接失败，而非全树重测；
   - Compose UI 1.11.4 无公开 `@IntrinsicMeasurer`；
   - Compiler Metrics、Layout Inspector 与 Perfetto 的诊断边界分开描述；
   - BoxWithConstraints 与 LookaheadScope 的适用边界对齐正文；
   - Android 17 framework 不引入专门 SubcomposeLayout 加速路径。
3. 未新增 unsupported Android 18/API38+ 结论；未大段扩写正文。

## Validation

- `python3 scripts/check-metadata.py --files src/part5-app/ch22-rendering-practice/34-compose-subcompose-layout-performance.md`：通过（1 个文件，0 失败，0 警告）。
- `python3 scripts/check-summary-links.py`：通过（661 local links，0 duplicate warnings）。
- `git diff --check`：通过，无 whitespace error 输出。

## Result

章节已有较完整的源码锚点、官方资料列表和正文纠错内容，本轮只做状态、来源标记和 outline 对齐，适合进入 Task6 复查队列。
