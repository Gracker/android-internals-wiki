# 草稿 Review 日志 · 2026-04-13 23:15

## Review 目标
- 章节：13.4 命令行打开超大 Trace
- 文件：src/part3-tools/ch13-perfetto/04-large-traces.md
- Review 模式：首次 review

## 四层质检结论

### L1 硬性规则
- 已做 10 处小修，主要清掉 `本质上`、`这意味着`、`这一点很重要`、`值得注意` 一类 AI 痕迹，并统一 `trace_processor` / `traceconv` 命名。
- 修正了大纲、节标题、正文之间的命名错位，避免 `trace_processor_shell` / `.read` / `trace_to_text` 与正文示例互相打架。
- 中英文间距、代码块、frontmatter 结构正常。

### L2 可读性
- 开头仍以“大 Trace 打不开”的具体场景切入，切口成立。
- 已修 7 处可读性问题，主要是压缩过渡句、把 `tp.query()` 说明改得更直接，以及修正“标题写 `.read`，正文却讲 `-q`”的阅读断点。
- 7/7 个锚点都有正文覆盖，主线完整。

### L3 内容深度
- 有 2 个问题需要交 Task 9 / Task 2B：`-W/--wait` 参数仍处于 `[待验证]` 状态；`traceconv` 文本过滤后“再转回 protobuf”的处理链缺少可执行依据。
- 这两处都涉及工具链/API/版本边界，不在 Task 6 内裁决。

### L4 活人感
- 全文主体仍像工程师在讲工具使用，没有出现整段汇报腔或教科书式空转。
- 本轮未发现需要单独因“活人感”回炉的段落。

## 各维度评分（1-5）
- 结构完整性：4/5
- 措辞质量：4/5
- 一致性：4/5
- 验证完整性：3/5
- 元数据完整性：5/5

## 修复内容
- 表达｜导语｜“更准确地说”句式｜改成直接工程表述｜减少 AI 清嗓。
- 表达｜隐私段｜“这一点很重要”｜改成“这种本地解析方式更稳妥”｜去掉空泛强调。
- 表达｜大 Trace 挑战｜“本质上是在浏览器里跑 ...”｜改成直接描述｜减少模板化判断。
- 表达｜大 Trace 挑战｜“这意味着在加载完成之前 ...”｜改成直接结果描述｜压缩因果废话。
- 表达｜Perfetto SQL 时间戳说明｜“这意味着我们可以 ...”｜改成“所以我们可以 ...”｜降低 AI 味。
- 一致性｜大纲锚点｜`trace_processor_shell`｜改成“用 trace_processor 批量跑 SQL 脚本”｜与正文一致。
- 一致性｜大纲锚点｜`trace_to_text`｜改成 `traceconv`｜与工具名一致。
- 一致性｜扩展小节标题｜`trace_to_text：格式转换工具`｜改成 `traceconv：格式转换工具`｜统一命名。
- 结构｜批量分析章节标题｜“用 .read 命令执行 SQL 文件”｜改成“用 `-q` 执行 SQL 文件”｜与正文示例一致。
- 结构｜`-q` 小节开头｜补一行“.read 适合交互式 shell，脚本更常用 `-q`”｜补齐上下文。
- 表达｜批量分析脚本示例引入｜“首先是 SQL 文件 ...”｜改成“SQL 文件 ... 如下”｜去掉 AI 元叙述。
- 表达｜Python API 过渡句｜“这时候我们应该切换到 Python”｜改成更直接的工程口吻｜减少说教感。
- 节奏｜`tp.query()` 说明｜单段提示句｜改成两句直述｜降低信息墙感。
- 表达｜结果存储建议｜“一个简单的做法是 ...”｜改成“最省事的做法 ...”｜更贴近工程语感。
- 表达｜常见问题｜“最佳实践是 ...”｜改成“实际工作里，通常先 ...”｜降低模板味。
- 元数据｜frontmatter｜补 `task6_result`，并把 `task6_state/pipeline_stage/task2b_state` 切到回炉状态｜保持状态机闭环。
- 元数据｜progress.json｜13.4 从 `task6_pending` 改到 `task2b_pending`｜与本轮结论对齐。

## 需高爷处理的问题
- 需确认｜`trace_processor` 的高级参数（约 L336-L344）｜`-W/--wait` 仍写成可用参数，但正文同时保留 `[待验证]`。｜交 Task 9 按当前 `trace_processor --help` / 官方文档核对，Task 2B 再决定保留还是删除。
- 需确认｜常见问题与误区（约 L579-L581）｜“`traceconv` 转文本 → 文本工具过滤 → 转回 protobuf” 缺少文档依据和具体命令，工具链风险较高。｜交 Task 9 核对 traceconv 支持边界，若不可行，Task 2B 改成受支持的替代方案。

## 统计
- 小修：17 处
- 大问题标注：2 处
- 锚点覆盖：7/7
