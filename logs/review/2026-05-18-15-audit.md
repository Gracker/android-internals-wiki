# Task 6 闲时抽检日志 · 2026-05-18 15:14

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 187
- 模式：闲时抽检（finalized + task6_state: reviewed）

## 抽检目标
- 章节：1.15 JNI/NDK 性能优化
- 文件：src/part1-fundamentals/ch01-architecture/15-jni-ndk-performance.md
- 选择原因：`status: finalized`、`task6_state: reviewed`、`pipeline_stage: ready-to-publish`，`reviewed_date=2026-04-23`，超过 14 天且无 `last_task6_audit` 记录。

## 抽检范围
- L1 禁用词 / 高频词扫描
- frontmatter 完整性
- outline 锚点覆盖
- 不做 L3/L4 深度审查

## 检查结果
- L1 禁用词：初扫发现 4 类命中：`下钻`、`兼容矩阵`、`这意味着`、`落地`；均已轻量小修。
- 高频词：初扫 `真正` 9 次、`根本` 2 次、`确实` 2 次；`真正` 超过阈值，已压到 0 次；`根本` 压到 0 次；`确实` 保持 2 次，均为功能性表达。
- 限制句式：`不是 X，而是 Y` 类 0 处。
- frontmatter：必填字段完整；已补 `last_task6_at`、`last_task6_audit`、`last_task6_audit_result: l1-light-edit`。
- outline 锚点覆盖：6/6 覆盖。
  1. Perfetto 中的 JNI 可观测性有三条路 → `先把证据链搭好：Perfetto 里到底怎么看 JNI`
  2. JNI transition 的量级要看来源和设备条件 → `JNI transition 到底有多贵`
  3. 减少 JNI 次数通常比追逐单次纳秒数更重要 → `先把次数降下来：缓存 ID，合并调用，做粗粒度接口`
  4. `@FastNative` 和 `@CriticalNative` 的边界完全不同 → 同名章节已覆盖
  5. 线程和引用生命周期比单次 transition 更常见地出问题 → `native 线程与引用生命周期，才是日常最容易踩坑的地方`
  6. 16KB page size 影响的是 native library 的可加载性和内存布局 → `16KB page size：JNI/NDK 项目的上线门槛`

## 修复内容
1. 开头：删除填充性 `真正`，并把“第三方 `.so` 根本没有对齐”改为“第三方 `.so` 未满足对齐要求”。
2. outline：`真正需要重编和校验` → `需要重编和校验`。
3. Perfetto 可观测性：删除 `真正` / `根本` 的填充性强调，保留原有技术判断。
4. transition 表头：`这组数字真正说明了什么` → `这组数字说明了什么`。
5. JNI 分析顺序：`再下钻具体函数` → `再看具体函数内部`。
6. 粗粒度接口段：`真正影响架构的` → `第二步更影响架构`。
7. `@CriticalNative` 示例段：`真正符合这条规则` → `符合这条规则`。
8. 兼容性段：`兼容矩阵` → `版本兼容规则`。
9. local reference 段：`这意味着...` → `因此，...`。
10. 16KB page size 段：删除两处填充性 `真正`。
11. Propeller 段：`PGO / AutoFDO 落地` → `PGO / AutoFDO 已接入`。
12. frontmatter：补充本次闲时抽检时间与结果字段。

## queue / 回炉
- `metadata/queue.json` 中 section=1.15 的 pending 条目：0
- 大问题标注：0
- queue.json 写入：0 条
- 本轮未做 L3/L4 深度审查，不新增 Task 2B 问题单

## 结论
- 抽检结果：通过（完成 L1/L2 轻量小修 + frontmatter 标注）。
- 章节状态：保持 `status: finalized` / `pipeline_stage: ready-to-publish`。
