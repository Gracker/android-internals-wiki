# 草稿 Review 日志 · 2026-04-14 13:05

## Review 目标
- 章节：14.4 dumpsys 系列命令
- 文件：src/part3-tools/ch14-other-tools/04-dumpsys.md

## writing-guide.md 合规性
- L1/L2 已做轻量小修，主要清掉 STYLE 禁词、统一验证标注格式，并把开头改成更直接的工程场景切入。
- 结构上 6/6 个必需锚点都有正文覆盖，两个扩展小节也有内容，但仍保留 2 处 `[待补充]` 占位。
- 仍有 3 个需要回炉的问题：activity / meminfo 的口径存在版本风险，SurfaceFlinger 的 Android 15+ 变化缺少交叉验证，进阶用法还缺素材与来源。

## 各维度评分（1-5）
- 结构完整性：4/5
- 措辞质量：4/5
- 一致性：4/5
- 验证完整性：3/5
- 元数据完整性：4/5

## 修复内容
- 表达｜开头说明｜“dumpsys 本质上是一个桥梁...”｜“可以把 dumpsys 看成一个桥梁...”并拆成 2 段｜去掉 STYLE 禁词，降低开头的信息墙。
- 表达｜开头收束｜“可以看到完整列表”｜“就能列出完整列表”｜清理汇报腔。
- 格式｜验证标注｜多处 ``[已验证]`` / ``[来源]``｜统一为普通 `[已验证]` / `[来源]`｜避免把验证标注渲染成代码样式。
- 表达｜PSS 解释｜“这意味着把所有进程的 PSS 加起来...”｜“把所有进程的 PSS 加起来，结果就接近...”｜去掉 AI 强调词，改成更朴素的技术描述。
- 命令示例｜meminfo 实战用法｜“重置 gfxinfo 计数器...”｜“第一次抓取，记录基线 / 第二次抓取，对比变化”｜修正明显笔误，保证示例可直接照着执行。
- 表达｜gfxinfo 基本用法｜“它有两种使用模式”｜“它最常见的三种用法如下”｜让标题和示例数量一致。
- 表达｜Battery Historian 段｜“需要注意的是”｜直接改成事实陈述｜清理 AI 清嗓词。
- 表达｜厂商 dump 例子｜“这是不可或缺的信息来源”｜“这类厂商自定义 dump 往往能直接给出关键线索”｜改成更克制的工程措辞。
- 元数据｜frontmatter｜缺少 task6 review 结果字段｜补 `reviewed_by` / `reviewed_date` / `task6_result`，并更新 `task6_state` / `task2b_state` / `pipeline_stage`｜保持状态机闭环。
- 元数据｜progress.json｜14.4 仍是 `task6_pending`｜改为 `task2b_pending`｜与本轮 review 结果对齐。

## 需高爷处理的问题
- 需确认｜dumpsys activity / dumpsys meminfo｜Activity 进程段直接使用 `oom_adj`、`VISIBLE_APP_LVL`、`FOREGROUND_APP` 和固定数值解释当前系统输出；Private Dirty 段又写成“Android 默认不用 swap”。这两处都带有明显的版本与实现口径风险。｜交给 Task 9 按当前 `dumpsys activity processes` 实际输出、`ProcessList` / `oom_score_adj` 口径和官方内存文档重核。
- 需确认｜dumpsys SurfaceFlinger / Android 15+ 的输出变化｜正文把 Android 15+ 输出格式变化、`--list` 适用版本和“额外参数或权限”写成确定结论，但当前只挂了 Obsidian 素材，缺少对 AOSP android-15/16 `SurfaceFlinger::dump` 与设备实测输出的交叉验证。｜交给 Task 9 核对输出结构、参数边界和 Winscope 替代关系。
- 需补充素材｜进阶用法｜`dumpsys package / alarm / jobscheduler` 和自定义 Service dump 两段仍保留 `[待补充]` 占位，厂商 `perfboost` 例子也没有来源。｜交给 Task 2B 补真实输出示例或代码片段，并补来源。

## 统计
- 小修：10 处
- 大问题标注：3 处
- 锚点覆盖：6/6
