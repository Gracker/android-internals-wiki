# Draft Polish Evidence · 20260727-193527-draft-polish-30f4d38e

- lane: draft-polish
- generated_at: 2026-07-27T19:35:27+08:00
- target: `src/part3-tools/ch14-other-tools/28-gpu-performance-profiling-advanced.md`
- status_before: draft
- status_after: ready-for-review
- quality_flags_before: pending-verification-marker, thin-source-marking, missing-last_verified, missing-confidence

## 本轮处理

1. 仅修改指定既有章节；未创建任何 `src/**/*.md` 新章节，未编辑 `src/SUMMARY.md`。
2. 补齐 draft-polish / review 准备所需 frontmatter：`task6_state`、`task9_state`、`pipeline_stage`、`last_draft_polish_at`、`last_draft_polish_run_id`、`last_verified`、`confidence`、`sources`。
3. 将章节状态从 `draft` 推进为 `ready-for-review`，并明确下一阶段为 `task6_pending`。
4. 将正文可信范围限定到 Android 17 / `android-17.0.0_r1` 的 Perfetto GPU counter、GPU memory event 与 trace processor 结构；未引入 Android 18/API38+ 结论。
5. 对缺少材料支撑的 outline 条目统一标注“待补材料”，避免工作负载阈值、Ray Tracing、NPU/ML 协同、profileable 权限差异、AGI 工作流被误读为已验证结论。
6. 修正两处可安全修复问题：`端侧 AI 推理理` typo；trace 体积估算从约 3.5 MB 更正为 36,000,000 bytes（约 34.3 MiB）。
7. 软化未充分支撑表述：删除 Adreno 6xx `block_capacity` 8-12 的无来源例值；将“OEM 至少 emit 同名 counter”改为“优先寻找同名或同单位语义 counter，并显式说明替代关系”。

## 边界

- materials 为空，本轮未进行无来源扩写。
- 章节现有正文已有 Perfetto proto / trace processor 源码索引和基线限定，因此只做结构、状态、术语、来源标记与已发现的安全事实修复。
- 工作负载基线、Ray Tracing、NPU/ML、隐私权限与 AGI 工作流仍需 body-apply/source material 后再扩写。

## 后续

- Task6 复查应优先核对 `GpuCounterDescriptor`、`GpuCounterEvent`、`GpuMemTotalEvent`、`GpuCounterSequenceState` 与 `gpu_counter_span_view.sql` 的字段和注释。
- Task9 前需确认 outline 中“待补材料”不会被发布链路当作正文结论；若要覆盖完整标题范围，需要后续 source-apply/rework 补材料。

## 校验

- `python3 scripts/check-metadata.py --files src/part3-tools/ch14-other-tools/28-gpu-performance-profiling-advanced.md`：通过（1 个文件，0 失败，0 警告）。
- `python3 scripts/check-summary-links.py`：通过；输出既有 duplicate SUMMARY warning 2 条，非本轮新增且本 lane 禁止编辑 `src/SUMMARY.md`。
- `git diff --check`：通过。
