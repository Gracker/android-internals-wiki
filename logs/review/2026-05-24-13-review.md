# 草稿 Review 日志 · 2026-05-24 13:10

## Review 目标
- 章节：8.2 App 启动全流程
  - 文件：src/part2-performance/ch08-responsiveness/02-app-launch.md
- 章节：7.5 优化策略
  - 文件：src/part2-performance/ch07-smoothness/05-optimization.md
- 章节：18.6 SurfaceView 直出路径
  - 文件：src/part2-performance/ch18-rendering-pipelines/06-surfaceview.md

## Review 模式
- 首次/复审模式：revisiting 正常复审
- 本轮处理：3 个章节
- external-review：活跃目录无可消费文件

## 8.2 App 启动全流程

### writing-guide.md / technical-writing 合规性
- 合规：L1 禁用词/高频词复扫通过，代码围栏语言完整。
- 处理：统一正文破折号，压掉“耗时黑洞/重灾区”等夸张表达；清理过期自动晋升字段。

### 结构与覆盖完整性
- 锚点覆盖：6/6，必须覆盖项均有正文对应段落。
- 内容充分性：正文有效内容超过过滤阈值，非空壳章节。

### 各维度评分（1-5）
- 结构完整性：5/5
- 措辞质量：5/5
- 一致性：4/5
- 验证完整性：4/5
- 元数据完整性：5/5

### 修复内容
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`在 Perfetto 中打开一个冷启动 Trace,会看到一段横跨 system_server、SurfaceFling`｜修改后：`在 Perfetto 中打开一个冷启动 Trace,会看到一段横跨 system_server、SurfaceFling`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`Android 把应用的启动分为三种状态:冷启动(Cold Start)、温启动(Warm Start)和热启动(Hot`｜修改后：`Android 把应用的启动分为三种状态:冷启动(Cold Start)、温启动(Warm Start)和热启动(Hot`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`Launcher 调用 startActivity(),经过几层封装后通过 Binder IPC 发送请求到 syste`｜修改后：`Launcher 调用 startActivity(),经过几层封装后通过 Binder IPC 发送请求到 syste`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`1. Application.attachBaseContext()--这是应用侧最早能介入的回调`｜修改后：`1. Application.attachBaseContext()——这是应用侧最早能介入的回调`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`2. Application.onCreate()--大多数 SDK 初始化代码放在这里`｜修改后：`2. Application.onCreate()——大多数 SDK 初始化代码放在这里`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`**先执行 callback**:LaunchActivityItem.execute() → 调用 handleLau`｜修改后：`**先执行 callback**:LaunchActivityItem.execute() → 调用 handleLau`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`**请求布局**:调用 requestLayout(),这是通过 Choreographer 向主线程 post 一个回`｜修改后：`**请求布局**:调用 requestLayout(),这是通过 Choreographer 向主线程 post 一个回`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`[图:冷启动完整时序图--从用户点击到首帧显示,标注 system_server、Zygote、App 主线程、Rend`｜修改后：`[图:冷启动完整时序图——从用户点击到首帧显示,标注 system_server、Zygote、App 主线程、Rend`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`TTID 只统计到首帧绘制,但很多应用的界面在首帧绘制时并没有显示完整内容--数据还在从网络加载,或者数据库还在查询。用`｜修改后：`TTID 只统计到首帧绘制,但很多应用的界面在首帧绘制时并没有显示完整内容——数据还在从网络加载,或者数据库还在查询。用`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`单独看每个 SDK 的初始化时间都不长,但 10-20 个 SDK 串行初始化的累积效果可能达到 1-2 秒。而且很多 `｜修改后：`单独看每个 SDK 的初始化时间都不长,但 10-20 个 SDK 串行初始化的累积效果可能达到 1-2 秒。而且很多 `
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`[图:首帧绘制的关键路径--从 performTraversals 到 queueBuffer,标注主线程和 Rende`｜修改后：`[图:首帧绘制的关键路径——从 performTraversals 到 queueBuffer,标注主线程和 Rende`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`在 Android 7.0(API 24)之前,ART 采用的是 AOT(Ahead-Of-Time)全量编译策略--安`｜修改后：`在 Android 7.0(API 24)之前,ART 采用的是 AOT(Ahead-Of-Time)全量编译策略——安`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`实测数据表明,对于中大型应用,Baseline Profile 可以将冷启动时间缩短 20%-40%。效果取决于应用自身`｜修改后：`实测数据表明,对于中大型应用,Baseline Profile 可以将冷启动时间缩短 20%-40%。效果取决于应用自身`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`这些预加载内容让应用在冷启动时不需要重新加载 Java 基础类、Android Framework 核心类、常用的 Dr`｜修改后：`这些预加载内容让应用在冷启动时不需要重新加载 Java 基础类、Android Framework 核心类、常用的 Dr`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`Zygote preload 的局限性在于:它只预加载系统级的类和资源,不会预加载应用自身的代码。Application`｜修改后：`Zygote preload 的局限性在于:它只预加载系统级的类和资源,不会预加载应用自身的代码。Application`
- 标点｜将正文中的双连字符统一为中文破折号｜修改前：`- **7.1 卡顿的定义与分类**:启动超时是卡顿的一种特殊形式--首帧耗时超过了用户可接受的范围。`｜修改后：`- **7.1 卡顿的定义与分类**:启动超时是卡顿的一种特殊形式——首帧耗时超过了用户可接受的范围。`
- 措辞｜弱化夸张表达｜修改前：`这个阶段是应用侧启动耗时的重灾区。`｜修改后：`这个阶段通常是应用侧启动耗时的集中区域。`
- 措辞｜标题去掉夸张比喻｜修改前：`### SDK 初始化:最大的耗时黑洞`｜修改后：`### SDK 初始化:常见耗时来源`

### 需高爷处理的问题
- 既有 Task9 pending：首帧 Surface 创建仍按旧 BufferQueueLayer/IGBP 模型描述，需按 Android 11+ BLASTBufferQueue 口径由 Task2B/Task9 复核。
- Task6 本轮未新增 L3/L4 回炉项；未重复写入 queue.json。

### 四层质检报告
**L1 硬性规则** ✅
- 禁用词：复扫 0 处；高频词 `真正/实际上/其实/根本/彻底/确实` 均 < 5。
- 事实硬伤：Task6 不裁决；沿用 Task9 pending 问题单。
- 格式：代码围栏语言完整；本轮已修复可直接处理的标点/标题/元数据问题。

**L2 可读性** ✅
- 开头、节奏、结构、读者视角：通过；可直接处理的问题已小修。

**L3 内容深度** ⚠️
- Task9 已有技术/版本边界 pending 队列；Task6 本轮不新增裁决。

**L4 活人感** ✅
- 已压掉本轮可处理的口号化、网络化和绝对化表达。

**总评**：Task6 写作复审通过，`task6_result=pass-light-edit`；不自动晋升，原因是 §8.2 仍有 Task9/queue pending。

### 统计
- 小修：18 处
- 大问题标注：0 处新增
- 锚点覆盖：已覆盖/总数

## 7.5 优化策略

### writing-guide.md / technical-writing 合规性
- 合规：L1 禁用词/高频词复扫通过，代码围栏语言完整。
- 处理：清理“核心武器/利器/神器/预字诀”等口号化表达；参考资料移动到全文末尾。

### 结构与覆盖完整性
- 锚点覆盖：5/5，必须覆盖项均有正文对应段落。
- 内容充分性：正文有效内容超过过滤阈值，非空壳章节。

### 各维度评分（1-5）
- 结构完整性：4/5
- 措辞质量：4/5
- 一致性：4/5
- 验证完整性：4/5
- 元数据完整性：5/5

### 修复内容
- 措辞/标题｜去掉夸张/营销化标题｜修改前：`### ConstraintLayout：扁平化布局的核心武器`｜修改后：`### ConstraintLayout：扁平化布局的主要手段`
- 措辞/标题｜去掉口号式标题｜修改前：`### ViewStub：按需加载的利器`｜修改后：`### ViewStub：按需加载的占位机制`
- 措辞/标题｜措辞更工程化｜修改前：`RecyclerView 是卡顿的高发地带——滑动场景下每一帧的预算只有 8-16ms，而列表滑动会频繁触发 onBindViewHolder 和 reques`｜修改后：`RecyclerView 是滑动卡顿的高发区域——滑动场景下每一帧的预算只有 8-16ms，而列表滑动会频繁触发 onBindViewHolder 和 requ`
- 措辞/标题｜标题改为技术描述｜修改前：`### Overdraw：画了又画`｜修改后：`### Overdraw：重复绘制`
- 措辞/标题｜标题改为机制描述｜修改前：`### Hardware Layer：动画加速器`｜修改后：`### Hardware Layer：属性动画缓存`
- 措辞/标题｜去掉情绪化标签｜修改前：`**三个陷阱：** 不要长期开启（占 GPU 内存）；不要对频繁 invalidate 的 View 使用；对简单 View 没意义。详见 [2.7 Hardw`｜修改后：`**三个注意点：** 不要长期开启（占 GPU 内存）；不要对频繁 invalidate 的 View 使用；对简单 View 没意义。详见 [2.7 Hard`
- 措辞/标题｜去掉口号化表达｜修改前：`"预"字诀的核心思想是：利用当前帧的空闲时间，提前为接下来的帧做好准备工作。`｜修改后：`预取和预计算的核心思路是：利用当前帧的空闲时间，提前为接下来的帧做好准备工作。`
- 措辞/标题｜去掉网络化/夸张表达｜修改前：`**"Hardware Layer 神器"**：只在属性动画场景有效，滥用反而增加开销`｜修改后：`**"Hardware Layer 适合所有动画"**：它只在属性动画场景有效，滥用反而增加开销`
- 结构｜保持 References 位于正文最后｜修改前：`参考资料位于技术补充前`｜修改后：`参考资料移动到全文末尾`

### 需高爷处理的问题
- 既有 Task9 pending：RenderEffect layer 模型自相矛盾、RecyclerView / Hardware Layer / RenderThread / Compose 等 API 口径需由 Task2B/Task9 复核。
- Task6 本轮未新增 L3/L4 回炉项；未重复写入 queue.json。

### 四层质检报告
**L1 硬性规则** ✅
- 禁用词：复扫 0 处；高频词 `真正/实际上/其实/根本/彻底/确实` 均 < 5。
- 事实硬伤：Task6 不裁决；沿用 Task9 pending 问题单。
- 格式：代码围栏语言完整；本轮已修复可直接处理的标点/标题/元数据问题。

**L2 可读性** ✅
- 开头、节奏、结构、读者视角：通过；可直接处理的问题已小修。

**L3 内容深度** ⚠️
- Task9 已有技术/版本边界 pending 队列；Task6 本轮不新增裁决。

**L4 活人感** ✅
- 已压掉本轮可处理的口号化、网络化和绝对化表达。

**总评**：Task6 写作复审通过，`task6_result=pass-light-edit`；不自动晋升，原因是 §7.5 仍有 Task9/queue pending。

### 统计
- 小修：9 处
- 大问题标注：0 处新增
- 锚点覆盖：已覆盖/总数

## 18.6 SurfaceView 直出路径

### writing-guide.md / technical-writing 合规性
- 合规：L1 禁用词/高频词复扫通过，代码围栏语言完整。
- 处理：frontmatter 禁用词“闭环”替换为“状态流转”；压掉“清一色/更省心/最省 GPU/大幅改善”等口语化或绝对表达。

### 结构与覆盖完整性
- 锚点覆盖：8/8，outline 中 8 个锚点均有对应正文小节。
- 内容充分性：正文有效内容超过过滤阈值，非空壳章节。

### 各维度评分（1-5）
- 结构完整性：5/5
- 措辞质量：4/5
- 一致性：4/5
- 验证完整性：4/5
- 元数据完整性：5/5

### 修复内容
- 措辞/格式｜frontmatter 禁用词替换｜修改前：`闭环不一致`｜修改后：`状态流转不一致`
- 措辞/格式｜去掉口语化绝对表达｜修改前：`这就是为什么视频播放器、游戏引擎、Camera 预览几乎清一色使用 SurfaceView。`｜修改后：`这也是视频播放器、游戏引擎、Camera 预览通常优先使用 SurfaceView 的原因。`
- 措辞/格式｜措辞更工程化｜修改前：`TextureView 仍然更省心。`｜修改后：`TextureView 的实现成本仍然更低。`
- 措辞/格式｜去掉口语化表述｜修改前：`这就是最省 GPU 的路径。`｜修改后：`这是 GPU 成本最低的路径。`
- 措辞/格式｜修正版本边界并压掉模糊强化词｜修改前：`Android 11+ BLAST 同步机制成熟后，SurfaceView 的同步问题已大幅改善。`｜修改后：`Android 12+ BLAST 路径成熟后，SurfaceView 的同步问题已改善。`

### 需高爷处理的问题
- 既有 Task9 pending：BUFFER_RELEASE_CHANNEL 版本边界、SurfaceView BLAST 边界与首帧版本拆分需由 Task2B/Task9 复核。
- Task6 本轮未新增 L3/L4 回炉项；未重复写入 queue.json。

### 四层质检报告
**L1 硬性规则** ✅
- 禁用词：复扫 0 处；高频词 `真正/实际上/其实/根本/彻底/确实` 均 < 5。
- 事实硬伤：Task6 不裁决；沿用 Task9 pending 问题单。
- 格式：代码围栏语言完整；本轮已修复可直接处理的标点/标题/元数据问题。

**L2 可读性** ✅
- 开头、节奏、结构、读者视角：通过；可直接处理的问题已小修。

**L3 内容深度** ⚠️
- Task9 已有技术/版本边界 pending 队列；Task6 本轮不新增裁决。

**L4 活人感** ✅
- 已压掉本轮可处理的口号化、网络化和绝对化表达。

**总评**：Task6 写作复审通过，`task6_result=pass-light-edit`；不自动晋升，原因是 §18.6 仍有 Task9/queue pending。

### 统计
- 小修：5 处
- 大问题标注：0 处新增
- 锚点覆盖：已覆盖/总数

## 自动晋升检查
- 结果：无。3 个章节均 `task9_result=needs-rework` 且 queue.json 中仍有 pending 条目，不满足 finalized 条件。

## queue.json 闭环
- 新增回炉项：0
- 既有 pending：8.2、7.5、18.6 均已有 Task9 pending 条目，未重复写入。
