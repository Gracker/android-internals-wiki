# AIW idle audit · 2026-07-27

- run_id: `20260727-223552-idle-audit-6c95044a`
- mode: `idle-audit`
- target: `src/part2-performance/ch18-rendering-pipelines/16-game-engine.md`
- title: 游戏引擎渲染链路
- status: patched

## 抽检范围

- Android 18 / API 38+ 越界：未发现。全文锚定 Android 17 / API 37。
- Android baseline：正文固定到 `android-17.0.0_r1` / `android17-6.18-2026-06_r6`，未引入 Android 18 结论。
- 待验证残留：未发现 TODO / FIXME / 待验证 / 占位类残留。
- 来源标记：frontmatter 之前缺少 `sources:` 块；正文末尾已列出 Android 17 AOSP / kernel 入口与官方文档链接。本次补齐 frontmatter `sources:`，类型覆盖 aosp / kernel / official，与正文「源码核对清单」一致。
- frontmatter 缺项：之前缺少 `last_verified`、`last_verified_against`、`confidence`（pre-run quality_flags 报告的 3 项）。本次全部补齐，`check-metadata.py` 由 `[warn]` 转为 ✅ 零警告。
- 表达问题：文中存在若干 CJK 与英文/代码 token 间缺空格的情况（如 `Swappy在`、`Swappy或`、`pacer控制`、`FPS评价`、`Insights补齐`、`frame id应`、`fence封装`、`memory reclaim和`、`driver tracepoint多`）。这些为排版一致性细节，非技术错误；idle-audit 不在正文做大面积 churn，留待写作复审 lane 统一处理。
- 技术准确性：Unity/Unreal 线程模型、Swappy frame pacing、BLAST/SurfaceControl 路径、`VK_EXT_present_timing`、`Surface.setProducerThrottlingEnabled()`、ADPF Hint Session、Frame Rate API、Game Mode/State 描述均与 AOSP android-17.0.0_r1 及官方文档口径一致，未发现可安全修复的技术错误。

## 处理结论

本次仅做安全补丁：补齐 frontmatter 抽检元数据（`last_verified`、`last_verified_against`、`confidence`、`sources`）并写入 idle audit 跟踪字段（`last_idle_audit_at`、`last_idle_audit_run_id`）。未改动正文技术内容、未新增章节、未编辑 `src/SUMMARY.md`。章节保持 `finalized` / `ready-to-publish`，未降级。

## 实际补丁

- 仅修改既有章节：`src/part2-performance/ch18-rendering-pipelines/16-game-engine.md`
- 改动范围：frontmatter 末尾新增 5 个字段 + 22 条 sources。
- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。

## 本地校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch18-rendering-pipelines/16-game-engine.md` → ✅ 1 文件通过，0 失败，0 警告。
- `python3 scripts/check-summary-links.py` → passed（165 links，2 duplicate warnings 为既有，非本次目标）。
- `git diff --check` → 干净，无空白错误。
