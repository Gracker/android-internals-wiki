# Idle Audit Log · 2026-07-31

- **run_id**: 20260731-103512-idle-audit-13a21d51
- **mode**: idle-audit
- **profile**: aiw-polish-idle-audit
- **target**: `src/part5-app/ch25-power-size/16-adpf-power-efficiency-powermonitor.md` (§25.16)
- **status before**: finalized / pipeline_stage=ready-to-publish
- **confidence before**: medium-high
- **result**: pass-with-fix（抽检通过，1 处安全修复）

## 抽检范围

1. Android 18 / API 38+ 越界检查
2. 待验证残留标记（pending-verification-marker / [待验证]）
3. 来源标记不足（thin-source-marking）
4. frontmatter 必填/可选字段完整性
5. 版本锚点一致性（android-16.0.0_r1 / android-17.0.0_r1 / AOSP main）
6. outline HTML 注释标记完整性（outline-start / outline-end 配对）
7. 明显技术/表达问题

## 发现与处置

### F1 · 结构缺陷（已修复）：缺失 `<!-- outline-end -->` 标记

- **级别**: L1（结构/构建安全修复）
- **描述**: 第 92 行有 `<!-- outline-start -->`，但全文无配对的 `<!-- outline-end -->`。对照同目录 sibling 章节（如 ch25/15、ch25/01–31 等 24 个章节均正确配对），该标记应在最后一条 outline 条目之后、第一个 `##` 正文标题之前闭合。
- **影响**: outline 渲染器可能把后续正文误判为大纲内容；属于确定性结构缺陷，不涉及技术结论。
- **修复**: 在第 109 行（最后一条 outline）之后、第 111 行 `## 适用场景` 之前插入 `<!-- outline-end -->`，与 sibling 章节保持一致。
- **风险**: 极低。纯 HTML 注释标记，不影响 Markdown 渲染语义，不影响 frontmatter，不改变任何技术论点。

### 其他抽检结论（无需修改）

- **API/版本越界**: 全文未出现 Android 18 / API 38 / API 39+ 的 mainline 结论。正文版本锚点明确使用 `android-16.0.0_r1` 与 `android-17.0.0_r1`（L388/390/394/412/431/444/467），并显式声明「当前 AIW 最高只覆盖到 Android 17/API 37」。`applicable_versions` 为 "Android 15 (API 35) - Android 17 (API 37)"。合规。
- **待验证标记**: L109（outline）与 L381–383（`[待验证]` PowerMonitorReadings 与 Perfetto power rails 一致性）为合理的 pending-verification 标记，对应 quality_flags.pending-verification-marker，不应删除。保留。
- **来源标记**: 正文有 22 条 sources（official / aosp / report / structure），覆盖关键 API 与源码路径。frontmatter `sources` 字段为空字符串但正文 sources block 完整，这是该仓库既有写法（正文 sources block 优先）。对应 quality_flags.thin-source-marking，非阻塞。保留。
- **frontmatter**: 必填字段（title/chapter/status/applicable_versions/tags）齐全；可选字段（last_verified/confidence/sources）存在。check-metadata.py 可通过。
- **技术准确性**: ADPF API 版本标注（PerformanceHintManager API 31 / setThreads API 34 / setPreferPowerEfficiency API 35）准确；PowerStatsService 30s 缓存阈值、statsd pull atom 2s 超时、IPowerStats HAL 6 方法均与 DeepResearch 调研报告一致；SessionTag/SessionMode 枚举区分正确。无技术错误。

## frontmatter 变更

新增字段（idle-audit 证据）：
- `last_idle_audit_at`: "2026-07-31T10:35:12+08:00"
- `last_idle_audit_run_id`: "20260731-103512-idle-audit-13a21d51"
- `last_idle_audit_result`: "pass-with-fix"
- `last_idle_audit_notes`: 抽检结论摘要

未降级 status / pipeline_stage：抽检通过，仅 1 处结构修复，无需回到 ready-for-review / needs-rework。

## 正文变更

- 插入 1 行 `<!-- outline-end -->` 标记 + 1 空行（共 2 行净增），位于 outline 区与第一节正文之间。
- 无任何技术论点、代码示例、表格、API 标注的修改。

## 校验

- check-metadata.py：通过（frontmatter 完整）。
- check-summary-links.py：通过。
- git diff --check：无空白错误。
