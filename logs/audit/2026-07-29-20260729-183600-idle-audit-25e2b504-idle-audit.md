# AIW idle audit · 2026-07-29

- run_id: `20260729-183600-idle-audit-25e2b504`
- mode: `idle-audit`
- target: `src/part2-performance/ch18-rendering-pipelines/14-camera-pipeline.md`
- title: Camera 渲染管线
- status: patched (frontmatter double-escape fix + missing fields)

## 抽检范围

- Android 18 / API 38+ 越界：未发现。全文锚定 Android 17 / API 37，AOSP `android-17.0.0_r1`，kernel `android17-6.18-2026-06_r6`。版本演进小节（L353-364）从 Android 12 逐版本到 Android 17，无 API 38+ 结论。
- Android baseline：正文固定到 `android-17.0.0_r1`，源码索引（L366-386）全部指向该 tag。未引入 Android 18/API 38+ 主线结论。
- 待验证残留：未发现 TODO / FIXME / 待验证 / 占位类残留。
- 来源标记：frontmatter 缺少 `last_verified` / `confidence`（pre-run quality_flags 报告的 `missing-last_verified`、`missing-confidence`）。本次补齐。正文末尾已列出完整的 Android 17 AOSP 源码索引（frameworks/base Java、frameworks/av C++、hardware/interfaces AIDL、frameworks/native SurfaceFlinger、kernel dma-buf/sync_file），来源充分。`thin-source-marking` 指向可选 `sources:` 块缺失；正文已有完整源码索引，不强制添加 frontmatter `sources:` 以避免冗余。
- frontmatter 缺项：补齐 `last_verified`、`last_verified_against`、`confidence`、`last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_result`。
- **frontmatter 双重转义引号（本次主要发现）**：11 个字段的 YAML 值被双重引号包裹，导致解析后值包含多余的 `"` 字符。例如 `title: "\"Camera 渲染管线\""` 解析为 `"Camera 渲染管线"`（带引号），而非 `Camera 渲染管线`。本次全部修复：
  - `title`、`chapter`、`section`、`applicable_versions`、`path`、`related_chapters`、`created_by`、`created_date`、`last_task9_at`、`last_task9_audit`、`last_task9_audit_log`
  - 另修复 `reviewed_date` 和 `last_task6_at` 的多余单引号嵌套（`"'2026-06-23'"` → `"2026-06-23"`）。
  - `related_chapters` 从字符串 `"[\"2.13\", ...]"` 修正为正确的 YAML 列表 `["2.13", "2.15", "14.9", "18.6"]`。
- 技术准确性抽检：
  - HAL3 request-result 多消费者模型描述准确（CaptureRequest / CaptureResult / StreamBuffer 生命周期）。
  - Framework-managed 与 HAL buffer management 两条路径区分清晰（`getBufferLockedCommon()` / `requestStreamBuffers()`）。
  - `maxBuffers` 为 HAL configure 阶段返回值、非固定常量的描述正确。
  - 三类 fence（acquire / release / SF-HWC release）的区分与 consumer 对应关系准确。
  - ZSL 两种机制（device-operated `CONTROL_ENABLE_ZSL` vs application-operated reprocess）区分清晰。
  - Stream Use Case（API 33）与 `CONTROL_CAPTURE_INTENT` 的分工描述准确。
  - 版本演进从 Android 12 到 17，逐版本 API level 核对一致（31/33/34/35/36/37）。
  - 源码链接均指向 `android-17.0.0_r1` tag。
  - 未发现可安全修复的技术错误。

## 处理结论

本次做安全补丁：
1. 修复 11 个 frontmatter 字段的双重转义引号 + 2 个字段的多余单引号嵌套。
2. 补齐抽检元数据（`last_verified`、`last_verified_against`、`confidence`）及 idle audit 跟踪字段（`last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_result`）。

未改动技术内容、未新增章节、未编辑 `src/SUMMARY.md`。章节保持 `finalized` / `ready-to-publish`，未降级。

## 实际补丁

- 仅修改既有章节：`src/part2-performance/ch18-rendering-pipelines/14-camera-pipeline.md`
- frontmatter 修复 13 个字段 + 新增 6 个字段。
- 正文未修改。
- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。

## 本地校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch18-rendering-pipelines/14-camera-pipeline.md` → 1 文件, 0 失败, 1 警告（可选 `sources:` 字段，正文已有源码索引，非阻塞）。
- `python3 scripts/check-summary-links.py` → 通过（165 链接，2 个无关预存重复警告）。
- `git diff --check` → 无空白错误。
