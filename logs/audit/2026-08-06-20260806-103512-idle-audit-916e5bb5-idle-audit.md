# AIW Idle Audit Log · 2026-08-06

- run_id: `20260806-103512-idle-audit-916e5bb5`
- mode: `idle-audit`
- target: `src/part1-fundamentals/ch01-architecture/1.49-Android-17-Staged-Install-状态机-提交-恢复链路.md`
- baseline: `android-17.0.0_r1` / Android 17 (API 37)
- status: `completed`

## 抽检范围

- Android 18/API 38+ 越界结论：未发现正文引入 Android 18/API 38+ 主线结论。
- 待验证残留：未发现 `[待验证]`、TODO/FIXME 类残留。
- 来源约束：frontmatter `sources` 指向两份 Android 17 Staged Install DeepResearch；pre-run 中的科研 Agent 材料非 AIW 路由材料，未采用。
- frontmatter：刷新 `last_verified`、`last_idle_audit_at`、`last_idle_audit_run_id`；保持 `status: finalized`、`pipeline_stage: finalized`、`task9_state: reviewed`。

## 修复内容

发现折叠的历史源码补充区仍有两处容易误导读者的旧口径：

1. 状态图把 `commit()` 后写成 `[STAGED: mSessionReady=true] ← commitSession() 触发`。
2. 状态公式把 `mSessionReady && !applied && !failed && !destroyed` 解释成“安装进行中（中间态）”。

按章节正文修订版和 `android-17.0.0_r1` 口径，已改为：

- `commitSession()` 仅记录 session / 纳入 StagingManager；
- `mSessionReady` 表示 pre-reboot verification 已通过，等待重启/后续应用，不等价于“安装进行中”。

## Issue pool

- added+closed: `AIW-20260806-1-49-ready-semantics` (`fixed`)

## 校验

已执行通过：

- `python3 scripts/check-metadata.py --files src/part1-fundamentals/ch01-architecture/1.49-Android-17-Staged-Install-状态机-提交-恢复链路.md` → 1 个文件通过，0 失败。
- `python3 scripts/check-summary-links.py` → 661 local links passed，0 duplicate warnings。
- `git diff --check` → passed。

备注：工作区存在其他 lane 预先留下的 `src/part4-system/ch17-oem/03-industry-cases.md` diff；本 idle-audit run 未修改该文件，本轮正文只触达指定 target。
