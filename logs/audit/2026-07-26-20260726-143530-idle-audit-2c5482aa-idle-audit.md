# AIW idle audit · 2026-07-26

- run_id: `20260726-143530-idle-audit-2c5482aa`
- mode: `idle-audit`
- target: `src/part2-performance/ch18-rendering-pipelines/11-angle-gles-vulkan.md`
- status: `completed`
- baseline: `android-17.0.0_r1` / Android 17

## 抽检输入

上游质量标记：

- `thin-source-marking`
- `missing-last_verified`
- `missing-confidence`

## 抽检结论

本次只处理指定既有章节，未新增 `src/**/*.md`，未修改 `src/SUMMARY.md`。

未发现 Android 18 / API 38+ 越界结论。章节保留 `finalized` / `ready-to-publish` / `task9_state: reviewed`。

## 实际修复

1. Frontmatter 补充 `last_verified: 2026-07-26`、`confidence: high`、`last_idle_audit_*` 和 `idle_audit_result`。
2. Sources 从泛化的 `AOSP external/angle/` 补强到 android-17.0.0_r1 的 `GraphicsEnvironment.java`、`Loader.cpp`、`SyncVk.cpp`。
3. 移除/改写待验证或过强表达：
   - 删除“cold compile 单 shader 5-30ms（未经源码验证）”式外推，改为要求按目标设备 cold / warm trace 对比。
   - 将 `PackedSPIRVBlockEncoder` 相关内容限定为 ANGLE 内部编码细节，避免误导成 App ABI 的固定偏移结论。
   - 删除私有 bug 状态式断言，改为排查 APK native library 路径、loader 日志和进程 maps。
   - 将 Android 16 口径同步到 Android 16/17 或 android-17.0.0_r1 基线。
   - 将 present 路径中的 `Swappy` 改为更通用的 `ANativeWindow / BufferQueue`。

## 校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch18-rendering-pipelines/11-angle-gles-vulkan.md` ✅ 1 个通过，0 失败，0 警告
- `python3 scripts/check-summary-links.py` ✅ passed（保留既有 2 条 duplicate warning；本 lane 未改 SUMMARY）
- `git diff --check` ✅
