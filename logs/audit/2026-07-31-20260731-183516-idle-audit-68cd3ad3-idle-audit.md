# Idle Audit · 13.10 Perfetto SQL 性能分析实战手册

- **Run ID**: `20260731-183516-idle-audit-68cd3ad3`
- **Lane**: `aiw-polish-idle-audit`
- **Date**: 2026-07-31
- **Target**: `src/part3-tools/ch13-perfetto/10-perfetto-sql-cookbook.md`
- **Mode**: idle-audit (sampling audit of finalized chapters)
- **Pre-run quality_flags**: `thin-source-marking`, `missing-last_verified`, `missing-confidence`

## 结论

**抽检通过，正文未修改。** 仅更新 frontmatter 审计字段：新增 `task9_state: reviewed`、`last_verified: 2026-07-31`、`confidence: high`、`last_idle_audit_at`、`last_idle_audit_run_id`。

## 检查项与结果

### 1. Android 18 / API 38 越界检查 — ✅ 通过

全文 grep `Android 18`、`API 38`、`API38`、`android-18`、`android18`：零命中。
基线统一为 `android-17.0.0_r1` / `android17-6.18-2026-06_r6`，正文锚点（第 23 行）与源码索引（第 1056–1064 行）均使用同一 tag，无越界内容。

### 2. 待验证残留标记检查 — ✅ 通过

全文 grep `TBD`、`TODO`、`FIXME`、`待补充`、`待确认`、`待验证`、`XXX`、`HACK`：零命中。无残留验证标记。

### 3. 来源标记检查 — ✅ 通过（flag 不成立）

`thin-source-marking` flag 不成立，来源标记非常充分：
- 正文第 23 行标注平台源码锚点（`android-17.0.0_r1`）与内核锚点（`android17-6.18-2026-06_r6`）；
- 末尾「源码与文档依据」含 15 条深链接，全部锚定到 `android-17.0.0_r1` 的 Perfetto stdlib 源码或 kernel `sched.h` tracepoint 定义；
- 7 个 `INCLUDE PERFETTO MODULE` 声明（android.frames.timeline、android.frames.per_frame_metrics、android.binder、android.garbage_collection、android.startup.startups、android.startup.time_to_display、android.anrs、android.monitor_contention），均对应标准库模块源码链接。

### 4. Frontmatter 完整性 — ✅ 通过（已补充审计字段）

必填字段齐全。审计字段已补：
- `missing-last_verified` → 新增 `last_verified: 2026-07-31`
- `missing-confidence` → 新增 `confidence: high`
- 新增 `task9_state: reviewed`（与同目录其他已审计 Perfetto 章节一致）
- 新增 `last_idle_audit_at: 2026-07-31T18:35:16+08:00`
- 新增 `last_idle_audit_run_id: 20260731-183516-idle-audit-68cd3ad3`

### 5. 技术准确性抽检 — ✅ 通过

重点核查以下技术结论：

| 检查项 | 正文主张 | 判定 |
|---|---|---|
| `dur = -1` 处理 | 未闭合区间裁到 `trace_end()` | ✅ 正确（Trace Processor 约定） |
| 半开区间交集 | `a.ts < b.end_ts AND a.end_ts > b.ts` | ✅ 正确 |
| `upid`/`utid` 语义 | Trace Processor 内唯一标识，优先用于关联 | ✅ 正确 |
| `thread_state` 区间语义 | `R`/`R+` = 可运行未获 CPU；`D`+`io_wait=1` = 内核 I/O | ✅ 正确（与 sched.h tracepoint 编码一致） |
| FrameTimeline 起始版本 | Android 12 / API 31 | ✅ 正确 |
| `PERCENTILE` 用法 | `PERCENTILE(dur / 1e6, 90)` 等分位数 | ✅ 正确（Trace Processor 内建聚合函数） |
| `SPAN_JOIN` 非重叠前提 | 输入在每个分区内不能互相重叠；业务 slice 常嵌套不适用 | ✅ 正确 |
| `cpufreq` dur 推导 | 用 `LEAD(counter.ts)` 补出 dur，末点用 `trace_end()` | ✅ 正确 |
| GC `reclaimed_mb` 依赖 | 依赖 GC 前后堆计数器，缺失时为空 | ✅ 正确 |
| ANR 无统一 5 秒窗口 | Broadcast/Service/前台服务/看门狗规则不同，OEM 可改写 | ✅ 正确 |
| `monitor_contention.binder_reply_id` | 有值时持锁工作可能在 Binder 服务线程 | ✅ 正确 |
| `doFrame` GLOB 模式 | `GLOB 'Choreographer#doFrame*'` 因名称带帧号后缀 | ✅ 正确 |
| `android_frames` 多 layer 聚合 | 同一帧关联多个 layer，表会聚合；只选第一个 DrawFrame | ✅ 正确（与 timeline.sql 源码注释一致） |

### 6. 表达质量 — ✅ 通过

结构清晰：导读 → 分析口径 → 帧时间 → 调度 → Binder → GC → 启动 → ANR → 锁竞争 → SPAN_JOIN → 证据链。每段「指标必须带上分母和边界」的方法论提醒贯穿全文，SQL 模板均声明窗口值为查询参数而非平台标准。无 churn-worthy 表达问题。

## 无安全补丁说明

本章正文质量高，所有抽检项通过，无需对正文进行修改。正文已过 3 轮 openclaw 审计（git log 可见），无系统性文字损坏。仅更新 frontmatter 审计字段以记录本次 idle-audit 通过状态。

## Issue pool

本轮未新增 finding。该章节此前无 open finding，本轮也未发现需要记录的新问题。
