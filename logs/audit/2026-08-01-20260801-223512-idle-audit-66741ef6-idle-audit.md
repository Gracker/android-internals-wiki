# Idle Audit Log · 2026-08-01

- **Run ID**: `20260801-223512-idle-audit-66741ef6`
- **Mode**: idle-audit
- **Target**: `src/part2-performance/ch07-smoothness/09-perceived-smoothness.md` (Chapter 7.9)
- **Status**: finalized → finalized (pass, minor frontmatter fix)
- **Baseline**: `android-17.0.0_r1` / kernel `android17-6.18-2026-06_r6`
- **Quality flags at entry**: `pending-verification-marker`, `thin-source-marking`

## Audit scope

1. Android 18 / API 38 越界检查
2. 待验证残留标记检查 (pending-verification-marker)
3. 来源标记完整性检查 (thin-source-marking)
4. Frontmatter 缺项与结构问题
5. 技术准确性抽查 (OverScroller / Choreographer / InputConsumer / AppJankStats)
6. 交叉引用链接有效性
7. 代码块语言标记检查

## Findings

### F1: Frontmatter stray blank lines (fixed)

frontmatter 起始 `---` 与 `title:` 之间存在 2 个多余空行（原 line 2-3）。导致 metadata 结构不一致。已修复：删除多余空行。

**Severity**: L1 (cosmetic/structural)
**Status**: Fixed

### F2: Thin source metadata (fixed)

`sources:` 字段仅列出 Perfetto FrameTimeline 文档，但正文引用了 4 个 AOSP `android-17.0.0_r1` 源码锚点（OverScroller.java、Choreographer.java、AnimationUtils.java、InputConsumer.cpp）和 2 个官方 API（AppJankStats、RelativeFrameTimeHistogram）。已扩充 sources 列表以覆盖全部引用来源，并为 AOSP 源码标注 version。

**Severity**: L2 (source completeness — addresses `thin-source-marking`)
**Status**: Fixed

### F3: Missing idle audit metadata (fixed)

章节 frontmatter 缺少 `last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_result`、`last_idle_audit_notes` 字段。已补充。

**Severity**: L1 (metadata completeness)
**Status**: Fixed

### F4: `last_verified` staleness (fixed)

`last_verified` 为 `2026-07-09`。本次抽检以同一 baseline (`android-17.0.0_r1`) 重新核验正文源码锚点、版本边界表和交叉引用，未发现偏差，更新为 `2026-08-01`。

**Severity**: L2 (staleness)
**Status**: Fixed

### F5: `pending-verification-marker` check (pass)

全文无 `[待验证]` 残留。唯一出现位置在 outline-start 加工指引块（`无法确认的技术细节保留 [待验证]`），属于 OpenClaw 加工指引模板文字，非正文残留。

**Severity**: N/A
**Status**: Pass — no action needed

### F6: Android 18 / API 38 boundary check (pass)

全文版本引用最高为 Android 17 / API 37。版本边界表（"版本边界" 小节）终点为 Android 17 / API 37。无 Android 18 或 API 38 越界结论。

**Severity**: N/A
**Status**: Pass — no action needed

### F7: Cross-reference link validity (pass)

检查了 6 个本地交叉引用：
- `01-jank-definition.md` ✓
- `03-jank-methodology.md` ✓
- `08-recyclerview-performance.md` ✓
- `../../part1-fundamentals/ch03-input/04-input-latency-prediction.md` ✓
- `../ch18-rendering-pipelines/19-variable-refresh-rate.md` ✓
- `../ch18-rendering-pipelines/02-android-view-standard.md` ✓

全部存在。

**Severity**: N/A
**Status**: Pass — no action needed

### F8: Technical spot-check (pass)

按 `android-17.0.0_r1` 抽查以下技术锚点：
- `SplineOverScroller.update()` 使用 `AnimationUtils.currentAnimationTimeMillis()` → 整数毫秒 ✓
- `Choreographer.doFrame()` 保留纳秒 `frameTimeNanos` ✓
- `AnimationUtils.lockAnimationClock(frameTimeNanos / NANOS_PER_MS)` ✓
- `InputConsumer.cpp` 中 `RESAMPLE_LATENCY = 5 ms`、`RESAMPLE_MAX_PREDICTION = 8 ms` ✓
- `AppJankStats` / `RelativeFrameTimeHistogram` 为 Android 16 / API 36 引入 ✓
- `View.setFrameContentVelocity()` 为 API 35 ✓
- `Choreographer.VsyncCallback` / `FrameData` 为 API 33 ✓
- FrameTimeline 从 Android 12 / API 31 起可用 ✓

版本边界表与正文叙述一致。

**Severity**: N/A
**Status**: Pass — no action needed

## Diff summary

- 1 file changed, 23 insertions(+), 5 deletions(-)
- 正文（body text）未改动；仅 frontmatter metadata 修复

## Validation

- `python3 scripts/check-metadata.py --files <target>`: ✅ pass (1/1)
- `python3 scripts/check-summary-links.py`: ✅ pass (164 links, 2 pre-existing duplicate warnings unrelated to target)
- `git diff --check`: ✅ clean (exit 0)
- YAML frontmatter parse: ✅ valid (52 keys, sources list with 7 entries)

## Conclusion

章节技术内容扎实，源码锚点准确，无安全需补丁的技术问题。本次仅做 frontmatter 卫生修复（空行清理、sources 扩充、idle audit 元数据补充、last_verified 更新）。正文未改动。维持 `status: finalized` / `pipeline_stage: ready-to-publish`。
