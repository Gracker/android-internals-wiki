# Idle Audit Log · 2026-07-30

- **run_id**: `20260730-223534-idle-audit-4ec8c98d`
- **mode**: idle-audit
- **lane**: aiw-polish-idle-audit
- **target**: `src/part3-tools/ch13-perfetto/14-perfetto-data-explorer-jank-cuj.md`
- **sha16**: `05bd956db73a0626`
- **audit_date**: 2026-07-30

## 抽检范围

1. Android 18 / API 38+ 越界结论检查
2. 待验证残留（pending-verification-marker）
3. 来源标记不足（thin-source-marking）
4. frontmatter 缺项 / 状态一致性
5. 明显技术错误 / 表达问题
6. v54 schema 变更、CUJ counter API、JankTracker 锚点等技术口径

## 发现与处置

### P1 · 技术错误（已修复）

**位置**: 第 129 行（"第三方 App 扩展路径" → AndroidX JankStats）

- **问题**: 正文写 `JankStats.createJankStats()`，但 AndroidX `androidx.performance:performance-jankstats` 不存在该方法。正确入口是 `JankStats.createAndTrack(window)`。
- **证据**:
  - 同章节第 158 行代码示例已正确使用 `JankStats.createAndTrack(window)`。
  - DeepResearch 2026-05-22 与 2026-06-15 两份材料均使用 `JankStats.createAndTrack(window)`。
  - AndroidX 官方 API：`androidx.benchmark:performance-jankstats` 仅提供 `createAndTrack(Window)` 工厂方法。
- **修复**: `JankStats.createJankStats()` → `JankStats.createAndTrack(window)`
- **风险**: 极低。仅修正 API 方法名，不改变章节结构、口径或结论。

### 通过项（未修改）

| 检查项 | 结论 |
|--------|------|
| Android 18 / API 38+ 越界 | ✅ 无。正文结论收窄到 Android 17 (API 37) / Perfetto v54.0；v55 仅作为范围边界出现 |
| v54.0 schema 变更 | ✅ stack_id / parent_stack_id 删除、machine_id 非空、metadata multi-trace 语义描述准确 |
| CUJ counter API 数据流 | ✅ `Trace.beginSection()` 只写 slice、`Trace.setCounter("J<*>#*", value)` 写 counter 的描述正确（7-11 已修复并复检） |
| `_is_jank_slice` 两层进程过滤 | ✅ base.sql GLOB + counters.sql cujs_ordered 回溯窗口描述与 v54.0 源码一致 |
| FrameTracker / JankTracker 数据流 | ✅ ViewRootImpl → Choreographer → JankTracker::finishFrame → SurfaceFlinger FrameTimeline 链路锚点正确 |
| statsd atom 标识 | ✅ 已使用 `UI_INTERACTION_FRAME_INFO_REPORTED` + `missed_frames`/`app_missed_frames`/`sf_missed_frames` 字段 |
| weighted jank counter 转换口径 | ✅ 除以 1000 + 乘 anim_duration_ms / 1000 描述正确 |
| JankType 阈值表 | ✅ kSlowRT 0.75× 与 android-15→16 边界一致 |

### 质量标记评估

- **pending-verification-marker**: sched_ext 调度器影响帧时间的备注（第 230 行）是诚实的待验证声明，素材也未给出确定结论。保留不改，符合"标注采集缺口"原则。
- **thin-source-marking**: frontmatter sources 已有 8 条（官方 release / 源码文件 / 文档 / 调研），来源充分，标记可视为已解决。

## frontmatter 更新

新增字段：
- `last_idle_audit_at: "2026-07-30"`
- `last_idle_audit_run_id: "20260730-223534-idle-audit-4ec8c98d"`
- `last_idle_audit_result: "pass-with-1-safe-fix"`
- `updated_by: "aiw-polish-idle-audit"`
- `updated_date: "2026-07-30"`

状态未降级：`status: finalized`、`task9_state: reviewed`、`pipeline_stage: ready-to-publish` 保持不变（仅 1 处 API 名修正，不触发回审）。

## 校验结果

- `python3 scripts/check-metadata.py --files <target>`: ✅ 通过
- `python3 scripts/check-summary-links.py`: ✅ 通过（2 处 duplicate 警告为既有问题，非本次引入，且 lane 规则禁止编辑 SUMMARY.md）
- `git diff --check`: ✅ 无空白错误

## 结论

闲时抽检通过并修复 1 处安全的 API 方法名错误（P1）。章节正文技术口径、版本边界、源码锚点均符合 Android 17 / Perfetto v54.0 基线。不触发降级，不需要回审。
