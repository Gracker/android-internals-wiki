# 闲时抽检 (Idle Audit) · 2026-07-30 10:35

- run_id: `20260730-103507-idle-audit-916e5bb5`
- lane: aiw-polish-idle-audit
- 章节：1.49 Android 17 Staged Install 状态机与提交/恢复链路
- 文件：`src/part1-fundamentals/ch01-architecture/1.49-Android-17-Staged-Install-状态机-提交-恢复链路.md`
- 前置状态：finalized（deep-review + review-finalize 均已通过）
- 本次状态：finalized（维持；修复 1 处事实残留，无降级）

## 抽检范围

逐行核对 621 行全文 + 2 份 DeepResearch 调研报告，覆盖以下维度：

1. **Android 18 / API 38+ 越界检查**
2. **待验证残留 / pending-verification-marker**
3. **来源标记 / thin-source-marking**
4. **frontmatter 完整性**
5. **技术准确性（与 `android-17.0.0_r1` 源码及 DeepResearch 报告交叉验证）**

## 发现与修复

### [P1] 事实错误：APEX 成功标记被描述为"避免逐 session 跨进程 RPC"（已修复）

**位置**：line 303，源码级补充区块「性能调优点（实测可观测）」第 1 条。

**问题**：原文称 `markStagedSessionsAsSuccessful()` "一次性遍历 `mSuccessfulStagedSessionIds` 通知 apexd，**避免逐 session 跨进程 RPC**"。

**核实**：同一文件 line 578-582（2026-07-30 源码复核新增区块）已明确修正此语义：
> "到 `PHASE_BOOT_COMPLETED`，`markStagedSessionsAsSuccessful()` 遍历列表，并**对每个 id 分别调用** `mApexManager.markStagedSessionSuccessful()`。"
> "这是一处延迟确认，**不是把多个 id 合成一次 apexd RPC**。"

DeepResearch 报告（2026-07-02）line 418-426 源码亦确认逐 id 调用。原文 line 303 的"避免逐 session 跨进程 RPC"与新修正区块直接矛盾，属于源码级补充区块内遗留的旧错误描述。

**补丁**：将 line 303 改为：
> "`markStagedSessionsAsSuccessful()`（line 425）在 `PHASE_BOOT_COMPLETED` 遍历 `mSuccessfulStagedSessionIds`，逐 id 调用 `markStagedSessionSuccessful()` 做成功确认。这是延迟确认而非合成单次 RPC，APEX activation 本身已在重启过程由 apexd 完成。"

### 抽检通过项（无问题）

| 维度 | 结论 |
|------|------|
| Android 18 / API 38+ 越界 | ✅ 无（仅审计表中提及"无 Android 18 结论"作为检查记录） |
| 版本边界 | ✅ 全文锚定 `android-17.0.0_r1` / API 37，源码链接指向 googlesource `android-17.0.0_r1` ref |
| 四布尔状态位语义 | ✅ 折叠区 line 159-206 与修正区 line 488-499 一致，setter destroy 行为正确 |
| commit → ready 调用链 | ✅ line 323-345 六跳 handler 链与 DeepResearch 2026-07-02 报告一致 |
| endVerification() 顺序 | ✅ line 356-360 "先 setSessionReady() 再 markStagedSessionReady()" 与源码注释一致 |
| restore 链路 | ✅ line 362-386 PMS.systemReady → restoreAndApplyStagedSessionIfNeeded → restoreSessions 与报告一致 |
| APK-only vs Multi-package 差异表 | ✅ line 388-398 逐项与源码对应 |
| 失败回滚路径 | ✅ line 255-279 + 584-594 checkpoint/非 checkpoint 分支描述准确 |
| applied 来源 | ✅ line 572 明确"applied 来自 install completion，非 resumeSession 直接置位" |
| shell 命令引用 | ✅ `pm list staged-sessions` / `pm get-stagedsessions` 为 PackageManagerShellCommand 真实命令 |
| frontmatter | ✅ status/confidence/pipeline_stage/task6_state/task9_state/sources 均完整；本次新增 last_idle_audit_at / last_idle_audit_run_id |

### 折叠区遗留内容说明

line 28-482 的 `<details>` 折叠区为历史追溯保留的旧审计内容（已有明确横幅标注"仅供追溯"）。其中 line 80（"APEX 延迟激活（boot 完成后批量标记）"）和 line 100（"批量通知 apexd"）存在与修正区相同的旧描述，但它们位于已弃用的折叠区内，顶部有显著的阅读指引指向修正版。按 idle-audit "不 churn 正文"原则，折叠区追溯内容不再逐字修改——可引用的权威内容已在折叠区之后的修正区块中。

## 统计

- P0 事实错误：0
- P1 重要残留：1 → 0（已修复）
- P2 建议改进：0
- 总体技术评分：4.4/5（修复前 4.2/5）

## 闭环动作

- 修复 1 处 P1 事实残留（line 303 APEX 成功标记语义）。
- 更新 frontmatter：新增 `last_idle_audit_at: 2026-07-30`、`last_idle_audit_run_id: 20260730-103507-idle-audit-916e5bb5`。
- 状态维持 finalized，未降级。
- 未新增章节，未修改 `src/SUMMARY.md`。
- 未运行 `aiw-git-sync.py`（交由后续 sync cron 批量提交）。
