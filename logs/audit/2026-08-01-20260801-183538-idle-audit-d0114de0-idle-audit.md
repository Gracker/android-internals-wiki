# Idle Audit Log · 2026-08-01

- **Run ID**: `20260801-183538-idle-audit-d0114de0`
- **Mode**: idle-audit
- **Target**: `src/part1-fundamentals/ch03-input/01-input-dispatch.md` (Chapter 3.1)
- **Status**: finalized → finalized (pass, minor frontmatter fix)
- **Baseline**: `android-17.0.0_r1` / kernel `android17-6.18-2026-06_r6`
- **Quality flags at entry**: `pending-verification-marker`, `thin-source-marking`

## Audit scope

1. Android 18 / API 38 越界检查
2. 待验证残留标记检查 (pending-verification-marker)
3. 来源标记完整性检查 (thin-source-marking)
4. Frontmatter 缺项与结构问题
5. 技术准确性抽查
6. 代码块语言标记检查

## Findings

### F1: Frontmatter stray blank line (fixed)

`last_task6_audit: "2026-06-20"` 前存在空行（line 73），导致 frontmatter 结构不一致。虽然 YAML 解析器仍可正确解析，但属于 metadata 卫生问题。已修复：删除空行，紧接上一字段。

**Severity**: L1 (cosmetic/structural)
**Status**: Fixed

### F2: Missing idle audit metadata (fixed)

章节 frontmatter 缺少 `last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_notes` 字段。已补充。

**Severity**: L1 (metadata completeness)
**Status**: Fixed

### F3: `last_verified` 过期 (fixed)

`last_verified` 为 `2026-07-25`，本次抽检以同一 baseline 重新核验，更新为 `2026-08-01`。

**Severity**: L2 (staleness)
**Status**: Fixed

### Q1: pending-verification-marker (flag resolved)

扫描正文中的 `待验证`、`TODO`、`FIXME`、`TBD`、`待确认` 关键词，仅找到 frontmatter review_notes 中历史引用（已标记为已解决/已清理），正文无残留待验证标记。**flag 不再适用**。

### Q2: thin-source-marking (flag resolved)

`sources` frontmatter 已包含 8 条 AOSP/kernel 源码路径（InputManager.cpp、EventHub.cpp、InputReader.cpp、InputDispatcher.cpp、InputTransport.cpp、ViewRootImpl.java、ViewGroup.java、evdev.c）。正文末尾"源码阅读入口"节列出 13 个入口文件并标注各自职责。来源标记完整，**flag 不再适用**。

### A1: 无 Android 18/API38 越界

扫描 `Android 18`、`API 38`、`API38`、`Android18`，正文无匹配。版本边界表止于 Android 17 / API 37。版本锚点正确。

### A2: 无技术错误

抽查以下关键声明，均与 android-17.0.0_r1 源码形态一致：
- listener stage 链：UnwantedInteractionBlocker → PointerChoreographer → InputProcessor → ... → InputDispatcher ✓
- `SOCK_SEQPACKET` InputTransport ✓
- `UNMULTIPLIED_DEFAULT_DISPATCHING_TIMEOUT_MILLIS = 5000` AIDL 常量 ✓
- `HwTimeoutMultiplier()` 乘数语义 ✓
- `processPreAnrsLocked()` / pre-ANR flag ✓
- `android.input.inputevent` Perfetto data source ✓
- `SetTaskProfiles(0, {"InputPolicy"})` 替代固定 nice 值 ✓
- 7 处 ```text 代码块均为流程/结构示意，语言标记正确 ✓

## Changes applied

| Line | Change |
|---|---|
| 12 | `last_verified: '2026-07-25'` → `'2026-08-01'` |
| 73 | 删除 frontmatter 中的 stray blank line |
| 74-76 | 新增 `last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_notes` |

## Validation

- `check-metadata.py`: ✅ 1/1 passed
- `check-summary-links.py`: ✅ passed (2 pre-existing duplicate warnings, unrelated)
- `git diff --check`: ✅ clean

## Conclusion

章节质量高，技术内容准确，无 P0/P1 问题。本次抽检仅修复 3 处 frontmatter 卫生项（L1/L2），未改动正文。quality_flags 中两个 flag 经核查已不适用。维持 `status: finalized`，`pipeline_stage: ready-to-publish`。
