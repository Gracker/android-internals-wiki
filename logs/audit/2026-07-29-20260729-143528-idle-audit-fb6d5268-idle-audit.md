# AIW idle audit · 2026-07-29

- run_id: `20260729-143528-idle-audit-fb6d5268`
- mode: `idle-audit`
- target: `src/part2-performance/ch18-rendering-pipelines/10-surface-control-api.md`
- title: SurfaceControl API 深入
- status: patched (frontmatter + minor CJK spacing)

## 抽检范围

- Android 18 / API 38+ 越界：未发现。全文锚定 Android 17 / API 37，AOSP `android-17.0.0_r1`。frontmatter `task9_review_notes` 中 "Android 18" 仅出现于历史审计记录文本，非正文结论，无越界。
- Android baseline：正文固定到 `android-17.0.0_r1`，源码索引（L623-L629）全部指向该 tag。未引入 Android 18/API 38+ 主线结论。
- 待验证残留：未发现 TODO / FIXME / 待验证 / 占位类残留。
- 来源标记：frontmatter 之前缺少 `last_verified` / `confidence`（pre-run quality_flags 报告的 `missing-last_verified`、`missing-confidence`）。本次补齐。正文末尾已列出 7 条 Android 17 AOSP 源码锚点链接（surface_control.h / surface_control_jni.h / surface_control.cpp / SurfaceControl.java / BLASTBufferQueue.cpp / SurfaceComposerClient.cpp / SurfaceFlinger.cpp + FrameTimeline.cpp），来源充分。`thin-source-marking` 指向可选 `sources:` 块缺失；正文已有完整源码索引，不强制添加 frontmatter `sources:` 以避免冗余。
- frontmatter 缺项：补齐 `last_verified`、`last_verified_against`、`confidence`、`last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_result`，消除 `missing-last_verified` 与 `missing-confidence` flag。
- 表达问题：发现 1 处 CJK/英文间距缺陷（L573 `FrameTimeline解释` → `FrameTimeline 解释`），已修复。
- 技术准确性：
  - 五类对象职责划分（ANativeWindow / ASurfaceControl / ASurfaceTransaction / AHardwareBuffer / sync fence fd）准确。
  - API 级别声明逐项核对：createFromWindow/create (29)、setBuffer/color/alpha/dataspace (29)、setDesiredPresentTime (29)、setFrameRate (30)、crop/position/scale/transform (31)、setOnCommit/backpressure (31)、setFrameTimeline (33)、fromJava/clearFrameRate (34)、setDesiredHdrHeadroom (35)、setBufferWithRelease/LUT (36)。与 AOSP surface_control.h (android-17.0.0_r1) 一致。
  - BLAST 与 NDK SurfaceControl 事务路径区分准确（BufferQueue 消费 vs 直接 setBuffer）。
  - eFXSurfaceBufferState 标志描述正确。
  - Fence 所有权转移语义（acquire fd 传入后不可再 close / 需 dup）准确。
  - OnCommit / OnComplete / OnBufferRelease 回调边界与 API 级别对应正确。
  - FrameTimeline NDK 流程（AChoreographer_postVsyncCallback / setFrameTimeline）API 33 起开放，与 choreographer.h 一致。
  - 跨进程边界（Parcelable / SurfaceControlViewHost / SurfacePackage / SurfaceSyncGroup）描述准确。
  - WebView 独立 Layer 条件描述严谨（renderer 独立 ≠ 网页主体必然独立 Layer）。
  - 未发现可安全修复的技术错误。

## 处理结论

本次做安全补丁：
1. frontmatter 补齐抽检元数据（`last_verified`、`last_verified_against`、`confidence`）及 idle audit 跟踪字段（`last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_result`）。
2. 修复 L573 CJK/英文间距（`FrameTimeline解释` → `FrameTimeline 解释`）。

未改动技术内容、未新增章节、未编辑 `src/SUMMARY.md`。章节保持 `finalized` / `ready-to-publish`，未降级。

## 实际补丁

- 仅修改既有章节：`src/part2-performance/ch18-rendering-pipelines/10-surface-control-api.md`
- frontmatter 新增 6 个字段 + 1 处正文 CJK 间距修正。
- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。

## 本地校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch18-rendering-pipelines/10-surface-control-api.md` → 1 文件, 0 失败, 1 警告（可选 `sources:` 字段，正文已有源码索引，非阻塞）。
- `python3 scripts/check-summary-links.py` → 通过（165 链接，2 个无关预存重复警告）。
- `git diff --check` → 无空白错误。
