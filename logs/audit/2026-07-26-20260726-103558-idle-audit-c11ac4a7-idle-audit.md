# AIW idle audit · 2026-07-26 · 20260726-103558-idle-audit-c11ac4a7

- lane: idle-audit
- mode: idle-audit
- target: `src/part2-performance/ch18-rendering-pipelines/19-variable-refresh-rate.md`
- chapter: 18.19 可变刷新率渲染管线
- status: completed
- result: safe patch applied

## 抽检范围

- Android 18 / API 38 越界扫描：未发现。
- 待验证残留扫描：未发现 `TODO` / `TBD` / `待验证` / `FIXME` 等残留。
- frontmatter：补齐 `last_verified`、`last_verified_against`、`confidence`、`sources`、`last_idle_audit_at`、`last_idle_audit_run_id`、`last_idle_audit_log`。
- 来源标记：由原先仅文末参考资料和 android-16.0.0_r4 历史复核，补为 android-17.0.0_r1 AOSP + Android Developers + Perfetto stdlib 的机器可读 `sources`。
- 技术锚点：通过 AOSP Gitiles android-17.0.0_r1 抽查 `Scheduler.cpp` 与 `RefreshRateSelector.cpp`。

## 实际修复

1. `RefreshRateSelector.cpp` 中单 Layer 评分函数名修正为 `calculateLayerScoreLocked()`，避免旧段落写成不存在/不精确的 `calculateLayerScore()`。
2. 源码锚点段落从 `android-16.0.0_r4` 历史复核升级为本轮 `android-17.0.0_r1` 基线，并补全 `frameworks/native/services/surfaceflinger/Scheduler/...` 路径。
3. 保留 `status: finalized`、`task9_state: reviewed`、`pipeline_stage: ready-to-publish`，未发现需要降级为 `ready-for-review` 或 `needs-rework` 的 P0/P1 问题。

## android-17.0.0_r1 抽查证据

- `Scheduler.cpp`: `Scheduler::chooseRefreshRateForContent()` 存在；同文件后续通过 `selectorPtr->getRankedFrameRates(...)` 获取排序候选。
- `RefreshRateSelector.cpp`: `RefreshRateSelector::getRankedFrameRates(...)` / `getRankedFrameRatesLocked(...)` 存在；`calculateLayerScoreLocked(...)` 存在。
- `RefreshRateSelector.cpp`: 排序循环中 `layer.isNoVote()`、`FrameRateCategory::NoPreference`、`LayerVoteType::Min` 会跳过评分。
- `Scheduler.cpp`: `FPS_THRESHOLD_FOR_KERNEL_TIMER = 65_Hz` 存在；`RefreshRateSelector.cpp` dump 中仍有 `KernelIdleTimerController`。

## 后续

无需写入 review findings；本轮为安全修复并通过抽检，无新增 P0/P1 finding。
