# Idle Audit · 5.21 Android 17 SoC 厂商电池优化架构

- **Run ID**: `20260731-143544-idle-audit-14162f29`
- **Lane**: `aiw-polish-idle-audit`
- **Date**: 2026-07-31
- **Target**: `src/part1-fundamentals/ch05-cpu-power/5.21-android17-battery-optimization-soc-architecture.md`
- **Mode**: idle-audit (sampling audit of finalized chapters)
- **Pre-run quality_flags**: `pending-verification-marker`, `thin-source-marking`

## 结论

**抽检通过，正文未修改。** 仅更新 frontmatter 审计字段（`last_verified` → 2026-07-31，新增 `last_idle_audit_at` / `last_idle_audit_run_id`）。

## 检查项与结果

### 1. Android 18 / API 38 越界检查 — ✅ 通过

全文 grep `Android 18`、`API 38`、`API38`、`android-18`、`android18`：零命中。
基线统一为 `android-17.0.0_r1` / `android17-6.18-2026-06_r6`，无越界内容。

### 2. 待验证残留标记检查 — ✅ 通过（flag stale）

`pending-verification-marker` flag 已过时：
- `last_verified` 此前为 `2026-07-27`，review-finalize 已复查；
- 正文中唯一 "待验证" 出现在第 301 行 `一个待验证因素`，这是方法论语境（"a variable to be verified"），不是残留验证标记；
- 无 `TBD`、`TODO`、`FIXME`、`待补充`、`待确认` 等残留。

### 3. 来源标记检查 — ✅ 通过（flag acceptable）

`thin-source-marking` flag 可接受：
- frontmatter `sources` 列出两条高层次基线标签（`android-17.0.0_r1` + `android17-6.18-2026-06_r6`）；
- 正文末尾源码索引含 7 条 cs.android.com / googlesource.com 深链接，覆盖 PowerManagerService、JNI、PowerHalController、IPower.aidl、Power.cpp、IPowerStats.aidl、cpufreq_schedutil.c；
- 内联代码块标注基线 tag，引用风格与全 Wiki 一致。
- 本章聚焦接口/源码边界，不引用外部素材，标记厚度足够。

### 4. Frontmatter 完整性 — ✅ 通过（已补充审计字段）

必填字段齐全；审计字段已补：
- `last_verified`: 2026-07-27 → 2026-07-31
- 新增 `last_idle_audit_at`: 2026-07-31T14:35:44+08:00
- 新增 `last_idle_audit_run_id`: 20260731-143544-idle-audit-14162f29

### 5. 技术准确性抽检 — ✅ 通过

重点核查以下技术结论：

| 检查项 | 正文主张 | 判定 |
|---|---|---|
| `energyUWs` 单位 | 微瓦秒（μWs）= 微焦耳（μJ），非微库仑 | ✅ 正确（AIDL 字段名为 energyUWs，uW*s = μJ） |
| 平均功率公式 | `ΔenergyUWs / ΔtimeMs` → mW | ✅ 量纲正确（μJ/ms = 10⁻³ W = mW） |
| `rate_limit_us` 初始值 | `cpufreq_policy_transition_delay_us(policy)` | ✅ 与 android17-6.18 源码一致 |
| `IOWAIT_BOOST_MIN` | `SCHED_CAPACITY_SCALE / 8` | ✅ 正确（=128） |
| `readEnergyMeter()` vs FMQ | 普通 AIDL，非 FMQ | ✅ 正确（FMQ 仅用于 HintSession） |
| `vendor_available` 语义 | 构建可见性，非设备实现要求 | ✅ 正确 |
| Mode.LAUNCH 时长 | 可启停 Mode，无固定时长 | ✅ 正确 |
| Boost.aidl 枚举 | 含 `INTERACTION` 等，无 `LAUNCH` | ✅ 正确 |
| schedutil 不读电池百分比 | 无此通用路径 | ✅ 正确 |

### 6. 交叉引用检查 — ✅ 通过

- `related_chapters: ["5.18", "8.33"]` — 5.18（CPU 缓存局部性）和 8.33（模块化启动）均存在；
- 正文提及 "详见 5.9 和 5.19" — 5.9（ADPF 自适应性能框架）和 5.19（OnDevice AI ADPF 智能调度）均存在。

### 7. 表达质量 — ✅ 通过

结构清晰，outline 与正文结论同步，"常见误判"表格有效总结了 10 个典型误解，源码阅读顺序段落实用。无 churn-worthy 表达问题。

## 无安全补丁说明

本章正文质量高，所有抽检项通过，无需对正文进行修改。仅更新 frontmatter 审计字段以记录本次 idle-audit 通过状态。

## Issue pool

本轮未新增 finding。该章节此前无 open finding，本轮也未发现需要记录的新问题。
