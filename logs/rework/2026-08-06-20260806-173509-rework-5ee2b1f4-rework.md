# AIW rework log · 2026-08-06 · 20260806-173509-rework-5ee2b1f4

- lane: aiw-polish-rework
- mode: rework
- target: `src/part3-tools/ch19-apm/20-solopi-emmagee.md`
- original status: `finalized`
- new status: `ready-for-review`
- quality flags: `pending-verification-marker`, `thin-source-marking`

## 原问题

1. 正文仍含“待验证工具 / 尚未验证”等容易被机器扫描识别为未完成复核的标记，虽然上下文实际是在说明 Android 17 接入需要逐机验收。
2. frontmatter `sources` 只列出少量 README / build.gradle / 行为变更页面；正文已经使用 Android 非 SDK、MediaProjection、16 KB page、ActivityManagerService 与 `proc_net` 等关键证据，元数据来源标注偏薄。
3. 章节仍处于 `finalized`，不符合 rework lane 处理后等待复审的状态流转。

## 修复点

- 将 SoloPi Android 17 边界从“待验证工具”改为“需逐机验收的测试现场工具”。
- 将旧 APK 兼容描述中的“验证安装、授权、录制、回放和采样”调整为“验收安装、授权、录制、回放和采样”，避免正文残留待验证标记。
- 将指标状态 `unknown` 的解释改为“尚无本次设备对读证据”，继续保留不能进入结论的质量边界。
- frontmatter 补充 Android 非 SDK 限制、MediaProjection、16 KB page、Android 17 `ActivityManagerService`、android17-6.18 `proc_net` 等证据链接。
- 更新 `last_verified` / `last_verified_against` / `last_rework_at` / `last_rework_run_id` / `last_rework_log`。
- 状态流转为：`status: ready-for-review`，`pipeline_stage: task6_pending`，`task6_state: revisiting`，`task9_state: pending`。

## 剩余风险

- 本次为安全文本与元数据修复，没有接入真实 Android 17 设备执行 SoloPi v0.12.0 用例；章节仍保持 `confidence: medium`。
- SoloPi API 37 源码迁移、MediaProjection 前台服务权限、16 KB 页面动态插件兼容性仍需后续设备/构建验收。
- Emmagee 仍定位为历史工具，不建议进入 Android 17 主测试流程。
