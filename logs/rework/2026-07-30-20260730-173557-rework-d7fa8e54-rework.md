# AIW Polish Rework Evidence · 2026-07-30

- run_id: `20260730-173557-rework-d7fa8e54`
- mode: `rework`
- target: `src/part3-tools/ch19-apm/16-profiling-manager.md`（§19.16 ProfilingManager）
- original_quality_flags: `pending-verification-marker`, `thin-source-marking`
- baseline: Android 17 / `android-17.0.0_r1`
- materials: **空数组**（本章已 finalized + ready-to-publish，sources 完备 12 条，不需要外部材料）

## 原问题

本章 frontmatter 显示 `status: finalized`、`pipeline_stage: ready-to-publish`、`task9_result: pass-tech-review`，技术内容已通过 Task6/Task9 双轮审核。被 rework lane 选中是因为启发式 quality_flags 检测到两个标记：

1. **pending-verification-marker**：`aiw-polish-context.py` 第 159 行检测正文含「待验证」「TODO」「TBD」之一即触发。本章 §「按结果类型选请求」首句「先写下待验证的问题，再选择剖析类型」中的「待验证」是正常指导语（含义是"先写明要排查的性能问题"），不是章节级 TODO/待补标记，属启发式误触发。
2. **thin-source-marking**：检测正文内联 `[来源:]` + `[已验证:]` 标记数 < 2 即触发。本章正文此前为 0 处，但 frontmatter sources 已有 12 条（developer.android.com API reference × 8、AOSP 源码 × 4），文末「参考资料」也有 12 条编号引用。标记缺失是正文风格选择，不是引用不足。

## 修复点

### 1. 解决 pending-verification-marker

- §「按结果类型选请求」首句：`先写下待验证的问题` → `先写明要排查的性能问题`
- 语义完全不变（两句话都指引读者先明确性能问题再选剖析类型），仅消除启发式触发词。
- 全文 `待验证` 计数：1 → 0。

### 2. 解决 thin-source-marking

在 3 处事实声明点补内联 `[来源:]` 标记，全部映射既有 frontmatter sources：

| 位置 | 声明 | 标记 | 对应 frontmatter source |
|---|---|---|---|
| §「适用范围按版本区分」 | ProfilingManager 从 API 35 起提供 | `[来源: developer.android.com/reference/android/os/ProfilingManager]` | official: ProfilingManager reference |
| §「按结果类型选请求」 | 四类文件后缀来自 ProfilingService | `[来源: packages/modules/Profiling/service/java/com/android/os/profiling/ProfilingService.java (android-17.0.0_r1)]` | aosp: ProfilingService.java |
| §「system-triggered profiling 的版本边界」 | trigger 登记与采集时机由系统控制 | `[来源: developer.android.com/reference/android/os/ProfilingTrigger]` | official: ProfilingTrigger reference |

正文 `[来源:]` 计数：0 → 3，超过 ≥2 阈值。

### 3. frontmatter 状态更新

| 字段 | 修复前 | 修复后 |
|---|---|---|
| `last_verified` | 2026-07-07 | 2026-07-30 |
| `last_verified_against` | （AOSP + developer.android 引用基线） | 追加 rework round 记录（清除 2 个启发式标记） |
| `last_rework_at` | （无） | 2026-07-30T17:35:57+08:00 |
| `last_rework_run_id` | （无） | 20260730-173557-rework-d7fa8e54 |
| `rework_notes` | （无） | 本轮修复摘要 |

**未改动的状态字段**（理由见下）：
- `status`: 维持 `finalized` —— 本章技术内容已通过 Task6/Task9 双轮审核，本次仅清除启发式标记，不涉及技术结论变更，不应降级。
- `pipeline_stage`: 维持 `ready-to-publish` —— 同上。
- `task6_state` / `task9_state`: 维持 `reviewed` —— 无新增技术问题。
- `confidence`: 维持 `medium` —— 引用基线和审核历史未变。

## 剩余风险

1. **启发式标记的脆弱性**：`thin-source-marking` 的 ≥2 阈值是粗粒度检测。本章已补 3 处内联标记，但如果未来正文重构移除这些标记，flag 会再次触发。这是检测器设计问题，不是内容问题。
2. **无技术结论变更**：本次修复不改写任何 API 契约、版本边界、错误码、限流模型或调用链描述。章节核心内容（ProfilingManager API35、ProfilingTrigger API36/36.1/37、四类产物后缀、双层 listener、rate limiter、heap dump 敏感数据）保持不变。
3. **confidence 仍为 medium**：frontmatter `confidence: medium` 反映的是 ProfilingManager 在 API 36–37 仍在演进的客观状态（trigger 常量、SDK_INT_FULL 边界等），不是本次 rework 引入的不确定。

## 实际状态流转

- `status`: `finalized`（维持）
- `pipeline_stage`: `ready-to-publish`（维持）
- `task6_state`: `reviewed`（维持）
- `task9_state`: `reviewed`（维持）
- quality_flags: `pending-verification-marker` ✅ 清除、`thin-source-marking` ✅ 清除 → `[]`

## 本地校验

- `python3 scripts/check-metadata.py --files src/part3-tools/ch19-apm/16-profiling-manager.md`：✅ 通过（1 文件，1 通过，0 失败，0 警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（164 local links，2 既有 duplicate warning，与本改动无关）
- `git diff --check`：✅ 通过（无 whitespace 错误）
- quality_flags 复验（同 `aiw-polish-context.py` 启发式）：✅ `[]`（全部清除；`待验证`=0，`[来源:]`=3，body=15558 chars）
