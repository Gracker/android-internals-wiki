# Task2B Lite · 2026-05-27 09:51

## 处理章节
- `src/part1-fundamentals/ch02-rendering/23-vsync-scheduler-displayframerate.md`

## 问题来源
- `logs/deep-review/2026-05-18-19-deep-review.md`: P0 指出 `Window.setFrameRate()` 不存在，应改为 `Surface.setFrameRate()` / `View.setRequestedFrameRate()` / `SurfaceControl.Transaction.setFrameRate()`，Window 只保留 touch boost / power-savings 策略。
- 章节正文已无 `Window.setFrameRate()`，但 frontmatter 仍保留 `pipeline_stage: task2b_pending` 与 `task9_state: reviewed`，导致已修正文未正确回流。

## Lite 修复
- 修复类型：frontmatter / progress
- 正文改动：0 行
- 将章节 frontmatter 回流为 `pipeline_stage: task6_pending`、`task6_state: revisiting`、`task9_state: pending`、`task2b_result: fixed-lite`。
- 同步 `metadata/progress.json` 中 `sections.2.23` 与 `issues_by_section.2.23` 的 Task2B Lite 状态。

## 锁
- `metadata/locks/task2b/src__part1-fundamentals__ch02-rendering__23-vsync-scheduler-displayframerate.md.lock`
