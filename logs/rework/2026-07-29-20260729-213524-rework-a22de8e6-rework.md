# AIW Rework Log · 2026-07-29

- lane: rework
- run_id: `20260729-213524-rework-a22de8e6`
- target: `src/part2-performance/ch14-debug/14.8-gpu-debug-performance-tools.md`
- baseline: Android 17 / `android-17.0.0_r1`

## 原问题

章节质量标记为 `pending-verification-marker` 和 `thin-source-marking`，state 为 `finalized`。

- **pending-verification-marker**：§14.8.9 标题为「本节未覆盖 / 待验证」，列出 4 条未验证项，但章节状态已标为 `finalized`，存在状态与内容不一致。这些条目本质上是已知边界而非待修复缺陷。
- **thin-source-marking**：frontmatter `sources` 仅列出 3 条 DeepResearch 二手调研路径，未标注章节代码块中引用的 7 条 AOSP `android-17.0.0_r1` 一手源码路径，来源链不可追溯。
- **ANGLE 命名精度**：§14.8.3 标题为「ANGLE denylist」，但章节正文和 DeepResearch 材料均确认 denylist 规则字段格式未经源码验证，标题使用「denylist」作为确定结论具有误导性。

## 修复点

### 1. 解决 pending-verification-marker

- 将 §14.8.9 标题从「本节未覆盖 / 待验证」改为「本节未覆盖 / 已知边界」。
- 逐条重写 4 条 → 5 条边界说明，每条注明：边界依据、正文对应位置（§14.8.2/§14.8.3/§14.8.6）、为何不是待修复缺陷。
- 新增第 5 条：`panopticon::share()` 在 RenderEngineThreaded 中的 frame timeline 注册边界说明。

### 2. 解决 thin-source-marking

- frontmatter `sources` 从 3 条纯路径字符串列表改为结构化 `type`/`path`/`role` 列表。
- 保留原 3 条 DeepResearch 来源，新增 7 条 AOSP `android-17.0.0_r1` 一手源码路径：
  - `frameworks/native/services/surfaceflinger/Layer.cpp`（FrameTracer 调用入口）
  - `frameworks/native/services/surfaceflinger/FrameTracer/FrameTracer.cpp`（Perfetto DataSource 注册）
  - `frameworks/native/services/gpuservice/gpuservice.cpp`（GpuService 构造、FeatureOverrideParser、ANGLE API）
  - `frameworks/native/libs/graphicsenv/include/graphicsenv/GpuStatsInfo.h`（Driver 枚举、ANGLE 统计字段）
  - `external/perfetto/protos/perfetto/trace/gpu/gpu_counter_event.proto`（GpuCounterEvent 双描述路径）
  - `frameworks/native/libs/renderengine/threaded/RenderEngineThreaded.cpp`（RenderEngine 异步模型）
  - `frameworks/native/libs/renderengine/skia/SkiaGLRenderEngine.cpp`（native fence 降级路径）

### 3. ANGLE 命名边界修正

- §14.8.3 标题从「ANGLE denylist」改为「ANGLE FeatureOverride」。
- 新增命名边界 blockquote，明确说明：
  - 已验证的是 `FeatureOverrideParser` 入口路径，不是 denylist 规则字段。
  - `feature_config_vk.binarypb` 的 protobuf schema 尚未逐字段验证。
  - 不使用 denylist 规则作为确定结论。

### 4. frontmatter 状态流转

| 字段 | 修复前 | 修复后 |
|------|--------|--------|
| status | finalized | ready-for-review |
| task6_state | reviewed | rework-done |
| task9_state | reviewed | ready-for-review |
| pipeline_stage | finalized | rework-verified |
| last_verified | 2026-07-27 | 2026-07-29 |
| confidence | medium-high | medium-high（维持） |
| last_rework_at | (无) | 2026-07-29T21:35:24+08:00 |
| last_rework_run_id | (无) | 20260729-213524-rework-a22de8e6 |

### 5. 新增 Rework 记录章节

在章节末尾新增「Rework 记录」节，记录本次修复的 4 项内容和状态流转。

## 状态流转

`finalized` → `ready-for-review`（rework-verified）。章节已解决两个质量标记，内容准确性提升，来源链可追溯。`confidence` 维持 `medium-high`：正文技术内容基于 DeepResearch 材料中的 AOSP 源码级调研，但部分结论（如 `panopticon::share()`、binarypb schema）仍为间接验证。

## 剩余风险

1. **binarypb schema**：`feature_config_vk.binarypb` 的 protobuf 字段定义仍未解析。本节已在 §14.8.3 和 §14.8.9 明确标注此限制，不影响其余内容的准确性。
2. **`panopticon` 命名空间**：该命名空间在 DeepResearch 材料中被引用但未在 AOSP main 分支独立确认，可能是 Android 17 新增或内部代号。本节仅引用其存在性。
3. **AGI 版本号 3.3.3**：来自 DeepResearch 材料中引用的 `version.bzl`，AGI 作为独立项目其版本号会持续演进，本节未锚定 AGI 版本到 Android 17 发布版本。
4. **本章不在 SUMMARY.md 中**：`src/part2-performance/ch14-debug/14.8-gpu-debug-performance-tools.md` 在 SUMMARY.md 中无对应条目，本 lane 约束禁止修改 SUMMARY.md。若需纳入正式目录，需后续单独处理。

## 校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch14-debug/14.8-gpu-debug-performance-tools.md`：✅ 通过（1 文件，0 失败，0 警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（165 links，2 既有 duplicate warning，与本改动无关）
- `git diff --check`：✅ 通过（无 whitespace 错误）
