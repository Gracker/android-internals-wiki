# Rework Log — 2026-07-31 09:37:47 (Asia/Shanghai)

- **Run ID:** `20260731-093747-rework-bc7d7877`
- **Lane:** rework
- **Profile:** aiw-polish-rework
- **Mode:** rework
- **Status in:** `polish_candidate_ready`
- **Quality flags in:** `pending-verification-marker`

## Target

| Field | Value |
|-------|-------|
| Path | `src/part5-app/ch26-observability/22-monitoring-under-memory-privacy-constraints.md` |
| Chapter / Section | 26.22 |
| sha16 | `9d3bec8bc3fe43ba` |
| Baseline | AOSP `android-17.0.0_r1` (Android 17, API 37, kernel 6.18) |

## 原问题清单

1. **`[待验证: SDK_INT 常量]`** — `MonitoringCapabilities.supportsMemoryLimiterAttribution()` 使用 `SDK_INT >= 36`（Android 16）判断 MemoryLimiter 归因，但本章 baseline 是 Android 17（API 37），且标注为待验证。Android 17 = API 37，数字应为 37。
2. **`[待验证: API 30+ importerState 字段含义]`** — `isCachedAppFreezerActive()` 引用了不存在的 `RunningAppProcessInfo.importerState` 字段，函数体只 `return false`（死代码）。
3. **ProfilingManager 版本边界错误** — 正文 §系统级诊断 API 写 `ProfilingManager / ProfilingTrigger（API 34+）`，但代码示例中 `supportsProfilingTrigger()` 用 `UPSIDE_DOWN_CAKE`（API 34）。`ProfilingManager` 的 trigger-based API 是 API 35（`VANILLA_ICE_CREAM`）起正式提供。
4. **伪造的 HPROF/Perfetto 源码调研附录** — 文末 `<!-- AIW-源码调研-2026-06-30 -->` 之后约 50 行的 HPROF 章节包含：
   - 虚构的 `Debug.dumpHprofData()` mmap 实现代码（AOSP 中该方法是通过 native call `dumpHprofData` 调用，不做 mmap）
   - 虚构的 proto message `JavaHprofPacket`（Perfetto java_hprof 的实际 proto 结构不是这个）
   - 编造的性能对比数字（1GB/4GB/8GB+ 解析时间，无任何来源）
   - 编造的厂商差异（Samsung/Xiaomi 定制策略）
   - 明确承认无源码访问："⚠️ 源码访问限制：由于技术站点访问限制，本次分析基于行业标准文档和公开技术规范"
   该附录违反 baseline 要求和真实性约束，且与本节主题（监控降级策略）不相关。

## 修复点（safe bounded patches）

| # | 位置 | 修复内容 |
|---|------|----------|
| 1 | frontmatter | `last_verified: 2026-06-27` → `2026-07-31`；`confidence: medium` → `medium-high`；新增 `last_rework_at`、`last_rework_run_id`、`pipeline_stage: ready-for-review`、`task6_state: revisiting`、`task9_state: pending` |
| 2 | §跨版本兼容框架 — `supportsMemoryLimiterAttribution()` | `SDK_INT >= 36` → `>= 37`；删除 `[待验证: SDK_INT 常量]`，改为确定性注释 `Android 17 (API 37)` |
| 3 | §跨版本兼容框架 — `supportsProfilingTrigger()` | `UPSIDE_DOWN_CAKE (API 34)` → `VANILLA_ICE_CREAM (API 35, Android 15)` |
| 4 | §跨版本兼容框架 — `isCachedAppFreezerActive()` | 删除 `[待验证: API 30+ importerState 字段含义]` 和死代码 `return false`；改为基于 `RunningAppProcessInfo.importance >= IMPORTANCE_CACHED` 的真实实现，附注 `runningAppProcesses` 只返回自身进程 |
| 5 | §系统级诊断 API | `ProfilingManager / ProfilingTrigger（API 34+）` → `ProfilingManager（API 35，Android 15 起正式提供 trigger-based 采集）` |
| 6 | 文末附录 | 删除整个伪造的 HPROF/Perfetto 源码调研章节（约 50 行），替换为一行说明：java_hprof 属于 §26.9 / §26.12 范畴，本节不展开 |

## 状态流转

- `status`: 保持 `ready-for-review`（本次是准确性/版本边界修复 + 删除伪造内容，技术结论可复审，不引入 needs-rework 阻断）
- `pipeline_stage`: → `ready-for-review`
- `task6_state`: → `revisiting`（内容已改，等待 Task6 复审确认）
- `task9_state`: → `pending`（等待 Task9 终审）
- `confidence`: `medium` → `medium-high`（删除伪造内容、修正版本边界后整体可信度提升）
- `last_verified`: → `2026-07-31`

## 剩余风险 / 未关闭项

1. **`[待补充]` 大厂实践 ×3** — 微信 Matrix / 字节 APMPlus / 阿里 APMP 的 Android 14+ 适配策略仍为 TODO。这是真实的知识缺口（业界尚无 Android 17 三重约束的成熟开源方案），正文下一段已正确声明这一点。保留这些标记作为后续补充的锚点。
2. **`VANILLA_ICE_CREAM` 编译器常量** — 代码示例使用 `Build.VERSION_CODES.VANILLA_ICE_CREAM`，对应 API 35。如果编译 SDK_INT 低于 35，该常量需替换为字面量 35。文中已注释版本号，开发者可自行适配。
3. **`runningAppProcesses` 可见性** — Android 9+ 对 `ActivityManager.getRunningAppProcesses()` 做了限制，只返回调用方自身应用的进程。实现中已注明此限制，不影响自身进程的 freezer 检测。
4. **未新增 / 关闭 finding** — 本章节在 `metadata/review-findings.json` 中无现存 open finding，本次 rework 不触发 finding 增删（问题来自 quality_flag `pending-verification-marker` 而非 review pool）。

## 本地校验结论

- `check-metadata.py --files <target>`: ✅ 通过（0 失败 / 0 警告）
- `check-summary-links.py`: ✅ 通过（164 链接；2 个 duplicate warning 为既有项，与本章节无关）
- `git diff --check`: ✅ 退出码 0（无空白错误）
- 未修改 `src/SUMMARY.md`；未新增 `src/**/*.md` 文件。
