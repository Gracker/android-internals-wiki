# AIW Polish Rework · 20260812-133503-rework-3aeaea29

- lane: aiw-polish-rework
- mode: rework
- target: `src/part1-fundamentals/ch04-memory/07-art-generational-gc.md`
- generated_at: 2026-08-12T13:43:41+08:00
- finding: `AIW-FRESH-704a10ad8e3f94fc`
- Android baseline: `android-17.0.0_r1` / `android17-6.18-2026-06_r6`

## 原问题

`AIW-FRESH-704a10ad8e3f94fc` 标记本章 `last_verified=2026-04-12`，超过 90 天未刷新来源核验日期。

## 核验范围

本轮没有改写读者正文；仅处理 supplied stale source verification finding。复核固定到 Android 17 / `android-17.0.0_r1`，检查范围包括：

- AOSP ART：`runtime.cc`、`runtime/gc/heap.cc`、`heap.h`、`heap-inl.h`、`collector/mark_compact.h`、`collector/mark_compact.cc`、`space/region_space.h`、`space/region_space.cc`、`write_barrier-inl.h`、`card_table.h`。
- Android common kernel：`android17-6.18-2026-06_r6/arch/arm64/configs/gki_defconfig`。
- 官方文档：Android 17 release notes、ART GC debug、Android 8.0 ART improvements、graphics memory management。
- Perfetto 文档：stdlib `android.garbage_collection`、`heap_profile` CLI、ART Java heap profiler。

## 修复点

- 将 frontmatter `last_verified` 从 `2026-04-12` 刷新为 `2026-08-12`。
- 扩展 `last_verified_against`，明确本章来源核验覆盖 Android 17 AOSP ART GC source set、GKI UFFD、官方 Android/ART 文档、Perfetto GC/heap 文档与 graphics memory 文档。
- 将 `confidence` 从 `medium` 调整为 `medium-high`。理由：本章核心 CMC 分代、young/mid/old、collector selection、write barrier/card table、RegionSpace evacuation、LOS 边界、Perfetto GC 观察口径均有 pinned AOSP 或官方文档证据；设备配置、厂商 ART 模块与真实 trace 表现仍需目标设备验证，因此不提升到 high。
- 补充 frontmatter source：`runtime.cc`、`heap.h`、`heap-inl.h` 与 graphics memory official doc。
- 将 `task6_state` / `task9_state` 回流到 `pending-review`，`pipeline_stage` 保持 `ready-for-review`，等待后续 Task6/Task9 复审。
- 关闭 machine-readable finding `AIW-FRESH-704a10ad8e3f94fc` 为 `verified`。

## 证据摘要

- Android 17 release notes 包含 “Generational garbage collection” 段落，说明 ART CMC 引入更频繁、成本更低的 young-generation collection，并提到 GC interference 与 maximum RSS 改善；同段还声明 ART improvements 可通过 Google Play System updates 覆盖 Android 12+ 设备。
- `runtime.cc` 的 `use_generational_gc` 组合条件仍受 read barrier/UFFD、`-Xgc` 与 `ShouldUseGenerationalGC()` 约束。
- `mark_compact.cc` 的 `ShouldUseGenerationalGC()` 路径检查 `use_generational_cmc` flag，并读取 `persist.device_config.runtime_native_boot.use_generational_gc` 默认 true；`ShouldUseUserfaultfd()` / UFFD feature / `MREMAP_DONTUNMAP` 路径仍是 CMC 边界。
- `mark_compact.h` 明确 generational mode 维护 young/mid/old 三代，mid 在 young collection 中回收，对象需存活两轮 GC 才晋升 old。
- `write_barrier-inl.h` 的 field/array write barrier 以 `dst` 调用 `MarkCard()`；`card_table.h` 定义 `kCardShift = 10`、dirty/aged/aged2 card 状态。
- `heap.h` / `heap-inl.h` 确认默认 large object threshold 为 `12 * KB`，且 `ShouldAllocLargeObject()` 只对 primitive array 或 `String` 达阈值时成立。
- `region_space.h` / `region_space.cc` 确认 CC evacuation mode 与 `ShouldBeEvacuated()` 条件：large region 不搬迁、force-all 搬迁、新分配 region 搬迁、普通 region 在 live ratio 严格低于 75% 时搬迁。
- Perfetto stdlib 文档包含 `android_garbage_collection_events`、`gc_running_dur`、`gc_runnable_dur`；heap_profile CLI 文档包含 `--heaps` 与 `com.android.art` 示例；Java heap profiler 文档包含 Android 11+ heap dump 与 `NativeAllocationRegistry` native size 边界。

## 本地 gate

待最终验证命令输出写入 result JSON。

## 状态流转

- result: `metadata-only`
- next gate: Task6 / Task9 复审
