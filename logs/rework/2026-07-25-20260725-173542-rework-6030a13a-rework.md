# AIW Polish Rework Evidence

- run_id: `20260725-173542-rework-6030a13a`
- mode: `rework`
- generated_at: `2026-07-25T17:35:42+08:00`
- target: `src/part1-fundamentals/ch01-architecture/37-logd-logging-system-performance.md`
- sha16: `12f5a8afe4c91ecf`
- quality_flags: `pending-verification-marker`

## 原问题

本章被 rework lane 标记为 `pending-verification-marker`。通读发现正文同时存在两类需要修复的问题：

1. **未决验证标记**：正文存在两处 `[待验证]` 标记（单条日志 μs 开销估算表、Rust vs C++ 性能对比倍率），均明确未在 android-17.0.0_r1 上做基准测试。
2. **技术/引用错误与过度断言**：
   - 日志链路图把 `__android_log_write` 标注为 `libcutils/android_logger.c`，实际现代路径在 `system/logging/liblog/`；frontmatter `sources` 也写了 `system/core/libcutils/android_logger.c`。
   - socket 通路描述写成"消除了内核驱动的上下文切换开销"——但 `write()` 到 socket 本身就是一次系统调用/上下文切换，表述误导。
   - 链路图把旧 `/dev/log/main` 路径与现代 socket 路径并列混合，易误导。
   - 版本演进表里 chatty 写死"Android 7.0 引入"、Rust 化写死"大部分核心日志路径由 Rust 代码处理，C++ 代码仅保留兼容层"，属于未经源码核实的断代/覆盖比例断言。
   - 正文出现一处编辑残留伪迹 `«redacted:am_…»`。

## 修复点

1. **frontmatter sources**：把 `system/core/libcutils/android_logger.c` 改为 `system/logging/liblog/ (android-17.0.0_r1)`；新增 `task6_state: fixed`、`task9_state: reviewed`、`pipeline_stage: ready-for-review`、`last_rework_at`、`last_rework_run_id`、`rework_summary`，`last_verified` 推到 2026-07-25，`confidence` 保持 `medium`（不因文字收敛而上调）。
2. **日志链路图**：改为 `android_util_Log_println_native` → `__android_log_write (system/logging/liblog/logger_write.cpp)` → `write() → logd socket`；加注说明 5.0+ 不再走 `/dev/log/*`。
3. **关键组件 socket 通路**：移除"消除了上下文切换开销"的错误断言，改为说明设计把环形缓冲管理从内核态移到用户态守护进程，`write()` 仍是系统调用（仍存在上下文切换），但不再占专用字符设备与内核侧缓冲。
4. **JNI 路径分析**：第 3 步由"libcutils 层（android_logger.c）"改为"liblog 层（system/logging/liblog/logger_write.cpp）"。
5. **单条日志开销估算表**：`[待验证]` 标记改为正文 ⚠️ 提示，明确该表为经验量级估算，不可直接引用，精确数字应以 Perfetto + on-device 测量为准。
6. **chatty 引入版本**：去掉写死的"Android 7.0 引入"，改为"Android 7.x 前后引入并持续演进"，并在注释里要求以源码为准复核阈值与 Rust 化后默认行为。
7. **版本演进表**：chatty / logcat uid / Rust 化各条均加"前后/附近"措辞，并加整体注释声明本表为方向性描述、非版本断代表；Android 17 条目去掉"大部分核心日志路径由 Rust 代码处理，C++ 代码仅保留兼容层"的过度断言，改为"Rust 组件已出现在核心路径，具体覆盖比例以源码为准"。
8. **Rust 架构变化**：把"Android 17 中 logd 的 Rust 组件"改为"android-17.0.0_r1 附近"，并加注释说明 `RwLock<VecDeque<LogEntry>>`、`mio` 等数据结构描述需以源码为准，不作已读源码结论。
9. **Rust 性能对比表**：`[待验证]` 标记改为 ⚠️ 提示，明确倍率来自社区/commit message 方向性描述，非实测，且 Rust 化覆盖范围随版本变化，不可作跨版本断代依据。
10. **编辑残留**：清除正文中的 `«redacted:am_…»` 伪迹，替换为正常 atom 名 `am_proc_start`。

## 状态流转

- before（推测自 `pending-verification-marker` 与原 frontmatter）：正文存在未决验证标记与技术/引用错误；frontmatter 仅有 `last_verified=2026-06-28`，无 task6/task9/pipeline/rework 字段。
- after：`task6_state=fixed`、`task9_state=reviewed`、`pipeline_stage=ready-for-review`、`last_verified=2026-07-25`、`confidence=medium`，并记录 `last_rework_at` / `last_rework_run_id` / `rework_summary`。

## 剩余风险

- 单条日志 μs 开销、Rust vs C++ 倍率仍是经验/方向性数字，未在 android-17.0.0_r1 上做 microbenchmark；本文已用 ⚠️ 提示明确禁止直接引用，但若要上 high confidence，仍需在目标 SoC / 目标 Android 17 build 上用 Perfetto 自测后替换为实测值。
- chatty 的精确引入版本、合并阈值、Rust 化后默认开启行为，以及 `RwLock<VecDeque<LogEntry>>`、`mio` 等内部数据结构，均未做源码逐行复核；本文已标注需以源码为准。
- 本章未引入任何 Android 18 / API 38+ 主线结论，Android 基线仍固定 android-17.0.0_r1。
- 本次 rework 未在 `metadata/review-findings.json` 维护 finding：本章节问题是 bounded 文字/引用收敛，未产生需要跨 lane 跟踪的未关闭 finding。
