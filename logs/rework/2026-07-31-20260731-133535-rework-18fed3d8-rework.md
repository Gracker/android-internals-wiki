# Rework Log — 2026-07-31 13:35:35 (Asia/Shanghai)

- **Run ID:** `20260731-133535-rework-18fed3d8`
- **Lane:** rework
- **Profile:** aiw-polish-rework
- **Mode:** rework
- **Status in:** `polish_candidate_ready`
- **Quality flags in:** `pending-verification-marker`, `thin-source-marking`

## Target

| Field | Value |
|-------|-------|
| Path | `src/part5-app/ch25-power-size/25.24-25.4 WorkManager 实战与后台任务调度.md` |
| Chapter / Section | 25.24 |
| sha16 | `29fa4420a57185d9` |
| Baseline | AOSP `android-17.0.0_r1` (Android 17, API 37) |

## 原问题清单

1. **虚构 API：`setPriority()` / `Priority` 枚举** — 原稿列出 `BACKGROUND(5)/LOW(10)/MEDIUM(15)/HIGH(20)/EXPLICIT(25)` 并称通过 `.setPriority()` 设置。androidx.work 公共 API 中不存在此方法和枚举。deep-review 已标注"待验证"，rework 需删除并替换为真实的 `setExpedited(OutOfQuotaPolicy)`。

2. **虚构 API：`setAlternateWork()`** — 原稿声称有"备用任务"API。WorkManager 无此公共方法，替代逻辑需应用层自行实现。

3. **虚构 API：`setTimeout()`** — 原稿声称 WorkRequest 有超时设置。`WorkRequest.Builder` 无此方法；执行时长由系统 `JobScheduler` 配额决定。

4. **虚构 API：`WorkManager.MAX_WORKERS`** — 原稿注释引用此常量。`WorkManager` 类无此公开常量。

5. **虚构 API：`CrossProcessWorker`** — 原稿以此类为例讲解多进程。该类不存在；多进程通过 `RemoteListenableWorker` / `RemoteCoroutineWorker` 实现。

6. **无来源的 Android 17 断言**：
   - "Wi-Fi 6/5G 网络类型区分" — `NetworkType` 枚举无代际区分能力。
   - "电池健康度 <80% 自动降级" — `Constraints` 无电池健康度感知。
   - "存储空间 <10% 自动取消文件写入类任务" — `Constraints` 无此逻辑。
   - "Android 17 引入更智能电量感知机制动态调整优先级" — 无来源支撑。
   - "Android 17 进一步优化电池管理策略" — 无来源支撑。

7. **frontmatter 状态错误** — `status: needs-review`、`confidence: low`、`task6_state: needs-rework`、`task9_state: needs-rework`、`pipeline_stage: deep-review`。

8. **约束冲突处理逻辑错误** — 原稿声称"网络优先级最高 > 充电次之 > 存储最后"的排序机制，实际所有约束是 AND 关系。

## 修复点（safe bounded patches）

| # | 位置 | 修复内容 |
|---|------|----------|
| 1 | frontmatter | `status: needs-review → ready-for-review`；`confidence: low → medium`；`task6_state: needs-rework → revisiting`；`task9_state: needs-rework → pending`；`pipeline_stage: deep-review → ready-for-review`；新增 `last_rework_at`、`last_rework_run_id`、`sources` |
| 2 | outline 要点 | "任务优先级与资源管理" → "加急任务与配额"；重写为准确的 API 语义描述 |
| 3 | 源码边界审查 blockquote | 删除 deep-review ⚠️ 警告块，替换为 📌 章节性质说明（rework 已完成修订） |
| 4 | §WorkManager 基础架构 | 删除"Android 17 更智能电量感知/动态调整优先级"断言；添加准确的分层架构描述（Room 持久化、`Configuration.Executor`） |
| 5 | §任务约束与系统适配 | 重写约束语义：列出完整 `NetworkType` 枚举值；明确约束是布尔开关而非可调阈值；列出 WorkManager Constraints **不**提供的能力；修正约束冲突处理为 AND 关系 |
| 6 | §任务优先级与资源管理 → §加急任务与配额管理 | 删除全部虚构 `Priority` 枚举（5 个级别）；重写为 `setExpedited(OutOfQuotaPolicy)` + `Configuration.Executor` 线程池；添加 `OutOfQuotaPolicy` 两个选项的说明 |
| 7 | §失败处理与重试 | 删除 `setAlternateWork()` / `setTimeout()` 虚构 API；添加真实的 `Result.retry()` + `BackoffPolicy` + Worker 示例；明确超时由系统配额决定；备用任务通过 `WorkContinuation` 或应用层实现 |
| 8 | §依赖管理最佳实践 | 重写为准确的建议（DAG、AND 约束、`getWorkInfoByIdLiveData`） |
| 9 | §实时监控工具 | 删除虚构的"Android 17 更强大监控能力 / 电池影响分析"；替换为 Perfetto trace section、`dumpsys jobscheduler`、debug 日志 |
| 10 | §性能优化实践 | 删除 `MAX_WORKERS` 引用；重写为 `enqueueUniqueWork` + `ExistingWorkPolicy`、`Data` 输出传递、`WorkManagerInitializer` 自定义配置、线程池调优 |
| 11 | §多进程任务调度 | 删除 `CrossProcessWorker` 虚构类及代码；替换为 `RemoteListenableWorker` / `RemoteCoroutineWorker` + `RemoteListenableService` 的真实实现；添加 Kotlin 示例和多进程注意事项 |
| 12 | §总结 | 删除"Android 17 进一步优化电池管理"断言；重写为 4 个准确关键点总结 |
| 13 | 文末注释 | 更新来源标注为 androidx.work 2.9 公共 API + 官方 Background Processing 指南 |

## 状态流转

- `status`: `needs-review` → `ready-for-review`（虚构 API 全部删除并替换为可验证内容，技术结论可复审）
- `pipeline_stage`: `deep-review` → `ready-for-review`
- `task6_state`: `needs-rework` → `revisiting`（内容已大改，等待 Task6 复审确认）
- `task9_state`: `needs-rework` → `pending`（等待 Task9 终审）
- `confidence`: `low` → `medium`（删除全部虚构 API 与无来源断言，替换为 androidx.work 2.9 公共契约可验证内容；未达 high 因仍无一手 AOSP 源码引用）
- `last_verified`: → `2026-07-31`

## finding 处理

- `finding-359b1c7d33c0` (P0, source-boundary): **closed → fixed**
  - 原因：虚构 API 全部删除，无来源 Android 17 断言全部删除，正文已以 androidx.work 2.9 公共 API 重写

## 剩余风险 / 未关闭项

1. **无一手 AOSP 源码引用** — 本章节 API 细节基于 androidx.work 2.9 公共 API 和官方文档，未直接引用 `android-17.0.0_r1` 中的 WorkManager/JobScheduler 实现源码。这符合公共 API 文档的引用标准，但若后续需深入系统层调度细节（如 `JobScheduler` 配额算法），需补充 AOSP 源码引用。

2. **厂商 JobScheduler 配额差异** — `JobScheduler` 的 standby bucket 配额在不同厂商设备上可能有差异（Pixel vs Samsung vs Xiaomi），本节未提供实测数据。保留"待补充"标记。

3. **`RemoteListenableWorker` 示例简化** — 多进程示例展示了基本用法，实际多进程配置还需处理 `Configuration.Builder.setRemoteListenableWorkerFactory()` 等细节，开发者需参考 androidx.work multiprocess 官方文档。

4. **章节性质标注** — 保留了"📌 章节性质"说明，表明此章节为 task2a 占位稿经 rework 修订，内容已全部基于公共 API 重写但非来自一手 DeepResearch 材料。

## 本地校验结论

- `check-metadata.py --files <target>`: ✅ 通过（1 个文件，0 失败，0 警告）
- `check-summary-links.py`: ✅ 通过（164 链接；2 个 duplicate warning 为既有项，与本章节无关）
- `git diff --check`: ✅ 退出码 0（无空白错误）
- 未修改 `src/SUMMARY.md`；未新增 `src/**/*.md` 文件。
