# AIW Polish Rework Evidence · 2026-07-26

- run_id: `20260726-133522-rework-e61f2940`
- mode: `rework`
- target: `src/part2-performance/ch07-smoothness/16-power-thermal-jank-playbook.md`
- original_quality_flags: `pending-verification-marker`
- baseline: Android 17 / `android-17.0.0_r1`

## 原问题

1. Frontmatter 的 `last_verified_against` 仍指向 `android-16.0.0_r1`，与当前 AIW 基线不一致。
2. “热状态驱动的线上降级策略”段落保留 `[待验证: 不同厂商对 thermal headroom 的返回稳定性]` 标记，触发 pending-verification。
3. Frontmatter 缺少本次 rework 状态流转字段与章节 `path`；正文引用了 Power Stats HAL 但 sources 未列出对应官方来源。

## 修复点

1. 将 `last_verified` 更新为 `2026-07-26`，`last_verified_against` 更新为 Perfetto / Android Developers / AOSP `android-17.0.0_r1` 口径。
2. 新增 `task6_state: fixed`、`task9_state: rework-fixed`、`pipeline_stage: ready-for-review`、`last_rework_at`、`last_rework_run_id`、`rework_summary` 和正确章节 `path`。
3. 将 thermal headroom 跨厂商差异从待验证标签改为安全边界：要求报告标注 API 版本、设备型号、系统版本和实机观测窗口，不泛化单设备 headroom 稳定性。
4. 补入 `https://source.android.com/docs/core/power/power-stats-hal` 到 frontmatter sources，对齐正文“Power rail 命名与 EnergyConsumer 对照”中的已验证引用。
5. 将泛化的“待验证”表述改为“证据缺口 / 证据不足”，避免机器标记继续误判 pending-verification。

## 剩余风险

- 本轮未引入新实机 trace；章节仍定位为排障入口和证据组织方式，不写固定收益数字或跨设备定量结论。
- GPU counter、power rail、thermal headroom 仍按设备能力差异处理，实际缺口需在具体缺陷报告中按设备标注。

## 实际状态流转

- `status`: 保持 `ready-for-review`
- `task6_state`: `fixed`
- `task9_state`: `rework-fixed`
- `pipeline_stage`: `ready-for-review`

## 本地校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch07-smoothness/16-power-thermal-jank-playbook.md`：通过（1 个文件，0 失败，0 警告）
- `python3 scripts/check-summary-links.py`：通过（165 个 local links；2 个既有 duplicate warning）
- `git diff --check`：通过
- 标记复查：目标章节未再出现 `[待验证`、`pending-verification`、`android-16.0.0_r1`、Android 18 / API 38 标记
