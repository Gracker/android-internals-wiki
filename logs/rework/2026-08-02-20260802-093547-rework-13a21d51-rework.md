# AIW Rework Log · 2026-08-02 · 20260802-093547-rework-13a21d51

- lane: rework
- target: `src/part5-app/ch25-power-size/16-adpf-power-efficiency-powermonitor.md`
- mode: polish_candidate_ready
- baseline: Android 17 / `android-17.0.0_r1`（未引入 Android 18/API 38+ 结论）

## 原问题

1. `quality_flags` 包含 `pending-verification-marker`：正文扩展小节仍以“待验证”标记描述 PowerMonitorReadings 与 Perfetto power rails 一致性，容易把未完成实验作为出版遗留问题。
2. `quality_flags` 包含 `thin-source-marking`：正文延伸阅读已引用 2026-07-07 PowerStats HAL OEM 差异报告，但 frontmatter `sources` 只列入 2026-06-13 PowerStatsService 报告。
3. 章节仍为 `finalized/ready-to-publish`，与本次 rework 后需要 Task6 复审的状态不一致。

## 修复点

- 将扩展大纲与正文小节改为“PowerMonitorReadings 与 Perfetto power rails 的同窗验证方法”，删除显式待验证标记。
- 正文保留保守口径：两类数据源可在同一设备/负载/温度起点下互相解释，但不能写成跨设备等价或 rail 级闭合校准；单一 Pixel/OEM 结果只绑定设备分桶。
- frontmatter `sources` 补入 `DeepResearch/2026-07-07-android17-power-stats-hal-impl-variations.md`。
- 更新 `last_verified=2026-08-02`、`last_rework_at`、`last_rework_run_id`、`last_rework_result`、`last_rework_notes`。
- 将章节状态从 `finalized/ready-to-publish` 调整为 `ready-for-review`，`pipeline_stage=ready-for-review`，`task6_state=pending`，保留 `task9_state=reviewed`。

## 剩余风险

- 本次未新增设备实测数据；正文只给出同窗验证方法和结论边界，未声明具体收益。
- 2026-07-07 DeepResearch 报告中的 20s/250ms 缓存阈值冲突仍在原材料内，章节正文和延伸阅读已明确以 2026-06-13 一手源码 30s 阈值为准。

## 校验

已通过：

- `python3 scripts/check-metadata.py --files src/part5-app/ch25-power-size/16-adpf-power-efficiency-powermonitor.md`：1 个文件通过，0 失败，0 警告。
- `python3 scripts/check-summary-links.py`：通过；保留既有 2 个 duplicate SUMMARY warning（与本章无关，未编辑 `src/SUMMARY.md`）。
- `git diff --check`：通过。
