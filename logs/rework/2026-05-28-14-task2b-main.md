# Task2B 主修复 · 2026-05-28 14:50

## 本轮目标

- 来源：frontmatter backlog fallback
- 章节：14.4 dumpsys 系列命令
- 文件：`src/part3-tools/ch14-other-tools/04-dumpsys.md`
- 问题来源：`logs/deep-review/2026-05-24-01-deep-review.md`

queue.json 中无 `task6-review` / `task9-deep-tech-review` / `external-ai-review` pending 条目，本轮按 frontmatter fallback 选择 `task2b_state: pending`、`pipeline_stage: task2b_pending` 的 14.4。

## 问题单

- task9 / P1 / 版本差异：`dumpsys gfxinfo framestats` 字段版本边界错误。正文把 `FrameDeadline`、`FrameInterval`、`WorkloadTarget` 都标成 Android 14+；Task9 指出 AOSP android-12.0.0_r1 已有 `FrameDeadline` / `FrameInterval`，android-16.0.0_r1 才新增 `WorkloadTarget`。

## 修复内容

- 将 `FrameDeadline`、`FrameInterval` 的版本边界改为 Android 12+。
- 将 `WorkloadTarget` 的版本边界改为 Android 16+。
- 补充解析脚本兼容建议：按 header 列名或字段数量识别列，避免把三列都绑定到 Android 14+。
- frontmatter 回流 Task6：`pipeline_stage=task6_pending`、`task6_state=revisiting`、`task9_state=pending`、`task2b_state=fixed`。

## 验证

- L2 源码验证：`android.googlesource.com/platform/frameworks/base`。
  - android-12.0.0_r1 `libs/hwui/FrameInfo.h` 包含 `FrameDeadline` / `FrameInterval`，不包含 `WorkloadTarget`。
  - android-16.0.0_r1 `libs/hwui/FrameInfo.h` 包含 `FrameDeadline` / `FrameInterval` / `WorkloadTarget`。
- 文风扫描：修改段落未命中 technical-writing 禁用词。

## 状态

- 产出：`src/part3-tools/ch14-other-tools/04-dumpsys.md`
- 当前状态：ready-for-review / task6_pending
- 自动晋升 finalized：否。`task9_result` 仍为 `needs-rework`，需等待 Task6 / Task9 复审。
- 剩余 frontmatter fallback backlog：P95 88 个 / P90 2 个 / P85 7 个。
