# Rework · 2026-08-04 21:35 (run 20260804-213514-rework-b52e6e4a)

- lane：aiw-polish-rework
- mode：rework
- target：`src/part3-tools/ch15-methodology/06-testing-best-practices.md`
- baseline：Android 17 / API 37；AOSP `android-17.0.0_r1` / android17-6.18
- pre-run flags：`pending-verification-marker`, `thin-source-marking`

## 原问题

1. 上游启发式标记提示章节可能存在 pending-verification 残留。人工复查正文未发现 `待验证`、`TODO`、`needs-rework`、占位结论等未收敛标记；风险主要来自泛化的 `[已验证：...]` 标记，不够直接指向 frontmatter/source list。
2. 上游标记 `thin-source-marking`。frontmatter 已有官方 Android Benchmark、Firebase 与 AOSP android-17.0.0_r1/AndroidX 源码来源，但正文 7 处来源标记写成泛化“已验证”，可追踪性弱。
3. 章节仍处于 `finalized` / `ready-to-publish`，不符合 rework lane 对 quality_flags 处理后回流复核的状态流转要求。

## 本轮修复

1. 将 `status` 从 `finalized` 回流为 `ready-for-review`。
2. 将 `pipeline_stage` 更新为 `rework-applied-awaiting-review`，并将 `task6_state`、`task9_state` 更新为 `rework-applied`，等待后续 Task6/Task9 复核。
3. 更新 `last_verified: 2026-08-04`、`last_verified_against`、`confidence: medium-high`、`last_rework_at`、`last_rework_run_id`、`last_rework_log`、`rework_by`、`rework_notes`。
4. 将 7 处正文泛化 `[已验证：...]` 标记改为可追踪 `[来源：...]` 标记，明确映射 Android 官方文档、Firebase 文档、AOSP `android-17.0.0_r1` 源码和 AndroidX Benchmark 1.4.1 来源。
5. 未新增 Android 18/API 38+ 结论；未新增章节；未编辑 `src/SUMMARY.md`。

## 剩余风险

- 本轮为安全 rework，不改动性能测试方法论的技术结论；后续 review/finalize lane 需要根据 `rework-applied` 状态确认是否重新晋升 finalized。
- Firebase 与 AndroidX “current” 文档具有时间敏感性，因此保留 `last_verified_against` 中的当前文档说明，并以 `last_verified=2026-08-04` 记录本次复查时点。

## 验证

- ✅ `python3 scripts/check-metadata.py --files src/part3-tools/ch15-methodology/06-testing-best-practices.md`：1 个文件通过，0 失败，0 警告。
- ✅ `python3 scripts/check-summary-links.py`：通过；保留既有 2 个 duplicate SUMMARY warning（非本轮引入，本 lane 禁止编辑 `src/SUMMARY.md`）。
- ✅ `git diff --check`：通过（exit 0）。
