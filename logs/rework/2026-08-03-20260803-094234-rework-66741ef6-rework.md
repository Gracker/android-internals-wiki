# AIW Polish Rework · 2026-08-03

- run_id: `20260803-094234-rework-66741ef6`
- lane: `aiw-polish-rework`
- target: `src/part2-performance/ch07-smoothness/09-perceived-smoothness.md`
- mode: `rework`
- status: `fixed-quality-markers`

## 原问题

预运行质量标记：

1. `pending-verification-marker`：章节正文前置 `outline-start` 加工指引保留阶段性“待验证”提示，容易被发布前质量扫描误判为未闭合验证项。
2. `thin-source-marking`：正文引用了 `Choreographer` API 与 `View.reportAppJankStats()`，frontmatter `sources` 已覆盖 AOSP/Perfetto/AppJankStats/RelativeFrameTimeHistogram，但缺少对应官方 API source 条目。

## 修复点

- 删除章节正文开头的 `outline-start` / `outline-end` 加工指引块；正文不再保留阶段性待核验提示。
- 在 frontmatter `sources` 增加：
  - `https://developer.android.com/reference/android/view/Choreographer`
  - `https://developer.android.com/reference/android/view/View#reportAppJankStats%28android.app.jank.AppJankStats%29`
- 更新 rework 元数据：`rework_count=4`、`rework_date=2026-08-03`、`rework_by=aiw-polish-rework`、`last_rework_at`、`last_rework_run_id`、`last_rework_log`。
- 将状态从历史 `finalized / ready-to-publish` 回流到 `ready-for-review / task6_pending / task6_state=revisiting`，等待 Task6 复审确认。
- 更新 `last_verified=2026-08-03`、`confidence=medium-high`，Android baseline 仍为 `android-17.0.0_r1` / Android 17。

## 剩余风险

- 本轮未引入 Android 18/API38+ 结论，未新增章节，未修改 `src/SUMMARY.md`。
- 正文技术结论沿用既有 Task9 pass-tech-review；本轮只处理质量标记、source 覆盖与流程状态。
- 章节需由 Task6 复审后再决定是否重新晋升 finalized。

## 校验结果

- `python3 scripts/check-metadata.py --files src/part2-performance/ch07-smoothness/09-perceived-smoothness.md`：通过（1 个文件，0 失败，0 警告）。
- `python3 scripts/check-summary-links.py`：通过；输出 2 条既有 duplicate SUMMARY warning，非本轮修改。
- `git diff --check`：通过。
- 目标章节检索确认不再包含 `outline-start` / `outline-end` / `pending-verification` / `needs-rework` / `[待验证]`。
