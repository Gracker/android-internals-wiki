# Task2B Main 回炉修复 · 2026-05-28 04:50

## 本轮处理

- `src/part2-performance/ch12-apk-network/02-network-performance.md`
- `src/part2-performance/ch12-apk-network/03-network-performance-deep.md`

## 入口

- queue.json 中无 `task6-review` / `task9-deep-tech-review` / `external-ai-review` pending 条目。
- 通过 frontmatter backlog fallback 命中 `task2b_state: pending` / `pipeline_stage: task2b_pending`。
- 已读取并遵守 `technical-writing/SKILL.md` 与 Task2B 并发锁协议。
- 两个章节均加锁成功；本轮无锁冲突。

## 修复摘要

### 12.2 网络性能优化

- task9 / P2：OkHttp EventListener 验证锚点仍指向 Android Developers 不存在页面。已改为 Square OkHttp EventListener 官方文档。
- task9 / P2：16KB page size 对 Cronet 冷启动写入无来源 10% 数值。已删除固定百分比，改为按 provider、ABI、设备和 `dlopen` / provider install / 首次请求发出时间点实测。
- task9 / P1：Android 16 + eBPF bandwidth estimator 被写成 `getLinkDownstreamBandwidthKbps()` 可依赖语义变化。已改为系统估算值，只适合粗粒度分档，真实吞吐和时延以 EventListener / APM 实测为准。
- task9 / P3：`addAllowedQuicHost()` 被写成 0-RTT 开关。已改为 QUIC host allowlist，并补 `enableTlsZeroRtt(boolean)`、session state 与 replay 防护边界。

### 12.3 网络性能深入

- task9 / P1：Android 16 `DnsResolver` Predictive Prefetching 缺公开 API / 行为锚点。已删除平台确定性结论，改写为 App 侧受控 DNS 预解析策略，强调线程、TTL、并发和取消边界。

## 状态更新

- 两章 frontmatter 均更新为：
  - `task2b_state: fixed`
  - `task2b_result: fixed`
  - `task6_state: revisiting`
  - `task9_state: pending`
  - `pipeline_stage: task6_pending`
- `metadata/progress.json` 已同步 `sections` / `sections_status` 与本轮 Task2B 历史。
- queue.json 无对应 pending 条目，本轮未修改 queue。
- 未自动晋升 finalized：两章 `task9_result` 仍为 `needs-rework`，需回流 Task6 / Task9。

## 验证

- 已复扫本轮命中的旧错误关键词：Android Developers okhttp3 EventListener、Cronet 10%、Android 16 eBPF bandwidth estimator、Android 16 DnsResolver Predictive Prefetching。
- 已对修改段落做禁用词扫描；未发现本轮新增禁用词。
