# Task2B 主修复 · 2026-05-28 08:50

## 本轮目标

- `src/part3-tools/ch13-perfetto/10-perfetto-sql-cookbook.md`
- `src/part3-tools/ch13-perfetto/15-bufferqueue-blocking-perfetto.md`

## 问题来源

- task9 audit / P0：13.10 `main_lock_span` 未输出 `utid`，但 `SPAN_JOIN(main_lock_span PARTITIONED utid, sp_sched)` 依赖该分区列。
- task9 audit / P1：13.10 多处 SQL 精确匹配 `slice.name = 'Choreographer#doFrame'`，Android 12+ trace section 会追加 vsync id。
- task9-deep-tech-review / P1：13.15 把 `BUFFER_RELEASE_CHANNEL` 写成 Android 14 之后的通用机制；复核口径为 android14/15 release 未命中，Android 16+/main 待验证。

## 修复结果

- 13.10：将 doFrame 查询改为 `GLOB 'Choreographer#doFrame*'`，补充 Android 10/11 与 Android 12+ trace section 命名边界；`SPAN_JOIN` 示例补 `blocked_utid AS utid`，并对 `sp_sched` 同样按 `utid` 分区。
- 13.15：将标题、outline 和正文统一为 `Android 16+/main BUFFER_RELEASE_CHANNEL 待验证`，android14/15 保持传统条件变量路径口径。

## 状态回流

- 两节 frontmatter 已更新为 `task2b_state: fixed`、`task2b_result: fixed`、`task6_state: revisiting`、`task9_state: pending`、`pipeline_stage: task6_pending`。
- 本轮为 frontmatter backlog fallback，无 pending queue 条目可完成。
- 未自动晋升 finalized：两节当前 `task9_result` 仍为 `needs-rework`，需回流 Task6/Task9。
