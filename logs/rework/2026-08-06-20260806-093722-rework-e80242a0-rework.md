# AIW rework evidence · 2026-08-06

- run_id: `20260806-093722-rework-e80242a0`
- mode: `rework`
- target: `src/part5-app/ch26-observability/05-online-troubleshooting.md`
- upstream quality_flags: `pending-verification-marker`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`

## 原问题

预检把章节标记为存在待验证类残留标记。复核正文后，正文没有 Android 18/API 38+ 结论，也没有未收束的 `needs-rework` 段落；触发点来自证据包模板中用于说明证据强弱的一句“尚待验证”，该短语会被机器规则识别为待验证标记。

## 修复点

1. 将证据包模板中的“尚待验证”改为“证据仍不充分”，保留原本“区分证据强度”的含义，同时避免被质量扫描误判为流程状态标记。
2. 更新 frontmatter：
   - `status` / `pipeline_stage` 调整为 `ready-for-review`。
   - `task6_state` / `task9_state` 调整为 `pending-review`，等待后续 review / deep-review lane 复核本次有界修改。
   - 更新 `last_rework_at`、`last_rework_run_id`、`last_rework_log`、`rework_result`、`rework_notes`、`last_verified`。
3. 未新增章节；未修改 `src/SUMMARY.md`；未引入 Android 18/API 38+ 内容。

## 剩余风险

- 本次只处理机器标记误报与流程状态，不重新展开官方文档深度复核。
- 章节已从 finalized 回退到 ready-for-review，后续 task6/task9 应完成轻量复查后再决定是否重新 finalized。
- `check-summary-links.py` 当前仍受仓库既有 `src/SUMMARY.md` 缺失/重复链接阻塞；本 lane 禁止修改 `src/SUMMARY.md`，未在本次修复。

## 校验记录

- `python3 scripts/check-metadata.py --files src/part5-app/ch26-observability/05-online-troubleshooting.md`：通过。
- `python3 scripts/check-summary-links.py`：失败，失败项指向既有 SUMMARY 链接问题，非目标章节新增。
- `git diff --check`：通过。
