# AIW Rework Log · 2026-07-27

- lane: rework
- run_id: `20260727-213543-rework-30f4d38e`
- target: `src/part3-tools/ch14-other-tools/28-gpu-performance-profiling-advanced.md`
- baseline: Android 17 / `android-17.0.0_r1`

## 原问题

Task6 复查指出该章标题与 outline 覆盖范围过宽：跨厂商阈值、Ray Tracing、NPU/ML、profileable 权限差异、AGI 工作流等主题缺少一手证据；同时早期稿件需要固定 Android 17 的 Perfetto 路径与字段边界，避免把上游后续字段写成 `android-17.0.0_r1` 结论。

## 修复点

1. 将章节标题收窄为“Perfetto GPU Counter 与 GPU Memory 事件分析”，不再声称已覆盖跨厂商性能阈值和完整工作负载剖析。
2. 移除原 outline 中未取证的 Ray Tracing、NPU/ML、远程调试、AGI 实战等正文候选结构；改为“范围外主题”列表，明确不得作为本章结论。
3. 保留并重写可由 `external/perfetto` 一手源码支撑的内容：
   - `GpuCounterDescriptor` 语义分组、单位、`GpuCounterSpec` 字段边界与 `GpuCounterBlock.block_capacity`。
   - `GpuCounterEvent` descriptor 直挂 / interned descriptor 两种 emission 模式。
   - `gpu_event_parser.h/.cc` 中 counter state 与 `last_id` 的 Android 17 实现位置。
   - `gpu_counter_span_view.sql` 的 `LEAD() OVER (PARTITION BY track_id ORDER BY ts)` 区间化逻辑。
   - `gpu_mem_event.proto` 中由 Android `GpuService` 生成的 `GpuMemTotalEvent`。
4. 更新 frontmatter：`status=ready-for-review`、`task6_state=reworked`、`task9_state=ready-for-review`、`pipeline_stage=rework_verified`、`last_rework_at`、`last_rework_run_id`、`last_verified`、`confidence=medium-high` 与 `rework_resolution`。
5. 关闭 review finding `finding-a29e53cc27ec`，理由为已通过收窄范围和删除未取证结论解决 source-boundary 问题。

## 剩余风险

- 本章仍不提供厂商私有 counter 等价映射、性能阈值、Ray Tracing / NPU / AGI 工作流结论；这些主题需要独立 source-apply 或实验材料后再扩写。
- 未修改 `src/SUMMARY.md`，因此目录显示名如需同步，应由非本 rework lane 后续处理。

## 校验

- `python3 scripts/check-metadata.py --files src/part3-tools/ch14-other-tools/28-gpu-performance-profiling-advanced.md`：通过，1 个文件、0 失败、0 警告。
- `python3 scripts/check-summary-links.py`：通过，保留 2 条既有 duplicate SUMMARY warning（本 lane 禁止修改 `src/SUMMARY.md`）。
- `git diff --check`：通过，无输出。
