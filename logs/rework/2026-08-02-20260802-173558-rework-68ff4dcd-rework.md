# AIW Polish Rework · 2026-08-02 · 20260802-173558-rework-68ff4dcd

- lane: aiw-polish-rework
- mode: rework
- target: `src/part2-performance/ch02-rendering/2.3-gpu-debug-performance-tools.md`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`; kernel anchor `android17-6.18-2026-06_r6`
- input status: `polish_candidate_ready`
- quality flags: `pending-verification-marker`

## 原问题

上游 finalize/rework 判定指出：章节正文在 outline 后已基本改写为可采信的 Android 17 源码复核版本，但文件前部 `outline-start` / `outline-end` 保护区仍保留大量已被否定的伪结论，且出现在正文警告之前，容易被读者误当作结论。主要包括：

- `FPSReporter` 60 帧 `mFrameIntervalHistory` 伪代码、`gpu.timestamp` 事件；
- `RenderEngine::present()` 中虚构 JankTracker 插桩与固定阈值分类；
- 独立 `android.view.FrameTimeline.java`、`gpu.frame` proto；
- `gfxinfo.gpu_*` / memory bandwidth 属性与 `dumpsys gfxinfo ... gpu` 子命令；
- 固定调试开销、多窗口 GPU 分配比例、`gpu-debugd` 服务等 Android 17 AOSP 未支持的说法。

## 本轮修复

1. 删除整段过期 `outline-start` / `outline-end` 保护区及其前后仅用于隔离该区块的 CAUTION 提示。
2. 保留并前置校正后的正文：FrameTimeline、FpsReporter、JankTracker、`dumpsys gfxinfo`、SurfaceFlinger/HWC、Perfetto GPU data source、AGI、`gpuservice` 等内容继续以 `android-17.0.0_r1` 为锚点。
3. 更新 frontmatter：
   - `task6_state: ready-for-review`
   - `task9_state: ready-for-review`
   - `pipeline_stage: rework-verified`
   - `last_verified: 2026-08-02`
   - `confidence: high`
   - 新增 `last_rework_at`、`last_rework_run_id`、`last_rework_reason`
4. 关闭机器可读 finding：`finding-659700187567` → `fixed`。

## 剩余风险

- 本轮未新增 Android 18 / API 38+ 结论。
- 章节仍包含对 `gpu.timestamp`、`gfxinfo.gpu_*`、`gpu-debugd` 等词的否定性说明；这些是正文纠错内容，不是待验证标记。
- 设备侧 GPU counter、render stages、BPF/tracepoint 支持仍需实机验证，正文已按能力边界表述。
