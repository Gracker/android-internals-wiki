# AIW Polish Rework · 20260812-173533-rework-5b334e2d

- lane: aiw-polish-rework
- mode: rework
- target: `src/part1-fundamentals/ch05-cpu-power/03-big-little.md`
- generated_at: 2026-08-12T17:44:11+08:00
- finding: `AIW-FRESH-014f54bb39766b56`
- Android baseline: `android-17.0.0_r1` / `android17-6.18-2026-06_r6`

## 原问题

`AIW-FRESH-014f54bb39766b56` 标记本章 `last_verified=2026-04-29`，超过 90 天未刷新来源核验日期。

## 核验范围

本轮没有改写读者正文；仅处理 supplied stale source verification finding。复核固定到 Android 17 / API 37、AOSP `android-17.0.0_r1` 与 Android common kernel `android17-6.18-2026-06_r6`。检查范围包括：

- Android common kernel scheduler docs/source：`Documentation/scheduler/sched-capacity.rst`、`Documentation/scheduler/sched-energy.rst`、`kernel/sched/fair.c`、`kernel/sched/cpufreq_schedutil.c`。
- AOSP Android 17 task profile controls：`system/core/libprocessgroup/profiles/task_profiles.json`。
- Perfetto：CPU scheduling docs、stdlib `linux.cpu.frequency` documentation、`external/perfetto/.../stdlib/linux/cpu/frequency.sql`。
- Arm public documentation kept as architectural background; no device-specific capacity, latency or DVFS guarantee was derived from Arm marketing/spec overview.

## 修复点

- 将 frontmatter `status` 从 `finalized` 回流为 `ready-for-review`，符合 rework lane 修复后不能直接 finalized 的规则。
- 将 `last_verified` 从 `2026-04-29` 刷新为 `2026-08-12`。
- 扩展 `last_verified_against`，明确核验基线为 Android 17 / API 37、`android-17.0.0_r1`、`android17-6.18-2026-06_r6` scheduler docs/source、AOSP task_profiles、Perfetto CPU scheduling/frequency docs/source，并把 Arm 文档限定为背景来源。
- 增加 `last_rework_at` / `last_rework_run_id`。
- 扩展 frontmatter sources：补入 Perfetto `linux.cpu.frequency` stdlib 文档/source、Linux sched-capacity/schedutil docs、Android common kernel scheduler docs/source、AOSP task_profiles。
- 将 `task6_state` / `task9_state` 回流到 `pending-review`，`pipeline_stage` 回流到 `ready-for-review`。
- 关闭 machine-readable finding `AIW-FRESH-014f54bb39766b56` 为 `verified`。

## 证据摘要

- `sched-capacity.rst`：核对 `capacity(cpu) = work_per_hz(cpu) * max_freq(cpu)` 与 `arch_scale_cpu_capacity()` original capacity 描述，支撑 capacity/work-per-Hz/max_freq 边界。
- `sched-energy.rst`：核对 EAS 依赖 CPU Energy Model，root domain over-utilized 约 80% compute capacity 的边界，支撑 EAS 适用范围说明。
- `fair.c`：核对 `fits_capacity(cap, max) ((cap) * 1280 < (max) * 1024)`、`util_fits_cpu()`、`find_energy_efficient_cpu()` 与 `select_task_rq_fair()` 路径，支撑 util/capacity margin、wake-up placement 与 EAS 候选选择描述。
- `cpufreq_schedutil.c`：核对 `sugov_get_util()`、`sugov_effective_cpu_perf()`、`map_util_perf()` DVFS headroom、`sugov_next_freq_shared()`、`SCHED_CPUFREQ_IOWAIT` 与 `scx_cpuperf_target()`，支撑 Android 17 schedutil 选频描述。
- `task_profiles.json`：核对 `cpu.uclamp.min`、`cpu.uclamp.max` 与 cpuset controller/profile entries，支撑 Android task profile / UClamp / cpuset 控制面。
- Perfetto CPU scheduling docs：核对 scheduling data 暴露为 `sched_slice` SQL table。
- Perfetto `frequency.sql`：核对 `cpu_frequency_counters` 表定义与 `freq LONG` 字段，支撑正文频率查询入口。

## 本地 gate

- `python3 scripts/check-metadata.py --files src/part1-fundamentals/ch05-cpu-power/03-big-little.md`: pass
- `python3 scripts/check-summary-links.py`: pass
- `git diff --check`: pass
- Gracker Writing hard gate: 读者正文未修改；正文 forbidden contrast/filler/evaluative/structural-meta/meaning-inflation/process-meta 命中均为 0，未增加。

## 状态流转

- result: `metadata-only`
- next gate: Task6 / Task9 复审
