# Rework Log — 2026-07-30 21:35:26 (Asia/Shanghai)

- **Run ID:** `20260730-213526-rework-9dc6f657`
- **Lane:** rework
- **Profile:** aiw-polish-rework
- **Mode:** rework
- **Status in:** `polish_candidate_ready`
- **Quality flags in:** `pending-verification-marker`

## Target

| Field | Value |
|-------|-------|
| Path | `src/part2-performance/ch12-apk-network/08-networkagent-lifecycle-scoring.md` |
| Chapter / Section | 12.8 |
| sha16 | `b92075c5accc593b` |
| Baseline | AOSP `android-17.0.0_r1` (Android 17, API 37, kernel 6.18) |

## 原问题清单

1. **Markdown 渲染断裂** — 正文评分因子列表中 `****`candidate`**`** 因 bold/code 嵌套错乱，渲染出多余的 `**`，且该"candidate"字段本身不成立。
2. **`candidate` 字段不成立** — `NetworkScore`（`packages/modules/Connectivity/.../NetworkScore.java`）没有名为 `candidate` 的字段；`NetworkScorecard` 是独立历史记录类，不会内嵌进每次 `sendNetworkScore()` 上报的 `NetworkScore` 对象。原文把二者混淆。
3. **MPTMP 笔误** — 协议名写成 `MPTMP`，正确为 `MPTCP`（Multipath TCP）。且"长期处于禁用状态"过于笼统，未锚定 Android 17 / kernel 6.18 基线。
4. **VPN 评分表错误** — "声明值 + 101" 在数学上无法解释（101 已是上限）；AOSP 语义是整数 `101` 表示无条件接管。
5. **`[待验证]` 标记 ×2** — PSI 小节中 `NetworkScorecard` 功耗因子被标 `[待验证]`，但在 `android-17.0.0_r1` 源码里该类根本没有能耗字段，应转为确定性边界结论而非开放验证项。

## 修复点（safe bounded patches）

| # | 位置 | 修复内容 |
|---|------|----------|
| 1 | §NetworkScore 对象描述 | 删除不存在的 `candidate` 字段；改为准确描述 `legacyInt` / `transportInfo` / `policies`；新增段落说明 `NetworkScorecard` 是独立排序参考、不内嵌进 NetworkScore；附 `[边界]` 注明更高维度字段以源码为准 |
| 2 | §网络切换延迟 | `MPTMP` → `MPTCP`；重写为锚定 `android-17.0.0_r1` / kernel 6.18 的确定性表述：GKI 有 `CONFIG_MPTCP` 但 CS 路径不协商子流 |
| 3 | §OEM 评分权重表 | VPN 行 "声明值 + 101" → "101（整数 101 表示无条件接管）" |
| 4 | §PSI 功耗因子 | 删除 `[待验证]`；改为基于源码的确定性结论：NetworkScorecard 无能耗字段，功耗由 OEM 通过 legacyInt 基线间接体现 |
| 5 | frontmatter | 新增 `last_verified: 2026-07-30`、`last_rework_at`、`last_rework_run_id`、`pipeline_stage: ready-for-review`、`task6_state: revisiting`、`task9_state: pending`、`rework_notes`；删除重复的旧 `last_verified: 2026-06-25` |

## 状态流转

- `status`: 保持 `ready-for-review`（本次是 content/accuracy 修复，技术结论可复审，不引入 needs-rework 阻断）
- `pipeline_stage`: → `ready-for-review`
- `task6_state`: → `revisiting`（内容已改，等待 Task6 复审确认）
- `task9_state`: → `pending`（等待 Task9 终审）
- `confidence`: 保持 `medium-high`
- `last_verified`: → `2026-07-30`

## 剩余风险 / 未关闭项

1. **源码行号漂移** — 文中 `ConnectivityService.java:10422 / 6287 / 13048 / 13092 / 13688` 等行号来自 `android-17.0.0_r1` 快照；如后续 rebase 到新 tag 行号会变，但内容签名（方法名 `registerNetworkAgentInternal` / `destroyNetwork` / `rematchNetworksAndRequests`）稳定，不影响复现。
2. **OEM 评分默认值** — Wi-Fi 60 / 蜂窝 50 / VPN 101 是 AOSP 默认，OEM 实际值因厂商而异；正文已声明 "AOSP 默认" 与 "OEM 可修改"，无需进一步收敛。
3. **NetworkScore.policies 具体策略常量名** — `POLICY_TRANSPORT_PRIMARY` 等在 `android-17.0.0_r1` 存在但跨版本可能增减；已用 `[边界]` 注明以源码为准。
4. **未新增 finding / 未关闭 finding** — 本章节在 `metadata/review-findings.json` 中没有现存 open finding，本次 rework 不触发 finding 增删（问题来自启发式 quality_flag 而非 review pool）。

## 本地校验结论

- `check-metadata.py --files <target>`: ✅ 通过（0 失败 / 0 警告）
- `check-summary-links.py`: ✅ 通过（164 链接；2 个 duplicate warning 为既有项，与本章节无关）
- `git diff --check`: ✅ 退出码 0（无空白错误）
- 未修改 `src/SUMMARY.md`；未新增 `src/**/*.md` 文件。
