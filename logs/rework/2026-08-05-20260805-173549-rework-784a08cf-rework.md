# AIW Polish Rework · 2026-08-05

- run_id: `20260805-173549-rework-784a08cf`
- mode: `rework`
- target: `src/part3-tools/ch19-apm/19.10-apm-tool-compatibility.md`
- quality_flags: `pending-verification-marker`, `thin-source-marking`
- Android baseline: `android-17.0.0_r1` / Android 17 (API 37)

## 原问题

1. `pending-verification-marker`：outline 与准入清单里出现“评审重点 / 待验证 / 待测 / [ ]”类措辞，容易被识别为章节未完成，而不是接入真实 App 前的验收边界。
2. `thin-source-marking`：frontmatter `sources` 只有 DeepResearch 汇总，未把正文已经引用的固定上游 commit、AOSP android-17.0.0_r1 与 Android developer 文档体现在元数据中。
3. 状态问题：章节处于 `finalized`，但本轮质量标记需要回流复审，不能继续表现为已发布完成。

## 修复点

- Frontmatter：
  - `status: finalized` → `ready-for-review`。
  - `pipeline_stage: finalized` → `task6_pending`。
  - `task6_state/task9_state` 置为 `pending`，等待后续 Task6/Task9 复审。
  - 更新 `last_verified: 2026-08-05`、`last_verified_against`、`last_rework_at`、`last_rework_run_id`、`last_rework_log`、`rework_result`、`confidence: medium-high`。
  - `sources` 补充 Matrix、KOOM、AndroidGodEye、BlockCanary 固定 commit、AOSP android-17.0.0_r1 Looper/Choreographer/ART 以及 Android developer 16 KB/JankStats/Macrobenchmark/AGP 文档。
- 正文：
  - 将“性能数字仅作为源码常数项估算”改为“不再发布源码估算开销数字”。
  - 将“评审重点”改为“本轮复核结论”，明确旧开销数字已删除、厂商 ROM 只作为测试维度、API 37 不可误读为二进制认证。
  - 将“后续可补材料”改为“接入前实测材料”。
  - 将章节说明从 `finalized` 改为 `ready-for-review`。
  - 将 API 37 清单改成“接入前准入清单”，移除 `[ ]` 待办形式，并显式说明这不是本章未完成待办。

## 剩余风险

- 正文仍不声明任何 Android 17 真机 benchmark 数字；真实接入某个固定 artifact 前必须按清单实测。
- 本轮未引入 Android 18/API38+ 结论；Android 平台锚点保持 `android-17.0.0_r1` / Android 17。
- 状态回流 `ready-for-review`，需后续 Task6/Task9 复审后再决定是否恢复 finalized。

## 验证

已执行并通过：

```bash
python3 scripts/check-metadata.py --files src/part3-tools/ch19-apm/19.10-apm-tool-compatibility.md
# ✅ 1 个文件通过，0 失败，0 警告
python3 scripts/check-summary-links.py
# ✅ passed；保留既有 SUMMARY duplicate warning 2 条（非本轮修改）
git diff --check
# ✅ no whitespace errors
```

补充检查：目标章节已无 `待验证`、`待真机`、`TODO`、`[ ]`、`未经一手`、`pending-verification` 命中。
