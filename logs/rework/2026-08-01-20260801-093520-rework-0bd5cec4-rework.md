# Rework · 2026-08-01 09:35 (run 20260801-093520-rework-0bd5cec4)

## Target
- 章节：7.10 图片加载与 Bitmap 性能优化
- 文件：src/part2-performance/ch07-smoothness/10-image-bitmap-performance.md
- mode：rework
- run_id：20260801-093520-rework-0bd5cec4
- quality_flags：pending-verification-marker, thin-source-marking

## 原问题（上游 quality_flags + 正文检查）

1. **thin-source-marking**：frontmatter `sources:` 仅列一条 research feed。正文实际引用 7 个 AOSP android-17.0.0_r1 源码文件和 11 条官方文档，但 YAML `sources` 未体现这些一手锚点，导致元数据质量被标薄。
2. **pending-verification-marker / confidence 不一致**：`confidence: medium` 与最近一轮 deep-review（2026-07-03 12:32）给出的全部维度 5/5、P0/P1/P2 = 0/0/0 结论不匹配。章节实际已通过 tech-review 并自动晋升 finalized，confidence 应上升。
3. **sources YAML 缩进断裂**（正文检查发现）：`sources:` 下的 `path:` 行缩进与 `- type: research` 列对齐，而非缩进 2 格。YAML 解析器将其解析为顶层独立 key `path`，导致 `sources` 列表中只有 `{type: research}`（丢失 path），且 frontmatter 多出非法顶层 `path` 字段。
4. **frontmatter 残留 stray `-`**：`---` 闭合标记与 `# 7.10` 标题之间存在一行仅含 `-` 的残留行（历史编辑痕迹），虽不影响 Markdown 渲染，但不符合发布稿规范。

## 修复点

| # | 问题 | 修复 |
|---|------|------|
| 1 | sources YAML 缩进断裂 | 将 `path:` 行缩进到 `- type:` 下 2 格，使其成为 list item 子字段。移除 YAML 解析产生的非法顶层 `path` key。 |
| 2 | sources 单薄 | 补充 5 条 AOSP android-17.0.0_r1 一手源码锚点（BitmapFactory.java、BitmapFactory.cpp、Bitmap.java、ImageDecoder.java、SkiaGpuPipeline.cpp）和 2 条官方文档（manage-memory、load-bitmap），共 8 条 source。 |
| 3 | confidence 与 review 结论不一致 | `confidence: medium` → `confidence: medium-high`。依据：2026-07-03 Task9 deep-review 全维度 5/5、P0/P1/P2 = 0/0/0、自动晋升 finalized。未设 high 因格式性能基准数据仍标注为非 benchmark。 |
| 4 | stray `-` 残留 | 删除 `---` 与 `# 7.10` 之间的 stray `-` 行。 |
| 5 | rework 追踪字段缺失 | 新增 `last_rework_at`、`last_rework_run_id`、`last_rework_by`、`last_rework_reason`。 |

## 正文未改动说明

本轮不修改章节技术正文。正文内容已经过多轮 Task6/Task9 复审（最后通过日期 2026-07-03），所有技术声明均有源码锚点支撑，无 P0/P1/P2 活跃 issue。rework 聚焦 frontmatter 元数据质量和发布稿格式清理。

## 剩余风险

- 格式对照性能数据仍以定性描述为主（章节已声明非 benchmark），不影响技术准确性。
- AVIF 硬件解码路径已收束为"按设备验证"，未引入未验证量化口径。
- SUMMARY.md 中 2 条 duplicate link 警告（4.9 MTE、9.10 ANR callback）为既有问题，与本章节无关。

## 校验结果

- `python3 scripts/check-metadata.py --files src/part2-performance/ch07-smoothness/10-image-bitmap-performance.md`：✅ 1 通过
- `python3 scripts/check-summary-links.py`：✅ passed（2 条既有 duplicate warning，非本章节）
- `git diff --check`：✅ 无 whitespace error
