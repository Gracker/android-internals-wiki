# AIW Polish Rework Log · 2026-08-08

- run_id: `20260808-093600-rework-39378b94`
- mode: `rework`
- target: `src/part3-tools/ch13-perfetto/16-agent-perfetto-analysis-protocol.md`
- chapter: `13.16 Agent 辅助 Perfetto 分析协议`
- input status: `polish_candidate_ready`
- input quality_flags: `pending-verification-marker`

## 原问题

预运行脚本标记章节存在待验证状态残留。人工复核 frontmatter 与正文后，主要风险是验证口径仍使用较宽的 `linux.cpu.utilization.*` 表述，且正文“官方资料与固定版本”只给出 Android 17 stdlib 根目录，没有把关键 stdlib 模块的固定源码路径展开到可直接复查的锚点。

## 修复点

1. 更新 frontmatter：
   - `last_verified` → `2026-08-08`
   - `last_rework_at` / `last_rework_run_id` / `last_rework_log` 指向本轮 rework
   - `rework_notes` 改为本轮待验证标记关闭说明
   - `confidence` 从 `medium` 提升到 `medium-high`
   - `task6_result`、`task9_result` 更新为 `rework-applied`，保留 `status: ready-for-review` 与 Task6/Task9 pending-review 回流状态
2. 将 `last_verified_against` 中的 `linux.cpu.utilization.*` 收窄为已核验入口 `linux.cpu.utilization.process`。
3. 在 frontmatter `sources` 中补入 Android 17 固定标签下的关键 stdlib 源码锚点：
   - `slices/time_in_state.sql`
   - `slices/cpu_time.sql`
   - `linux/cpu/utilization/process.sql`
4. 在正文“官方资料与固定版本”补充关键 stdlib 模块固定链接，覆盖启动、帧、Binder、slice、sched、CPU frequency 与 process utilization 入口。

## 版本边界

本轮没有引入超出 Android 17 / `android-17.0.0_r1` / `android17-6.18-2026-06_r6` 的主线结论。正文继续保留“主机侧 Trace Processor 能改变解析/stdlib 能力，但不能补出设备未采集数据”的边界。

## 剩余风险

- 本章是协议型章节，不绑定单一实机 trace；SQL 模板语义仍需在团队 golden trace / smoke test 中持续验证。
- 厂商 ROM、userdebug 符号、电源轨和 GPU 计数器可见性仍按采集条件降级，不作为通用保证。

## 状态流转

- `status`: `ready-for-review`（保持）
- `pipeline_stage`: `ready-for-review`（保持）
- `task6_state`: `pending-review`（等待复审）
- `task9_state`: `pending-review`（等待复审）
- `task6_result`: `rework-applied`
- `task9_result`: `rework-applied`
