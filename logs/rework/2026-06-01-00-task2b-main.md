# Task2B Main 回炉修复 · 2026-06-01 00:50

## 本轮处理

- `src/part1-fundamentals/ch02-rendering/05-main-render-thread.md`

## 来源

- frontmatter backlog fallback
- 问题来源：`logs/deep-review/2026-05-17-14-audit.md`
- 章节：2.5 MainThread 与 RenderThread 协作
- 优先级：P95

## 修复摘要

1. task9 / P1：`syncFrameState` 长耗时不能单独归因为上一帧 GPU 未完成。正文改为 RenderThread 同步阶段等待，并要求结合 `DrawFrame`、`dequeueBuffer`、fence、Frame Timeline 判断。
2. task9 / P1：DeliQueue 不能写成 RenderThread 内部 WorkQueue 替代实现。正文改为 Android 17 `android.os.MessageQueue` 行为变化，并补 AOSP android-16.0.0_r1 HWUI `WorkQueue.h` 仍使用 `std::mutex` / `std::vector<WorkItem>` 的边界。
3. task9 / P1：RenderThread ADPF hint session 不能写成 Android 16 才接入。正文改为 android-14.0.0_r1 起 `CanvasContext.cpp` 已有 `HintSessionWrapper` / `reportActualWorkDuration()`，Android 16 headroom API 单独说明。

## 状态更新

- 章节 frontmatter：`status: ready-for-review`、`pipeline_stage: task6_pending`、`task6_state: revisiting`、`task9_state: pending`、`task2b_state: fixed`。
- `metadata/progress.json`：记录本轮 Task2B main 修复与 2.5 章节状态。
- `metadata/queue.json`：无对应 pending 条目，本轮未修改。

## 验证

- Android 版本边界：未引入 Android 18 / API 38+ 内容。
- frontmatter：未发现重复 key。
- 禁用词扫描：本轮新增 / 修改段落未命中文风禁令词库。
- 自动晋升 finalized：不满足。`task9_result` 仍为 `needs-rework`，需回流 Task6 / Task9。
