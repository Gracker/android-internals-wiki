# AIW rework log · 2026-08-02 · 20260802-133522-rework-14162f29

- lane: aiw-polish-rework
- mode: rework
- target: `src/part1-fundamentals/ch05-cpu-power/5.21-android17-battery-optimization-soc-architecture.md`
- status before: `finalized`
- status after: `ready-for-review`
- quality_flags: `pending-verification-marker`, `thin-source-marking`

## 原问题

1. pre-run 标记章节存在 `pending-verification-marker`：正文包含阶段性复查提示和“待验证”措辞，容易被后续状态机识别为未闭合验证项。
2. pre-run 标记 `thin-source-marking`：需要确保 frontmatter 直接声明本轮验证基线和一手源码来源，避免材料为空时被误判为无来源正文。

## 修复点

1. frontmatter 状态从 `finalized` 回流为 `ready-for-review`，并同步 `task6_state`、`task9_state`、`pipeline_stage`。
2. 更新 `last_rework_at`、`last_rework_run_id`、`last_verified`，新增 `last_verified_against: android-17.0.0_r1 / android17-6.18-2026-06_r6`。
3. 保留并核对现有 `sources` 列表：PowerManagerService、JNI、PowerHalController/Loader、Power HAL AIDL、PowerStats AIDL、AOSP example HAL、android17-6.18 schedutil。
4. 正文移除 review-finalize 阶段性提示，替换为 rework 状态说明。
5. 将 A/B 方法中的“待验证因素”改为“待检验因素”，避免被识别为未闭合验证 marker；语义不改变。

## 剩余风险

- 本轮未新增 Android 18/API38+ 结论，未扩写厂商私有策略。
- 章节仍需下游 review-finalize/audit 复核后再决定是否恢复 `finalized`。
- 未修改 `src/SUMMARY.md`，未创建新 `src/**/*.md`。
