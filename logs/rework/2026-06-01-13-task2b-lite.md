# Task2B Lite 回炉小修 - 2026-06-01 13:35

- lane: Task2B Lite
- source: frontmatter backlog fallback
- section: 20.5 OOM 治理
- file: `src/part5-app/ch20-stability/05-oom-governance.md`
- result: fixed-lite
- lock: locked

## 问题来源

- `logs/deep-review/2026-05-19-00-deep-review.md`
  - P1: FD 耗尽不应等价于 ART `OutOfMemoryError` 投递路径，应改成资源型崩溃/初始化失败并说明为何放入 OOM 治理章。
  - P1: `sigsetjmp` / `siglongjmp` Native Crash 兜底缺 async-signal-safe、锁/堆/JNI 状态和安全退出边界。
  - P2: `mallopt(M_PURGE, 1)` 建议改为 `mallopt(M_PURGE, 0)`。
  - P2: Binder 连接不应归到 Socket FD 示例。

## 改动摘要

- 将 FD 小节标题与正文改为资源型崩溃/初始化失败口径，补齐与 ART `OutOfMemoryError` 的边界。
- Socket FD 示例移除 Binder，改为 HTTP/WebSocket。
- `mallopt(M_PURGE, 1)` 改为 `mallopt(M_PURGE, 0)`。
- `sigsetjmp` / `siglongjmp` 两处补充 async-signal-safe、状态一致性和安全退出边界。
- frontmatter 回流到 `task6_pending`，`task2b_result` 标记为 `fixed-lite`。

## 验证

- 正文改动控制在 Lite 范围内，未新增小节，版本边界合规。
- `mallopt(M_PURGE, 0)` 参考本地代码：`/Users/gracker/Github/matrix/matrix/matrix-android/matrix-mallctl/src/main/cpp/mallctl/MallCtl.cpp` 与 Perfetto `src/base/utils.cc`。
