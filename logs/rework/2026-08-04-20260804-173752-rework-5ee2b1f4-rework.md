# AIW rework log · 20260804-173752-rework-5ee2b1f4

- run_id: `20260804-173752-rework-5ee2b1f4`
- mode: `rework`
- target: `src/part3-tools/ch19-apm/20-solopi-emmagee.md`
- quality_flags: `pending-verification-marker`, `thin-source-marking`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`；内核引用保持 `android17-6.18-2026-06_r6`

## 原问题

1. frontmatter 中 `sources` 列表缩进错误，`path` 被解析为顶层字段，且 `task9_review_notes` 包含未加引号的冒号，导致 YAML 解析失败。
2. frontmatter 只保留 Android 13 Restricted Settings 一个来源，和正文大量固定提交 / Android 17 来源不匹配，触发 thin-source-marking。
3. frontmatter 后、正文标题前存在单独 `-` 行，表现为未完成占位 / pending-verification marker。
4. 章节已被标为 `finalized` / `ready-to-publish`，但 rework 问题尚未完成机器校验闭环。

## 修复点

- 修正 `sources` YAML 结构，补充 SoloPi README、SoloPi 构建配置、Emmagee README、Android 17 行为变更与 Restricted Settings 等 frontmatter 来源。
- 为 `task9_review_notes` 加引号，并追加本次 rework 说明，恢复 YAML 可解析性。
- 删除正文开头的孤立 `-` 占位行。
- 将 `status` 调整为 `ready-for-review`，`pipeline_stage` 调整为 `ready-for-review`，`task6_state` / `task9_state` 调整为 `needs-review`，等待复审 lane 重新确认。
- 更新 `last_verified`、`last_verified_against`、`last_rework_at`、`last_rework_run_id`、`last_rework_log`。

## 剩余风险

- 本次只做安全元数据与待验证标记修复，没有新增 Android 18/API38+ 结论。
- 正文技术结论仍以既有固定提交、Android 17 文档与 android17-6.18 内核引用为边界；后续复审应重点确认外链可达性与 Android 17 行为变更页的具体段落映射。
