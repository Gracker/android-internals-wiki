# AIW Polish Rework Log · 2026-08-07

- run_id: `20260807-173540-rework-bf0a3299`
- mode: `rework`
- target: `src/part5-app/ch25-power-size/11-adpf-coroutine-thread-migration.md`
- quality_flags: `pending-verification-marker`, `thin-source-marking`
- baseline: `android-17.0.0_r1` / Android 17 (API 37)

## 原问题

1. 正文末尾存在 `[待验证]` 标记，导致 ready-to-publish/finalized 状态与证据边界不一致。
2. frontmatter `last_verified_against` 仍停留在 android-15/android-16，对正文宣称的 Android 17 源码锚点支撑不足。
3. frontmatter `sources` 指向旧 DeepResearch 文件名，未列出本轮上游提供的两份 ADPF/协程调研材料。
4. 正文对关键版本边界和协程迁移结论已有来源，但缺少足够内联证据标记，容易被判为 thin-source-marking。

## 修复点

- 将章节状态从 `finalized` / `ready-to-publish` 回退为 `ready-for-review`，`task6_state` 改为 `pending-review`，`task9_state` 改为 `rework-fixed`。
- 更新 `last_verified`、`last_verified_against`、`last_rework_at`、`last_rework_run_id`、`last_rework_log`、`rework_result`、`rework_summary`。
- 将旧的 DeepResearch 来源替换/补齐为：
  - `DeepResearch/2026-05-17-kotlin-coroutine-adpf-hint-engineering.md`
  - `DeepResearch/2026-05-27-adpf-performancehintmanager-api-version-boundary.md`
- 在正文关键段落增加 `[已验证:]` / `[来源:]` 标记，覆盖 Android 17 `PerformanceHintManager.java`、SDK `Session.setThreads()`、`PerformanceHintManager.Session` / `WorkDuration` 参考与协程迁移材料。
- 将 `[待验证]` 改为正式“证据边界”说明，明确本章不提供收益百分比、不声称已有实机收益。

## 剩余风险

- 本章仍无 Pixel/厂商机型 Perfetto + 功耗实测数据；已在正文中保留为证据边界，不影响版本边界与工程模型复审。
- `reportActualWorkDuration(WorkDuration)` / `setPreferPowerEfficiency()` 的公开 SDK 边界以当前章节既有 SDK 参考描述保留，本轮未引入 Android 18/API38+ 结论。

## 结果

- rework_result: `ready-for-review`
- 正文修改文件数: 1
- 新增章节: 0
- `src/SUMMARY.md`: 未修改
