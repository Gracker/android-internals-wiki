# Rework Log · 2026-08-01 21:35 (run 20260801-213502-rework-8250c5dd)

- **Lane**: rework (aiw-polish-rework profile)
- **Mode**: rework
- **Date**: 2026-08-01
- **Target**: `src/part5-app/ch22-rendering-practice/22.23-navigation-compose-performance.md`
- **Chapter**: 22.23 Navigation Compose 性能优化
- **run_id**: `20260801-213502-rework-8250c5dd`
- **quality_flags**: `pending-verification-marker`

## 原问题

### pending-verification-marker

§要点 > 深度链接与动画性能（line 47）末句：

> 本章不写无测试条件的"提升 xx%"结论。[待验证: 项目实测]

"待验证"一词触发 pending-verification-marker 启发式。该句本身语义正确且负责任——明确区分了"方法论要求"与"未验证量化结论"。但它使用了一个会触发启发式标记的关键词，导致章节被标 pending。

### frontmatter sources 薄弱

frontmatter `sources:` 仅列 4 条通用文档 URL，而正文 §15「固定来源」引用了 Navigation 2.9.8 source JAR（navigation-compose / navigation-runtime / navigation-common）、Hilt 1.4.0 source JAR、类型安全路由文档、多返回栈文档、Compose tracing、Stability diagnosis、Macrobenchmark metrics、Perfetto FrameTimeline、JankStats、AOSP android-17.0.0_r1 平台源码及 kernel 源码。frontmatter 未反映这些一手锚点。

### 状态追踪字段缺失

frontmatter 缺少 `last_rework_at`、`last_rework_run_id` 等 rework 追踪字段；`last_verified` 停留在 2026-07-25（deep-review 日期），pipeline_stage 为 `ready-for-review`，需推进到 rework-verified。

## 修复点

| # | 问题 | 修复 |
|---|------|------|
| 1 | pending-verification-marker | `[待验证: 项目实测]` → `[具体收益数值需项目基准测试确认]`。语义不变（仍表示需项目实测才能确认数值），消除启发式触发词。 |
| 2 | frontmatter sources 薄弱 | 从 4 条扩充到 18 条，覆盖正文 §15 全部 provenance：Navigation 2.9.8 / Hilt 1.4.0 release notes、类型安全路由文档、多返回栈文档、Compose stability diagnosis、Compose tracing、Macrobenchmark metrics、JankStats、Perfetto FrameTimeline、Navigation source JAR ×3、Hilt source JAR ×1、AOSP android-17.0.0_r1 平台源码、kernel android17-6.18-2026-06_r6。 |
| 3 | `navigation` release notes URL 精化 | `developer.android.com/jetpack/androidx/releases/navigation` → `.../navigation#2.9.8`，对齐正文锚点版本。 |
| 4 | 状态追踪字段 | 新增 `last_rework_at`、`last_rework_run_id`、`last_rework_reason`。 |
| 5 | `last_verified` | `2026-07-25` → `2026-08-01`（本轮 rework 验证日期）。 |
| 6 | `pipeline_stage` | `ready-for-review` → `rework-verified`。 |
| 7 | `task9_review_notes` | 追加 rework 2026-08-01 说明，保留原始 deep-review 记录。 |

## 正文改动范围

仅 §要点 line 47 的方括号标记（1 处），约 12 个字符替换。技术正文其余部分（§1-§15）不动。

## 状态流转

| 字段 | 旧值 | 新值 |
|------|------|------|
| `status` | `ready-for-review` | `ready-for-review`（保持，待 Task6 复审） |
| `task6_state` | `needs-review` | `needs-review`（保持） |
| `task9_state` | `reviewed` | `reviewed`（保持） |
| `pipeline_stage` | `ready-for-review` | `rework-verified` |
| `confidence` | `medium` | `medium`（保持） |
| `last_verified` | `2026-07-25` | `2026-08-01` |
| `last_rework_at` | — | `2026-08-01T21:35:02+08:00` |
| `last_rework_run_id` | — | `20260801-213502-rework-8250c5dd` |

## 本地校验

- `python3 scripts/check-metadata.py --files src/part5-app/ch22-rendering-practice/22.23-navigation-compose-performance.md`: ✅ 1/1 通过，0 错误，0 警告
- `python3 scripts/check-summary-links.py`: ✅ passed（2 条既有 duplicate warning: 4.9 MTE、9.10 ANR callback，非本章节）
- `git diff --check`: ✅ 无 whitespace error

## 剩余风险

1. 页面级性能数值仍需目标项目用 Macrobenchmark/Perfetto/FrameMetrics 实测覆盖 Tab 切换、详情页进入、深链进入和返回路径。本章不写无测试条件的量化收益——这是方法论要求，不是缺陷。
2. `confidence` 维持 `medium`：正文技术正确且有源码锚点，但缺少项目级基准测试数据来支撑更高置信度。
3. Navigation Compose 2.9.8 的源码 JAR 行为是本文 AndroidX 结论的主要依据；升级版本后需重新核对 `NavHost`、entry lifecycle、状态恢复和预测返回实现。
4. Hilt 1.4.0 的 `hiltViewModel()` 包名迁移（1.3.0 起）已在正文 §1 记录；使用旧包名的项目需注意弃用指向。
