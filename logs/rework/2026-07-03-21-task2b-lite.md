# Task2B Lite Rework Log — 2026-07-03 21:45

## 章节 13.21: Perfetto 版本演进与 Android 9-17 新特性验证

**文件**: `src/part3-tools/ch13-perfetto/13.21-perfetto-version-evolution.md`

### 修复内容

#### Issue #2 (需确认→fixed): Perfetto CLI 参数错误
- **问题**: 表格和代码示例使用 `--buffer-size=256M`、`--compression=zstd`、`--duration-seconds=300` 等 CLI flags
- **事实**:
  - `--buffer-size` 存在但单位为 KB（非 MB），且仅在无 config 文件的 simple mode 下可用
  - `--compression` 不存在为 CLI flag
  - `--duration-seconds` 不存在；正确 flag 为 `-t` / `--time`（秒）
- **修复**:
  - 表格: 改为引用 config 内 `buffers { size_kb: N }` 和默认 deflate 压缩
  - 代码块 1: 改为 `perfetto -c trace_config.txt -o trace.perfetto` + 注释说明 config 参数
  - 代码块 2: 改为 `perfetto -c real_time_config.txt -t 300 -o real_time_trace.perfetto`

#### Issue #3 (需确认→fixed): Systrace 迁移示例 buffer 单位不一致
- **问题**: `systrace -b 128`（128 KB）映射到 Perfetto `size_kb: 131072`（128 MB），约 1000x 差异
- **事实**: `systrace -b N` 单位为 KB；Perfetto `size_kb` 单位同为 KB
- **修复**: 改用 `systrace -b 8192`（8 MB）→ `size_kb: 8192`（8 MB），添加单位说明注释

### 未处理
- Issue #1（FrameTimeline 通用内容需重写）: 属于中度重写，留给主 Task2B

### 改动统计
- 正文改动: ~14 行（单章 ≤ 20 行 ✓，本轮 ≤ 40 行 ✓）

### 证据来源
- Perfetto CLI flags: AOSP external/perfetto/src/traced/cmd_trace.cc
- systrace.py -b parameter: Android SDK platform-tools/systrace/py
- Queue entry: task6-review round5, priority 70
