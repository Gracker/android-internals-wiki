# Task2B Lite · 2026-06-29 03:37 (Asia/Shanghai)

## 本轮处理

### 18.25 Jetpack Compose 渲染管线架构
- **来源**: Task 9 Deep Tech Review (P95, 5 issues)
- **Lite 处理范围**: Issue 1 (LayoutNode/MeasurePolicy API 名) + Issue 4 (syncAndDrawFrame fence 语义)
- **转交主 Task2B**: Issue 2 (Choreographer 绑定重写+时序图), Issue 3 (requestRedraw/RenderNodeLayer 流程图), Issue 5 (Snapshot 性能表重写)

#### Issue 1 修复内容
- `LayoutNode.nodes` 类型从 `OwnerSnapshotObserver` 修正为 `NodeChain`
- `LayoutNode.lookaheadRoot` 从 `Boolean` 字段修正为 `isVirtualLookaheadRoot: Boolean` + `lookaheadRoot: LayoutNode?` getter
- 移除 `LayoutNode.measure(constraints): Placeable`（measure 在 MeasurePolicy 上，不在 LayoutNode 上）
- `LayoutNode.draw(canvas)` 签名修正为 `draw(canvas, graphicsLayer)`
- `MeasureScope.measure(...)` 重写为 `MeasurePolicy.fun MeasureScope.measure(...)`，补充 child 返回 Placeable、父返回 MeasureResult 说明

#### Issue 4 修复内容
- "同步阻塞"段落：改为等待 HWUI syncFrameState/prepareTree，默认不等 GPU fence
- 时序图 note：从"MT 等待 GPU sync fence signal"改为"MT 等待 HWUI syncFrameState 完成"
- "同步阶段"段落：补充 DrawFrameTask.postAndWait() 解除阻塞时机，setWaitForPresent 默认 false

#### 改动行数
- 正文改动约 14 行（Issue 1: ~9 行, Issue 4: ~5 行）
- 未超 Lite 单章 20 行限制

### 锁
- `src/part2-performance/ch18-rendering-pipelines/25-compose-rendering-pipeline.md` → locked by lite, 待 commit 后释放
