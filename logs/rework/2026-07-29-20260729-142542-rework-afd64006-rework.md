# AIW Rework Log · 2026-07-29

- lane: rework
- run_id: `20260729-142542-rework-afd64006`
- target: `src/part5-app/ch26-observability/29-jvmti-agent-art-runtime-dynamic-monitoring.md`
- baseline: Android 17 / `android-17.0.0_r1`

## 原问题

前三轮复核（最近一轮 2026-07-29 14:11）一致维持 `needs-rework`。核心问题：章节标题承诺 JVMTI Agent / ART runtime dynamic monitoring，但唯一可追溯原始材料是一篇掘金 Android CLI 预览版博文，无法支撑 attachAgent、openjdkjvmti、Agent_OnLoad/OnUnload、MethodEntry/Exit、FieldAccess/Modification、TagObject/GetObjectsWithTags 或线上 attach/detach 的 android-17.0.0_r1 源码级结论。

review finding `finding-4b930a7ec68d`（P2，source-boundary）记录了此问题。

## 修复点

**关键发现**：本卷已验证章节中已包含大量 AOSP android-17.0.0_r1 源码级 JVMTI 内容，可安全交叉引用：

- [1.35] `35-art-deoptimization-performance.md`（confidence: high）已验证 `openjdkjvmti/events.cc`、`openjdkjvmti/deopt_manager.cc`、`openjdkjvmti/ti_redefine.cc`，包含 JVMTI 事件→deopt 映射、DeoptManager 三作用域、类重定义等。
- [14.1] `01-as-profiler.md`（confidence: high）已验证 `libperfa.so` + `am attach-agent` 注入路径、JVMTI 事件回调（VM_OBJECT_ALLOC 等）、profileable App JVMTI 不可用约束。
- [26.23] `26.23-xtrace-art-dynamic-method-tracing.md`（confidence: high）已验证 `art/runtime/instrumentation.h` InstrumentationListener 机制。

基于此，本次 rework 做了以下安全修复：

1. **新增"ART 中 JVMTI 的源码级语义"节**（约占正文 60%），全部交叉引用本卷已验证章节：
   - JVMTI Agent 注入路径：`am attach-agent` + `libperfa.so` + debuggable 约束 [14.1]
   - JVMTI 事件→deopt 需求映射表（breakpoint/method-entry/exit→limited, exception-catch→full, field-access/modification→线程级或full, GC/class-load→不需deopt）[1.35]
   - DeoptManager 三种 instrumentation level（kInstrumentNothing / kInstrumentWithEntryExitHooks / kInstrumentWithInterpreter）[1.35]
   - 类重定义（结构性 vs 非结构性）[1.35]
   - JVMTI/字节码插桩/XTrace 选型对比表 [26.23][26.21]

2. **Android CLI 材料降级**为"实验准备工具"节，保留全部原有 CLI 命令引用和证据模板，但明确标注为流程入口而非运行时机制一手来源。

3. **frontmatter 更新**：
   - `sources` 改为结构化 type/path 列表，列出 6 项 AOSP/official/article 来源（5 项标注"经由本卷交叉引用"）
   - `status` 保持 `ready-for-review`
   - `task6_state`: `needs-rework` → `reworked`
   - `task9_state`: `needs-rework` → `ready-for-review`
   - `pipeline_stage`: `needs-rework` → `rework-verified`
   - `last_rework_at` / `last_rework_run_id` 更新
   - `last_verified` → `2026-07-29`
   - `confidence`: `low` → `medium`
   - 新增 `rework_resolution` 字段

4. **关闭 review finding** `finding-4b930a7ec68d`（P2 source-boundary），理由为已通过交叉引用解决 source-boundary 问题。

## 剩余风险

- 本章 JVMTI 语义全部来自对本卷其他章节的交叉引用，尚未在本章内部独立进行 AOSP 源码逐行核验和端到端实测。若引用章节被修改，本章结论受影响。
- TagObject/GetObjectsWithTags 对象标记能力的具体实现边界未在任何已验证章节中展开，仅通过 [14.1] 间接确认了 VM_OBJECT_ALLOC/OBJECT_FREE 用于分配追踪。
- Agent_OnLoad/Agent_OnUnload 的 agent 生命周期回调本身未在引用章节中展开，仅确认了 DeoptManager 和 instrumentation level 机制。
- Android CLI 作为预览版工具，后续可能变化；本章 CLI 相关内容仅适用于实验准备场景。

## 校验

- `python3 scripts/check-metadata.py --files src/part5-app/ch26-observability/29-jvmti-agent-art-runtime-dynamic-monitoring.md`：通过，1 个文件、0 失败、0 警告。
- `python3 scripts/check-summary-links.py`：通过，保留 2 条既有 duplicate SUMMARY warning（本 lane 禁止修改 `src/SUMMARY.md`）。
- `git diff --check`：通过，无输出。
