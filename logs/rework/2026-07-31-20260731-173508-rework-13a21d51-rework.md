# Rework Log — 25.16 ADPF Power Efficiency Mode 与 PowerMonitor 能耗验证

- **Run ID**: `20260731-173508-rework-13a21d51`
- **Lane**: rework (aiw-polish-rework profile)
- **Date**: 2026-07-31
- **Target**: `src/part5-app/ch25-power-size/16-adpf-power-efficiency-powermonitor.md`
- **Chapter**: 25.16

## 原问题

### quality_flags
- `pending-verification-marker`
- `thin-source-marking`

### P1: 延伸阅读摘要中 PowerStatsService 缓存阈值矛盾

延伸阅读「Android 17 Power Stats HAL OEM 厂商功耗统计实现差异」的摘要引用了 2026-07-07 DeepResearch 报告（`7e12d66afab1bfe3`）中的数值：

> `MAX_POWER_MONITOR_AGE_MILLIS = 20_000`（20 秒老化）
> `MAX_FINE_POWER_MONITOR_AGE_MILLIS = 250`（250ms 精细）

但正文「PowerStatsService 完整数据通路」一节（来自 2026-06-13 报告 `24dac1c44871e038`，含 L52 行号引用）明确为：

> `MAX_POWER_MONITOR_AGE_MILLIS = 30_000`（30 秒缓存）

两处数值冲突，`MAX_FINE_POWER_MONITOR_AGE_MILLIS = 250` 在权威报告中不存在。

## 修复点

1. **延伸阅读摘要修正**：将 "20s/250ms 双老化策略" 改为 "30s 缓存阈值（`MAX_POWER_MONITOR_AGE_MILLIS = 30_000`，与正文 §PowerStatsService 完整数据通路一致）"。
2. **添加冲突说明注脚**：新增一条注脚，说明该报告提及 `20_000` 与 `250`，与一手源码 L52（`30_000`）冲突，以正文 30s 阈值为准。
3. **移除 "9 种预定义 EnergyConsumerType"**：延伸阅读摘要中的次要细节，因不属于该条目的核心价值（接口定义与 OEM 扩展点），精简后保留聚焦。

## 保留项（非本次修复范围）

- **`[待验证]` 标记（PowerMonitorReadings 与 Perfetto power rails 一致性）**：idle-audit 明确保留（"待验证标记保留"）；该 gap 为真实未验证项，有 materials 支撑其存在，不应删除或补伪造数据。保留。
- **`android-16.0.0_r1` 版本锚点**：Task9 已修复，idle-audit 确认合规（"版本锚点 android-16/17 合规"）；§系统层硬件协同设计架构 统一使用 `android-16.0.0_r1`，并在版本限定说明中标注 Android 17 差异。保留。

## frontmatter 变更

| 字段 | 旧值 | 新值 |
| --- | --- | --- |
| `last_verified` | 2026-06-13 | 2026-07-31 |
| `last_rework_at` | — | 2026-07-31T17:35:08+08:00 |
| `last_rework_run_id` | — | 20260731-173508-rework-13a21d51 |
| `last_rework_notes` | — | 缓存阈值矛盾修正说明 |

## 状态流转

- `status`: 保留 `finalized`（本次为安全事实修正，章节技术结论不受影响）
- `task6_state`: 保留 `reviewed`
- `task9_state`: 保留 `reviewed`
- `pipeline_stage`: 保留 `ready-to-publish`
- `confidence`: 保留 `medium-high`

## 本地校验

- `check-metadata.py --files <target>`: ✅ 1/1 通过
- `check-summary-links.py`: ✅ 通过（2 个 pre-existing duplicate 警告在 SUMMARY.md，非本次变更）
- `git diff --check`: ✅ 无问题

## 剩余风险

1. `[待验证]` 标记仍保留，需后续实机对比数据（Pixel + OEM）后清除。
2. 延伸阅读摘要中的 OEM 差异数据（如 BatteryTrigger/TimerTrigger 触发频率估算、HAL 延迟数值）来自 2026-07-07 报告，部分标注为推测（"估计是…"），后续如有新源码可进一步收窄。
