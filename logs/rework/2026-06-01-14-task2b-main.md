# Task2B Main Rework · 2026-06-01 14:50

- lane: main
- source: frontmatter backlog fallback
- section: 21.2 启动框架设计与任务编排
- file: `src/part5-app/ch21-startup/02-startup-framework.md`
- source_log: `logs/deep-review/2026-05-22-03-deep-review.md`

## Issues Fixed

1. task9 P0: Alpha `Project.Builder` / `AlphaManager.getInstance()` 示例与源码不一致。
   - 修复: 示例改为 `new Project.Builder().setProjectName(...).add(...).after(...).create()`，入口改为 `AlphaManager.getInstance(context)`。
2. task9 P0: Alpha 默认 `ExecutorService` 被写成单线程池。
   - 修复: 改为 `AlphaConfig` 默认 CPU 核数固定线程池 + 无界 `LinkedBlockingQueue`。
3. task9 P1: Alpha 执行模型被写成全局拓扑排序和环检测。
   - 修复: 改为 `AlphaManager.start()` 选择 Project、`Project.start()` 启动 start anchor、`Task.notifyFinished()` / `onPredecessorFinished()` 触发 successor。
4. task9 P1: 线程优先级建议过强。
   - 修复: 关键启动线程默认保持 `THREAD_PRIORITY_DEFAULT`，只在短任务、Perfetto 证明 TTID 收益时小范围使用 `THREAD_PRIORITY_FOREGROUND`；保留 `THREAD_PRIORITY_DISPLAY` 的应用侧限制。

## State

- `pipeline_stage`: `task6_pending`
- `task6_state`: `revisiting`
- `task9_state`: `pending`
- `task2b_state`: `fixed`
- queue: no pending queue item for section 21.2; consumed through frontmatter fallback.

## Verification

- Android version cap: no Android 18 / API 38+ content introduced.
- Source anchors: alibaba/alpha `Task.java`, `Project.java`, `AlphaManager.java`, `AlphaConfig.java`; no AOSP main-only Android 18 content used.
- Style scan: no banned phrase hit in the modified section; high-frequency filler terms `真正|实际上|其实|根本|彻底|确实` remain zero.
