# AIW Polish Rework · 2026-08-04

- run_id: `20260804-133512-rework-39378b94`
- mode: `rework`
- target: `src/part3-tools/ch13-perfetto/16-agent-perfetto-analysis-protocol.md`
- quality_flags: `pending-verification-marker`, `thin-source-marking`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`; kernel anchor `android17-6.18-2026-06_r6`

## 原问题

1. frontmatter 的 `last_verified_against` 仍包含 `sched.with_context`，与正文中“`sched.with_context` 并不存在”的修正口径不一致，容易被识别为 pending verification / stale verification marker。
2. 正文已列出多条官方资料，但关键段落缺少内联来源标记，触发 `thin-source-marking` 启发式风险。
3. 章节处于 `finalized` / `ready-to-publish`，不符合 rework 后需要 Task6/Task9 重新复审的状态流转。

## 修复点

1. 只修改既有章节 `src/part3-tools/ch13-perfetto/16-agent-perfetto-analysis-protocol.md`；未新增 `src/**/*.md`；未编辑 `src/SUMMARY.md`。
2. 将 `status` 与 `pipeline_stage` 回退为 `ready-for-review`，将 `task6_state`、`task9_state` 改为 `needs-review`。
3. 更新 `last_verified`、`last_verified_against`、`last_rework_at`、`last_rework_run_id`、`last_rework_log`、`rework_result`、`rework_notes`；`confidence` 保持 `medium`。
4. 清除 frontmatter 中失效的 `sched.with_context` 来源口径，改为正文实际使用的 `slices.with_context`、`slices.time_in_state`、`slices.cpu_time`、`linux.cpu.frequency`、`android.binder` 等 Perfetto stdlib 边界。
5. 在正文 3 处关键段落增加 `[来源:]` 标记，覆盖 android/skills 固定提交、Perfetto Using AI、v57.1/v57.2 release notes、Trace Processor / stdlib / SQL tables。
6. frontmatter `sources` 补入 Perfetto stdlib docs、Using AI、v57.1/v57.2 release notes、diff tests，减少“正文资料列表多、frontmatter 来源薄”的不一致。

## 剩余风险

- 本轮未引入 Android 18/API38+ 内容；Android 基线仍固定在 Android 17 / `android-17.0.0_r1`。
- 本轮是元数据与来源标记 rework，不重新证明 Perfetto v57.2 实机/trace 查询结果；章节已回到 `ready-for-review`，等待 Task6/Task9 对发布态重新确认。
- `finalized_by/finalized_date` 等历史字段保留为历史记录；当前发布状态以 `status: ready-for-review` 与 `pipeline_stage: ready-for-review` 为准。

## 校验

- `python3 scripts/check-metadata.py --files src/part3-tools/ch13-perfetto/16-agent-perfetto-analysis-protocol.md`
- `python3 scripts/check-summary-links.py`
- `git diff --check`

校验结果见 formal report 与本次终端输出。
