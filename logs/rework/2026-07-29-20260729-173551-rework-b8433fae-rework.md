# Rework Log · 2026-07-29

**Run ID:** `20260729-173551-rework-b8433fae`
**Target:** `src/part1-fundamentals/ch04-memory/4.04-AppFlow与Android-17-LMKD兼容性方案.md`
**Mode:** rework
**Status in:** polish_candidate_ready / task6_state=needs-rework

## 原问题

Chapter 4.04 正文由 `task2a-knowledge-gap` 基于 "DeepResearch + daily-info hot topic" 生成，描述了一个虚构的 AppFlow 冷启动内存调度系统：

- 虚构类：`AppFlowManager`、`LmkdService`、`MemoryTracker.preAllocate()`
- 虚构架构：AppFlow/LMKD 共享内存池、优先级继承、回滚协议
- 虚构 Kotlin 代码块引用不存在的 `frameworks/base/services/core/java/com/android/server/am/AppFlowManager.java`
- 虚构性能数据表（1200ms→850ms 冷启动等），无设备、build、样本量、测试命令

deep-review lane（run `20260729-142027-deep-review-b8433fae`）已确认这些内容在 `android-17.0.0_r1` 中不存在，标记 `needs-rework` 并添加警告横幅和 "源码边界" 章节。

## 修复点

1. **删除全部虚构 outline**：移除原 "要点"（锚点 1–5，含 Kotlin 代码和性能表）和 "扩展"（扩展点 1–3）。这些内容经 deep-review 确认无 AOSP 源码证据。

2. **重写为 source-boundary 章节**：
   - 锚点 1：明确声明 AppFlow 在 AOSP android-17.0.0_r1 中不存在，列举具体不存在的类和方法。
   - 锚点 2：记录 framework → lmkd 的真实单向通知路径（OomAdjuster → ProcessList → LMK_PROCPRIO/LMK_PROCS_PRIO）。
   - 锚点 3：澄清 MemoryLimiter 是独立机制，非 AppFlow 配额服务。
   - 锚点 4：提供冷启动内存性能的正确取证方法表。

3. **保留并强化 "源码边界" 章节**：deep-review 已写入的源码边界内容完整保留，确保已核验的 AOSP 关系不被丢失。

4. **frontmatter 更新**：
   - `status`: `needs-review` → `ready-for-review`
   - `task6_state`: `needs-rework` → `rework-done`
   - `pipeline_stage`: `deep-review` → `rework`
   - `confidence`: `low` → `medium`
   - 新增 `last_rework_at: 2026-07-29`、`last_rework_run_id`
   - tags 增加 `source-boundary`、`debunked`

## 状态流转

| 字段 | 修复前 | 修复后 |
|------|--------|--------|
| status | needs-review | ready-for-review |
| task6_state | needs-rework | rework-done |
| pipeline_stage | deep-review | rework |
| confidence | low | medium |

## 校验结果

- `check-metadata.py --files <target>`: ✅ 通过（0 失败，1 可选字段警告）
- `check-summary-links.py`: ✅ 通过（165 links，2 预存重复警告，与本改动无关）
- `git diff --check`: ✅ 无 whitespace 错误

## 剩余风险

1. **标题仍含 "AppFlow"**：章节标题为 "AppFlow与Android 17 LMKD兼容性方案"，可能误导新读者以为该方案存在。本 lane 约束不修改 SUMMARY.md，建议后续 SUMMARY 调整时考虑重命名（如 "AppFlow 伪方案辟谣与冷启动 lmkd 源码边界"）。
2. **confidence=medium 而非 high**：重写后的 source-boundary 内容基于 deep-review 已核验的 AOSP 结论，但原始 AppFlow 前提被永久辟谣，保留 medium 以反映该章节的 "辟谣 + 边界" 定位而非正向技术深度。
3. **task9_state 保持 pending**：audit lane 尚未运行。
