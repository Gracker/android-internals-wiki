# AIW Rework Log · 2026-07-26

- lane: rework
- run_id: 20260726-173554-rework-19d43518
- target: `src/part5-app/ch27-application-cpu-optimization.md`
- baseline: AOSP `android-17.0.0_r1` / android17-6.18
- source materials:
  - `DeepResearch/2026-07-02-android17-app-cpu-optimization-thread-priority-source-verification.md`
  - `DeepResearch/2026-07-02-android17-threadpoolexecutor-times-syscall-source-deepdive.md`

## 原问题

Task6/Task9 标记为 `needs-rework`：章节仍混有 public SDK 示例、framework/system hidden API 口径、教学伪 JNI 片段与未分层的源码说明，且 frontmatter 状态停留在 `task6_needs_rework`。

## 修复点

1. frontmatter 状态推进到 `ready-for-review`：
   - `pipeline_stage: task6_ready_for_review`
   - `task6_state: ready-for-review`
   - `task9_state: ready-for-review`
   - `confidence: medium-high`
   - 增加 `last_rework_at` / `last_rework_run_id`
2. 替换开头 rework 说明，明确 Android 17 baseline 与 public SDK / system API 边界。
3. 修正 outline：移除“偏向锁”“NUMA”这类容易误导为 Android/ART 可依赖结论的表述，改为 synchronized/Lock 边界与移动 SoC 异构核口径。
4. 删除 `Process.times(long[])`/`__times(jlongArray)` 伪 JNI 示例，改为 NDK `<sys/times.h>` public 调用 + framework 源码口径说明。
5. 修正 `readProcFile` 栈/堆缓冲描述，不再声称 `/proc/pid/status` 1024 字节栈缓冲必然命中。
6. 将 `/proc/self/stat` 进程 CPU 使用率示例改为两次采样 `utime + stime` 与同窗口 `/proc/stat` 总量归一化，避免一次性累计值与当前 idle 直接相除。
7. 修正 Worker 优先级附录：普通应用只推荐 public `setThreadPriority(DEFAULT/BACKGROUND)`；`THREAD_PRIORITY_DISPLAY` 和 cpuset/group API 不写成通用应用优化手段。

## 状态流转

- before: `task6_state=needs-rework`, `task9_state=needs-rework`, `pipeline_stage=task6_needs_rework`
- after: `task6_state=ready-for-review`, `task9_state=ready-for-review`, `pipeline_stage=task6_ready_for_review`
- review finding: `finding-a08466400908` 已通过 `aiw-review-findings.py close --status fixed` 标记 fixed

## 剩余风险

- 章节仍保留大量教学型应用伪代码，适合进入 Task6 final review，但不直接标记 finalized。
- `Process.readProcFile` 示例仍为 framework/system app 源码口径，正文已标注普通应用应使用 BufferedReader 或自有 JNI。

## 校验

已执行并通过：

```bash
python3 scripts/check-metadata.py --files src/part5-app/ch27-application-cpu-optimization.md
python3 scripts/check-summary-links.py
git diff --check
```

`check-summary-links.py` 保留 2 条既有 duplicate SUMMARY warning，命令 exit 0；本 lane 未修改 `src/SUMMARY.md`。
