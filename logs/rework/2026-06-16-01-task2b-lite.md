# Task2B Lite · 2026-06-16 01:35

## 本章处理

### 20.7 异常处理架构设计 (`src/part5-app/ch20-stability/07-exception-architecture.md`)

**修复类型：** 版本限定 / API 边界

**问题来源：** Task9 Deep Tech Review (P85), issue #1 of 3

**问题：** 正文 L124 残留 `[需确认: ApplicationExitInfo.getTraceInputStream() 对 native tombstone 的 API 边界]` 占位，缺少具体版本信息。

**修复：** 替换为版本化表述：
- Android 11/API 30: getTraceInputStream 用于 REASON_ANR 的 ANR trace
- Android 12/API 31+: REASON_CRASH_NATIVE 可通过该方法读取 tombstone protobuf 流，底层是全局循环缓冲区，可能被覆盖返回 null
- 调用方必须处理 null/IOException
- Crashpad/Breakpad 仍是 native crash 主链路

**改动行数：** 1 行删除 → 1 段（~4 行有效内容）写入，净增 3 行

**Lite 范围内：** 是（API 版本限定小修）

**未修复（超出 Lite 范围）：**
- Issue #2: SafeMode 状态机缺 launch marker 三态 → 需中重度修复，留主 Task2B
- Issue #3: crash 文件持久化缺 fsync/rename 协议 → 需中重度修复，留主 Task2B

**锁状态：** locked (lite) → 已在提交后释放

**章节状态流转：** task2b_pending → task6_pending (ready-for-review)
