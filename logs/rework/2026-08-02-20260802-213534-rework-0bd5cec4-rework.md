# Rework · 2026-08-02 21:35 (run 20260802-213534-rework-0bd5cec4)

- lane：aiw-polish-rework
- mode：rework
- target：`src/part2-performance/ch07-smoothness/10-image-bitmap-performance.md`
- baseline：Android 17 / `android-17.0.0_r1`
- pre-run flags：`pending-verification-marker`, `thin-source-marking`

## 原问题

1. 章节 outline 中保留了显式核验占位写法，容易被启发式扫描误判为发布稿仍存在未收敛结论。
2. frontmatter `sources` 已在上一轮补齐核心 BitmapFactory / Bitmap / ImageDecoder / SkiaGpuPipeline 来源，但尚未覆盖正文末尾列出的 Gainmap、RecordingCanvas、ImageDecoder API、Glide Hardware Bitmap 与 Coil allowHardware 资料；正文关键结论缺少内联来源标记。

## 本轮修复

1. 将章节状态从 `finalized` 回流到 `ready-for-review`，并更新 `task6_state: rework-applied`、`task9_state: rework-applied`、`pipeline_stage: rework-applied-awaiting-review`，等待后续 review/finalize lane 复核。
2. 将 `chapter` / `section` 加引号，避免 YAML 将 `7.10` 解析为浮点 `7.1`。
3. 将 outline 的核验占位改为“后续核验事项”，不改变“不把未证据化结论写成确定结论”的编辑要求。
4. 扩充 frontmatter `sources` 至 13 条：新增 `Gainmap.java`、`RecordingCanvas.cpp`、Android `ImageDecoder` API、Glide Hardware Bitmap、Coil allowHardware。
5. 在正文关键结论补 5 处 `[来源:]` 内联标记，覆盖版本锚点、图片库默认策略边界、`inSampleSize` 公共契约、Gainmap 内存模型、software texture preparation、`inBitmap` 对象语义。
6. 更新 `last_verified: 2026-08-02`、`last_rework_at`、`last_rework_run_id`、`last_rework_reason`、`rework_summary`。

## 剩余风险

- 本轮不新增 Android 18 / API 38+ 结论，不改变既有 Android 17 技术判断。
- 未引入新章节、未编辑 `src/SUMMARY.md`。
- 后续需要 Task6/Task9 根据 rework-applied 状态执行复核；当前正文已具备进入 review 的来源标记与元数据条件。

## 验证

- ✅ `python3 scripts/check-metadata.py --files src/part2-performance/ch07-smoothness/10-image-bitmap-performance.md`：1 个文件通过，0 失败，0 警告。
- ✅ `python3 scripts/check-summary-links.py`：通过；保留既有 2 个 duplicate SUMMARY warning（非本轮引入，且本 lane 禁止编辑 `src/SUMMARY.md`）。
- ✅ `git diff --check`：通过（exit 0）。
