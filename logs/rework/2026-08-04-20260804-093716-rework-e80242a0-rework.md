# AIW Polish Rework · 2026-08-04

- run_id: `20260804-093716-rework-e80242a0`
- mode: `rework`
- target: `src/part5-app/ch26-observability/05-online-troubleshooting.md`
- quality_flags: `pending-verification-marker`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`

## 原问题

- frontmatter 仍保留 `deepseek_cn_review_state: needs-structure-rework`，但正文已经完成 Task2b 修复与 verifier 记录，状态未回流。
- 自动发现段落存在待验证标记风险：`[自动发现]` 未把复核依据直接写在段落标记处，容易被后续门禁识别为 pending verification。
- `status/pipeline_stage/task6_state/task9_state` 仍停留在发布态/已 review 态，不能表达 rework 后需要重新进入 review 的状态。

## 修复点

1. 只修改既有章节 `src/part5-app/ch26-observability/05-online-troubleshooting.md`，未新增章节，未编辑 `src/SUMMARY.md`。
2. 将章节状态从 `finalized` 回退到 `ready-for-review`，将 `pipeline_stage` 改为 `ready-for-review`，将 `task6_state/task9_state` 改为 `needs-review`。
3. 更新 `last_verified`、`last_verified_against`、`confidence`、`polish_count`，并新增 `last_rework_at`、`last_rework_run_id`、`last_rework_log`、`rework_result`、`rework_notes`。
4. 将 `deepseek_cn_review_state` 改为 `structure-reworked`，清除 stale needs-rework 标记。
5. 对两个 `[自动发现]` 段落补充复核依据：Perfetto / AndroidX Tracing、ApplicationExitInfo / ProfilingManager / StatsD 权限资料。

## 剩余风险

- 本轮未引入 Android 18/API38+ 内容；Android 17 相关 StatsD、ProfilingManager、ProfilingTrigger 结论保持既有边界。
- 因正文状态已回到 `ready-for-review`，后续应由 task6/task9 重新确认发布态。

## 校验

校验结果见 formal report 与本次终端输出。
