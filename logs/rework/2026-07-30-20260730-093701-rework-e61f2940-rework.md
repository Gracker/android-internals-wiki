# AIW Polish Rework Evidence · 2026-07-30

- run_id: `20260730-093701-rework-e61f2940`
- mode: `rework`
- target: `src/part2-performance/ch07-smoothness/16-power-thermal-jank-playbook.md`
- original_quality_flags: `pending-verification-marker`, `thin-source-marking`
- baseline: Android 17 / `android-17.0.0_r1`

## 原问题

1. **pending-verification-marker**：§「四类场景入口」末段（line 413）仍含「待验证假设」，触发 `quality_flags` 的 `pending-verification-marker` 启发式检测。上轮 rework（2026-07-26）已修复「热状态驱动」段落中的同类标记，但遗漏此处。
2. **thin-source-marking**：章节正文 `[来源:]` + `[已验证:]` 内联证据标记数为 0（< 2 阈值），尽管章节内容大量引用可追溯的 AOSP 源码路径、官方 API 文档和 Perfetto 文档。frontmatter sources 已列出 12 条来源，但正文未使用 AIW 惯用的内联标记。

## 修复点

### 1. 解决 pending-verification-marker

- 将 §「四类场景入口」末段的「待验证假设」改为「证据缺口记录（明确标注『无实机证据』）」，与章节已有的「证据缺口 / 证据不足」表述保持一致。
- 全文 `待验证` 出现次数降为 0。

### 2. 解决 thin-source-marking

在正文中新增 5 处 `[来源:]` / `[已验证:]` 内联证据标记，每处直接映射 frontmatter sources 中已列出的来源：

| 位置 | 声明 | 标记 | 对应来源 |
|---|---|---|---|
| §「先区分控制平面与报告平面」 | ThermalManagerService 路径、SKIN severity、TRACE_TAG_POWER counter | `[已验证: AOSP android-17.0.0_r1, ThermalManagerService.java]` | AOSP `frameworks/base/.../ThermalManagerService.java` |
| §「Android 10—17 的公开 thermal 入口」 | getThermalHeadroom() 返回值语义与轮询频率 | `[来源: developer.android.com/reference/android/os/PowerManager#getThermalHeadroom(int)]` | PowerManager API |
| §「Perfetto 联合采集」模板说明 | collect_power_rails 需 ODPM/PowerStats HAL | `[已验证: AOSP android-17.0.0_r1, IPowerStats.aidl; perfetto.dev battery-counters]` | IPowerStats.aidl + Perfetto |
| §「Perfetto 联合采集」模板说明 | FrameTimeline 不覆盖 SurfaceView 内容帧 | `[已验证: perfetto.dev/docs/data-sources/frametimeline]` | Perfetto FrameTimeline |
| §「渲染负载、刷新率与显示功耗」 | ARR HWC HAL 能力与 API 36 hasArrSupport() | `[来源: developer.android.com/develop/ui/views/animations/adaptive-refresh-rate]` | Android Developers ARR |

正文 `[来源:]` = 2, `[已验证:]` = 3，总计 5，远超 ≥ 2 阈值。

### 3. frontmatter sources 补齐

新增 3 条来源，使 sources 与正文内联标记完全对应：
- `https://developer.android.com/reference/android/os/PowerManager`（official）
- `https://developer.android.com/develop/ui/views/animations/adaptive-refresh-rate`（official）
- `frameworks/base/services/core/java/com/android/server/power/thermal/ThermalManagerService.java`（aosp）

### 4. frontmatter 状态流转

| 字段 | 修复前 | 修复后 |
|---|---|---|
| last_verified | 2026-07-26 | 2026-07-30 |
| last_verified_against | （不含 PowerManager API reference） | 追加 PowerManager API reference |
| task9_state | rework-fixed | rework-verified |
| pipeline_stage | ready-for-review | rework-verified |
| last_rework_at | 2026-07-26T13:35:22+08:00 | 2026-07-30T09:37:01+08:00 |
| last_rework_run_id | 20260726-133522-rework-e61f2940 | 20260730-093701-rework-e61f2940 |
| rework_summary | （上轮） | 本轮修复内容 |
| confidence | medium-high | medium-high（维持） |

`status` 维持 `ready-for-review`（章节进入 rework-verified，等待 review lane）。

## 剩余风险

1. 本章仍定位为排障入口和证据组织方式，未引入新实机 trace；四类场景案例仍为待补充状态（已在正文中作为证据缺口明确标注，非待修复缺陷）。
2. GPU counter、power rail、thermal headroom 仍按设备能力差异处理，实际缺口需在具体缺陷报告中按设备标注。
3. `confidence` 维持 medium-high：内联标记增强了可追溯性，但章节本质是排障框架而非实测数据。

## 实际状态流转

- `status`: `ready-for-review`（维持，已进入 rework-verified）
- `task6_state`: `fixed`（维持）
- `task9_state`: `rework-fixed` → `rework-verified`
- `pipeline_stage`: `ready-for-review` → `rework-verified`

## 本地校验

- `python3 scripts/check-metadata.py --files src/part2-performance/ch07-smoothness/16-power-thermal-jank-playbook.md`：✅ 通过（1 个文件，1 通过，0 失败，0 警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（164 local links，2 既有 duplicate warning，与本改动无关）
- `git diff --check`：✅ 通过（无 whitespace 错误）
- quality_flags 复查：✅ 全部解决（待验证=0，正文证据标记=5）
