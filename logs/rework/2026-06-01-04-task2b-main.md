# Task2B Main 回炉修复 - 2026-06-01 04:50

## 处理章节

- `src/part1-fundamentals/ch02-rendering/01-rendering-overview.md`
- `src/part1-fundamentals/ch02-rendering/05-main-render-thread.md`

## 问题来源

- task9-deep-tech-review P95: 2.1 版本演进段落混合 Android 3.0 DisplayList 与 Android 5.0 RenderNode/RenderThread。
- task9-deep-tech-review P95: 2.1 BufferQueue 伪代码不对应 AOSP 方法签名。
- task9-deep-tech-review P95: 2.1 三缓冲缺少显示延迟副作用说明。
- task9-deep-tech-review P95: 2.5 ADPF hint session 与 Android 16 headroom API 版本口径混在一起。
- task9-deep-tech-review P95: 2.5 syncFrameState 阻塞点解释需要回到本帧同步阶段。
- 自发现: 2.5 源码调研补注仍保留“只等待任务提交”和固定耗时范围,与正文修复后的 syncFrameState 语义冲突。

## 修复结果

- 2.1: 将三缓冲副作用补到机制段,说明第三个 buffer 会增加 input 到 actual present 的队列等待。
- 2.1: 删除无签名依据的 BufferQueue 消费伪代码,改为 `BufferQueueConsumer::acquireBuffer(BufferItem*, nsecs_t, uint64_t)` 与 `BufferItemConsumer::acquireBuffer(BufferItem*, nsecs_t, bool)` 签名引用。
- 2.1: 在 Skia OpenGL 后端小节重新拆分 Android 3.0-4.4 DisplayList / DisplayListRenderer / OpenGLRenderer 与 Android 5.0+ RenderNode / RenderThread。
- 2.5: 保留 Android 14+ `CanvasContext.cpp` 中 hint session 上报与 Android 16 headroom API 的区分。
- 2.5: 清理无 AOSP 支撑的 Adreno 830+ 分片并行提交段落,避免把厂商特性写成 Android 16 通用结论。
- 2.5: 修正源码调研补注中的 `postAndWait()` 语义和固定耗时范围,改为按 Trace 观察 `prepareTree()`、layer update、`makeCurrent()`、纹理准备、`CanvasContext::draw()` 和 buffer / fence 反压。

## 状态更新

- `metadata/queue.json`: 2.1、2.5 两条 P95 task9-deep-tech-review 条目置为 `completed`。
- `metadata/progress.json`: 2.1、2.5 回到 `pipeline_stage: task6_pending`, `task6_state: revisiting`。
- 自动晋升 finalized: 不满足。两章 `task9_result` 仍为 `needs-rework`,等待 Task6/Task9 复审。

## 验证

- Android 版本边界: 未引入 Android 18 / API 38+ 内容。
- frontmatter: 2.1、2.5 无重复键。
- 禁用词扫描: 修改段落未引入 SKILL.md 硬禁词；全文件仅剩既有非本轮修改的“关键不在于”语句。
- queue 剩余: P95 0 个, P90 1 个, P85 0 个。
