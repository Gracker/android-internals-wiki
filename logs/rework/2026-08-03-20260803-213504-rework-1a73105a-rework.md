# AIW Polish Rework Evidence · 2026-08-03

- run_id: `20260803-213504-rework-1a73105a`
- mode: `rework`
- target: `src/part1-fundamentals/ch04-memory/11-cached-app-freezer-gc-boundary.md`
- quality_flags: `pending-verification-marker`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`; kernel note remains `android17-6.18-2026-06_r6`

## 原问题

1. Frontmatter 仍写 `last_verified_against: AOSP android-16.0.0_r1 ... Android 17 tag 未公开`，与正文已采用 Android 17 基线不一致。
2. outline 中 16KB Page Size 条目仍保留“待验证点”表述，触发 pending-verification 质量标记。
3. sources 中仍指向 Android 16/旧包路径下的 `ProcessList.java`、`OomAdjuster.java`，没有反映 Android 17 `psc/Constants.java` / `psc/OomAdjuster.java` 路径迁移。
4. 章节处于 `finalized` / `ready-to-publish`，但 rework lane 命中待验证标记后应退回复审流。

## 修复点

- 将 frontmatter `last_verified` 更新到 `2026-08-03`，`last_verified_against` 改为 Android 17 `android-17.0.0_r1` 的已核对文件集合。
- 将 `confidence` 从 `medium` 提升为 `medium-high`，同时把 `status` / `pipeline_stage` 推回 `ready-for-review`，避免未复审直接发布。
- 将 `task6_state`、`task9_state` 改为 `ready-for-review`，`task6_result` 记录为 `rework-applied`，`task9_result` 记录为 `pending-review`。
- 新增 `last_rework_at`、`last_rework_run_id`、`rework_by`、`rework_result`。
- 修正 sources 中 Android 17 AOSP 路径：`psc/Constants.java` 与 `psc/OomAdjuster.java`。
- 删除 outline 的“待验证点”措辞，改为本轮已按 Android 17 基线核对、未发现 page-size 分支改写 GC/freezer 策略。
- 将扩展标题从 Android 16 演进更新为 Android 17 演进，并补充 CPU_TIME capability 线索。

## 剩余风险

- 本轮为 bounded rework，未引入 Android 18/API 38+ 结论，未新增章节，未修改 `src/SUMMARY.md`。
- 章节已回到 review 阶段，后续仍需 Task6/Task9 做正式复审确认。

## 本地校验

- `python3 scripts/check-metadata.py --files src/part1-fundamentals/ch04-memory/11-cached-app-freezer-gc-boundary.md`：通过（1 个指定章节，0 失败，0 警告）。
- `python3 scripts/check-summary-links.py`：通过；输出 2 条既有 duplicate SUMMARY link warning，未修改 `src/SUMMARY.md`。
- `git diff --check`：通过。
