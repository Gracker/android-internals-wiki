🔧 **Task 2B · 回炉修复** — 根据 Task 6 / Task 9 / External Review 的问题单，对章节做定向修复。

📋 本轮处理：src/part5-app/ch20-stability/03-native-crash-governance.md
📝 修改摘要：frontmatter fallback/task9 P95：修复 ApplicationExitInfo native tombstone API 30/31 边界；task9 P95：修正 Breakpad 符号查找与地址映射源码锚点；task9 P95：移除不存在的 JNIEnv->FindSymbol 口径；task9 P95/P1：修正 CFI 与 Java frame、Crashpad signal/ptrace 职责、mooner 安全边界。自发现：补充 frontmatter 源码锚点和 Task2B 回流状态。
📁 修改的文件：src/part5-app/ch20-stability/03-native-crash-governance.md；metadata/progress.json；logs/rework/2026-06-01-12-task2b-main.md；/Users/gracker/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian/OpenClaw定时任务/知识加工/2026-06-01-12-知识加工(回炉).md

## 处理来源

queue.json 中无 task6-review / task9-deep-tech-review / external-ai-review pending 条目，因此执行 frontmatter backlog fallback。`src/part3-tools/ch19-apm/27-apm-client-architecture.md` 命中优先级更高但存在 3 小时内有效锁，本轮跳过；随后加锁处理 `20.3 Native Crash 分析与治理`。

问题来源：`logs/deep-review/2026-05-19-20-deep-review.md`。

## 修复内容

- ApplicationExitInfo：把 API 30 的 exit reason 查询与 API 31+ `getTraceInputStream()` native tombstone protobuf 拆开，补充可能返回 `null` 的系统缓冲覆盖边界。
- Breakpad：将无效锚点 `FindSymbolFile()` / `SourceLineResolver::FindLine()` 改为 `SimpleSymbolSupplier::GetSymbolFileAtPathFromRoot()` 与 `BasicSourceLineResolver::Module::LookupAddress()`。
- JNI：删除不存在的 `JNIEnv->FindSymbol()` 说法，改成 ART native method resolve、`dlsym()`、`RegisterNatives()` 和 `UnsatisfiedLinkError` 边界。
- CFI / Java frame：明确 `.eh_frame` 只支撑 native unwind，Java / Dex / JIT / interpreter 帧由 `libunwindstack` 结合 ART / Dex / JIT 元数据识别。
- Crashpad：修正 Android / Linux 侧 signal handler、handler process、socket、`ptrace` / broker 的职责分工。
- mooner：补充隔离线程、竞态、跳过 C++ 栈展开/锁释放、状态破坏、保留 tombstone / ApplicationExitInfo / minidump 的使用边界。

## 状态回流

章节已设置为 `status: ready-for-review`、`pipeline_stage: task6_pending`、`task6_state: revisiting`、`task9_state: pending`、`task2b_state: fixed`，等待下一轮 Task6 → Task9 复核。queue 中无可标记 completed 的同源 pending 条目。

## 验证

- Android 版本边界：未引入 Android 18 / API 38+ 内容；正文仍以 android-16.0.0_r1 与 API 31+ 公共边界为依据，处于 Android 17 / API 37 上限内。
- 文风禁用词扫描：修改后全文禁用词命中 0；高频词 `真正/实际上/其实/根本/彻底/确实` 均为 0。
- 限制句式：全文现存 2 处“不是 X，而是 Y”旧句式，未超过 SKILL.md 上限，本轮未新增。
- 剩余 fallback backlog：67。queue P95/P90/P85：0/0/0。
