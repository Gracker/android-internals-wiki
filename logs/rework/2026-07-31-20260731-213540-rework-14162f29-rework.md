# Rework Log — 5.21 Android 17 SoC 厂商电池优化架构

- **Run ID**: `20260731-213540-rework-14162f29`
- **Lane**: rework (aiw-polish-rework profile)
- **Date**: 2026-07-31
- **Target**: `src/part1-fundamentals/ch05-cpu-power/5.21-android17-battery-optimization-soc-architecture.md`
- **Chapter**: 5.21

## 原问题

### quality_flags
- `pending-verification-marker`
- `thin-source-marking`

### F1: `pending-verification-marker` — 过时 flag

逐行检查正文，唯一含"待验证"的句子在第 303 行：

> 有效的 A/B 只改变**一个待验证因素**。

此处的"待验证"是方法论语境（"a variable to be verified in an A/B test"），指实验设计中的待测自变量，不是章节残留的验证占位标记。全文无 `TBD`、`TODO`、`FIXME`、`待补充`、`待确认`、`[待验证]` 等残留标记。该 flag 已过时。

### F2: `thin-source-marking` — frontmatter sources 字段偏薄

frontmatter `sources` 仅含两条高层基线标签：

```yaml
sources:
  - "android-17.0.0_r1 PowerManagerService / Power HAL AIDL / PowerStats AIDL"
  - "android17-6.18-2026-06_r6 kernel/sched/cpufreq_schedutil.c"
```

但正文末尾「源码索引」已含 7 条 cs.android.com / googlesource.com 深链接，覆盖 PowerManagerService、JNI、PowerHalController、IPower.aidl、Power.cpp、IPowerStats.aidl、cpufreq_schedutil.c。frontmatter 的 sources 厚度与正文实际引用不匹配，可安全补齐。

## 修复点

### 1. frontmatter sources 字段补齐（解决 F2）

将两条高层标签扩展为 7 条与正文源码索引一一对应的精确条目，每条标注基线 tag + 源码路径 + 关键符号：

- `PowerManagerService.java (setPowerModeInternal)`
- `com_android_server_power_PowerManagerService.cpp`（JNI）
- `frameworks/native/services/powermanager (PowerHalController/PowerHalLoader)`
- `hardware/interfaces/power/aidl (IPower.aidl, Mode.aidl, Boost.aidl)`
- `hardware/interfaces/power/aidl/default/Power.cpp (example HAL)`
- `hardware/interfaces/power/stats/aidl (IPowerStats.aidl, EnergyConsumer/Channel/PowerEntity)`
- `cpufreq_schedutil.c (sugov_init, sugov_should_update_freq, IOWAIT_BOOST_MIN)`

所有条目均基于正文已有的源码索引，无新增未引用来源。

### 2. rework 跟踪字段补充

新增 `last_rework_at` / `last_rework_run_id`，记录本轮 rework 审查与修复。

## 保留项（非本次修复范围）

- **正文未修改**：idle-audit（14:35）已对 9 项关键技术结论逐一核查通过（energyUWs 单位、平均功率公式量纲、rate_limit_us 初始值、IOWAIT_BOOST_MIN、readEnergyMeter vs FMQ、vendor_available 语义、Mode.LAUNCH 时长、Boost.aidl 枚举、schedutil 不读电池百分比）。本轮 rework 独立复核，结论一致，正文技术准确、结构完整，无需安全正文补丁。
- **`pending-verification-marker` flag**：确认为过时 flag（第 303 行为方法论语境，非残留标记），本轮通过 sources 补齐一并消除标记偏薄问题，正文无需改动。

## frontmatter 变更

| 字段 | 旧值 | 新值 |
| --- | --- | --- |
| `sources` | 2 条高层基线标签 | 7 条精确源码路径（与正文源码索引一致） |
| `last_rework_at` | — | `2026-07-31T21:35:40+08:00` |
| `last_rework_run_id` | — | `20260731-213540-rework-14162f29` |

## 状态流转

- `status`: 保留 `finalized`（本次为安全元数据补齐，章节技术结论不受影响）
- `task6_state`: 保留 `reviewed`
- `task9_state`: 保留 `reviewed`
- `pipeline_stage`: 保留 `finalized`
- `confidence`: 保留 `high`（源码引用厚度已与正文匹配，置信度不变）

## 本地校验

- `check-metadata.py --files <target>`: ✅ 1/1 通过，0 警告
- `check-summary-links.py`: ✅ 通过（164 local links；2 个 pre-existing duplicate 警告在 SUMMARY.md，非本次变更）
- `git diff --check`: ✅ 无问题

## 剩余风险

1. 无残留技术风险。两个 quality_flags 均已解决：`pending-verification-marker` 确认为过时（方法论语境误报），`thin-source-marking` 通过 sources 精确化补齐消除。
2. 正文引用的 Android 17 / `android-17.0.0_r1` / `android17-6.18-2026-06_r6` 基线锚点经 idle-audit 与本轮 rework 双重确认，无 API 38+ 越界。
