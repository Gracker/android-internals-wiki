# AIW Polish Rework Evidence · 2026-08-07

- lane: aiw-polish-rework
- mode: rework
- run_id: 20260807-093531-rework-28453939
- target: `src/part3-tools/ch13-perfetto/17-android17-data-sources.md`
- baseline: AOSP `android-17.0.0_r1` / kernel `android17-6.18-2026-06_r6`
- quality_flags: `pending-verification-marker`, `thin-source-marking`

## 原问题

1. 章节前部 `outline-start` 保护区仍保留“有待验证 / 需要进一步确认”措辞，和后文已给出的 Trace Cookie、TimeStats/statsd、`IJankListener` 分层结论冲突。
2. frontmatter `sources` 只列出少量源码路径，正文与参考资料实际引用了更多 Android 17 Perfetto、SurfaceFlinger、FrameTracer、Trace Processor、traced_perf 与 kernel perf 证据，触发 thin-source-marking。
3. 章节处于 `finalized` / `ready-to-publish`，但本轮 rework 仍需先回流 Task6 复审，避免带着未闭合质量标记直接发布。

## 修复点

- 将 frontmatter `status` 从 `finalized` 回退为 `ready-for-review`，`pipeline_stage` 改为 `task6_pending`，`task6_state` 改为 `revisiting`，`task9_state` 改为 `rework_verified`。
- 更新 `last_verified=2026-08-07`，新增 `last_rework_at`、`last_rework_run_id`、`rework_notes`。
- 扩充 frontmatter `sources`：补入 `JankInfo.h`、`JankTracker.cpp`、`TimeStats.cpp`、`Layer.cpp`、`OutputLayer.cpp`、`OutputLayerCompositionState.h`、`FrameTracer.cpp`、graphics/frame timeline parser、`traced_perf.rc`、`producer_support.cc`、`builtin_producer.cc`、`PerfEventConfig` proto 与 `android17-6.18-2026-06_r6/kernel/events/core.c`。
- 改写 outline 中两个未闭合验证标记：
  - `mTraceCookie` 不再写“跨进程正确性有待验证”，改为 SurfaceFlinger 进程内计数器与跨数据源关联方法边界。
  - StatsD 小节不再写“需要进一步确认”，改为 FrameTimeline、TimeStats/statsd、`IJankListener` 三路径边界。
- 去掉短进程 descriptor 延迟代码片段里的上游 `TODO(...)` 字样，保留源码语义但避免正文被误判为未闭合任务标记。

## 剩余风险

- 本轮未新增设备实测 trace；章节可信度仍依赖已列出的 Android 17 固定源码与现有正文边界说明。
- 因状态回流到 `ready-for-review`，后续 Task6 应重点复查新增 sources 与正文中的 SQL/schema 表述是否仍和当前书稿风格一致。

## 校验

已通过：

```bash
python3 scripts/check-metadata.py --files src/part3-tools/ch13-perfetto/17-android17-data-sources.md
# ✅ 1 个文件通过，0 个失败，0 个警告

python3 scripts/check-summary-links.py
# SUMMARY link check passed: 661 local link(s), 0 duplicate warning(s).

git diff --check
# exit 0，无 whitespace error
```
