# 深度技术 Review 闲时抽检 · 2026-07-11 10:29

## Review 目标
- 章节：13.14 Perfetto DataGrid 与 Jank CUJ 标准库
- 文件：src/part3-tools/ch13-perfetto/14-perfetto-data-explorer-jank-cuj.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 抽检结论
- 结果：AUTO-FIX
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：0 处

## AUTO-FIX
1. [P0][源码/API 数据流] 自定义 CUJ marker 与 counter metrics 数据流
   - 原文位置：自定义 atrace marker 小节、调用链总图
   - 问题：图中把 `Trace.beginSection("J<my_cuj>") ... Trace.endSection()` 直接连到 `J<my_cuj>#totalFrames` process counter track，容易误导为 begin/end section 会写 counter。Android 17 `Trace.java` 中 `beginSection()` / `endSection()` 写 slice，counter 由 `Trace.setCounter()` 写入。
   - 修复：补充 `Trace.setCounter("J<my_cuj>#totalFrames", value)` 才能写出 `J<*>#*` process counter；图中把 slice 与 counter 两条路径拆开。
   - 证据：AOSP android-17.0.0_r1 `frameworks/base/core/java/android/os/Trace.java`：`beginSection()` / `endSection()` 与 `setCounter()` 是不同 API；Perfetto v54.0 `jank/internal/counters.sql` 读取 `process_counter_track` 中的 `J<*>#*` counter。

2. [版本边界][源码锚点] FrameTimeline 文档来源
   - 原文位置：frontmatter sources
   - 问题：来源仍指向 `google/perfetto/main/docs/data-sources/frametimeline.md`。
   - 修复：改为 `https://raw.githubusercontent.com/google/perfetto/v54.0/docs/data-sources/frametimeline.md`，避免 main 分支资料进入 Android 17 正文链路。
   - 证据：Perfetto v54.0 tag 下该文档存在。

## 已复核通过的锚点
- Perfetto v54.0 `cujs/base.sql`：`_is_jank_slice` 过滤 `J<*>` 且进程名匹配 `com.google.android*` / `com.android.*`。
- Perfetto v54.0 `jank/internal/counters.sql`：`weightedAppJank` / `weightedSfJank` 先除以 1000，counter 起点由 `ts_earliest_allowed_counter` 控制。
- Perfetto v54.0 `android/android_jank_cuj.sql`：最终 weighted total 使用 `weighted_missed_app_frames * anim_duration_ms / 1000` 等字段。
- Perfetto v54.0 `jank/relevant_threads.sql`：`android_jank_cuj_sf_main_thread` / `android_jank_cuj_sf_thread()` 是 `_android_sf_*` 的兼容包装，并带 google3 迁移 TODO。
- AOSP android-17.0.0_r1 `ViewRootImpl.java`：`scheduleTraversals()` -> `postVsyncCallback()` -> `TraversalCallback#onVsync()` -> `doTraversal()` 行号与正文一致。
- Perfetto v54.0 `dev.perfetto.ExplorePage`：`NodeExplorer.updateQuery()` -> `QueryExecutionService.processNode()` -> summarizer API / `SQLDataSource` 路径成立。

## 闭环动作
- 修改章节正文：是
- 更新 frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
