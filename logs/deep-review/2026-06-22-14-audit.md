# Task 9 闲时抽检 · 2026-06-22 14:25

## 抽检目标
- 章节：22.7 WebView 性能优化实战
- 文件：src/part5-app/ch22-rendering-practice/07-webview-optimization.md
- 状态：finalized
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖

## 证据来源
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/WebSettings.java`: `getDefaultUserAgent(Context)` 调用 `WebViewFactory.getProvider().getStatics().getDefaultUserAgent(context)`。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/WebViewFactory.java`: `getProvider()` 负责加载并缓存 WebView provider。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/WebView.java`: `destroy()` 先 `checkThread()`，`evaluateJavascript()` 要求 UI thread，`setDataDirectorySuffix()` 要求在本进程创建 WebView 或调用 android.webkit 其他方法前执行。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/WebViewClient.java`: `shouldInterceptRequest()` 在非 UI 线程调用；`onRenderProcessGone()` 对受影响的每个 WebView 回调，返回 false 时 renderer crash 会导致 app crash，renderer 被系统 kill 时 app 会被 kill。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/RenderProcessGoneDetail.java`: `didCrash()` 与 `rendererPriorityAtExit()` API 存在。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：5/5
- P0/P1：0
- 结论：章节中 WebView provider 预装载、`setDataDirectorySuffix()`、`shouldInterceptRequest()` 线程边界、JS Bridge 私有后台线程、`evaluateJavascript()` UI 线程约束、`destroy()` 线程检查、`onRenderProcessGone()` 多 WebView 回调与返回值语义，均与 android-17.0.0_r1 AOSP / Android API 文档一致。

### 维度 3：版本差异覆盖
- 评分：5/5
- P0/P1：0
- 结论：章节范围标为 Android 10(API 29)-Android 17(API 37)，未出现 Android 18/API 38+ 内容；对 Android P/API 28 `setDataDirectorySuffix()` 与 Android 8/API 26 `onRenderProcessGone()` 的引入说明只作为历史边界，未越过 AIW 版本上限。

## 发现问题
- P0：0
- P1：0
- P2：0
- P3：1
  - 后续可把文内 `[已验证: AOSP android16-release]` 的 WebView API 锚点补充为 `android-17.0.0_r1`，本轮已验证关键 API 在 Android 17 标签下仍一致；该项不影响正文结论。

## 闭环动作
- queue.json：0
- research-gaps.md：0
- suggestions.md：0
- 章节内容：未改动
- frontmatter：记录 `last_task9_audit: 2026-06-22` 与 `last_task9_audit_log: logs/deep-review/2026-06-22-14-audit.md`
