# 深度技术 Review · 2026-04-11 15:20

## Review 目标
- 章节：2.16 Sync Fence 框架与帧同步机制
- 文件：src/part1-fundamentals/ch02-rendering/16-sync-fence.md
- 状态：ready-for-review（task6_state: reviewed，task2b_state: pending）

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：3
- 具体问题：
  1. [P0] 行 92 — “用户空间不能创建或 signal fence”与 `system/core/libsync/sw_sync.h` 暴露的 `sw_sync_timeline_create()` / `sw_sync_timeline_inc()` / `sw_sync_fence_create()` 矛盾。
  2. [P0] 行 276-278 — 把 Android 14-16 写成“HWUI 全面切换到 Skia”，并把真实路径写成 `GrDirectContext::flushAndSignal()`；`android-16.0.0_r1` 实际路径在 `libs/hwui/pipeline/skia/SkiaOpenGLPipeline.cpp`，使用 `skgpu::ganesh::FlushAndSubmit(surface)` + `EglManager::createReleaseFence()`。
  3. [P2] 行 284-290 — “fd 没关 → buffer 永远不回到 free pool”把 fd 泄漏与 BufferQueue 生命周期问题混写，缺少精确源码/诊断边界。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] 行 57-65、267-269 — 从 `dma-buf fence` 直接跳到 `sync_timeline` / `sync_pt` / `sync_fence`，但没有解释 legacy 文档模型与 modern `sync_file_info` / `sync_fence_info` API 的映射，读者很难把正文结论对到当前 AOSP。

### 维度 3：版本差异覆盖
- 评分：1/5
- 问题数：3
- 具体问题：
  1. [P0] 行 263-265 — “Android 8：引入 HWC2”不准确，`HWC2.h` 在 `android-7.0.0_r1` 已存在。
  2. [P0] 行 267-269 — “Android 10：迁移到 libsync”不准确，`libsync` 与 `android/sync.h` 至少在 `android-8.1.0_r81` 已存在，`android-16` 仍保留 legacy 兼容层。
  3. [P0] 行 276-278 — 把 Skia 切换与 fence API 变化压缩到 Android 14-16，版本线严重失真。

### 维度 4：知识盲区
- 评分：2/5
- 盲区数：1
- 具体问题：
  1. [P1] 缺“现代 explicit sync 演进链”：HWC1 → HWC2、legacy sync → modern sync_file、HWUI release fence 生成、present fence → `VSyncReactor` / FrameTimeline 没有被串起来。

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] 行 243-247、251-259 — BufferQueue queued 数量阈值、SurfaceFlinger track 观察点都没有真实 Perfetto / 设备上下文支撑，读者无法判断这些结论适用范围。

### 维度 6：交叉引用一致性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] 行 134-163、317-319 vs §2.5 行 213-223 — 本章把 `queueBuffer()` 携带的 fence 写成 acquire fence，§2.5 把同一条 fence 写成 releaseFence，术语不一致。

## 统计
- P0 事实错误：4 处
- P1 重要缺失：2 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：2.1/5

## 闭环动作
- 写入 queue.json（P95）：1 处（升级并合并现有 2.16 pending review 条目）
- 写入 research-gaps.md：1 处
- 写入 suggestions.md：2 处
- 仅日志记录：0 处
