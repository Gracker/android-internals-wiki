# 深度技术 Review · 2026-05-19 07:31

## Review 目标
- 章节：13.6 线程 CPU 状态分析
- 文件：src/part3-tools/ch13-perfetto/06-thread-cpu-states.md
- 状态：ready-for-review → finalized（自动晋升）
- 章节：22.6 图片加载与显示优化
- 文件：src/part5-app/ch22-rendering-practice/06-image-loading.md
- 状态：ready-for-review → finalized（自动晋升）
- 章节：22.8 帧率监控与线上卡顿治理
- 文件：src/part5-app/ch22-rendering-practice/08-frame-monitoring.md
- 状态：ready-for-review → finalized（自动晋升）

## external-review 输入
- `logs/external-review/` 根目录无活跃文件；本轮未消费 external-review。

## 一手核对锚点
- Perfetto CPU Scheduling docs：`sched_switch`、`sched_waking`、`sched_wakeup_new`、`end_state=R/R+/S/D` 与 UI thread state 解释。
- Perfetto SQL docs / stdlib 搜索结果：`sched` 与 `sched_slice` 兼容口径；当前 SQL 使用 `sched` 不构成 P0/P1。
- AOSP android16-release：`FrameMetrics.java` 中 `GPU_DURATION=12`、`DEADLINE=13`、`getMetric()` 不可用时返回 `-1`。
- AOSP android16-release：`BitmapFactory.java` 的 `inBitmap` / `inSampleSize` 约束，`BitmapRegionDecoder.java` 的区域解码入口与格式列表。
- AndroidX metrics-performance main：`FrameData` 每帧复用，`StateInfo(key, value)` 字段与章节示例一致。
- Android Developers 搜索摘要：AVIF Android 12/API 31+ 支持；JankStats `FrameDataApi31.frameOverrunNanos` 与 StateInfo 用法。

## 审查结果：13.6 线程 CPU 状态分析

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 说明：Perfetto `sched_switch` / `sched_waking` / `sched_wakeup_new`、`R+` preempted、`sched_blocked_reason` 与 D 状态说明复核通过；此前 irq/softirq 上下文问题已修正为 ARM64 IRQ 独立栈、softirq/ksoftirqd 分流。

### 维度 2：原理链完整性
- 评分：4.5/5
- 问题数：0
- 说明：Running/Runnable/Sleeping/D 状态 → wakeup → scheduling latency → SQL 量化 → CPU 架构/频率的链路完整。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：Android 8-16 范围内，`sched_blocked_reason` Android 内核补丁、Perfetto v53+ `io_wait` 边界已有说明。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：未发现需要进入 research-gaps 的高优盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：状态诊断以 Trace/SQL 观测点为主，足够支撑本节定位目标。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：5.1、13.1、13.5 引用方向一致；`sched`/`sched_slice` 口径仅保留为既有 P2 建议，不构成阻塞。

## 审查结果：22.6 图片加载与显示优化

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0
- 说明：`Bitmap.java`、`BitmapFactory.Options`、`BitmapRegionDecoder`、`ImageDecoder` 路径与核心 API 复核通过；`inSampleSize` / `inBitmap` 的细节补充属于既有 P2，不新增 queue。

### 维度 2：原理链完整性
- 评分：4.5/5
- 问题数：0
- 说明：框架选型 → 解码缩放 → 大图 tile/region decode → 缓存层级 → 格式兼容的链路完整。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：上一轮 AVIF/API 31 fallback 的 P1 已补：Android 10/11 需要 WebP/JPG fallback，`ImageDecoder.setCrop()` 不替代 tile 方案。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：未发现 P1 级盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：像素内存、缓存命中、解码耗时等指标入口覆盖到位；“15 倍以上”虽可更精确到 16 倍，但属于既有 P2 文案/数据精度建议。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：22.1、23.2、7.10 引用方向一致。

## 审查结果：22.8 帧率监控与线上卡顿治理

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 说明：`Choreographer.FrameCallback`、`JankStats.createAndTrack()`、`FrameData.states` / `StateInfo.key/value`、`FrameMetrics` 拷贝用法复核通过。

### 维度 2：原理链完整性
- 评分：4.5/5
- 问题数：0
- 说明：帧节奏 → JankStats 状态 → FrameMetrics 阶段归因 → 堆栈采样 → 服务端告警/分派链路完整。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0
- 说明：上一轮 `GPU_DURATION` / `DEADLINE` API 31+ P1 已补，Android 10/11 返回 `-1` 与兜底口径已写明。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：未发现 P1 级盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：`frozen_count` 阈值来源、动态刷新率监听仍是既有 P2 建议，已在 suggestions.md，不重复写入。

### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0
- 说明：7.2、7.9、19.06、19.11、19.12、21.8、22.1、22.3、22.5、26.3 引用方向一致。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- 新增 P2 建议：0 处
- 既有 P2 建议：6 处（已在 `intake/suggestions.md`，本轮不重复写入）
- P3 锦上添花：0 处
- 总体技术评分：4.3/5

## 闭环动作
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（无新增，既有 P2 不重复）
- 仅日志记录：既有 P2 跟踪 6 处
- 更新 frontmatter：3 个章节 `task9_result=pass-tech-review`、`task9_state=reviewed`
- 自动晋升 finalized：3 个章节（均满足 task6_result=pass-light-edit 且 queue 无 pending）
