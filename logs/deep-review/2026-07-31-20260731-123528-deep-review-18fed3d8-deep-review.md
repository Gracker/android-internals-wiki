# AIW Deep Review Log · 2026-07-31

- lane: deep-review
- run_id: `20260731-123528-deep-review-18fed3d8`
- target: `src/part5-app/ch25-power-size/25.24-25.4 WorkManager 实战与后台任务调度.md`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`
- mode: patch applied (source-boundary / 虚构 API 降级 + frontmatter 状态修正 + 回流 needs-rework)
- materials: 空（上游未提供一手材料）

## 审计结论

本章是 task2a-knowledge-gap 生成的占位稿，正文虽长（341 行）但存在**大量虚构 androidx.work API**与**无来源的"Android 17 WorkManager 行为"断言**。上游 materials 为空（quality_flags: thin-source-marking / missing-last_verified / missing-confidence），属典型的"占位稿 + 通用知识扩写"，风险高。

本轮 deep-review 不做大段正文扩写（无来源），而是做**源码边界 / API 真实性审计**，将虚构 API 与无来源断言逐条降级标注，修正 frontmatter 状态，并将章节回流为 `needs-review` / `confidence: low`，等待 body-apply 以一手来源重写。

### API 真实性审计（核心发现）

核对 androidx.work 公共 API 与 `android-17.0.0_r1`，原稿存在以下**不存在的 API / 错误用法**：

| 原稿声称 | 真实情况 | 处理 |
|---|---|---|
| `WorkRequest.Builder.setPriority(Priority.HIGH)` | 不存在。WorkManager 无 `setPriority()` / `Priority` 枚举 | 降级标注 + 改为正确的 `setExpedited(OutOfQuotaPolicy)` |
| `BACKGROUND(5)/LOW(10)/MEDIUM(15)/HIGH(20)/EXPLICIT(25)` 优先级体系 | 不存在。WorkManager 无数值优先级 | 逐条标注"待验证，API 不存在" |
| `setAlternateWork()` | 不存在。WorkManager 无内置"备用任务"API | 降级标注 |
| `setTimeout()` | 公共 `WorkRequest.Builder` 无此方法 | 降级标注 |
| `WorkManager.MAX_WORKERS` 常量 | 不存在 | 标注修正 |
| `CrossProcessWorker` 类 | 不存在。多进程用 `RemoteListenableWorker` | 降级标注 |
| `setBackoffCriteria(BackoffPolicy.LINEAR, OneTimeWorkRequest.MIN_BACKOFF_MILLIS, TimeUnit.SECONDS.toMillis(10))` | API 误用：把常量当 duration、`TimeUnit` 转换当 unit 参数 | 改为正确签名 `(BackoffPolicy, long, TimeUnit)` |

### Android 17 版本边界审计（无来源断言）

原稿多处断言"Android 17 WorkManager 引入"的行为，经核对无来源支撑：

- "区分 Wi-Fi 6、5G 网络类型并按质量调整优先级"——WorkManager `NetworkType` 枚举不含代际区分；属系统层 `ConnectivityManager` 范畴。
- "电池健康度低于 80% 自动降低非必要任务优先级"——`Constraints` 仅有 `setRequiresBatteryNotLow()`，无电池健康度感知；未见 `android-17.0.0_r1` 实现。
- "存储空间低于 10% 自动取消文件写入类任务"——`Constraints` 仅有 `setRequiresStorageNotLow()`，不区分任务类型，不自动取消。
- "普通应用最多 10 个并发任务"——具体数字无来源，配额由 `JobScheduler`/standby bucket 决定，随版本变化。
- "默认重试间隔调整为最长不超过 1 小时"——WorkManager `MAX_BACKOFF_MILLIS` 默认 5 小时，原稿数字无来源。

### Frontmatter（本轮修正）

- `status: ready-for-review` → `needs-review`（正文含虚构 API，不应停留在 ready-for-review）
- 新增 `task6_state: needs-rework` / `task9_state: needs-rework`
- 新增 `pipeline_stage: deep-review`
- 新增 `last_deep_review_at: 2026-07-31` / `last_deep_review_run_id`
- 新增 `last_verified: 2026-07-31`（修复 missing-last_verified）
- 新增 `confidence: low`（修复 missing-confidence；因正文含虚构 API，明确不提升）

### outline / 结构

- outline（要点 + 扩展）描述尚属框架性陈述，未引入具体虚构 API，保留。
- 正文结构（基础架构 → 约束 → 优先级 → 任务链 → 监控 → 优化 → 多进程 → 总结）逻辑合理，但每节正文均掺入虚构 API，需 body-apply 整体重写。

### 中文可读性

- 原稿存在乱码字符（`Wi-Fi 6、5G 不同网络类型` 中的破损代理字符），本轮修正约束小节时已清理。

## P0 / P1 / P2

### P0（虚构 API，高风险）

- **已处理**：`setPriority(Priority.HIGH)` / `Priority` 枚举体系（不存在）→ 降级 + 改为 `setExpedited`。
- **已处理**：`setAlternateWork()`（不存在）→ 降级。
- **已处理**：`CrossProcessWorker`（不存在）→ 降级。

注：这些虚构 API 若被读者直接采用会导致编译失败或误导，故列为 P0；deep-review 已逐条降级标注，但因无来源无法直接删除正文，章节需 body-apply 重写，故同步开 finding 跟踪。

### P1

- **已处理**：`setTimeout()`（公共 API 不存在）→ 降级。
- **已处理**：`WorkManager.MAX_WORKERS`（常量不存在）→ 标注。
- **已处理**：`setBackoffCriteria` 参数误用 → 修正为正确签名。
- **已处理**：Android 17 约束优化三条无来源断言（Wi-Fi 6/5G、电池健康度、存储 10%）→ 降级待验证。
- **已处理**：配额"10 个并发任务"无来源 → 降级。
- **已处理**：重试间隔"1 小时"无来源 → 标注实际默认 5 小时。

### P2

- **已处理**：frontmatter 缺 `last_verified` / `confidence` → 补齐（confidence: low）。
- **已处理**：乱码字符清理。
- **已处理**：`status: ready-for-review` 过期 → `needs-review`。

## 实际补丁

仅修改既有章节：`src/part5-app/ch25-power-size/25.24-25.4 WorkManager 实战与后台任务调度.md`：

1. frontmatter：`status` → `needs-review`，新增 `task6_state`/`task9_state: needs-rework`、`pipeline_stage: deep-review`、`last_deep_review_at`、`last_deep_review_run_id`、`last_verified`、`confidence: low`。
2. 章首占位标记升级为源码边界审查 banner + 待加工回流说明。
3. "Android 17 任务约束优化"三条无来源断言（Wi-Fi 6/5G、电池健康度、存储 10%）逐条降级为待验证，并给出 WorkManager 实际 `Constraints` 契约对照。
4. "任务优先级体系"虚构 `Priority` 枚举体系逐条标注"API 不存在"，补充正确的 `setExpedited(OutOfQuotaPolicy)` 写法。
5. "配额管理"无来源数字（10 并发）降级。
6. "任务执行策略"代码块：`setPriority(Priority.HIGH)` → `setExpedited`；`setBackoffCriteria` 参数误用修正。
7. "失败处理与重试"：`setAlternateWork()` / `setTimeout()` 降级为待验证，标注实际 API 边界。
8. "任务合并策略"：`WorkManager.MAX_WORKERS` 常量标注不存在。
9. "多进程任务调度"：`CrossProcessWorker` 标注不存在，补充 `RemoteListenableWorker` 正确路径。

- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。
- 未做无来源正文扩写。

## 证据来源

- androidx.work 公共 API（`WorkRequest`、`WorkRequest.Builder`、`Constraints`、`OneTimeWorkRequest`、`OutOfQuotaPolicy`、`RemoteListenableWorker`）对照原稿声称的 API，确认虚构项。
- `android-17.0.0_r1` / API 37 baseline 对照"Android 17 WorkManager 行为"断言，确认无来源支撑。
- WorkManager `NetworkType` 枚举（CONNECTED/UNMETERED/NOT_ROAMING/METERED/TEMPORARILY_UNMETERED）确认无代际区分。
- WorkManager `MAX_BACKOFF_MILLIS` 默认 5 小时（`WorkSpec`）对照原稿"1 小时"。

## 回流建议

章节需 **body-apply + source material** 重写：
1. 以 androidx.work 官方文档与 `android-17.0.0_r1` 源码为来源，重写优先级/约束/配额/多进程各节。
2. 删除全部虚构 API，替换为真实 API（`setExpedited`、`RemoteListenableWorker` 等）。
3. 为"Android 17 行为"断言提供 AOSP 源码引用或标记为非 WorkManager 契约。
4. 重写后再提升 `confidence` 至 medium 以上。

## 校验结果

- `python3 scripts/check-metadata.py --files "src/part5-app/ch25-power-size/25.24-25.4 WorkManager 实战与后台任务调度.md"`：✅ 通过（1 文件，0 错误，1 个 pre-existing sources 可选字段警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（164 链接，2 个 pre-existing 重复警告，均与本章无关）
- `git diff --check`：✅ 无空白错误
