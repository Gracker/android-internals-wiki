# 深度技术 Review 闲时抽检 · 2026-07-02 11:25

## Audit 目标
- 章节：9.5 案例集
- 文件：src/part2-performance/ch09-anr/05-case-studies.md
- 模式：闲时抽检（源码引用 + 版本差异）
- Android 基准：android-17.0.0_r1（Android 17 / API 37）

## 结论
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 AUTO-FIX）
- P2 建议改进：0 处
- 本轮不写入 queue.json；章节回流 Task6 复审。

## 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 复核：`SharedPreferencesImpl.apply()` 仍通过 `QueuedWork.addFinisher()` 注册等待；`ActivityThread.handlePauseActivity()` 仅 pre-Honeycomb 路径调用 `QueuedWork.waitToFinish()`，非 pre-Honeycomb 的 Activity 等待点在 `handleStopActivity()`；Service 等待点在 `handleServiceArgs()` 与 `handleStopService()`。Android 17 `InputDispatcher.cpp` 仍导出 `iq`、`oq:<InputChannel>`、`wq:<InputChannel>` atrace counter，`dumpsys input` 仍输出 `WaitQueue: length=%zu`。`ProcessState.cpp` 仍定义 `DEFAULT_MAX_BINDER_THREADS = 15`，并保留 joinThreadPool 额外线程边界。

## 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- AUTO-FIX/P1：章节 frontmatter 与正文两处源码解释仍锚定 `android-14.0.0_r1`，不符合 2026-06-27 后 AIW Android 17 / API 37 主线基准。已按一手源码把 `last_verified_against`、QueuedWork 等待点说明、Binder 线程池说明统一更新为 `android-17.0.0_r1`；行为口径与 Android 17 源码一致。

## 证据
- AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/app/SharedPreferencesImpl.java`：`apply()` 调用 `QueuedWork.addFinisher()`，后台写入后 `removeFinisher()`。
- AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/app/ActivityThread.java`：`handlePauseActivity()` 仅 `r.isPreHoneycomb()` 时 `waitToFinish()`；`handleStopActivity()` 对非 pre-Honeycomb 调用；`handleServiceArgs()` / `handleStopService()` 调用。
- AOSP `android-17.0.0_r1` `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp`：`ATRACE_INT("iq")`、`oq:%s`、`wq:%s` 与 `WaitQueue: length=%zu` dump 仍存在。
- AOSP `android-17.0.0_r1` `frameworks/native/libs/binder/ProcessState.cpp`：`DEFAULT_MAX_BINDER_THREADS 15`，注释说明 `joinThreadPool()` 直接加入线程池会影响总处理线程口径。

## 闭环动作
- AUTO-FIX：1 处（版本基准锚点统一到 Android 17）
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_audit: 2026-07-02`、`last_task9_autofix_at: 2026-07-02`。
- 写入 queue.json：0 处
- 写入 suggestions.md：0 处
