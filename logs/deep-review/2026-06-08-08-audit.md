# Task 9 闲时抽检 · 2026-06-08 08:20

## 抽检目标
- 章节：24.1 文件 I/O 优化
- 文件：src/part5-app/ch24-io-network/01-file-io-optimization.md
- 状态：finalized
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异

## 审计结论
- 结果：auto-fixed
- P0：0
- P1：2
- P2：0
- Queue：未写入，问题已本轮小范围修复
- Task6：已回到 revisiting / task6_pending

## 发现与修复

### P1 · 源码锚点未 pin 到稳定 tag
- 位置：frontmatter `last_verified_against`、AOSP sources、正文 4 处 `[已验证]` 标注
- 问题：章节仍使用 `AOSP master snapshot 2026-05-14`。按 2026-05-29 Android 版本边界，main/master 资料不能直接作为 Android 17/API 37 范围内的正文结论；本节 applicable_versions 到 Android 16，但也需要稳定 release tag 锚点。
- 复核：AOSP Gitiles `android-16.0.0_r1` 下 `SharedPreferencesImpl.java`、`QueuedWork.java`、`StrictMode.java`、`AtomicFile.java` 均返回 200，路径存在。
- AUTO-FIX：将源码标注统一改为 `AOSP android-16.0.0_r1`，并在 sources 中为 4 个 AOSP 路径补齐 `@ android-16.0.0_r1`。

### P1 · QueuedWork 生命周期等待点表述过宽
- 位置：`SharedPreferences 的坑与 DataStore 迁移` 小节，`apply()` 段落
- 问题：原文写 `Activity onPause 等位置可以等待这些写入结束`，对 Android 10-16 的普通现代 App 过宽。
- 复核：`ActivityThread.handlePauseActivity()` 仅在 `r.isPreHoneycomb()` 时调用 `QueuedWork.waitToFinish()`；`handleStopActivity()` 对非 pre-Honeycomb 路径调用 `QueuedWork.waitToFinish()`。`QueuedWork.waitToFinish()` 注释仍提到 Activity onPause，但正文应按 Android 10-16 实际路径写清边界。
- AUTO-FIX：改为“普通现代 App 主要在 `handleStopActivity()` 等待，`handlePauseActivity()` 只对 pre-Honeycomb 兼容路径等待”，并补 `ActivityThread.java @ android-16.0.0_r1` 锚点。

## 版本边界
- 未引入 Android 18 / API 38+ 内容。
- `android-17.0.0_r1` Gitiles 检查返回 404，本轮不把 Android 17 源码实现写成已验证结论。
- 正文源码结论收敛到 Android 16 / API 36 的公开 release tag。

## 闭环动作
- 修改章节：1 个
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- 日志：logs/deep-review/2026-06-08-08-audit.md
