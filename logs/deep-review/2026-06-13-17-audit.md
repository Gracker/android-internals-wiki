# 深度技术 Review 抽检 · 2026-06-13 17:20

## 抽检目标
- 模式：闲时抽检（preflight：CANDIDATES=0 / TASK2B_BACKLOG=0 / IDLE_AUDIT=297）
- 章节：1.18 Binder Freezer 与缓存进程冻结性能
- 文件：src/part1-fundamentals/ch01-architecture/18-binder-freezer-cached-process.md
- 状态：finalized / task9_result=pass-tech-review（抽检前）
- 选择原因：无 `last_task9_audit`，且 `last_task9_at=2026-05-27`；章节大量依赖 AOSP framework freezer 源码和版本边界，适合闲时抽检的源码引用 / 版本差异复核。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界
- Android 版本边界：未使用 Android 18 / API 38+ 资料；未取到 AOSP `android-17.0.0_r1` tag，framework 源码锚点使用 `android-15.0.0_r1` 与 `android-16.0.0_r1`，不把 AOSP main/master 当作 Android 17 正文结论。
- external-review：`logs/external-review/` 根目录无近 3 天活跃文件，本轮未消费 external-review。

## 核对资料
- AOSP `android-15.0.0_r1` `frameworks/base/services/core/java/com/android/server/am/ProcessList.java`：`FREEZER_CUTOFF_ADJ = CACHED_APP_MIN_ADJ`。
- AOSP `android-16.0.0_r1` `frameworks/base/services/core/java/com/android/server/am/ActivityManagerConstants.java`：`KEY_FREEZER_CUTOFF_ADJ = "freezer_cutoff_adj"`，`DEFAULT_FREEZER_CUTOFF_ADJ` 受 `Flags.prototypeAggressiveFreezing()` 影响，字段可由 DeviceConfig 更新。
- AOSP `android-16.0.0_r1` `frameworks/base/services/core/java/com/android/server/am/OomAdjuster.java`：`getFreezePolicy()` 使用 `mConstants.FREEZER_CUTOFF_ADJ` 判断冻结资格，并检查 `PROCESS_CAPABILITY_CPU_TIME` / `shouldNotFreeze()` / `isFreezeExempt()`。
- AOSP `android-16.0.0_r1` `CachedAppOptimizer.java`：`freezeProcess()` 在 `setProcessFrozen()` 前调用 `freezeBinder(pid, true, ...)`，`DEFAULT_FREEZER_DEBOUNCE_TIMEOUT = 10_000L`，`getBinderFreezeInfo()` 命中同步事务时使用 `REASON_FREEZER` + `SUBREASON_FREEZER_BINDER_TRANSACTION` kill。
- AOSP `android-16.0.0_r1` `IBinder.java`：`addFrozenStateChangeCallback()` 是 `@FlaggedApi(Flags.FLAG_BINDER_FROZEN_STATE_CHANGE_CALLBACK)`，并声明 kernel 不支持时抛 `UnsupportedOperationException`。

## 审查结果

### 维度 1：源码 / API 引用准确性
- 评分：4/5
- P0：0
- P1：1（已 AUTO-FIX）
- P2：0
- P3：0

1. [P1][AUTO-FIX] L77 / L123 / 参考资料 — 正文把冻结阈值写成 `ProcessList.FREEZER_CUTOFF_ADJ`，并标为 AOSP main 当前口径。复核发现 Android 15 tag 中该常量在 `ProcessList`，但 Android 16 tag 已改由 `ActivityManagerConstants.FREEZER_CUTOFF_ADJ` / DeviceConfig 管理；AOSP `android-17.0.0_r1` tag 当前不可取，不能用 main/master 代表 Android 17。
   - 修复：正文改为 Android 15 与 Android 16 分版本描述；补 `ActivityManagerConstants.java` 源码锚点；将 AOSP main 链接替换为 `android-15.0.0_r1` / `android-16.0.0_r1` Gitiles 链接。

## 维度 3：版本差异覆盖
- 评分：4/5
- P0：0
- P1：1（同上，已 AUTO-FIX）
- P2：0
- P3：0

1. [P1][AUTO-FIX] Android 16 边界 — 原版本表把 Android 16/17 合并成 “AOSP 主线路径稳定”，容易把 main 行为写成 Android 17 结论，也漏掉 Android 16 `FREEZER_CUTOFF_ADJ` 的 DeviceConfig 化。
   - 修复：版本表拆成 Android 16 与 Android 17 两行；Android 16 写明 `ActivityManagerConstants` / DeviceConfig 边界；Android 17 标注未取到公开 tag，正文结论仅使用 Android 16 及以下可验证源码。

## 已复核通过项
- `CachedAppOptimizer.freezeProcess()` 的 Binder 先冻结、再 `setProcessFrozen()` 的顺序与 Android 16 源码一致。
- `DEFAULT_FREEZER_DEBOUNCE_TIMEOUT = 10_000L` 与 Android 16 源码一致，且正文保留设备侧 DeviceConfig / 厂商配置差异。
- `ApplicationExitInfo.REASON_FREEZER`、`SUBREASON_FREEZER_BINDER_TRANSACTION`、`SUBREASON_FREEZER_BINDER_IOCTL`、`SUBREASON_FREEZER_BINDER_ASYNC_FULL` 在 Android 16 源码中存在；正文对 subreason 可见性已保留权限 / API 边界。
- `IBinder.addFrozenStateChangeCallback()` 在 Android 16 源码中为 flagged API，正文已改为 Android 16 可用时使用，不再写成泛泛“最新 API”。

## 统计
- P0 事实错误：0
- P1 重要缺失 / 需修正：1（已 AUTO-FIX）
- P2 建议改进：0
- P3 锦上添花：0
- 总体抽检评分：4.0/5

## 闭环动作
- AUTO-FIX：1 处
- queue.json：未写入（P1 已局部修复）
- research-gaps.md：未写入
- suggestions.md：未写入
- 章节正文：已修复冻结阈值源码锚点、Android 16 / Android 17 版本边界、AOSP 参考链接
- frontmatter：更新 `status=ready-for-review`、`pipeline_stage=task6_pending`、`task6_state=revisiting`、`task9_result=auto-fixed`、`last_task9_audit=2026-06-13`、`last_task9_autofix_at=2026-06-13`
