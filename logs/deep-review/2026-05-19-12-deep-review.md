# 深度技术 Review · 2026-05-19 12:20

## Review 目标
- 章节：14.1 Android Studio Profiler
- 文件：src/part3-tools/ch14-other-tools/01-as-profiler.md
- 状态：ready-for-review → task2b_pending（needs-rework）
- 章节：22.10 RenderEffect 与 RuntimeShader 性能实践
- 文件：src/part5-app/ch22-rendering-practice/10-rendereffect-runtime-shader-performance.md
- 状态：ready-for-review → task2b_pending（needs-rework）

## 审查依据
- Android Studio profile 官方文档：profileable/debuggable 边界、Profiler task 流程、Network Inspector 入口
- Android Power Profiler 官方文档：ODPM 是 device-level power rail，非 app-specific power attribution
- Android `ProfilingTrigger` API：`TRIGGER_TYPE_APP_FULLY_DRAWN` 为 cold start 中 `Activity.reportFullyDrawn()` 后触发
- AOSP master `View.java`：公开 `setRenderEffect(RenderEffect)`；未发现公开 `getRenderEffect()` / Kotlin `renderEffect` 属性；`setRenderEffect()` 仅在 `RenderNode.setRenderEffect()` 返回 true 时触发 `invalidateViewProperty(true, true)`
- Android 16 CDD 7.1.4.6 与 Perfetto `GpuCounterDescriptor`：CDD 要求 GPU profiling 输出符合 GPU counters / RenderStages schema；proto 未定义标准 `gpu_busy` 名称

## 审查结果

### 14.1 Android Studio Profiler

#### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1] L245 — `TRIGGER_TYPE_APP_FULLY_DRAWN` 被描述为“首帧绘制时触发”。公开 API 语义是 cold start 中 `Activity.reportFullyDrawn()` 被调用后触发，且系统提供 running system trace snapshot；不是首帧自动触发器。

#### 维度 2：原理链完整性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L233 — Power Profiler / ODPM 被写成可看到“App 的总功耗”并精确归因到子系统。官方文档说明 ODPM 是设备级功耗数据，不是 app-specific；只能与 App 操作时间窗做相关分析，需说明其他活跃 App 的噪声。

#### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L89-L100 — Android Studio 2020.3.1+ 以后 Network Inspector 位于 App Inspection，不应继续写成当前 Profiler 的同级模块并共享同一个 timeline。
  2. [P1] L199-L208 — profileable/debuggable 能力矩阵把 Java Method Trace 标成 profileable 通用不可用，但当前官方 profile 文档只明确要求 allocations、heap dump、部分 Interaction timeline 使用 debuggable；Java/Kotlin Method Recording 需要补版本证据或标 `[待验证]`。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0

#### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0

### 22.10 RenderEffect 与 RuntimeShader 性能实践

#### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L181-L223 — RuntimeShader 示例使用 `view.renderEffect !== effect`，但公开 View API 没有 `getRenderEffect()` / Kotlin `renderEffect` 属性。示例不可编译。应在 wrapper 内自行保存安装状态，或拆分首次安装与每帧更新逻辑。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：uniform 更新后必须显式触发 redraw 的原理已写入，但示例的状态判断实现错误。

#### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L255 — `gpu_busy` 仍被写成 Android 16+ 可查看/近似标准项，并声称 CDD 要求设备暴露 GPU 利用率 counter。CDD 与 Perfetto schema 只约束 GPU counters/RenderStages 输出格式，不标准化 `gpu_busy` 名称。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0

#### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0

## 统计
- P0 事实错误：1 处
- P1 重要缺失：5 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：14.1 为 3.8/5；22.10 为 3.8/5

## 闭环动作
- 写入 / 重开 queue.json：2 条
  - `task9-20260517-14.1-profiler-profilingmanager-api` → pending，priority 85，P1 × 4
  - `task9-2026-05-15-22.10-rendereffect-runtime-shader-review` → pending，priority 95，P0 × 1 / P1 × 1
- 更新章节 frontmatter：2 个章节均为 `task9_state: reviewed`、`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：0 个（存在 P0/P1）
