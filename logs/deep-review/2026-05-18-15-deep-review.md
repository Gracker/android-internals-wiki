# 深度技术 Review · 2026-05-18 15:25

## Review 目标
- 章节：13.12 Perfetto Profile 导入与 Flamegraph 分析
- 文件：src/part3-tools/ch13-perfetto/12-perfetto-profiles-flamegraph.md
- 状态：ready-for-review → finalized（自动晋升）

- 章节：2.6 SurfaceFlinger 与合成
- 文件：src/part1-fundamentals/ch02-rendering/06-surfaceflinger.md
- 状态：ready-for-review → task2b_pending

- 章节：5.3 大小核架构
- 文件：src/part1-fundamentals/ch05-cpu-power/03-big-little.md
- 状态：ready-for-review → task2b_pending

- 章节：14.7 ProfilingManager
- 文件：src/part3-tools/ch14-other-tools/07-profiling-manager.md
- 状态：ready-for-review → finalized（自动晋升）

- 章节：25.6 APK 体积分析与瘦身
- 文件：src/part5-app/ch25-power-size/06-apk-analysis.md
- 状态：ready-for-review → task2b_pending

## 选择说明
Preflight 曾发现 5 个候选。本轮先处理 13.12、2.6、5.3 三节；随后复核剩余候选 14.7、25.6。`logs/external-review/` 根目录无最近 3 天活跃文件。

## 审查结果

### 13.12 Perfetto Profile 导入与 Flamegraph 分析
- 结论：pass-tech-review
- 自动晋升：finalized（Task6 已通过，queue 无 pending）
- 维度评分：源码 4/5，原理 4/5，版本 4/5，盲区 4/5，数据 4/5，交叉引用 3/5
- 问题：无 P0/P1。P2：ch13 README 与 13.12 `related_chapters` 仍需补 13.11/13.12，已在 `intake/suggestions.md` 2026-05-15 条目记录。

### 2.6 SurfaceFlinger 与合成
- 结论：needs-rework
- 维度评分：源码 2/5，原理 4/5，版本 2/5，盲区 4/5，数据 3/5，交叉引用 4/5
- P0：L176-L224 把 Android 12-13 合并为 `onMessageReceived` / `INVALIDATE` / `REFRESH` 旧模型。AOSP android-13.0.0_r1 已无 `SurfaceFlinger::onMessageReceived()`、`onMessageInvalidate()`、`onMessageRefresh()`；`Scheduler/MessageQueue.cpp::Handler::handleMessage()` 直接调用 `commit()` → `composite()` → `sample()`。
- P2：L218 多显示段落写 `composite()` “可能按 display 并行触发”，但 android-16.0.0_r1 主路径是一次 `SurfaceFlinger::composite()` 收集多个 output 后调用 `mCompositionEngine->present(refreshArgs)`，未见按 display 并行触发依据。

### 5.3 大小核架构
- 结论：needs-rework
- 维度评分：源码 2/5，原理 4/5，版本 4/5，盲区 3/5，数据 3/5，交叉引用 4/5
- P0：L266-L290 `sugov_get_util()` 代码块标注 android16-6.12 实际实现，但实际源码以 `scx_cpuperf_target(sg_cpu->cpu)` 为 `util` 初值，`!scx_switched_all()` 时叠加 `cpu_util_cfs_boost()`，`effective_cpu_util()` 后再 `util = max(util, boost)`。正文把 boost 提前加到 CFS util，且省略 sched_ext 主路径。
- P2：L154 骁龙 8 Elite Performance 核 `capacity≈837` / 18% 级差缺设备 `cpu_capacity`、kernel EM/DT 或厂商公开资料锚点。

### 14.7 ProfilingManager
- 结论：pass-tech-review
- 自动晋升：finalized（Task6 已通过，Task2B 已修正 `ProfilingResult` 包名，queue 无 pending）
- 维度评分：源码/API 4/5，原理 4/5，版本 4/5，盲区 4/5，数据 4/5，交叉引用 4/5
- 问题：无 P0/P1/P2。复核点：显式请求类来自 `androidx.core.os`，结果类来自平台 `android.os.ProfilingResult`；`BufferFillPolicy` 仅 `DISCARD` / `RING_BUFFER`；API 36 / 36.1 / 37 trigger 分层与官方口径一致。

### 25.6 APK 体积分析与瘦身
- 结论：needs-rework
- 维度评分：源码 4/5，原理 3/5，版本 4/5，盲区 4/5，数据 4/5，交叉引用 3/5
- P1（既有 pending，未重复写入）：L190-L193 低频 `.so` 下发方案仍写“动态特性模块、Play Asset Delivery，或国内渠道自研按需下载方案”。官方 PAD asset pack 只包含 textures / shaders / sounds 等资产，不包含 executable code；native 库应走 Dynamic Feature Module / Play Feature Delivery，或自研下载并明确加载、审核、回滚边界。
- 复核：此前 P0 `Raw File Size` 口径已修正为 zipped/raw file size，未再发现阻塞项。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：1 处（25.6 既有 pending 条目，未重复写入）
- P2 建议改进：3 处（新增 2，既有 1）
- P3 锦上添花：0 处
- 总体技术评分：3.8/5

## 闭环动作
- 写入 queue.json（P95）：2 条新增（2.6、5.3）；P85：复用既有 25.6 pending 条目并追加本日志路径
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：2 条新增（2.6 多显示 composite 证据、5.3 Oryon capacity 数据）；13.12 P2 沿用既有条目
- 更新章节 frontmatter：13.12、14.7 `pass-tech-review` 并自动晋升 finalized；2.6、5.3、25.6 `needs-rework` + `task2b_pending`
- 自动晋升 finalized：13.12 Perfetto Profile 导入与 Flamegraph 分析；14.7 ProfilingManager
- external-review：根目录无活跃文件，无需归档
