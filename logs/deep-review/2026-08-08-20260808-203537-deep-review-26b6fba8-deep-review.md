# AIW Deep Review Log · 2026-08-08

- run_id: `20260808-203537-deep-review-26b6fba8`
- mode: `deep-review`
- target: `src/part3-tools/ch15-methodology/09-observability-closed-loop.md`
- chapter: `15.9 从采集到治理的反馈回路`
- baseline: Android 17 / API 37, AOSP `android-17.0.0_r1`, kernel `android17-6.18-2026-06_r6`
- materials: none from pre-run; used chapter's existing official/source anchors only

## 审计范围

1. 复核上游质量标记：核验正文是否仍存在未闭合核验标记、占位提示或薄来源段落。
2. 抽查源码锚点：`androidx.metrics:metrics-performance:1.0.0` sources 中 `FrameData.copy()`、`JankStats.createAndTrack()`、`JankStatsApi31Impl` 使用 `FrameMetrics.DEADLINE` 计算 overrun。
3. 审查版本边界：章节保持 Android 8/API 26 到 Android 17/API 37；未提升平台上限。
4. 审查结构与可读性：治理八阶段下的 JankStats、ApplicationExitInfo、版本边界属于采集阶段的技术边界说明，应降为四级标题，避免破坏“八阶段”的章节结构。

## 证据摘录

本轮通过脚本下载并打开 AndroidX `metrics-performance-1.0.0-sources.jar`，确认：

- `androidx/metrics/performance/FrameData.kt`：`FrameData` 为 open class，包含 `open fun copy(): FrameData`。
- `androidx/metrics/performance/JankStats.kt`：包含 `fun createAndTrack(window: Window, frameListener: OnFrameListener): JankStats`，并说明 `FrameData` 对象会复用，listener 不应长期依赖原对象。
- `androidx/metrics/performance/JankStatsApi31Impl.kt`：`val overrun = totalDuration - frameMetrics.getMetric(FrameMetrics.DEADLINE)`。

## Findings

### P0

- 无。

### P1

- 无。

### P2

1. **结构层级不清**：`帧信号`、`进程退出`、`版本边界` 使用三级标题，与“治理回路的八个阶段”同级，容易误读为额外阶段。
   - 处理：降级为四级标题，保留为 `1. 采集` 下的技术边界说明。
2. **质量标记触发词残留**：frontmatter `rework_notes` 保留了上游质量标记的字面值，正文工单模板包含“待验证假设”字样，可能继续触发机器标记。
   - 处理：改为“上游质量标记”“核验标记”“仍需确认的假设”，不改变技术含义。

## 实际补丁

- 更新 `last_verified` 到 `2026-08-08`，补充 `last_verified_against` 的 AndroidX 源码抽查点。
- 新增 `last_deep_review_at`、`last_deep_review_run_id`、`last_deep_review_log`、`deep_review_result`、`deep_review_notes`。
- 保留 `status: ready-for-review`、`pipeline_stage: ready-for-review`、`task6_state: pending-review`、`task9_state: pending-review`、`confidence: medium-high`。
- 正文只做标题层级和风险措辞修复；未新增章节，未修改 `src/SUMMARY.md`。

## 结论

章节已有充分正文与可核验官方/source anchors。本轮 deep-review 未发现 P0/P1；完成 P2 结构与机器标记残留修复后，章节继续回流 `ready-for-review`，等待 Task6/Task9 复审。
