# 深度技术 Review 闲时抽检 · 2026-07-03 07:26

## 审计目标
- 章节：19.04 btrace / RheaTrace
- 文件：src/part3-tools/ch19-apm/04-btrace.md
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 证据
- bytedance/btrace v3.0.0 `README.zh-CN.md`：示例命令为 `java -jar rhea-trace-shell.jar -a ${your_package_name} -t 10 -o output.pb -r sched`，参数表写 `-mode perfetto/simple`，并声明 8.1 及以上默认 perfetto、8.0 及以上可用、Android 15+ Java object allocation monitoring 受限、32 位设备或应用无法采集 Trace。
- bytedance/btrace v3.0.0/v3.1.0 `Main.java`：`getSystemLevelCapture()` 自动模式读取 `ro.build.version.sdk`，`>= 28` 才选择 `PerfettoCapture`，否则选择 `LiteCapture`。
- bytedance/btrace v3.0.0 `Arguments.java`：`-r` 只设置 restart；`sched` 作为 category 透传，且无 category 时 `putSchedIfNoCategory()` 会默认补 `sched`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] L106/L153 — 正文沿用 README 的“Android 8.1+ 默认 perfetto”表述，但 v3.0.0/v3.1.0 shell 源码自动选择阈值是 API 28。已补充源码边界，并把参数表改为 Android 9/API 28+ 默认走 `PerfettoCapture`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] Android 8.1 边界 — Android 8.1 设备不应按“默认 perfetto”下结论，需按设备显式验证 `-mode perfetto` 或回退 simple。已在正文写入。

## 闭环动作
- AUTO-FIX：1 处（2 个局部位置）
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-07-03`、`last_task9_audit: 2026-07-03`

---

# 深度技术 Review 闲时抽检 · 2026-07-03 07:39

## 审计目标
- 章节：13.5 专题解读
- 文件：src/part3-tools/ch13-perfetto/05-topic-analysis.md
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 证据
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/android/binder.sql` 仍包含 Binder 事务 client/server 字段实现；Perfetto stdlib docs 暴露 `android_binder_txns` 与 `android_sync_binder_thread_state_by_txn`。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/linux/block_io.sql` 包含 `linux_active_block_io_operations_by_device`、`linux_device_major_id()`、`linux_device_minor_id()`，确认 `linux.block_io` 相关 SQL 在 Android 17 基线仍可用。
- AOSP `android-17.0.0_r1`：`ProcessStatsConfig` 仍包含 `proc_stats_poll_ms` 与 `scan_smaps_rollup`；`HeapprofdConfig` 仍包含 `process_cmdline`、`sampling_interval_bytes` 与 Android 12 引入的 `heaps` 字段。
- Perfetto stdlib/docs：`actual_frame_timeline_slice`、`android_is_app_jank_type()`、`android_is_sf_jank_type()`、Binder 与 block I/O stdlib 入口仍在公开文档中。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] Frontmatter L10 — `last_verified_against` 仍停在 AOSP `android-16.0.0_r1`，与当前 Android 17 基线不一致。已改为 `android-17.0.0_r1`，并更新 `last_verified`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] L266-L274/L282/L300/L484 — 表格和正文使用 `Android 10-16`、`Android 10+`、`Android 11+`、`Android 12+`、`Android 14+` 等未封顶或停在 Android 16 的范围。已统一收束到 Android 17：Binder 与 Block I/O 为 Android 10-17，FrameTimeline 为 Android 12-17 / 14-17，Java heap 相关入口为 Android 11-17 / 12-17。

## 闭环动作
- AUTO-FIX：1 处（版本边界与验证基线局部修正）
- 写入 queue.json：0 处；已关闭既有 Task3 freshness 队列项 1 条
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-07-03`、`last_task9_audit: 2026-07-03`
