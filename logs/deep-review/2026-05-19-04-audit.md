# 闲时深度技术抽检 · 2026-05-19 04:29

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 193
- 模式：闲时抽检（Task 9）

## 抽检目标
- 章节：4.1 Android 内存模型全景
- 文件：src/part1-fundamentals/ch04-memory/01-memory-overview.md
- 状态：finalized
- 选择理由：无 pending 章节；该章节为 `status: finalized` / `task9_result: pass-tech-review`，且既往 Task9 有 P2 建议，适合作为源码引用与版本差异抽检目标。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮不做完整 6 维审查，不处理文风、格式、段落流畅度。

## 核验依据
- AOSP `android-16.0.0_r1`：
  - `frameworks/base/services/core/java/com/android/server/am/ProcessList.java` 存在，包含 `oom_score_adj` / lmkd command 相关逻辑。
  - `frameworks/base/core/java/android/content/ComponentCallbacks2.java` 存在，`TRIM_MEMORY_*` 多个等级标注 “Apps are not notified of this level since API level 34”。
  - `system/core/libprocessgroup/profiles/task_profiles.json` 存在，`MemLimit` 映射 v1 `memory.limit_in_bytes` / v2 `memory.max`，`MemSoftLimit` 映射 v1 `memory.soft_limit_in_bytes` / v2 `memory.low`。
  - `system/memory/lmkd/lmkd.cpp` 存在，包含 PSI / vmpressure、`oom_score_adj`、`kill_heaviest_task` 与 `/proc/<pid>/statm` RSS 读取路径。
  - `frameworks/base/core/java/android/app/ActivityManager.java` 存在，包含 `getMemoryClass()` / `getLargeMemoryClass()`。
- Android Developers：
  - Android 17 behavior changes 确认 app memory limits、`ApplicationExitInfo.REASON_OTHER`、`getDescription()` 包含 `MemoryLimiter:AnonSwap`、可用 `TRIGGER_TYPE_ANOMALY` 获取命中限额时的 heap dump。
  - Android 17 features 确认 `ProfilingTrigger.TRIGGER_TYPE_ANOMALY` 可覆盖 excessive memory usage / OS-defined memory limits。
  - `ComponentCallbacks2` API reference 确认相关 `TRIM_MEMORY_*` 等级自 API 34 起不再通知，API 35 deprecated。
  - Managing Bitmap Memory 确认 API 11-25 bitmap pixel data 在 Dalvik heap，API 26+ 在 native heap。
  - 16 KB page size 资料确认 Android 15+ 16 KB page 支持与额外内存开销方向。
- Perfetto：
  - Memory counters 文档确认 `linux.process_stats` per-process counter 包含 `mem.virt` / `mem.rss` / `mem.rss.anon` / `mem.rss.file` / `mem.swap`，未把 PSS 作为该数据源输出。
  - Java heap profiler / OOME case study 确认 `android.java_hprof` 与 `android.java_hprof.oom` 数据源名称。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4/5
- P0：0
- P1：0
- P2：1

具体问题：
1. [P2] 行 379 — “一个只使用 1 KB 的对象也要占用整个 16 KB 页面”容易被读成“每个小对象独占一个页”。16 KB page 的关键是 resident / mapping / allocator span 的页粒度变粗；通用 malloc / ART heap 中多个小对象仍可被 allocator 打包到同一页。建议后续改成：只触碰少量字节的已提交页面也会按 16 KB 粒度进入 RSS/PSS；对单个对象要结合 allocator span、page packing 与对象生命周期判断。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0：0
- P1：0
- P2：1（与既有 P2 重合，不重复入队）

具体问题：
1. [P2] frontmatter 行 8-10 — 正文已经覆盖 Android 17 app memory limits / `MemoryLimiter:AnonSwap` / `TRIGGER_TYPE_ANOMALY`，且本轮核验通过；但 `last_verified: 2026-04-21` 与 `last_verified_against: AOSP android-16.0.0_r1 ...` 没有记录 Android 17 behavior changes / features 文档。此前 `intake/suggestions.md` 已有同类 Task9 P2，本轮只在抽检日志记录，不重复写 suggestions。

## 闭环动作
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0（避免重复既有 4.1 P2）
- 更新章节 frontmatter：`last_task9_audit: '2026-05-19'`
- 章节状态：保持 `finalized` / `ready-to-publish`

## 结论
- 本轮闲时抽检未发现 P0/P1。
- 源码路径、AOSP 类/文件、Perfetto 数据源名称、Android 17 app memory limits 描述均可通过现有一手资料支撑。
- 建议后续 Task2B/人工修订时处理 2 个 P2：补 Android 17 验证基线到 frontmatter；软化 16 KB page 对“小对象占页”的表述。
