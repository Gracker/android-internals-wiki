# 深度技术 Review 抽检 · 2026-06-08 02:20

## 抽检目标
- 章节：23.2 Bitmap 与图片内存优化
- 文件：src/part5-app/ch23-memory-practice/02-bitmap-optimization.md
- 状态：finalized
- 抽检模式：闲时抽检，仅覆盖维度 1（源码引用）与维度 3（版本差异）

## 版本边界
- AIW 本轮最高范围：Android 17 / API 37。
- 本节 frontmatter 覆盖 Android 10 (API 29) - Android 16 (API 36)，未写入 Android 18 / API 38+ 内容。
- `android-17.0.0_r1` Gitiles 路径本轮请求返回 404；因本节声明范围止于 Android 16，源码复核使用 `android-16.0.0_r1`，属于 AIW 允许范围内的低版本锚点。

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 复核证据：
  1. `frameworks/base/graphics/java/android/graphics/Bitmap.java` 在 `android-16.0.0_r1` 存在；`mNativePtr`、`getAllocationByteCount()`、`NativeAllocationRegistry.registerNativeAllocation(this, mNativePtr)` 与正文 L104、L160 的描述一致。
  2. `frameworks/base/graphics/java/android/graphics/BitmapFactory.java` 在 `android-16.0.0_r1` 存在；`Options.inBitmap` 注释说明 Android 4.4+ 复用条件为 decoded `byteCount <= allocationByteCount`，`validate()` 会拒绝 `Config.HARDWARE` 的 `inBitmap`，与正文 L231、L291 一致。
  3. `frameworks/base/graphics/java/android/graphics/BaseCanvas.java` 在 `android-16.0.0_r1` 存在；软件渲染路径遇到硬件 Bitmap 会抛 `Software rendering doesn't support hardware bitmaps`，与正文 L302 一致。
  4. `frameworks/base/graphics/java/android/graphics/ImageDecoder.java` 在 `android-16.0.0_r1` 存在；默认输出通常 immutable 且常见为 `Config.HARDWARE`，与正文 L300 一致。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 复核证据：
  1. Android Developers `manage-memory` 文档仍列出 Bitmap 像素内存版本边界：Android 2.3.3 及以下 native memory；Android 3.0-7.1 Dalvik heap；Android 8.0+ native heap。正文 L158 与该边界一致。
  2. 本节 `applicable_versions` 止于 Android 16 / API 36，正文没有使用 Android 18 / API 38+ 资料或结论。
  3. Android 17 标签不可用时未改写正文结论，也未把 main/master 资料作为 Android 17 结论。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体抽检评分：5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节内容修改：0 处
- frontmatter：补记 `last_task9_audit: 2026-06-08`
