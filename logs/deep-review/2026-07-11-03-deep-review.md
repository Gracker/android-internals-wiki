# 深度技术 Review · 2026-07-11 03:30

## Review 目标
- 章节：8.18 Binder Trace 驱动的 Activity 冷启动性能分析
- 文件：src/part2-performance/ch08-responsiveness/18-binder-trace-cold-start-analysis.md
- 状态：ready-for-review

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：5
- 具体问题：
  1. [P0] Perfetto 标准库源码路径写成 `src/trace_processor/sql/stdlib/android/*.sql`，android-17.0.0_r1 下实际为 `src/trace_processor/perfetto_sql/stdlib/android/*.sql`。
  2. [P0] `frameworks/base/core/java/com/android/server/pm/PackageManagerService.java` 与 `.../wm/WindowManagerService.java` 在 android-17.0.0_r1 下不存在，实际在 `frameworks/base/services/core/java/com/android/server/...`。
  3. [P0] 内核锚点写为 `android17-6.12`，该分支不可验证；Android 17 可验证分支为 `android17-6.18`。
  4. [P0] `binder_lock` / `binder_locked` / `binder_unlock` 不在 android17-6.18 `drivers/android/binder_trace.h` 的 tracepoints 中。
  5. [P0] `TF_UPDATE_TXN_FROZEN` 不是 android17-6.18 binder uapi 标志。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P0] `dispatch_dur` 被当作 `android_binder_txns` 原生字段使用，但 Android 17 表定义没有该列，导致三段归因 SQL 不可执行。
  2. [P1] `server_dur = 0` 被等价为 frozen reply，证据链不足；应结合 binder_return 的 `BR_FROZEN_REPLY` / `BR_TRANSACTION_PENDING_FROZEN` 与 thread_state 共同确认。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] 扩展段落把 `is_merged`、`frozen_reply`、`parent_txn_id` 写成 Android 17 `android.binder` 模块字段，但 android-17.0.0_r1 `binder.sql` 无这些字段。
  2. [P1] Frozen Reply 版本表包含 Android 15/16/17 行为结论，但多处未能由 Android 17 源码锚点支撑，需降级为待验证或移除超出基准内容。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：本轮主要是源码/API 锚点错误，不新增 research-gaps。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] 冷启动 Binder 事务数量级与真实案例数据仍以经验估算为主；当前已有阻断性 P0，数据补强可在修完源码与 SQL 后处理。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：章节间引用目标基本存在，但 §1.30/§1.18 的 Android 17 队列与 freezer 结论需要在 Task2B 修复时重新核对。

## 统计
- P0 事实错误：6 处
- P1 重要缺失：2 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.0/5

## 闭环动作
- 写入 queue.json（P95）：1 条（合并 7 个 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（阻断性 P0 优先进入 queue）
- 仅日志记录：1 处
- 更新 frontmatter：`task9_result: needs-rework`、`task9_state: reviewed`、`task2b_state: pending`、`pipeline_stage: task2b_pending`

## 证据摘录
- `external/perfetto` android-17.0.0_r1：`src/trace_processor/perfetto_sql/stdlib/android/binder.sql` 存在，`src/trace_processor/sql/stdlib/android/binder.sql` 404。
- `android_binder_txns` 表字段（android-17.0.0_r1）：`client_ts`、`client_dur`、`server_ts`、`server_dur`、`is_sync`、`is_main_thread` 等；无 `dispatch_dur`、`is_merged`、`frozen_reply`、`parent_txn_id`。
- `android_binder_metrics_by_process` 字段：`process_name`、`pid`、`slice_name`、`event_count`；无 `transaction_count`。
- `frameworks/base` android-17.0.0_r1：PKMS/WMS 位于 `services/core/java/com/android/server/...`，不是 `core/java/com/android/server/...`。
- `kernel/common`：存在 `android17-6.18` 分支；`android17-6.12` 未列出。`drivers/android/binder_trace.h` 无 `binder_lock`/`binder_locked`/`binder_unlock` tracepoints。
