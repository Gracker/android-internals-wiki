# AIW deep-review log · 2026-08-07

- run_id: `20260807-203506-deep-review-44dab4a0`
- mode: `deep-review`
- target: `src/part1-fundamentals/ch04-memory/4.18-android-17-memorylimiter-深度解析.md`
- baseline: `AOSP android-17.0.0_r1 / android17-6.18-2026-06_r6`
- quality_flags: `android18-boundary-risk`, `pending-verification-marker`, `thin-source-marking`

## 审计结论

本轮对章节正文已有的 MemoryLimiter 源码链路做边界复核。正文并非占位稿，已有 Java/JNI/XSD/kernel-doc 源码索引，允许做小范围事实补丁。未发现 P0/P1 级别错误；保留 `ready-for-review`、`deep-reviewed`、`pending-final-audit` 和 `high` 置信度。

## P0 / P1 / P2

- P0: 0
- P1: 0
- P2: 1（已修复）
  - 启用边界缺口：原文列出了 feature flag、system UID、vendor XML 和 MemTotal 选择，但没有提醒 `initializeExemptList()` 会把 `config_defaultOnDeviceSandboxedInferenceService` 解析出的包名加入 `mExemptList`，容易让读者仅凭 proc state 映射推断所有同状态进程都会被写入限制。

## 实际补丁

1. 更新 frontmatter：
   - `last_verified: 2026-08-07`
   - `last_deep_review_at: 2026-08-07T20:36:02+08:00`
   - `last_deep_review_run_id: 20260807-203506-deep-review-44dab4a0`
2. 在“启用条件与配置选择”小节补充默认 on-device sandboxed inference service 包名进入 `mExemptList` 的排查边界。
3. 更新“本轮深度审计补记”，说明本轮新增的豁免边界，并明确未引入 Android 18/API 38 或 mainline 结论。
4. 源码索引新增 `initializeExemptList()` 锚点。

## 源码依据

- `MemoryLimiter.java@android-17.0.0_r1`：1291 行；`initializeExemptList()` 读取 `config_defaultOnDeviceSandboxedInferenceService`，解析包名并加入 `mExemptList`。
- `MemoryLimiter.cpp@android-17.0.0_r1`：1276 行；继续确认 JNI 写入 `memory.high` / `memory.swap.max`、red-zone 轮询和 30 秒周期边界。
- `ProcessRecord.java@android-17.0.0_r1`：1936 行；继续确认 `setPid()`、`setUidRecord()`、`onProcStateUpdated()` 生命周期锚点。
- `cgroup-v2.rst@android17-6.18-2026-06_r6`：继续作为 `memory.high` 与 `memory.swap.max` 内核语义依据。

## 风险控制

- 未新增章节；未修改 `src/SUMMARY.md`。
- 未使用 Android 18/API 38 或 mainline 结论。
- 未创建新的 review finding：本轮只有已落地的小范围 P2 补充，且既有 4.18 finding 已处于 fixed 状态。
