# Task 9 闲时抽检 · 2026-06-12 10:20

## 抽检目标
- 章节：19.13 androidx.tracing（Tracing SDK）
- 文件：src/part3-tools/ch19-apm/13-tracing-sdk.md
- 状态：finalized
- 选择原因：无 task9 pending 候选；Task2B backlog=0；本节 last_task9_at=2026-05-19T18:28:22+08:00，距本轮超过 21 天；历史 Task9 多次命中 AndroidX Tracing 版本/API 边界问题。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 不做完整 6 维度审查。

## 一手资料与版本边界
- Android 17 / API 37 以内；未使用 Android 18 / API 38+ 资料。
- `android-17.0.0_r1` 锚点当前未能从 android.googlesource 解析到对象；本轮使用已发布的 `android-16.0.0_r1` 源码锚点，不使用 main/master 作为正文结论。
- AOSP `frameworks/base/core/java/android/os/Trace.java`（android-16.0.0_r1）：`Trace.beginSection()` 走 `TRACE_TAG_APP`、`nativeTraceBegin()`，`forceEnableAppTracing()` 对应 `setAppTracingAllowed()` Android S 起 no-op。
- AOSP `system/core/libcutils/trace-dev.cpp`（android-16.0.0_r1）：常规设备打开 `/sys/kernel/tracing/trace_marker`，写入 `B|pid|name` / `E|pid` / `S|pid|name|cookie` / `F|pid|name|cookie`。
- AOSP `system/core/libcutils/trace-container.cpp`（android-16.0.0_r1）：容器场景可 fallback 到 reserved `trace` socket，而非直接写 ftrace trace_marker。
- Google Maven metadata（2026-06-12 复核）：`androidx.tracing:tracing` / `tracing-ktx` latest 为 `2.0.0-alpha08`；`tracing-perfetto` latest 为 `1.0.1`。
- AndroidX release notes：1.3.0 将 `tracing-ktx` 代码合并入 `tracing`；2.0.0-alpha08 仍是 alpha 线，包含 `traceCoroutine` / exception instant 等变更。
- AndroidX `tracing-android:1.3.0` sources：`Trace.beginSection(label)` 调用 `android.os.Trace.beginSection(label.truncatedTraceSectionLabel())`；API 29+ async 走平台公开 API，API 18-28 fallback 反射 `android.os.Trace.asyncTraceBegin/asyncTraceEnd`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- P0/P1：0
- 观察项：
  1. [P2/仅日志] L172、L257 的 `trace_marker` 口径与常规设备源码一致，但 Android 16 `trace-container.cpp` 还存在容器 trace socket fallback。正文面向 App 性能排查时不构成事实错误；后续如要更严谨，可把“写入 trace_marker”收窄为“常规设备路径写入 trace_marker”。
  2. [通过] L174 的 AndroidX 1.3.0 调用链与 source jar 一致：`androidx.tracing.Trace.beginSection()` -> `android.os.Trace.beginSection()` -> `nativeTraceBegin()`；未再出现此前的“API31+ 跳过 JNI”错误。
  3. [通过] L240 的 API 18-28 async fallback 方法名正确：`asyncTraceBegin(long, String, int)` / `asyncTraceEnd(long, String, int)`。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- 观察项：
  1. [P2/仅日志] L232/L242 对 Android 12+ custom trace 默认开启的结论正确，但官方 AndroidX 1.3.0 source 同时列出 `<profileable enabled=false/>` 与 `<profileable shell=false/>` 两个限制项；正文只显式写了 `enabled=false`。建议后续把 `shell=false` 一并补上。
  2. [通过] L250-L253 的版本线仍成立：1.3.0 是 stable 线；2.0.0-alpha08 是最新 alpha；`tracing-perfetto` 独立停在 1.0.1，正文没有把 `tracing-perfetto` 混成 2.0 alpha。
  3. [通过] L251/L402 对 `traceCoroutine` 的 alpha 边界描述与 2.0.0-alpha08 source/release notes 一致，且未把 alpha 能力推荐为生产默认方案。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：2（仅日志观察，闲时抽检不写 suggestions）
- P3 锦上添花：0
- 总体技术评分：4.5/5

## 闭环动作
- queue.json：0
- research-gaps.md：0
- suggestions.md：0
- 章节正文：未改动
- frontmatter：已标注 `last_task9_audit: "2026-06-12"`
- external-review：未消费活跃文件，无需归档
