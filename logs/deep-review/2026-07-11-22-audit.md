# 深度技术 Review · 闲时抽检 2026-07-11 22:29

## Review 目标
- 章节：8.18 Binder Trace 驱动的 Activity 冷启动性能分析
- 文件：src/part2-performance/ch08-responsiveness/18-binder-trace-cold-start-analysis.md
- 状态：finalized
- 模式：闲时抽检（无 task9_state=pending 候选；该章节 review 时有 P2 建议且缺少 last_task9_audit）

## 闲时抽检范围（聚焦维度）
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.8/5
- 问题数：0 个 P0/P1；1 个 P3 记录项
- 核心验证点：
  1. AOSP android-17.0.0_r1 `external/perfetto/src/trace_processor/perfetto_sql/stdlib/android/binder.sql` 定义 `android_binder_txns`，字段包含 `client_ts`、`client_dur`、`server_ts`、`server_dur`、`is_sync`、`method_name`、`client_oom_score`、`server_oom_score`，与正文 SQL 口径一致。
  2. AOSP android-17.0.0_r1 `binder_breakdown.sql` 的 `_binder_reason()` 基于 descendant slice + `thread_state` 做延迟归因，正文对 monitor_contention / binder / io 等边界描述成立。
  3. AOSP android-17.0.0_r1 `binder_tracker.cc` 对 `BR_FROZEN_REPLY` / `BR_TRANSACTION_PENDING_FROZEN` 有状态处理，并通过 flow 关联 client `binder_txn_id` 与 server `binder_reply_id`，正文 frozen reply 多信号校验方向成立。
  4. kernel/common `android17-6.18` `drivers/android/binder_trace.h` 包含 `binder_transaction`、`binder_transaction_received`、`binder_transaction_alloc_buf`、`binder_command`、`binder_return` tracepoint；`binder_lock` / `binder_locked` / `binder_unlock` 未命中，正文旧 tracepoint 边界成立。
  5. kernel/common `android17-6.18` `drivers/android/binder.c` 中 `binder_proc_transaction()` 对同步 frozen 目标返回 `BR_FROZEN_REPLY`，oneway frozen 目标可返回 `BR_TRANSACTION_PENDING_FROZEN`；`TF_UPDATE_TXN` 只作用于 pending async 更新路径，正文未混入同步 batch 结论。
  6. AOSP android-17.0.0_r1 frameworks/base WMS、ContentProvider、SystemServer Binder thread pool 与 frameworks/native libbinder 路径抽查通过。
- P3 记录项：正文 §1 中 `IPCThreadState.cpp L854` 行号与 android-17.0.0_r1 tag 不一致；`IPCThreadState::transact()` 实际在 L921，`waitForResponse()` 在 L1153。路径、方法名和行为描述正确，不构成 P0/P1。

### 维度 3：版本差异覆盖
- 评分：4.8/5
- 问题数：0
- 具体检查：
  1. 正文主线锚定 AOSP `android-17.0.0_r1` 与 kernel/common `android17-6.18`，未引用 Android 18 / API 38+ 作为结论。
  2. `android.binder` 标准库字段边界写清楚：Android 17 没有 `is_merged`、`frozen_reply`、`parent_txn_id` 字段。
  3. frozen reply、oneway pending、`TF_UPDATE_TXN` 的适用场景分开描述，未把 Android 17 pending async 更新误写成同步事务合并。
  4. Android 14-17 的适用范围主要依赖分析侧 `trace_processor` 是否包含 `android.binder` 模块，正文已经标明该版本依赖。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处（本轮新增；既有 Frozen Reply SQL 覆盖边界 P2 已在 2026-07-11 13:28 deep-review 写入 suggestions）
- P3 锦上添花：1 处
- 总体技术评分：4.8/5

## 闭环动作
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter `last_task9_audit`：1 处

## 抽检结论
该章节在源码引用与版本边界上无 P0/P1 问题，继续维持 finalized / ready-to-publish。P3 行号漂移仅记录在本日志中，不影响正文的技术结论。
