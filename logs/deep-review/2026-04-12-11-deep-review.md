# 深度技术 Review · 2026-04-12 11:20

## Review 目标
- 章节：5.9 ADPF 自适应性能框架
- 文件：src/part1-fundamentals/ch05-cpu-power/09-adpf.md
- 状态：ready-for-review
- applicable_versions: Android 12 (API 31) - Android 17 (API 37)

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：6
- 具体问题：
  1. [P0] 行 61-75 —— `createHintSession()` 示例把 `PerformanceHintManager.Session` 写成 `HintSession`，把 `int[] tids` 写成 `Collections.singletonList(...)`，代码无法编译，`updateTargetWorkDuration(16_666_000L)` 注释还把 60 fps / 120 fps 写反。
  2. [P0] 行 88-94 —— system_server 中实际实现是 `HintManagerService`，不是 `PerformanceHintService`。
  3. [P0] 行 106-112 —— 把 `SystemHealthManager` 说成 Android 16 新引入的类，实际新增的是 headroom API，不是这个类本身。
  4. [P0] 行 120-162 —— 热状态与 headroom API 错归到 `android.os.ThermalManager`，公开 SDK 实际在 `PowerManager`。
  5. [P0] 行 142-159 —— `getThermalHeadroom()` 返回范围被写死为 0.0-1.0，还建议每帧查询；官方文档明确允许 >1.0，且高频调用可能返回 NaN。
  6. [P0] 行 206-221 —— `GameStateManager` / `GameState.create()` / `isPerformanceCritical` 这一套公开 API 并不存在，真实入口是 `GameManager#setGameState(GameState)`。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] 行 86-90 / 164-168 —— 缺少 `PerformanceHintManager.Session -> HintManagerService -> vendor power hint HAL/AIDL` 与 `PowerManager -> IThermalService -> Thermal HAL` 的控制面，App / system_server / OEM 三层边界不清楚。
  2. [P1] 行 108-110 / 140-149 —— 正文建议“每帧开始时查询 Headroom”，但 `SystemHealthManager` source 明确说 headroom 调用可能 >1 ms，不建议放在 critical thread。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] 行 263-269 —— 版本演进表把 `GameStateManager` 写成 Android 13 新类，同时把 `AThermalManager` 标成 API 31，Android 12 / 13 / 16 边界混乱。
  2. [P1] 行 106-112 / 行 268-269 —— Android 16 Headroom API 的“新增 API”与“已有类”边界没有分开，容易把版本线记错。

### 维度 4：知识盲区
- 评分：2/5
- 盲区数：2
- 具体问题：
  1. [P1] 缺少 ADPF 控制面分层：App API、system_server 服务、vendor HAL / AIDL 没串起来。
  2. [P1] 缺少可复现的 ADPF 观测路径：Perfetto / AGI / Frame Timeline / PowerManager 热状态各自该看什么没有交代。

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：3
- 具体问题：
  1. [P1] 行 236-259 —— `power.hint_session` / `power.thermal` 没有 trace config、截图或官方锚点。
  2. [P2] 行 301-307 —— Unity / Unreal 集成版本没有包版本矩阵与官方文档。
  3. [P2] 行 311-317 —— PerfLock / Perfservice / Tensor 定制策略没有公开资料支撑。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] 行 295 —— `§14.7 Perfetto 高级分析` 与 SUMMARY.md 不一致，14.7 实际是 ProfilingManager，Perfetto 的高级用法是 13.7。

## 统计
- P0 事实错误：6 处
- P1 重要缺失：3 处
- P2 建议改进：3 处
- P3 锦上添花：0 处
- 总体技术评分：1.8/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：2 处
- 写入 suggestions.md：3 处
- 仅日志记录：0 处

## 结论
本章离可发布还差一轮硬修。最硬的坑不是文风，而是 API 面写错了：`PowerManager` 被写成 `ThermalManager`，`GameStateManager` / `GameState.create()` 不存在，HintSession 示例代码也不能直接编译。再往下还有一个实操坑，正文建议“每帧查询 Headroom”，这和官方对 `getThermalHeadroom()` / `getCpuHeadroom()` 的调用约束正面冲突。建议先按 P95 回炉，把 API 名、版本边界、调用频率和 Perfetto 证据链修正，再谈发布流。
