# 深度技术 Review 闲时抽检 · 2026-07-13 00:26

## Audit 目标
- 章节：13.11 Perfetto 时间跨度关联：SPAN_JOIN 与窗口函数
- 文件：src/part3-tools/ch13-perfetto/11-perfetto-span-join-window-functions.md
- 状态：finalized
- 模式：闲时抽检
- 范围：维度 1（源码引用）+ 维度 3（版本差异）
- 选择原因：Preflight 显示 CANDIDATES=0、TASK2B_BACKLOG=0，进入闲时抽检；本轮 finalized/pass-tech-review 池中按最新复检时间排序，13.11 是最早一批，且历史曾有 frame 边界裁剪、GC pause window 合并、multi-machine CPU 分区口径等 SQL 语义风险，适合复查源码引用和版本边界。

## 审查依据
- Android 版本边界：本轮只使用 `android-17.0.0_r1` 的 AOSP `external/perfetto` 源码和 Perfetto 官方工具文档作对照，未采用 Android 18 / API 38+ 内容。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/intrinsics/operators/span_join_operator.cc`、`.h` 存在；`Column()` 中 `dur` 按 `min(raw_ts_end) - max(ts)` 计算，`FindOverlappingSpan()` / `FindEarliestFinishQuery()` 负责按分区和时间推进。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/prelude/after_eof/cpu_scheduling.sql` 中 `sched` 公开 `cpu` 和 `ucpu`，`cpu` 是 `ucpu AS cpu` 的单机 trace 便利列；multi-machine trace 需要按 `ucpu` 关联 `cpu` 表。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/linux/cpu/frequency.sql` 使用 `counter_leading_intervals`、`cpu_counter_track` 和 `cpu` 表生成 `cpu_frequency_counters`，正文 counter + `LEAD()` 的手写建模与标准库方向一致。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/sched/thread_executing_span_with_slice.sql` 仍使用 `_slice_flattened`、`thread_state` 与 `SPAN_LEFT_JOIN` 组合，正文关于 flattened slice 后再 join 的边界成立。

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- P0/P1：0
- 说明：frontmatter 中列出的 `span_join_operator.cc`、`span_join_operator.h`、`thread_executing_span_with_slice.sql` 均能在 Android 17 tag 定位；正文对 `SPAN_JOIN` 输出交集、整数分区、同分区不可重叠、`SPAN_LEFT_JOIN`/`SPAN_OUTER_JOIN` 的技术描述与源码和官方文档一致。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- 说明：章节适用范围仍限定到 Android 17/API 37，没有 Android 18/API 38+ 结论。`sched.cpu` 写法在 Android 17 Perfetto prelude 中存在，且正文场景是常规 Android 单机 trace；此前记录的 multi-machine/`ucpu` 差异仍属于低风险边界，不升级为 P0/P1。

## 低风险观察
1. [P3] 若后续把 SQL 模板扩展到 multi-machine trace，建议统一用 `ucpu` 或显式关联 `cpu` 表，避免 `sched.cpu` 与 `cpu_counter_track.cpu` 在远端机器场景下产生歧义。本轮只记录，不写入 suggestions。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：4.75/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter：`last_task9_audit=2026-07-13`、`last_task9_audit_result=pass-idle-audit`
- 章节正文：未改动
