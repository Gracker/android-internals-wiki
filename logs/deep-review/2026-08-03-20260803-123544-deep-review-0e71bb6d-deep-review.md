# AIW Deep Review Log · 2026-08-03 · 20260803-123544-deep-review-0e71bb6d

- lane: deep-review
- target: `src/part1-fundamentals/ch01-architecture/34-oomadjuster-process-priority-performance.md`
- baseline: AOSP `android-17.0.0_r1` + kernel `android17-6.18-2026-06_r6`
- materials: none from pre-run; review used existing article sources and direct AOSP tag spot checks.

## 审计结论

章节正文已经是充分正文，不属于占位稿。核心版本边界（Android 17 / API 37、PSC 包、ProcStateController 仍在 flag/TODO 演进阶段、lmkd 与 OomAdjuster 分层）可继续保留 `ready-for-review` 与 `confidence: high`。

本轮做了小范围 source-bound 修复：`LMK_PROCS_PRIO` 批量路径虽然每条记录布局含 5 个字段，但 `ProcessList.batchSetOomAdj()` 在 API 37 中固定写入 process type app 与 `for_lmkd_only=0`；已在正文 §6.2 补充，避免读者误以为 batch 路径也可携带 zram writeback 专用的 `for_lmkd_only` 例外。

## 源码抽查证据

- `frameworks/base/services/core/java/com/android/server/am/ProcessList.java (android-17.0.0_r1)`：`MAX_PROCS_PRIO_PACKET_SIZE = 3`，`MAX_OOM_ADJ_BATCH_LENGTH = ((4 * 5) * 3) + 4`；`batchSetOomAdj()` 每进程写 pid、uid、amt、process type、`for_lmkd_only`，其中 process type 注释为 default app，`for_lmkd_only is not supported in batch updates` 并写 0。
- `frameworks/base/services/core/java/com/android/server/am/psc/OomAdjusterImpl.java (android-17.0.0_r1)`：`computeOomAdjLSP(ProcessRecordInternal app, ProcessRecordInternal topApp, boolean doingAll, long now)` 签名存在；short FGS 使用 `PERCEPTIBLE_MEDIUM_APP_ADJ + 1`，recently-top short FGS 使用 `PERCEPTIBLE_RECENT_FOREGROUND_APP_ADJ + 1`，expedited job 相关路径使用 `+ 2`。
- `frameworks/base/services/core/java/com/android/server/am/psc/OomAdjuster.java (android-17.0.0_r1)`：`getFreezePolicy()` 以 CPU capability 判断 freezer 资格；Perfetto SDK tracing 写 `process_state_changed` instant。
- `frameworks/base/core/java/android/app/ActivityManager.java (android-17.0.0_r1)`：未发现 `PROCESS_STATE_FOREGROUND_SERVICE_LOCATION`，procState 顺序包含 `BOUND_TOP`、`FOREGROUND_SERVICE`、`CACHED_EMPTY` 等章节所列状态。

## Findings

### P0

- 无。

### P1

- 已修复：§6.2 对 `LMK_PROCS_PRIO` batch payload 的 `for_lmkd_only` 语义不够精确。补丁已补充 batch 不支持该例外、字段固定写 0。

### P2

- 无新增。原 `thin-source-marking` 风险通过 frontmatter sources 与本轮源码抽查可接受；未新增 Android 18/API38+ 结论。

## 实际补丁

- 更新 frontmatter：`last_verified=2026-08-03`、`last_deep_review_at=2026-08-03`、`last_deep_review_run_id=20260803-123544-deep-review-0e71bb6d`，新增 `task9_state: deep-reviewed`。
- 正文 §6.2：补充 `LMK_PROCS_PRIO` batch 对 process type / `for_lmkd_only` 的限制。

## 验证

已执行：

```bash
python3 scripts/check-metadata.py --files src/part1-fundamentals/ch01-architecture/34-oomadjuster-process-priority-performance.md
# ✅ 1 个文件通过，0 失败，0 警告

python3 scripts/check-summary-links.py
# ✅ passed；保留既有 2 个 duplicate SUMMARY warning，非本轮引入

git diff --check
# ✅ no output
```
