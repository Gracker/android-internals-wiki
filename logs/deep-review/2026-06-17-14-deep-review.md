# 深度技术 Review · 2026-06-17 14:34

## Review 目标
- 章节：14.23 StrictMode 性能检查与开发期诊断
- 文件：src/part3-tools/ch14-other-tools/23-strictmode-performance-diagnostics.md
- 状态：ready-for-review
- 结论：AUTO-FIX，回到 Task6 复审

- 章节：15.2 如何区分系统问题和 App 问题
- 文件：src/part3-tools/ch15-methodology/02-system-vs-app.md
- 状态：ready-for-review
- 结论：pass-tech-review；Task6 仍为 revisiting，本轮不自动晋升

## 审查结果

### 14.23 StrictMode 性能检查与开发期诊断

#### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：6（均已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] VmPolicy 配置 — `detectExplicitGc()` 被写进 `StrictMode.VmPolicy.Builder` 示例。android-17.0.0_r1 `core/api/current.txt` 与 `StrictMode.java` 均显示该方法属于 `ThreadPolicy.Builder`；已移到 ThreadPolicy 示例，并把 API 口径修为 Android 14+/API 34+。
  2. [P0][AUTO-FIX] 检测项说明 — `detectResourceMismatches()` 示例写成 `TextView.setImageResource()`。AOSP Javadoc 示例是 `TypedArray.getInt()` 读取 String 类型资源；已修正。
  3. [P1][AUTO-FIX] `detectNonSdkApiUsage()` 实现 — 原文写成 `VMRuntime setHiddenApiExemptions()`。android-17.0.0_r1 `setVmPolicy()` 实际注册 `VMRuntime.setNonSdkApiUsageConsumer()` 并关闭 dedupe；已修正。
  4. [P1][AUTO-FIX] `detectActivityLeaks()` 实现 — 原文写成 `performDestroyActivity()` 注册弱引用检查。实际通过 Activity 创建/销毁期望计数、`InstanceTracker` 与 `VMDebug.countInstancesOfClass()` 触发 `InstanceCountViolation`；已修正。
  5. [P1][AUTO-FIX] `detectUnsafeIntentLaunch()` 语义 — 原文写成 `setPackage()` / `setComponent()` 外部组件启动检测。AOSP Javadoc 指向外部来源 Intent 被继续发送导致未保护组件/URI 授权风险；已修正。
  6. [P1][AUTO-FIX] AOSP 锚点 — 反哺节仍写 main 分支、android-17 tag 未发布。已用 git ls-remote 确认 `android-17.0.0_r1` 存在，并把锚点固定到该 tag。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：2（均已 AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] Compose 小节把 `LaunchedEffect` 文件 I/O 写成 Recomposer 线程且 ThreadPolicy 不会拦截；Android UI composition 常在主线程，effect 体直接做 I/O 仍会触发 ThreadPolicy。已改为显式切 `Dispatchers.IO`。
  2. [P1][AUTO-FIX] `NETWORK_POLICY_LOG` 被写成应用可见 `SocketException`。AOSP `StrictMode.setVmPolicy()` 只在 LOG 模式记录、REJECT 模式拒绝；已改为按策略区分。

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：2（均已 AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] VmPolicy bit 小节称 main 分支定义 15 个 bit 且 android-17 tag 未公开。已改为 API 37 范围内讨论 bit 9-14，不把超出 AIW 边界的后续兼容项写入正文。
  2. [P2][AUTO-FIX] `ActivityScenario` 自动开启 StrictMode 的说法无 AndroidX Test 源码支撑；已改为测试需显式配置。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：2（均已 AUTO-FIX）
- 具体问题：
  1. [P2][AUTO-FIX] StrictMode 开销给出固定 +0.1-0.5ms，缺测试条件和来源。已改为要求按设备/penalty 路径实测。
  2. [P1][AUTO-FIX] DropBox 24h/8KB 说法与 android-17.0.0_r1 `DropBoxManagerService` 默认 3 天、全局 10MB/20MB 配额不一致。已修正为全局配置口径。

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

### 15.2 如何区分系统问题和 App 问题

#### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0
- 复核点：Perfetto `thread_state` / `sched` 归因、SurfaceFlinger `commit` / `composite` 口径、ANR 官方文档口径。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：章节仍以 Android 16 tag 作为 SurfaceFlinger 源码锚点，未写入超出 AIW 版本边界的内容。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

## 统计
- P0 事实错误：1 处（已 auto-fix，不写 queue）
- P1 重要缺失/错误：8 处（已 auto-fix，不写 queue）
- P2 建议改进：3 处（已 auto-fix，不写 suggestions）
- P3 锦上添花：0 处
- 总体技术评分：14.23 为 4/5（auto-fixed）；15.2 为 4/5（pass-tech-review）

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- 章节 auto-fix：14.23 共 12 处小修，frontmatter 标记 `task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`
- 章节通过：15.2 标记 `task9_result: pass-tech-review`、`task9_state: reviewed`；因 Task6 仍为 revisiting，本轮不自动晋升 finalized

## 证据来源
- AOSP frameworks/base `StrictMode.java` @ `refs/tags/android-17.0.0_r1`
- AOSP frameworks/base `ActivityThread.java` @ `refs/tags/android-17.0.0_r1`
- AOSP frameworks/base `SharedPreferencesImpl.java` @ `refs/tags/android-17.0.0_r1`
- AOSP frameworks/base `DropBoxManagerService.java` @ `refs/tags/android-17.0.0_r1`
- AOSP libcore `BlockGuard.java` @ `refs/tags/android-17.0.0_r1`
- AndroidX Test `ActivityScenario.java` main branch（源码中未出现 StrictMode 配置）
