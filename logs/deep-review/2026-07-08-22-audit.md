# Task 9 闲时抽检 · 2026-07-08 22:25

## 审计目标
- 章节：1.13 MessageQueue 机制与 DeliQueue 无锁优化
- 文件：src/part1-fundamentals/ch01-architecture/13-messagequeue-deliqueue.md
- 状态：finalized / task9_result: pass-tech-review
- 模式：闲时抽检；范围限维度 1（源码引用）与维度 3（版本差异）
- external-review：最近 3 天根目录无活跃文件
- queue：1.13 无 pending 条目
- Android 基线：android-17.0.0_r1；未使用 Android 18 / API 38+ 内容

## 核验依据
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/os/CombinedDeliMessageQueue/MessageQueue.java`：确认 `USE_NEW_MESSAGEQUEUE`、`@EnabledAfter(targetSdkVersion = Build.VERSION_CODES.BAKLAVA)`、`computeUseDeliQueue()`、`Flags.useConcurrentMessageQueueInApps()`、`mStack`、`nextMessage()`、`mSyncBarrier`。
- AOSP android-17.0.0_r1 `MessageStack.java`：确认 `mSyncHeap` / `mAsyncHeap`、`pushMessage()` 的 `weakCompareAndSetRelease`、`heapSweep()`、`drainFreelist()`、`moveMatchingToFreelist()`。
- AOSP android-17.0.0_r1 `MessageHeap.java` 与 `Message.java`：确认最小堆使用 `Message.compareMessages()` 按 `when` + `insertSeq` 排序，`FLAG_REMOVED` / `markRemoved()` 作为 tombstone 标记。
- AOSP android-16.0.0_r1 `CombinedMessageQueue/MessageQueue.java`：确认 legacy 默认，concurrent 只限 core UID / SystemUI 或 flag 强制开启。
- AOSP android-16.0.0_r1 `ConcurrentMessageQueue/MessageQueue.java`：确认 `ConcurrentSkipListSet`、`mPriorityQueue` / `mAsyncPriorityQueue`、barrier 下优先 async queue 的语义。
- AOSP android-15.0.0_r1 `MessageQueue.java` 与 android-16.0.0_r1 `Looper.java`：确认 legacy `synchronized (this)` 链表路径，以及 `mQueue.next()` 与 `dispatchMessage()` 的边界。

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 结论：正文列出的 Android 17 主线源码路径、关键类名、方法/字段名与 android-17.0.0_r1 一致；Android 15/16 参照路径也可定位。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 结论：正文已区分 Android 16 的受控 rollout 与 Android 17 / targetSdk 37 的 app-facing 默认启用边界；未发现 Android 18/API38+ 资料进入正文结论。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：5/5（抽检范围）

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 正文修改：0 处
- frontmatter：更新 `last_task9_audit: 2026-07-08` 与 `last_task9_audit_log: logs/deep-review/2026-07-08-22-audit.md`
