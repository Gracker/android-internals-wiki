# 闲时技术抽检 · 2026-07-07 21:20

## 抽检目标
- 章节：7.6 案例集
- 文件：src/part2-performance/ch07-smoothness/06-case-studies.md
- 状态：finalized / ready-to-publish（auto-fix 后回到 task6_pending）
- 选择说明：本轮 `CANDIDATES=0`、`TASK2B_BACKLOG=0`、`IDLE_AUDIT=336`，进入闲时抽检模式。该节 `task9_result=pass-tech-review`，且 `last_task9_audit=2026-06-14` 已超过 21 天；历史 review 曾有 P2/P3 建议，适合复检源码引用与版本差异。
- 范围：闲时抽检仅复核维度 1（源码引用准确性）和维度 3（版本差异覆盖）。

## 抽检结论

### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：1（P1，已 AUTO-FIX）
- 结论：源码行为与正文机制一致；但章节源码锚点仍写 `android-16.0.0_r1`，不符合 2026-06-27 之后的 Android 17 主线基准。本轮已重锚到 `android-17.0.0_r1`。
- 复核点：
  1. AOSP `android-17.0.0_r1` `frameworks/base/graphics/java/android/graphics/drawable/AnimatedVectorDrawable.java`：`draw(Canvas)` 中 `!canvas.isHardwareAccelerated()` + `VectorDrawableAnimatorRT` + `mPendingAnimationActions` 触发 `fallbackOntoUI()`；`forceAnimationOnUI()` 仍存在。
  2. AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/view/View.java`：`LAYER_TYPE_HARDWARE = 2`，注释仍说明硬件加速开启时可将复杂 View tree 缓存为 texture。
  3. AOSP `android-17.0.0_r1` `frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp` 与 `CompositionEngine/src/Output.cpp`：`SurfaceFlinger::composite()` 调用 `mCompositionEngine->present()`，`Output::present()` 进入 `composeSurfaces()`，CLIENT composition 走 `RenderEngine::drawLayers()`；`OutputLayer::requiresClientComposition()` 仍以 `Composition::CLIENT` 判断。
  4. AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/os/PowerManager.java`：`addThermalStatusListener()`、`OnThermalStatusChangedListener`、`THERMAL_STATUS_MODERATE/SEVERE` 和 `IThermalService` 字段存在，章节未误写成不存在的 `ThermalManager`。

### 维度 3：版本差异覆盖
- 评分：4.8/5
- 问题数：1（P3，已 AUTO-FIX）
- 结论：未发现需要写 queue 的版本错误。Android 17 基准下，AVD、PowerManager thermal API、ComponentCallbacks2、SurfaceFlinger CLIENT 合成链路与正文机制一致。
- 复核点：
  1. `ComponentCallbacks2.java` 在 AOSP `android-17.0.0_r1` 中仍标注 `TRIM_MEMORY_RUNNING_*`、`TRIM_MEMORY_COMPLETE/MODERATE` 自 API 34 起不再通知 App；正文只依赖 `TRIM_MEMORY_UI_HIDDEN`，边界正确。
  2. AOSP cached apps freezer 文档写明 Android 14+ cached 进程进入 cached state 后 10 seconds 冻结；正文原“10-30 秒”口径偏宽，本轮已收紧为主线约 10 秒，并保留厂商配置差异。
  3. 正文的 Android 8-10 / 11-12 / 13+ SurfaceFlinger trace 入口分段未发现反向版本错误；Android 18/API 38+ 内容未引入。

## AUTO-FIX
- 证据来源：AOSP Gitiles `android-17.0.0_r1`（frameworks/base、frameworks/native）与 source.android.com cached apps freezer 文档。
- 改动位置：frontmatter `last_verified` / `last_verified_against` / Task9 状态字段；正文 AVD、View hardware layer、SF、PowerManager 源码锚点；cached apps freezer 冻结时间；OPPO 极光引擎待验证版本锚点。
- 修改摘要：将 `android-16.0.0_r1` 主线源码锚点重锚为 `android-17.0.0_r1`；将 cached app freezer 时间从“10-30 秒”收紧为“主线约 10 秒，厂商可配置”；章节回到 Task6 复审。

## 统计
- P0 事实错误：0
- P1 重要缺失：1（已 AUTO-FIX）
- P2 建议改进：0
- P3 锦上添花：1（已 AUTO-FIX）
- 总体技术评分：4.8/5

## 闭环动作
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- 章节正文改动：1 节
- Frontmatter 标注：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-07-07`、`last_task9_audit: 2026-07-07`

---

# 深度技术 Review 抽检 · 2026-07-07 21:40

## Audit 目标
- 章节：1.22 ART Verifier Quickening 与 dexopt 过滤器性能边界
- 文件：src/part1-fundamentals/ch01-architecture/22-art-verifier-quickening-dexopt-filters.md
- 状态：finalized
- 模式：闲时抽检，仅覆盖维度 1（源码引用）与维度 3（版本差异）
- Android 基准：android-17.0.0_r1；未使用 Android 18 / API 38+ 资料

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 核验证据：
  1. `platform/art` tag `android-17.0.0_r1` 下 `art/libartbase/base/compiler_filter.h` 存在；枚举包含 `kVerify`、`kSpaceProfile`、`kSpace`、`kSpeedProfile`、`kSpeed`、`kEverythingProfile`、`kEverything`，无 `kQuicken`。
  2. `art/libartbase/base/compiler_filter.cc` 在同 tag 下仍解析 obsolete `quicken` 字符串，但映射到 `kVerify` 并提示改用 `verify`；正文“Android 12+ 不再作为当前主线 filter 描述、旧日志需按版本判断”的边界成立。
  3. `art/dex2oat/dex2oat.cc` 在同 tag 下存在 `input_vdex_file_`、DexMetadata archive VDEX fast verification、verifier deps 解析路径；正文 VDEX 复用描述与源码一致。
  4. `art/libartservice/service/README.md` 在同 tag 下存在，确认 ART Service introduced in Android 14、管理 dexopt artifact、查询 compiler filter / compilation reason / up-to-date 状态等描述。
  5. `frameworks/base/services/core/java/com/android/server/pm/PackageManagerShellCommand.java` 在同 tag 下包含 `art` namespace 入口，正文 `cmd package art dump` 命令路径可成立。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 核验证据：
  1. source.android.com Configure ART 文档明确 `quicken` 为 Android 11 or lower；正文 Android 8-11 / 12-13 / 14-17 的 quicken 边界划分成立。
  2. `platform/build` tag `android-17.0.0_r1` 的 `target/product/runtime_libart.mk` 确认默认值包括 `pm.dexopt.first-boot?=verify`、`boot-after-ota?=verify`、`boot-after-mainline-update?=verify`、`bg-dexopt?=speed-profile`、`inactive?=verify`、`cmdline?=verify`、`shared?=speed`；正文列出的系统属性与 Android 17 基准一致。
  3. ART Service README 对 first boot / OTA、mainline update、install、bg-dexopt 的场景描述与正文表格一致：boot/OTA 主 dex 默认 `verify`，mainline update 优先 System UI 和 Launcher，install 有 profile 用 `speed-profile` 否则 `verify`，background dexopt 用 `speed-profile` 且可取消。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：5/5（抽检维度）

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 正文改动：0 处
- Frontmatter：已标注 `last_task9_audit: 2026-07-07`
