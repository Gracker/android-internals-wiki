# 深度技术 Review 闲时抽检 · 2026-06-07 14:20

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 294
- TASK2B_BACKLOG: 0
- 模式：闲时抽检

## Review 目标
- 章节：19.25 耗电与发热监控 (Battery & Thermal)
- 文件：src/part3-tools/ch19-apm/25-battery-thermal-apm.md
- 状态：finalized → task6_pending（AUTO-FIX 后送 Task6 复审）
- 抽检原因：`status: finalized`、`task9_result: pass-tech-review`、`last_task9_audit` 缺失，且上次 Task9 为 2026-05-13，超过 21 天复检优先线。

## 版本边界
- 本轮只覆盖 Android 17 / API 37 及以下。
- 未引用 Android 18 / API 38+ 资料。
- AOSP public refs 中未找到 `android-17.0.0_r1` 的 frameworks/base tag；源码锚点回退到低于边界的 `android-16.0.0_r4`，不据此生成 Android 17 新行为结论。

## external-review
- `logs/external-review/` 根目录近 3 天无本章节活跃文件；本轮未消费 external-review。

## 审查范围
闲时抽检只检查高风险维度：
- 维度 1：源码/API 引用准确性
- 维度 3：版本差异覆盖

## 审查结果

### 维度 1：源码/API 引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][源码/API 错误][AUTO-FIX] §3 exact alarm 表格与后续段落 — 正文把 `setExact()` 一概写成未持有 `SCHEDULE_EXACT_ALARM` 时会抛 `SecurityException`。Android Developers exact-alarm 文档明确说明：使用 `OnAlarmListener` 对象设置 exact alarm（例如 `setExact` 的 listener 重载）时不需要 `SCHEDULE_EXACT_ALARM`；`PendingIntent` 版本的 `setExact()` / `setExactAndAllowWhileIdle()` / `setAlarmClock()` 才需要在未授权时处理 `SecurityException`。已就地修正为区分 `PendingIntent` 版本与 `OnAlarmListener` 版本。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][版本差异][AUTO-FIX] §3 exact alarm 版本表 — 原表述使用 `Android 14+` 和 `target 33+`，存在 AIW 版本边界上的开放式措辞，也没有交代 Android 13 的 `USE_EXACT_ALARM` 与 `SCHEDULE_EXACT_ALARM` 差异。已收敛为 `Android 14-17`、`targetSdk 33+`，并补充 Android 13 的两种权限口径与安装/升级来源字段。

## AUTO-FIX
- 修改文件：src/part3-tools/ch19-apm/25-battery-thermal-apm.md
- 修改位置：§3 “Android 12+ 精确闹钟权限对 APM 归因的影响”
- 修正摘要：
  - `Android 13` 行改为区分 `SCHEDULE_EXACT_ALARM` 与 `USE_EXACT_ALARM`。
  - `Android 14+` 改为 `Android 14-17`，避免越过 AIW Android 17/API 37 上限。
  - 对 `setExact()` 增加 `PendingIntent` 与 `OnAlarmListener` 重载差异，避免 API 行为错误。
- frontmatter 更新：
  - `task9_result: auto-fixed`
  - `task9_state: reviewed`
  - `task2b_state: fixed`
  - `task6_state: revisiting`
  - `pipeline_stage: task6_pending`
  - `last_task9_audit: 2026-06-07`
  - `last_task9_autofix_at: 2026-06-07`

## 一手资料与锚点
- Android Developers exact alarm 变更文档：`https://developer.android.com/about/versions/14/changes/schedule-exact-alarms`。核对点：`SCHEDULE_EXACT_ALARM` 默认拒绝、`setExact()` / `setExactAndAllowWhileIdle()` / `setAlarmClock()` 未授权抛 `SecurityException`、`OnAlarmListener` 例外。
- Android Developers alarm scheduling 文档：`https://developer.android.com/develop/background-work/services/alarms`。核对点：Android 12+ exact alarm 权限、Android 13-17 `USE_EXACT_ALARM` / `SCHEDULE_EXACT_ALARM` 差异、`canScheduleExactAlarms()`。
- AOSP `android-16.0.0_r4` PowerManager：`https://android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r4/core/java/android/os/PowerManager.java`。核对点：`WakeLock.mToken`、`acquireWakeLock()` / `releaseWakeLock()`、Thermal status 常量、`getCurrentThermalStatus()`、`addThermalStatusListener()`。
- AOSP `android-16.0.0_r4` PowerManagerService：`https://android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r4/services/core/java/com/android/server/power/PowerManagerService.java`。核对点：`mWakeLocks`、`acquireWakeLockInternal()` / `releaseWakeLockInternal()`、BinderService 入口。
- AOSP Thermal mitigation 文档：`https://source.android.com/docs/core/power/thermal-mitigation`。核对点：Thermal API callback/polling、状态码 `THERMAL_STATUS_NONE` 到 `THERMAL_STATUS_SHUTDOWN`。

## 统计
- P0 事实错误：1 处（已 auto-fix）
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5（auto-fix 后）

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 直接修改章节：1 个
- 送 Task6 复审：1 个
- external-review 归档：无
