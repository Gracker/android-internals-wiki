# 深度技术 Review 闲时抽检 · 2026-06-19 04:38

## Review 目标
- 章节：13.15 BufferQueue 阻塞的 Perfetto 识别
- 文件：src/part3-tools/ch13-perfetto/15-bufferqueue-blocking-perfetto.md
- 状态：finalized
- 模式：闲时抽检

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 复核：Perfetto FrameTimeline 官方文档仍标注 Android 12(S)+，`actual_frame_timeline_slice` 示例包含 `surface_frame_token`、`display_frame_token`、`jank_type`、`on_time_finish`、`present_type`、`layer_name`，`Buffer Stuffing` 样例与正文 SQL 兼容口径一致。
- 复核：AOSP `android-14.0.0_r1` / `android-15.0.0_r1` 的 `BufferQueueProducer.cpp` 未命中 `BUFFER_RELEASE_CHANNEL`，仍走 `mDequeueCondition.wait()` / `wait_for()` 与 `notify_all()` 路径；正文没有把 release channel 写成 Android 14/15 通用机制。
- 复核：AOSP `android-16.0.0_r1` / `android-17.0.0_r1` 的 `BufferQueueProducer.cpp` 存在 `waitForBufferRelease()` 与 `notifyBufferReleased()` 调用；`BufferQueueCore::notifyBufferReleased()` 在 Android 16/17 仍落到 `mDequeueCondition.notify_all()`。正文使用“待验证差异 / 不直接写成用户可感知结论”的保守口径，不构成事实错误。
- 复核：`BufferQueueConsumer::releaseBuffer()` 在 Android 14/16/17 均会释放 slot 并唤醒 producer 等待路径；`BufferQueueCore` 维护 `mFreeSlots`、`mFreeBuffers`、`mActiveBuffers`、`mQueue` 的描述成立。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 复核：章节 `applicable_versions` 为 Android 12(API 31)-Android 16(API 36)，正文未写入 Android 18/API 38+ 内容。
- 复核：Android 17 / API 37 已有 AOSP `android-17.0.0_r1` 可用于后续增强，但当前章节没有依赖 Android 17 才成立的正文结论；不需要写入 queue。
- P3 记录：后续常规维护可把 Android 17 r1 的 `waitForBufferRelease()` / `notifyBufferReleased()` 复核结果补进“Android 16+/main BUFFER_RELEASE_CHANNEL 待验证”段落，把“待验证”收束为 Android 16/17 已验证边界。本轮不改正文。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处（仅日志记录）
- 总体技术评分：4.3/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 内容 auto-fix：0 处
- Frontmatter：标注 `last_task9_audit: 2026-06-19`
- Android 版本边界：未使用 Android 18/API 38+；未使用 main/master 作为正文结论。

## 证据锚点
- Perfetto FrameTimeline docs：`https://perfetto.dev/docs/data-sources/frametimeline`
- AOSP `android-14.0.0_r1` / `android-15.0.0_r1` / `android-16.0.0_r1` / `android-17.0.0_r1`：`frameworks/native/libs/gui/BufferQueueProducer.cpp`
- AOSP `android-14.0.0_r1` / `android-16.0.0_r1` / `android-17.0.0_r1`：`frameworks/native/libs/gui/BufferQueueConsumer.cpp`、`BufferQueueCore.cpp`
