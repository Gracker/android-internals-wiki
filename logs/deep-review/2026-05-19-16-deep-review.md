# 深度技术 Review · 2026-05-19 16:30

## Review 目标
- 章节：5.15 SensorService 与传感器批处理功耗模型
- 文件：src/part1-fundamentals/ch05-cpu-power/15-sensorservice-batching-power.md
- 状态：ready-for-review → finalized / ready-to-publish

## 选择说明
- Preflight: CANDIDATES=5、IDLE_AUDIT=195。
- 按去重规则，本轮只审 5.15：13.5、1.12、14.8 已在 2026-05-19 今日审过；19.13 于 2026-05-18 已审且未满 3 天。
- logs/external-review 根目录无活跃文件；本轮未消费 external-review。

## 审查结果：5.15 SensorService 与传感器批处理功耗模型

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0（P3 细节建议 1）
- 说明：`SensorManager.registerListener()` 的 `maxReportLatencyUs` / FIFO 语义、`SystemSensorManager.registerListenerImpl()` → `SensorEventQueue.addSensor()` → native enable 路径、`SensorService::enable()` 先 `batch()` 后 `activate()`、continuous sensor 多连接时首轮 `flush()`、`SensorDevice::dump()` 的 active-count / sampling_period / batching_period / selected 输出，均与 AOSP main 对齐。
- P3：`SensorDevice::BatchParams::merge()` 不是简单取两个客户端的 `mTBatch` 最小值，而是 `min(current.mTBatch, max(other.mTBatch, other.mTSample))`；随后 `mTBatch <= mTSample` 会归一为 0。正文“批量延迟也取更紧的请求”方向正确，但后续扩写 `dumpsys sensorservice` 解读时可补这条源码细节，避免把 selected batching period = 0 误读成某个客户端显式传 0。

### 维度 2：原理链完整性
- 评分：5/5
- 问题数：0
- 说明：采样成本、FIFO / hub 缓存、AP 唤醒与 App 回调成本分层清楚；`samplingPeriodUs` 与 `maxReportLatencyUs` 的因果边界完整，没有把低频采样和批量交付混成同一优化手段。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：Android 4.4+ batching、wake-up / non-wake-up suspend 语义与当前 AOSP main 路径基本稳定；Sensor Direct Channel 已单独标注 API 26+ 且 `[待验证]`，HAL 2.0 说明没有外推到所有旧版本。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：OEM FIFO 深度、vendor hub 策略、定位/BLE/后台任务协同归因均已作为扩展或待补充边界标出，不构成本轮 P1 盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：正文没有保留未经支撑的量化收益；排查流程要求对齐 `dumpsys sensorservice`、Perfetto、Battery Historian / batterystats 和 App timestamp，证据入口可执行。

### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0
- 说明：related_chapters 中 5.6、11.2、25.5、14.11 均能在 src 中找到；正文与 25.5、14.11 的边界描述一致。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：4.3/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：1 处 P3
- 自动晋升 finalized：1 处（5.15）

## 一手核对锚点
- AOSP main `frameworks/base/core/java/android/hardware/SensorManager.java`：`maxReportLatencyUs`、`maxFifoEventCount()`、wake-up sensor interrupt / AP wake-up 注释。
- AOSP main `frameworks/base/core/java/android/hardware/SystemSensorManager.java`：`registerListenerImpl()` 与 `SensorEventQueue.addSensor()` / `enableSensor()` 回退连续模式。
- AOSP main `frameworks/native/services/sensorservice/SensorService.cpp`：`SensorService::enable()` 调用 `batch()`、continuous sensor 多连接时 `flush()`、再 `activate()`。
- AOSP main `frameworks/native/services/sensorservice/SensorDevice.cpp` / `.h`：`dump()` 输出 selected 参数；`Info::selectBatchParams()` 与 `BatchParams::merge()` 合并逻辑。
- Android Source sensors 文档：batching 定义、FIFO / sensor hub、wake-up / non-wake-up suspend 行为、Sensors HAL 2 `batch()` / `flush()` / direct channel 语义。
