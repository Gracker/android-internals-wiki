# Task 9 闲时抽检 · 2026-06-08 00:20

## 抽检目标
- 章节：21.7 多进程启动优化
- 文件：src/part5-app/ch21-startup/07-multiprocess-startup.md
- 状态：finalized
- 模式：闲时抽检，只检查维度 1（源码引用）和维度 3（版本差异）
- 选择原因：`last_task9_at` 为 2026-05-14，超过 21 天；上次 Task9 曾留下 `MODE_MULTI_PROCESS` P2 建议，已由 Task2B 修正。

## 版本边界
- 本轮仅使用 Android 16 / API 36 及以下资料复核正文结论，未使用 main/master 作为正文依据。
- 正文 `applicable_versions` 为 Android 10 (API 29) - Android 16 (API 36)，未写入 Android 17 / API 37 结论。
- 未发现 Android 18 / API 38+、`targetSdk` 38+ 或无法证明属于 Android 17 / API 37 及以下的内容。

## 维度 1：源码引用准确性
- 评分：5/5
- P0/P1：0
- 复核结论：通过。

### 复核证据
1. `frameworks/base/core/java/android/app/ActivityThread.java`（AOSP `android16-release`）存在 `handleBindApplication()` 与 `installContentProviders()`；`handleBindApplication()` 中的顺序为 `makeApplicationInner()` → `installContentProviders(app, data.providers)` → `mInstrumentation.callApplicationOnCreate(app)`，正文第 85 行关于 provider 与 `Application.onCreate()` 的顺序描述成立。
2. `frameworks/base/core/java/android/os/ZygoteProcess.java`（AOSP `android16-release`）存在 `startViaZygote()`，且进程启动路径会进入 `zygoteSendArgsAndGetResult(openZygoteSocketIfNeeded(...))`，正文第 83 行关于 Zygote 创建入口的描述成立。
3. Android Developers `processes-and-threads` 文档仍说明组件启动且应用未运行时系统会启动新的 Linux 进程，并支持 `android:process`；正文第 79 行描述成立。
4. AndroidX App Startup 文档仍说明 App Startup 通过一个 `InitializationProvider` 管理初始化器、声明依赖，并支持关闭自动初始化后手动懒加载；正文第 110 行描述成立。
5. Android Developers `Context#MODE_MULTI_PROCESS` 标记 API 23 deprecated，`SharedPreferences` 文档说明不支持跨进程使用；正文第 143 行的版本口径成立。

## 维度 3：版本差异覆盖
- 评分：5/5
- P0/P1：0
- 复核结论：通过。

### 复核说明
- 章节范围限定为 Android 10 - Android 16，源码锚点也标注为 `android16-release`；本轮未发现用旧版本行为冒充 Android 16 当前行为的问题。
- `MODE_MULTI_PROCESS` 的 API 23 废弃口径已经写入正文，并覆盖 Android 10 - Android 16 期间的使用边界。
- 后台 service、前台 service、绑定 service 的版本收紧只作为方向性边界并交叉指向 5.8 节，没有在本节展开成未验证细节，未构成版本差异缺口。

## 抽检结论
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0
- 本轮不写入 `metadata/queue.json`、`intake/research-gaps.md`、`intake/suggestions.md`。
- 已在章节 frontmatter 标注 `last_task9_audit: "2026-06-08"`。
