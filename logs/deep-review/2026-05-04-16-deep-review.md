# 深度技术 Review · 2026-05-04 16:20

## 本轮选择说明
- `logs/external-review/` 根目录无活跃文件；本轮未消费 external-review。
- 候选池按 frontmatter 复核：1.5、7.9、9.1 处于 `task9_state: pending`；11.2 仍显示 `task2b_state: pending`，本轮跳过。
- 7.9 虽同日早些时候被 Task9 审过，但 13:54 后 Task2B 已完成修复、15:15 Task6 已重审通过，因此作为回归审计纳入。
- memory_search 不可用（Unknown memory embedding provider: zai）；本轮以项目文件、AOSP/AndroidX 源码复核为准。

---

## Review 目标
- 章节：1.5 线程模型
- 文件：src/part1-fundamentals/ch01-architecture/05-threading-model.md
- 状态：ready-for-review → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L450-L453 Choreographer ThreadLocal — 正文写“只有主线程的 Choreographer 会收到 VSync 信号并驱动渲染”。AOSP android-16.0.0_r1 `Choreographer` 的 `sThreadInstance.initialValue()` 对任何已有 Looper 的线程创建 `Choreographer`，构造函数会创建 `FrameDisplayEventReceiver(looper, VSYNC_SOURCE_APP, ...)`，`scheduleVsyncLocked()` 直接调用 `mDisplayEventReceiver.scheduleVsync()`。因此“只有主线程会收到 VSync”不准确；准确边界是主线程 Choreographer 驱动 ViewRootImpl traversal，其他 Looper 线程也可请求 VSync callback，但不会自动驱动 View 树渲染。 建议：改成本章只讨论主线程 View 渲染路径；补一句“非主 Looper 线程也能持有 Choreographer/接收 VSync callback，但不参与默认 ViewRootImpl traversal”。
  2. [P1] L415-L423 WorkManager 适用场景表 — 表格把“精确定时的重复任务”推荐为 `WorkManager（PeriodicWorkRequest）`。AndroidX WorkManager `PeriodicWorkRequest.kt` 文档明确写明 periodic work “willing to accept inexactness due to battery optimizations and doze mode”，最小周期 15 分钟，且“Work will almost never run at this exact time in the real world”。这会把 exact alarm / calendar alarm 类需求误导到非精确定时工具。 建议：把该行改为“可延迟的周期后台任务 → WorkManager”；精确定时用户可见闹钟/日历类任务另指向 `AlarmManager.setExact*` / `setAlarmClock()`，并标注权限和省电限制。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1] L450-L453 Choreographer ThreadLocal — 正文写“只有主线程的 Choreographer 会收到 VSync 信号并驱动渲染”。AOSP android-16.0.0_r1 `Choreographer` 的 `sThreadInstance.initialValue()` 对任何已有 Looper 的线程创建 `Choreographer`，构造函数会创建 `FrameDisplayEventReceiver(looper, VSYNC_SOURCE_APP, ...)`，`scheduleVsyncLocked()` 直接调用 `mDisplayEventReceiver.scheduleVsync()`。因此“只有主线程会收到 VSync”不准确；准确边界是主线程 Choreographer 驱动 ViewRootImpl traversal，其他 Looper 线程也可请求 VSync callback，但不会自动驱动 View 树渲染。 建议：改成本章只讨论主线程 View 渲染路径；补一句“非主 Looper 线程也能持有 Choreographer/接收 VSync callback，但不参与默认 ViewRootImpl traversal”。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 4：知识盲区
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：2 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：3.6/5

## 闭环动作
- 写入 queue.json：1 条（priority 85）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 仅日志记录：1 处

---

## Review 目标
- 章节：7.9 感知流畅性：步幅波动与无掉帧卡顿
- 文件：src/part2-performance/ch07-smoothness/09-perceived-smoothness.md
- 状态：ready-for-review → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L251-L257 AppJankStats / RelativeFrameTimeHistogram 精度限制 — 正文写 `RelativeFrameTimeHistogram` “API 显式暴露的精度为 1ms”，并据此推导“会漏掉所有小于 12% 的步幅波动”。AOSP android-16.0.0_r1 `RelativeFrameTimeHistogram.java` 实际使用预定义 bucket：-20ms 到 20ms 区间为 2ms 桶，外侧为 5/10/50/100ms 等更粗粒度；`addRelativeFrameTimeMillis(int)` 的输入是整数毫秒，但输出统计不是 1ms 精度。同时该 histogram 统计的是 frame time relative to deadline，不记录 displacement / scrollY / translation，因此不能直接判断“步幅波动”大小。 建议：把这段改成：AppJankStats 只适合 widget 级 frame-deadline 分布；`RelativeFrameTimeHistogram` 是预定义毫秒桶且不含位移信息，不能替代应用侧纳秒时间 + 位移采样。删除“小于 12% 步幅波动会被漏掉”的量化断言，除非补独立实验。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L269-L276 ARR 动态刷新率对步幅波动的放大 — 正文把 ARR 切换点、`AnimationUtils.currentAnimationTimeMillis()` 毫秒取整和肉眼“阶跃感”串成确定因果链，但未给出 AOSP 机制或 Perfetto + 位移采样证据；其中“毫秒取整误差仍以旧周期为基础”也不符合 `Choreographer` 每帧基于当前 `frameTimeNanos / NANOS_PER_MS` 计算动画时钟的行为。 建议：将该段收窄为待验证假设，或补一组 trace：Display mode track / vsyncId / App 侧 displacement sample 三者对齐，证明波动集中在 ARR 切换窗口。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 4：知识盲区
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L269-L276 ARR 动态刷新率对步幅波动的放大 — 正文把 ARR 切换点、`AnimationUtils.currentAnimationTimeMillis()` 毫秒取整和肉眼“阶跃感”串成确定因果链，但未给出 AOSP 机制或 Perfetto + 位移采样证据；其中“毫秒取整误差仍以旧周期为基础”也不符合 `Choreographer` 每帧基于当前 `frameTimeNanos / NANOS_PER_MS` 计算动画时钟的行为。 建议：将该段收窄为待验证假设，或补一组 trace：Display mode track / vsyncId / App 侧 displacement sample 三者对齐，证明波动集中在 ARR 切换窗口。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json：1 条（priority 95）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 仅日志记录：0 处

---

## Review 目标
- 章节：9.1 ANR 设计思想
- 文件：src/part2-performance/ch09-anr/01-anr-design.md
- 状态：ready-for-review → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L212-L214 ContentProvider 发布超时常量 — 正文写 ContentProvider 发布超时常量是 `CONTENT_PROVIDER_PUBLISH_TIMEOUT`，定义在 `ActivityManagerService.java`；紧随其后的验证标注又写 `ActivityManager.java`。AOSP android-14.0.0_r1 中 AMS 只有 `CONTENT_PROVIDER_PUBLISH_TIMEOUT_MSG` 消息编号，真正的 10s 常量是 `frameworks/base/core/java/android/content/ContentResolver.java` 的 `CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS = 10 * 1000 * Build.HW_TIMEOUT_MULTIPLIER`，AMS 在发送 timeout message 时引用该常量。 建议：把源码锚点改为 `ContentResolver.CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS` + `ActivityManagerService.CONTENT_PROVIDER_PUBLISH_TIMEOUT_MSG`；删除 `ActivityManager.java` 标注。
  2. [P0] L361 ApplicationExitInfo.getTraceInputStream() — 正文写 `ApplicationExitInfo.getTraceInputStream()` 是 API 33+。AOSP android-11.0.0_r1 已包含 `ApplicationExitInfo#getTraceInputStream()`，对应公开 API 随 `ApplicationExitInfo` 在 Android 11/API 30 引入。将其写成 API 33+ 会误导 Android 11/12 设备上的 ANR trace 获取方案。 建议：改为 Android 11/API 30+；如要强调 Android 13/14 的访问限制，应单独说明 `/data/anr/` 权限变化与 bugreport/ApplicationExitInfo 两条获取路径。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L361 ApplicationExitInfo.getTraceInputStream() — 正文写 `ApplicationExitInfo.getTraceInputStream()` 是 API 33+。AOSP android-11.0.0_r1 已包含 `ApplicationExitInfo#getTraceInputStream()`，对应公开 API 随 `ApplicationExitInfo` 在 Android 11/API 30 引入。将其写成 API 33+ 会误导 Android 11/12 设备上的 ANR trace 获取方案。 建议：改为 Android 11/API 30+；如要强调 Android 13/14 的访问限制，应单独说明 `/data/anr/` 权限变化与 bugreport/ApplicationExitInfo 两条获取路径。
  2. [P3] L401-L405 Android 10/13 trace 存储演进 — Android 10 与 Android 13 两段都在描述“trace 按文件/进程独立存储、可靠性提升”，信息有重复。此项仅日志提醒，等修复 P0 后可顺手统一版本线。 建议：保留一个可复核版本节点，另一个改成具体差异或删除。

### 维度 4：知识盲区
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 具体问题：未发现新的 P0/P1/P2。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json：1 条（priority 95）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 仅日志记录：1 处

---

## 本轮汇总
- P0：3 处
- P1：3 处
- P2：0 处
- 自动晋升 finalized：无
- 下一候选：11.2 App 耗电优化（需先确认 Task2B 状态是否已从 pending 修正为 fixed）
