# 深度技术 Review · 2026-05-18 10:20（闲时抽检）

## Review 目标
- 章节：2.13 图形缓冲区管理 (BufferQueue)
- 文件：src/part1-fundamentals/ch02-rendering/13-buffer-queue.md
- 抽检模式：无 task9_state=pending 候选；抽检 finalized + pass-tech-review 章节
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异
- 选择原因：last_task9_at=2026-04-26（超过 21 天）、last_task9_audit 缺失；上一轮曾有 P2（Perfetto 证据缺失）。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L240-L248 / L304 — `platform/frameworks/native/libs/gui/BufferQueueCore.h` 路径不存在，真实路径是 `frameworks/native/libs/gui/include/gui/BufferQueueCore.h`；代码片段也与 android-14/main 不一致：`mMutex` 是 `mutable std::mutex`，`mSlots` 是 `BufferQueueDefs::SlotsType`，`mFrameCounter` 是 `uint64_t`。
  2. [P0] L376 / L439 — `queueBuffer - <vsyncId>` trace 名称未在 AOSP android-16.0.0_r1/main 的 `Surface::queueBuffer()` 中出现；该函数仍为 `ATRACE_CALL()`。BLAST 的 `QueuedBuffer - {name}BLAST#{producerId}` 名称也未加 vsyncId。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L170 / L439 — Android 16 `BLASTBufferQueue` 默认 `maxDequeuedBufferCount` 从 2 提升至 3 的说法不成立。android-16.0.0_r1 `BLASTBufferQueue.cpp` L262-L264 与 AOSP main L268-L270 均仍为 `mProducer->setMaxDequeuedBufferCount(2)`；Android 14 L160-L161 也是 2。
  2. [P0] L376 / L439 — vsyncId 不是 Android 16 新增的 `queueBuffer` trace 后缀。AOSP 证据显示 vsyncId 出现在 `BLASTBufferQueue::acquireNextBufferLocked()` 的 trace 中，且 android-14.0.0_r1 已有对应 `ATRACE_FORMAT(... vsyncId ...)`。

### 其他维度
- 原理链、知识盲区、数据支撑、交叉引用：本轮闲时抽检未做完整 6 维审查。

## 统计
- P0 事实错误：3 处（合并为 1 个 queue 条目）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 处，id=`task9-audit-20260518-2.13-bufferqueue-version-trace-source-errors`
- 更新章节 frontmatter：`task9_result=needs-rework`、`pipeline_stage=task2b_pending`、`task2b_state=pending`、`last_task9_audit=2026-05-18`
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处

## 建议修正
1. 删除 Android 16 `maxDequeuedBufferCount` 默认提升至 3 的版本表项；如保留缓冲数量变化，需重新锚定实际 AOSP commit、设备分支或 Surface 类型条件。
2. 删除 `queueBuffer - <vsyncId>` 说法；改为通过 BLAST `acquireNextBufferLocked(... vsyncId ...)` / FrameTimeline 关联 App 提交与 SF 合成。
3. 修正 `BufferQueueCore.h` 路径与代码片段，并在 frontmatter sources 增加 `frameworks/native/libs/gui/include/gui/BufferQueueCore.h`。
