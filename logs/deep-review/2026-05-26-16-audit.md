# Task 9 闲时抽检 · 2026-05-26 16:22

## 抽检目标
- 章节：19.17 Firebase Performance
- 文件：src/part3-tools/ch19-apm/17-firebase-performance.md
- 状态：finalized / ready-to-publish
- 选择说明：Preflight 返回 `CANDIDATES=0`、`IDLE_AUDIT=163`，进入闲时抽检模式。本节 `last_task9_at=2026-05-08T02:30:38+08:00`，缺少 `last_task9_audit`，历史 Task9 曾保留 App start 起点口径 P2，且正文依赖 Firebase 官方在线文档，属于产品/SDK 口径易过期内容。
- 抽检范围：维度 1（源码/官方引用准确性）+ 维度 3（版本差异覆盖）。
- external-review：`logs/external-review/` 根目录无最近 3 天活跃文件，本轮未消费 external-review。

## 复核依据
- Firebase Android get-started：BoM `34.13.0`、`firebase-perf:22.0.5`、perf plugin `2.0.2`；SDK 自动采集 screen/lifecycle，network 需 Gradle plugin；Android 已知限制包含仅支持主进程。
- Firebase troubleshooting：HTTP/S 自动网络覆盖 OkHttp 3.x、URLConnection、Apache HttpClient；近实时可见性、300 events / 10 mins、动态采样、Fragment traces 需 Android SDK 20.1.0+。
- Firebase Trace Android reference：`MAX_ATTRIBUTE_KEY_LENGTH=40`、`MAX_ATTRIBUTE_VALUE_LENGTH=100`、`MAX_TRACE_CUSTOM_ATTRIBUTES=5`、trace/metric name 100 字符。
- Firebase screen traces：slow/frozen frame 指标按 60Hz 假设计算，这是当前 SDK 限制。
- Firebase app lifecycle traces：`_app_start` 是 duration trace，公开文档描述为用户打开应用到应用响应；Android 端具体起点仍建议以 SDK 实现和官方文档共同限定，不把 Android API 24+ 平台进程启动时间直接等同于 Firebase 控制台口径。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：2（P2）
- 具体问题：
  1. [P2][官方能力边界] L137-L150 / L219-L223 — 正文列了启动、屏幕渲染、HTTP/S、自定义 trace 的自动/手工采集边界，但没有写 Firebase Performance Android SDK 当前只支持主进程。多进程 App 的 remote service、WebView 独立进程或独立采集进程容易误判为“Firebase 已自动覆盖”。建议后续精修在接入边界或使用建议中补一句：Android 官方已知限制为 only supports the main process，多进程指标需自采或另设进程内上报。
  2. [P2][官方指标口径] L144-L150 / L198-L209 — Screen rendering 与 JankStats 分工正确，但未写 Firebase slow/frozen frame 指标按 60Hz 假设计算。高刷新率设备上 Firebase 控制台的 slow/frozen 百分比不能直接和 JankStats、FrameMetrics 或 120Hz 帧预算对齐。建议补到分工表或 Screen rendering 边界中。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（P2）
- 具体问题：
  1. [P2][版本/实现口径] L143-L150 — 历史 P2 中的 App start 起点口径仍有残留：正文写 API 24+ 可用 `Process.getStartUptimeMillis()` 作为冷启动起点，但 Firebase `_app_start` 控制台指标不应简化成平台 API 24+ 口径。建议后续把这句改成“Android 平台 API 24+ 提供进程启动时间，Firebase SDK/控制台 `_app_start` 口径仍以 SDK 实现和官方 lifecycle trace 定义为准”，避免读者把平台起点和 Firebase 指标起点混同。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：3 处
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（闲时抽检仅记录 P2，不新增待处理队列）
- 更新 frontmatter：`last_task9_audit=2026-05-26`，`last_task9_audit_log=logs/deep-review/2026-05-26-16-audit.md`
- 章节正文：未修改
