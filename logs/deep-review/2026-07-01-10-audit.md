# 闲时抽检 · 2026-07-01 10:28

## Audit 目标
- 章节：1.10 ContentProvider 性能与优化
- 文件：src/part1-fundamentals/ch01-architecture/10-content-provider.md
- 状态：finalized
- 模式：闲时抽检，只覆盖维度 1（源码引用）与维度 3（版本差异）
- Android 基线：android-17.0.0_r1 / API 37

## 复核证据
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/content/ContentResolver.java`：`CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS` 为 10s，`CONTENT_PROVIDER_READY_TIMEOUT_MILLIS` 为 20s，`CONTENT_PROVIDER_TIMEOUT_MILLIS` 为 3s。
- AOSP android-17.0.0_r1 `frameworks/base/services/core/java/com/android/server/am/ContentProviderHelper.java`：存在 `WAIT_FOR_CONTENT_PROVIDER_TIMEOUT_MSG` / `CONTENT_PROVIDER_PUBLISH_TIMEOUT_MSG` 处理链；源码未出现 `ContentProviderTimeout` / `contentProviderTimeout` 稳定日志关键字。发布超时移除进程 reason 为 `timeout publishing content providers`，已连接 provider 的 system API 检测 reason 为 `ContentProvider not responding`。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/app/ActivityThread.java`：`handleBindApplication()` 中 `installContentProviders(app, data.providers)` 在 `mInstrumentation.callApplicationOnCreate(app)` 之前。
- AOSP android-17.0.0_r1 `frameworks/base/services/core/java/com/android/server/pm/ComputerEngine.java`：`queryContentProviders()` 使用 `sProviderInitOrderSorter` 按 `ProviderInfo.initOrder` 降序排序；`ActivityThread.installContentProviders()` 只按传入列表遍历。
- AOSP android-17.0.0_r1 `frameworks/native/libs/binder/ProcessState.cpp`：`DEFAULT_MAX_BINDER_THREADS = 15`。

## 发现问题
1. [P0][源码/日志关键字][AUTO-FIX] L111、L302、L478-L483 把 `ContentProviderTimeout` / `contentProviderTimeout` 写成 system_server 稳定日志或 trace 事件。android-17.0.0_r1 源码未核到该关键字，实际应拆成 provider publish / ready timeout 与 `appNotRespondingViaProvider()` 两条路径。已直接修正文。
2. [P0][版本锚点][AUTO-FIX] L248、L615 仍引用 `AOSP master / android-16-qpr2` 作为主线验证锚点，不符合 Android 17 基线。已改为 `android-17.0.0_r1`，并同步 frontmatter `last_verified_against` 与 sources。
3. [P2][源码锚点精度][AUTO-FIX] L165 将 initOrder 排序归到 `ActivityThread.installContentProviders()`。android-17.0.0_r1 中排序发生在 `ComputerEngine.queryContentProviders()`，ActivityThread 只遍历已排序列表。已直接修正验证句。

## 闭环动作
- 修改章节：src/part1-fundamentals/ch01-architecture/10-content-provider.md
- 更新 frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_autofix_at=2026-07-01`、`last_task9_audit=2026-07-01 10:28:31`
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- external-review：未发现根目录活跃文件，本轮未消费

## 统计
- P0 事实错误：2 处（已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：1 处（已 auto-fix）
- 结果：auto-fixed，回到 Task6 复审
