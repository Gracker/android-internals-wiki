# 深度技术 Review 闲时抽检 · 2026-05-22 14:20

## Audit 目标
- 章节：6.2 文件系统
- 文件：src/part1-fundamentals/ch06-storage/02-filesystem.md
- 状态：finalized / ready-to-publish → task2b_pending（发现 P0/P1）
- 模式：闲时抽检；聚焦维度 1（源码引用）与维度 3（版本差异）
- 选择原因：无 `task9_state: pending` 候选；本节 `last_task9_at=2026-04-29`，超过 21 天，且历史 Review 有 P2 建议。
- external-review：`logs/external-review/` 根目录无 active 文件，本轮未消费外部 Review。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- P0：1
- P1：0
- P2：0

具体问题：
1. [P0][源码错误] L214-L231 — f2fs 前台 GC 触发机制源码片段已过期。正文展示的 `has_not_enough_free_secs()` 仍是 `free_secs < needed + reserved_sections + __get_secs_required` 口径；当前 android15-6.6 `fs/f2fs/segment.h` 是 `__get_secs_required(sbi, &lower_secs, &upper_secs, &curseg_space)`，再按 `free_secs > upper_secs`、`free_secs <= lower_secs`、`!curseg_space` 三段判断。正文还把 GC_MERGE 路径写成 `wake_up(&fggc_wq)`；实际 `fs/f2fs/segment.c` 是写线程等待 `fggc_wq`，唤醒 `gc_wait_queue_head`。
   - 证据：`kernel/common` android15-6.6 `fs/f2fs/segment.h:636-656`、`fs/f2fs/segment.c:444-452`。
   - 建议：按当前源码重写这段；本轮只更新队列与 frontmatter，不直接改正文内容。

已复核通过的源码锚点：
- `kernel/common/include/uapi/linux/f2fs.h` 中 `F2FS_IOC_START_ATOMIC_WRITE` / `COMMIT` / `ABORT` 存在。
- `kernel/common/include/linux/f2fs_fs.h` 中 `F2FS_BLKSIZE PAGE_SIZE` 存在，16KB page size 下 f2fs block size 约束方向成立。
- `frameworks/base/services/usage/java/com/android/server/usage/StorageStatsService.java` 与 `frameworks/base/core/java/android/app/usage/StorageStatsManager.java` 在 android15/16 分支存在。

### 维度 3：版本差异覆盖
- 评分：3/5
- P0：0
- P1：1
- P2：0

具体问题：
1. [P1][版本差异] L313、L497 — EROFS 压缩算法没有标 Android/kernel 版本边界。正文写 EROFS 支持 LZ4（默认）、Zstandard 和 DEFLATE，并把 Android 16 改进概括为更大压缩单元与去重增强。android15-6.6 `fs/erofs/decompressor.c` 只列 LZ4、可选 LZMA、可选 DEFLATE，没有 ZSTD；android16-6.12 / android-mainline 才出现 `CONFIG_EROFS_FS_ZIP_ZSTD` 与 `Z_EROFS_COMPRESSION_ZSTD`。
   - 证据：android15-6.6 `fs/erofs/decompressor.c:393-410`；android16-6.12 `fs/erofs/decompressor.c:469-476`。
   - 建议：改成版本化表述：Android 13-15 常见只读分区以 LZ4 为主，内核可选 LZMA/DEFLATE；ZSTD 需要 Android 16/6.12+ 或厂商 backport 并启用配置。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.0/5（闲时抽检评分，仅覆盖维度 1/3）

## 闭环动作
- 写入 queue.json（P95）：1 条（合并 1 个 P0 + 1 个 P1 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：`status: ready-for-review`、`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-05-22`
- 自动晋升 finalized：无

## Top 问题
1. [P0] L214-L231 — f2fs 前台 GC 源码片段与 android15-6.6 当前 `segment.h` / `segment.c` 不一致。
2. [P1] L313、L497 — EROFS ZSTD 支持缺 Android 16/6.12+ 与内核配置边界，且漏列 LZMA。
