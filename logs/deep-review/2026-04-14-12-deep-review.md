# 深度技术 Review · 2026-04-14 12:22

## Review 目标
- 章节：13.4 命令行打开超大 Trace
- 文件：src/part3-tools/ch13-perfetto/04-large-traces.md
- 状态：ready-for-review

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：3
- 具体问题：
  1. [P0] L266-L272 — `trace_processor` 批量示例把 trace 从 stdin 喂给 `-q`，但当前 CLI 要求位置参数 `trace_file.pb`，示例不可执行。
  2. [P0] L328-L331 — 查询结果示例使用了不存在的 `--json` 选项。
  3. [P0] L342-L346 — `-D/--debug`、`-W/--wait` 的参数语义写反，当前 CLI 中这两个 long option 都不存在。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L247-L252 / L299-L302 / L399-L425 — 章节从 `.tables` / `.schema` 直接跳到硬编码 slice 名，没有补“先验证事件是否存在”的步骤，方法链在可执行性上断了一段。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 具体问题：无新增 P0/P1/P2。当前问题集中在 CLI 实参和文档路径，不在 Android 版本边界本身。

### 维度 4：知识盲区
- 评分：3/5
- 盲区数：0
- 具体问题：无需要单独写入 research-gaps.md 的新研究主题；本轮主要是工具用法与样例前提缺失。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] L71-L79 — 浏览器内存上限与 Trace 放大倍数给了具体数字，但没有实验条件和来源。

### 维度 6：交叉引用一致性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] frontmatter `sources` / L488 / 参考资料 — `trace-analysis-with-sql` 与 `analysis/traceconv` 链接当前为 404。

## 统计
- P0 事实错误：3 处
- P1 重要缺失：1 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：3.0/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：2 处
- 仅日志记录：0 处

## 核查依据
- 当前 `trace_processor --help`：`-q/--query-file FILE`、`-Q/--query-string QUERY`，并要求位置参数 `trace_file.pb`
- 实测：`printf '' | ./trace_processor -Q 'select 1;'` 会直接打印 usage 并退出，说明 stdin-only 不是本章写法所宣称的合法模式
- 当前 `trace_processor --help`：`-D/--httpd`、`-W/--wide`，不存在 `--json` / `--debug` / `--wait`
- `traceconv --help` 与官方 quickstart 页面：traceconv 文档入口为 `/docs/quickstart/traceconv`
- PerfettoSQL getting started：`EXTRACT_ARG` 官方示例是 `ftrace_event` 上的 `sched_switch` / `prev_comm`

## 结论
13.4 的结构可以保留，但当前有 3 处命令级错误，会让读者复制后直接撞墙。先修 CLI 示例和参数表，再补样例查询的前置验证条件，这一节才算可用。
