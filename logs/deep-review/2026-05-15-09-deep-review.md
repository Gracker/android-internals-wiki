# 深度技术 Review · 2026-05-15 09:27

## 本轮说明
- 按流水线规则，本轮目标池中仅 `13.11` 仍可审：`status=ready-for-review`，`task9_state` 缺省按 pending 处理，`task6_state=pending` 但 Task9 可并行审计。
- `logs/external-review/` 根目录无近 3 天活跃文件，本轮未消费 external-review。
- 13.11 本轮存在 P0/P1，未自动晋升 finalized。

## Review 目标
- 章节：13.11 Perfetto 时间跨度关联：SPAN_JOIN 与窗口函数
- 文件：src/part3-tools/ch13-perfetto/11-perfetto-span-join-window-functions.md
- 状态：ready-for-review → task2b_pending

## 审查结果

### 维度 1：源码引用准确性
- 评分：3.2/5
- 问题数：1
- 具体问题：
  1. [P0][代码逻辑错误] L256-L274 — `sched_with_freq` 与 `frame_span` 用普通 JOIN 判断重叠后直接聚合 `joined.dur`。当 sched/cpufreq 交集段跨过 frame 边界时，frame 外时长会被算进当前帧，导致 `main_cpu_ms`、`avg_freq_khz`、`low_freq_pct` 错误。建议改成 clipped overlap duration，或继续用 `SPAN_JOIN(frame_span PARTITIONED utid, sched_with_freq PARTITIONED utid)` 后聚合。

### 维度 2：原理链完整性
- 评分：3.6/5
- 问题数：1
- 具体问题：
  1. [P1][原理断裂] L321-L348 — GC 示例把全局 GC slice `CROSS JOIN` 到单个主线程 utid，再按 utid 做 `SPAN_JOIN`。这跳过了“同一分区内输入 span 必须互斥”的前置整理步骤；不同 track / ART 阶段可能重叠，Perfetto 明确说明此时会静默产生错误行。

### 维度 3：版本差异覆盖
- 评分：4.0/5
- 问题数：1
- 具体问题：
  1. [P2][版本差异] frontmatter L8-L10、L352-L359 — 章节把适用范围写成 Android 12-17，但 SPAN_JOIN、PerfettoSQL 宏和 stdlib 模块属于 Trace Processor 工具版本能力；Android API 只影响 trace 输入数据。建议补工具版本前提与旧 Trace Processor fallback。

### 维度 4：知识盲区
- 评分：4.0/5
- 盲区数：1
- 具体问题：
  1. [P1] GC pause 与主线程帧重叠分析缺少两层区分：时间重叠、STW 阻塞证据。仅看 GC slice 与 frame overlap 不能证明主线程被 GC pause 阻塞，还需要 thread_state、ART pause 事件和多线程同时停顿证据。

### 维度 5：数据与案例支撑
- 评分：3.8/5
- 问题数：1
- 具体问题：
  1. [P2] 本节的 SQL 示例没有给出一条最小 trace fixture 或 dry-run 验证。考虑到 L256-L274 已出现边界裁剪 bug，Task2B 修复时建议至少补一组跨 frame 边界的人工表或 trace_processor_shell 验证结果。

### 维度 6：交叉引用一致性
- 评分：4.6/5
- 问题数：0
- 说明：13.10、13.6、14.10 引用目标均存在；13.6 当前仍 `ready-for-review`，但不是技术矛盾。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.9/5

## 闭环动作
- 写入 queue.json（P95）：1 条，含 2 个 review_issues
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处
- 仅日志记录：0 处
- 章节 frontmatter：更新 `task9_result=needs-rework`、`task9_state=reviewed`、`task2b_state=pending`、`pipeline_stage=task2b_pending`
- 自动晋升 finalized：无
- external-review：无活跃文件，无需归档

## Top 问题
1. [P0] L256-L274 — per-frame CPU/freq 聚合没有裁剪到 frame 边界，指标会错。
2. [P1] L321-L348 — GC 示例可能把重叠 GC span 塞进同一 `utid` 分区，违反 SPAN_JOIN 输入约束。
3. [P2] frontmatter L8-L10 — 需要区分 Android API 版本与 Trace Processor / PerfettoSQL 工具版本。
