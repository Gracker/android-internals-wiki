# 闲时抽检 · 2026-07-02 17:27

## Review 目标
- 章节：15 Android 性能优化研究方法论
- 文件：src/ch15-methodology.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- Android 基准：android-17.0.0_r1（Android 17 / API 37）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] §3.2 工具版本演进 — 正文把采集命令写成 `traced -b`、`traced --async`。android-17.0.0_r1 `external/perfetto/src/traced/service/service.cc` 中 `traced` 只支持服务端选项，没有 `-b` 或 `--async`；`-b/--buffer`、`-d/--background`、`--detach/--attach` 属于 `external/perfetto/src/perfetto_cmd/perfetto_cmd.cc` 的 `perfetto` CLI。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：2
- 具体问题：
  1. [P1][AUTO-FIX] §3.2 — 同一小节先写 AOSP Perfetto service 默认 `disabled`，后面又写 Android 11+ 默认启用，版本边界自相矛盾。已改成“标准 AOSP 通过 `persist.traced.enable=1` 启动；Pixel/OEM 可覆盖”的边界。
  2. [P2][AUTO-FIX] §3.3 与参考资料 — “Android 14+ 标准 tracing 工具”会误导读者忽略 Android 10-13 的 Perfetto 主入口地位；已改为 Android 10+，并保留 Android 9 启用差异。

## 证据来源
- AOSP android-17.0.0_r1 `external/perfetto/perfetto.rc`：`traced`、`traced_relay`、`traced_probes` 均为 `disabled`；`persist.traced.enable=1` 时启动 `traced` / `traced_probes` 并创建 perfetto traces/configs 目录。
- AOSP android-17.0.0_r1 `external/perfetto/src/perfetto_cmd/perfetto_cmd.cc`：`perfetto` CLI 支持 `-c/-o/-t/-b/-d/-D` 与 `--detach/--attach`。
- AOSP android-17.0.0_r1 `external/perfetto/src/traced/service/service.cc`：`traced` 不支持 `-b` / `--async`。
- AOSP android-9.0.0_r61 / android-10.0.0_r47 / android-11.0.0_r48 `external/perfetto/perfetto.rc`：Android 9 使用 `sys.traced.enable_override`，Android 10+ 使用 DeviceConfig sys_traced 映射到 `persist.traced.enable`。

## 统计
- P0 事实错误：0 处
- P1 重要缺失/版本错误：2 处（已 auto-fix）
- P2 建议改进：1 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：1 个小节 + 2 处参考/版本口径
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_autofix_at=2026-07-02`
