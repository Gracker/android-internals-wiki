# Task 9 闲时抽检 · 2026-07-03 01:24

## 抽检目标
- 章节：12.2 网络性能优化
- 文件：src/part2-performance/ch12-apk-network/02-network-performance.md
- 状态：finalized / pass-tech-review
- 选择原因：Preflight `CANDIDATES=0`、`TASK2B_BACKLOG=0`、`IDLE_AUDIT=336`，进入闲时抽检。当前池子未找到 `last_task9_audit` 缺失或超过 21 天的章节；本节属于最早一批 `last_task9_audit=2026-06-13`，且历史 Review 曾命中过 OkHttp EventListener、ADPF、NetworkCapabilities 版本边界问题，因此选作本轮抽检。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 版本边界：仅使用 `android-17.0.0_r1` 及以下资料；本轮未引用 Android 18 / API 38+ 内容。

## 复核证据
- AOSP `android-17.0.0_r1`：`packages/modules/Connectivity/framework/src/android/net/NetworkCapabilities.java` 中 `getLinkDownstreamBandwidthKbps()` 仍注明为 estimated first-hop transport bandwidth，正文 L404/L449 的“只能粗粒度分档，不是真实吞吐/RTT”成立。
- AOSP `android-17.0.0_r1`：`frameworks/base/core/java/android/os/PerformanceHintManager.java` 中 `Session#setPreferPowerEfficiency(boolean)` 的语义是让 session 内线程 prefer power efficiency over performance，正文 L256-L263 没有写成固定低功耗核心或降频保证，边界成立。
- AOSP `android-17.0.0_r1`：`frameworks/base/telephony/java/android/telephony/SubscriptionInfo.java` 中存在 `getStreamingAppMaxDownlinkKbps()` / `getStreamingAppMaxUplinkKbps()`，但带 `@FlaggedApi(Flags.FLAG_SUBSCRIPTION_PLAN_ENHANCEMENT)`；如正文后续扩展 Android 17 网络能力，应保留 flag / 设备可用性边界。
- AOSP `android-17.0.0_r1`：`frameworks/base/core/res/AndroidManifest.xml` 中存在 `android.permission.ACCESS_LOCAL_NETWORK`，protectionLevel 为 dangerous，且带 `android.permission.flags.access_local_network_permission_enabled` feature flag。
- 当前 `logs/external-review/` 根目录无活跃 external-review 文件，本轮未消费外部 review。
- `metadata/queue.json` 中 section 12.2 无 pending 条目。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.8/5
- 问题数：0（无 P0/P1）
- 说明：OkHttp EventListener / ConnectionPool、Cronet 0-RTT 配置边界、`PerformanceHintManager.Session#setPreferPowerEfficiency(boolean)`、`NetworkCapabilities#getLinkDownstreamBandwidthKbps()` 等前轮高风险点已保持修复状态；本轮未发现路径、类名、方法名或 API 语义错误。

### 维度 3：版本差异覆盖
- 评分：4.4/5
- 问题数：1（P2，仅日志）
- 具体问题：
  1. [P2][仅日志] L23/L25/L484 — frontmatter 适用范围已到 Android 17，但 `last_verified_against` 仍是 Android 16，正文没有展开 Android 17 的 `SubscriptionInfo` streaming bitrate 与 `ACCESS_LOCAL_NETWORK` 差异；参考资料 L484 仍以二手文章为入口。AOSP `android-17.0.0_r1` 已可验证这两组 API/permission，但它们均带 flag/feature flag 边界，不应直接写成所有 Android 17 设备的稳定可用能力。建议后续若扩展本节，改用 AOSP `android-17.0.0_r1` / 官方 API 文档作为主锚点，并明确 flag 与设备可用性。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：1 处（仅日志，不写 suggestions.md）
- P3 锦上添花：0 处
- 总体技术评分：4.6/5（抽检范围评分）

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter：`last_task9_audit=2026-07-03`
- 内容修改：0 处
