# 深度技术 Review 闲时抽检 · 2026-05-17 17:27

## 抽检目标
- 章节：13.3 Perfetto View 解读
- 文件：src/part3-tools/ch13-perfetto/03-perfetto-view.md
- 状态：finalized → task2b_pending（发现 P1 后回炉）
- 模式：闲时抽检；只查维度 1（源码引用准确性）+ 维度 3（版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] Counter Track（约第 238 行）— `Trace.traceCounter()` 在 AOSP android-16.0.0_r1 `frameworks/base/core/java/android/os/Trace.java` 中是 `@hide` / `@SystemApi(client = MODULE_LIBRARIES)`；普通 App 的公开入口是 `Trace.setCounter(String, long)`，native 侧可用 `ATRACE_INT` / `ATRACE_INT64`。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] SurfaceFlinger Track（约第 216-224 行）— 版本边界不准确。AOSP android-12.1.0_r1 仍有 `SurfaceFlinger::onMessageReceived`、`onMessageInvalidate`、`onMessageRefresh`；但 android-13.0.0_r1 `Scheduler/MessageQueue.cpp::Handler::handleMessage` 已直接调用 `compositor.commit()` → `compositor.composite()` → `sample()`，`SurfaceFlinger.cpp` 也已有 `SurfaceFlinger::commit()`。Android 14/16 则继续由 `Scheduler::onFrameSignal` 组织 commit/composite，present 落到 CompositionEngine `Output::present()` / `presentFrameAndReleaseLayers()`。正文把 Android 12-13 归为旧消息模型，会误导 Android 13 trace 入口选择。
  2. [P2] FrameTimeline Track（约第 201 行）— 官方 Perfetto FrameTimeline 文档使用 `AppDeadlineMissed`、`BufferStuffing`、`SurfaceFlingerCpuDeadlineMissed`、`SurfaceFlingerGpuDeadlineMissed`、`DisplayHAL`、`PredictionError` 等枚举名；Dropped frame 是蓝色帧状态，文档标注 “Not related to jank”，不宜与 `Jank Type` 并列。

## 证据锚点
- AOSP android-12.1.0_r1：`frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp` line 1870 `onMessageReceived`，line 1883 `onMessageInvalidate`，line 2062 `onMessageRefresh`。
- AOSP android-13.0.0_r1：`frameworks/native/services/surfaceflinger/Scheduler/MessageQueue.cpp` line 51-55 调用 `commit()` / `composite()` / `sample()`；`SurfaceFlinger.cpp` line 1992 定义 `SurfaceFlinger::commit()`。
- AOSP android-16.0.0_r1：`Scheduler/Scheduler.cpp` line 207 `onFrameSignal()`，line 244 `compositor.commit()`，line 278 `compositor.composite()`；`CompositionEngine/src/Output.cpp` line 440 `Output::present()`，line 1636 `presentFrameAndReleaseLayers()`。
- AOSP android-16.0.0_r1：`frameworks/base/core/java/android/os/Trace.java` line 196 `traceCounter()` 为 hidden/SystemApi；line 520 `setCounter(String,long)` 为公开入口。
- Perfetto docs `docs/data-sources/frametimeline.md`：line 132/140/167/176/183/191 为 jank 枚举名；line 115 标注 Dropped frame “Not related to jank”。

## 统计
- P0 事实错误：0 处
- P1 重要缺失/版本错误：1 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json：1 处（P1，priority 85）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：2 处（P2）
- 更新 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
