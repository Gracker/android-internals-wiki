# 深度技术 Review · 2026-04-14 19:20

## Review 目标
- 章节：13.9 Android Tracing 基础设施：atrace、ftrace 与 Perfetto 数据采集原理
- 文件：src/part3-tools/ch13-perfetto/09-tracing-infrastructure.md
- 状态：ready-for-review

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：5
- 具体问题：
  1. [P0] L138-L145 — `libcutils/Trace.cpp` / `system/core/libcutils/Trace.cpp` 路径写错，当前 system/core 实现文件是 `libcutils/trace-dev.cpp`。
  2. [P0] L150-L152 — boot trace 写成 `persist.sys.atrace.boot` + `/data/misc/trace/`，与现行 atrace/perfetto boottrace 口径不符。
  3. [P0] L164-L209, L236 — `android.os.TracingManager`、Java `DataSource.register(...)`、`LazyThreadSafetyMode` 都对不上公开 API。
  4. [P0] L194-L196 — `ftrace_drain_period_ms` / `ftrace_buffer_size_kb` 字段名错误，且默认 250ms 的说法不成立。
  5. [P0] L292-L309 — kernel tracepoint 注册流程和 Perfetto SQL 查询都写错了。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] L131-L146, L217-L236 — 缺少 `debug.atrace.tags.enableflags`、`atrace_apps`、`atrace_categories(_prefer_sdk)` 这些决定用户态埋点是否可见的配置链。
  2. [P1] L166-L209 — `traced_probes -> traced` 之间的 producer / shared memory / IPC 边界和 ftrace reader 源码入口没有展开。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L150-L152 — 把 legacy atrace boottrace 和 modern Perfetto boottrace 混成一条版本线，属性名、配置位置和输出目录都需要拆开。

### 维度 4：知识盲区
- 评分：2/5
- 盲区数：1
- 具体问题：
  1. [P1] 用户态 tracing 现代路径缺失：正文没有拆开 legacy atrace / trace_marker 与 Perfetto SDK / track_event / custom data source 的边界。

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] L75, L320-L324, L352 — 多组开销量化缺设备、内核版本、事件集和 trace 配置条件。

### 维度 6：交叉引用一致性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P2] L103 — `sched` tracepoint 指向 §13.3，但真正对应线程状态/CPU 调度分析的是 §13.6。
  2. [P2] frontmatter sources — `https://source.android.com/docs/core/debug/atrace` 当前 404，`system/traced/` 也不是本章主要引用目录。

## 统计
- P0 事实错误：5 处
- P1 重要缺失：2 处
- P2 建议改进：3 处
- P3 锦上添花：0 处
- 总体技术评分：2.0/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：1 处
- 写入 suggestions.md：3 处
- 仅日志记录：0 处

## 核查依据
- `system/core/libcutils/trace-dev.cpp`：`trace_marker` 打开路径、`debug.atrace.tags.enableflags`、`atrace_begin_body()` / `atrace_end_body()`
- Perfetto `protos/perfetto/config/ftrace/ftrace_config.proto`：`ftrace_events`、`atrace_categories`、`atrace_apps`、`atrace_categories_prefer_sdk`、`atrace_userspace_only`、`buffer_size_kb`、`drain_period_ms`
- Perfetto `src/traced/probes/ftrace/ftrace_controller.cc`：默认 tick 100ms、`per_cpu/cpu0/trace_pipe_raw`、ftrace reader 入口
- PerfettoSQL docs：通用 ftrace 事件表为 `ftrace_event`，参数通过 `arg_set_id` / `EXTRACT_ARG()` 提取
- AndroidX Trace API reference：公开方法为 `beginSection` / `endSection` / `beginAsyncSection` / `endAsyncSection` / `forceEnableAppTracing()`，无 `LazyThreadSafetyMode`
- Android Developers reference：`android.os/TracingManager` 页面不存在；`android.os/ProfilingManager` 才是公开文档入口之一
- source.android / AOSP boottrace 资料：legacy `persist.debug.atrace.boottrace` 与 modern `persist.debug.perfetto.boottrace` 是两条不同路径

## 结论
13.9 需要回炉，而且优先级应该上调到 P95。最严重的问题不是“描述还不够顺”，而是多处 API、配置项、版本线和 SQL 示例会把读者直接带到跑不通的路径上。
