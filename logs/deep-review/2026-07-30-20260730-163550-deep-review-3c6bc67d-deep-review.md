# AIW Deep Review Log · 2026-07-30

- lane: deep-review
- run_id: `20260730-163550-deep-review-3c6bc67d`
- target: `src/part1-fundamentals/ch01-architecture/1.47-Android-17-Binder优先级继承与内核批处理流水线.md`
- baseline: Android 17 / API 37 / `android-17.0.0_r1` / kernel `android17-6.18-2026-06_r6`
- mode: patch applied (frontmatter 补全 + 交叉引用显示名修正 + sources 补全)

## 审计结论

本章（1.47）此前未被 deep-review lane 审过（logs/deep-review 中无 1.47 记录）。本章当前形态由上游 commit `745f06a42 docs: quarantine fabricated Binder batching claims` 定型：原始 Hermes 提纲混入了不存在的 Binder batching 实现（`mBatchQueue`/`MAX_BATCH_SIZE`/`shouldFlushBatch()` 等代码）和固定阈值（<4KB、16ms、64KB），已通过折叠 `<details>` 区隔离，并在折叠区之后的"源码核查"六组小节逐条纠正。

本轮 deep-review 重点放在：源码/版本边界、来源支撑、交叉引用准确性、frontmatter 完整性、中文可读性、章节结构。

### 源码/版本边界 ✅

- "源码核查"六组结论均锚定 `android-17.0.0_r1` 与 kernel `android17-6.18-2026-06_r6`，与项目基线一致，无 Android 18/API38+ 主线结论。
- 六组纠正（system_server 非守护进程、优先级继承面向同步事务目标线程、无 transaction batch queue、缓冲区非固定 1MB、Perfetto 应使用 `android_binder_txns`、调优无统一阈值）技术内容正确、边界清晰。
- 四个源码入口链接（IPCThreadState.cpp、ProcessState.cpp、binder.c、binder_alloc.c）锚点版本正确。

### 交叉引用 ✅（本轮修正一项 P1）

- §2 引用 `[1.44 Android 17 Binder IPC 优先级继承与内核批处理流水线](1.44-binder-ipc-priority-inheritance.md)`：目标文件存在，显示名与目标 frontmatter title 一致 ✅。
- §4 引用目标 `1.44-android17-binder-sz4m-kernel-buffer-pool.md` 存在，但**原显示名"1.44 Binder 4 MiB 映射上限、异步预算与优先级状态"与目标实际 title"Android 17 Binder 4M 内核缓冲池与优先级去重"不符**（P1 可读性/准确性）→ 本轮已修正为正确显示名。

### Frontmatter（本轮修正）

- quality_flags 标记 `missing-last_verified`、`missing-confidence`：frontmatter 原本缺少这两项 → 本轮已补 `last_verified`/`last_verified_against`/`confidence`。
- `confidence` 设为 **medium**（不提升至 high）：本章正文是"纠正错误提纲"的审计结论，无新增独立来源材料（materials 为空），纠正结论可靠但不应据此升级置信度。
- 补 `pipeline_stage: deep-review`、`last_deep_review_at`、`last_deep_review_run_id`、`related_chapters`。
- 补 `sources:`（四个 AOSP 锚点，与正文"源码入口"一致），消除 check-metadata 可选字段警告。

### 章节结构 ✅

- 折叠区隔离 + 修订说明的分层结构合理，读者默认看到的是正确的"源码核查"结论，错误提纲不污染正文。
- "可引用的结论"与"源码入口"收尾完整。

## P0 / P1 / P2

### P0

- 无。本章无未隔离的危险错误结论：原始错误提纲已被折叠隔离，正文为正确核查结论。

### P1（本轮已修复）

- **已修复**：frontmatter 缺 `last_verified`/`confidence`（对应 quality_flags `missing-last_verified`、`missing-confidence`）。
- **已修复**：§4 交叉引用显示名与目标文件 title 不符，已改为"Android 17 Binder 4M 内核缓冲池与优先级去重"。

### P2（本轮已修复）

- **已修复**：frontmatter 补 deep-review 跟踪字段与 `related_chapters`。
- **已修复**：frontmatter 补 `sources:`，check-metadata 由 1 警告降为 0 警告。
- **记录（不修复）**：本章未出现在 `src/SUMMARY.md` 中；此问题超出本 lane 职责（不修改 SUMMARY.md），交由目录维护 lane 处理。
- **未修改（thin-body 不适用）**：本章是审计/纠正型章节，正文厚度由核查结论决定，不应无来源扩写；未做正文扩写。

## 实际补丁

仅修改既有章节：`src/part1-fundamentals/ch01-architecture/1.47-Android-17-Binder优先级继承与内核批处理流水线.md`

1. frontmatter：新增 `last_verified`、`last_verified_against`、`confidence: medium`、`pipeline_stage`、`last_deep_review_at`、`last_deep_review_run_id`、`related_chapters`、`sources`（4 个 AOSP 锚点）。
2. §4 交叉引用显示名：`1.44 Binder 4 MiB 映射上限、异步预算与优先级状态` → `1.44 Android 17 Binder 4M 内核缓冲池与优先级去重`。

- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。
- 未做无来源正文扩写。

## 证据来源

- 目标章节正文"源码核查"六组结论与四个源码入口链接（已核对版本锚点）。
- 交叉引用目标文件 frontmatter title 实读核对：
  - `1.44-binder-ipc-priority-inheritance.md` title = "Android 17 Binder IPC 优先级继承与内核批处理流水线" ✅
  - `1.44-android17-binder-sz4m-kernel-buffer-pool.md` title = "Android 17 Binder 4M 内核缓冲池与优先级去重" ✅
- 上游 commit `745f06a42 docs: quarantine fabricated Binder batching claims`（隔离错误提纲的历史提交）。

## 校验结果

- `python3 scripts/check-metadata.py --files src/part1-fundamentals/ch01-architecture/1.47-Android-17-Binder优先级继承与内核批处理流水线.md`：✅ 通过（1 文件，0 错误，0 警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（164 链接，2 个 pre-existing 重复警告，均与本章无关；本章不在 SUMMARY.md 内）
- `git diff --check`：✅ 无空白错误
