# 闲时抽检 · 2026-06-11 17:25

## Review 目标
- 章节：4.8 ART 分代垃圾回收与 GC 暂停优化
- 文件：src/part1-fundamentals/ch04-memory/08-art-generational-gc.md
- 状态：finalized（抽检前）
- 模式：Task 9 idle audit；仅检查维度 1（源码引用）与维度 3（版本差异）
- external-review：根目录近 3 天未发现匹配 4.8 的活跃文件。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1 组 P0
- 具体问题：
  1. [P0] L706-L732 “Generational CMC 开关与年轻代参数” — 附录把 Android 16 QPR2 Generational CMC 写成基于 `ConcurrentCopying` 收集器扩展，并把启用机制写成创建 `concurrent_copying_collector_` / `young_concurrent_copying_collector_`。android-16.0.0_r1 的 CMC 路径实际在 `heap.cc` 创建 `MarkCompact` 与 `YoungMarkCompact`，`YoungMarkCompact::RunPhases()` 复用 `MarkCompact::RunPhases()` 并切换 `young_gen_`。CC 的 `young_concurrent_copying_collector_` 是另一条 CC 路径，不能作为 Gen-CMC 的实现说明。
- 证据：
  - android-16.0.0_r1 `art/runtime/gc/heap.cc:839-845`：CMC 路径创建 `MarkCompact` 和 `YoungMarkCompact`。
  - android-16.0.0_r1 `art/runtime/gc/collector/mark_compact.cc:453-479`：`YoungMarkCompact` 构造与 `RunPhases()`。
  - android-16.0.0_r1 `art/runtime/gc/heap.cc:847-874`：`young_concurrent_copying_collector_` 位于 CC 路径。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1 组 P1
- 具体问题：
  1. [P1] L117、L459-L460、L638-L644、L748-L754 — 正文和附录多处把 `YoungMarkCompact` / `use_generational_cmc` / Gen-CMC 三代模型写成 “Android 15+” 或 “Android 16 QPR2+/17” 泛化口径。本轮复核 `android-14.0.0_r1` 与 `android-15.0.0_r1`，`mark_compact.*` 和 `heap.cc` 均未包含 `YoungMarkCompact`、`mid_gen_end_`、`ShouldUseGenerationalGC`、`use_generational_cmc`；`android-16.0.0_r1` 才包含这些锚点。`git ls-remote` 未发现 `android-17.0.0_r1` tag，因此 AOSP main/master 不能作为 Android 17 正文确定结论。
- 证据：
  - `git ls-remote --tags platform/art` 本轮只确认 `android-14.0.0_r1`、`android-15.0.0_r1`、`android-16.0.0_r1`，未返回 `android-17.0.0_r1`。
  - android-14.0.0_r1 / android-15.0.0_r1 `mark_compact.*`、`heap.cc` 未命中 `YoungMarkCompact` / `mid_gen_end_` / `ShouldUseGenerationalGC` / `use_generational_cmc`。
  - android-16.0.0_r1 `mark_compact.h:863-883` 明确 young/mid/old 三代模型和 `mid_gen_end_`。
  - android-16.0.0_r1 `mark_compact.cc:317-324`：`ShouldUseGenerationalGC()` 受 `use_generational_cmc()` 与 `persist.device_config.runtime_native_boot.use_generational_gc` 约束。

## 统计
- P0 事实错误：1 组
- P1 重要缺失：1 组
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5（闲时抽检，仅覆盖源码引用与版本差异）

## 闭环动作
- 写入 queue.json（P95）：1 条（section 4.8，含 1 个 P0 + 1 个 P1）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`status=ready-for-review`，`task9_result=needs-rework`，`task2b_state=pending`，`pipeline_stage=task2b_pending`，`last_task9_audit=2026-06-11`
- auto-fix：0 处。原因：问题集中在多个附录和版本表，需清理/重写超过 Task 9 小范围 auto-fix 上限。
