# Task 9 闲时抽检 · 2026-06-18 20:38

## 抽检目标
- 章节：13.16 Agent 辅助 Perfetto 分析协议
- 文件：src/part3-tools/ch13-perfetto/16-agent-perfetto-analysis-protocol.md
- 模式：闲时抽检
- 范围：维度 1 源码/官方引用准确性；维度 3 版本差异

## 结论
- 结果：AUTO-FIX
- P0：1 处（已直接修复）
- P1：0 处
- P2：0 处
- Android 18/API 38+：未使用，未写入正文结论

## 发现问题
1. [P0][源码/API 语义] `Perfetto SQL 生成守卫` 表格的 `stdlib 优先` 行把 CPU 频率明细写成优先使用 prelude `cpu_freq` 表。当前 Perfetto SQL table docs 中 `cpu_freq` 只有 `id/ucpu/freq`，不是带 `ts/dur` 的频率时间区间表；官方 stdlib 中 CPU 频率时间区间入口是 `linux.cpu.frequency` 模块的 `cpu_frequency_counters`。

## AUTO-FIX
- 改动位置：`Perfetto SQL 生成守卫：先查 schema，再写查询` 表格，`stdlib 优先` 行。
- 改动摘要：将 CPU 频率明细入口改为 `INCLUDE PERFETTO MODULE linux.cpu.frequency` + `cpu_frequency_counters`；保留 `linux.cpu.utilization.*` 用于进程/线程聚合频率；明确 `cpu_freq` 只是 CPU/freq 维度表。
- Frontmatter：`task9_result=auto-fixed`，`task6_state=revisiting`，`pipeline_stage=task6_pending`，`last_task9_audit=2026-06-18`。

## 复核通过项
- `android.startup.startups`、`android.frames.timeline`、`android.frames.per_frame_metrics`、`sched.with_context`、`linux.cpu.utilization.process` 在当前 Perfetto stdlib 文档中存在。
- `thread_state.state` 可为 `Running` 或 `sched.end_state` 字符串；`R/R+` 映射到 Runnable、`S` 映射 Sleeping、`D` 映射 Uninterruptible Sleep 的正文口径可接受。
- FrameTimeline Android 12(S/API 31)+ 边界与官方 Perfetto/Android Studio 文档一致，正文的 Android 10/11 fallback 没有越界到 Android 18/API 38+。

## 证据来源
- Perfetto stdlib docs: https://perfetto.dev/docs/analysis/stdlib-docs (`linux.cpu.frequency` / `cpu_frequency_counters`, `linux.cpu.utilization.process`, `android.frames.*`, `android.startup.startups`, `sched.with_context`)
- Perfetto SQL table docs: https://perfetto.dev/docs/analysis/sql-tables (`thread_state`, `sched`, `cpu_freq`)
- Perfetto FrameTimeline docs: https://perfetto.dev/docs/data-sources/frametimeline
- android/skills profilers commit: https://github.com/android/skills/tree/4328beaf36f00265db107eb316f9add6b8764144/profilers

## 闭环动作
- 写入 queue.json：0 处（高置信局部 auto-fix，不新增 backlog）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 回到 Task6 复审：1 章
