# 深度技术 Review 闲时抽检 · 2026-05-20 00:20

## 抽检目标
- 章节：18.18 PIP 与自由窗口渲染
- 文件：src/part2-performance/ch18-rendering-pipelines/18-pip-freeform.md
- 状态：finalized
- 抽检原因：无 `task9_state: pending` 候选；该章 `last_task9_at=2026-04-24T03:39:00+08:00`，超过 21 天，且历史 Task9 曾有 PiP / Freeform trace、BLAST 版本边界类 P2 建议。
- 抽检范围：闲时模式，仅复核维度 1（源码/官方 API 引用）与维度 3（版本差异）。

## 复核依据
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/view/ViewRootImpl.java`：存在 `BLASTBufferQueue`、`Choreographer.getInstance()`、`scheduleTraversals()`、`relayoutWindow` 等路径。
- AOSP android-16.0.0_r1 `frameworks/native/libs/gui/BLASTBufferQueue.cpp`：存在 `acquireNextBufferLocked()`、`syncNextTransaction()`、`mergeWithNextTransaction()`、`QueuedBuffer - <name>BLAST#<id>` trace 名称拼接。
- AOSP android-11.0.0_r1 `frameworks/native/libs/gui/BLASTBufferQueue.cpp`：存在 `processNextBufferLocked()`、`setNextTransaction()`，但没有 `acquireNextBufferLocked()` / `syncNextTransaction()` / `mergeWithNextTransaction()`。
- AOSP android-12.0.0_r1 `BLASTBufferQueue.cpp`：已出现 `mergeWithNextTransaction()`，但仍没有 `acquireNextBufferLocked()` / `syncNextTransaction()`。
- AOSP android-12.1.0_r1 `BLASTBufferQueue.cpp`：出现 `acquireNextBufferLocked()` 和 `mergeWithNextTransaction()`。
- AOSP android-13.0.0_r1 `BLASTBufferQueue.cpp`：出现 `syncNextTransaction()`。
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/window/TaskOrganizer.java`、`WindowContainerTransaction.java`、`frameworks/base/libs/WindowManager/Shell/src/com/android/wm/shell/pip/PipTaskOrganizer.java`、`Transitions.java`：路径存在，Shell / WCT / PiP 控制面引用成立。
- AOSP `DisplayEventReceiver.java` 版本对比：android-11.0.0_r1 无 `VsyncEventData` / FrameTimeline 相关结构；android-12.0.0_r1 开始出现 `VsyncEventData`；android-16.0.0_r1 已包含 `FrameTimeline` / `preferredFrameTimelineIndex`。

## 审查结果

### 维度 1：源码引用准确性 / API 名称准确性
- 评分：4/5
- 问题数：1（无 P0/P1）
- 复核结论：frontmatter 中列出的 `ViewRootImpl.java`、`Choreographer.java`、`DisplayEventReceiver.java`、`BLASTBufferQueue.cpp`、`TaskOrganizer.java`、`WindowContainerTransaction.java`、`PipTaskOrganizer.java`、`Transitions.java` 在 android-16.0.0_r1 对应路径均可复核；PiP / Freeform 控制面与 BLAST / BufferQueue 主链路成立。
- 具体问题：
  1. [P2][源码引用/版本锚点] L150、L215 — 正文把 `acquireNextBufferLocked()`、`syncNextTransaction()`、`mergeWithNextTransaction()` 一起放在 “Android 11，`BLASTBufferQueue` 进入主窗口路径” 段落里。复核显示 Android 11 的 `BLASTBufferQueue.cpp` 还没有这三个方法；Android 12.0 只有 `mergeWithNextTransaction()`，Android 12.1 才有 `acquireNextBufferLocked()`，Android 13 才有 `syncNextTransaction()`。建议后续改成：Android 11 说明 BLAST 主窗口路径与 `processNextBufferLocked()` / `setNextTransaction()`；Android 12/12.1/13+ 再拆出现代 `merge/acquire/sync` 方法名。当前问题会影响溯源码时的版本定位，但不改变“现代 Android 通过 BLAST 合并 buffer 与 transaction”的主结论，不升级为 P0/P1。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（无 P0/P1）
- 具体问题：
  1. [P2][版本差异] L187、L195、L203 — Trace 章节写“至少要能看到 … FrameTimeline”，但 frontmatter 适用范围覆盖 Android 8-16。Android 8-11 没有当前 FrameTimeline 观察面；Android 12+ 才适合把 FrameTimeline 作为主线排查入口。建议后续补一句降级口径：Android 8-11 以 Choreographer / RenderThread / BufferQueue / SurfaceFlinger slice、`dumpsys SurfaceFlinger` 和 gfxinfo 为主，Android 12+ 再增加 FrameTimeline / actual present 对照。该问题属于版本边界补充，不影响 Android 12+ 结论。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：2 处（闲时抽检仅日志记录）
- P3 锦上添花：0 处
- 闲时抽检技术评分：4.0/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（闲时抽检无 P0/P1；P2 仅在本日志记录，避免扩大写入面）
- 更新 frontmatter：`last_task9_audit: "2026-05-20"`
- 自动晋升 finalized：无（章节已是 finalized）
