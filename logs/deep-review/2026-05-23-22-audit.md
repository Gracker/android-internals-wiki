# Task 9 闲时技术抽检 · 2026-05-23 22:20

## 抽检目标
- 章节：19.08 ArgusAPM
- 文件：src/part3-tools/ch19-apm/08-argusapm.md
- 状态：finalized
- 模式：闲时抽检（源码引用 + 版本差异）
- 选择原因：当前无 task9_state=pending 候选；该节 task9_result=pass-tech-review，last_task9_at=2026-05-04T08:42:32+08:00，属于本轮可抽检池中较早通过且曾有历史 Task9 回炉记录的章节。

## 复核资料
- Qihoo360/ArgusAPM GitHub master，HEAD 75ead19，最后提交 2019-05-09T14:20:48+08:00。
- README：服务端免费新增接入停止公告；模块范围包含 UI、网络、内存、进程、文件、卡顿、ANR；项目结构分为性能采集模块与 Gradle Plugin。
- sample / module Gradle：sample compileSdkVersion 27、targetSdkVersion 27、JavaVersion.VERSION_1_7、okhttp:3.10.0；argus-apm-okhttp module provided okhttp:3.7.0。
- 源码锚点：argus-apm-aop、argus-apm-gradle、argus-apm-gradle-asm 下的 AspectJTransform / ASMWeaver / FuncClassAdapter / OkHttp3ClassAdapter / WebClassAdapter / TypeUtil。
- OkHttp 3.x changelog：EventListener 3.9 preview，3.11 stable。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4/5
- P0/P1：0
- P2：2

1. [P2][源码引用] 第 95 行 ASM 入口路径使用了缩写：`argus-apm-gradle-asm/…/asm/ASMWeaver.kt`、`bytecode/func/FuncClassAdapter.kt`、`bytecode/okhttp3/OkHttp3ClassAdapter.kt`、`bytecode/webview/WebClassAdapter.kt`。实际路径分别位于 `argus-apm-gradle-asm/src/main/kotlin/com/argusapm/gradle/internal/asm/ASMWeaver.kt` 与 `argus-apm-gradle-asm/src/main/kotlin/com/argusapm/gradle/internal/asm/bytecode/...`。当前写法不影响主结论，但不适合作为可复制源码路径。
2. [P2][源码引用] 第 159 行 `OkHttp3ClassAdapter` “只匹配 `OkHttpClient.Builder` 构建路径”的说法方向正确，但源码细节更具体：`TypeUtil.isOkhttpClientBuilder()` 匹配 `okhttp3/OkHttpClient$Builder`，`TypeUtil.isOkhttpClientBuild()` 匹配 Builder 构造函数 `<init>()V` / `<init>(Lokhttp3/OkHttpClient;)V`，`Okhttp3MethodAdapter` 在构造函数返回前向 `interceptors` 字段插入 `OkHttpUtils.insertToOkHttpClientBuilder()`。后续精修可补这个锚点，避免被读成拦截 `build()` 方法。

已复核通过的源码点：
- `TraceActivity.java`、`TraceNetTrafficMonitor.java`、`AspectJTransform.kt` 路径存在。
- `FuncClassAdapter` 只对 `Runnable.run()` 和 `BroadcastReceiver.onReceive()` 注入计时代码的描述与 `TypeUtil.isRunMethod()` / `isOnReceiveMethod()` 一致。
- `WebClassAdapter` 匹配 `WebViewClient.onPageFinished(WebView, String)` 的描述与 `TypeUtil.isOnPageFinishedMethod()` 一致。
- “不要把 ASM 路径理解成任意方法耗时采集”的边界判断成立。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- P2：0

已复核通过的版本点：
- 章节把 ArgusAPM 定位为历史 APM 架构参考，并明确现代 Android / AGP / 隐私策略需要重新验证，符合仓库最后活跃时间与 sample 基线。
- sample 中 `compileSdkVersion 27`、`targetSdkVersion 27`、`JavaVersion.VERSION_1_7`、`okhttp:3.10.0` 的描述成立；网络模块自身还存在 `provided okhttp:3.7.0`，不推翻章节使用 sample 作为迁移风险基线的判断。
- OkHttp EventListener “3.9 preview、3.11 stable”的版本描述与 OkHttp 3.x changelog 一致。
- “Gradle Transform 要迁到现代 AGP API”的风险判断成立；ArgusAPM 插件仍使用 `com.android.build.api.transform.Transform` 与 `TransformManager.SCOPE_FULL_PROJECT`。

## 闭环动作
- queue.json：0（未发现 P0/P1）
- research-gaps.md：0
- suggestions.md：0（闲时抽检仅记录 P2 备注，不新增处理队列）
- 章节内容：未修改正文，仅更新 frontmatter `last_task9_audit: "2026-05-23"`
- external-review：未消费，无归档动作

## 结论
本次闲时抽检未发现会阻塞发布的源码错误或版本差异缺失。章节保持 finalized / ready-to-publish。后续如果做轻量精修，可把 ASM 路径和 OkHttp3 ASM 注入点补成完整源码锚点。
