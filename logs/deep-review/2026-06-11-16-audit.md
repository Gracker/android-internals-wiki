# Task9 闲时抽检 · 2026-06-11 16:25

## Review 目标
- 章节：13.4 命令行打开超大 Trace
- 文件：src/part3-tools/ch13-perfetto/04-large-traces.md
- 状态：finalized / ready-to-publish
- 选择说明：preflight CANDIDATES=0、TASK2B_BACKLOG=0，进入闲时抽检；本节 status=finalized、task9_result=pass-tech-review，last_task9_at=2026-04-28T06:20:00+08:00，超过 21 天；历史 Task9 有 P2 建议记录。
- external-review：最近 3 天 logs/external-review/ 根目录无 13.4 / large-traces / Perfetto 活跃文件。
- queue：未发现 section=13.4 的 pending 条目。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 版本边界：仅使用 Android 17 / API 37 及以下资料；未采用 Android 18 / API 38+ 资料。

## 证据来源
- Perfetto 官方 Trace Processor 文档：trace_processor 是 C++ 库和命令行 shell；当前推荐 subcommand 入口 `query` / `server http`，同时明确 `-q`、`-Q`、`--httpd`、`-e` 等 classic flat flags 仍通过兼容层支持。
- Perfetto 官方 Visualising large traces 文档：UI 可把解析和查询卸载到本地 `trace_processor server http`，默认探测 `127.0.0.1:9001`；classic `./trace_processor --httpd /path/to/trace.pftrace` 仍被支持；浏览器内存典型约 2GB，trace processor 内存表示通常为原始 proto trace 的 2-4 倍。
- AOSP android-16.0.0_r1 `external/perfetto/src/trace_processor/trace_processor_shell.cc`：classic help 中存在 `--http-port`、`-q/--query-file`、`-Q/--query-string`、`-e/--export`，未出现 `--http-ip-address`。
- 当前 get.perfetto.dev 预编译 trace_processor help：新版 subcommand 与 classic interface 均可用；classic help 已包含 `--http-ip-address`，正文已有“独立上游二进制以本机 help 为准”的边界。
- Perfetto Python API 文档与 pip `perfetto==0.56.0` 签名：`TraceProcessor.query()`、`as_pandas_dataframe()`、`TraceProcessorConfig(ingest_ftrace_in_raw=False)`、`BatchTraceProcessor` 行为与正文一致。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.7/5
- 问题数：0（P0/P1/P2）
- 复核结论：正文对 `trace_processor` 交互式 shell、`-q/--query-file`、`-Q/--query-string`、CSV 输出、`-e/--export`、HTTP 守护进程、Python `TraceProcessor` / `BatchTraceProcessor` 的描述与 Perfetto 官方文档、当前二进制 help、AOSP android-16.0.0_r1 源码锚点一致。
- 本地验证：空 trace 上 `-Q 'select 1 as a, 2 as b'` 输出 CSV 表头和值；多条 `SELECT` 产出结果时 classic CLI 报错“only the final statement is a SELECT statement”，与正文说明一致。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0（P0/P1/P2）
- 复核结论：正文保留 classic 参数用于 Android 8-16 / AOSP external/perfetto 范围，并提示独立上游二进制以本机 `--help` 为准；这能覆盖 AOSP android-16 与当前 Perfetto upstream CLI 已转向 subcommand 的差异。
- P3 记录：后续可追加一小句新版等价命令：`trace_processor query -f analysis.sql trace.pftrace`、`trace_processor server http trace.pftrace`。classic flags 官方仍承诺兼容，不构成本轮问题单。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：4.6/5

## 闭环动作
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：1 处
- Frontmatter：更新 `last_task9_audit: 2026-06-11`、`last_task9_audit_log: logs/deep-review/2026-06-11-16-audit.md`
