# 深度技术 Review · 2026-05-28 15:23

## Review 目标
- 章节：14.4 dumpsys 系列命令
- 文件：src/part3-tools/ch14-other-tools/04-dumpsys.md
- 状态：ready-for-review
- 选择说明：preflight 候选 5 个；13.10、13.14、14.10、14.12 今日已被 Task9 处理过，按当天防重复规则跳过，本轮只复审 Task2B 回流的 14.4。
- external-review：logs/external-review 根目录近 3 天无活跃匹配文件，本轮未消费 external-review。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：3（均已 AUTO-FIX）
- 具体问题：
  1. [P2][AUTO-FIX] L117-L119 — `mFocusedActivity` 与 `initiating/resumed` 不是 Android 16 `dumpsys activity activities` 的准确字段/状态口径。AOSP `RootWindowContainer.dump()` 输出 `topDisplayFocusedRootTask`，`dumpActivities()` 打印 TaskDisplayArea 的 `Resumed:` Activity；`ActivityRecord.State` 使用 `INITIALIZING/STARTED/RESUMED`。已修正为 `topDisplayFocusedRootTask`、`Resumed:` 与 `INITIALIZING` / `STARTED` / `RESUMED`。
  2. [P2][AUTO-FIX] L277-L287 — `dumpsys cpuinfo` 进程行被写成包含 IRQ/SoftIRQ。AOSP `ProcessCpuTracker.printCurrentState()` 对进程行只传 user/kernel，`TOTAL` 行才传 iowait/irq/softirq。已修正进程行与 TOTAL 行的分析口径。
  3. [P2][AUTO-FIX] L514 — `frame_ready_time` 被写成提交给 HWC 的时间。AOSP Android 16 `FrameTimeline::generateFrameStats()` 将它填为 SurfaceFrame actual endTime；旧 `FrameTracker` 注释定义为帧内容准备好可显示的时间。已修正为 ready time，不等同于 HWC 提交或 GPU 完成时间。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1（已 AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] L428-L459 — FrontEnd 补充段把 `LayerSnapshotBuilder.update()` 描述为 `mStateLock` 外的无锁路径，并用过时的 `commit()` / `commitTransactionsLocked()` 顺序解释。AOSP android-16.0.0_r1 `SurfaceFlinger::updateLayerSnapshots()` 在事务收集、apply 和 `LayerHierarchyBuilder.update()` 后才进入 `mStateLock`，但 `applyAndCommitDisplayTransactionStatesLocked()` 与 `LayerSnapshotBuilder.update(args)` 在锁内。已重写为 Android 16 的实际状态锁边界。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0
- 说明：复核 Android 12 / Android 16 `FrameInfo.h`：`FrameDeadline`、`FrameInterval` Android 12 已存在，`WorkloadTarget` Android 16 存在；Task2B 对 gfxinfo framestats 的版本边界修复有效。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：14.4 对 activity/meminfo/gfxinfo/window/batterystats/SurfaceFlinger 已覆盖常用排查入口；package/alarm/jobscheduler 与自定义 dump 保留为扩展待补充，不构成本轮 P1。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：本节定位为命令手册，关键风险在字段/版本口径；暂未追加 benchmark 或 trace 数据要求。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：related_chapters 中 4.1、4.5、7.3、13.1、14.1 均存在；正文回到 Perfetto / Window / Memory 章节的引用方向一致。

## 一手资料
- AOSP android-16.0.0_r1 `frameworks/base/services/core/java/com/android/server/wm/RootWindowContainer.java`：`dump()` / `dumpActivities()` 输出 `topDisplayFocusedRootTask` 与 `Resumed:` Activity。
- AOSP android-16.0.0_r1 `frameworks/base/services/core/java/com/android/server/wm/ActivityRecord.java`：`State` enum 包含 `INITIALIZING`、`STARTED`、`RESUMED`。
- AOSP android-16.0.0_r1 `frameworks/base/core/java/com/android/internal/os/ProcessCpuTracker.java`：进程行 user/kernel；TOTAL 行包含 iowait/irq/softirq。
- AOSP android-16.0.0_r1 `frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp`：`updateLayerSnapshots()` 中 `mStateLock` 位于 hierarchy 更新后、snapshot 更新前。
- AOSP android-16.0.0_r1 `frameworks/native/services/surfaceflinger/FrameTimeline/FrameTimeline.cpp` 与 `FrameTracker.h`：`frameReadyTimesNano` 来源与 ready-time 定义。
- AOSP android-12.0.0_r1 / android-16.0.0_r1 `frameworks/base/libs/hwui/FrameInfo.h`：`FrameDeadline` / `FrameInterval` / `WorkloadTarget` 版本边界。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 AUTO-FIX）
- P2 建议改进：3 处（已 AUTO-FIX）
- P3 锦上添花：0 处
- 总体技术评分：4.1/5

## 闭环动作
- AUTO-FIX：4 处，已直接修改 `src/part3-tools/ch14-other-tools/04-dumpsys.md`。
- frontmatter：已更新 `task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-05-28`。
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：0 节（本轮存在 P1 auto-fix，回到 Task6 复审）
