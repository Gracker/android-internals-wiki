# 深度技术 Review 闲时抽检 · 2026-05-26 02:20

## Review 目标
- 章节：11.4 案例集
- 文件：src/part2-performance/ch11-power/04-case-studies.md
- 状态：finalized（抽检后转 ready-for-review / task2b_pending）
- 选择原因：当前无 `task9_state: pending` 候选；本节 `last_task9_at=2026-05-07T07:28:39+08:00`，曾有 P2 建议，适合抽检源码引用与版本差异。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L642-L654 JobScheduler runtime 表 — AOSP android-16.0.0_r1 `JobSchedulerService.Constants` 中 `DEFAULT_RUNTIME_MIN_EJ_GUARANTEE_MS=3min`，不是正文写的 expedited 10min；`DEFAULT_RUNTIME_FREE_QUOTA_MAX_LIMIT_MS=30min` 是 free quota max，不是 User-Initiated 最小保障。User-Initiated 另有 `DEFAULT_RUNTIME_MIN_UI_GUARANTEE_MS=max(6h,10min)`、`DEFAULT_RUNTIME_UI_LIMIT_MS=max(12h,30min)`、累计 UI limit 24h。
  2. [P2] L893 Excessive Wakeups 参考链接 — `developer.android.com/topic/performance/vitals/wakeups` 返回 404，官方当前路径为 `.../vitals/wakeup`。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L296 后台位置权限版本链 — `foregroundServiceType=location` 的起点不应写到 Android 12。Android 10 privacy changes 已要求持续访问位置的 foreground service 声明 location 类型，并配套 `ACCESS_BACKGROUND_LOCATION`；Android 12 更应落到 while-in-use 权限和后台启动 FGS 限制。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5

## 闭环动作
- 写入 queue.json（P95）：1 条（含 P0/P1 两个 review_issues）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：1 条（Excessive Wakeups URL）
- 更新 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-05-26`

## 复核依据
- AOSP android-16.0.0_r1 `JobSchedulerService.java`：`DEFAULT_RUNTIME_MIN_EJ_GUARANTEE_MS=3min`、UIJ runtime constants 与 `getMaxJobExecutionTimeMs()` 分支。
- Android Developers `JobService` API：系统为执行中的 Job 持有 wakelock，直到 `jobFinished()` 或 `onStopJob()`。
- Android 10 privacy changes：`ACCESS_BACKGROUND_LOCATION` 与 location foreground service type。
- Android Vitals Excessive Wakeups：官方当前路径为 `/topic/performance/vitals/wakeup`。
