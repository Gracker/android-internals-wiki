# 深度技术 Review · 2026-06-06 02:20

## Review 目标
- 章节：2.19 刷新率切换与帧率适配性能
- 文件：src/part1-fundamentals/ch02-rendering/19-refresh-rate-switching.md
- 状态：ready-for-review

## Preflight
- CANDIDATES: 1
- IDLE_AUDIT: 285
- TASK2B_BACKLOG: 2
- external-review: logs/external-review/ 根目录无该章节最近 3 天活跃文件
- AOSP tag 边界：platform/frameworks/base 命中 android-16.0.0_r1；未命中 android-17.0.0_r1。本轮不把 main/master 作为 Android 17 正文结论。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3.6/5（auto-fix 后）
- 问题数：4 处残留 P0 已小范围修复
- 具体问题：
  1. [AUTO-FIX/P0] App 侧优化 — 普通游戏仍建议 `FRAME_RATE_COMPATIBILITY_EXACT`。android-16.0.0_r1 `Surface.java` 明确 `EXACT=100` 且 `@hide`，并在注释中建议 game content 使用 `DEFAULT`；已改为 `DEFAULT`，Android 16+ 最低约束补 `AT_LEAST`。
  2. [AUTO-FIX/P0] Game Mode 案例 — `GameManager#setGameMode(String,int)` 需要 `MANAGE_GAME_MODE`，属于系统/OEM 管理入口；已改为普通游戏读取 `GameManager.getGameMode()`。
  3. [AUTO-FIX/P0] Perfetto 采集 — `android.surfaceflinger.frame` / `display_refresh_rate` 不符合 Perfetto FrameTimeline 官方数据源口径；已改为 `android.surfaceflinger.frametimeline` + `actual_frame_timeline_slice` / `expected_frame_timeline_slice`。
  4. [AUTO-FIX/P0] HWC 无缝切换 — `HWComposer.mSeamlessCapability` 在 android-16.0.0_r1 不可溯源；已改为 `HWComposer::setActiveModeWithConstraints()` 路径和设备实现边界。

### 维度 2：原理链完整性
- 评分：3.1/5
- 问题数：1
- 具体问题：
  1. [P1] ARR 原理节仍把“场景识别 / 预切换 / fallback / 渐进切换”串成确定链路，但正文缺少官方 ARR HAL 字段、`notifyExpectedPresent`、`VrrConfig` 与 SurfaceFlinger 决策之间的可验证状态迁移。

### 维度 3：版本差异覆盖
- 评分：2.8/5
- 问题数：1
- 具体问题：
  1. [P1] ARR 版本演进仍不准确。官方文档把 ARR 放在 Android 15 / Android 15-QPR1+，Android 11-14 只能作为多刷新率/MRR 背景；正文仍有 Android 13 场景识别 ARR、Android 16-17 机器学习识别/连续范围控制等不可验证结论。

### 维度 4：知识盲区
- 评分：3.5/5
- 盲区数：1
- 具体问题：
  1. [P2] 与 2.18 ARR 章节的边界需要对齐：2.18 已把 Android 15-QPR1+ ARR 与 Android 16 Display 查询 API 分清，2.19 仍混写。

### 维度 5：数据与案例支撑
- 评分：2.7/5
- 问题数：1
- 具体问题：
  1. [P1] 厂商 PLL 延迟、帧丢失率、功耗百分比、切换次数减少 60-80% 仍缺设备型号、系统版本、亮度、样本和 trace/benchmark，不能作为发布稿量化结论。

### 维度 6：交叉引用一致性
- 评分：3.2/5
- 问题数：1
- 具体问题：
  1. [P1] 2.18 `Adaptive Refresh Rate 与动态帧率控制` 已给出 ARR 主体从 Android 15-QPR1+ 开始、Android 16 暴露 `Display.hasArrSupport()` / `getSuggestedFrameRate()` 的边界；2.19 当前版本表与其不一致。

## 统计
- P0 事实错误：4 处，已 AUTO-FIX
- P1 重要缺失：3 处，合并保留 queue.json 2.19 pending 条目（priority 85）
- P2 建议改进：1 处，并入 P1 回炉说明
- P3 锦上添花：0 处
- 总体技术评分：3.0/5

## 闭环动作
- AUTO-FIX：src/part1-fundamentals/ch02-rendering/19-refresh-rate-switching.md 8 处小修
- 写入 queue.json：1 个现有 2.19 pending 条目已更新，P0 标记 auto-fixed，剩余 P1 保持 pending
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节 frontmatter：`task9_state: reviewed`、`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`

## 证据来源
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/view/Surface.java`：`FRAME_RATE_COMPATIBILITY_DEFAULT/FIXED_SOURCE/AT_LEAST/EXACT/MIN` 常量与 game content 注释
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/app/GameManager.java`：`setGameMode(String,int)` 权限与 `@SystemApi/@hide` 边界
- AOSP android-16.0.0_r1 `frameworks/native/services/surfaceflinger/Scheduler/Scheduler.cpp`：`chooseRefreshRateForContent()`
- AOSP android-16.0.0_r1 `frameworks/native/services/surfaceflinger/Scheduler/RefreshRateSelector.cpp`：`getRankedFrameRates()` / `getRankedFrameRatesLocked()` 与 `SFTRACE_CALL()`
- AOSP android-16.0.0_r1 `frameworks/native/services/surfaceflinger/DisplayHardware/HWComposer.cpp`：`setActiveModeWithConstraints()`
- Perfetto official FrameTimeline docs：data source `android.surfaceflinger.frametimeline`，SQL 表 `actual_frame_timeline_slice` / `expected_frame_timeline_slice`
- Android official ARR docs：ARR introduced in Android 15；developer docs state supported devices run Android 15-QPR1+ and expose `hasArrSupport()` for support checks
