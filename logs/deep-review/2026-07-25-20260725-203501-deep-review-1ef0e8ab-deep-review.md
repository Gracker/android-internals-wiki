# AIW Deep Review · 2026-07-25 · 14.30

- run_id: `20260725-203501-deep-review-1ef0e8ab`
- lane: `deep-review`
- target: `src/part3-tools/ch14-other-tools/14.30-android17-gpuservice-gpu-memory-observability.md`
- baseline: `android-17.0.0_r1` / Android 17
- status: completed

## 审计结论

本轮仅处理预选的既有章节 §14.30，未新增章节，未修改 `src/SUMMARY.md`。章节主体已有 DeepResearch 与 AOSP source list 支撑，可做 bounded deep-review 补丁。

结论：章节主线成立，继续保留 `ready-for-review` 与 `confidence: medium-high`。本轮主要修复 source-boundary/readability 小问题：

1. frontmatter 增加 `last_deep_review_at` / `last_deep_review_run_id`，将 `task6_state` 从 `pending` 推进为 `reviewed`，`pipeline_stage` 改为 `task9_pending`；
2. 将 GpuMem attach 重试描述从“最多 30 秒”精确为“失败且 `mStop` 未置位时，每秒重试，最多 30 次（约 30 秒）”；
3. 针对材料中 `android.gpu_mem` 与正文 `android.gpu.memory` 的不一致，核对 AOSP `GpuMemTracer.h @ android-17.0.0_r1` 后在正文显式写明本章采用 `android.gpu.memory`，早期材料写法不作为结论；
4. 将 remaining risk 表述收敛为“实机或厂商源码确认”，避免 pending-verification marker 被误读为正文未审。

## P0 / P1 / P2

- P0: 无。
- P1: 无。
- P2: `AIW-14.30-20260725-P2-perfetto-source-boundary`，已修复并在 `metadata/review-findings.json` 标记 `fixed`。

## 证据

- `frameworks/native/services/gpuservice/tracing/include/tracing/GpuMemTracer.h @ android-17.0.0_r1`: `kGpuMemDataSource[] = "android.gpu.memory"`
- `frameworks/native/services/gpuservice/tracing/GpuMemTracer.cpp @ android-17.0.0_r1`: `set_gpu_mem_total_event()` 写入路径
- `frameworks/native/services/gpuservice/gpumem/GpuMem.cpp @ android-17.0.0_r1`: `bpf_attach_tracepoint()` 初始化与重试路径
- `frameworks/native/services/gpuservice/bpfprogs/gpuMem.c @ android-17.0.0_r1`: `(gpu_id << 32) | pid` key 与 `GPU_MEM_TOTAL_MAP_SIZE`
- `hardware/interfaces/memtrack/aidl/android/hardware/memtrack/IMemtrack.aidl @ android-17.0.0_r1`: memtrack GL/GRAPHICS 语义

## 实际补丁

- 修改正文文件：`src/part3-tools/ch14-other-tools/14.30-android17-gpuservice-gpu-memory-observability.md`
- 更新 issue pool：`metadata/review-findings.json`

## 后续风险

- 仍需 Pixel / 厂商设备实测 `dumpsys gpuservice --gpumem` 输出、Perfetto UI / trace processor 呈现、Adreno/Mali/PowerVR tracepoint 完整度。
- 不引入 Android 18 / API 38+ 主线结论。
