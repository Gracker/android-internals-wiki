# Task 9 闲时抽检 · 2026-06-13 10:20

## 抽检目标
- 模式：闲时抽检（维度 1 源码/资料锚点 + 维度 3 版本差异）
- 章节：19.03 KOOM
- 文件：src/part3-tools/ch19-apm/03-koom.md
- 状态：finalized / pass-tech-review（抽检前）
- 选择原因：当前无 `task9_state: pending` 候选，Task2B backlog=0；该章节 `last_task9_at=2026-04-24T13:23:00+08:00`，超过 21 天，且 KOOM native/thread 模块涉及 Android 15+ 16KB page-size 兼容风险。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮不做完整 6 维度审查。

## 复核资料
- KwaiAppTeam/KOOM `master`：`df3b8c33f63ab1f23e814c19792314efb653deaf`，HEAD 未较 2026-05-20 复检变化。
- KOOM `README.zh-CN.md`：确认 Java Heap / Native Heap / Thread 三个模块定位；Native 模块支持 `c++_shared` / `c++_static`，并提示 shared/static 不能混用、`pickFirst` 慎用。
- `koom-java-leak/README.zh-CN.md`：确认 Android L/API 21+，ABI 支持 `armeabi-v7a arm64-v8a x86 x86-64`，触发条件为 Java heap、线程数、FD 等连续超阈值，dump 路线为 `虚拟机 suspend -> fork -> resume -> dump`，并用 Shark 做端侧分析。
- `koom-native-leak/README.zh-CN.md`：确认 Android N/API 24+、仅 `arm64-v8a`，hook `malloc/free`，基于 mark-and-sweep / `libmemunreachable` 思路输出不可达 native block 和分配栈。
- `koom-thread-leak/README.zh-CN.md`：确认 Android N/API 24+、仅 `arm64-v8a`，hook `pthread_create/pthread_exit`，关注 joinable 线程未 `detach/join` 后退出的泄漏信息。
- KOOM upstream `build.gradle`：`compileSdkVersion=34`、`targetSdkVersion=30`、`ndk=28.2.13676358`、AGP `7.1.0`；源码未直接声明 16KB page-size 运行验证结论。
- Android Developers 16KB page-size 文档（2026-05-28）：Android 15+ 支持 16KB page-size；使用直接或间接 NDK/native SDK 的 App 需要重建/验证；NDK r28+ 默认 16KB ELF alignment；AGP 8.5.1+ 负责 16KB ZIP alignment；Android 17 可将 16KB backcompat 设为 fatal 以暴露不兼容二进制。
- Android Developers `ApplicationExitInfo` / API diff 30：`ApplicationExitInfo`、`ActivityManager.getHistoricalProcessExitReasons()`、`ActivityManager.isLowMemoryKillReportSupported()` 均为 API 30+ 边界。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0（正文事实错误 0；新增 1 处边界 auto-fix）
- 复核点：
  1. 三个 KOOM 模块能力、minSdk/ABI 边界与 upstream README 一致：Java 模块 API 21+；Native / Thread 模块 API 24+ 且仅 `arm64-v8a`。
  2. Java fork dump、Hprof 裁剪、Shark 分析主链与 `koom-java-leak` README 一致。
  3. Native leak 的 `malloc/free` hook、mark-and-sweep / `libmemunreachable` 思路、xhook 依赖与 upstream README / 源码一致。
  4. Thread leak 的 `pthread_create/pthread_exit` hook、joinable 线程未 `detach/join` 判定与 upstream README / 源码一致。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（已 AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] `使用建议` 段落后 — 章节覆盖 Android 8-17，且 KOOM native/thread/fast dump 路径包含 native `.so`；正文已有 minSdk、ABI、STL 边界，但缺 Android 15+ 16KB page-size 对第三方 native SDK 的验证要求。Android 官方文档 2026-05-28 明确：NDK/native SDK 需要 16KB ELF/ZIP alignment 验证，NDK r28+ 默认 ELF 对齐，AGP 8.5.1+ 处理包内 ZIP alignment，Android 17 可强制关闭 backcompat。已补一段验证边界，要求以实际业务 APK/AAB 做 `llvm-objdump` 与 `zipalign -P 16` 检查。

## AUTO-FIX
- 文件：src/part3-tools/ch19-apm/03-koom.md
- 位置：`使用建议` 中 Native 模块依赖/STL 段落之后
- 改动：补 Android 15+/API 35 至 Android 17/API 37 的 16KB page-size 兼容边界；说明 KOOM native/thread/fast dump 不能只按 minSdk/ABI 判断，需要检查 NDK/AGP/`libc++_shared.so`/预编译 so 与最终 APK/AAB 对齐。
- 证据：KOOM upstream README + Android Developers 16KB page-size 官方文档。

## 统计
- P0 事实错误：0
- P1 重要缺失：1（已 AUTO-FIX，不入 queue）
- P2 建议改进：0
- P3 锦上添花：0
- 总体技术评分：4.25/5

## 闭环动作
- 写入 queue.json（P95/P85）：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- AUTO-FIX：1 处
- 更新章节 frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_audit=2026-06-13`
