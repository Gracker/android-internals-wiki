# 深度技术 Review 抽检 · 2026-05-21 10:23

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 193
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## Review 目标
- 章节：19.11 JankStats
- 文件：src/part3-tools/ch19-apm/11-jankstats.md
- 状态：finalized → task2b_pending（发现 P0/P1，写入 queue）
- 选择原因：`last_task9_at=2026-04-27T10:57:00+08:00`，超过 21 天；近期 Task6/Task2B 刚补过工具分工表，适合复核最容易过期的 AndroidX 源码与 FrameMetrics 版本边界；该章节当前无 pending queue 条目。

## 抽检证据
- AndroidX Maven metadata：`androidx.metrics:metrics-performance` latest/release 为 `1.0.0`。
- AndroidX metrics-performance 1.0.0 source：`JankStats.kt` 在 API 31/26/24/else 分别选择 `JankStatsApi31Impl`、`JankStatsApi26Impl`、`JankStatsApi24Impl`、`JankStatsApi16Impl`。
- `JankStatsApi24Impl.kt`：类注释与 `setupFrameTimer()` 均显示 API24+ 通过 `Window.addOnFrameMetricsAvailableListener` / `OnFrameMetricsAvailableListener` 采集 `FrameMetrics`；`getFrameData()` 调 `PerformanceMetricsState.getIntervalStates(startTime, prevEnd, stateInfo)` 做状态区间匹配。
- `JankStatsApi16Impl.kt`：`OnPreDrawListener` 是 API16 低版本估算帧时长路径，不是 API24+ `JankStatsApi24Impl` 的同步锚点。
- `PerformanceMetricsState.kt`：`putState()` / `removeState()` 使用 `System.nanoTime()` 记录状态添加/移除时间，再由 `getIntervalStates(startTime,endTime,...)` 与帧区间做 overlap。
- AOSP `FrameMetrics.java`：android-11.0.0_r1 未定义 `DEADLINE`；android-12.0.0_r1 起定义 `FrameMetrics.DEADLINE = 13`。AndroidX `JankStatsApi31Impl` 才读取 `FrameMetrics.DEADLINE` 并计算 `frameOverrunNanos`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L140 — `JankStatsApi24Impl` 被写成使用 `OnPreDrawListener` 作为状态同步锚点。源码显示 API24+ 使用 `FrameMetrics` listener；`OnPreDrawListener` 属于 API16 估算路径。该段需按 API16/API24/API26/API31 实现分层重写。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1] L172-L173 — `FrameMetrics.DEADLINE` 被放入 API24+ 通用指标列表。AOSP 显示 `DEADLINE` Android 12/API31 才出现，JankStats 也只在 `JankStatsApi31Impl` 中使用它计算 overrun。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（task9-audit-20260521-19.11-jankstats-framemetrics-source）
- queue 动作：新增 pending 条目（task9-audit-20260521-19.11-jankstats-framemetrics-source）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；task2b_result=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-21
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
