# Task 9 闲时抽检 · 2026-05-17 18:20

## 抽检目标
- 章节：20.2 Java Crash 治理
- 文件：src/part5-app/ch20-stability/02-java-crash-governance.md
- 状态：finalized / task9_result=pass-tech-review（抽检前）
- 模式：闲时抽检；范围限定为维度 1（源码引用）+ 维度 3（版本差异）
- 选择理由：当前无 `task9_state: pending` 候选；本节 `last_task9_audit` 不存在，且历史 Task9 对 20.2 曾有 P2 建议，符合闲时抽检优先级。

## 复核证据
- `frameworks/base/core/java/com/android/internal/os/RuntimeInit.java` android-10.0.0_r47 → android-16.0.0_r1：`commonInit()` 均注册 `RuntimeHooks.setUncaughtExceptionPreHandler(loggingHandler)` 与 `Thread.setDefaultUncaughtExceptionHandler(new KillApplicationHandler(loggingHandler))`。
- `libcore/ojluni/src/main/java/java/lang/Thread.java` android-10.0.0_r47 → android-16.0.0_r1：`dispatchUncaughtException(Throwable)` 均先调用 pre-handler，再调用 `getUncaughtExceptionHandler().uncaughtException(this, e)`。
- `art/runtime/thread.cc` android-16.0.0_r1：`Thread::HandleUncaughtExceptions()` 在 thread detach 清理路径中调用 Java 层 `Thread.dispatchUncaughtException(Throwable)`。
- `libcore/.../Throwable.java` + `art/runtime/native/java_lang_Throwable.cc` android-16.0.0_r1：`Throwable.fillInStackTrace()` 通过 `nativeFillInStackTrace()` 调 `Thread::CreateInternalStackTrace()`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0][源码准确性] L129 — 正文把 `CreateInternalStackTrace()` 中的 `kMaxSavedFrames = 256` 写成“最大深度 256 帧”。AOSP android-16.0.0_r1 实际逻辑是先缓存最多 256 帧；如果 `depth < kMaxSavedFrames` 直接复用缓存，否则执行 `build_trace_visitor.WalkStack()` 二次遍历构建完整 trace。256 是快速路径缓存阈值，不是最大堆栈深度。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：Android 10/11/12/13/14/15/16 的 `RuntimeInit` handler 注册、`Thread.dispatchUncaughtException()` pre-handler 顺序、`Thread::HandleUncaughtExceptions()` 入口在本轮抽检范围内未发现影响正文结论的版本差异。正文提到 Android 8.0/8.1 的 coroutine pre-handler 历史问题不在 `applicable_versions: Android 10 - Android 16` 主线范围内，暂不作为 P0/P1。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3/5

## 闭环动作
- 写入 queue.json（P95）：1 处（`task9-audit-20.2-throwable-stack-depth-20260517`）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：`task9_result=needs-rework`、`task9_state=reviewed`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-05-17`
- 章节正文：未修改
