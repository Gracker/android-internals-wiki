# Task 9 闲时抽检 · 2026-06-19 19:27

## Audit 目标
- 章节：21.3 ContentProvider 启动治理
- 文件：src/part5-app/ch21-startup/03-contentprovider-optimization.md
- 状态：finalized
- 抽检原因：无 pending Task 9 章节，Task2B backlog=0；本章 last_task9_audit=2026-05-26，超过 21 天。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 一手资料复核
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/app/ActivityThread.java`：`handleBindApplication()` 中 `data.info.makeApplicationInner(...)` 位于 8260 行，`installContentProviders(app, data.providers)` 位于 8293 行，`mInstrumentation.callApplicationOnCreate(app)` 位于 8308 行。Android 10/16 同一路径顺序一致。
- AOSP android-17.0.0_r1 `frameworks/base/services/core/java/com/android/server/pm/ComputerEngine.java`：`ProviderInfo.initOrder` 仍按高值优先排序（383-387 行），`queryContentProviders()` 返回前执行排序（4951 行）。
- AndroidX Startup 1.2.0 source jar：`InitializationProvider.onCreate()` 调用 `AppInitializer.getInstance(context).discoverAndInitialize(getClass())`；`AppInitializer` 通过 `Initializer.dependencies()` 递归初始化并检测 cycle。
- AOSP android-16.0.0_r1 / android-17.0.0_r1 `ViewTreeObserver.java`：`removeOnDrawListener()` 在 `mInDispatchOnDraw` 时构造 `IllegalStateException`；`targetSdkVersion >= Build.VERSION_CODES.O` 时抛出，否则记录日志。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0 个 P0/P1
- 结论：章节 79-90 行关于 Provider 在 `Application.onCreate()` 前安装的描述，与 Android 10/16/17 AOSP 顺序一致。章节 282-299、344-365、369-380 行关于 App Startup 的入口、手动初始化和多进程 metadata 读取，与 AndroidX Startup 1.2.0 源码一致。
- P3 备注：章节 230-231 行注释说 `OnDrawListener.onDraw()` 内移除 listener 在 android-16 会抛异常。AOSP 16/17 源码显示：targetSdk >= O 时抛出，旧 targetSdk 只记录日志。该示例使用 `OnPreDrawListener` 避开问题，且现代应用目标 SDK 通常高于 O，本轮不改正文。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0 个 P0/P1
- 结论：本章适用范围标为 Android 10-16；抽检额外复核 Android 17/API 37，核心启动顺序、Provider initOrder 排序、`ViewTreeObserver` 限制未发现与正文结论冲突的变化。未使用 Android 18/API 38+ 资料。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：4.5/5

## 闭环动作
- 更新 frontmatter：`last_task9_audit: "2026-06-19"`
- 正文改动：0 处
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
