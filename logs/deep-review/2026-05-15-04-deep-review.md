# 深度技术 Review · 2026-05-15 04:28

## 本轮说明
- 规范：已先读取 `/Users/gracker/.agents/skills/technical-writing/SKILL.md` 与 `openclaw-tasks/task9-deep-tech-review.md`。
- 选择：本轮有效候选仅 26.6 一节；`20.7`、`26.7` 仍为 draft，按规则跳过，因此低于默认 3 节。
- external-review：`logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。
- 自动晋升检查：26.6 本轮存在 P1，且 Task6 仍 pending，未晋升 finalized。

## Review 目标
- 章节：26.6 A/B Test 与性能回归防护
- 文件：src/part5-app/ch26-observability/06-ab-testing-regression.md
- 状态：ready-for-review → task2b_pending

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1] L116 Macrobenchmark `FrameTimingMetric` 指标口径 — 章节适用范围写到 Android 10-16，但把 `FrameTimingMetric` 的 `frameOverrunMs` 与 `frameDurationCpuMs` 并列为通用输出。官方资料显示 `frameDurationCpuMs` 是基础输出，`frameOverrunMs` 在 Android 12(API 31)+ 才可用；Android 10/11 的 CI 或实验室基线不能直接依赖 overrun 口径。 建议：在 Macrobenchmark 段落补版本边界：API 31+ 优先使用 `frameOverrunMs` 判断超预算；API 29/30 退回 `frameDurationCpuMs`、slow/frozen frame rate 或自定义 trace section。门禁配置要把设备 API level 写入维度。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：2
- 具体问题：
  2. [P1] L91-L98 性能实验样本量字段 — 正文说 `baseline_value`、`minimum_detectable_effect`、`alpha`、`power` 决定样本量，但连续性能指标/长尾分位值还必须依赖历史方差或完整分布、统计检验对象（均值/比例/分位值）、分桶后的 allocation ratio 和最低分群样本。只列四个字段会让读者误以为没有波动估计也能算样本量。 建议：补一层统计模型边界：均值类指标需要方差/标准差，比例类指标需要 baseline rate，P90/P99 需要 quantile CI、bootstrap 或阈值违约率替代；分设备/版本后要重新计算每个关键分群的最小样本量。
  3. [P1] L177 自动归因贡献度排序 — “样本量 × P90 变化幅度 = 对全量退化的贡献”对分位值不成立。P90 是非线性统计量，分群 P90 的加权变化不能相加还原全量 P90；这个公式只适合均值、计数或比例类指标的近似贡献。 建议：把归因算法分成两类：均值/比例用 `样本量 × delta` 排序；P90/P99 用原始样本重算 counterfactual 分布、tail violation count（超过阈值样本数）、mean excess over threshold 或 bootstrap 估计分群贡献。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L116 — `frameOverrunMs` 缺 API 31+ 边界，和章节 Android 10-16 适用范围不匹配。

### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P1] 性能 A/B Test 统计模型 — 样本量、分位值置信区间、分群归因、多重比较/中途看数控制需要补齐。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] L85/L98/L100 停止条件、数据不足与 A/A Test — 正文已经提醒“边看边停会抬高误判概率”，但没有给出日常看板频繁窥探、多分群/多护栏指标同时检查时的校正办法。性能实验上线后很容易每天看数并按设备、系统、国家多维拆分，假阳性会被放大。 建议：补充预注册中途检查窗口、sequential testing/alpha spending，或至少要求多重比较校正/FDR 与连续异常窗口；A/A Test 除主指标无系统性差异外，也应检查每个关键分群的 SRM 和假阳性率。

### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0
- 说明：26.3、15.6 文件存在且核心术语基本一致；26.7 为 draft，但本节仅作为后续章节提示，未构成技术矛盾。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：3 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.6/5

## 闭环动作
- 写入 queue.json（P85）：1 条，合并 3 个 P1 issue
- 写入 research-gaps.md：1 条
- 写入 suggestions.md：1 条
- 仅日志记录：0 条

## Top 问题
1. [P1] L91-L98 — 样本量缺少方差/分布/检验对象，统计链路不完整。
2. [P1] L177 — P90 分群贡献不能用 `样本量 × P90 delta` 线性相加。
3. [P1] L116 — `FrameTimingMetric.frameOverrunMs` 缺 Android 12(API 31)+ 边界。
