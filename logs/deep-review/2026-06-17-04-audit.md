# 深度技术 Review 闲时抽检 · 2026-06-17 04:34

## Review 目标
- 章节：1.5 线程模型
- 文件：src/part1-fundamentals/ch01-architecture/05-threading-model.md
- 状态：finalized / task9_result=pass-tech-review
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- 选择原因：last_task9_at=2026-05-26T01:27:00+08:00，超过 21 天；章节含 Android 16/17 MessageQueue、RenderThread、Binder、线程优先级等高风险源码锚点。

## 审查范围
- Android 版本边界：最高 Android 17 / API 37；本节未命中 Android 18 / API 38+ 文本。
- external-review：logs/external-review 根目录最近 3 天未命中 §1.5 活跃文件，本轮未消费 external-review。
- queue：§1.5 无 pending queue 条目。

## 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 核验点：
  1. AOSP `android-17.0.0_r1` 当前从 android.googlesource 返回 404，本轮未把未公开 tag 当作源码结论；Android 17 行为仅用 Android Developers 官方行为页和本地 API 37 SDK stub 核验。
  2. AOSP `android-16.0.0_r1` 可核验：`ActivityThread.main()` 创建主 Looper 后进入 `Looper.loop()`；`Looper` 使用 `ThreadLocal<Looper>`，循环中从 `MessageQueue.next()` 取消息并分发到 `msg.target.dispatchMessage()`。
  3. AOSP `android-16.0.0_r1` 可核验：`LegacyMessageQueue/MessageQueue.java`、`CombinedMessageQueue/MessageQueue.java`、`ConcurrentMessageQueue/MessageQueue.java` 均存在，且三者都有 `MQ.Delivered` 计数器，正文“不能用 MQ.Delivered 区分队列实现”的判断成立。
  4. AOSP `android-16.0.0_r1` 可核验：`system/core/libutils/Looper.cpp` 使用 `epoll_wait` / `eventfd` / `addFd()`；`frameworks/native/libs/binder/IPCThreadState.cpp` 通过 `BINDER_WRITE_READ` ioctl 与 driver 交互，Binder pool 不属于主线程 Looper epoll 路径。
  5. AOSP `android-16.0.0_r1` 可核验：`RenderThread.cpp` 中 `threadLoop()` 调用 `setpriority(PRIO_PROCESS, 0, PRIORITY_DISPLAY)`；`DrawFrameTask.cpp/.h` 存在 `postAndWait()`、`syncFrameState()`、`unblockUiThread()`；`HardwareRenderer.java` 入口为 `syncAndDrawFrame()`。
  6. AOSP `android-16.0.0_r1` 可核验：`Process.java` 线程优先级常量与 `setThreadPriority()`；`Choreographer.java` 使用 `ThreadLocal` 和 `FrameDisplayEventReceiver(..., VSYNC_SOURCE_APP)`。
  7. 本地 Android SDK `platforms/android-Baklava/android-stubs-src.jar` 中 `java/lang/Thread.java` 的 `isVirtual()` 注释明确写明 Android 尚未实现 virtual thread，正文边界成立。

## 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 核验点：
  1. Android Developers MessageQueue behavior change guidance 与 Android 17 behavior changes 页面均确认：Android 17 / API 37 起，targetSdk 37+ 应用默认获得 lock-free `android.os.MessageQueue`；可用 `adb am compat enable/disable USE_NEW_MESSAGEQUEUE` 测试或回退；新实现下兼容保留的 `mMessages` 字段为 null。
  2. §1.13《MessageQueue 机制与 DeliQueue 无锁优化》已 finalized 且 Task9 通过，内部口径与本节一致：Android 16 公开源码多实现并存，Android 17 / API 37 面向应用默认启用。
  3. Android 3.0/API 11 支持 2D 硬件加速、targetSdk >= 14 默认启用、Android 5.0 引入 RenderThread 的版本拆分沿用上轮修复结果，本轮未发现回退。

## 观察项
- [P3] 参考资料列表可在后续维护时补充 `frameworks/base/libs/hwui/renderthread/DrawFrameTask.cpp` / `.h`，让 `DrawFrameTask::postAndWait()` 的源码锚点更直接；当前正文结论和已有 `RenderProxy.cpp` / `RenderThread.cpp` 引用不构成源码错误。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：5/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 内容 auto-fix：0 处
- Frontmatter：更新 `last_task9_audit` / `last_task9_audit_at` / `last_task9_audit_log` / `last_task9_audit_result` / `task9_audit_notes`
