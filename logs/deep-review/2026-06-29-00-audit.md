# 深度技术 Review 抽检 · 2026-06-29 00:25

## Review 目标
- 模式：闲时抽检（无 task9_state: pending 候选，Task2B backlog = 0）
- 章节：18.25 Jetpack Compose 渲染管线架构
- 文件：src/part2-performance/ch18-rendering-pipelines/25-compose-rendering-pipeline.md
- 状态：finalized
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：3
- 具体问题：
  1. [P0] 「与 Choreographer 的绑定」和「与 Choreographer FrameData 的协作」——文中 `Choreographer.getFrameTimeline()` / `FrameData.deadlineNanos` 不是 android-17.0.0_r1 的可用 API。证据：`frameworks/base/core/java/android/view/Choreographer.java` 中 `FrameCallback.doFrame(long)` 仍是单参数；`FrameData` 通过 `getPreferredFrameTimeline().getDeadlineNanos()` 暴露 deadline；`getFrameDeadline()` 是 hidden 方法。`AndroidUiFrameClock.android.kt` 只调用 `postFrameCallback` 并把 `frameTimeNanos` 传给 `withFrameNanos`。
  2. [P0] 「RenderNode 与 DisplayList 提交」把 `HardwareRenderer.syncAndDrawFrame()` 放进 Compose 自己的 `FrameCallback.doFrame()`。证据：android-17.0.0_r1 的 `ViewRootImpl.performDraw()` → `draw()` 调用 `mAttachInfo.mThreadedRenderer.draw(mView, mAttachInfo, this)`；`AndroidComposeView.dispatchDraw()` 只执行 `measureAndLayout()`、`root.draw(...)` 和脏 layer 更新。
  3. [P0] 「绘制阶段与 RenderNode 构建」称 `RenderNodeLayer.android.kt` 存在 `LayerManager` 决策规则。证据：`RenderNodeLayer.android.kt` 未定义 `LayerManager`；当前 `AndroidComposeView.createLayer()` 优先返回 `GraphicsLayerOwnerLayer(graphicsContext.createGraphicsLayer())`。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] 「版本演进要点」称 Android 17 引入 `HardwareRenderer.setFramePacingEnabled()` / `FramePacing` API。证据：android-17.0.0_r1 的 `frameworks/base/graphics/java/android/graphics/HardwareRenderer.java` 未找到 `setFramePacingEnabled`、`FramePacing` 或 pacing 相关 API。

## 统计
- P0 事实错误：3 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5（抽检评分，仅覆盖源码与版本维度）

## 闭环动作
- 写入 queue.json（P95）：1 条（合并 3 个 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-06-29`
- auto-fix：0 处；问题涉及整段渲染管线与版本演进叙述，不满足局部 auto-fix 条件

## 证据锚点
- AOSP android-17.0.0_r1：`frameworks/base/core/java/android/view/Choreographer.java`
- AOSP android-17.0.0_r1：`frameworks/base/core/java/android/view/ViewRootImpl.java`
- AOSP android-17.0.0_r1：`frameworks/base/graphics/java/android/graphics/HardwareRenderer.java`
- AndroidX：`compose/ui/ui/src/androidMain/kotlin/androidx/compose/ui/platform/AndroidUiFrameClock.android.kt`
- AndroidX：`compose/ui/ui/src/androidMain/kotlin/androidx/compose/ui/platform/AndroidComposeView.android.kt`
- AndroidX：`compose/ui/ui/src/androidMain/kotlin/androidx/compose/ui/platform/RenderNodeLayer.android.kt`
- AndroidX：`compose/ui/ui/src/androidMain/kotlin/androidx/compose/ui/platform/GraphicsLayerOwnerLayer.android.kt`
