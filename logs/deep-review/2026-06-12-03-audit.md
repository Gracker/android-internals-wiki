# 深度技术闲时抽检 · 2026-06-12 03:20

## Audit 目标
- 章节：2.6 SurfaceFlinger 与合成
- 文件：src/part1-fundamentals/ch02-rendering/06-surfaceflinger.md
- 状态：finalized
- 模式：闲时抽检，仅覆盖维度 1（源码引用）与维度 3（版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：2，均已 AUTO-FIX
- 具体问题：
  1. [P0][AUTO-FIX][源码错误] L416-L420 — HWC2 `Display::getRequests()` 示例把 outLayerRequests 写成 `std::unordered_map<HWC2::Layer, LayerRequest>*`，并调用不存在的 `mComposer.getRequests(...)`。AOSP android-14.0.0_r1 中真实签名为 `std::unordered_map<HWC2::Layer*, LayerRequest>*`，内部调用 `mComposer.getDisplayRequests(mId, &intDisplayRequests, &layerIds, &layerRequests)`。
  2. [P0][AUTO-FIX][源码路径/API 错误] L424-L435 — 正文把 HIDL composer 2.1 写成 `IComposerClient.aidl`，并把 `BACKGROUND`、`DISPLAY_DECORATION` 放入同一个 HIDL 2.1 enum。android-14.0.0_r1 的 HIDL composer 2.1 enum 在 `hardware/interfaces/graphics/composer/2.1/IComposerClient.hal`，只包含 `CLIENT/DEVICE/SOLID_COLOR/CURSOR/SIDEBAND`；AIDL composer3 的 `Composition.aidl` 才包含 `DISPLAY_DECORATION = 6`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1，已 AUTO-FIX
- 具体问题：
  1. [P1][AUTO-FIX][版本边界] 参考资料区存在 Android 17 SurfaceFlinger 调研条目，但当前 `git ls-remote` 未发现 `android-17.0.0_r1` tag。已补充“未进入 Android 17，仅作研究线索，不作为正文结论”的边界，符合 Android 17 / API 37 上限规则。

## 证据来源
- AOSP android-14.0.0_r1：`frameworks/native/services/surfaceflinger/DisplayHardware/HWC2.cpp`，`Display::getRequests()`。
- AOSP android-14.0.0_r1：`hardware/interfaces/graphics/composer/2.1/IComposerClient.hal`，`enum Composition`。
- AOSP android-14.0.0_r1：`hardware/interfaces/graphics/composer/aidl/android/hardware/graphics/composer3/Composition.aidl`，`enum Composition`。
- `git ls-remote --tags https://android.googlesource.com/platform/frameworks/native refs/tags/android-17.0.0_r1`：无返回。

## 闭环动作
- AUTO-FIX：3 处
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result: auto-fixed`，`task6_state: revisiting`，`pipeline_stage: task6_pending`，`last_task9_audit: 2026-06-12`
