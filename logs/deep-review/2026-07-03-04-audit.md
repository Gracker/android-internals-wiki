# Task 9 闲时抽检 · 2026-07-03 04:59

## 抽检目标
- 章节：18.8 OpenGL ES 渲染链路
- 文件：src/part2-performance/ch18-rendering-pipelines/08-opengl-es.md
- 状态：finalized
- 选择原因：无 task9_state=pending 候选；Task2B backlog=0；本节 last_task9_at=2026-05-25T20:22:00+08:00，超过 21 天，且历史 Task9 记录过 triple buffering / EGL native fence 相关 P2 维护项。当前 queue.json 无本节 pending 条目。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 版本边界：android-17.0.0_r1 / Android 17 / API 37；未使用 Android 18+ 资料。

## 一手资料复核
- AOSP android-17.0.0_r1 `frameworks/base/opengl/java/android/opengl/GLSurfaceView.java`：`setRenderer()` 创建并启动 `GLThread`；`setRenderMode()` / `requestRender()` 对应 Continuous 与 Dirty 模式；`guardedRun()` 中 `onDrawFrame()` 后调用 `EglHelper.swap()`；`onAttachedToWindow()` / `onDetachedFromWindow()` 处理重建与退出。
- AOSP android-17.0.0_r1 `frameworks/native/opengl/libs/EGL/eglApi.cpp`：`eglSwapBuffers()` 与 `eglSwapBuffersWithDamageKHR()` 仍是 EGL wrapper，具体 queue/dequeue 与 native window/driver 实现需结合 Surface / BufferQueue 侧观察，正文没有把 wrapper 误写成唯一实现点。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/os/GraphicsEnvironment.java`：ANGLE 选择顺序包含 `angle_gl_driver_all_angle`、`angle_gl_driver_selection_pkgs` / `values`、Android 16 起平台 allowlist、denylist、游戏默认 ANGLE 配置，以及 Android 17 可见的 `com.android.graphics.driver.prefer_angle` manifest metadata。

## 审计结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- P0/P1：0
- 结论：`GLSurfaceView`、`eglSwapBuffers`、`requestRender()`、`onAttachedToWindow()` / `onDetachedFromWindow()` 的核心描述与 Android 17 源码一致；EGL native fence 示例与前次修复后的边界一致。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0/P1：0
- P2：1
- 具体问题：
  1. [P2][版本差异] L295-L322 ANGLE 路径 — 正文写“Android 15+ 仍是可选路径”不构成事实错误，但 Android 17 `GraphicsEnvironment` 已把 Android 16 起 allowlist / denylist、游戏默认 ANGLE 配置、manifest metadata `com.android.graphics.driver.prefer_angle` 纳入选择链。后续维护建议补一张 Android 16/17 ANGLE 选路优先级小表，避免读者只记住 `settings put global angle_gl_driver_selection_*`。

## 闭环动作
- 写入 queue.json：0
- 写入 suggestions.md：0（闲时抽检仅记录 P2 维护建议，未扩大待办）
- 更新 frontmatter：`last_task9_audit: 2026-07-03`
- 正文修改：0
