# AIW deep-review evidence · 2026-08-03

- run_id: `20260803-203556-deep-review-44dab4a0`
- mode: `deep-review`
- target: `src/part1-fundamentals/ch04-memory/4.18-android-17-memorylimiter-深度解析.md`
- baseline: `AOSP android-17.0.0_r1 / android17-6.18`
- materials: none from pre-run; used existing chapter sources and live AOSP tag fetch for source-bound verification.

## 审计结论

章节正文已有充分源码锚点，不属于占位稿；本轮可做小范围、来源绑定的修正，而不是回流 body-apply。复核重点覆盖：

1. JNI `cgroupPath()` 确认 `CgroupFile::kSwapMax` 写入 `memory.swap.max`，不是 Java 注释中的 `memory.swap.high`。
2. Native 事件路径确认 watch 仅绑定 `memory.events`，`getEventCount(kSwapMax)` 返回 0。
3. `initializeMemoryLimits()` 确认 cached 为 `Limits(LIMIT_IS_IGNORED, LIMIT_IS_DISABLED)`。
4. `onLimitExceeded()` 确认 `LIMIT_TYPE_ANON_SWAP` 后解除限制、可选 profiling、再投递 30 秒延迟 kill。
5. XSD 已作为独立 source 加入 frontmatter，因为正文直接引用 vendor XML schema。

## P0 / P1 / P2

- P0: 0。未发现会导致 Android 17 结论反向或误导生产排障的硬错误。
- P1: 0。未发现需登记到 review-findings 的未修复问题。
- P2: 3，均已修复：
  - frontmatter `last_verified` / `last_deep_review_*` 仍停留在上一轮；已更新为本轮 run。
  - `sources` 缺少正文引用的 `memory-limiter-config.xsd`；已补入。
  - 文中 “`memory.limiter` 调用路径” 容易被误读为文件/节点名；已改为 MemoryLimiter 后台 Handler/JNI 调用路径。

## 实际补丁

- 仅修改既有章节文件：`src/part1-fundamentals/ch04-memory/4.18-android-17-memorylimiter-深度解析.md`。
- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 新增本 evidence log 与正式 Obsidian report，不属于章节正文文件。

## 风险与后续

- confidence 保持 `high`，没有因无材料新增而提升。
- `task9_state` 保持 `pending-final-audit`，等待后续 final-audit lane。
- 本轮不引入 Android 18/API 38+ 或 mainline 结论。
