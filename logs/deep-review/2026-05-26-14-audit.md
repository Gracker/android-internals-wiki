# 深度技术 Review 闲时抽检 · 2026-05-26 14:20

## Audit 目标
- 章节：1.11 Zygote 机制与启动性能优化
- 文件：src/part1-fundamentals/ch01-architecture/11-zygote-startup.md
- 状态：finalized → finalized
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖
- 选择原因：Preflight 返回 CANDIDATES=0、IDLE_AUDIT=163，进入闲时抽检；本节缺少 last_task9_audit，且历史 Task9 曾记录 16KB page size / VMA 边界 P2，适合做源码与版本快速复检。
- external-review：logs/external-review/ 根目录最近 3 天无活跃文件，本轮未消费 external-review。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1（P2，既有建议）
- 具体问题：
  1. [P2][既有建议仍成立] L185 — 16KB Page Size 段把 PTE/page-table 规模下降继续写成“fork 期间需要遍历和复制的 VMA 链表条目就越少”。VMA 数量由 mmap 区间决定，page size 不会直接减少 VMA 条目；`dup_mmap` 遍历 VMA 与复制页表是不同成本项。本问题已在 `intake/suggestions.md` 的 2026-05-18 Task9 Deep Review 条目记录，本轮不重复写入。
- 复核通过项：
  1. `nativePreloadAppProcessHALs()` → `GraphicBufferMapper::preloadHal()` → `Gralloc2/3/4/5Mapper::preload()` 口径与 android-16.0.0_r1 源码一致。
  2. android-16 `ZygoteInit.preload()` 的 `PreloadClasses`、`CacheNonBootClasspathClassLoaders`、`PreloadResources`、`PreloadAppProcessHALs`、`PreloadGraphicsDriver`、`HttpEngine.preload()` 锚点与正文一致。
  3. Child Zygote 不使用 USAP Pool 的边界与 android-16.0.0_r1 `ZygoteServer()` / `ZygoteProcess` 注释一致；`ZygoteConfig.USAP_POOL_ENABLED_DEFAULT = false` 支持正文对默认状态的描述。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0（P0/P1）
- 具体结论：
  1. Android 9 版本边界成立：`android-9.0.0_r1` 的 `ZygoteInit.java` 已存在 `PreloadAppProcessHALs`，且仍有 `preloadOpenGL`。
  2. Android 10 版本边界成立：`android-10.0.0_r1` 的 `ZygoteInit.java` 已存在 `CacheNonBootClasspathClassLoaders` 与 `PreloadGraphicsDriver`。
  3. Android 16 当前口径成立：`HttpEngine.preload()` 出现在 android-16.0.0_r1 的 `ZygoteInit.preload()` 中；正文没有发现会影响发布判断的新版本错配。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：1 处（既有 suggestions，不重复入队）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- 写入 queue.json（P85/P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：1 处既有 P2
- 更新章节 frontmatter：`last_task9_audit=2026-05-26`、`last_task9_audit_at=2026-05-26T14:20:00+08:00`、`last_task9_audit_log=logs/deep-review/2026-05-26-14-audit.md`

## 复核素材
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r1/core/java/com/android/internal/os/ZygoteInit.java：Android 16 preload trace 名与 `HttpEngine.preload()`。
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r1/core/jni/com_android_internal_os_ZygoteInit.cpp：`GraphicBufferMapper::preloadHal()` 与 `zygote_preload_graphics()`。
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r1/core/java/com/android/internal/os/ZygoteServer.java：Child Zygote 构造函数禁用 USAP Pool。
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r1/core/java/com/android/internal/os/ZygoteConfig.java：USAP Pool 默认开关。
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-16.0.0_r1/core/java/android/os/ZygoteProcess.java：primary / secondary Zygote 与 child zygote 的 USAP 边界。
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-9.0.0_r1/core/java/com/android/internal/os/ZygoteInit.java：`PreloadAppProcessHALs` / `preloadOpenGL` 版本边界。
- android.googlesource.com/platform/frameworks/base/+/refs/tags/android-10.0.0_r1/core/java/com/android/internal/os/ZygoteInit.java：`PreloadGraphicsDriver` / `CacheNonBootClasspathClassLoaders` 版本边界。
