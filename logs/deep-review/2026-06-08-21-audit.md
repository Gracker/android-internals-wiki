# Task 9 闲时抽检 · 2026-06-08 21:09

## Audit 目标
- 章节：22.11 AnimatedVectorDrawable 线程退化与动画卡顿
- 文件：src/part5-app/ch22-rendering-practice/11-animated-vector-drawable-performance.md
- 模式：闲时抽检
- 抽检范围：维度 1（源码引用）+ 维度 3（版本差异）
- Android 边界：仅使用 Android 17 / API 37 及以下资料；`android-17.0.0_r1` 在 AOSP Gitiles 未命中，实际复核使用章节声明范围内的 `android-16.0.0_r1`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] 正文与参考资料多处写作 `AOSP master`，不符合 AIW 的版本锚点要求。已用 AOSP `android-16.0.0_r1` 复核并替换为固定 tag。

复核证据：
- `AnimatedVectorDrawable()` 构造路径在 `android-16.0.0_r1` 中创建 `VectorDrawableAnimatorRT`。
- `draw(Canvas)` 在软件 Canvas、当前动画器为 RT、动画未运行且存在 pending action 时触发 `fallbackOntoUI()`。
- `forceAnimationOnUI()` 对已在 RenderThread 启动的动画抛出 `UnsupportedOperationException`。
- `VectorDrawableAnimatorRT.onDraw()` 仅在 `canvas.isHardwareAccelerated()` 时记录 `RecordingCanvas` / `RenderNode`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 具体问题：无 P0/P1。

版本复核：
- Android Developers `AnimatedVectorDrawable` reference 明确 API 25 起运行在 RenderThread，早期版本在 UI 线程。
- 章节声明适用范围到 Android 16 / API 36，本轮没有使用 Android 18 / API 38+ 资料。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：1 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：将章节内 AOSP 证据锚点从 `AOSP master` 改为 `AOSP android-16.0.0_r1`。
- Frontmatter：`task9_result: auto-fixed`，`task6_state: revisiting`，`pipeline_stage: task6_pending`，`task2b_state: fixed`，`last_task9_autofix_at: 2026-06-08`，`last_task9_audit: 2026-06-08`。
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- external-review：无本章节最近 3 天活跃文件，未消费归档。
