# 深度技术 Review 闲时抽检 · 2026-06-14 17:20

## Review 目标
- 章节：18.11 ANGLE（GLES-over-Vulkan 翻译层）
- 文件：src/part2-performance/ch18-rendering-pipelines/11-angle-gles-vulkan.md
- 状态：finalized
- 模式：闲时抽检，只审维度 1（源码引用）与维度 3（版本差异）
- 选择原因：当前无 `task9_state: pending` 候选；Task2B backlog 为 0；本节 `last_task9_audit=2026-05-21`，超过 21 天，且历史 Task9 留过 ANGLE 性能证据 P2 建议。
- Android 边界：最高 Android 17 / API 37；未使用 Android 18/API 38+ 材料。`android-17.0.0_r1` tag 本轮复核 404，因此源码锚点使用章节适用范围内的 `android-16.0.0_r1`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][AUTO-FIX] `SyncVk.cpp` 代码片段 — 正文标注 AOSP android-16.0.0_r1 同一逻辑在 L508-L538，但片段仍保留 Chromium/main 口径：`waitSemaphore.get().init(device, VK_SEMAPHORE_TYPE_BINARY)` 与 AOSP 16 不一致，并漏掉 `contextVk->addGarbage(&waitSemaphore.get())`。这会把 semaphore 初始化签名和 handle 生命周期讲错。已直接改为 AOSP android-16.0.0_r1 的 `init(device)` 口径，并补回 `addGarbage()`。
- 证据：
  - AOSP android-16.0.0_r1 `external/angle/src/libANGLE/renderer/vulkan/SyncVk.cpp`：`SyncHelperNativeFence::serverWait()` 位于 L508-L538，使用 `waitSemaphore.get().init(device)`，L537 调用 `contextVk->addGarbage(&waitSemaphore.get())`。
  - AOSP android-16.0.0_r1 `GraphicsEnvironment.java`：`queryAngleChoice()`、`config_angleAllowList`、`getAnglePackageName()` 与正文 Android 16 driver selection 口径一致。
  - AOSP android-16.0.0_r1 `frameworks/native/opengl/libs/EGL/Loader.cpp`：加载顺序为 ANGLE → updated driver → native/system driver，与正文排查边界一致。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0
- 具体问题：无 P0/P1。Android 14 `shouldUseAngleInternal()` / Game Mode 分支、Android 15 `queryAngleChoiceInternal()`、Android 16 `queryAngleChoice()` + `config_angleAllowList` 的版本表复核通过。正文适用范围仍是 Android 10/API 29 - Android 16/API 36，本轮未引入 Android 17 正文结论。

## 统计
- P0 事实错误：1 处（已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：1 处
  - 将 ANGLE `SyncHelperNativeFence::serverWait()` 代码片段改为 AOSP android-16.0.0_r1 源码口径，补回 semaphore garbage 生命周期处理。
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-06-14`、`last_task9_audit: 2026-06-14`
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 归档 external-review：0 处（本轮无活跃 external-review 输入）
