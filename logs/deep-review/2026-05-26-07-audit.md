# Task 9 闲时抽检 · 2026-05-26 07:27

## 抽检目标
- 章节：13.7 Perfetto 的高级用法
- 文件：src/part3-tools/ch13-perfetto/07-advanced-usage.md
- 状态：finalized / ready-to-publish
- 选择说明：本轮 `CANDIDATES=0`、`IDLE_AUDIT=164`，进入闲时抽检模式。`status: finalized` 且 `task9_result: pass-tech-review` 的池子中未找到 `last_task9_at` 超过 21 天的章节；本节有历史 P2 线索，且未记录 `last_task9_audit`，因此选作本轮抽检。
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）。
- external-review：`logs/external-review/` 根目录无最近 3 天活跃文件，本轮未消费。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4.1/5
- P0/P1：0
- 复核结论：旧版 Metric 的 `TraceMetrics` 扩展字段号 450-500、本地 SQL 文件名 / `_output` 视图约定、`trace_processor --run-metrics /path/file.sql` 用法与 Perfetto legacy metrics 文档一致；Trace Summary 的 `trace_summary(specs=[...], metric_ids=[...])` 与 `trace_processor_shell summarize --metrics-v2 ... TRACE SPEC` 也与当前官方文档一致。
- 复核结论：Perfetto UI macro / extension server 的口径与当前文档一致。宏是 Settings > Macros 中的 JSON command 序列，Extension Server 分发 macros、SQL modules、proto descriptors；正文的团队级宏描述没有构成源码/API 错误。
- 既有 P2 仍保留：L180-L194 `cold_start_metric_output` 外层 `FROM cold_start_phases LIMIT 1` 在空结果时不输出 metric proto。建议改为无表外层 SELECT 或保证空 trace 也产出空 repeated field，避免 CI 里把“无启动样本”误判成 metric 运行失败。
- 既有 P2 仍保留：L361-L363 `query_and_flatten` 写“带有一列标识来源 Trace”过于具体。当前官方文档说明会隐式加入来源列，但具体列取决于 resolver；建议改成“会追加 resolver 定义的来源/metadata 列”。
- 既有 P2 仍保留：L751-L765 Custom Data Source 示例仍容易把 `set_render_pass_info()` 理解成普通应用工程可直接生成的方法。官方 SDK 文档明确 custom data source 需要 Trace Processor 侧导入支持；当前正文已写 extension/parser 边界，因此不升 P1。

### 维度 3：版本差异覆盖
- 评分：4.2/5
- P0/P1：0
- 复核结论：L252-L264 旧版 Metric（v1）/ Standard Library / Trace Summarization（v2）的三层关系与当前 Perfetto 文档一致；Trace Summary 作为 legacy metrics successor 的口径仍成立。
- 复核结论：L385-L435 Trace Summary 命令行示例仍符合当前 `summarize` 子命令口径；官方同时保留 classic `--summary --summary-spec --summary-metrics-v2` 调用，因此无需新增版本阻断。
- 既有 P2 仍保留：L809-L810 `ATrace_*` 单次约 50-100ns 缺设备、构建类型、tracing enabled/disabled 条件；Android 平台/Perfetto 版本变化不会改变该数据口径需要来源的事实。建议后续改成定性描述或补基准环境。
- P3 记录：frontmatter `applicable_versions` 仍停在 Android 16/API 36。章节主体讨论的是 Perfetto 工具链，不是单一 Android framework API；发布前可把 `last_verified_against` 增加 `perfetto.dev/docs/analysis/batch-trace-processor`、`ui-automation`、`extension-servers`，并把验证日期更新到本轮抽检日期。

## 证据锚点
- Perfetto Trace-based metrics：`https://perfetto.dev/docs/analysis/metrics`，legacy metrics 明确 `TraceMetrics` 本地字段号 450-500、SQL 文件名/输出视图/extension field 命名约定，以及 `--run-metrics /path/file.sql` 用法。
- Perfetto Trace Summarization：`https://perfetto.dev/docs/analysis/trace-summary`，`trace_summary()` 参数、`trace_processor_shell summarize --metrics-v2 ... TRACE SPEC`、classic summary flags 兼容口径。
- Perfetto Batch Trace Processor：`https://perfetto.dev/docs/analysis/batch-trace-processor`，`query_and_flatten` 会追加来源列，但具体列取决于 resolver。
- Perfetto Tracing SDK：`https://perfetto.dev/docs/instrumentation/tracing-sdk`，TrackEvent 是默认选择；Custom Data Source 需 `DataSourceDescriptor` 注册、TraceConfig 启用，并需要 Trace Processor 侧导入支持。
- Perfetto UI Automation / Extension Servers：`https://perfetto.dev/docs/visualization/ui-automation`、`https://perfetto.dev/docs/visualization/extension-servers`。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0 新增（既有 P2 复核 4 条，未重复写入 suggestions.md）
- P3 锦上添花：1
- 总体技术评分：4.15/5

## 闭环动作
- queue.json：0
- research-gaps.md：0
- suggestions.md：0
- 章节 frontmatter：仅新增 `last_task9_audit: "2026-05-26"`
- 日志：logs/deep-review/2026-05-26-07-audit.md
