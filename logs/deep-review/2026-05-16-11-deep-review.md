# 深度技术 Review · 2026-05-16 11:31

## Review 目标
- 章节：3.7 InputDispatcher 反压与无响应窗口降级
- 文件：src/part1-fundamentals/ch03-input/07-inputdispatcher-backpressure.md
- 状态：finalized（本轮自动晋升）
- 章节：5.1 Linux 进程调度基础
- 文件：src/part1-fundamentals/ch05-cpu-power/01-linux-scheduling.md
- 状态：ready-for-review → task2b_pending
- 章节：5.7 CPU 相关的版本演进
- 文件：src/part1-fundamentals/ch05-cpu-power/07-cpu-evolution.md
- 状态：ready-for-review → task2b_pending

## 审查结果

### 3.7 InputDispatcher 反压与无响应窗口降级

#### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 复核：`frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp` android-16.0.0_r1 路径存在；`startDispatchCycleLocked()` 的 `WOULD_BLOCK` 分支、`mAnrTracker`、`processAnrsLocked()`、`onAnrLocked()`、`shouldPruneInboundQueueLocked()`、`traceInboundQueueLengthLocked()` / `traceOutboundQueueLength()` / `traceWaitQueueLength()` 与正文描述一致。

#### 维度 2：原理链完整性
- 评分：5/5
- 问题数：0
- 复核：反压点 → waitQueue 计时 → unresponsive 隔离 → 跨应用队列裁剪 → dumpsys/Perfetto 观察 → App/Binder/调度归因边界完整。

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：章节以 android-16.0.0_r1 为主锚，Android 10-17 的公开行为未发现冲突；OEM 游戏/触控策略保留 `[待验证]` 边界。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：`iq/oq/wq` counter 与 `dumpsys input` 入口可执行；无硬量化断言缺证据。

#### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0

### 5.1 Linux 进程调度基础

#### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P0] L656-L661 — per-task UClamp 示例使用不存在的 `/proc/<pid>/task/<tid>/util_clamp_min/max` 标准接口。Linux per-task UClamp 应通过 `sched_setattr()` / `uclampset` 设置 `sched_util_min/max`；Android 产品机更常见的是 task profiles 写 cgroup `cpu.uclamp.*`。已写入 queue.json。
  2. [P1] L505-L515 — Perfetto SQL 使用 `thread_state.thread_name`，该字段不存在；应 join `thread` 表取 `utid`。已写入 queue.json。

#### 维度 2：原理链完整性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L538 与 L800 — `sched_base_slice_ns` 偏大/偏小对频繁切换的判断前后矛盾，且 L538 方向与 slice 语义不符。已写入 queue.json。

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] L522-L526 — `sched_base_slice_ns` 默认值应补 Linux v6.6 normalized default 750µs 与多核缩放边界。已写入 suggestions.md。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P2] L351 — Snapdragon 8 Elite 迁移开销 `1.5μs - 3.5μs` / 前代 `10μs+` 缺少测试来源、设备条件和复现口径。已写入 suggestions.md。
  2. [P2] L725-L732 — Perfetto SQL `GROUP BY core_type` 同时 `SELECT cpu`，结果字段不稳定。已写入 suggestions.md。

#### 维度 6：交叉引用一致性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L787-L792 — `sched_wakeup` / `sched_switch` 不直接暴露进程组从 `foreground` 到 `top-app` 的变化，进程名也不会改变。已写入 queue.json。

### 5.7 CPU 相关的版本演进

#### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0
- 复核：frontmatter 中列出的 android-16.0.0_r1 AOSP 路径存在；`UsageStatsManager.java`、`DeviceIdleController.java`、`AppStandbyController.java`、`JobSchedulerService.java`、`task_profiles.json`、`PerformanceHintManager.java` 均可定位。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1] L331-L335 / L388 — Android 17 `Reduced Wakelocks for Idle Alarms` 被写成系统压缩既有 idle alarm WakeLock 持有时间；官方语义是新增 `OnAlarmListener` 版 `setExactAndAllowWhileIdle`，帮助减少持续 partial wakelock 需求。已写入 queue.json。

#### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L196-L217 — Android 9 App Standby Bucket 表混入 `Restricted=45`。AOSP android-9.0.0_r61 无 `STANDBY_BUCKET_RESTRICTED`；需要拆到后续版本差异。已写入 queue.json。
  2. [P2] L331-L341 — Android 17 三项来源分别属于 behavior changes / features / release notes，当前来源口径合并过粗。已写入 suggestions.md。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：1
- 具体问题：
  1. [P2] L317-L327 — Android 16 JobScheduler 配额优化缺 `getPendingJobReasonsHistory()` 和 Perfetto statsd 采集入口。已写入 suggestions.md。

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

## 统计
- P0 事实错误：1 处
- P1 重要缺失/错误：5 处
- P2 建议改进：5 处
- P3 锦上添花：0 处
- 总体技术评分：3.9/5

## 闭环动作
- 写入 queue.json：2 个 pending 条目（5.1 priority 95；5.7 priority 85）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：5 处
- 自动晋升 finalized：3.7
