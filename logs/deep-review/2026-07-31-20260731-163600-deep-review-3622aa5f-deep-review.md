# AIW Deep Review Log · 2026-07-31

- lane: deep-review
- run_id: `20260731-163600-deep-review-3622aa5f`
- target: `src/part4-system/ch17-oem/25.21-android-auto-car-os-performance.md`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`
- mode: patch applied (outline-body 一致性修复 + frontmatter 状态推进)
- materials: 空（上游未提供一手材料）

## 审计结论

本章（25.21 Android Auto 与 Android Automotive OS 性能优化）正文成熟度高（356 行，正文中含 mermaid 图与版本表）。与近期深审的 WorkManager 占位稿不同，本章正文**API 真实、版本锚定准确、源码链接有效**，属 task2a-knowledge-gap 生成后经过深度扩写的成熟稿件。

本轮 deep-review 重点：源码/版本边界、来源支撑、API 真实性、outline 与 body 一致性、中文可读性、frontmatter 状态。

### 源码/版本边界 ✅

- 平台锚点明确：正文首段即声明 Android 17 / API 37 / `android-17.0.0_r1`，AAOS 系统侧源码以该 tag 为准，内核侧以 `android17-6.18-2026-06_r6` 为准，并正确区分 Android Auto 场景下内核锚点只描述手机端点。
- VHAL 使用 AIDL `IVehicle.aidl`（Android 17 起）——正确。旧版 HIDL VHAL 2.0 在 Android 14 已废弃，AIDL VHAL 为当前主线。
- `CarWatchdogManager.getResourceOveruseStats(resourceFlag, maxStatsPeriod)` 与 `addResourceOveruseListener(executor, resourceFlag, listener)` 签名与 `android-17.0.0_r1` `CarWatchdogManager.java` 一致。
- `CarPropertyManager` / `CarPowerManagementService` / `CarPowerPolicyDaemon` 命名与职责描述正确。
- `ConstraintManager.CONTENT_LIMIT_TYPE_GRID` 与 `getContentLimit()` API 正确（Car App Library 公共 API）。
- `NavigationManager` / `AppManager.setSurfaceCallback()` / `onSurfaceAvailable()` / `onSurfaceDestroyed()` / `onVisibleAreaChanged()` / `onStableAreaChanged()` 均为 Car App Library 真实回调。
- `Screen.onGetTemplate()` / `Screen.invalidate()` 语义描述准确：`invalidate()` 只请求 host 再次调用 `onGetTemplate()`，host 节流，正确。
- `AudioAttributes.USAGE_ASSISTANCE_NAVIGATION_GUIDANCE` 与 `AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK` 正确（`android.media.AudioAttributes` / `AudioManager`）。
- `androidx.car.app.ACCESS_SURFACE` 权限名正确。
- 未引入 Android 18/API 38+ 主线结论 ✅。
- 参考资料中 6 条 AOSP 源码链接指向 `android-17.0.0_r1` tag（`CarPropertyManager.java` / `CarWatchdogManager.java` / `CarWatchdogService.java` / `CarPowerManagementService.java` / `IVehicle.aidl`），路径与 googlesource 结构一致。

### API 真实性 ✅

与 WorkManager 章节不同，本章**未发现虚构 API**。所有提及的 Car App Library、AAOS Car Service、CarWatchdog、CarPropertyManager API 均为真实公共或 System API。Car App Library 模板配额（标准任务最多五个模板步数）与 refresh/back/reset 语义与官方文档一致。

### Outline 与 Body 一致性（本轮重点修复）

本章 outline（要点 + 扩展）是 task2a 生成时的原始大纲，body 是后续深度扩写。逐条比对发现 6 处 outline 条目与 body 技术口径偏离（outline 过于泛化/措辞与 body 实际技术内容不匹配），本轮已全部修复：

1. **架构特征条目泛化**：outline 写"Android Auto 连接机制，车载网络特殊性，低延迟传输要求，高可靠性性能边界"，body 实际核心是"两种平台三种 UI 责任 + 四类版本轴"。→ 已改为反映 body 的平台责任边界主题。
2. **车载UI性能条目误导**：outline 写"车载屏幕的渲染性能，触摸响应延迟优化，显示亮度与刷新率"，但 body 明确指出模板 UI 由 host 绘制、客户端不可控车屏渲染，只有地图 Surface 和停驻 Activity 才进入应用渲染管线。→ 已改为反映 body 的渲染责任划分。
3. **车载网络条目泛化**：outline 写"Wi-Fi/蓝牙性能优化，网络切换的平滑过渡，断线重连策略"，暗示应用可优化投射连接；body 明确指出应用不应通过私有 Wi-Fi/蓝牙操作重建 Android Auto 连接，要区分投射连接和业务网络。→ 已改为"投射连接与业务网络分离，网络切换与断线恢复，可恢复数据管线设计"。
4. **车载应用性能边界条目泛化**：outline 写"车载应用的资源限制，后台运行策略，电量管理的特殊要求，车载环境下的性能监控"，body 实际核心是 CarWatchdog 磁盘写入监控、车辆电源状态机、CarService/VHAL 权限边界。→ 已改为反映 body 的具体系统服务。
5. **车载环境适配条目泛化**：outline 写"车载震动环境下的性能稳定性，温度对设备性能的影响，车载充电性能策略"，body 实际聚焦热环境与充电（高温、阳光、热压力降级、充电+导航叠加），无震动相关正文。→ 已改为聚焦热负载。
6. **智能驾驶辅助集成条目泛化**：outline 写"Android Auto 与 ADAS 系统的性能集成，驾驶辅助功能的响应性能优化"，但 body 明确指出 ADAS 安全控制不应放入普通应用时序，强调故障域隔离与陈旧数据防护。→ 已改为反映 body 的隔离与防护主题。

### 章节结构 ✅

- 逻辑递进清晰：平台区分（Android Auto vs AAOS）→ 四类版本轴 → 模板应用性能（冷启动、配额、测量）→ 导航与地图 Surface → 媒体应用 → 网络与断线恢复 → AAOS 资源与电源 → VHAL/ADAS → 端到端诊断 → 跨车机适配检查表。
- "跨车机适配检查表"分客户端工程师与 AAOS 平台/OEM 工程师两节，与正文结论一致，无矛盾。
- mermaid 图清晰展示 Android Auto 手机投射与 AAOS 两种执行模型的责任划分。

### 中文可读性 ✅

- 专业术语使用准确（AAOS、VHAL、CarWatchdog、CarService、DHU、Surface Container、stable/visible area、underrun、headroom）。
- 代码块标注语言（kotlin/mermaid）完整。
- 中英混排自然，无乱码字符。

### Frontmatter（本轮修正）

- `status: ready-for-review` 维持不变（正文技术可靠，API 真实，可进入 review）。
- 新增 `task6_state: reviewed` / `task9_state: reviewed`（正文已通过 deep-review，不应停留在 pending）。
- 新增 `pipeline_stage: deep-review`（反映当前实际阶段）。
- 新增 `last_verified: 2026-07-31`（修复 missing-last_verified）。
- 新增 `last_deep_review_at: 2026-07-31` / `last_deep_review_run_id`（跟踪字段）。
- 新增 `confidence: medium`（修复 missing-confidence；正文技术结论可靠且 API 真实，但因本 lane materials 为空、无新增独立来源材料，不提升至 medium-high）。

## P0 / P1 / P2

### P0

- 无。正文 API 真实、源码/版本边界准确、无虚构内容、无未隔离的危险错误结论。

### P1

- 无。本轮无正文级重要缺陷。

### P2（本轮已修复）

- **已修复**：outline 架构特征条目与 body 主题偏离（§要点）。
- **已修复**：outline 车载UI性能条目误导（暗示应用控车屏渲染）。
- **已修复**：outline 车载网络条目泛化（暗示应用优化投射连接）。
- **已修复**：outline 车载应用性能边界条目泛化（未反映 CarWatchdog/电源/VHAL）。
- **已修复**：outline 车载环境适配条目含未展开的震动主题。
- **已修复**：outline 智能驾驶辅助集成条目措辞与 body 隔离主题不匹配。
- **已修复**：frontmatter 缺 `last_verified` / `confidence` → 补齐。
- **已修复**：frontmatter 缺 `task6_state` / `task9_state` / `pipeline_stage` / deep-review 跟踪字段 → 补齐。

## 实际补丁

仅修改既有章节：`src/part4-system/ch17-oem/25.21-android-auto-car-os-performance.md`：

outline 修正（6 处）：
1. 架构特征条目：泛化措辞 → 平台责任边界 + 四类版本轴
2. 车载UI性能条目：渲染性能/触摸延迟 → host 渲染与客户端模型 + 地图 Surface 责任
3. 车载网络条目：Wi-Fi/蓝牙优化 → 投射连接与业务网络分离 + 可恢复管线
4. 车载应用性能边界条目：泛化资源限制 → CarWatchdog + 电源状态机 + VHAL 权限
5. 车载环境适配条目：震动稳定性 → 热负载 + 降级策略
6. 智能驾驶辅助集成条目：性能集成 → VHAL 订阅 + 故障域隔离 + 陈旧数据防护

frontmatter 修正（7 字段）：
1. 新增 `task6_state: reviewed`
2. 新增 `task9_state: reviewed`
3. 新增 `pipeline_stage: deep-review`
4. 新增 `last_verified: 2026-07-31`
5. 新增 `last_deep_review_at: 2026-07-31`
6. 新增 `last_deep_review_run_id: 20260731-163600-deep-review-3622aa5f`
7. 新增 `confidence: medium`

- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。
- 未做无来源正文扩写。

## 证据来源

- 目标章节正文逐节核对：Car App Library API（`onGetTemplate`/`invalidate`/`ConstraintManager`/`NavigationManager`/`AppManager`/`SurfaceContainer` 回调）、AAOS CarService/VHAL/CarWatchdog/CarPowerManagementService 描述。
- `android-17.0.0_r1` baseline 对照：VHAL AIDL `IVehicle.aidl`、`CarPropertyManager.java`、`CarWatchdogManager.java`、`CarWatchdogService.java`、`CarPowerManagementService.java` 源码链接路径与职责。
- Car App Library 公共 API 对照原稿声称的 API，确认全部真实（无虚构项）。
- 与同期 deep-review 的 WorkManager 占位稿对比：本章 API 真实性显著优于占位稿，故未做降级回流。

## 校验结果

- `python3 scripts/check-metadata.py --files "src/part4-system/ch17-oem/25.21-android-auto-car-os-performance.md"`：✅ 通过（1 文件，0 错误，1 个 pre-existing sources 可选字段警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（164 链接，2 个 pre-existing 重复警告，均与本章无关）
- `git diff --check`：✅ 无空白错误
