# Task 9 闲时抽检 · 2026-06-20 06:26

## 抽检目标
- 章节：18.6 SurfaceView 直出路径
- 文件：src/part2-performance/ch18-rendering-pipelines/06-surfaceview.md
- 模式：闲时抽检
- 范围：维度 1 源码引用 + 维度 3 版本差异
- 选择原因：`last_task9_at=2026-05-29T06:27:31+08:00`，超过 21 天；该节历史上有 SurfaceView BLAST / 挖洞路径 P1/P2 线索，适合复核源码锚点与版本边界。

## 结论
- 发现 3 处可高置信局部修复的问题：正文仍出现 `master` 源码锚点；独立 Layer 概述未先标明 Android 11- / Android 12+ 创建路径边界；`HWComposer.cpp` 引用了 Android 17 中不存在的 `validateLayerCompositionTypes` 名称。
- 未发现 Android 18 / API 38+ 内容；本轮只使用 Android 17 / API 37 及以下资料。
- 未发现需要写入 queue.json 的 P0/P1 技术问题。
- 本轮执行 AUTO-FIX，章节回到 Task6 复审。

## 复核证据
- `android-17.0.0_r1` `frameworks/base/core/java/android/view/SurfaceView.java`：存在 `updateSurface()`、`createBlastSurfaceControls()`、`clearSurfaceViewPort()`、`Canvas.punchHole(...)`、`setAlpha(float)` 与 `mSubLayer` 逻辑。
- `android-12.0.0_r1` `SurfaceView.java`：存在 `createBlastSurfaceControls()`；`android-11.0.0_r1` 同文件未命中 `createBlastSurfaceControls()`，支撑 Android 12+ SurfaceControl / BLAST 创建路径边界。
- `android-16.0.0_r1` / `android-17.0.0_r1` `frameworks/native/libs/gui/BufferQueueCore.cpp`：`notifyBufferReleased()` 仍调用 `mDequeueCondition.notify_all()`；不需要引用 `master`。
- `android-17.0.0_r1` `BufferQueueProducer.cpp` / `BufferQueueConsumer.cpp`：`waitForFreeSlotThenRelock()` 与 `releaseBuffer()` 存在；固定行号会随 tag 漂移，正文已改成类名/方法名锚点。
- `android-17.0.0_r1` `frameworks/native/services/surfaceflinger/DisplayHardware/HWComposer.cpp`：未发现 `validateLayerCompositionTypes`；实际路径是 `getDeviceCompositionChanges()` 调用 HWC `validate()`，再读取 `getChangedCompositionTypes()`。
- 官方图形架构资料：`https://source.android.com/docs/core/graphics/architecture` 作为 overlay / SurfaceFlinger / HWC 背景资料，不作为 Android 18+ 结论来源。

## AUTO-FIX
- frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_verified=2026-06-20`、`last_task9_at=2026-06-20T06:24:54+08:00`、`last_task9_audit=2026-06-20`、`last_task9_autofix_at=2026-06-20`。
- 正文：将开头的 WMS 注册概述改成 Android 11- / Android 12+ 分版本表述，避免与后文 BLAST 路径冲突。
- 正文：删除 `BufferQueueProducer.cpp` / `releaseBuffer()` 的漂移行号，改为 Android 17 类名/方法名锚点，并补 `BufferQueueConsumer.cpp` / `BufferQueueCore.cpp`。
- 正文：将 `android-16.0.0_r1 / master` 改为 `android-16.0.0_r1 / android-17.0.0_r1`。
- 正文：将不存在的 `validateLayerCompositionTypes` 改为 `getDeviceCompositionChanges()` / `validate()` / `getChangedCompositionTypes()` 路径。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0
- AUTO-FIX：3
- queue.json：0
- research-gaps.md：0
- suggestions.md：0
