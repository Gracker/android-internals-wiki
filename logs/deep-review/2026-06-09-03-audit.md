# Task 9 闲时抽检 · 2026-06-09 03:20

## Preflight
- CANDIDATES: 0
- TASK2B_BACKLOG: 0
- IDLE_AUDIT: 293
- 进入闲时抽检模式：status=finalized 且 task9_result=pass-tech-review 的章节中，优先选择无 last_task9_audit 且曾有 P2 建议的章节。

## 抽检目标
- 章节：20.11 MTE memtagMode 与 Native 崩溃治理
- 文件：src/part5-app/ch20-stability/11-mte-memtag-native-crash.md
- 状态：finalized / ready-to-publish
- 上次 Task9：2026-05-16T01:26:00+08:00
- 本次范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖
- Android 版本边界：未使用 Android 18 / API 38+ 材料；AOSP 复核优先使用 android-16.0.0_r3，未把 main/master 作为新增结论来源。

## external-review 输入
- logs/external-review/ 根目录无活跃文件，本轮未消费 external-review，未执行归档。

## 一手资料复核
- Android Developers `android:memtagMode`：`<application>` / `<process>` 支持 `off/default/sync/async`，并给出 debug manifest 合并示例。
- source.android.com MTE configuration：`mte_tcf_preferred` 可在 per-CPU 层把请求 ASYNC 的进程静默升级到 SYNC 或 ASYMM；生产 manifest / Android.bp 不应直接使用 `sync`。
- Android Developers memory-debug：MTE 只适用于 64-bit apps，误报/漏检边界与正文结论一致。
- AOSP android-16.0.0_r3：
  - `frameworks/base/core/java/android/content/pm/ApplicationInfo.java` 定义 `MEMTAG_DEFAULT=-1`、`MEMTAG_OFF=0`、`MEMTAG_ASYNC=1`、`MEMTAG_SYNC=2`。
  - `frameworks/base/core/java/com/android/internal/os/Zygote.java` 定义 `MEMORY_TAG_LEVEL_NONE/TBI/ASYNC/SYNC`，`memtagModeToZygoteMemtagLevel()` 只把 `MEMTAG_ASYNC` 和 `MEMTAG_SYNC` 转成 runtime flag；`getRequestedMemtagLevel()`、`decideTaggingLevel()`、`getMemorySafetyRuntimeFlags()` 仍在该 tag 中存在。
  - `frameworks/base/core/jni/com_android_internal_os_Zygote.cpp` 的 `SpecializeCommon()` 仍按 `runtime_flags` 设置 `M_HEAP_TAGGING_LEVEL_*`，并调用 `mallopt(M_BIONIC_SET_HEAP_TAGGING_LEVEL, heap_tagging_level)`。
  - `bionic/libc/bionic/malloc_common.cpp` 对 `M_BIONIC_SET_HEAP_TAGGING_LEVEL` 调用 `SetHeapTaggingLevel()`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.3/5
- P0/P1：0
- 结论：正文 L116-L139 的类名、方法名、runtime flag、mallopt 参数和 bionic 路径可在 AOSP android-16.0.0_r3 复核到；未发现源码路径/API 名称级事实错误。
- P2 记录：正文校验标签仍写 `AOSP main`，虽然本轮已用 android-16.0.0_r3 复核关键路径，但后续整理时建议把 L116/L137/L139 的验证标签和 frontmatter `last_verified_against` 改为明确 tag，避免违反 2026-05-29 版本锚点偏好。

### 维度 3：版本差异覆盖
- 评分：4.1/5
- P0/P1：0
- 结论：章节适用范围写 Android 12-16，正文没有引入 Android 18/API 38+ 内容；manifest 可见值、ASYMM 非应用 API、设备侧 per-CPU 升级、ASYNC/SYNC 崩溃特征与官方文档一致。
- P2 记录：之前 Task9 提到的 64-bit/arm64 生效边界仍值得补到灰度筛选（正文已在 APM 字段记录 ABI，但 L149 设备筛选未明确“只把 arm64/64-bit 进程纳入 MTE 灰度”）。该点不构成 P0/P1 阻塞。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：2 处（仅抽检日志记录，不新增 queue）
- 总体技术评分：4.2/5

## 闭环动作
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0（闲时抽检无 P0/P1，仅记录日志）
- 章节 frontmatter：已标注 `last_task9_audit: "2026-06-09"`
- 自动晋升 finalized：0（抽检对象已 finalized）
