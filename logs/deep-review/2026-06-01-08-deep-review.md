# 深度技术 Review · 2026-06-01 08:20

## Review 目标
- 章节：22.3 Jetpack Compose 性能优化
- 文件：src/part5-app/ch22-rendering-practice/03-compose-performance.md
- 状态：ready-for-review
- 章节：24.17 Room 3.0 与 SQLiteDriver 迁移性能边界
- 文件：src/part5-app/ch24-io-network/17-room3-sqlitedriver-kmp-performance.md
- 状态：ready-for-review -> finalized
- 章节：20.15 Android 17 Native DCL 只读约束与动态库加载稳定性
- 文件：src/part5-app/ch20-stability/15-android17-native-dcl-stability.md
- 状态：ready-for-review -> finalized

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：4
- 具体问题：
  1. [P0][AUTO-FIX] 22.3 `produceState` 示例与源码锚点 — 原文把 `produceState` 写成 `LaunchedEffect(Unit)` 等价，且示例在 `userId` 变化时不会重启 producer。已改为 `produceState(initialValue, userId)`，并将源码锚点改成 `LaunchedEffect(key...)` 语义。
  2. [P0][AUTO-FIX] 22.3 ProfileInstaller 写入链路 — 原文写成 `/data/data/<pkg>/files/*.prof`、`ProfileInstaller.compileProfile()`、`PROFILE_ADDED` 广播；AOSP/官方 API 显示 current profile 在 `/data/misc/profiles/cur/<user>/<pkg>/primary.prof`，app filesDir 仅保存 skip 标记，receiver action 为 `ACTION_INSTALL_PROFILE` / `ACTION_SAVE_PROFILE`。已直接修正。
  3. [P1][AUTO-FIX] 22.3 Android 17/Compose 工具链边界 — 原文写“Android 17 默认 Compose 工具链约为 1.5.x”，把平台版本和 Jetpack library/compiler 版本绑定。已改为 Android 17 平台不内置 Compose 工具链，Strong Skipping 默认状态由 Kotlin / Compose Compiler 版本决定。
  4. [P2][AUTO-FIX] 22.3 Strong Skipping 非 restartable 边界 — 原文暗示非 restartable Composable 可被 Strong Skipping 独立跳过。已改为非 restartable 不获得独立重启/跳过边界。
- 24.17 Room 3.0 的 release notes、`SQLiteDriver`、`SQLiteConnection`、`SQLiteStatement`、connection pool API、KSP/schema 口径与官方文档一致。
- 20.15 Android 17 Native DCL 的 `System.load()` 只读约束、Android 14 DEX/JAR DCL 顺序与官方文档一致，未引入 Android 18/API 38+ 内容。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][AUTO-FIX] 22.3 `produceState` 因果链 — key 选择会决定协程取消/重启，原文把 `Unit` 和 `userId` 变化同时写入同一结论，存在逻辑冲突。已修复为“使用到且变化后需要重启的数据必须作为 key”。
- 24.17 的“API 迁移 -> 连接/statement 生命周期 -> 构建/schema -> 性能验收”链路完整。
- 20.15 的“文件只读状态 -> 发布顺序 -> 多进程加载 -> 崩溃归因/回滚”链路完整。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] 22.3 Compose 工具链版本边界 — 已拆开 Android API 版本、Compose Foundation 版本、Kotlin/Compose Compiler 版本三条边界。
- 24.17 覆盖 Room 3.0 alpha02/alpha04/alpha05 变化，Room alpha05 仍处 AIW 范围内，不涉及 Android 18/API 38+。
- 20.15 覆盖 Android 14 DEX/JAR DCL 与 Android 17 Native DCL，targetSdk/API 边界清晰。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 22.3 已保留 Pausable Composition/性能数据/Perfetto slice 的待验证标记；当前不新增 research-gaps。
- 24.17 和 20.15 未发现阻塞发布的高优盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] 22.3 `derivedStateOf` 代价口径 — 原文写“对象分配会导致监听判断失准”，缺少源码依据且容易误导。已改为对象分配会增加派生计算成本，但不会让依赖监听失准。
- 22.3 的“View 性能对等/卡顿率 0.2%”仍以存疑/待验证标注保留，不作为正式结论。
- 24.17 的 benchmark、StrictMode、Perfetto 验证指标覆盖迁移验收；20.15 的负样本和上报字段足够支撑灰度门禁。

### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0
- 22.3 与 7.7、2.4、22.1 的定位不冲突；24.17 与 10.7、14.1、19.14、24.2 的边界清晰；20.15 与 8.11、20.3、20.13、1.15 的引用方向一致。

## 统计
- P0 事实错误：2 处（均 auto-fix）
- P1 重要缺失：1 处（auto-fix）
- P2 建议改进：2 处（均 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4.4/5

## 闭环动作
- AUTO-FIX：22.3 共 5 处；章节 frontmatter 更新为 `task9_result: auto-fixed`，回到 Task6 复审
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- 自动晋升 finalized：2 处（24.17、20.15；Task6 已通过，queue 无 pending，本轮无 P0/P1）

## 证据来源
- Android Developers Compose side-effects：`produceState` 使用 `remember { mutableStateOf(initialValue) }` 并在 `LaunchedEffect` 中运行 producer；effect key 变化会取消旧任务并重启。
- Android Developers Strong Skipping：Kotlin 2.0.20 默认启用 Strong Skipping，低于该版本需显式配置。
- AOSP AndroidX `ProfileInstaller.java`：`PROFILE_BASE_DIR = /data/misc/profiles/cur/<user>`，目标文件为 `primary.prof`。
- Android Developers Room 3.0 release notes：3.0.0-alpha05、alpha04 connection pool API、Room 3.0 包名/坐标/KMP/SQLiteDriver 迁移边界。
- Android Developers Android 17 behavior changes：target Android 17/API 37 时，`System.load()` 加载的 native 文件必须只读，否则抛 `UnsatisfiedLinkError`。
