# 深度技术 Review · 2026-06-29 05:25

## Review 目标
- 章节：18.25 Jetpack Compose 渲染管线架构
- 文件：src/part2-performance/ch18-rendering-pipelines/25-compose-rendering-pipeline.md
- 状态：ready-for-review（Task2B 修复后复审；task9_state=pending）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：4（均已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] PausableComposition apply 提交机制 — 正文残留 `resume()` 返回 `Complete` 的旧 API 模型；androidx-compose-release 的 `PausedComposition.resume(ShouldPauseCallback)` 返回 `Boolean`，完成状态由 `true` / `isComplete` 表示。
  2. [P0][AUTO-FIX] RenderThread 同步图 — 时序图仍写成 `fence signal -> MT 释放`；Android 17 `ThreadedRenderer.draw()` 默认只等 HWUI sync/prepare 阶段，`setWaitForPresent` 默认 false，不等 buffer present/GPU fence。
  3. [P0][AUTO-FIX] Snapshot 读写观察 — 状态读追踪误把 `Snapshot.registerApplyObserver` 写成 reader 注册路径；实际 reader 由 Composer / `SnapshotStateObserver` read observer 记录，apply observer 负责写集合通知。
  4. [P0][AUTO-FIX] AndroidView interop invalidation — 章节把 `AndroidView` 写成普通 View invalidation 直接重绘；实际 `AndroidViewHolder.invalidateChildInParent()` / `onDescendantInvalidated()` 调用 `layoutNode.invalidateLayer()`，不进入 Snapshot 读写追踪。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1（已 AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] `AndroidView` 互操作链路缺少 `ComposeNode<LayoutNode, UiApplier>` -> `AndroidViewHolder` -> `AndroidComposeView.addAndroidView()` -> `layoutNode.invalidateLayer()` 这一层，已补入正文。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 具体问题：无。PausableComposition 默认启用状态已收窄为 Compose Foundation 版本/flag 边界；未绑定 Android 17 平台事实。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 具体问题：无新增 P0/P1。GraphicsLayerOwnerLayer / RenderNodeLayer 主路径与回退边界已在前序修复中覆盖。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：1（P2 已有建议，不重复写入）
- 具体问题：
  1. [P2] Perfetto 正常/异常阈值仍缺可复现 trace、设备、刷新率、样本数与统计口径；`intake/suggestions.md` 已有 18.25 条目，本轮未重复写入。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：1（已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] `CompositingStrategy` / offscreen buffer 条件在表格与误区中残留“alpha < 1 + RenderEffect”组合式表述；已按 `CompositingStrategy.Auto` 文档修正为 `alpha < 1`、`RenderEffect`、非默认 `BlendMode`/`ColorFilter` 等条件。

## 统计
- P0 事实错误：5 处（均已 AUTO-FIX）
- P1 重要缺失：1 处（已 AUTO-FIX）
- P2 建议改进：1 处（既有 suggestions.md 条目）
- P3 锦上添花：0 处
- 总体技术评分：4.2/5

## 闭环动作
- AUTO-FIX：6 处局部修复，均有源码依据，单处改动 < 15 行，本轮总改动 < 60 行
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（未重复写入既有 Perfetto 阈值建议）
- frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task2b_state=fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_autofix_at=2026-06-29`
- 自动晋升 finalized：0 处（本轮存在 auto-fix，回到 Task6 复审）

## 证据锚点
- androidx-compose-release：`PausableComposition.kt`（`PausedComposition.resume()` 返回 `Boolean`）、`AndroidView.android.kt`、`AndroidViewHolder.android.kt`、`AndroidComposeView.android.kt`、`SnapshotStateObserver.kt`、`Snapshot.kt`、`GraphicsLayerModifier.kt`、`CompositingStrategy.kt`
- AOSP android-17.0.0_r1：`frameworks/base/core/java/android/view/ThreadedRenderer.java`、`frameworks/base/graphics/java/android/graphics/HardwareRenderer.java`、`frameworks/base/libs/hwui/renderthread/DrawFrameTask.cpp`
