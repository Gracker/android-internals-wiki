# 深度技术 Review 抽检 · 2026-05-23 02:20

## Review 目标
- 章节：2.8 过度绘制
- 文件：src/part1-fundamentals/ch02-rendering/08-overdraw.md
- 状态：finalized
- 模式：闲时抽检（无 task9_state: pending 候选；聚焦维度 1 源码引用 + 维度 3 版本差异）
- 选择原因：last_task9_at=2026-04-30T09:28:00+08:00，距本轮超过 21 天；queue.json 中无该 section pending 条目；最近 3 天无匹配 external-review 活跃文件。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- P0/P1：0
- 核对结论：
  1. AOSP android-16.0.0_r1 中 `frameworks/base/libs/hwui/Properties.h` / `Properties.cpp` 存在，`debug.hwui.overdraw`、`show`、`show_deuteranomaly` 仍可核对；`Properties.cpp` 未启用旧 `count` 分支，章节关于当前主线只保留颜色叠加的描述未发现 P0/P1。
  2. AOSP android-16.0.0_r1 中 `frameworks/base/libs/hwui/pipeline/skia/` 仍以 `SkiaGpuPipeline.cpp`、`SkiaOpenGLPipeline.cpp`、`SkiaVulkanPipeline.cpp` 为主，未发现 Graphite 后端默认启用入口；章节把 Graphite 写为 `[待验证]` 且明确不能写成 Android 16 通用行为，处理合理。
  3. `Canvas.quickReject(..., EdgeType)` 在 API 30 deprecated，`quickReject(RectF)` / `quickReject(float, float, float, float)` 为 API 30；`clipRect(..., Region.Op)` / `clipPath(..., Region.Op)` 在 API 26 deprecated，P 后只允许 `INTERSECT` / `DIFFERENCE` 的表述与当前 API 文档一致。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0/P1：0
- 具体问题：
  1. [P2][版本差异/当前文档口径] “Window 默认背景”一节仍沿用早期经验，写成 root 背景完全覆盖 Window 背景时“它仍然被绘制一次”，并建议直接透明化 Window 背景。2026-03-05 Android 官方 overdraw 文档补充说明：系统优化在很多情况下会避免 app 内部 overdraw，例如 Fragment background 覆盖 window background；同页也更推荐把 window background 设为应用主背景色，并让上层容器不再重复设置背景。建议后续把该段改成条件判断：先用 Debug GPU Overdraw / Layout Inspector 验证是否确实多画，再在“透明 Window 背景”和“保留 Window 主背景、移除容器重复背景”之间取舍，避免把旧版本经验写成 Android 16 通用结论。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1
- P3 锦上添花：0
- 总体技术评分：4.3/5

## 闭环动作
- queue.json：未写入（抽检未发现 P0/P1）
- research-gaps.md：未写入
- suggestions.md：未写入（闲时抽检 P2 仅记录日志）
- 章节 frontmatter：已标注 `last_task9_audit: "2026-05-23"`
- external-review：无可消费文件
