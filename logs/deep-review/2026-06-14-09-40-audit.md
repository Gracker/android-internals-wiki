# 深度技术 Review · 2026-06-14 09:40（闲时抽检）

## Review 目标
- 章节：2.8 过度绘制
- 文件：src/part1-fundamentals/ch02-rendering/08-overdraw.md
- 状态：finalized
- 模式：闲时抽检；本轮只扫维度 1（源码引用）和维度 3（版本差异）
- 选择原因：`last_task9_at=2026-04-30T09:28:00+08:00`，超过 21 天；历史抽检曾记录 P2，适合复核源码引用与版本差异；queue.json 中无该 section pending 条目；最近 3 天无匹配 external-review 活跃文件。

## 一手资料与边界
- Android 17 / API 37 边界：未使用 Android 18/API 38+ 资料。`android-17.0.0_r1` 在 android.googlesource 返回 404，因此源码锚点按规则落在更低版本 `android-16.0.0_r1`。
- AOSP `android-16.0.0_r1`：`frameworks/base/libs/hwui/Properties.h` 声明 `debug.hwui.overdraw`、`show`、`show_deuteranomaly`、`count`；`Properties.cpp` 当前只处理 `show` / `show_deuteranomaly`，未启用旧 counter 分支。
- AOSP `android-16.0.0_r1`：`frameworks/base/libs/hwui/pipeline/skia/` 目录仍以 `SkiaGpuPipeline.cpp`、`SkiaOpenGLPipeline.cpp`、`SkiaVulkanPipeline.cpp` 等文件为主，未发现 Graphite 后端默认启用入口。
- AOSP 历史 tags：`android-4.2.2_r1` 存在 `debug.hwui.show_overdraw`；`android-4.4.4_r1` / `android-5.0.0_r1` 存在 `debug.hwui.overdraw` 与旧 `countOverdraw()` 路径；`android-6.0.0_r1` 起本轮核到的 `Properties.cpp` 不再处理 `count`。
- Android Developers Overdraw / Inspect GPU Rendering 文档（Last updated 2026-05-19）：Debug GPU Overdraw 颜色表仍为 true color、blue、green、pink、red；官方同时提醒 OS 在很多情况下会避免 app 内部 overdraw，例如 Fragment background 覆盖 window background。
- Android Developers Hardware Acceleration / Canvas API：硬件加速下 `clipPath()`、`clipRegion()`、`clipRect(Region.Op.*)`、带 rotation/perspective 的 `clipRect()` 支持边界与 API 18 表一致；`clipRect(..., Region.Op)` / `clipPath(..., Region.Op)` API 26 deprecated、P 后操作限制与正文一致；`quickReject(RectF, EdgeType)` API 1 且 deprecated in API 30，简化重载 API 30。
- Android Developers Compose graphics modifiers：`graphicsLayer` 的 alpha < 1 且未使用 `CompositingStrategy.ModulateAlpha` 时会走 offscreen buffer；`ModulateAlpha` 不会为 alpha < 1 自动创建 offscreen buffer，边界与正文一致。
- Perfetto trace proto：`GpuCounterDescriptor` 由 GPU counter producer 描述硬件可用 counters，counter ID、名称、语义依赖 producer / driver；正文未把 `pixels_drawn` 写成跨厂商标准 counter。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.6/5
- P0/P1：0
- 结论：正文关键源码锚点成立。`Properties.h` / `Properties.cpp` 的 `debug.hwui.overdraw` 当前行为、`pipeline/skia/` 目录的 Graphite 边界、`Canvas.clipRect()` / `quickReject()` API 历史与官方 API reference 均可核；Perfetto GPU counter 的 driver-defined 口径也成立。
- 备注：参考资料区域仍有空 bullet，这是格式/整理问题，不纳入 Task 9 技术问题。

### 维度 3：版本差异覆盖
- 评分：4.1/5
- P0/P1：0
- P2：1
- 具体问题：L168-L187 “Window 默认背景”仍沿用旧经验，写成根布局背景完全覆盖 Window 背景时“它仍然被绘制一次”，并把透明化 Window 背景写成默认解决方法。Android Developers Overdraw 文档 2026-05-19 口径明确提醒 OS 在很多情况下会避免 app 内部 overdraw，例如 Fragment background 覆盖 window background；因此该段更适合改成条件判断：先用 Debug GPU Overdraw / Layout Inspector 验证是否确实多画，再在“透明 Window 背景”和“保留 Window 主背景、移除容器重复背景”之间取舍。该问题为 P2，前轮已记录；本轮闲时抽检按规则只入日志，不改正文、不写 suggestions。
- P3：L127 的历史表述“Android 5.0 之后数值显示被移除”方向可接受，但本轮 AOSP 核对显示 `android-5.0.0_r1` 仍保留旧 `countOverdraw()` 路径，`android-6.0.0_r1` 起 `Properties.cpp` 不再处理 `count`。后续修订时可把文字收窄为“Android 6.0 以后当前主线不再处理旧 counter 分支”，避免读者误读为 5.0 tag 已删除。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1（日志记录）
- P3 锦上添花：1
- 总体技术评分：4.3/5

## 闭环动作
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0（闲时抽检无 P0/P1，按规则只记录日志并更新时间戳）
- 更新 frontmatter：`last_task9_audit=2026-06-14`、`last_task9_audit_at=2026-06-14T09:40:00+08:00`、`last_task9_audit_log=logs/deep-review/2026-06-14-09-40-audit.md`
- external-review：未消费活跃文件
