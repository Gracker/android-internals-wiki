# 闲时深度技术抽检 · 2026-05-19 05:36

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 193
- 模式：闲时抽检（Task 9）

## 抽检目标
- 章节：13.5 专题解读
- 文件：src/part3-tools/ch13-perfetto/05-topic-analysis.md
- 状态：finalized（抽检发现 P0 后已回退到 ready-for-review / task2b_pending）
- 选择理由：无 pending 章节；该章节为 `status: finalized` / `task9_result: pass-tech-review`，`last_task9_at=2026-04-21T07:38:43+08:00`，超过 21 天，且既往 Task9 有 P2 记录。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮不做完整 6 维审查，不处理文风、格式、段落流畅度。

## 核验依据
- Perfetto `docs/data-sources/frametimeline.md`：FrameTimeline Android 12+；`actual_frame_timeline_slice` 字段包含 `jank_type` / `on_time_finish` / `present_type`。
- Perfetto `stdlib/android/binder.sql`：`android_binder_txns` 含 `aidl_name`、`interface`、`method_name`、`client_dur`、`server_dur`；`android_sync_binder_thread_state_by_txn` 字段与正文 SQL 对齐。
- Perfetto `docs/data-sources/native-heap-profiler.md` / `java-heap-profiler.md`：`android.heapprofd`、`heaps: "com.android.art"`、`android.java_hprof` 与 Android 12+ Java allocation sampling 边界成立。
- Perfetto `ProcessStatsConfig` / `system_probes_parser.cc`：周期采样依赖 `proc_stats_poll_ms`；RSS track 名为 `mem.rss*`；PSS 只在 `scan_smaps_rollup=true` 时进入 `mem.smaps.pss*`；内存 counter 在 Trace Processor 中按 bytes 存储。
- Android common kernel `include/trace/events/sched.h`：`sched_blocked_reason` tracepoint 字段为 `pid`、`caller`、`io_wait`。
- Perfetto `stdlib/linux/block_io.sql`：`track.type='block_io'`、`linux_active_block_io_operations_by_device`、`linux_device_major_id()` / `linux_device_minor_id()` 与正文 I/O SQL 对齐。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：3/5
- P0：2
- P1：0
- P2：0

具体问题：
1. [P0][源码错误] L470-L476 / L553-L576 — `linux.process_stats` 被写成默认周期采集 RSS/PSS，SQL 过滤不存在的 `rssanon` track。正确口径：普通 RSS track 是 `mem.rss` / `mem.rss.anon` / `mem.rss.file` / `mem.rss.shmem`；PSS 需要 `scan_smaps_rollup=true` 才有 `mem.smaps.pss*`，且有权限边界。建议补 `proc_stats_poll_ms: 1000`，SQL 改 `mem.rss.anon` 或另开 PSS 小节。
2. [P0][源码错误] L611 — `sched_blocked_reason` 不会给出 block device / sector。该 tracepoint 只有 `pid`、`caller`、`io_wait`；设备与扇区要从 `block_rq_issue` / `block_rq_complete` / 文件系统事件关联。建议修正 I/O 判读链。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0：0
- P1：0
- P2：0

核验结论：
- FrameTimeline Android 12+ 与 Android 10/11 fallback 口径仍成立。
- heapprofd Java allocation sampling Android 12+、Java HPROF 独立数据源、Binder stdlib 字段、block_io stdlib 字段均与当前 Perfetto main 文档/源码对齐。

## 闭环动作
- 写入 queue.json：1 条（P95，`task9-20260519-13.5-perfetto-process-stats-io-source`）
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- 更新章节 frontmatter：`status: ready-for-review`、`pipeline_stage: task2b_pending`、`task9_result: needs-rework`、`task2b_state: pending`、`last_task9_audit: '2026-05-19'`
- 章节状态：从 finalized / ready-to-publish 回退到 Task2B 回炉

## 结论
- 本轮闲时抽检发现 2 个 P0，均属于 Perfetto 数据源/SQL 与内核 tracepoint 字段口径错误。
- 不修改正文内容；已写入 queue.json，等待 Task2B 修正后再进入 Task9 复核。
