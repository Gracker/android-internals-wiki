# 深度技术 Review 闲时抽检 · 2026-05-21 17:28

## Review 目标
- 章节：7.13 SystemUI 性能分析
- 文件：src/part2-performance/ch07-smoothness/13-systemui-performance.md
- 原状态：finalized / pass-tech-review / task9_state=reviewed
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：4
- 具体问题：
  1. [P0] L403-L407 — `NavigationBarController.java` / `NavigationBarControllerImpl.java` 路径写成 `statusbar/phone`，AOSP main 实际在 `packages/SystemUI/src/com/android/systemui/navigationbar/`；Impl 使用 `SparseArray<NavigationBar> mNavigationBars`，不是 `HashMap<Int, NavigationBarView>`。
  2. [P0] L426-L430、L443 — `DisplayContent.supportsSystemDecorations()` / `DisplayContent.shouldShowSystemDecors()` 不是 AOSP main 锚点；当前锚点是 `DisplayContent.isSystemDecorationsSupported()` 和 `DisplayWindowSettings.shouldShowSystemDecorsLocked(DisplayContent)`。
  3. [P0] L432-L437 — `CentralSurfacesImpl.onWallpaperVisibilityChanged(displayId, visible)` 未在 AOSP main 命中；带 `displayId` 的 wallpaper visibility 更新锚点是 `TaskbarDelegate.updateWallpaperVisibility(boolean visible, int displayId)`。
  4. [P0] L210-L212 — WM Shell `DesktopModeController` 未在 AOSP main desktopmode 目录命中；当前可核对锚点是 `DesktopTasksController.kt`、`DesktopDisplayEventHandler.kt`、`DesktopRepository.kt`、`DesktopMode.java` 等。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] L430-L443 — “Android 10+ Secondary Display 仅 NavigationBar/Wallpaper” 口径不能直接覆盖 Android 12-17，尤其 Android 16 desktop windowing 与 `isSystemDecorationsSupported()` 的 force desktop / trusted display 分支需要单独说明。
  2. [P1] L210-L212 — “SystemUI 渲染外部显示器任务栏、多桌面视图、Universal Cursor、CPU/显存阶跃增长”缺少源码/trace/设备基线支撑；应降级为待验证或补官方/trace 证据。

## 证据摘录
- AOSP main：`packages/SystemUI/src/com/android/systemui/navigationbar/NavigationBarController.java` 存在 `NavigationBarView getNavigationBarView(int displayId);`。
- AOSP main：`packages/SystemUI/src/com/android/systemui/navigationbar/NavigationBarControllerImpl.java` 使用 `SparseArray<NavigationBar> mNavigationBars = new SparseArray<>();`。
- AOSP main：`services/core/java/com/android/server/wm/DisplayContent.java` 当前方法为 `isSystemDecorationsSupported()`，并读取 `DisplayWindowSettings.shouldShowSystemDecorsLocked(this)` 与 desktop mode force-enable 分支。
- AOSP main：`packages/SystemUI/src/com/android/systemui/navigationbar/TaskbarDelegate.java` 存在 `updateWallpaperVisibility(boolean visible, int displayId)`；`CentralSurfacesImpl.java` 未命中 `onWallpaperVisibilityChanged`。
- AOSP main：`libs/WindowManager/Shell/src/com/android/wm/shell/desktopmode/` 未命中 `DesktopModeController.java`。

## 统计
- P0 事实错误：4 处
- P1 重要缺失：2 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 条（`task9-audit-20260521-7.13-systemui-multidisplay-source-anchors`，含 4 个 P0 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：`task9_result=needs-rework`、`pipeline_stage=task2b_pending`、`task2b_state=pending`、`last_task9_audit=2026-05-21`
