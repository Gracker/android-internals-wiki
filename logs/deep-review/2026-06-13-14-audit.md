# 深度技术 Review 抽检 · 2026-06-13 14:20

## 抽检目标
- 章节：25.16 ADPF Power Efficiency Mode 与 PowerMonitor 能耗验证
- 文件：src/part5-app/ch25-power-size/16-adpf-power-efficiency-powermonitor.md
- 状态：finalized → ready-for-review / task2b_pending
- 抽检模式：闲时抽检；聚焦维度 1（源码引用）与维度 3（版本差异）
- 选择原因：`CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检；本节 `last_task9_audit` 缺失，且 ADPF / PowerMonitor 历史上多次出现源码与版本口径 P0/P1。
- external-review：根目录无活跃文件，本轮未消费外部 review。

## 审查结果
- 结果：needs-rework
- 技术评分：源码引用 2/5 · 版本覆盖 2/5
- P0/P1/P2/P3：1/2/0/0

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0][源码错误] L368-L374 — 新增扩展把 Power HAL `Boost.ML_ACC` 写成 `ML_ACC = 20`。AOSP android-16.0.0_r1 的 `hardware/interfaces/power/aidl/android/hardware/power/Boost.aidl` 中 `ML_ACC` 没有显式数值，位于 `INTERACTION`、`DISPLAY_UPDATE_IMMINENT` 之后；正文代码块会误导读者按不存在的常量值做适配。
  2. [P1][源码错误/API 边界] L411-L464 — 正文把 `GRAPHICS_PIPELINE` 写成 `SessionTag`，并建议长周期工作“使用 SessionTag 分类”。AOSP `SessionTag.aidl` 只有 OTHER/SURFACEFLINGER/HWUI/GAME/APP/SYSUI；`GRAPHICS_PIPELINE` 属于 `SessionMode.aidl`。公开 `PerformanceHintManager.createHintSession(int[], long)` 没有让普通 App 选择 tag/mode 的参数。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1][版本边界/范围越界] frontmatter L5、L361-L465 — 6/10 注入的“系统层硬件协同设计架构”扩展使用 `main branch` / `AOSP main source snapshot` 作为正文源码锚点。AIW 最高范围只到 Android 17/API 37；仅来自 main/master 的结论不得作为正文结论，必须标注“未进入 Android 17”并跳过。

## 证据摘录
- AOSP android-16.0.0_r1 `hardware/interfaces/power/aidl/android/hardware/power/Boost.aidl`：`ML_ACC` 只有枚举名和注释，没有 `= 20`。
- AOSP android-16.0.0_r1 `hardware/interfaces/power/aidl/android/hardware/power/SessionTag.aidl`：枚举值为 OTHER、SURFACEFLINGER、HWUI、GAME、APP、SYSUI；没有 `GRAPHICS_PIPELINE`。
- AOSP android-16.0.0_r1 `hardware/interfaces/power/aidl/android/hardware/power/SessionMode.aidl`：`GRAPHICS_PIPELINE` 是 session mode，不是 tag。
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/os/PerformanceHintManager.java`：公开 `createHintSession(int[] tids, long initialTargetWorkDurationNanos)` 不暴露 tag/mode 选择参数；`setPreferPowerEfficiency(boolean)` 是 App 侧可表达的能效偏好入口。
- Android 17 源码 tag 本轮未作为可核验公共 tag 命中；未采用 main/master 结论作为 Android 17 正文依据。

## 闭环动作
- 写入 queue.json（P95）：1 条（25.16 系统层扩展回炉）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 更新章节 frontmatter：status=ready-for-review，pipeline_stage=task2b_pending，task9_result=needs-rework，task2b_state=pending，last_task9_audit=2026-06-13
- external-review：无活跃输入，未归档
