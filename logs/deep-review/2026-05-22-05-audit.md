# 深度技术 Review 抽检 · 2026-05-22 05:34

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 186
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## Review 目标
- 章节：5.12 Thermal 管控深度：从内核子系统到 ADPF 主动降频
- 文件：src/part1-fundamentals/ch05-cpu-power/12-thermal-management-deep-dive.md
- 状态：finalized → ready-for-review / task2b_pending（发现 P1，写入 queue）
- 选择原因：`last_task9_at=2026-04-27T10:02:28+08:00`，超过 21 天；本节涉及 Thermal HAL、ADPF headroom、Linux thermal governor / critical trip 源码，属于源码与版本差异容易过期的抽检对象。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界

## 核对资料
- AOSP `hardware/interfaces/thermal/aidl/android/hardware/thermal/IThermal.aidl`：`android-14.0.0_r1` 已有 AIDL 基础接口；`android-15.0.0_r1` 增加 cooling device changed callback；`android-16.0.0_r1` 新增 `forecastSkinTemperature(in int forecastSeconds)`。
- AOSP `frameworks/base/services/core/java/com/android/server/power/ThermalManagerService.java`：Android 16 `TemperatureWatcher#getForecast()` 在 HAL 支持 skin forecast 且只有一路 skin threshold 时优先调用 `mHalWrapper.forecastSkinTemperature()`，否则回落到本地采样 + 线性回归。
- Linux common kernel `drivers/thermal/thermal_core.c`：`android14-6.1`、`android16-6.12`、`android-mainline` 的 critical trip 保护路径符号不同，不能混作同一“Linux kernel 6.1”源码锚点。
- Linux common kernel `drivers/thermal/gov_step_wise.c`：`android14-6.1` / `android16-6.12` / `android-mainline` 的 `get_target_state()` 都包含 `bool throttle` 参数，逻辑受 `throttle` 和 initialized 状态控制。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L13、L172-L176、L200-L225 — Linux thermal 源码锚点混用不同分支。正文标注 Linux kernel 6.1，但 critical trip point 段把 6.1、6.12/mainline 的 `thermal_zone_device_halt()`、`thermal_zone_device_critical_shutdown()`、`__hw_protection_trigger()` 混在一起；`step_wise` 代码片段也缺少当前分支的 `bool throttle` 参数与对应分支逻辑。需要选定 `android16-6.12` 或明确分支差异后重写。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L341-L370、L400-L420、L624-L635 — Thermal HAL AIDL / headroom API 版本链过粗。`forecastSkinTemperature()` 只在 Android 16 AIDL HAL 中出现，Android 14/15 AIDL 基础接口没有该方法；Android 16 Framework 也会在支持时优先走 HAL forecast，而不只是本地 ring buffer 回归。版本演进表需要拆出 Android 14 AIDL 基础、Android 15 cooling callback / thresholds、Android 16 forecast 与 Framework 行为。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：2 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P85）：1 条（`task9-audit-20260522-5.12-thermal-hal-kernel-version-boundary`）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-22
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无

---
# 深度技术 Review 抽检 · 2026-05-22 05:34 · 14.12

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 185
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：0

## Review 目标
- 章节：14.12 APM / 可观测性平台与 SDK 选型
- 文件：src/part3-tools/ch14-other-tools/12-apm-observability.md
- 状态：finalized → ready-for-review / task2b_pending（发现 P0/P1，写入 queue）
- 选择原因：`last_task9_at=2026-04-27T18:35:00+08:00`，超过 21 天；本节曾包含源码调研补充，且 AppExitInfoTracker / KOOM 等源码细节容易随 AOSP tag 变化过期。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界

## 核对资料
- AndroidX `JankStats.kt`（androidx-main）：运行时分支覆盖 API 31 / 26 / 24 / 16 fallback，章节中 `JankStats` 与 `FrameMetrics(API 24+)` 的基础版本边界未发现 P0/P1。
- AOSP `AppExitInfoTracker.java`（android-16.0.0_r1）：`APP_EXIT_STORE_DIR = "procexitstore"`、`APP_EXIT_INFO_FILE = "procexitinfo"`；`KillHandler` 覆盖 `MSG_LMKD_PROC_KILLED`、`MSG_CHILD_PROC_DIED`、`MSG_PROC_DIED`、`MSG_APP_KILL`、`MSG_STATSD_LOG`、`MSG_APP_RECOVERABLE_CRASH`。
- AOSP `core/res/res/values/config.xml`（android-15/16）：`config_app_exit_info_history_list_size` 默认 16。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2.5/5
- 问题数：2
- 具体问题：
  1. [P0] L373-L377 — AppExitInfoTracker 持久化路径与历史条数写错。AOSP android-16 默认文件应落在 `/data/system/procexitstore/procexitinfo`；历史条数由 `config_app_exit_info_history_list_size` 控制，android-15/16 默认 16，不是 8。
  2. [P1] L371、L377、L389-L399 — AppExitInfoTracker 触发链过窄。正文只写 `MSG_PROC_DIED` 主链，遗漏 `MSG_APP_KILL`、`MSG_APP_RECOVERABLE_CRASH`、`MSG_LMKD_PROC_KILLED`、`MSG_CHILD_PROC_DIED` 等来源，容易误导 crash、ANR、系统主动 kill 和 lmkd/zygote 修正路径。

### 维度 3：版本差异覆盖
- 评分：3.5/5
- 问题数：1
- 具体问题：
  1. [P1] L377、L389-L399 — Android 16 的 lmkd 外部来源已向 `onProcDied()` 传入 `rss_kb`，正文仍按较粗的 zygote/lmkd 二来源模型描述，建议补 `android-15/16` 差异或改为版本无关的高层描述。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（`task9-audit-20260522-14.12-appexitinfo-source-path-message-chain`）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-22
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
