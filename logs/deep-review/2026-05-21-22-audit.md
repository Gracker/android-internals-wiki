# 深度技术 Review 抽检 · 2026-05-21 22:26

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 185
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## Review 目标
- 章节：19.14 Jetpack Benchmark（Microbenchmark + Macrobenchmark）
- 文件：src/part3-tools/ch19-apm/14-jetpack-benchmark.md
- 状态：finalized → ready-for-review / task2b_pending（发现 P1，写入 queue）
- 选择原因：`last_task9_at=2026-04-27T10:57:00+08:00`，超过 21 天；本节依赖 AndroidX Benchmark / Baseline Profile API 版本边界，属于源码与版本差异容易过期的抽检对象。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界

## 核对资料
- AndroidX `BaselineProfileRule.kt`（androidx-main）：类注释写明 `BaselineProfileRule` 只在 Android 13 / API 33+ 完整支持，或 rooted Android P / API 28+ 支持；类本身标注 `@RequiresApi(28)`；`collect(packageName, ...)` 是生成 baseline profile 的入口。
- AndroidX `CompilationMode.kt`（androidx-main）：`CompilationMode.DEFAULT` 在 API 24+ 等同 `Partial(BaselineProfileMode.UseIfAvailable)`，API 24 以下为 `Full()`；文件注释写明 Android M / API 23 只有 `Full` 支持。
- AndroidX `BaselineProfileMode.kt`（androidx-main）：`Require` / `UseIfAvailable` / `Disable` 三档，`Require` 缺少 ProfileInstaller 或 APK 内 baseline profile 时会在编译阶段抛错。
- Android Developers Macrobenchmark 文档 / release note：target app 需要 ProfileInstaller 1.3.0+ 才能支持 profile capture / reset 与 shader cache clearing。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0
- 说明：`androidx.benchmark.BlackHole.consume()`、`BenchmarkRule.getState()`、`MacrobenchmarkRule.measureRepeated()`、`CompilationMode.DEFAULT / Partial(BaselineProfileMode.Require)` 的 API 名称与 AndroidX 源码一致；未发现 P0 源码路径或方法签名错误。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L8、L97-L103、L331-L364 — Baseline Profile 生成 / 验证的 API 边界被并入 Macrobenchmark API 23+。`BaselineProfileRule.collect()` 当前只在 API 33+ 完整支持，或 rooted API 28+ 支持；`CompilationMode.Partial(...)` 也只适用于 API 24+，API 23 只有 `Full`。正文和 frontmatter 应拆开“ProfileInstaller/Baseline Profile 收益覆盖低版本设备”与“实验室生成/验证环境”的边界，否则读者会在 API 23-27 设备上按错误前提配置生成或验证脚本。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P85）：1 条（`task9-audit-20260521-19.14-baseline-profile-api-floor`）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-21
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
