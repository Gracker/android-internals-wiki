# 深度技术 Review · 2026-06-01 14:37

## Review 目标
- 章节：20.5 OOM 治理
- 文件：src/part5-app/ch20-stability/05-oom-governance.md
- 状态：ready-for-review -> task6_pending（auto-fixed）
- 章节：20.3 Native Crash 分析与治理
- 文件：src/part5-app/ch20-stability/03-native-crash-governance.md
- 状态：ready-for-review -> task6_pending（auto-fixed）
- 章节：20.1 应用稳定性全景
- 文件：src/part5-app/ch20-stability/01-stability-overview.md
- 状态：ready-for-review -> task6_pending（auto-fixed）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：4
- 具体问题：
  1. [P0][AUTO-FIX] 20.5 ART Heap 源码路径 — `art/runtime/heap.cc` 在 android-16.0.0_r1 不存在，实际为 `art/runtime/gc/heap.cc`；已修正 frontmatter、正文路径和代码块注释。
  2. [P0][AUTO-FIX] 20.5 JNI 源码路径 — `NewStringUTF` 位于 `art/runtime/jni/jni_internal.cc`，不是 `art/runtime/jni_internal.cc`；已修正代码块注释。
  3. [P0][AUTO-FIX] 20.3 sigaction 示例 API 名称 — 示例用 `struct sigaction*` 调用 `dlsym(..., "sigaction64")`，32 位 bionic 中 `sigaction64` 对应 `struct sigaction64*`，存在签名不匹配；已改为从 libc handle 取 `sigaction`。
  4. [P1][AUTO-FIX] 20.1 ContentProvider ANR 超时口径 — 官方 ANR 文档说明查询型 ContentProvider ANR 的 timeout 由 `ContentProviderClient.setDetectNotResponding()` 指定，不是固定 10 秒；已修正表格和机制段落。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：20.5 的 Java heap/native/thread/FD/VA 空间分类，20.3 的 signal → debuggerd/crash_dump → tombstone/minidump 链路，20.1 的稳定性分类主线均能闭合；本轮未发现需要回炉的原理断裂。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] 20.5 heapprofd 版本边界 — Perfetto 官方文档说明 heapprofd requires Android 10 or higher，正文写成 Android 12+；已改为 Android 10+，并补充 user build 上 `debuggable` / `profileable` 条件。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：未发现需要新增 research-gaps 的高优盲区；MTE、16KB page、Native DCL 等专项已有独立章节承接。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：Google Play Vitals 阈值已用官方文档复核；OOM/Native Crash 示例以源码和官方文档作为证据，未发现无来源的硬性数值结论。

### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0
- 说明：20.1 / 20.3 / 20.5 与 20.2、20.4、23.1、23.3、23.4、23.6、1.15 等关联章节均存在；未发现编号不存在或结论冲突。

## 统计
- P0 事实错误：3 处（均 auto-fix）
- P1 重要缺失：1 处（auto-fix）
- P2 建议改进：1 处（auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4.3/5

## 闭环动作
- AUTO-FIX：5 处；20.1、20.3、20.5 frontmatter 已更新为 `task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- 自动晋升 finalized：0 处（auto-fix 后需回到 Task6 复审）

## 证据来源
- AOSP android-16.0.0_r1：`art/runtime/gc/heap.cc` 存在，`art/runtime/heap.cc` 404；`Heap::ThrowOutOfMemoryError` 位于 `runtime/gc/heap.cc`。
- AOSP android-16.0.0_r1：`art/runtime/jni/jni_internal.cc` 包含 `NewStringUTF input has 2^31 or more characters`。
- AOSP android-16.0.0_r1：bionic `signal.h` 中 `sigaction64(int, const struct sigaction64*, struct sigaction64*)`；`sigaction.cpp` 在 64 位下将 `sigaction64` alias 到 `sigaction`，32 位下两者实现路径不同。
- Android Developers ANR 文档（2026-05-19）：Content provider not responding 的默认 timeout 由 `ContentProviderClient.setDetectNotResponding()` 指定。
- Perfetto 官方文档：heapprofd requires Android 10 or higher；user build 限制通常要求 debuggable 或 profileable。
