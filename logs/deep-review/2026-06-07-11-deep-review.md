# 深度技术 Review · 2026-06-07 11:22

## Review 目标
- 章节：3.1 Input 事件分发全流程
- 文件：src/part1-fundamentals/ch03-input/01-input-dispatch.md
- 状态：ready-for-review（跳过深审：已有 Task6 纯结构回炉 queue，pipeline_stage=task2b_pending）
- 章节：18.5 Android View 多窗口渲染路径
- 文件：src/part2-performance/ch18-rendering-pipelines/05-android-view-multi-window.md
- 状态：ready-for-review → task6_pending（auto-fixed）

## 审查结果

### 3.1 Input 事件分发全流程

- 跳过原因：queue.json 已有该 section 的 pending 回炉条目，问题类型为重复 H1、outline、intro 块，属于 Task6/Task2B 结构修复范围；本轮未发现必须在该结构问题修复前抢先处理的 Android 18/API 38+ 风险信号。
- 闭环：不更新该章节 Task9 frontmatter，等待 Task2B 清掉现有 queue 后再深审。

### 18.5 Android View 多窗口渲染路径

#### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 复核：AOSP android-16.0.0_r1 中 `Choreographer.getInstance()` 来自线程局部实例，`ViewRootImpl` 构造时保存 `mChoreographer`；`Dialog.show()` 和 `PopupWindow.invokePopup()` 均走 `WindowManager.addView()`；`RenderThread::getInstance()` 为进程内静态单例。正文关于同进程多窗口、Dialog/PopupWindow 独立 Window/ViewRoot、RenderThread 单例的锚点成立。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1 AUTO-FIX] 完整执行流程 / 典型 Trace 模式 — 原文把 UI Thread 与 RenderThread 的耗时简单相加，并暗示 Window B 必须等待 Window A 的 GPU draw 结束。AOSP android-16.0.0_r1 `DrawFrameTask::run()` 在 `syncFrameState(info)` 后会根据 `canUnblockUiThread` 释放 UI Thread，再继续 `context->draw()`。已修正文中同步边界、示意图说明和 Trace 判读口径。

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 复核：正文最高版本锚点停在 Android 16/API 36；未使用 Android 18/API 38+ 或 main/master 资料作为正文结论。当前远端未检出 `android-17.0.0_r1` tag，本轮按 android-16.0.0_r1 复核。

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 记录：跨进程多窗口除 SF latch/HWC 外，还会受 CPU/GPU 调度、内存带宽和热限制影响；正文已有 Desktop/SystemUI 资源阶跃提示，本轮仅作为 P3 后续增强点记录。

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 复核：已移除“UI 15ms + RT 8ms 勉强控制在 16ms 内”的错误判读，改为以 `syncFrameState`、`queueBuffer`、FrameTimeline present/jank 结果判断。

#### 维度 6：交叉引用一致性
- 评分：4.5/5
- 问题数：0
- 复核：18.2、18.4、2.6、2.14 的相对路径存在且主题匹配。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：4.2/5

## 闭环动作
- AUTO-FIX：1 处（18.5 RenderThread/UI Thread 同步边界与 Trace 判读）
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：0（18.5 已 auto-fix，回到 Task6 复审；3.1 仍有既有 Task2B queue）
- 仅日志记录：1 处
