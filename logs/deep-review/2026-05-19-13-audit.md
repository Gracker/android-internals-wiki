# Task 9 闲时抽检 · 2026-05-19 13:20

## 抽检目标
- 章节：18.19 可变刷新率渲染管线
- 文件：src/part2-performance/ch18-rendering-pipelines/19-variable-refresh-rate.md
- 状态：finalized
- 抽检原因：`last_task9_at=2026-04-24T08:27:00+08:00`，超过 21 天；主题涉及 ARR / VRR、Android 15-QPR1 与 Android 16 API 边界，属于版本差异高风险点。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异覆盖
- 本轮为闲时抽检，不执行完整 6 维度审查。

## 维度 1：源码 / API 引用准确性
- 评分：4.5/5
- P0/P1：0
- 核查结论：文中列出的核心公开 API 和 AOSP 路径可对上当前公开资料。

### 已复核锚点
1. `frameworks/base/core/java/android/view/View.java`
   - Android 16 tag `android-16.0.0_r4` 中存在 `setRequestedFrameRate(float)`、`setFrameContentVelocity(float)`、`REQUESTED_FRAME_RATE_CATEGORY_LOW/NORMAL/HIGH`。
   - `core/api/current.txt` 中这些 API 为 public flagged API：`android.view.flags.toolkit_set_frame_rate_read_only`、`android.view.flags.view_velocity_api`。
2. `frameworks/base/core/java/android/view/Display.java`
   - Android 16 tag `android-16.0.0_r4` 中存在 `hasArrSupport()`、`getSuggestedFrameRate(int)`、`getSupportedRefreshRates()`。
   - `getSuggestedFrameRate(int)` 入参只接受 `FRAME_RATE_CATEGORY_NORMAL` 或 `FRAME_RATE_CATEGORY_HIGH`，章节中“不是任意 fps 映射”的描述准确。
3. `frameworks/base/core/java/android/view/Surface.java`
   - `Surface.setFrameRate(float, int)`、`FRAME_RATE_COMPATIBILITY_FIXED_SOURCE`、`FRAME_RATE_COMPATIBILITY_DEFAULT` 存在。
   - Android 16 还增加了 `FRAME_RATE_COMPATIBILITY_AT_LEAST` 与 `FrameRateParams`，章节没有错误引用。
4. Perfetto stdlib
   - `android.frames.timeline`、`actual_frame_timeline_slice`、`expected_frame_timeline_slice` 在 Perfetto stdlib 文档中存在；章节避免了旧的 `android.frames` / `android_frames.jank_type` 写法。

### 非阻塞观察
- AOSP `core/api/current.txt` 对上述 ARR API 多处带 `@FlaggedApi`。当前章节已把 API 35 投票入口、API 36 查询入口、设备 HAL 支持分开，未构成事实错误。若后续精修，可补一句“以目标 SDK / 设备镜像实际暴露的公开 SDK 为准”，降低读者在 QPR / preview 镜像上复现时的误解风险。

## 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- 核查结论：版本时间线基本准确，覆盖了 Android 11、12-14、15 / 15-QPR1+、16 的主要边界。

### 已复核锚点
1. Android 11：`Surface.setFrameRate()` 作为多刷新率投票入口，章节表述准确。
2. Android 12-14：FrameTimeline 作为慢帧分析主入口，章节表述准确。
3. Android 15 / 15-QPR1+：官方 ARR 文档提到实现需支持特定 HAL API，并运行在 Android 15-QPR1 及以上；章节将 ARR 主体和 Android 11-14 的 mode switching 分开，边界准确。
4. Android 16：Android Developers Blog “Android 16 is here” 明确提到 `hasArrSupport()`、`getSuggestedFrameRate(int)`；章节将 `Display` 查询 API 放在 API 36，准确。

## 抽检结论
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0（未写入 suggestions.md）
- P3 观察：1（仅日志记录：`@FlaggedApi` / QPR 复现边界可在后续精修时补一句）
- 处理：无 P0/P1，按闲时抽检规则仅更新 `last_task9_audit`。
