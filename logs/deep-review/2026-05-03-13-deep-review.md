# 深度技术 Review · 2026-05-03 13:11

## Review 目标
本轮候选池只剩 7.8 / 7.9 两个 `task9_state: pending` 且可审章节；两篇均涉及 AndroidX/AOSP 主线源码、DeliQueue、InputConsumer/Choreographer 等复杂源码链，本轮按规则降为 2 篇。
- 章节：7.8 RecyclerView 列表滑动性能深度优化
- 文件：src/part2-performance/ch07-smoothness/08-recyclerview-performance.md
- 状态：ready-for-review → task2b_pending（needs-rework）
- 章节：7.9 感知流畅性：步幅波动与无掉帧卡顿
- 文件：src/part2-performance/ch07-smoothness/09-perceived-smoothness.md
- 状态：ready-for-review → task2b_pending（needs-rework）

## external-review 检查
- `logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。

## 7.8 RecyclerView 列表滑动性能深度优化

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
  1. [P0] L240-L246 共享 Pool 的锁竞争边界 — 正文称 AndroidX `RecyclerView.RecycledViewPool#getRecycledView()` / `putRecycledView()` 均为 `synchronized`，并据此推导共享 Pool 会出现锁竞争；但 AndroidX `androidx-main` 当前源码中这两个方法没有 `synchronized` 修饰，内部只是操作 `SparseArray<ScrapData>` / `ArrayList<ViewHolder>`，该段源码锚点与结论不成立。 建议：删除“synchronized/锁竞争”结论；如要讨论共享 Pool 边界，改为主线程访问、Pool 默认每 viewType 5 个、create/bind running average 跨共享 Pool 聚合、不同 Adapter 共用 viewType 时可能污染 deadline 估计等可源码验证点。
  2. [P0] L417-L422 GapWorker bindTime 盲区的应对策略 — 正文写“提前 `prepareToDraw()` 触发异步 measure”。AndroidX RecyclerView/ViewHolder 没有可用于 item 异步 measure 的 `prepareToDraw()` API；常见 `Bitmap.prepareToDraw()` 是纹理/GPU 上传预热，不是 View measure。该建议会把图片预上传和布局测量混为一谈。 建议：删除该条，或改为可验证做法：固定 item 尺寸、降低 ConstraintLayout 约束复杂度、用 RecyclerView prefetch / LayoutPrefetchRegistry 覆盖 create/bind，measure 仍需通过 Trace 单独定位。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
  - [P1] L161-L167 Android 17 DeliQueue 对预取调度的影响 — 正文写“多层嵌套 RecyclerView + 高频数据更新场景 P95 掉帧率下降约 4%”，但未给设备、样本、trace 或 benchmark。Google 官方 DeliQueue 博客的口径是 apps targeting SDK 37+ 才启用新 MessageQueue，并给出 “-4% missed frames in apps / -7.7% System UI and Launcher / -9.1% time to first frame P95”，不是 RecyclerView 场景 P95 掉帧率。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0（未发现需要写入 research-gaps.md 的新增高优盲区）

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
  - [P1] L161-L167 Android 17 DeliQueue 对预取调度的影响 — 正文写“多层嵌套 RecyclerView + 高频数据更新场景 P95 掉帧率下降约 4%”，但未给设备、样本、trace 或 benchmark。Google 官方 DeliQueue 博客的口径是 apps targeting SDK 37+ 才启用新 MessageQueue，并给出 “-4% missed frames in apps / -7.7% System UI and Launcher / -9.1% time to first frame P95”，不是 RecyclerView 场景 P95 掉帧率。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

### P2 建议
- [P2] L280、L415-L421 `setHasFixedSize(true)` — `setHasFixedSize(true)` 的语义是 Adapter 内容变化不改变 RecyclerView 自身尺寸，可减少 RecyclerView 级 requestLayout；它不等于 item 内容变化时不重新 measure 子 View，也不能保证动态高度 item 的滚动范围一定正确。 建议：改为“仅当 RecyclerView 自身尺寸不随 adapter 内容变化时使用；动态高度/内容改变场景要实测 scroll range 与 child measure”。

### P3 仅日志记录
- [P3] DiffUtil “只对新加载一页做 diff”建议应限定 append-only / Paging 场景；中间插入仍需全列表或带位置的差异计算。

### 统计
- P0 事实错误：2 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：1 处
- 总体技术评分：3.0/5

## 7.9 感知流畅性：步幅波动与无掉帧卡顿

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
  1. [P0] L323-L345 输入重采样 AIW-源码调研块 — 正文写触摸重采样由 `frameworks/native/libs/input/InputTransport.cpp` 的 `resampleTouchEvent()` 实现，并写 `ro.input.noresample=1` 禁用。AOSP android-15/16/main 中重采样逻辑已在 `frameworks/native/libs/input/InputConsumer.cpp`，关键函数是 `InputConsumer::resampleTouchState()` / `updateTouchState()`，`InputTransport.cpp` 只携带 `coords.isResampled` 等 transport 字段；配置属性为 `ro.input.resampling`，默认 true，设 0 禁用。 建议：把源码锚点改为 `libs/input/InputConsumer.cpp`，方法改为 `InputConsumer::consume()`、`resampleTouchState()`、`updateTouchState()`；把属性改为 `ro.input.resampling=0` 禁用，并补 Android 14 及以前重采样曾在 `InputTransport.cpp` 的版本迁移说明。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
  - [P2] L170-L189 `AnimationUtils.lockAnimationClock()` 代码片段 — 正文引用双参数 `lockAnimationClock(vsyncMillis, expectedPresentationTimeNanos)`，但章节适用范围写 API 29-37；旧版本只有单参数/不含 expectedPresentationTimeNanos 的实现形态。核心毫秒截断结论成立，但源码片段需要按版本标注。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0（未发现需要写入 research-gaps.md 的新增高优盲区）

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：0

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

### P2 建议
- [P2] L170-L189 `AnimationUtils.lockAnimationClock()` 代码片段 — 正文引用双参数 `lockAnimationClock(vsyncMillis, expectedPresentationTimeNanos)`，但章节适用范围写 API 29-37；旧版本只有单参数/不含 expectedPresentationTimeNanos 的实现形态。核心毫秒截断结论成立，但源码片段需要按版本标注。 建议：补一句“Android 15/16/main 为双参数；早期版本只锁定 vsyncMillis，`frameTimeNanos / NANOS_PER_MS` 的毫秒截断行为仍是关键点”。
- [P2] L244-L250 RelativeFrameTimeHistogram 精度限制 — `addRelativeFrameTimeMillis(int)` 的输入单位是整数毫秒，但 AOSP android-16 `RelativeFrameTimeHistogram` 预设 bucket 在 -20ms 到 20ms 附近是 2ms 一档，并非“显式暴露 1ms 精度”的完整统计分辨率。 建议：改成“输入以 ms 为单位，bucket 近 deadline 区域约 2ms 一档；它统计相对 deadline 的帧时间分布，不直接记录位移采样”。

### P3 仅日志记录
- [P3] L295 可补充 `MotionEvent.getEventTimeNano()` 从 API 34 起可用，避免把触摸事件时间精度一概写死为毫秒。
- [P3] L327-L330 常量值建议标注为 android-15/16/main 当前值，避免读者套到更旧分支。

### 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：2 处
- P3 锦上添花：2 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json（P95）：2 个章节，P0/P1 合并为 2 条 pending entry
- 写入 suggestions.md：3 条 P2
- 写入 research-gaps.md：0 条
- 更新章节 frontmatter / progress.json：7.8, 7.9 → needs-rework / task2b_pending
- 自动晋升 finalized：无

## 自动晋升检查
- 7.8：不满足。原因：本轮存在 P0/P1，`task9_result=needs-rework`。
- 7.9：不满足。原因：本轮存在 P0/P1，`task9_result=needs-rework`。

## 证据锚点
- AndroidX `androidx-main` `RecyclerView.RecycledViewPool#getRecycledView()` / `putRecycledView()`：当前无 `synchronized` 修饰。
- AndroidX `GapWorker.postFromTraversal()`：仍通过 `recyclerView.post(this)` 调度，DeliQueue 影响应绑定 Android 17 targetSdk 37+ 行为变更。
- AOSP `android-15.0.0_r1` / `android-16.0.0_r1` / `main`：触摸重采样逻辑位于 `frameworks/native/libs/input/InputConsumer.cpp`，属性为 `ro.input.resampling`。
- AOSP `android-16.0.0_r1` `RelativeFrameTimeHistogram`：bucket endpoints 在 -20ms 到 20ms 附近为 2ms 步长。
