# 深度技术 Review 闲时抽检 · 2026-06-21 16:27

## Review 目标
- 章节：20.3 Native Crash 分析与治理
- 文件：src/part5-app/ch20-stability/03-native-crash-governance.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- Android 边界：仅核对到 Android 17 / API 37；本轮未使用 Android 18/API 38+ 资料
- external-review：最近 3 天无活跃文件

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][AUTO-FIX] 第 123 行 native crash 通知链路写成 `AppErrors.crashApplication()` / `handleApplicationCrash()`。Android 17 源码显示 `crash_dump.cpp::activity_manager_notify()` 连接 `/data/system/ndebugsocket`，`NativeCrashListener.consumeNativeCrashData()` 生成 `NativeCrashReporter`，最终调用 `ActivityManagerService.handleApplicationCrashInner()`；`ActivityManagerService.java` 注释也说明 native crash 使用 inner 版本以避开 AM cleanup lifecycle。已直接修正为 `NativeCrashListener -> handleApplicationCrashInner()`。
- 已复核通过的锚点：
  - `art/sigchainlib/sigchain.cc`：`sigaction()` / `sigaction64()` wrapper 与 `chains[_NSIG]` 仍存在，`AddSpecialSignalHandlerFn()` 会 claim 对应 signal chain。
  - `art/runtime/fault_handler.cc`：`art_sigsegv_handler()` 仍委托 `FaultManager::HandleSigsegvFault()`。
  - `system/core/debuggerd/handler/debuggerd_handler.cpp`：`_Fork()` + `execle(CRASH_DUMP_PATH, ...)` 路径仍成立。
  - `system/core/debuggerd/crash_dump.cpp`：连接 tombstoned、ptrace、`/data/system/ndebugsocket` 通知 AMS 的路径仍成立。
  - `external/google-breakpad/src/processor/simple_symbol_supplier.cc` 与 `basic_source_line_resolver.cc`：`<module>/<debug-id>/<module>.sym` 查找与地址解析锚点仍成立。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 具体结论：
  - Android 12(API 31) 到 Android 17(API 37) 的 `ApplicationExitInfo.getTraceInputStream()` 注释仍说明 `REASON_CRASH_NATIVE` 返回 tombstone protobuf，且可能因全局循环缓冲覆盖返回 null；章节第 192 行描述与 Android 17 源码一致。
  - Android 17 的 debuggerd / crash_dump / NativeCrashListener 链路未发现会推翻正文机制的版本漂移。

## 统计
- P0 事实错误：1 处（已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- AUTO-FIX：1 处
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 回流：`task6_state: revisiting`、`pipeline_stage: task6_pending`
