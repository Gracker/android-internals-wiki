# 深度技术 Review 抽检 · 2026-05-21 21:20

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 186
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## Review 目标
- 章节：2.11 Flutter 渲染管线与性能
- 文件：src/part1-fundamentals/ch02-rendering/11-flutter-rendering.md
- 状态：finalized → ready-for-review / task2b_pending（发现 P0/P1，写入 queue）
- 选择原因：`last_task9_at=2026-04-27T02:20:00+08:00`，超过 21 天；上一轮 Task9 / 日志中存在 P2 记录，适合闲时抽检。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界

## 核对资料
- Flutter Engine main ae5c360：`shell/platform/android`、`impeller`、`fml`、`runtime`、`lib/ui`、`common` 目录全文搜索 `PerformanceHint|APerformanceHint|reportActualWorkDuration|ADPF|HintSession|performance_hint`，无命中。
- Flutter issue #155097：`[Android] Determine if Android Performance Hint Manager is useful`，关闭状态 `not_planned`。
- Flutter Engine main ae5c360 `impeller/renderer/backend/vulkan/capabilities_vk.h` L120-L143、`capabilities_vk.cc` L209-L218：可选 device extension 不含 `VK_EXT_host_image_copy`。
- Flutter Engine main ae5c360 `impeller/renderer/backend/vulkan/texture_vk.cc` L75-L130：`CreateBufferWithCopy()` 创建 staging buffer，随后 `vk_cmd_buffer.copyBufferToImage()`。
- Android Developers `Support 16 KB page sizes`：NDK r28+ 默认 16KB aligned；NDK r27 或更低需额外 linker flags / 构建配置。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2.5/5
- 问题数：2
- 具体问题：
  1. [P0] L248-L258 — Flutter Engine ADPF 接入缺少源码支撑。正文写 Android 16+ / Flutter 3.27+ 自动创建 `PerformanceHintManager` `HintSession` 并逐帧 `reportActualWorkDuration()`，但 engine main ae5c360 相关目录无任何 ADPF / PerformanceHint 命中，且 Flutter issue #155097 已关闭为 `not_planned`。该段应删除或改为 `[待验证]` 平台能力说明。
  2. [P1] L347-L357 — Impeller Vulkan `VK_EXT_host_image_copy` 归因缺少源码支撑。当前 Impeller capability 未启用该扩展，纹理上传仍走 staging buffer + `copyBufferToImage()`；正文不能写成 Flutter 已用路径或稳定 Perfetto 现象。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L347-L357 — Android 16 / Vulkan 1.4 通用能力与 Flutter Impeller 实现版本边界混在一起。45% / 50% 数据若保留，应标注为 Android Developers synthetic Vulkan benchmark，不是 Flutter 实测。
  2. [P1] L175-L184 — 16KB page size 的 NDK 边界写成 NDK ≥ r27，缺少 r28+ 默认对齐、r27 或更低需 linker flags 的差异。该写法会误导 Flutter plugin 原生库适配判断。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：2 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2.8/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（`task9-audit-20260521-2.11-flutter-adpf-host-image-copy`）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-21
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
