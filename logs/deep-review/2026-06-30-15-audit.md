# 闲时技术抽检 · 2026-06-30 15:37

## Review 目标
- 章节：23.5 内存抖动与 GC 治理
- 文件：src/part5-app/ch23-memory-practice/05-memory-churn-gc.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- 选择原因：当前 `CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检；本节 `last_task9_audit=2026-06-08`，距本轮超过 21 天，且 ART GC 触发路径受 Android 17 基线影响。

## 审查证据
- `platform/art` tag：`android-17.0.0_r1` 存在（`fc31298bb626c4b5d53ae3b715c42a2daa802c08`）。
- AOSP android-17.0.0_r1 `art/runtime/gc/heap-inl.h`：`Heap::AllocObjectWithAllocator()` 仍在分配后检查 `ShouldConcurrentGCForJava(new_num_bytes_allocated)`；`ShouldConcurrentGCForJava()` 已从 Android 16 的 bool threshold check 扩展为 `Heap::NeedGc`，包含 `enable_time_based_gc_triggering()`、`time_based_gc_threshold_`、`kNeedGcThresholdCheck` 分支，`concurrent_start_bytes_` 是兜底阈值。
- AOSP android-17.0.0_r1 `art/runtime/gc/heap.cc`：`RequestConcurrentGCAndSaveObject()`、`ConcurrentGCTask`、`task_processor_->AddTask(...)` 路径存在。
- AOSP android-17.0.0_r1 `libcore/libart/src/main/java/java/lang/Daemons.java`：`HeapTaskDaemon` 调用 `VMRuntime.getRuntime().runHeapTasks()`。
- AOSP android-17.0.0_r1 `art/runtime/gc/task_processor.cc`：`TaskProcessor::RunAllTasks()` 取出 `HeapTask` 并执行。
- Android 17 release notes：Performance & Runtime 中列出 ART Concurrent Mark-Compact collector 支持 generational GC。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] L7-L28、L97-L102、L114-L119、L231、L312-L315 — 正文和 frontmatter 仍锚定 `android-16.0.0_r1`，且把 `ShouldConcurrentGCForJava()` 描述为单纯比较 Java 已分配字节数与 `concurrent_start_bytes_`。Android 17 中该函数已加入 time-based GC triggering 分支，原描述会遗漏 Android 17 主线行为。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] L110 — ART 版本演进只写 Android 8/10，未承接 Android 17 release notes 对 CMC generational GC 的公开口径。已补充 Android 17 边界。

## AUTO-FIX
- 修改 `src/part5-app/ch23-memory-practice/05-memory-churn-gc.md` frontmatter：`last_verified_against` 改为 `AOSP android-17.0.0_r1`，AOSP source path 改成锚定 `refs/tags/android-17.0.0_r1` 的 Gitiles URL。
- 修改正文：补充 Android 17 `ShouldConcurrentGCForJava()` 的 time-based GC triggering 分支、`kNeedGcThresholdCheck` 和 `concurrent_start_bytes_` 兜底语义。
- 修改误区段：将“堆达到阈值后加入任务队列”收窄为“`ShouldConcurrentGCForJava()` 判定需要 GC 后加入任务队列”。
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-06-30`。
- 本问题不写入 `queue.json`。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节回到 Task6 复审：1 处
