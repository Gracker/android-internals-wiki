# 深度技术 Review · 2026-04-11 14:27

## Review 目标
- 章节：2.15 DMA-BUF、Gralloc 与跨进程图形内存共享
- 文件：src/part1-fundamentals/ch02-rendering/15-dmabuf-gralloc.md
- 状态：ready-for-review（task6_state: reviewed，task2b_state: pending）

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：3
- 具体问题：
  1. [P0] 行 182-188、200-206 — 把高频路径写成 `queueBuffer()` 携带 GraphicBuffer handle/Binder fd 传给 consumer；AOSP `requestBuffer()` 明确只在 `BUFFER_NEEDS_REALLOCATION` 后同步句柄，正常高频路径是 slot + metadata + fence。
  2. [P0] 行 264-268 — 把 fd 泄漏原因写成“GC 回收前没调 `unflatten()` 清理 fd”；`unflatten()` 是反序列化入口，AOSP 释放路径在 `GraphicBuffer::~GraphicBuffer()` → `free_handle()` / mapper / allocator。
  3. [P0] 行 119-124 — heap 示例路径混入非通用 contract，`system-secure-<vendor>` 写法与官方 vendor-specific `system-secure<vendor-suffix>` 不一致，`default_cma_region` 也不是 AOSP 通用路径。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] 行 182-188、200-206 — 分配链跳过 `dequeueBuffer()` → `BUFFER_NEEDS_REALLOCATION` → `requestBuffer()` → slot mirror，直接把“首次句柄同步”和“每帧提交”混成一个动作。
  2. [P1] 行 198-214 — “一帧零拷贝旅程”缺 BLASTBufferQueue / local consumer / handle cache，无法指导现代窗口路径下的 trace 定位。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] 行 141-145、149 — 正文列的是 legacy `GRALLOC_USAGE_HW_*`，验证锚点却指向 Android 16 AIDL allocator/common，缺少 legacy 到 `BufferUsage` 的版本映射。
  2. [P1] 行 192、317-321 — `android-16.0.0_r1` 同时存在 AIDL allocator 与 HIDL allocator/mapper，正文把它写成“Android 16 开始从 HIDL 迁到 AIDL”过于线性。

### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P1] 缺 BLASTBufferQueue / BufferStateLayer / handle cache 的现代窗口路径说明，读者容易把 app 侧 `queueBuffer()` 与 SurfaceFlinger import 混为一谈。

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] 行 276-294 — “1-5ms 分配延迟”和“不同 heap 缓解带宽竞争”缺设备、SoC、trace/benchmark 证据，建议降级或补实测。

### 维度 6：交叉引用一致性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] 行 190、220 vs §2.13 行 134-154 — 本章把 slot 状态写成单态流转，但 §2.13 已明确当前 AOSP `BufferState` 为 counter-based，shared mode 下状态可叠加。

## 统计
- P0 事实错误：3 处
- P1 重要缺失：3 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：2.0/5

## 闭环动作
- 写入 queue.json（P95）：1 处（升级并合并现有 2.15 pending review 条目）
- 写入 research-gaps.md：1 处
- 写入 suggestions.md：2 处
- 仅日志记录：0 处
