# Task 9 闲时抽检 · 2026-07-08 17:49

## 审计目标
- 章节：2.23 SurfaceFlinger VSync Scheduler 与 DisplayFrameRate 策略
- 文件：src/part1-fundamentals/ch02-rendering/23-vsync-scheduler-displayframerate.md
- 模式：闲时抽检
- 范围：维度 1（源码引用）+ 维度 3（版本差异）
- Android 基线：android-17.0.0_r1 / API 37

## 复核证据
- AOSP android-17.0.0_r1 `frameworks/native/services/surfaceflinger/Scheduler/VsyncSchedule.cpp`：`historySize=20`、`minSamples=6`；VRR + present fence + `use_last_vsync_predict` 时切为 `1/1`；`kDiscardOutlierPercent=20`。
- AOSP android-17.0.0_r1 `frameworks/native/services/surfaceflinger/Scheduler/VSyncPredictor.cpp`：`validate()` 与 model fitting 使用 `kOutlierTolerancePercent`。
- AOSP android-17.0.0_r1 `frameworks/native/services/surfaceflinger/Scheduler/VSyncReactor.cpp`：`periodConfirmed()` 使用独立的 ±10% allowance。
- AOSP android-17.0.0_r1 `frameworks/native/services/surfaceflinger/Scheduler/FrameTimeline.cpp`：`jankTypeBitmaskToProto()` 映射 15 类 jank bit。
- AOSP android-17.0.0_r1 `frameworks/native/services/surfaceflinger/FrameTracer/FrameTracer.cpp`、`Scheduler/FrameTimeline.h/.cpp`、`VSyncDispatchTimerQueue.cpp` 路径存在，正文主要源码路径可解析。
- AOSP android-17.0.0_r1 `frameworks/base/core/api/current.txt`：`Surface.setFrameRate()`、`SurfaceControl.Transaction.setFrameRate()`、`Display.hasArrSupport()`、`Display.getSupportedRefreshRates()`、`View.setRequestedFrameRate()`、`View.setFrameContentVelocity()`、Window touch boost / power savings API 存在于 Android 17 API surface。

## 发现与处理
1. [P2][AUTO-FIX][源码准确性] L280：正文写“13 类 jank bitmask”，但 Android 17 `jankTypeBitmaskToProto()` 实际映射 15 类，且正文列表本身也列出 15 项。已修正为“15 类”。
2. [P2][AUTO-FIX][源码准确性/版本边界] L382/L387：标题把 `VSyncPredictor` 容差写成 10%，与 Android 17 `VsyncSchedule.cpp` 传入的 `kDiscardOutlierPercent=20` 不符；10% 属于 `VSyncReactor::periodConfirmed()` 的周期确认 allowance。已修正标题与 bullet，保留 Reactor 10% 边界。

## 结果
- P0：0
- P1：0
- P2：2（已 auto-fix）
- queue.json：0 条新增
- research-gaps.md：0 条新增
- suggestions.md：0 条新增
- 章节状态：`task9_result=auto-fixed`，`task6_state=revisiting`，`pipeline_stage=task6_pending`
