# AIW Deep Review Log · 2026-07-27

- lane: deep-review
- run_id: `20260727-203535-deep-review-fbe968b7`
- target: `src/part1-fundamentals/ch01-architecture/1.48-Android-17-ResourcesManager-Configuration-Activity-Relaunch-判定模型.md`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`
- mode: patch applied

## 审计结论

本轮执行 OpenClaw-style 技术审计，目标章节存在可安全修复的问题：早期正文包含伪代码、重复结构、无来源性能数字，以及后续注入材料中“Android 17 中 `ActivityRecord#shouldRelaunchLocked()` 已不存在”的错误结论，与同批 DeepResearch 一手源码材料冲突。已将章节收敛为基于 `android-17.0.0_r1` 的可验证模型。

## P0 / P1 / P2

### P0

- **已修复**：移除“Android 17 中 `shouldRelaunchLocked()` 不存在或被重构”的错误段落；章节明确以 `ActivityRecord.java:8740-8798` 的 `shouldRelaunchLocked(int changes, Configuration changesConfig)` 为判定锚点。
- **已修复**：删除占位式 Kotlin/Java 伪代码（例如不存在的 `ResourcesManager.handleConfigurationChanged()`、`ActivityStack.applyLessRecreationStrategy()`），避免读者误以为是 AOSP 实现。

### P1

- **已修复**：删除无 Perfetto/benchmark 证据的百分比性能表与“15-25%/20-30%/5-8x”等不稳健结论；改为源码级机制收益描述。
- **已修复**：把“6 层掩码”校正为包含 `CONFIG_RESOURCES_UNUSED` 快速返回在内的 7 层判定顺序。
- **已修复**：澄清 less-recreation / RecreateOnConfigChangePolicy 的语义：不是无条件少重建，而是可能通过 `skipMask &= ~recreateMask` 反向强制 recreate。

### P2

- **已修复**：去掉重复 `## 要点` 与早期大纲噪声；重排为“主链路 → relaunch 判定 → 性能边界 → 策略读法 → 待验证项”。
- **已修复**：frontmatter 补充 `last_verified`、`confidence`、`task6_state`、`task9_state`、`pipeline_stage`、`last_deep_review_at`、`last_deep_review_run_id`、`sources`。

## 实际补丁

- 仅修改既有章节：`src/part1-fundamentals/ch01-architecture/1.48-Android-17-ResourcesManager-Configuration-Activity-Relaunch-判定模型.md`
- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。

## 证据来源

- `DeepResearch/2026-07-01-android17-resourcesmanager-config-activity-relaunch-pipeline.md`
- `DeepResearch/2026-07-01-android17-relaunch-mask-policies-deep-dive.md`

## 待后续补充

1. PiP enter/exit、drag-resize、RRO 切换的 Perfetto 实测 trace。
2. DisplayCompatPolicy 的两个 display changed flag 的上游赋值路径。
3. `SKIP_ACTIVITY_RECREATION_ON_CONFIG_CHANGE` manifest opt-in 装载链路。
4. RRO `overlayPaths` 与 OverlayManager constraints 的缓存失效语义。
