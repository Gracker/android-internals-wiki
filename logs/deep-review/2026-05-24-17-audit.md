# 深度技术 Review 闲时抽检 · 2026-05-24 17:20

## Audit 目标
- 章节：3.7 InputDispatcher 反压与无响应窗口降级
- 文件：src/part1-fundamentals/ch03-input/07-inputdispatcher-backpressure.md
- 状态：finalized → ready-for-review
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖
- 选择原因：无 task9_state=pending 候选；本节缺少 last_task9_audit，且 InputDispatcher 属于源码/版本高风险章节。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L6、L104、L113-L119、L125-L131、L173-L179 — frontmatter 标注 Android 10-17，但正文按 android-16.0.0_r1/r4 的 InputDispatcher 主线叙述。Android 10 的 `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp` 不存在，实际路径是 `frameworks/native/services/inputflinger/InputDispatcher.cpp`；`docs/anr.md` 在 Android 10/11 标签下也不存在。Android 10 没有 `mAnrTracker`、`processAnrsLocked()`、`shouldPruneInboundQueueLocked()`、`canReceiveForegroundTouches()`，不能把这些锚点写成覆盖 Android 10 的通用实现。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] Android 10-13 / Android 17 边界缺失 — 复核 AOSP：Android 11 引入 `mAnrTracker` 与 `shouldPruneInboundQueueLocked()`；Android 12.1 可见 `processConnectionResponsiveLocked()`；Android 13 可见 `canReceiveForegroundTouches()`；Android 16 r4 仍保留这些核心锚点。AOSP refs 当前无 `android-17.0.0_r1`，章节若继续写 Android 17，需要标为待验证或补预览分支证据。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3/5

## 闭环动作
- 写入 queue.json（P85）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：`task9_result=needs-rework`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-05-24`

## 复核素材
- AOSP android-10.0.0_r47：`services/inputflinger/InputDispatcher.cpp`，旧路径；`mInputTargetWaitCause` / `mInputTargetWaitTimeoutTime` / `onANRLocked()` 路径。
- AOSP android-11.0.0_r48：`services/inputflinger/dispatcher/InputDispatcher.cpp`，出现 `mAnrTracker` / `shouldPruneInboundQueueLocked()`。
- AOSP android-12.1.0_r27：出现 `processConnectionResponsiveLocked()`。
- AOSP android-13.0.0_r83：出现 `canReceiveForegroundTouches()`。
- AOSP android-16.0.0_r4：章节当前主线锚点仍存在。
