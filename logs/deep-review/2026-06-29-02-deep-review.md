# 深度技术 Review · 2026-06-29 02:27

## Review 目标
- 章节：18.25 Jetpack Compose 渲染管线架构
- 文件：src/part2-performance/ch18-rendering-pipelines/25-compose-rendering-pipeline.md
- 状态：ready-for-review（task9_state: pending；queue 中已有 18.25 pending 条目）

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：5
- 具体问题：
  1. [P0] §PausableComposition 控制流（约 246-271 行）— 正文把 PausableComposition 写成 `resume(shouldPause): CompositionResult` + `apply()`；androidx-compose-release 的 `PausableComposition.kt` 中 `PausableComposition` 只提供 `setPausableContent/setPausableContentWithReuse`，返回 `PausedComposition`，`PausedComposition.resume(ShouldPauseCallback): Boolean`，不存在 `CompositionResult`。
  2. [P0] §Checkpoint 插入机制（约 274-315 行）— 正文伪造 `pauseIfNeeded()` / `PauseException` 插入点；`Composer.kt` 未定义这两个符号，`shouldPause` 作为 `InternalComposer.composeContent/recompose` 参数传递，checkpoint 粒度不能按正文断言。
  3. [P0] §与 Choreographer 帧 deadline 的协作（约 317-336 行）— 正文称 Android 14-17 使用 `VsyncCallback → FrameData.getPreferredFrameTimeline().getDeadlineNanos()`。androidx-compose-release 的 `AndroidPrefetchScheduler` 实现 `Choreographer.FrameCallback`，`availableTimeNanos()` 由 `nextFrameTimeNs - System.nanoTime()` 估算，未接入 `VsyncCallback/FrameData`。
  4. [P0] §绘制阶段与 RenderNode 构建 / 误区 4（约 174-183、560-561 行）— 正文把 `graphicsLayer`、独立 RenderNode、离屏缓冲混成同一层概念。`GraphicsLayerModifier` 通过 `placeWithLayer` 创建 graphics layer；`CompositingStrategy`/alpha/RenderEffect 决定是否需要 offscreen buffer，`drawBehind` 复杂度不是创建独立层的源码条件。
  5. [P0] §Snapshot 系统与重组触发（约 228、354、380-395 行）— 正文称 Recomposer 维护 user input 高优先级队列和普通队列并按 priority 排序；`Recomposer.kt` 有 `snapshotInvalidations`、`compositionInvalidations`、`toRecompose/toApply`，未找到 priority 队列模型。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] PausableComposition 的调用方与 runtime 恢复协议之间断裂：正文从 LazyList resume 直接跳到 compiler checkpoint，但缺少 `SubcomposeLayoutState.createPausedPrecomposition()`、`PausedCompositionImpl.resume()`、`performPausableComposition()` 的实际链路。
  2. [P1] graphics layer 与 offscreen buffer 的因果链断裂：正文需要区分 LayoutNode 默认绘制、OwnedLayer/GraphicsLayer 创建、offscreen buffer 三层。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] Compose 1.10 / Android 17 版本结论仍把 PausableComposition 默认启用写成平台事实；应收窄为 Compose Foundation flag/依赖版本事实，不绑定 Android 17 平台。
  2. [P1] `androidx-main` 被作为扩展 6 的正文源码引用（约 581 行），不符合本轮 Android 17 边界。若保留，应改为 `androidx-compose-release` 并注明未进入 Android 17 的 main 资料不得作为正文结论。

### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P2] Perfetto 侧只给 thread/track 名称，未给 Compose tracing 开关、`compose:lazy:prefetch:*` trace value、`actual_recomp/skipped_count` 等可观测信号与正文机制的对应关系。

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P2] 约 518-530 行给出 Snapdragon 8 Gen 2/3 阈值，但没有测试工程、设备、刷新率、样本量、trace 截图或 SQL 依据。
  2. [P2] 扩展 6.8 的 `~10ns/~30ns/1-5µs/10-100µs` 标为“实测推断”，但没有 benchmark 条件，不能作为正文数据结论。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] frontmatter `related_chapters` 均能在 `src/` 中找到，但正文“详见 §22.26”只覆盖 Snapshot 成本，PausableComposition 的 flag 边界与 §22.3/§22.22 的描述不一致，需要 Task2B 对齐。

## 统计
- P0 事实错误：5 处
- P1 重要缺失：4 处
- P2 建议改进：4 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 处（合并到已有 18.25 pending 条目，新增 Choreographer deadline 残留问题；其余 4 个 P0 已在 pending 条目中）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（章节仍有 P0/P1，先进入 Task2B 主修；P2 留在日志中）
- 仅日志记录：4 处
- frontmatter：`task9_state` 更新为 `reviewed`，保持 `task9_result: needs-rework`、`pipeline_stage: task2b_pending`

## 证据索引
- AOSP android-17.0.0_r1 `Choreographer.java`: `FrameCallback.doFrame(long)` 与 `VsyncCallback.onVsync(FrameData)` 分离；`FrameData.getPreferredFrameTimeline().getDeadlineNanos()` 存在，但 Compose 当前 Android prefetch scheduler 未使用该路径。
- androidx-compose-release `PrefetchScheduler.android.kt`: `AndroidPrefetchScheduler` 实现 `Choreographer.FrameCallback`；`nextFrameTimeNs = maxOf(frameStartTimeNanos, viewDrawTimeNanos) + frameIntervalNs`；`availableTimeNanos() = max(0, nextFrameTimeNs - System.nanoTime())`。
- androidx-compose-release `PausableComposition.kt`: `PausedComposition.resume(ShouldPauseCallback): Boolean`，无 `CompositionResult`。
- androidx-compose-release `Composer.kt`: 未找到 `pauseIfNeeded` / `PauseException`。
- androidx-compose-release `Recomposer.kt`: 未找到 priority 队列；使用 `snapshotInvalidations`、`compositionInvalidations` 和 `performRecompose()`。
- androidx-compose-release `GraphicsLayerModifier.kt`: `graphicsLayer` 通过 `placeWithLayer` 放置；`CompositingStrategy` 说明 offscreen buffer 行为。
