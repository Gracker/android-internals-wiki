# 深度技术 Review 抽检 · 2026-05-21 18:20

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 186
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## Review 目标
- 章节：18.11 ANGLE（GLES-over-Vulkan 翻译层）
- 文件：src/part2-performance/ch18-rendering-pipelines/11-angle-gles-vulkan.md
- 状态：finalized → ready-for-review / task2b_pending（发现 P0，写入 queue）
- 选择原因：`last_task9_at=2026-04-27T04:25:00+08:00`，超过 21 天，且无 `last_task9_audit`；上一轮 Task9 记录 P2 1 处，适合闲时抽检。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界

## 核对资料
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/os/GraphicsEnvironment.java`：`queryAngleChoice()`、`config_angleAllowList`、`setupAngleFromApk()` / `setupAngleFromSystem()`、`getAngleDebugPackage()`。
- AOSP android-14.0.0_r1 / android-15.0.0_r1 `GraphicsEnvironment.java`：Android 14 `shouldUseAngleInternal()` / Game Mode 分支，Android 15 `queryAngleChoiceInternal()`。
- AOSP android-16.0.0_r1 `frameworks/native/opengl/libs/EGL/Loader.cpp`：ANGLE namespace、updatable driver namespace、`ro.hardware.egl` 加载边界。
- AOSP android-16.0.0_r1 `external/angle/android/AndroidManifest.xml`：system ANGLE package 为 `com.android.angle`。
- Chromium ANGLE main 与 AOSP android-16.0.0_r1 `external/angle/src/libANGLE/renderer/vulkan/SyncVk.cpp`：`SyncHelperNativeFence::initializeWithFd()`、`serverWait()`、`SyncWaitFd()`、`ANGLE_TRACE_EVENT0`。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P0] L338-L365 — `serverWait()` 的 `dup()` 解释把 ownership transfer 写错。源码是 `dup(mExternalFence->getFenceFd())` 后把复制出来的 fd 交给 Vulkan semaphore import；原 fd 仍由 ANGLE `ExternalFence` 持有。当前“原始 fd 所有权已转移到 VkSemaphore”与源码行为矛盾。
  2. [P2] L369-L371 — Perfetto slice 名称缺少源码支撑。`SyncHelperNativeFence::waitForGpu` 在 `SyncVk.cpp` 中不存在；`serverWait()` 无 `ANGLE_TRACE_EVENT0`，默认不应写成稳定 Perfetto slice 名称。已确认 trace event 名称主要是 `SyncHelperNativeFence::clientWait` / `... block (unlocked)`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] L339-L345 — 代码块行号与签名对应 Chromium ANGLE main；AOSP android-16.0.0_r1 同一逻辑在 L508-L538，且 `Semaphore::init()` 签名不同。行为一致，但正文应标明 tag，避免 Android 16 读者按错误行号核对。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（`task9-audit-20260521-18.11-angle-native-fence-fd-ownership`）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-21
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
