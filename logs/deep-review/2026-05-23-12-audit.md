# Task 9 闲时抽检 · 2026-05-23 12:32

## 抽检目标
- 章节：7.4 典型场景分析
- 文件：src/part2-performance/ch07-smoothness/04-typical-scenarios.md
- 状态：finalized / task9_result=pass-tech-review（抽检前）
- 选择原因：无 pending 候选；本节无 last_task9_audit，且上次 Task9 曾有 P2 建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮为 idle audit，不做完整 6 维度审查。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2.5/5
- 问题数：2
- 具体问题：
  1. [P0] L244、L253-L265 — FragmentTransaction commit 段引用不存在的 `BackStackRecord.mTrack()`，并写出不可核验的 `mHostHandler.execPendingActions()` 链路。AndroidX Fragment 与 AOSP android-16.0.0_r1 平台 FragmentManager 均不支持该方法名；正确锚点应回到 `BackStackRecord.commitInternal()`、`FragmentManager.enqueueAction()`、`scheduleCommit()`、Handler post `mExecCommit`、`execPendingActions()`。
  2. [P2] L309、L314 — `predictive_back_progress` 未核到 Perfetto/AOSP 官方标准 counter，不能作为跨设备稳定观察点。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L305 — “Android 15 将预测性返回动画设为默认行为”版本边界错误。Android 15 是去掉开发者选项且对已 opt-in app/activity 显示系统动画；Android 16/API 36 target + Android 16 device 才是默认启用 predictive back system animations。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3/5（抽检范围）

## 闭环动作
- 写入 queue.json（P95）：1 条（7.4）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处（`predictive_back_progress`）
- 更新章节 frontmatter：`task9_result=needs-rework`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-05-23`

## 复核证据
- AndroidX Fragment `BackStackRecord.java`：`commitInternal()` / `enqueueAction()` / `generateOps()`，无 `mTrack()`。
- AndroidX Fragment `FragmentManager.java`：`scheduleCommit()` post `mExecCommit`，`execPendingActions()` 处理 pending actions。
- AOSP android-16.0.0_r1 platform `FragmentManager.java`：`enqueueAction()` 位于 1903 起，`scheduleCommit()` 通过 `mHost.getHandler().post(mExecCommit)`，`execPendingActions()` 位于 2060 起。
- Android Developers Predictive Back：Android 15 起开发者选项不再可用，但 system animations 仍针对 opted-in app/activity；Android 16 behavior changes 明确 API 36 target + Android 16 device 默认启用。
