# 深度技术 Review · 2026-05-08 10:29

## Review 目标
- 章节：3.2 触摸响应的性能分析
- 文件：src/part1-fundamentals/ch03-input/02-touch-performance.md
- 状态：ready-for-review → task2b_pending
- 章节：11.2 App 耗电优化
- 文件：src/part2-performance/ch11-power/02-app-power-optimization.md
- 状态：ready-for-review → finalized / ready-to-publish

## 选择说明
- 严格候选池仅剩 2 个 `task9_state: pending` 章节：3.2、11.2；默认 3 章规则因候选不足降为 2 章。
- `logs/external-review/` 根目录无本轮可消费 active external-review 文件。
- 两节均已在 10:05 通过 Task6 light edit，本轮只复核技术闭环。

## 审查结果

### 3.2 触摸响应的性能分析

#### 维度 1：源码引用准确性
- 评分：3.0/5
- 问题数：P0 1
- 具体问题：
  1. [P0][源码错误] L130-L145 InputReader.loopOnce / QueuedListener.flush() — 正文写 `QueuedListener.flush()` / `mQueuedListener->flush()`。复核 AOSP android-16.0.0_r1 `frameworks/native/services/inputflinger/reader/InputReader.cpp`：`loopOnce()` 使用 `mPendingArgs += processEventsLocked(...)`，`std::swap(notifyArgs, mPendingArgs)` 后在锁外逐个 `mNextListener.notify(args)`；当前文件未见 `mQueuedListener` 路径。
  - 建议：按 android-16.0.0_r1 重写代码片段；若保留 `QueuedListener.flush()`，必须标注旧版本边界。

#### 维度 2：原理链完整性
- 评分：3.4/5
- 问题数：P1 1
- 具体问题：
  1. [P1][Trace 观测错误/原理断裂] L164 InputDispatcher 队列表现 — 正文仍写 iq/oq/wq “以 Slice 的形式”出现。AOSP `InputDispatcher.cpp` 的 `traceInboundQueueLengthLocked()`、`traceOutboundQueueLength()`、`traceWaitQueueLength()` 均使用 `ATRACE_INT` 写队列长度 counter；后文 L285 已写成计数器，前后冲突。
  - 建议：统一为 counter / 计数器轨道；不要把 WaitQueue 堆积当作 batching 直接证据。

#### 维度 3：版本差异覆盖
- 评分：4.0/5
- 问题数：0 个新增；`PointerCoords.isResampled()` 已限定 API 35+，MotionPredictor API 34+ 与 stylus 边界复核通过。

#### 维度 4：知识盲区
- 评分：4.0/5
- 盲区数：0 个新增；InputReader 旧/新监听器分发模型归入 P0 回炉，不单独写 research-gaps。

#### 维度 5：数据与案例支撑
- 评分：3.8/5
- 问题数：0 个新增；HCI 阈值与 16KB 输入链路收益已保留待验证边界。

#### 维度 6：交叉引用一致性
- 评分：3.8/5
- 问题数：P1 1（同 L164：前文 Slice 与后文 counter 口径冲突）。

### 11.2 App 耗电优化

#### 维度 1：源码引用准确性
- 评分：4.4/5
- 问题数：0；`setAlarmClock()` exact alarm 权限、`PerformanceHintManager.Session.setPreferPowerEfficiency()`、FGS timeout 异常路径、Camera2 close 链复核通过。

#### 维度 2：原理链完整性
- 评分：4.3/5
- 问题数：0；WorkManager 调度与 ADPF 运行时 hint 已拆清，FGS `shortService` ANR 与 `dataSync/mediaProcessing` crash 路径已拆清。

#### 维度 3：版本差异覆盖
- 评分：4.3/5
- 问题数：0；Android 12 exact alarm、Android 14 FGS type / exact alarm 默认授权、Android 15 time-limited FGS 与 API 35 ADPF 边界复核通过。

#### 维度 4：知识盲区
- 评分：4.0/5
- 盲区数：0 个新增。

#### 维度 5：数据与案例支撑
- 评分：4.0/5
- 问题数：0 个阻断项；截图类 `[待补充]` 不属于本轮技术阻断。

#### 维度 6：交叉引用一致性
- 评分：4.2/5
- 问题数：0；与 §11.1、§5.4、§5.6、§5.10、§11.5 的交叉引用口径一致。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.0/5

## 闭环动作
- 写入 queue.json（P95）：1 条（3.2 InputReader 源码片段 + InputDispatcher counter 观测口径）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 仅日志记录：0 条
- 更新 frontmatter/progress：3.2 → `task2b_pending`；11.2 → `pass-tech-review` 并自动晋升 `finalized` / `ready-to-publish`
- 自动晋升 finalized：11.2

## 复核依据
- AOSP android-16.0.0_r1 `frameworks/native/services/inputflinger/reader/InputReader.cpp`：`loopOnce()` 中 `mPendingArgs` / `notifyArgs` / `mNextListener.notify(args)`。
- AOSP android-16.0.0_r1 `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp`：`ATRACE_INT("iq")`、`ATRACE_INT("oq:...")`、`ATRACE_INT("wq:...")`。
- Android SDK android-35 `MotionEvent.PointerCoords.isResampled()`、`AlarmManager` exact alarm permission 注释、`PerformanceHintManager.Session.setPreferPowerEfficiency()`。
- AOSP android-16.0.0_r1 `ActiveServices.java`：`onFgsCrashTimeout()` / `ForegroundServiceDidNotStopInTimeException` 与 short FGS ANR timer。
