# 深度技术 Review · 2026-05-07 18:28

## Review 目标
- 章节：8.4 其他响应速度场景
- 文件：src/part2-performance/ch08-responsiveness/04-other-scenarios.md
- 状态：ready-for-review → task2b_pending
- 章节：15.6 性能测试最佳实践
- 文件：src/part3-tools/ch15-methodology/06-testing-best-practices.md
- 状态：ready-for-review → finalized / ready-to-publish

## 选择说明
- 本轮符合 `status in [ready-for-review, finalized, ready-to-publish]` 且 `task9_state=pending` 的有效章节只有 8.4、15.6，因此按规则降为 2 节。
- `logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。
- 两节均在 2026-05-07 18 点 Task6 复审通过；本轮只审技术事实，不处理文风。

## 审查结果

### 8.4 其他响应速度场景
- 维度 1：源码引用准确性 2.5/5
  - [P0] L93 — Android 9+ / Android 16 Activity launch 入口不是 `scheduleLaunchActivity()`，而是 `ClientTransaction` / `LaunchActivityItem` 经 `IApplicationThread.scheduleTransaction()` 分发；`scheduleLaunchActivity()` 只覆盖 Android 8.x 旧路径。
  - [P0] L97 — `DEFAULT_MAX_BINDER_THREADS` 被写成 16。AOSP android-16.0.0_r1 `frameworks/native/libs/binder/ProcessState.cpp` 实际为 `#define DEFAULT_MAX_BINDER_THREADS 15`；`BINDER_VM_SIZE` 约 1MB 减 2 个 page 正确。
  - [P2] L115 — AndroidX Fragment 当前执行路径应写 `FragmentManager` pending actions → `BackStackRecord.executeOps()`，不是 `BackStackRecord.execute()`。
- 维度 2：原理链完整性 3.5/5
  - [P1] L222 — ViewPager2 不提供公开 API 替换内部 `LinearLayoutManagerImpl`；“自定义 LayoutManager 微调 prefetch”是不可执行/不稳定建议。
  - 既有 P2：`flatMapLatest` 取消边界与 InputChannel fd/Looper 描述已在 `intake/suggestions.md` 2026-05-06 记录，本轮不重复新增。
- 维度 3：版本差异覆盖 3.0/5
  - Activity launch path 需要拆 Android 8.x 与 Android 9+；当前用 Android 8.x 方法覆盖 Android 16。
- 维度 4：知识盲区 4.0/5
  - 无新增高优 research gap。
- 维度 5：数据与案例支撑 3.5/5
  - Binder 1-5ms、layout inflate 30-100ms 等仍建议后续用 trace 样例增强；未构成本轮 P1。
- 维度 6：交叉引用一致性 4.0/5
  - related_chapters 目标均可定位。

### 15.6 性能测试最佳实践
- 维度 1：源码引用准确性 4.0/5
  - [P2] L498-L521 — Macrobenchmark JSON schema 描述不准。AndroidX `BenchmarkData.kt` 使用 `metrics` / `sampledMetrics` map，metric name 是 key；采样指标字段是 `P50/P90/P95/P99`，不是稳定的 `metricName` / 小写 `p90` 字段；`BenchmarkData` 为 `@RestrictTo(LIBRARY_GROUP)` 内部 schema。
- 维度 2：原理链完整性 4.5/5
  - 前轮 P1（Macrobenchmark 自动稳定化）已收敛到 IsolationActivity、sustained performance mode 与 side effects 边界，未发现新的 P0/P1。
- 维度 3：版本差异覆盖 4.5/5
  - CompilationMode、thermalservice、background dexopt 的版本边界已覆盖。
- 维度 4：知识盲区 4.5/5
  - 无新增高优 research gap。
- 维度 5：数据与案例支撑 4.0/5
  - P90/FPS 方向已修正；报告示例已使用 FPS P10 / 慢帧占比。
- 维度 6：交叉引用一致性 5.0/5
  - §14.6、§15.5、§8.3、§13.2、§5.5 均可定位。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：1 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：8.4 = 3.2/5；15.6 = 4.4/5

## 闭环动作
- 写入 queue.json（P95）：1 条（8.4，含 2 个 P0 + 1 个 P1）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：2 处（8.4 Fragment 调用链；15.6 Macrobenchmark JSON schema）
- 仅日志记录：0 处
- frontmatter：8.4 → `task9_result=needs-rework`、`task9_state=reviewed`、`task2b_state=pending`、`pipeline_stage=task2b_pending`
- frontmatter：15.6 → `task9_result=pass-tech-review`、`task9_state=reviewed`、`status=finalized`、`pipeline_stage=ready-to-publish`
- 自动晋升 finalized：15.6 性能测试最佳实践
