# 深度技术 Review · 2026-05-08 17:40

本轮审计 3 个 `task9_state: pending` 且正文充分章节：7.1、13.9、14.11。1.0 为 README 且正文不足 100 行，按规则跳过。
`logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。
memory_search 因 embedding/provider 配置错误不可用，本轮以项目文件、AOSP/Perfetto 公开源码与本地复核为准。

## Review 目标
- 章节：7.1 卡顿的定义与分类
- 文件：src/part2-performance/ch07-smoothness/01-jank-definition.md
- 状态：ready-for-review → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L299-L356 Binder SQL 佐证 — [P0] `android_frames` 是 Perfetto stdlib `android.frames.timeline` 模块里的表，使用前需要 `INCLUDE PERFETTO MODULE android.frames.timeline;`。该表字段为 `ui_thread_utid` / `render_thread_utid`，没有 `utid` 列；当前 SQL 两处写 `af_ui.utid`，并用 `af.id = af_ui.actual_frame_timeline_id` 后再取不存在列，实际执行会报 no such column / no such table，无法定位 Binder 事务。 建议：在 SQL 顶部加入 `INCLUDE PERFETTO MODULE android.frames.timeline;`；用 `android_frames f ON af.id = f.actual_frame_timeline_id`，直接取 `f.ui_thread_utid AS ui_utid`、`f.render_thread_utid AS rt_utid`，第二个 breakdown 查询复用同一 CTE，避免复制错误。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：0

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：1 处

## Review 目标
- 章节：13.9 Android Tracing 基础设施：atrace、ftrace 与 Perfetto 数据采集原理
- 文件：src/part3-tools/ch13-perfetto/09-tracing-infrastructure.md
- 状态：ready-for-review → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L123 Android 常用 tracepoint 分类表 — [P0] 表格列出 `drm_atomic_commit` 作为 DRM ftrace tracepoint。公开 Linux DRM trace 定义与 Perfetto ftrace event registry 中可见 `drm_vblank_event`、`drm_sched_job`、`drm_run_job` 等事件，未见 `drm_atomic_commit`；读者按 `drm/drm_atomic_commit` 写 TraceConfig 会采集失败。 建议：替换为公开 GKI/Perfetto 可采集的 DRM 事件；如果该名字来自厂商私有内核，需标注“厂商内核特有，GKI 不保证存在”。
  2. [P0] L225、L423 `ftrace_drain_period_ms`；L110/L149/L209 `TraceConfig.ftrace_events` — [P0] Perfetto proto 字段是 `FtraceConfig.drain_period_ms`，完整路径为 `TraceConfig.ftrace_config.drain_period_ms`；源码 `ftrace_controller.cc` 中读取的是 `ds->config().drain_period_ms()`，不存在 `ftrace_drain_period_ms`。同理，`ftrace_events` 不是 `TraceConfig` 顶层字段，而是 `TraceConfig.ftrace_config.ftrace_events`。 建议：全文统一为 `TraceConfig.ftrace_config.ftrace_events` 与 `TraceConfig.ftrace_config.drain_period_ms`；源码描述中不要引入不存在的 `ftrace_drain_period_ms` 名称。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] L212 `buffer_size_kb` 通常 32-128MB — `FtraceConfig.buffer_size_kb` 是每 CPU kernel ftrace ring buffer，Perfetto v43+ 文档建议多数配置不显式设置，示例常见 4096KB lower bound；“通常 32-128MB”缺少来源，且容易被理解为每 CPU 巨大缓冲区。 建议：改为“按场景设置，v43+ 多数配置可留空；若需兼容旧版本，可用 `buffer_size_kb: 4096` + `buffer_size_lower_bound: true` 作为起点”，并补 Perfetto proto 注释来源。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

## 统计
- P0 事实错误：2 处
- P1 重要缺失：0 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处
- 仅日志记录：0 处

## Review 目标
- 章节：14.11 Battery Historian 与功耗分析工具
- 文件：src/part3-tools/ch14-other-tools/11-battery-historian.md
- 状态：ready-for-review → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L391-L392 PowerMonitor 类型常量 — [P0] AOSP android-15.0.0_r1 / android-16.0.0_r1 `frameworks/base/core/java/android/os/PowerMonitor.java` 中定义为 `POWER_MONITOR_TYPE_CONSUMER = 0`、`POWER_MONITOR_TYPE_MEASUREMENT = 1`。正文写 `MEASUREMENT=0x1` 正确，但把 `CONSUMER` 写成 `0x2`，会误导读者做类型判断。 建议：修正为 `POWER_MONITOR_TYPE_CONSUMER = 0`、`POWER_MONITOR_TYPE_MEASUREMENT = 1`；如果只讲语义，避免手写十六进制值。
  2. [P0] L448 PowerStatsService 路径与 Android 15 引入断言 — [P0] `PowerStatsService.java` 实际位于 `frameworks/base/services/core/java/com/android/server/powerstats/PowerStatsService.java`，不是 `com/android/server/power/stats/`。该类在 android-12.0.0_r1 已存在（Copyright 2020），并非 Android 15 才引入；`power/stats/` 目录承载 BatteryStatsImpl、PowerCalculator/Collector/Processor 等模型代码。 建议：改成“PowerMonitor API 在 API 35 向应用层开放；内部 PowerStatsService 更早已存在，并与 BatteryStatsImpl/功耗模型代码并存”，同时给出两个正确目录：`powerstats/PowerStatsService.java` 与 `power/stats/*PowerStats*.java`。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：0

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] L542 ADPF power efficiency “功耗降低 15-30%” — “功耗降低 15-30%”是量化收益断言，但正文未给设备、负载、CPU cluster、温控状态、测试窗口或来源；`setPreferPowerEfficiency` 本身是 hint，收益高度依赖平台调度实现。 建议：删掉固定百分比，或补一组明确实验条件；更稳妥写成“可能降低能耗，但需要用 PowerMonitor/Perfetto rail 做 A/B 验证”。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

## 统计
- P0 事实错误：2 处
- P1 重要缺失：0 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处
- 仅日志记录：0 处

# 本轮汇总
- P0：5 处；P1：0 处；P2：2 处；P3：1 处。
- 自动晋升 finalized：无。三节均有 P0，已转入 Task2B 回炉。
- 下一 Review 候选：当前严格候选池只剩短 README 1.0；等待 Task2B/Task6 产生新候选。
