# 深度技术 Review 闲时抽检 · 2026-05-26 05:35

## Review 目标
- 章节：2.1 Android 渲染架构全景
- 文件：src/part1-fundamentals/ch02-rendering/01-rendering-overview.md
- 状态：finalized（抽检后转 ready-for-review / task2b_pending）
- 选择原因：当前无 `task9_state: pending` 候选；本节 `last_task9_at=2026-05-07T17:29:52+08:00`，曾有 P2 建议，适合抽检源码引用与版本差异。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0
- 具体问题：无 P0/P1。抽检核对 `View.getDefaultSize()`、`Choreographer.doFrame()` callback 顺序、`RenderNode` staging/display list 成员、Skia OpenGL/Vulkan pipeline 与 SurfaceFlinger `scheduleComposite()` / `commit()` / `composite()` / `present()` 主路径，当前 Android 16 源码锚点基本一致。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1][版本差异] L622-L626 — 版本演进把 Android 3.0 硬件加速与 DisplayList/RenderNode 架构合并描述。AOSP android-4.4.4_r2 的 `libs/hwui` 目录有 `DisplayList` / `DisplayListRenderer`，但没有 `RenderNode.h` 或 `renderthread`；`RenderNode.h`、`RenderNode.cpp` 与 `renderthread` 目录出现在 android-5.0.0_r1。当前写法会让 API 11-20 的 HWUI 架构被误读成已经具备 Android 5.0 之后的 RenderNode/RenderThread 分工。建议拆成：Android 3.0 = 硬件加速、HWUI、早期 DisplayList/OpenGLRenderer；Android 4.1 = VSync / Choreographer / 三缓冲；Android 5.0 = RenderNode + RenderThread + staging display list 同步路径。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json（P85）：1 条
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 更新 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`task2b_result: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-05-26`

## 复核依据
- AOSP android-16.0.0_r1 `View.java`、`Choreographer.java`、`RenderNode.h/.cpp`、`RecordingCanvas.h`、`pipeline/skia/`、`SurfaceFlinger.cpp`。
- AOSP android-4.4.4_r2 `frameworks/base/libs/hwui/` 目录：存在 `DisplayList` / `DisplayListRenderer`，无 `RenderNode.h` / `renderthread`。
- AOSP android-5.0.0_r1 `frameworks/base/libs/hwui/` 目录：出现 `RenderNode.h`、`RenderNode.cpp`、`renderthread`。
- Android AOSP Vulkan 文档：Android 16 Vulkan 1.4 设备基线成立，但不表示 HWUI 平台级默认切 Vulkan。
