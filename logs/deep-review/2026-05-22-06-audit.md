# Task 9 闲时抽检 · 2026-05-22 06:24

## 审计目标
- 文件：src/part1-fundamentals/ch06-storage/01-storage-architecture.md
- 章节：6.1 Android 存储架构
- 模式：闲时抽检（无 task9 pending 候选；聚焦维度 1 源码引用 + 维度 3 版本差异）
- 上次 Task9：2026-04-28T16:21:00+08:00

## 抽检依据
- AOSP `system/vold/FsCrypt.cpp` android-16.0.0_r1：`install_storage_key()` / `fs_prepare_user_storage()` / `prepare_dir_with_policy()`
- AOSP `system/vold/Utils.cpp` android-16.0.0_r1：`BuildDataSystemDePath()`、`BuildDataMiscDePath()`、`BuildDataUserDePath()`、`BuildDataSystemCePath()`
- AOSP `system/core/libprocessgroup/profiles/task_profiles.json` android-15/16：`LowIoPriority`、`HighIoPriority`、`SCHED_SP_FOREGROUND`、`SCHED_SP_TOP_APP`
- AOSP `system/core/libprocessgroup/profiles/cgroups.json` android-16：`blkio` 位于 `/dev/blkio`，cgroups2 默认 controller 为 freezer/memory
- Android 官方 FBE / metadata encryption 文档

## 发现问题

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
1. [P0] L297-L309 — FBE 密钥层次的目录和伪源码片段与 AOSP 不一致。正文写 System DE Key 用于 `/data/system/`、`/data/misc/`，示意代码写 `installKey("scrypt_key_system_de", "/data/system_de/")`。AOSP `Utils.cpp` 实际生成 `/data/system_de/<user>`、`/data/misc_de/<user>`、`/data/user_de/<user>`、`/data/system_ce/<user>`、`/data/user/<user>`；`FsCrypt.cpp` 通过 `install_storage_key()` 与 `prepare_dir_with_policy()` 安装 policy，不存在正文这种源码调用形态。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
1. [P1] L138-L140、L388-L389 — I/O 优先级段落把 AOSP blkio/task profiles 口径写成 cgroup v2 IO controller。AOSP android-15/16 默认 `cgroups.json` 仍有 `blkio` `/dev/blkio`，cgroups2 默认 controller 为 freezer/memory；`task_profiles.json` 通过 `LowIoPriority` 加入 `blkio/background`，前台/Top App profile 不等于“cgroup v2 I/O 请求标记”。

## 闭环动作
- 写入 `metadata/queue.json`：1 条（priority 95，含 P0/P1 两个 issue）
- 更新章节 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-05-22`
- 未修改章节正文

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 抽检结论：needs-rework
