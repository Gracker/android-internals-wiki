# Task 9 闲时抽检 · 2026-06-20 07:28

## 抽检目标
- 章节：2.17 Frame Pacing Library 与帧节奏控制
- 文件：src/part1-fundamentals/ch02-rendering/17-frame-pacing.md
- 状态：finalized / ready-to-publish → ready-for-review / task2b_pending
- 选择说明：Preflight 返回 `CANDIDATES=0`、`TASK2B_BACKLOG=0`、`IDLE_AUDIT=309`，进入闲时抽检模式。本节 `last_task9_at=2026-05-08T05:27:25+08:00`，`last_task9_audit=2026-05-26`，均超过 21 天；历史 Task9 保留过 P2，且正文依赖 AGDK/Swappy、Perfetto FrameTimeline、Android 17 MessageQueue 行为变更。
- 抽检范围：维度 1（源码/官方引用准确性）+ 维度 3（版本差异覆盖）。
- external-review：`logs/external-review/` 根目录无最近 3 天活跃文件，本轮未消费 external-review。

## 复核依据
- AOSP `frameworks/base` `android-17.0.0_r1`：`core/java/android/os/CombinedDeliMessageQueue/MessageQueue.java`、`core/java/android/os/LegacyMessageQueue/MessageQueue.java`、`MessageStack.java`、`MessageHeap.java`。
- AOSP `external/perfetto` `android-17.0.0_r1`：`src/trace_processor/metrics/sql/android/jank/frames.sql` 与 `android_frame_timeline_metric.sql`。
- AOSP `platform/frameworks/opt/gamesdk` refs：`android-17.0.0_r1`、`android-16.0.0_r1`、`android-15.0.0_r1` 下 `games-frame-pacing/common/SwappyCommon.cpp` 等文件 URL 均返回 404；可取到 `refs/heads/main` 与 `refs/heads/android-games-sdk-games-frame-pacing-release`，但这些不是 Android 17 platform tag。
- Khronos Vulkan Registry：`VK_KHR_present_wait` 依赖 `VK_KHR_present_id`；二者仍需 feature/extension 查询，不能仅凭 Vulkan 版本推断 Swappy 已接入。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：2.5/5
- 问题数：1（P0）
- 具体问题：
  1. [P0][源码错误] L405/L411 — 正文引用 `frameworks/base/core/java/android/os/MessageQueue.java` 作为 Android 17 DeliQueue 实现，但该路径在 `android-17.0.0_r1` 返回 404。实际文件拆为 `CombinedDeliMessageQueue/MessageQueue.java` 与 `LegacyMessageQueue/MessageQueue.java`。正文还写“`MessageQueue.nativePollOnce()` 和其他线程的同步操作共用一把 `mLock`”，但 Android 17 的 DeliQueue 文件没有这个 `mLock` 字段；legacy 路径是在 `nativePollOnce()` 后进入 `synchronized (this)` 读写队列。建议回炉改成 Android 17 的真实路径与 monitor 竞争描述。

### 维度 3：版本差异覆盖
- 评分：2.5/5
- 问题数：1（P1）+ 1（P2）
- 具体问题：
  1. [P1][版本边界] frontmatter `last_verified_against`、L64-L181、L393 — 章节大量 Swappy 关键结论依赖 `platform/frameworks/opt/gamesdk refs/heads/main` 或“当前 main 分支”。本轮复核该仓库的 Android 17/16/15 platform tag 文件均取不到，只有 main/gamesdk release branch 可用。按 AIW Android 17 范围规则，main/master 资料未证明进入 Android 17 时只能标注“未进入 Android 17”，不得作为正文结论。建议回炉改用可证明的 AGDK release 资料，或将 main-sourced 细节降级为待验证/研究备注。
  2. [P2][扩展依赖] L399 — `VK_KHR_present_wait` 依赖 `VK_KHR_present_id`，正文虽已说明 Swappy 未接入二者，但仍可补一句依赖关系与 feature/extension 查询边界。该问题不阻塞本轮 P0/P1 回炉。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5

## 闭环动作
- 写入 queue.json（P95）：1 条（合并 P0/P1 两个 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter：`status=ready-for-review`、`task9_result=needs-rework`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-06-20`
- 章节正文：未修改
