# 深度技术 Review 抽检 · 2026-05-21 06:36

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 195
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## Review 目标
- 章节：18.12 Flutter 渲染管线
- 文件：src/part2-performance/ch18-rendering-pipelines/12-flutter-rendering.md
- 状态：finalized → task2b_pending（发现 P0/P1，写入 queue）
- 选择原因：`last_task9_at=2026-04-26T13:26:21+08:00`，超过 21 天，且无 `last_task9_audit` 记录；文件当前无 pending queue 条目。

## 抽检证据
- Flutter 官方 Architectural overview（当前 docs）：Flutter 3.29 起 iOS/Android 合并 UI 与 platform 线程；UI thread removed，Dart code runs on the native platform thread。章节对 merged model 的主线判断成立。
- Flutter 官方 Impeller 文档（当前 docs）：3.27 起 Impeller 是 iOS 与 Android API 29+ 默认渲染引擎；低于 API 29 或设备不支持 Vulkan 时回退到 legacy OpenGL renderer。章节对 Impeller 默认边界成立。
- Flutter Android embedding Javadoc / engine main `RenderMode.java`：`RenderMode` 当前包含 `surface`、`texture`、`image` 三个枚举值；`image` 通过 `ImageReader` + `Canvas` 绘制，并服务于需要 PlatformView 完整交互的场景。
- Flutter engine main `FlutterView.java` / `FlutterImageView.java` / `PlatformViewsController.java`：`FlutterView(Context, FlutterImageView)` 存在；`convertToImageView()` 会切到 `FlutterImageView`；`FlutterImageView` API 29+ 使用 `Image.getHardwareBuffer()` + `Bitmap.wrapHardwareBuffer()`，overlay 由 `PlatformOverlayView extends FlutterImageView` 承载。
- Flutter 官方 Platform Views 文档（当前 docs）：除 Hybrid Composition 与 Texture Layer Hybrid Composition 外，新增 Flutter 3.44+ 实验性 Hybrid Composition++（HCPP），要求 Android API 34+ 与 Vulkan，不满足条件时回退到既有策略。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] §`FlutterImageView` 是另一个维度的承载 L218-L225 — 当前 `RenderMode.java` 已包含 `image` 枚举；正文“只有 surface/texture、FlutterImageView 不是 RenderMode 枚举值”的判断与当前 Javadoc/engine main 不一致。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] §Platform Views 嵌入 L229-L241 — Flutter 3.44+ 官方文档新增 HCPP，Android API 34+/Vulkan 门槛明确；章节适用范围是 Flutter 3.29+，但没有记录该版本差异。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（task9-audit-20260521-18.12-flutter-rendermode-hcpp-source）
- queue 动作：新增 pending 条目（task9-audit-20260521-18.12-flutter-rendermode-hcpp-source）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-21
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
