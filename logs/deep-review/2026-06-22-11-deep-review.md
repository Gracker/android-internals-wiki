# 深度技术 Review · 2026-06-22 11:30

## Review 目标
- 章节：20.12 SafeMode 崩溃循环判定与启动补偿链路
- 文件：src/part5-app/ch20-stability/12-safemode-crash-loop-recovery.md
- 状态：ready-for-review
- external-review：无（logs/external-review 根目录本轮未发现活跃文件）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：4（均已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] `tombstoned` 临时文件创建 — 正文沿用旧版 `.temporary<N>` fallback；android-17.0.0_r1 `create_temporary_file()` 只使用 `O_TMPFILE`，失败走 `PLOG(FATAL)`。
  2. [P0][AUTO-FIX] `RecoverySystem.installPackage` BCB 链路 — 正文误写成 `IBootloader.setBootControl()`；android-17.0.0_r1 是 `RecoverySystemService.setupBcb()` 拉起 init `setup-bcb` 服务并通过 `/dev/socket/uncrypt` 发送 command。
  3. [P0][AUTO-FIX] `AtomicFile.finishWrite()` 注释 — 正文把 `FileUtils.sync()` 标成 `fdatasync`；源码实际是 `FileDescriptor.sync()` / `fsync(2)`。
  4. [P0][AUTO-FIX] `AppExitInfoTracker` 查询维度 — 正文误写五元组定位；源码是 packageName + uid 定位容器、pid 过滤、timestamp 排序，realUid 用于映射和外部信号匹配。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：修正后 crash 文件状态机、BCB 控制文件、AppExitInfoTracker 参照关系能够闭合。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：移除“API 26 及以上行为一致”的未验证跨版本断言，限定 android-17.0.0_r1 源码锚点。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：本轮未发现需要新增 research-gaps 的高优盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：本文是机制设计章节，现有示例足够支撑技术判断。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：SUMMARY 与 20.7、20.10、26.2 等引用未发现阻断性错误。

## 统计
- P0 事实错误：4 处（均已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：4 处
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- frontmatter：task9_result=auto-fixed，task9_state=reviewed，task6_state=revisiting，pipeline_stage=task6_pending
- 自动晋升 finalized：0（本轮有 auto-fix，且 queue.json 中 20.12 仍有 Task6 pending 条目）

## 一手证据
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/util/AtomicFile.java`：`finishWrite()` 调用 `FileUtils.sync(str)`，`failWrite()` 也会 sync/close/delete `.new`。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/os/FileUtils.java`：`sync(FileOutputStream)` 调用 `stream.getFD().sync()`。
- AOSP android-17.0.0_r1 `system/core/debuggerd/tombstoned/tombstoned.cpp`：`create_temporary_file()` 使用 `O_TMPFILE`，失败 `PLOG(FATAL)`；提交路径是 `unlinkat` + `linkat`。
- AOSP android-17.0.0_r1 `frameworks/base/services/core/java/com/android/server/recoverysystem/RecoverySystemService.java`：`setupBcb()` → `setupOrClearBcb()` → init `setup-bcb` → `/dev/socket/uncrypt`。
- AOSP android-17.0.0_r1 `frameworks/base/services/core/java/com/android/server/am/AppExitInfoTracker.java`：`getExitInfoLocked(packageName, uid, pid)`、`AppExitInfoContainer.getExitInfosLocked()` 按 timestamp 倒序返回。
