# AIW Deep Review Log

- run_id: `20260809-123557-deep-review-6fc42164`
- lane: `aiw-polish-deep-review`
- target: `src/part5-app/ch20-stability/07-exception-architecture.md`
- base_sha: `7e241b3e9e342c4db6737aeb3144f0ec4f0eadf1`
- target_sha_before: `4cc948ba15691b62`
- target_sha_after: `bf5e28aecf7e83d7`

## Finding

### AIW-20-7-20260809-P2-runtimeinit-profilingtrigger-boundary

- severity: P2
- type: source-boundary
- claim: Java 未捕获异常段落把 `RuntimeInit` 默认 handler 结论与 `ProfilingTrigger.TRIGGER_TYPE_OOM` 旁支断言绑定。
- evidence: `android-17.0.0_r1` 的 `RuntimeInit.commonInit()` 安装 `LoggingHandler` 和 `KillApplicationHandler`；`KillApplicationHandler` 在报告 ActivityManager 后执行 `Process.killProcess()` 与 `System.exit(10)`。本段可直接支撑的读者结论是替换默认 handler 后应调用前一 handler，以保持平台 fatal 报告和退出路径。
- target_range: `src/part5-app/ch20-stability/07-exception-architecture.md` Java 未捕获异常入口段落。
- recommended_action: 移除未由本段来源直接支撑的 `ProfilingTrigger` 旁支断言，改写为 `RuntimeInit` 默认退出路径的来源边界。
- status: fixed

## Change summary

- 正文将自定义 crash handler 的原因说明收束到 `RuntimeInit` / `KillApplicationHandler`：吞掉默认 handler 会绕过 ActivityManager 报告和终止进程路径。
- Frontmatter 更新 `last_verified`、`polish_count`、Task9 深审时间、日志路径和 `task9_result: pass-tech-review`。
- 机器 issue pool 中新增并关闭同一 finding id。

## Source evidence checked

- AOSP `RuntimeInit.java` (`android-17.0.0_r1`): `commonInit()` 注册 pre-handler 和默认 uncaught handler；`KillApplicationHandler` 在 `finally` 中执行进程终止。
- AOSP `ApplicationExitInfo.java` (`android-17.0.0_r1`): reason 常量仍为 0–16；未改动正文中该表。
- AOSP `ActivityManager.java` (`android-17.0.0_r1`): `setProcessStateSummary()` 的 128 字节、敏感信息与节流边界；未改动正文中该段。

## Writing gate

正文 hard-gate 未增加禁用对比句式、填充副词、评价式开头或分析尾句；新增正文不包含流水线过程描述。
