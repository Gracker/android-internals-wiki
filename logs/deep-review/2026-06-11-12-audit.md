# 闲时技术抽检 · 2026-06-11 12:00

## Review 目标
- 章节：2.13 图形缓冲区管理 (BufferQueue)
- 文件：src/part1-fundamentals/ch02-rendering/13-buffer-queue.md
- 状态：finalized
- 选择原因：`CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检；本节 `last_task9_audit=2026-05-18`，距今超过 21 天，且历史 Task9 曾有 BufferQueue slot / 版本口径 P2 建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] Perfetto 观测点小节 — 原文写 `android.monitor_contention` 表可通过 `lock_class_name` 定位 `BufferQueueCore::mMutex` 争用。Perfetto 官方 stdlib 文档说明 `android_monitor_contention` 是 Java monitor contention 表，字段包括 `blocking_method`、`blocked_method`、`lock_name` 等；Android trace analysis 文档也把它限定在 Java/Kotlin `synchronized` monitor contention 场景。它不能直接定位 libgui native `std::mutex`。已改为：libgui slice 只能观察函数耗时和等待形态，native mutex 争用需要结合 libgui slice、thread_state/sched blocked reason、SF/HWC release 时序间接判断。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][仅日志] `ViewRootImpl` 中 `new BLASTBufferQueue(...)` 的示例签名覆盖 Android 12-14 更贴近；android-16.0.0_r1 已变为 `new BLASTBufferQueue(mTag, true /* updateDestinationFrame */)` 后再 `update(mSurfaceControl, ...)`。正文主结论“ViewRootImpl 创建/更新 BLASTBufferQueue，常规窗口走 BLAST 路径”仍成立，后续可把代码块拆成 Android 12-14 / Android 16 两个版本锚点。

## 证据
- AOSP android-16.0.0_r1：`frameworks/native/libs/gui/BLASTBufferQueue.cpp` 存在 `setMaxDequeuedBufferCount(2)`、`AsyncProducerListener`、`consumerIsSurfaceFlinger=false`、`QueuedBuffer - ...BLAST#...`、`acquireNextBufferLocked()` vsyncId trace、`mergeWithNextTransaction()` / `applyPendingTransactions()`。
- AOSP android-16.0.0_r1：`BufferSlot.h` 仍为 counter-based `BufferState`；`BufferQueueCore.h` 中 `mMutex`、`mDequeueCondition`、`mDequeueBufferCannotBlock` 路径存在；`BufferQueueProducer.cpp` 的 `waitForFreeSlotThenRelock()` 与 `WOULD_BLOCK` 语义可复核。
- Perfetto 官方文档：`android.monitor_contention` / `android_monitor_contention` 针对 Java/Kotlin monitor contention，不是 native `std::mutex` 争用表。
- Android 17 源码 tag 未在本轮 `frameworks/native` 公共 tag 列表中命中；本轮源码锚点使用 Android 16.0.0_r1，未采用 main/master 结论作为 Android 17 正文依据。

## 统计
- P0 事实错误：0 处
- P1 重要缺失/错误：1 处（已 AUTO-FIX）
- P2 建议改进：1 处（仅日志）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：1 处（Perfetto `android.monitor_contention` native mutex 口径）
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（闲时抽检 P2 仅日志）
- Frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_audit=2026-06-11`
