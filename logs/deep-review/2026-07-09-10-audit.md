# 闲时技术抽检 · 2026-07-09 10:27

## 抽检目标
- 章节：2.7 Hardware Layer
- 文件：src/part1-fundamentals/ch02-rendering/07-hardware-layer.md
- 模式：Task9 idle audit
- 范围：维度 1 源码引用准确性；维度 3 版本差异覆盖

## Android 版本边界
- 本轮只使用 Android 17 / API 37 或更低资料。
- 主线源码锚点统一升级到 `android-17.0.0_r1`。
- 未使用 Android 18 / API 38+、targetSdk 38+、main/master 结论。

## 复核证据
- AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/view/View.java`：`buildLayer()` 仍为 `LAYER_TYPE_HARDWARE -> updateDisplayListIfDirty() -> ThreadedRenderer.buildLayer(mRenderNode)`，`LAYER_TYPE_SOFTWARE -> buildDrawingCache(true)`；章节中 Software/Hardware Layer 的分支描述成立。
- AOSP `android-17.0.0_r1` `frameworks/base/graphics/java/android/graphics/RenderNode.java`：`setUseCompositingLayer(boolean, Paint)` / `getUseCompositingLayer()` 仍为公开 API，注释明确 `forceToLayer=false` 为默认且推荐值，`paint` 只在强制 compositing layer 时叠加 blend mode、alpha、ColorFilter；章节描述成立。
- AOSP `android-17.0.0_r1` `frameworks/base/libs/hwui/RenderProperties.h`：`promotedToLayer()` 条件仍包含 functor、ImageFilter、StretchEffect、`alpha < 1 && hasOverlappingRendering` 与 `fitsOnLayer()`；章节自动升层条件描述成立。
- AOSP `android-17.0.0_r1` `libs/hwui/pipeline/skia/SkiaOpenGLPipeline.cpp`、`SkiaPipeline.cpp`、`RenderNodeDrawable.cpp`：可复核 `SkiaOpenGLPipeline::draw()`、`SkiaPipeline::renderFrame()`、`renderLayersImpl()`、`RenderNodeDrawable` 与 `RenderNode::updateSnapshotIfRequired()` 路径；RenderEffect / ImageFilter 的离屏处理入口描述未发现 Android 17 越界。
- Android Developers Compose UI release notes：可复核 `CompositingStrategy` 与 `graphicsLayer` 的 offscreen buffer 语义；未找到“Compose 1.10 graphicsLayer 离屏缓冲池化”的官方 release note 明确锚点，保留 P2 建议。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4.7/5
- P0/P1：0
- P2：1
- 结论：`View.buildLayer()`、`RenderNode.setUseCompositingLayer()`、`RenderProperties::promotedToLayer()` 与 RenderEffect/ImageFilter 相关路径在 `android-17.0.0_r1` 下可复核；章节原先使用 `android-16.0.0_r1` 作为当前源码锚点，不符合 2026-06-27 后的 Android 17 主线基准，已直接修正。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- P2：1
- 结论：Android 10/12/17 的自动升层版本演进描述可复核。Compose 1.10 “offscreen buffer pooling”仍缺 AndroidX 官方 release note / Gerrit commit / 源码路径锚点，建议后续补充证据后再作为强结论保留。

## AUTO-FIX
- 证据来源：AOSP `android-17.0.0_r1` `View.java`、`RenderNode.java`、`RenderProperties.h`；Android Developers Compose UI release notes。
- 改动位置：frontmatter `last_verified*` / AOSP source path、正文源码验证标记、版本演进表、参考资料 AOSP 锚点。
- 修改摘要：将当前源码验证锚点从 `android-16.0.0_r1` 升级到 `android-17.0.0_r1`；未改动技术主结论；章节回到 Task6 复审。

## 闭环动作
- queue.json：未写入（无 P0/P1）。
- intake/research-gaps.md：未写入。
- intake/suggestions.md：未写入；Compose 1.10 证据缺口作为 P2 记录在本次 audit 日志。
- 章节正文：已 auto-fix Android 17 源码锚点。
- Frontmatter：更新 `task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_audit`、`last_task9_autofix_at` 与审计日志路径。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1
- P3 锦上添花：0
- 总体技术评分：4.6/5
