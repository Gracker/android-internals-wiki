# Deep-Review Audit Log

- **Run ID**: `20260729-163543-deep-review-12768822`
- **Lane**: deep-review (profile: aiw-polish-deep-review)
- **Date**: 2026-07-29
- **Target**: `src/part1-fundamentals/ch05-cpu-power/5.19-ondevice-ai-adpf-intelligent-scheduling.md`
- **SHA16**: `45ba930a2b4ccdf5`
- **Mode**: deep-review / technical audit
- **Materials**: none (empty)
- **Android baseline**: `android-17.0.0_r1` / Android 17 (API 37)

---

## 审计结论

本章节正文质量较高：ADPF 三层控制面拆解（runtime / PerformanceHintManager / NPU scheduling HAL）准确，
Java/NDK API 版本边界（API 31/33/34/35/36/37）清晰，WorkDuration CPU/GPU 拆分说明正确，
LLM prefill/decode 差异、Perfetto 观测建议和 A/B 方法论均符合官方文档与 AOSP 源码语义。
NNAPI "Android 15 (API 35) deprecated" 与全书口径一致（ch16 02-version-changes.md 引用 source.android.com 原文）。
无 Android 18/API 38+ 主线结论。

materials 为空，但正文已有充分技术内容（约 25K 字符，含代码骨架、表格和源码索引），
故按 deep-review lane 规则做源码/版本边界/来源支撑/可读性审计并修复小范围有依据的问题。

### 技术复核通过项

| 维度 | 结论 |
|------|------|
| PerformanceHintManager.Session API 31+ | ✅ 与 AOSP `PerformanceHintManager.java` 公开契约一致 |
| NDK performance_hint.h API 33/36 | ✅ workload increase/reset/spike API 36 正确 |
| WorkDuration API 35 只拆 CPU/GPU | ✅ 无 NPU 字段，total 不要求等于两项相加 |
| setPreferPowerEfficiency API 35 | ✅ 能效偏好语义正确 |
| NPU IScheduling VINTF AIDL | ✅ system/vendor 接口，非应用 SDK，UID priority 0–1000 |
| FEATURE_NEURAL_PROCESSING_UNIT API 37 | ✅ 能力声明，无性能保证 |
| NNAPI deprecated Android 15 (API 35) | ✅ 与全书口径和 source.android.com 一致 |
| EAS / schedutil 边界 | ✅ kernel/sched/fair.c + cpufreq_schedutil.c 正确 |
| 公开 Session 无 priority/callback/frequency setter | ✅ 与 current.txt 一致 |
| MAP_HUGETLB / affinity 警告 | ✅ 正确不建议 |

---

## 实际补丁

### P1 — version-boundary-error (finding-88b36c1b6ae1) — 已修复

**问题**: frontmatter `applicable_versions` 写 `"Android 16 (API 35) - Android 17 (API 37)"`。
API 35 = Android 15，不是 Android 16（Android 16 = API 36）。章节正文讨论 API 35 特性
（WorkDuration、setPreferPowerEfficiency），最低版本为 Android 15。

**修复**: 改为 `"Android 15 (API 35) - Android 17 (API 37)"`，与姐妹章节 05.26 一致。

### P2 — wrong-crossref (finding-0d9b7743656d) — 已修复

**问题**: frontmatter `related_chapters` 旧值 `["5.11", "5.13", "5.16"]` 为占位值，
与正文 line 19 实际引用不符（正文引用 5.9 ADPF 基础/Thermal、5.14 NPU 平台接口、5.16 异构执行）。

**修复**: 改为 `["5.9", "5.14", "5.16"]`。

### P2 — missing-metadata (finding-8e7228d72c08) — 已修复

**问题**: frontmatter 缺少 `last_verified`、`confidence`、`sources`、`pipeline_stage`、`task6_state`、`task9_state`。

**修复**: 全部补齐。confidence 设为 `medium`（有充分正文和正确源码引用，但本次无上游 materials 做完整重新核验，不提升到 high）。

### P2 — table-artifact — 已修复

**问题**: API 速查表中 `WorkDuration` 行写 "与 report duration 2"，"2" 为 `reportActualWorkDuration(WorkDuration)` 第二重载的残留文本。

**修复**: 改为 `\`WorkDuration\` + \`reportActualWorkDuration(WorkDuration)\``。

### P3 — url-format-consistency — 不修复（仅记录）

AOSP reference URL 同时存在 `+/android-17.0.0_r1/` 和 `+/refs/tags/android-17.0.0_r1/` 两种写法，Googlesource 均可解析。全书均有此混用，非本章独有问题，不做单独修改以避免无意义 churn。

---

## 验证

| 检查项 | 结果 |
|-------|------|
| `check-metadata.py --files <target>` | ✅ 1 文件通过, 0 失败, 0 警告 |
| `check-summary-links.py` | ✅ passed（2 个 pre-existing duplicate 警告，与本章无关） |
| `git diff --check` | ✅ clean |

---

## 回流状态

- `status`: 保留 `ready-for-review`（正文质量已通过技术复核，可进入正式 review 流程）
- `confidence`: `medium`（正文技术准确，但无上游 materials 做完整独立核验）
- 无需回流到 body-apply lane
