# Task 9 闲时抽检 · 2026-06-14 19:20

## 抽检目标
- 章节：2.16 Sync Fence 框架与帧同步机制
- 文件：src/part1-fundamentals/ch02-rendering/16-sync-fence.md
- 状态：finalized / task9_result=pass-tech-review / task9_state=reviewed
- 选择原因：Preflight `CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检；本节 `last_task9_audit=2026-05-24`，距本轮超过 21 天，且上一轮 Task9 留有 Binary Semaphore reset 语义 P2。
- external-review：根目录无活跃文件，本轮未消费 external-review。
- 审计范围：闲时抽检，只覆盖维度 1（源码引用）与维度 3（版本差异）。
- Android 版本边界：本轮未使用 Android 18 / API 38+ 资料；AOSP tag 查询仅确认 frameworks/base、frameworks/native、system/core 存在 `android-16.0.0_r1`，未发现这些仓库的 `android-17.0.0_r1` tag，因此源码复核锚点仍限定在 Android 16 及更早。

## 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0 个 P0/P1；1 个既有 P2 仍保留。
- 复核结论：正文主要源码锚点与 Android 16 及更早 tag 对得上。
  - `frameworks/native/libs/ui/Fence.cpp`：`Fence::merge()` 在 android-16.0.0_r1 存在，并调用 `sync_merge()`。
  - `system/core/libsync/sync.c`：android-16.0.0_r1 同时存在 `legacy_sync_merge()` / `modern_sync_merge()`、`legacy_sync_fence_info()` / `modern_sync_file_info()`。
  - `system/core/libsync/sw_sync.h`：android-16.0.0_r1 暴露 `sw_sync_timeline_create()`、`sw_sync_timeline_inc()`、`sw_sync_fence_create()`。
  - `frameworks/native/services/surfaceflinger/DisplayHardware/HWC2.h`：android-7.0.0_r1 已有 `setBuffer(... acquireFence)`、`setClientTarget(... acquireFence)`、`getReleaseFences()`、`present(... outRetireFence)`，正文“Android 7 已明确 HWC2 fence 语义”成立。
  - `SkiaOpenGLPipeline.cpp`：android-16.0.0_r1 中 `SkiaOpenGLPipeline::flush()` 调用 `skgpu::ganesh::FlushAndSubmit(surface)` 与 `mEglManager.createReleaseFence(true, &sync, &fence)`，正文 GL release fence 路径成立。
  - `SkiaVulkanPipeline.cpp` / `VulkanManager.cpp`：android-16.0.0_r1 中 Vulkan 路径调用 `VulkanManager::createReleaseFence()`；`createReleaseFence()` 和 `finishFrame()` 创建 `VkExportSemaphoreCreateInfo(... SYNC_FD_BIT)`，通过 `mGetSemaphoreFdKHR` 导出 fd，未设置 `VkSemaphoreTypeCreateInfo` / `VK_SEMAPHORE_TYPE_TIMELINE`，正文 Binary Semaphore → sync fd 桥接成立。
- 既有 P2 仍保留：L267 “传统 Vulkan Binary Semaphore signal 一次后回到 unsignal 状态”仍偏简化。Vulkan binary semaphore 通常是 signal 后保持 signaled，等一次 wait 消耗后才回到 unsignaled；该问题已在 2026-05-24 Task9 记录为 P2，本轮不重复写入 suggestions.md。

## 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0 个 P0/P1。
- 复核结论：正文没有把 Android 18 / API 38+ 内容写入结论；Android 7 HWC2、Android 8.1 libsync modern/legacy 共存、Android 14-16 GL/Vulkan HWUI release fence 分流、Timeline Semaphore 与 native fence fd 边界均有明确版本锚点。
- Android 17 边界：正文 L321 已明确“当前未找到 Android 17 CDD、source.android.com 或 AOSP tag 中关于强推 Timeline Semaphores / 移除 fd 依赖的公开依据”，没有把 main/master 或 Android 18 行为提升为 Android 17 结论。frontmatter `applicable_versions` 覆盖到 Android 17，但 `last_verified_against` 仍停在 android-16.0.0_r1；在未取得 android-17.0.0_r1 tag 前，维持中置信度即可，不升 P1。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1（既有 P2，未重复入库）
- P3 锦上添花：0
- 总体技术评分：4/5

## 闭环动作
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0（既有 P2 不重复）
- 正文内容改动：0
- frontmatter：更新 `last_task9_audit: "2026-06-14"`
- 自动晋升 finalized：无（闲时抽检，章节已是 finalized）
