# 深度技术 Review · 2026-06-28 09:29

## Review 目标
- 章节：1.25 Android 17 Binder IPC 异步机制与批处理流水线
- 文件：src/part1-fundamentals/ch01-architecture/01.25-binder-ipc-async-pipeline.md
- 状态：ready-for-review
- 模式：Task2B 修复后复审；本轮执行高置信局部 AUTO-FIX
- Android 基准：android-17.0.0_r1；未使用 Android 18/API 38+ 资料

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：3
- 具体问题：
  1. [P0][AUTO-FIX] 性能表与迁移要点 — 表格把 frozen 响应写成 oneway 列 `BR_FROZEN_REPLY`，且使用“5s Binder 超时”。Android 17 中 sync frozen 返回 `BR_FROZEN_REPLY`，oneway frozen 返回 `BR_TRANSACTION_PENDING_FROZEN`；`waitForResponse()` 没有固定 5s Binder timeout。已改为 sync/oneway 分列描述和“普通同步等待卡住”。
  2. [P0][AUTO-FIX] RPC Binder 缓冲区常量 — 文中写 `libbinder/RpcSession.cpp` 的 `kDefaultRpcBinderSize`。Android 17 实际为 `libs/binder/Constants.h:37` 的 `kRpcTransactionLimitBytes = 600 * 1024`。已修正路径和常量名。
  3. [P0][AUTO-FIX] Binder 线程池上限 — 文中把 `DEFAULT_MAX_BINDER_THREADS` 写成进程 Binder 线程总数。Android 17 `ProcessState.h` 注释说明 `maxThreads` 只限制内核启动的 lazy 线程，不包含 `startThreadPool()`/`joinThreadPool()` 主动加入的线程；system_server 通过 `BinderInternal.setMaxThreads(31)` 设置。已修正。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：2
- 具体问题：
  1. [P1][AUTO-FIX] frozen oneway 队列描述 — 文中写“frozen pending 队列”。Android 17 binder driver 没有这个专门队列名；frozen oneway 仍进入 `proc->todo`/`thread->todo` 或 `node->async_todo`，调用方收到 `BR_TRANSACTION_PENDING_FROZEN` 报告。已改为源码队列名。
  2. [P1][AUTO-FIX] oneway 调用线程归属 — 文中把 `waitForResponse(nullptr, nullptr)` 描述成 Binder 线程快速轮转。该等待发生在发起调用的线程；对端 Binder 线程仍执行事务。已改为“调用方线程占用时间短”。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] Android 16/17 RPC Binder 差异 — Android 16 `android-16.0.0_r1` 与 Android 17 `android-17.0.0_r1` 都是 `kRpcTransactionLimitBytes = 600 * 1024`，不是 Android 17 相对 Android 16 的新增扩容。已改为“16/17 保持一致”。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：前轮指出的 async_todo、priority inheritance、freezer/mCallRestriction 分离已进入正文。本轮未新增 P1 盲区。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] 性能量化数据仍是 `[待补充]`：oneway vs 同步延迟、批处理 syscall 减少量、frozen sync 立即返回错误 vs 普通同步等待卡住的 ANR 影响仍需 trace/strace/系统数据。该 P2 已存在于 `intake/suggestions.md`，本轮仅修正其中过时的“5s 超时”措辞，未重复新增。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：关联章节 `1.4`、`1.13`、`§4.11` 与当前主题一致；未发现需要进入 queue 的交叉引用错误。

## 统计
- P0 事实错误：3 处（均 AUTO-FIX）
- P1 重要缺失：3 处（均 AUTO-FIX）
- P2 建议改进：1 处（已存在 suggestion，本轮修正 suggestion 描述）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- 直接修改章节：1 个文件
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-06-28`
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 条新增；修正既有 1.25 数据支撑 suggestion 的过时表述 1 处
- external-review：本轮无近 3 天活跃文件可消费

## AUTO-FIX 证据来源
- `platform/frameworks/native` `android-17.0.0_r1`：`libs/binder/IPCThreadState.cpp:921-996, 1153-1197, 1246-1363, 1787-1823`
- `platform/frameworks/native` `android-17.0.0_r1`：`libs/binder/ProcessState.cpp:48-50, 591-598`
- `platform/frameworks/native` `android-17.0.0_r1`：`libs/binder/include/binder/ProcessState.h:70-90, 120-130`
- `platform/frameworks/native` `android-17.0.0_r1`：`libs/binder/Constants.h:21-37`
- `platform/frameworks/native` `android-16.0.0_r1`：`libs/binder/Constants.h:21-37`
- `platform/frameworks/base` `android-17.0.0_r1`：`services/java/com/android/server/SystemServer.java:508-510, 944-948`
- `platform/frameworks/base` `android-17.0.0_r1`：`services/core/java/com/android/server/am/CachedAppOptimizer.java:2827-2874, 2962-3022`
- `kernel/common` `android17-6.18-2026-06_r1`：`drivers/android/binder.c:3023-3129, 3557-3561, 4026-4038, 4328-4339, 5083-5089`
- `kernel/common` `android17-6.18-2026-06_r1`：`drivers/android/binder_alloc.c:471-514, 602-614`
