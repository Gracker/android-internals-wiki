# 深度技术 Review 闲时抽检 · 2026-06-09 19:20

## Audit 目标
- 章节：5.15 SensorService 与传感器批处理功耗模型
- 文件：src/part1-fundamentals/ch05-cpu-power/15-sensorservice-batching-power.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- external-review：近 3 天无活跃文件

## 证据源
- Gitiles `frameworks/base` / `frameworks/native` `refs/tags/android-17.0.0_r1`：本轮查询 `SensorManager.java` 与 `SensorDevice.cpp` 返回 404，不能把 AOSP main 作为 Android 17 正文锚点。
- AOSP `android-16.0.0_r1`：`SensorManager.java`、`SystemSensorManager.java`、`SensorService.cpp`、`SensorDevice.cpp`、`SensorEventConnection.cpp` 路径存在。
- AOSP `android-16.0.0_r1` `SystemSensorManager.registerListenerImpl()`：确认 `SensorEventQueue.addSensor(sensor, delayUs, maxBatchReportLatencyUs)` 路径。
- AOSP `android-16.0.0_r1` `SensorDevice::Info::selectBatchParams()` / `BatchParams::merge()`：确认同一 handle 多客户端合并时 `mTSample` 取更紧请求，`mTBatch` 取更紧的 apparent batch period。
- AOSP `android-16.0.0_r1` `SensorDevice::dump()`：确认输出 `sampling_period(ms)`、`batching_period(ms)` 与 selected 值。
- AOSP `android-16.0.0_r1` `SensorService::enable()`：确认 continuous sensor 新连接场景下 batch 后按条件 flush，再 activate。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [AUTO-FIX/P1] 全文 AOSP 源码锚点 — 原文使用 `AOSP main` 作为 `SensorManager`、`SystemSensorManager`、`SensorService`、`SensorDevice` 的验证来源。按 2026-05-29 Android 边界，main/master 不能支撑 Android 17 正文结论；本轮已用 `android-16.0.0_r1` 复核路径和关键行为，并把正文锚点替换为 `AOSP android-16.0.0_r1`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [AUTO-FIX/P1] frontmatter 适用范围 — `android-17.0.0_r1` 本轮 Gitiles 查询返回 404，无法证明章节源码结论已进入 Android 17；已将 `applicable_versions` 从 Android 4.4-17 收窄为 Android 4.4-16，并更新 `last_verified_against`。
- 具体结论：未发现 Android 18 / API 38+ 或 targetSdk 38+ 越界内容。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：frontmatter 版本边界 3 处；正文 AOSP 锚点 7 处
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result=auto-fixed`，`task6_state=revisiting`，`pipeline_stage=task6_pending`，`last_task9_audit=2026-06-09`
