# Task 9 闲时抽检 · 2026-05-20 04:23

## 抽检目标
- 章节：19.03 KOOM
- 文件：src/part3-tools/ch19-apm/03-koom.md
- 状态：finalized
- 选择原因：当前无 `task9_state: pending` 候选；该章节 `last_task9_at=2026-04-24T13:23:00+08:00`，超过 21 天，且上轮曾有 P2（ApplicationExitInfo 版本边界、KOOM Native STL 接入边界），适合闲时复检。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮不做完整 6 维度审查。

## 复核资料
- KwaiAppTeam/KOOM `master`：`df3b8c33f63ab1f23e814c19792314efb653deaf`，commit date `2026-01-12T14:11:03Z`。
- KOOM `README.zh-CN.md`：确认 Java Heap / Native Heap / Thread 三个模块定位，Native 模块支持 `c++_shared` / `c++_static`。
- `koom-java-leak/README.zh-CN.md`：确认 Android L/API 21+，ABI 支持 `armeabi-v7a arm64-v8a x86 x86-64`，触发条件为 Java heap、线程数、FD 等连续超阈值，dump 路线为 `虚拟机 suspend -> fork -> resume -> dump`，并用 Shark 做端侧分析。
- `koom-native-leak/README.zh-CN.md`：确认 Android N/API 24+、仅 `arm64-v8a`，hook `malloc/free`，基于 mark-and-sweep / `libmemunreachable` 思路输出不可达 native block 和分配栈。
- `koom-thread-leak/README.zh-CN.md`：确认 Android N/API 24+、仅 `arm64-v8a`，hook `pthread_create/pthread_exit`，关注 joinable 线程未 `detach/join` 后退出的泄漏信息。
- AOSP `frameworks/base/core/java/android/os/Debug.java`（android-11.0.0_r1）：确认 `Debug.dumpHprofData(String)` 直接调用 `VMDebug.dumpHprofData(fileName)`。
- AOSP `frameworks/base/core/java/android/app/ActivityManager.java`（android-11.0.0_r1）：确认 `getHistoricalProcessExitReasons()` 与 `isLowMemoryKillReportSupported()` 存在，后者读取 `persist.sys.lmk.reportkills`，用于判断 low-memory kill 是否会进入 historical exit reasons。

## 抽检结论

### 维度 1：源码引用准确性
- 评分：4.5/5
- P0/P1：0
- 结论：通过。
- 复核点：
  1. L91-L93 的三个模块能力、minSdk/ABI 边界与 KOOM module README 一致：Java 模块 API 21+，Native / Thread 模块 API 24+ 且仅 `arm64-v8a`。
  2. L99 的 `Debug.dumpHprofData()` → `dalvik.system.VMDebug.dumpHprofData()` 与 AOSP `Debug.java` 调用关系一致。
  3. L101 的 fork dump 主链与 KOOM `koom-java-leak` README 的 `suspend -> fork -> resume -> dump` 路线一致。
  4. L113-L115 的 Native leak 说明与 KOOM native README 一致：hook `malloc/free` 记录分配元数据，再按 tracing GC / mark-and-sweep 思路找不可达块；`xhook` 依赖存在。
  5. L130 与 L202-L217 的线程生命周期 hook、joinable 线程、白名单口径与 thread README 一致。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- 结论：通过。
- 复核点：
  1. L91-L93 已写出 KOOM 三个模块的 Android 版本下限，未把 Native / Thread 错写成全版本可用。
  2. L242-L247 已覆盖 ApplicationExitInfo 的 API 30+ 边界，并补充 Android 8-10 无该 API 时的替代信号；`isLowMemoryKillReportSupported()` 这一设备能力检查与 AOSP/官方说明一致。
  3. L148 已补上上轮 P2 提到的 `c++_shared` / `c++_static` 与 `pickFirst` 风险，工程接入边界闭环。

## 低风险观察
- [P3][版本复检] Frontmatter 的 `applicable_versions: Android 8 (API 26) - Android 17 (API 37)` 是全书覆盖口径；KOOM upstream README 只声明模块 minSdk/ABI，没有声明 Android 15/16/17 的额外适配结论。本轮未找到会导致正文误导的事实错误，建议后续 Android 17 SDK / NDK 变化落定后复检 native hook、unwind、16KB page size 相关兼容性。

## 闭环动作
- P0/P1：0，未写入 `metadata/queue.json`。
- research-gaps.md：0。
- suggestions.md：0（闲时抽检无 P0/P1，仅记录低风险观察）。
- 更新 frontmatter：`last_task9_audit: "2026-05-20"`。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0
- P3 锦上添花：1
- 总体技术评分：4.5/5
