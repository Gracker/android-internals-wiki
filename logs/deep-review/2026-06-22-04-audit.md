# Task9 闲时抽检 · 2026-06-22 04:28

## Audit 目标
- 章节：25.1 功耗诊断与分析方法
- 文件：src/part5-app/ch25-power-size/01-power-diagnosis.md
- 状态：finalized → task2b_pending
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）
- Android 边界：最高 Android 17 / API 37；未使用 Android 18/API 38+ 资料

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] 源码级实现细节 / Android 15 Streamlined Battery Stats — 正文把主功耗归因链写成 `MultiStatePowerAttributor.estimatePowerConsumption()` → `PowerStatsInternal.getStateResidencyAsync()` → `BatteryUsageStats.Builder.aggregate()`，但 tagged AOSP 不支持这个链路。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] Android 15/16/17 边界未拆开：`MultiStatePowerAttributor` 在 `android-15.0.0_r1` 对应目录不存在；Android 17 中 `BatteryStatsService` 已无 `Flags.streamlinedBatteryStats()` 命中，`PowerStatsScheduler.start(boolean)` 变为 `start()`。

## 一手证据
- `android-15.0.0_r1`：`BatteryStatsService.java` L619-L631 配置 `Flags.streamlinedBatteryStats()` / connectivity / misc flags；L707-L710 调用 `mPowerStatsScheduler.start(Flags.streamlinedBatteryStats())`；`services/core/java/com/android/server/power/stats/processor/MultiStatePowerAttributor.java` 不存在。
- `android-16.0.0_r1`：`PowerStatsScheduler.java` L92-L97 为 `start(boolean enablePeriodicPowerStatsCollection)`；`processor/MultiStatePowerAttributor.java` 存在。
- `android-17.0.0_r1`：`BatteryStatsService.java` L573-L576 调用 `mPowerStatsScheduler.start()`，文件中无 `streamlinedBatteryStats` 命中；`PowerStatsScheduler.java` L91-L94 为无参 `start()`；`BatteryUsageStatsProvider.java` L286-L287 调用 `mPowerAttributor.estimatePowerConsumption()`；`processor/MultiStatePowerAttributor.java` L240-L243 委托 `mPowerStatsExporter.exportAggregatedPowerStats()`。
- `android-17.0.0_r1`：`BatteryStatsService.java` L294-L297 的 `PowerStatsInternal.getStateResidencyAsync()` 属于低功耗状态查询路径，不是 `BatteryUsageStatsProvider` 的主归因链。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节 frontmatter：`task9_result=needs-rework`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-06-22`
