# Task 9 闲时抽检 · 2026-05-22 16:38

## 抽检目标
- 章节：16.1 Google 官方的性能优化思路
- 文件：src/part4-system/ch16-aosp/01-google-optimization.md
- 当前状态：status=finalized；task9_result=pass-tech-review；task9_state=reviewed
- 选择原因：last_task9_at=2026-04-27T13:32:46+08:00，距本轮抽检 25 天；属于 finalized/pass-tech-review 长期未复检章节。
- 抽检范围：维度 1（源码/一手资料引用）+ 维度 3（版本差异），不做完整 6 维度复审。
- external-review：根目录未发现与 16.1 / 本章节标题匹配的活跃文件。

## 抽检证据

### 维度 1：源码与一手资料引用
- AOSP `platform/frameworks/base/core/java/android/os/LockedMessageQueue/MessageQueue.java`（refs/heads/master）存在，类名与 legacy locked queue 描述匹配；`next()`、`enqueueMessage` 路径可定位。
- AOSP `platform/frameworks/base/core/java/android/os/ConcurrentMessageQueue/MessageQueue.java`、`CombinedMessageQueue/MessageQueue.java`、`SemiConcurrentMessageQueue/MessageQueue.java`（refs/heads/master）均存在，支撑文中“Android 17 后 MessageQueue 实现拆分”的描述。
- AOSP `platform/frameworks/native/libs/binder/ProcessState.cpp`（refs/heads/master）存在 `DEFAULT_MAX_BINDER_THREADS 15` 与 `BINDER_SET_MAX_THREADS`，支撑文中 Binder worker 上限描述。
- AOSP `platform/frameworks/native/libs/gui/BLASTBufferQueue.cpp` / `.h`（refs/heads/master）存在 `syncNextTransaction()`、`mergeWithNextTransaction()`、`applyPendingTransactions()`，且 `.cpp` 引入 `BufferQueueCore` / producer / consumer 相关头文件，支撑文中 BLAST 仍使用 BufferQueue 基础设施的描述。
- AOSP `SurfaceControl.java` 存在 `onMergeWithNextTransaction()`，`BLASTSyncEngine.java` 和 `WindowContainerTransaction.java` 路径存在，支撑 WMS / SurfaceControl transaction 同步描述。
- AOSP `Choreographer.java` 在 `android-16.0.0_r1` 与 `refs/heads/master` 均存在，参考资料中的 Project Butter / VSync 入口锚点可定位。
- 官方 Baseline Profiles 文档仍写明 “about 30% from the first launch”、Cloud Profiles、Startup Profiles 的关系；文中相关表述未发现过期。
- 官方 Mainline 文档仍写明 Android 10 引入 Mainline，模块可用 APEX/APK，ART `com.android.art` 为 Android 12 引入的 APEX；文中 Mainline / ART 交付边界描述未发现过期。
- 官方 Binder IPC 文档仍写明 early nice priority inheritance、Android 8 real-time priority inheritance 与 hwbinder/domain 变化、Android 10 Stable AIDL；文中 Binder 时间线未发现 P0/P1 问题。

### 维度 3：版本差异
- Android 17 release notes（Beta 4，2026-04-16）仍列出 `Lock-free MessageQueue` 与 `Generational Garbage Collection`；行为变更页仍写明 targetSdkVersion 37+ 应用获得新的 lock-free `android.os.MessageQueue`。
- Android Developers Blog《Under the hood: Android 17’s lock-free MessageQueue》（2026-02-17）仍写明 target SDK 37+、lock-free implementation、DeliQueue 与反射私有字段风险；文中 Android 17 / targetSdk 边界描述匹配。
- AutoFDO 官方博客内容与文中 `android15-6.6`、`android16-6.12`、GKI profile 更新描述一致；但章节引用的博客 URL 少了编码空格，当前返回 404。正确 URL 为 `https://android-developers.googleblog.com/2026/03/BoostingAndroid%20PerformanceIntroducingAutoFDO.html`。

## 问题分级

### P0 / P1
- 无。

### P2（仅日志记录，闲时抽检不写入 queue）
1. [P2][源码/资料引用] 参考资料与 frontmatter `sources` 中的 AutoFDO 官方博客 URL 当前返回 404：`https://android-developers.googleblog.com/2026/03/BoostingAndroidPerformanceIntroducingAutoFDO.html`。正确可访问 URL 为 `https://android-developers.googleblog.com/2026/03/BoostingAndroid%20PerformanceIntroducingAutoFDO.html`。建议下一轮资料校正时替换，避免后续验证链断开。

## 抽检结果
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1
- 处理动作：未写入 `queue.json`；未写入 `intake/suggestions.md`；已在章节 frontmatter 标注 `last_task9_audit: "2026-05-22"`。
- 结论：通过闲时抽检。章节技术主线、AOSP 锚点、Android 17 版本边界未发现需要返工的问题。
