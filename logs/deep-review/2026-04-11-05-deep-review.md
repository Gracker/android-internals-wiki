# 深度技术 Review · 2026-04-11 05:20

## Review 目标
- 章节：1.11 Zygote 机制与启动性能优化
- 文件：src/part1-fundamentals/ch01-architecture/11-zygote-startup.md
- 状态：ready-for-review（task6_state: reviewed，task2b_state: pending）

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：3
- 具体问题：
  1. [P0] 行 101, 186, 192, 289, 483 — 把 system_server → Zygote 的创建进程请求写成 Binder IPC；AOSP android-16 实际是 `Process.start()` → `ZygoteProcess.startViaZygote()` → `zygoteSendArgsAndGetResult()`，底层是 LocalSocket/zygote socket。
  2. [P0] 行 230-265, 411-420 — 在 `last_verified_against: android-16.0.0_r1` 前提下仍使用旧版 `preloadOpenGL` / `PreloadOpenGL` / `BeginIcuCachePinning` 叙述，并把 BootTimingsTraceLog slice 说成出现在 system_server；android-16 `ZygoteInit.java` 实际 trace 名为 `PreloadClasses` / `CacheNonBootClasspathClassLoaders` / `PreloadResources` / `PreloadGraphicsDriver`，发生在 zygote 进程，且 `frameworks/base/config/preloaded-classes` 不包含 `RecyclerView`。
  3. [P0] 行 324-336 — Perfetto SQL 用 `process.name='zygote64' AND slice.name LIKE '%fork%'` 定位 fork；android-16 `Zygote.java` 追踪点是子进程 `PostFork`，不是 zygote64 进程里的 fork slice。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] 行 299-336, 405-425 — “fork 后到 Application.onCreate 之前”的链路省略了 `RuntimeInit.applicationInit()`、`ActivityThread.attach()`、`bindApplication`、`launching: xxx` 等可观测节点，读者无法把章节方法直接落到 Perfetto。

### 维度 3：版本差异覆盖
- 评分：1/5
- 问题数：2
- 具体问题：
  1. [P0] 行 381-382 — `android.app.ZygotePreload` 不是 Android 9 引入；AOSP android-9.0.0_r1 无该接口，android-10.0.0_r1 起才有，并且它面向 App Zygote / isolated service（`useAppZygote`, `zygotePreloadName`），不是“系统 App/OEM 专用的主 Zygote 自定义预加载”。
  2. [P1] 行 385 — 把 Android 17 DeliQueue 归入 Zygote 版本演进，并宣称优化“Zygote fork 后第一个 Message 分发”，把 MessageQueue 演进和 Zygote 机制串成单一因果链，边界错位。

### 维度 4：知识盲区
- 评分：2/5
- 盲区数：1
- 具体问题：
  1. [P1] 版本演进 / 优化策略整体 — 缺少 App Zygote / child zygote / WebViewZygote 三者的职责边界，导致 `ZygotePreload` 的适用范围和现代 Android 的自定义 preload 能力讲不清。

### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] 行 101, 182, 239, 271, 297, 356, 429, 449, 459, 483, 491 — 多个关键数字没有绑定设备、build、Trace 或 benchmark，证据链不够。

### 维度 6：交叉引用一致性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] 行 101, 186, 192, 289, 483 与 §1.3 行 72-79、§8.2 行 142 — 本章写“Binder IPC”，相关章节写“Local Socket/zygote socket”，同一概念口径冲突。

## 统计
- P0 事实错误：4 处
- P1 重要缺失：3 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：2.0/5

## 闭环动作
- 写入 queue.json（P95）：1 处
- 写入 queue.json（P85）：1 处
- 写入 research-gaps.md：1 处
- 写入 suggestions.md：2 处
- 仅日志记录：0 处

## 下一 Review 候选
待下一轮按“最近 24 小时经 Task 6 处理且尚未 deep review”规则重新筛选。
