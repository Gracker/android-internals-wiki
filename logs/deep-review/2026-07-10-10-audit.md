# 深度技术 Review 闲时抽检 · 2026-07-10 10:35

## Review 目标
- 章节：20.6 稳定性度量与指标体系
- 文件：src/part5-app/ch20-stability/06-stability-metrics.md
- 模式：闲时抽检
- 范围：维度 1（源码引用）+ 维度 3（版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] `ANR 率与 Google Play Vitals 标准` 的系统 ANR 触发阈值表把 ContentProvider 写成固定 10 秒 ANR。复核 Android 17 后，10 秒常量是 provider 发布等待超时（`ContentResolver.CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS`），超时路径是 `processContentProviderPublishTimedOutLocked()` 按 `ApplicationExitInfo.REASON_INITIALIZATION_FAILURE` 移除进程；远端 provider 调用阻塞的 ANR 由 `ContentProviderClient.setDetectNotResponding()` 调用方指定超时，并通过 `ContentProviderHelper.appNotRespondingViaProvider()` 进入 ANR。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（已修复）
- 具体问题：
  1. [P1][AUTO-FIX] 将 ContentProvider 行从“全版本固定 10 秒”收窄为 Android 17 源码下的发布等待 10 秒 + 调用阻塞 ANR 调用方配置，避免把发布初始化失败路径写成通用 ContentProvider ANR 阈值。

## 证据
- AOSP android-17.0.0_r1: `frameworks/base/core/java/android/content/ContentResolver.java`，`CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS = 10 * 1000 * Build.HW_TIMEOUT_MULTIPLIER`。
- AOSP android-17.0.0_r1: `frameworks/base/services/core/java/com/android/server/am/ActivityManagerService.java`，`CONTENT_PROVIDER_PUBLISH_TIMEOUT_MSG` 延迟使用 `ContentResolver.CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS`。
- AOSP android-17.0.0_r1: `frameworks/base/services/core/java/com/android/server/am/ContentProviderHelper.java`，`processContentProviderPublishTimedOutLocked()` 以 `ApplicationExitInfo.REASON_INITIALIZATION_FAILURE` 移除进程；`appNotRespondingViaProvider()` 才调用 `mAnrHelper.appNotResponding()`。
- AOSP android-17.0.0_r1: `frameworks/base/core/java/android/content/ContentProviderClient.java`，`setDetectNotResponding(timeoutMillis)` 由调用方配置远端 provider 调用阻塞 ANR 超时。
- Android Developers: `Diagnose and fix ANRs`，Content provider not responding 的默认超时由 `ContentProviderClient.setDetectNotResponding()` 指定。
- 同轮复核：Input dispatch 默认 5 秒、FGS shortService / dataSync / mediaProcessing 边界、Broadcast Android 14+ 10-20s / 60-120s、Play Vitals 1.09% / 0.47% / 8% 阈值均未发现新 P0/P1。

## 闭环动作
- AUTO-FIX：1 处，修正 ContentProvider ANR / 发布等待超时口径。
- queue.json：0 处。
- suggestions.md：0 处。
- frontmatter：`task9_result=auto-fixed`，`task6_state=revisiting`，`pipeline_stage=task6_pending`，`last_task9_audit=2026-07-10`。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
