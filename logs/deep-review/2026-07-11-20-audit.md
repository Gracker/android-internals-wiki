# 闲时技术抽检 · 2026-07-11 20:30

## Review 目标
- 章节：20.4 ANR 治理策略
- 文件：src/part5-app/ch20-stability/04-anr-governance.md
- 状态：finalized → task6_pending（auto-fix 后回 Task6 复审）
- 模式：闲时抽检，仅审维度 1（源码引用准确性）与维度 3（版本差异覆盖）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：3
- 具体问题：
  1. [P0][AUTO-FIX] L91 — `CONTENT_PROVIDER_PUBLISH_TIMEOUT` 不是 Android 17 可定位常量；android-17.0.0_r1 实际为 `ContentResolver.CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS`，AMS 消息为 `CONTENT_PROVIDER_PUBLISH_TIMEOUT_MSG`。已改为 `ContentResolver.CONTENT_PROVIDER_PUBLISH_TIMEOUT_MILLIS`。
  2. [P0][AUTO-FIX] L425 — `frameworks/base/core/java/android/app/BroadcastReceiver.java` 路径不存在；android-17.0.0_r1 实际路径为 `frameworks/base/core/java/android/content/BroadcastReceiver.java`。已修正。
  3. [P2][AUTO-FIX] L587 — Cached app freezer 主实现锚点应指向 `CachedAppOptimizer.java`，`ProcessList.java` 只包含包冻结/cgroup 辅助逻辑。已修正引用。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] L381 — BroadcastReceiver 超时总结只写前台 10 秒 / 后台 60 秒，和本节前表 Android 14+ 10-20 秒 / 60-120 秒口径不完全一致。已补 Android 14+ `BroadcastConstants` 拉长窗口说明。

## 证据来源
- android-17.0.0_r1 `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp` 与 `libs/input/android/os/IInputConstants.aidl`：默认 input dispatch timeout 为 5000ms。
- android-17.0.0_r1 `ActivityManagerService.java` / `BroadcastConstants.java`：前台/后台广播基础超时 10s/60s，队列配置走 `BroadcastConstants.TIMEOUT`。
- android-17.0.0_r1 `ContentResolver.java` / `ActivityManagerService.java` / `ContentProviderHelper.java`：provider publish timeout 常量与处理链存在。
- android-17.0.0_r1 `ActivityManagerConstants.java` / `ActiveServices.java` / `Service.java`：FGS start timeout、shortService 3min + 10s ANR 宽限和 `onTimeout()` 链路存在。
- android-17.0.0_r1 `Binder.java`、`BroadcastReceiver.java`、`CachedAppOptimizer.java`、`art/runtime/monitor.cc`：正文引用的 Binder、goAsync、freezer、monitor 锚点存在。

## 闭环动作
- AUTO-FIX：已修改章节正文与 frontmatter sources / last_verified_against。
- 更新 frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_audit: 2026-07-11`。
- 写入 queue.json：0 处（P0 已高置信局部修复）。
- 写入 research-gaps.md：0 处。
- 写入 suggestions.md：0 处。
