# 深度技术 Review 闲时抽检 · 2026-06-30 07:25

## Review 目标
- 章节：13.4 命令行打开超大 Trace
- 文件：src/part3-tools/ch13-perfetto/04-large-traces.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 选择原因
- `last_task9_at=2026-04-28T06:20:00+08:00`，超过 21 天。
- 历史 Task9 记录有 P2：大 Trace 内存数据支撑、Bigtrace 边界。
- 本节包含 `trace_processor` / `traceconv` CLI 与 Python API，工具参数容易随 Perfetto 版本漂移。

## external-review
- 最近 3 天 `logs/external-review/` 根目录无 13.4 / large-traces / trace_processor 活跃文件。

## 复核资料
- AOSP android-17.0.0_r1 `external/perfetto/src/trace_processor/trace_processor_shell.cc`：classic help 包含 `--http-port PORT`、`--http-ip-address ip`、`-q/--query-file`、`-Q/--query-string`、`-e/--export`、`-W/--wide`，并保留 subcommand interface；classic path 不能组合 `-e` 与 `-q`。
- AOSP android-17.0.0_r1 `external/perfetto/src/traceconv/main.cc`：`profile` 模式不支持输出单个文件，需使用 `--output-dir DIR`；支持 `--alloc`、`--perf`、`--java-heap`、`--pid`、`--timestamps`。
- Perfetto 官方 Trace Processor 文档：当前推荐 subcommand `query` / `server http`，classic flag interface 仍可用。
- Perfetto 官方 traceconv 文档：当前页面为 `https://perfetto.dev/docs/quickstart/traceconv`；`https://perfetto.dev/docs/analysis/traceconv` 返回 404。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：3
- 具体问题：
  1. [P0 · AUTO-FIX] `trace_processor` HTTP 参数 — 原文说 AOSP `external/perfetto` 不应假设存在 `--http-ip-address`。Android 17 tag 的 `trace_processor_shell.cc` classic help 已包含该参数。已改为 Android 17 包含 `--http-port` / `--http-ip-address`，旧版或独立二进制以本机 help 为准。
  2. [P0 · AUTO-FIX] `traceconv profile` 命令 — 原文写 `./traceconv profile trace.perfetto-trace heap_profile.pb`。Android 17 `traceconv/main.cc` 与当前官方文档均说明 `profile` 输出目录应使用 `--output-dir`，且不支持单个 output file。已改为 `./traceconv profile --output-dir ./profiles trace.perfetto-trace`。
  3. [P0 · AUTO-FIX] `trace_processor` SQLite export 命令 — 原文写 `./trace_processor -q analysis.sql -e result.sqlite trace.perfetto-trace`。Android 17 `trace_processor_shell.cc` 明确拒绝组合 `-e` 与 `-q`；`export_subcommand.cc` 的当前用法是 `export sqlite -o FILE <trace_file>`。已改为 `./trace_processor export sqlite -o result.sqlite trace.perfetto-trace`，并补充 classic `-e` 不能和 `-q/--query-file` 同用。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2 · AUTO-FIX] traceconv 文档入口 — 原引用 `perfetto.dev/docs/analysis/traceconv` 已 404。已改为 `perfetto.dev/docs/quickstart/traceconv`，frontmatter sources 与参考资料同步更新。

## 统计
- P0 事实错误：3 处（均已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：1 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：3.8/5

## 闭环动作
- AUTO-FIX：4 处
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`task9_result=auto-fixed`，`task6_state=revisiting`，`pipeline_stage=task6_pending`，`last_task9_audit=2026-06-30`，`last_task9_autofix_at=2026-06-30`
