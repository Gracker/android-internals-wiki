# 深度技术 Review 抽检 · 2026-06-13 16:20

## 抽检目标
- 模式：闲时抽检（preflight：CANDIDATES=0 / TASK2B_BACKLOG=0 / IDLE_AUDIT=297）
- 章节：18.8 OpenGL ES 渲染链路
- 文件：src/part2-performance/ch18-rendering-pipelines/08-opengl-es.md
- 状态：finalized / task9_result=pass-tech-review
- 选择原因：last_task9_audit=2026-05-20，超过 21 天；该章曾有 native fence / BLAST / triple buffering P2/P0 历史风险，符合闲时抽检优先级。

## 抽检范围
- 维度 1：源码 / API 引用准确性
- 维度 3：版本差异与兼容性边界
- Android 版本边界：仅使用 Android 17 / API 37 及以下资料；AOSP `android-17.0.0_r1` tag 当前未取到，源码锚点使用 `android-16.0.0_r1`，未用 main/master 作为正文结论。

## 核对资料
- AOSP `android-16.0.0_r1` `frameworks/base/opengl/java/android/opengl/GLSurfaceView.java`：`GLThread`、`setRenderer()`、`requestRender()`、`onAttachedToWindow()`、`onDetachedFromWindow()`、`requestExitAndWait()`。
- AOSP `android-16.0.0_r1` `frameworks/base/core/java/android/provider/Settings.java`：`ANGLE_GL_DRIVER_SELECTION_PKGS = "angle_gl_driver_selection_pkgs"`、`ANGLE_GL_DRIVER_SELECTION_VALUES = "angle_gl_driver_selection_values"`。
- AOSP `android-16.0.0_r1` `frameworks/native/opengl/libs/EGL/eglApi.cpp`：`eglSwapBuffers()`、`eglClientWaitSyncKHR()`、`eglDupNativeFenceFDANDROID()` platform dispatch。
- AOSP `android-16.0.0_r1` `frameworks/native/libs/gui/BufferQueueProducer.cpp` / `BLASTBufferQueue.cpp`：`dequeueBuffer()`、`waitForFreeSlotThenRelock()`、`acquireNextBufferLocked()`。
- Khronos / AOSP `EGL_ANDROID_native_fence_sync` 规范：`EGL_SYNC_NATIVE_FENCE_ANDROID` 与 `eglDupNativeFenceFDANDROID()` 类型边界。
- Android Developers Vulkan / ANGLE 文档：Android 17+ 对 ANGLE APK / layer opt-in 增加 manifest metadata 路径。

## 审查结果

### 维度 1：源码 / API 引用准确性
- 评分：4/5
- P0：0
- P1：0
- P2：0
- P3：0
- 结论：未发现新的源码路径、API 名称或示例用法错误。
- 复核说明：
  1. `GLSurfaceView` 路径与生命周期口径成立。AOSP 中 `GLThread` 仍位于 `frameworks/base/opengl/java/android/opengl/GLSurfaceView.java`，`setRenderer()` 创建并启动 GLThread，detach 路径调用 `requestExitAndWait()`，reattach 时按旧 renderMode 重建。
  2. ANGLE 调试切换键名成立。`Settings.Global` 中 `angle_gl_driver_selection_pkgs` / `angle_gl_driver_selection_values` 在 Android 16 源码仍存在，正文 L315-L326 的命令名未发现拼写或路径错误。
  3. EGL native fence 修复点保持正确。正文已把 CPU wait 与 FD 导出拆开，并用 `EGL_SYNC_NATIVE_FENCE_ANDROID` + `eglDupNativeFenceFDANDROID()` 表达 native fence FD 导出，未复发 2026-05-20 的 P0。
  4. `dequeueBuffer` / release fence 归因已按 slot、outstanding buffer、返回 fence 分层，未再把所有 `eglSwapBuffers` 长耗时直接等同于 release fence。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0：0
- P1：0
- P2：0
- P3：1
- 结论：Android 9-11 BufferQueue、Android 12+ BLAST、Android 15/16 ANGLE 可选路径的主线边界没有发现反转或越界；正文未包含 Android 18 / API 38+ 内容。
- 版本观察：
  1. [P3] Android 17 ANGLE 调试路径 — Android Developers 当前文档对 Android 17+ 增加了通过 manifest metadata opt in ANGLE APK / layer 注入的说明。正文 `applicable_versions` 目前到 Android 16，且 L313-L326 只写 `Settings.Global` 命令，因此不是 P0/P1；若后续把本章扩展到 Android 17，建议补一小句说明 Android 17+ 的 ANGLE APK / layer opt-in 与 settings 强制切换分别适用的场景。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0
- P3 锦上添花：1
- 总体抽检评分：4.0/5

## 闭环动作
- queue.json：未写入
- research-gaps.md：未写入
- suggestions.md：未写入（闲时抽检无 P0/P1，仅日志记录 P3 版本观察）
- 章节正文：未改动
- frontmatter：更新 `last_task9_audit: "2026-06-13"`
