# Task 9 闲时抽检 · 2026-05-25 06:25

## 抽检目标
- 章节：7.10 图片加载与 Bitmap 性能优化
- 文件：src/part2-performance/ch07-smoothness/10-image-bitmap-performance.md
- 当前状态：finalized / ready-to-publish
- 触发原因：本轮 `CANDIDATES=0`、`IDLE_AUDIT=171`，进入闲时抽检模式；该章上次 Task9 为 2026-05-06，且历史 review 曾有 P2 建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮只做高风险快速复核，不做完整 6 维深审。

## 核对来源
- Android Developers Supported media formats：AVIF baseline profile 在 Android 14+ mandatory encoder/decoder；AV1 video encoder Android 14+、decoder Android 10+。
- Android Developers Android 12 features：Android 12 引入 AVIF image support。
- Android 14 CDD：Handheld media requirements 包含 AV1 hardware decoder 与 AVIF baseline hardware image decoder 条款。
- AOSP frameworks/base `libs/hwui/jni/BitmapFactory.cpp`：`inSampleSize` 进入 `SkAndroidCodec::getSampledDimensions()`；`javaBitmap != nullptr` 分支执行 `bitmap::reinitBitmap()` 后返回 `javaBitmap`；Gainmap 解码路径仍在 `getGainmapAndroidCodec()` / `decodeGainmap()`。
- AOSP frameworks/base `libs/hwui/pipeline/skia/SkiaGpuPipeline.cpp`：`prepareToDraw()` 对 `!bitmap->isHardware()` 执行 `PinAsTexture()` / `UnpinTexture()` / `flushAndSubmit()`。
- Android Developers Bitmap / Bitmap.Config / BitmapFactory.Options：HARDWARE added API 26、只存 graphic memory、直接读写像素抛 `IllegalStateException`，`inSampleSize` 文档仍保留 2 的幂兼容口径。

## 审计结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- P0/P1：0
- 结论：本轮未发现阻塞级源码错误。
- 已复核：`BitmapFactory.cpp` 的 `inBitmap` 返回对象语义与正文一致；`BitmapFactory.cpp` 的 sample size / fine scale 路径与正文一致；`SkiaGpuPipeline::prepareToDraw()` 的 hardware bitmap no-op 分支与正文一致；`Bitmap.java` 中 hardware bitmap 读像素异常、slow call、Gainmap API 与正文一致。
- 仅记录：正文仍有若干 `[待验证]` / 历史 P2 方向，例如 Gainmap 显存倍率、AVIF fallback 实现名和 Perfetto 调用栈采集条件；这些不是 P0/P1，不在闲时抽检中新增待办。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- 结论：版本口径可保留。
- 已复核：Bitmap 像素内存三段式版本边界与官方 Managing Bitmap Memory 一致；`Bitmap.Config.HARDWARE` API 26 边界一致；`ImageDecoder` API 28 边界一致；AVIF 的 Android 12 引入支持与 Android 14 mandatory baseline encoder/decoder / CDD hardware image decoder 条款之间没有冲突。
- 仅记录：如果后续 Task2B 处理 P2，可把 “Android 14 对部分新设备要求支持 AV1 硬件解码” 改得更精确：区分 Supported media formats 的 platform mandatory codec 口径、CDD handheld 条款、以及具体 SoC 是否走 AVIF hardware image decoder。

## 闭环动作
- queue.json：0
- research-gaps.md：0
- suggestions.md：0
- frontmatter：标注 `last_task9_audit: 2026-05-25`
- 日志：logs/deep-review/2026-05-25-06-audit.md

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0（未新增，历史 P2 保持）
- 抽检结论：pass-idle-audit
