# 深度技术 Review · 2026-07-11 05:34

## Review 目标
- 章节：8.18 Binder Trace 驱动的 Activity 冷启动性能分析
- 文件：src/part2-performance/ch08-responsiveness/18-binder-trace-cold-start-analysis.md
- 状态：ready-for-review
- 模式：post-Task2B 技术复审 + 高置信 auto-fix

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5（auto-fix 后）
- 问题数：5 组，均已 auto-fix
- 具体问题：
  1. [P0/AUTO-FIX] `android_binder_metrics_by_process` SQL 示例使用不存在的 `transaction_count`，Android 17 `binder.sql` 定义为 `event_count`。
  2. [P0/AUTO-FIX] kernel ftrace 配置写入不存在的 `binder/binder_reply`；android17-6.18 使用 `binder_transaction` 的 `reply=1`、`binder_command`、`binder_return`。
  3. [P0/AUTO-FIX] `binder_transaction` 字段/触发点旧口径：改为 `target_node`、`to_proc`、`to_thread`，并锚定 `binder_alloc_new_buf()` 前后。
  4. [P0/AUTO-FIX] `IActivityManager.contentProviderAcquire` 不存在；改为 `getContentProvider()` / `refContentProvider()`。
  5. [P0/AUTO-FIX] 参考链接从 generic `cs.android.com` / 404 Perfetto 文档改为 android-17.0.0_r1 或 android17-6.18 锚点。

### 维度 2：原理链完整性
- 评分：4/5（auto-fix 后）
- 问题数：3 组，均已 auto-fix
- 具体问题：
  1. [P0/AUTO-FIX] Frozen Reply 把同步与 oneway pending 混在一起；Android 17 同步 frozen 返回 `BR_FROZEN_REPLY`，oneway pending 才返回 `BR_TRANSACTION_PENDING_FROZEN`。
  2. [P0/AUTO-FIX] SharedPreferences/DataStore 被写成 Binder round-trip；AOSP 证据显示主要是应用进程内 `QueuedWork` / 文件 I/O 等待，不属于 Binder 预算。
  3. [P1/AUTO-FIX] oneway 优先级继承被写成 Android 17 全量保证；修正为仅在已选中目标 Binder 线程时才会调用 `binder_transaction_priority()`。

### 维度 3：版本差异覆盖
- 评分：4/5（auto-fix 后）
- 问题数：2 组，均已 auto-fix
- 具体问题：
  1. [P0/AUTO-FIX] “Android 17 frozen batch 合并扩展至同步事务”与 android17-6.18 源码不符；修正为 `TF_UPDATE_TXN` 仅替换 pending async oneway 事务。
  2. [P2/AUTO-FIX] PKMS “Android 17 固定降低到 3-5 次”缺少源码可推出的因果；改为必须以 `android_binder_txns` 聚合验证。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：本轮是源码/API/版本口径复核，未新增 research-gaps。

### 维度 5：数据与案例支撑
- 评分：4/5（auto-fix 后）
- 问题数：1 组，已 auto-fix
- 具体问题：
  1. [P2/AUTO-FIX] PKMS 延迟/次数从源码缓存机制直接外推；已改为 trace 聚合口径。

### 维度 6：交叉引用一致性
- 评分：4/5（auto-fix 后）
- 问题数：2 组，均已 auto-fix
- 具体问题：
  1. [P0/AUTO-FIX] 真实案例仍写 `IWindowManager.addView`，与本章 §4.3 修正后的 `IWindowSession.addToDisplay` / `relayout` 不一致。
  2. [P0/AUTO-FIX] `androidx.appsearch.startup` API 名错误；改为 `androidx.startup` / `AppInitializer`。

## 统计
- P0 事实错误：8 组，已全部 auto-fix
- P1 重要缺失：1 组，已 auto-fix
- P2 建议改进：2 组，已 auto-fix
- P3 锦上添花：0 处
- 开放 P0/P1：0 处
- 总体技术评分：4.0/5（auto-fix 后）

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- 直接修改章节文件：1 个
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-07-11`

## AUTO-FIX 证据摘录
- Perfetto android-17.0.0_r1 `binder.sql`：`android_binder_metrics_by_process` 列为 `event_count`；`android_binder_txns` 无 `is_merged`、`frozen_reply`、`parent_txn_id`。
- kernel/common android17-6.18 `binder_trace.h`：有 `binder_transaction`、`binder_transaction_received`、`binder_command`、`binder_return`；无 `binder_reply` tracepoint。
- kernel/common android17-6.18 `binder.c`：同步 frozen 目标返回 `BR_FROZEN_REPLY`；oneway frozen pending 返回 `BR_TRANSACTION_PENDING_FROZEN`；`TF_UPDATE_TXN` 只在 `TF_ONE_WAY | TF_UPDATE_TXN` pending async 替换路径生效。
- AOSP android-17.0.0_r1 `SystemServer.java`：`sMaxBinderThreads = 31`；`ProcessState.cpp` 普通进程默认 `DEFAULT_MAX_BINDER_THREADS = 15`。
- AOSP android-17.0.0_r1 `ActivityThread.acquireProvider()` / `IActivityManager.aidl`：Provider 获取路径为 `getContentProvider()` / `refContentProvider()`。
- AOSP android-17.0.0_r1 `SharedPreferencesImpl.apply()` / `QueuedWork.waitToFinish()`：SharedPreferences 等待不是 Binder round-trip。
