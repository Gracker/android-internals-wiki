# Task 9 闲时抽检 · 2026-06-09 04:26

## Preflight
- CANDIDATES: 0
- TASK2B_BACKLOG: 0
- IDLE_AUDIT: 293
- 进入闲时抽检模式：status=finalized 且 task9_result=pass-tech-review 的章节中，优先选择长期未复检且有源码/版本锚点的章节。

## 抽检目标
- 章节：20.12 SafeMode 崩溃循环判定与启动补偿链路
- 文件：src/part5-app/ch20-stability/12-safemode-crash-loop-recovery.md
- 状态：finalized / ready-to-publish
- 上次 Task9：2026-05-16T15:38:55+08:00
- 本次范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖
- Android 版本边界：未使用 Android 18 / API 38+ 材料；`android-17.0.0_r1` 未在 android.googlesource 的 `frameworks/base` tag 列表中公开，本轮使用已公开的 `android-16.0.0_r4` 复核，不把 main/master 作为正文结论来源。

## external-review 输入
- logs/external-review/ 根目录无活跃文件匹配本章节，本轮未消费 external-review，未执行归档。

## 一手资料复核
- AOSP `android-16.0.0_r4`：`frameworks/base/core/java/android/util/AtomicFile.java` 存在 `startWrite()`、`finishWrite()`、`failWrite()`；`finishWrite()` sync/close 后 rename `.new` 到目标文件，`failWrite()` sync/close 后删除 `.new`。
- AOSP `android-16.0.0_r4`：`frameworks/base/core/java/com/android/internal/os/RuntimeInit.java` 存在 `LoggingHandler`、`KillApplicationHandler`、`mCrashing` 防重入、`Process.killProcess()` 和 `System.exit(10)` 路径。
- AOSP `android-16.0.0_r4`：`frameworks/base/core/java/android/app/ActivityManager.java` 的 `getHistoricalProcessExitReasons(packageName, pid, maxNum)` 文档说明结果按从近到远排序，历史记录保存在 ring buffer；`pid=0` 表示忽略 pid；`setProcessStateSummary()` 最大 128 bytes 且禁止敏感信息；`isLowMemoryKillReportSupported()` 读取 `persist.sys.lmk.reportkills`。
- AOSP `android-16.0.0_r4`：`frameworks/base/core/java/android/app/ApplicationExitInfo.java` 的 `getTraceInputStream()` 注释说明 ANR trace 通常可用，API 31 起 native tombstone protobuf 可用于 `REASON_CRASH_NATIVE`，且全局 circular buffer 可能导致 null。
- AOSP `android-16.0.0_r4`：`frameworks/base/core/java/android/webkit/WebViewClient.java` 的 `onRenderProcessGone()` 注释说明同一 renderer 可能关联多个 WebView，受影响 WebView 会分别回调，传入 WebView 不可继续使用，默认实现返回 `false`。
- AOSP `android-16.0.0_r4`：`frameworks/base/core/java/android/webkit/WebViewRenderProcessClient.java` 说明 `onRenderProcessUnresponsive()` 最小回调间隔为 5 seconds；应用主动 terminate renderer 时必须处理所有相关 WebView 的 `onRenderProcessGone()`。
- 跨版本复核：`ApplicationExitInfo.java` 在 `android-10.0.0_r47` 不存在，在 `android-11.0.0_r48` 起存在；`REASON_USER_STOPPED` 在 `android-13.0.0_r83` 已存在，`REASON_PACKAGE_STATE_CHANGE` 在 `android-14.0.0_r75` 起存在。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- P0/P1：1（已 auto-fix）
- 结论：正文引用的 `AtomicFile`、`RuntimeInit`、`ActivityManager`、`ApplicationExitInfo`、`WebViewClient`、`WebViewRenderProcessClient` 路径和关键 API 均能在 AOSP `android-16.0.0_r4` 复核到；未发现类名、方法名或行为描述级事实错误。
- [P1][AUTO-FIX] 原 frontmatter 与正文 6 处校验标签使用 `AOSP master`，且 `applicable_versions` 写到 Android 17/API 37；本轮无法在 android.googlesource 复核 `android-17.0.0_r1`。已将源码校验标签收敛到 `AOSP android-16.0.0_r4`，并将适用范围暂回退为 Android 8/API 26 - Android 16/API 36。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0/P1：0
- 结论：`ApplicationExitInfo` 从 Android 11 起可用、API 31 起 native tombstone protobuf、LMK 上报能力和 WebView renderer 回调语义与正文主线一致；没有 Android 18/API 38+ 内容。
- P2 记录：原文 “Android 14 之后对用户请求、用户停止、包状态变化等 reason/subreason 拆得更细” 容易把 `REASON_USER_STOPPED` 也读成 Android 14 新增。已顺手改成 Android 13 已有 `REASON_USER_STOPPED`、Android 14 起新增 `REASON_PACKAGE_STATE_CHANGE`，避免版本边界含混。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix，不写 queue）
- P2 建议改进：1 处（已随 auto-fix 局部修正）
- 总体技术评分：4/5

## 闭环动作
- 直接修复章节：1 处源码/版本锚点问题，附带修正 1 处版本差异表述
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- 章节 frontmatter：`task9_result: auto-fixed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_audit: "2026-06-09"`
- 自动晋升 finalized：0（抽检对象已 finalized；auto-fix 后回到 Task6 复审）
