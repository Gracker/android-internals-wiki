# 深度技术 Review 闲时抽检 · 2026-06-20 11:30

## Review 目标
- 章节：2.23 SurfaceFlinger VSync Scheduler 与 DisplayFrameRate 策略
- 文件：src/part1-fundamentals/ch02-rendering/23-vsync-scheduler-displayframerate.md
- 状态：finalized
- 模式：闲时抽检，只审维度 1（源码引用）与维度 3（版本差异）
- 选择原因：当前无 `task9_state: pending` 候选；Task2B backlog 为 0；本节 `last_task9_at=2026-05-27T13:20:00+08:00`，超过 21 天；此前无 `last_task9_audit`，适合复查易过期的 Scheduler / ARR 版本口径。
- Android 边界：最高 Android 17 / API 37；未使用超出范围材料。源码锚点复核到 `android-17.0.0_r1`，未引用 main/master 结论。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 具体问题：无 P0/P1。`VsyncSchedule.cpp`、`VSyncPredictor.cpp`、`VSyncDispatchTimerQueue.cpp`、`Scheduler.cpp`、`RefreshRateSelector.cpp`、`SurfaceFlinger.cpp` 在 `android-17.0.0_r1` 下路径均存在；`nextAnticipatedVSyncTimeFrom()`、`setRenderRate()`、`setVsyncConfig()`、`chooseRefreshRateForContent()`、`setFrameRateVote` 等正文引用入口仍可对上。
- 证据：
  - AOSP android-17.0.0_r1 `services/surfaceflinger/Scheduler/VsyncSchedule.cpp`：`createTracker()` 位于 L114-L133。
  - AOSP android-17.0.0_r1 `services/surfaceflinger/Scheduler/VSyncDispatchTimerQueue.cpp`：`VSyncDispatchTimerQueueEntry::schedule()` 仍通过 `nextAnticipatedVSyncTimeFrom(max(lastVsync, now + workDuration + readyDuration), ...)` 反推目标 VSync。
  - AOSP android-17.0.0_r1 `services/surfaceflinger/SurfaceFlinger.cpp`：snapshot `frameRate` 仍写入 `LayerProps.setFrameRateVote` 后进入 Scheduler 刷新率选择。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] §一 `VsyncSchedule::createTracker()` — 正文只写 Android 16 默认参数 20 / 6 / 20，章节适用范围覆盖 Android 17，但 `android-17.0.0_r1` 在 `use_last_vsync_predict` 打开、目标显示设备存在 VRR config 且 present fence 可用时，会把 `historySize` / `minSamples` 改成 1 / 1。已在正文补充 Android 17 条件分支，避免把 Android 16 默认收敛窗口外推到 Android 17 VRR 设备。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.3/5

## 闭环动作
- AUTO-FIX：1 处
  - 在 §一补充 Android 17 `VsyncSchedule::createTracker()` 的 `use_last_vsync_predict` + VRR + present fence 单样本预测模式边界。
- 元数据修复：原文件缺少 frontmatter 结束分隔线，已在正文 H1 前补回 `---`，确保本轮 Task9 字段可被流水线解析。
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-06-20`、`last_task9_audit: 2026-06-20`。
- 写入 queue.json（P95）：0 处（高置信局部 auto-fix，不另建队列）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 归档 external-review：0 处（本轮无活跃 external-review 输入）


---

# 深度技术 Review 抽检 · 2026-06-20 11:42

## Review 目标
- 章节：2.1 Android 渲染架构全景
- 文件：src/part1-fundamentals/ch02-rendering/01-rendering-overview.md
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- 状态：finalized → ready-for-review（auto-fix 后回到 Task6）

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 抽检证据：AOSP `android-16.0.0_r1` 与 `android-17.0.0_r1` 均可打开 `View.java`、`Choreographer.java`、`BufferQueueConsumer.cpp`、`BufferItemConsumer.cpp`、HWUI `RenderNode.*`、`pipeline/skia/*`、SurfaceFlinger `Scheduler/` 与 `SurfaceFlinger.cpp`；关键入口名与正文一致。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][AUTO-FIX] “Android 16 要求 64 位新设备支持 Vulkan 1.4” 与 Android 16/17 CDD 不一致。Android 16 CDD 对非低内存 64 位 handheld 设备硬性要求为 Vulkan 1.1；Android 16/17 CDD 对 Vulkan 1.3 是 strongly recommended，未见 Vulkan 1.4 兼容性硬要求。

## AUTO-FIX
- 修改位置：正文“三缓冲机制/在 Android 中的实现”与“版本演进/Android 16(2025)”两处。
- 修改摘要：将 Vulkan 1.4 硬性要求改为 Android 16 CDD 硬性 Vulkan 1.1，并补充 Vulkan 1.3 是 strongly recommended，不再把 1.4 写成设备兼容性要求。
- 证据来源：Android 16 CDD 7.1.4.2/H-2-1；Android 16/17 CDD 7.1.4.2 core Vulkan 条款；AOSP Gitiles `android-16.0.0_r1` / `android-17.0.0_r1` 源码锚点抽检。
- Frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task2b_state=fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_autofix_at=2026-06-20`、`last_task9_audit=2026-06-20`。

## 统计
- P0 事实错误：1 处（已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 直接修改章节：1 处问题 / 3 个正文位置
- 回到 Task6 复审：1 章
- 复核补充：同类 Vulkan CDD 版本断言还出现在“Vulkan 在 Android 中的采用”段，已纳入同一 auto-fix；本次共修正 3 个正文位置。

