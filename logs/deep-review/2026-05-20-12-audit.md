# 深度技术 Review 抽检 · 2026-05-20 12:22

## 抽检目标
- 模式：闲时抽检（无 task9_state: pending 候选）
- 章节：19.06 BlockCanary
- 文件：src/part3-tools/ch19-apm/06-blockcanary.md
- 状态：finalized
- 上次 Task 9：2026-04-25T02:26:14+08:00（约 25 天前）

## 抽检范围
- 维度 1：源码 / 上游引用准确性
- 维度 3：版本差异与兼容性边界

## 核对资料
- markzhai/AndroidPerformanceMonitor `build.gradle`：AGP 2.2.2、`LIBRARY_COMPILE_SDK_VERSION = 23`、`LIBRARY_TARGET_SDK_VERSION = 22`
- markzhai/AndroidPerformanceMonitor `README.md`：`compile` / `debugCompile` 依赖写法、`setMessageLogging()` 说明、`provideBlockThreshold()` / `provideDumpInterval()` / `provideQualifier()` / `provideNetworkType()` / `providePath()`
- markzhai/AndroidPerformanceMonitor `LooperMonitor.java` / `StackSampler.java` / `AbstractSampler.java`：`Printer` 监听、`Thread.getStackTrace()` 采样、首次采样延迟与采样间隔
- AOSP `frameworks/base/core/java/android/os/Looper.java` tags：`android-9.0.0_r1` 与 `android-10.0.0_r1`

## 审查结果

### 维度 1：源码 / 上游引用准确性
- 评分：4/5
- P0：0
- P1：0
- P2：1
- 结论：章节对 BlockCanary 公开构建基线、`Looper.setMessageLogging()`、单槽位 `Printer`、配置项和主线程堆栈采样的主线描述基本准确。
- 具体问题：
  1. [P2][源码细节边界] L193-L201 / L233 — 文中把抓栈描述为“按固定间隔”并举 1200ms block、300ms 间隔最多 4 个堆栈。上游 BlockCanary 的 `AbstractSampler.start()` 不是立即采样，而是先按 `getSampleDelay() = provideBlockThreshold() * 0.8` 延迟第一次采样，再按 `provideDumpInterval()` 继续；默认 `provideDumpInterval()` 又等于阈值。因此这个例子更像自研采样模型，不应让读者误以为是 upstream 的实际 sample count。
     - 建议：正文保留概念例子时标注“自研实现示例”；若描述 BlockCanary upstream，则补 `getSampleDelay()` 与 `provideDumpInterval()` 边界。

### 维度 3：版本差异覆盖
- 评分：3/5
- P0：0
- P1：1
- P2：0
- 具体问题：
  1. [P1][版本差异/API 版本错误] L269 — “Android 9（API 28）之后，`Looper` 内部有 `@hide` 的 `Looper.Observer`”版本边界错误。AOSP `android-9.0.0_r1` 的 `Looper.java` 没有 `Observer` / `setObserver`；AOSP `android-10.0.0_r1` 才出现 `private static Observer sObserver`、`setObserver()`、`messageDispatchStarting()` / `messageDispatched()`。
     - 建议：改成 Android 10（API 29）起存在隐藏 `Looper.Observer`；Android 9 仍以公开 `Printer` / `setMessageLogging()` 为边界，并补 Hidden API 限制。

## 统计
- P0 事实错误：0
- P1 重要缺失：1
- P2 建议改进：1
- P3 锦上添花：0
- 总体抽检评分：3.5/5

## 闭环动作
- queue.json：写入 P85 1 条（`task9-audit-19.06-looper-observer-api-version`）
- research-gaps.md：未写入
- suggestions.md：闲时抽检仅记录日志，未写入
- frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: "2026-05-20"`
