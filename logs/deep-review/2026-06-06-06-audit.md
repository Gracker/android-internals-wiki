# Task9 闲时抽检 · 2026-06-06 06:20

## 抽检目标
- 章节：10.6 内存抖动与频繁 GC
- 文件：src/part2-performance/ch10-memory-perf/06-memory-churn.md
- 状态：finalized
- 模式：闲时抽检
- 范围：维度 1 源码引用准确性；维度 3 版本差异覆盖

## 版本边界
- AIW 范围限定到 Android 17 / API 37。
- 本轮 `git ls-remote` 复核 AOSP `platform/art` 与 `frameworks/base`：存在 `android-14.0.0_r1`、`android-15.0.0_r1`、`android-16.0.0_r1`；未发现 `android-17.0.0_r1` tag。
- 正文的 Android 17 分代 CMC 默认状态已标为待验证，未作为已验证结论使用。

## 源码与官方资料核验
- AOSP `art/runtime/gc/heap.cc`：`android-14.0.0_r1` 中 `gUseReadBarrier` 对应 `kCollectorTypeCC`，同时源码已有 `kCollectorTypeCMC` 相关实现入口；`android-15.0.0_r1` 中 `gUseUserfaultfd` 对应 `kCollectorTypeCMC` / `kCollectorTypeCMCBackground`。
- AOSP `art/runtime/gc/collector/mark_compact.cc`：`android-14.0.0_r1` 已有 `MREMAP_DONTUNMAP`、`KernelSupportsUffd()`、`ShouldUseUserfaultfd()` 与 `SigbusHandler()`；`android-16.0.0_r1` 的 `ShouldUseGenerationalGC()` 受 `use_generational_cmc()` 与 `persist.device_config.runtime_native_boot.use_generational_gc` 约束。
- AOSP `frameworks/base/core/java/com/android/internal/os/BinderInternal.java`：`android-16.0.0_r1` 中 `GcWatcher` 与 `addGcWatcher()` 路径存在；`ActivityThread.java` 通过 `BinderInternal.addGcWatcher()` 注册，正文路径修正有效。
- Perfetto 官方 native heap profiler 文档：Java Allocation Profiling 标注 Android 12+，使用 `heaps: "com.android.art"` 或 `tools/heap_profile android --heaps com.android.art`，正文 heapprofd 入口与版本边界有效。

## 抽检结论
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1（既有）：L94 / L98 / L123-L127 / L437 的 GC 版本线仍建议与 §4.3 统一：Android 14 已有 CMC / UFFD 相关源码入口，Android 15 更适合作为 userfaultfd CMC 默认主线的起点，Android 16/17 分代 CMC 需要继续保留 flag、设备配置和正式 tag/release notes 边界。
- P3：0

## 闭环动作
- 未写入 queue.json。
- 未写入 research-gaps.md。
- 未写入 suggestions.md：该 P2 已在 2026-05-08 Task9 日志中记录，本轮不重复追加。
- 章节 frontmatter 标注 `last_task9_audit: "2026-06-06"`，正文不改动。
