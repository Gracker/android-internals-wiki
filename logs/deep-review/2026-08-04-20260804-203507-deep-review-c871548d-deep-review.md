# AIW Deep Review Evidence · 2026-08-04

- lane: deep-review
- run_id: `20260804-203507-deep-review-c871548d`
- target: `src/part1-fundamentals/ch08-startup/8.1-bootanalyze-optimization-toolchain.md`
- baseline: `android-17.0.0_r1`
- materials: empty; review relied on existing body plus AOSP tag source spot-checks.

## 审计结论

本章不是薄占位稿：正文已经包含 bootanalyze / bootstat / init / Zygote / SystemServer / AMS 的明确源码边界，并主动排除了旧稿中常见误读（如 bootanalyze 不是 bootstat 数据源、lazy preload 不是 Android 17 新增、FULL_PSS_MIN_INTERVAL 不是固定 60 秒）。本轮做源码边界复查和小范围修补，保持 `ready-for-review`，更新为 deep-reviewed/source-verified 状态。

## 源码 spot-check

- `system/extras/boottime_tools/bootanalyze/bootanalyze.py`: shebang 为 Python 3；包含 `BootComplete`、`LauncherStart`、`fetch_boottime_property()` 等路径。
- `system/extras/boottime_tools/bootanalyze/bootanalyze.sh`: README 示例变量拼写为 `CONFIG_YMAL`。
- `system/extras/boottime_tools/bootanalyze/config.yaml`: 包含 BootComplete / LauncherStart 事件正则。
- `frameworks/base/core/java/com/android/internal/os/ZygoteInit.java`: 接受 `--enable-lazy-preload`，lazy path 使用 `ZygoteInitTiming_lazy`。
- `frameworks/base/services/java/com/android/server/SystemServer.java`: `Producer.init(..., 4 * 1024)`；APEX service 后调用 `sealStartedServices()`；60 秒 `Slog.wtf` 是异常检查。
- `frameworks/base/services/core/java/com/android/server/am/ActivityManagerService.java`: boot complete 后调用 `ProcessList.startPsiMonitoringAfterBoot()`，注释为 skipped earlier；PSS 延后使用 `mConstants.FULL_PSS_MIN_INTERVAL`。

## P0 / P1 / P2

- P0: 无。
- P1: 关闭历史 `needs-body-apply` blocker（`finding-8c411b5c0b84`）：当前目标章已有可审计正文，不再是受保护历史错稿占位。
- P2: frontmatter 来源列表未覆盖正文引用的 `bootanalyze.sh`、`builtins.cpp`、`init.rc`、`init.zygote64_32.rc`；正文中 Zygote rc 路径写法可更精确；小标题缺少中文空格。

## 实际补丁

1. 更新 frontmatter：
   - `last_verified: 2026-08-04`
   - `pipeline_stage: deep-reviewed`
   - `task6_state: source-verified`
   - `task9_state: deep-reviewed`
   - `last_deep_review_at` / `last_deep_review_run_id`
2. 扩充 AOSP source anchors：补入 `bootanalyze.sh`、`system/core/init/builtins.cpp`、`system/core/rootdir/init.rc`、`system/core/rootdir/init.zygote64_32.rc`。
3. 正文小修：将 `init.zygote64_32.rc` 明确为 `system/core/rootdir/init.zygote64_32.rc`；修复 `Perfetto解释` 为 `Perfetto 解释`。
4. 维护 issue pool：关闭 `finding-8c411b5c0b84` 为 `fixed`。

## 风险与后续

- 未引入 Android 18/API38+ 结论。
- 未新增 `src/**/*.md`，未修改 `src/SUMMARY.md`。
- 本轮没有设备实测样本，因此不新增具体毫秒级 KPI；章节继续以源码边界和可复核方法论为主。

## 本地校验

- `python3 scripts/check-metadata.py --files src/part1-fundamentals/ch08-startup/8.1-bootanalyze-optimization-toolchain.md`：通过。
- `python3 scripts/check-summary-links.py`：通过；报告 2 个既有 duplicate SUMMARY warning，本轮未触碰 `src/SUMMARY.md`。
- `git diff --check`：通过。
