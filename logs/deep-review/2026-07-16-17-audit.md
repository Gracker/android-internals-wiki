# 闲时抽检 · 2026-07-16 17:27

## 抽检目标
- 章节：18.4 Android View 混合渲染链路
- 文件：src/part2-performance/ch18-rendering-pipelines/04-android-view-mixed.md
- 状态：finalized（pipeline_stage=ready-to-publish）
- 抽检原因：last_task9_at=2026-05-09，距今 68 天 > 21 天阈值
- 抽检维度：维度 1（源码引用）+ 维度 3（版本差异）

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0（P0/P1）

**验证记录：**
- `frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp` → 路径有效，锚定 android-17.0.0_r1
  - `commitTransactions()` / `latchBuffers()` 方法名与 AOSP 一致
- `frameworks/native/libs/gui/SurfaceComposerClient.cpp` → 路径有效
  - `Transaction::merge()` / `setDesiredPresentTime()` 方法名与 AOSP 一致
- `frameworks/native/libs/gui/Surface.cpp` → 路径有效
  - `Surface::setBuffersTimestamp()` 是 native/EGL 层方法，描述准确（非 Java `Surface` 公共 API）
- 章节中所有 AOSP 引用均有 `[已验证]` 标记，溯源完整。

**轻微观察（不构成 P0/P1）：**
- 跨 Surface 同步机制表中 `SurfaceSyncGroup` 一行的 `@hide` 边界说明准确，SurfaceView 的 `add(SurfaceView, ...)` 为 framework/internal 路径这一陈述与 AOSP `frameworks/base/core/java/android/view/SurfaceSyncGroup.java` 中 `@hide` 标注一致。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（P2 待复核）

**验证记录：**
- `addTransactionCommittedListener` 标为 Android 13 / API 33+。  
  **待复核**：AOSP 历史显示 `SurfaceControl.Transaction#addTransactionCommittedListener(Executor, TransactionCommittedListener)` 基础签名实际在 Android 11 (API 30) 即引入；Android 13 主要强化了 future-fence 相关路径与回调时序。章节此处表述可能略简化，但不影响整体机制说明的准确性。如需精确到 overload 级别（API 30 基础版 vs API 33 强化版），建议下一轮 Review 复核。
- `SurfaceSyncGroup` 标为 Android 14 / API 34+ → 正确，`SurfaceSyncGroup` 公开类在 API 34 引入。
- BLAST / Transaction 模型允许几何 + Buffer 同一 Transaction 的描述 → 正确，与 Android 12 引入的 BLASTBufferQueue 行为一致。
- 章节正确处理了 `@hide` / `framework internal` / `普通应用不可直接调用` 的边界。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 待复核：1 处（API 级别边界精确化，非阻断性）
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：1 处（P2 待复核项）
- frontmatter 更新：`last_task9_audit: 2026-07-16`
- 本次不自动晋升 finalized（章节已是 finalized 状态）
- 下一轮 Task 9 全量 Review 建议复核 `addTransactionCommittedListener` API 级别精确性

## 备注
- 本次为 闲时抽检模式（Task 9 无 pending 候选），聚焦维度 1+3 快速扫描。
- 该章节 18.4 在 2026-05-09 完成 Task 9 全量 Review，距今 68 天未复检；本次抽检未发现 P0/P1，仅一处 P2 待复核项。
- 章节整体技术质量稳定，可继续作为 published-finalized 内容使用。
