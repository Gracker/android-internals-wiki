# 闲时抽检 · Task 9 深度技术 Review · 2026-06-21 01:29

## 抽检目标
- 章节：18.13 WebView 渲染管线
- 文件：src/part2-performance/ch18-rendering-pipelines/13-webview-rendering.md
- 状态：finalized
- 抽检原因：无 pending Review 章节；Task2B backlog 为 0；该章节 last_task9_audit=2026-05-31，且历史 Task9 曾记录 P2 建议，适合做闲时复检。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 范围边界：仅核验 Android 17 / API 37 及以下资料；未使用 Android 18 / API 38+ 资料。Chromium main 只用于 provider 源码路径存在性复核，不作为 Android 17 平台结论。

## 证据来源
- AOSP `platform/frameworks/base` `android-17.0.0_r1`：`WebView.java`、`WebChromeClient.java`、`WebViewFactory.java`、`WebViewUpdateServiceImpl2.java` 均可访问；`WebViewFactory.java` 中入口是 `getProvider()`，`WebView.java` 私有 `getFactory()` 包装它。
- AOSP `platform/frameworks/base`：`android-14.0.0_r1` 有 `WebViewUpdateServiceImpl.java`；`android-15.0.0_r1` 同时有 `WebViewUpdateServiceImpl.java` 与 `WebViewUpdateServiceImpl2.java`；`android-15.0.0_r20`、`android-16.0.0_r1`、`android-17.0.0_r1` 使用 `WebViewUpdateServiceImpl2.java`。
- AOSP `platform/frameworks/native` `android-17.0.0_r1`：`GraphicBuffer.h` 存在；`native_window.h` 定义的是 `ANativeWindow_Buffer`，不是 `ANativeWindowBuffer`。
- Chromium `chromium/src` main：`browser_view_renderer.cc`、`hardware_renderer.cc`、`overlay_processor_webview.cc`、`draw_fn.h`、`aw_draw_fn_impl.cc` 路径与关键符号存在；`aw_contents.cc` 中是 `AwContents::OnDraw()` 调用 `BrowserViewRenderer::OnDrawSoftware()`。

## 抽检结论

### 维度 1：源码引用准确性
- 评分：4/5（auto-fix 后 5/5）
- 问题数：4
- 具体问题：
  1. [P0][AUTO-FIX] L15 / L422 — `android-17.0.0_r1 tag unavailable` 已过期；`WebViewUpdateServiceImpl2.java` 在 Android 17 tag 中存在。已改为 Android 15 later tags - Android 17。
  2. [P0][AUTO-FIX] L138 — `WebViewFactory.getFactory()` 口径不准。Android 17 中 `getFactory()` 是 `WebView.java` 私有包装，实际落到 `WebViewFactory.getProvider()`。已修正。
  3. [P0][AUTO-FIX] L221 / L223 — Chromium `AwContents.onDrawSoftware()` 锚点不存在。已改为 Java `AwContents.onDraw()` / native `AwContents::OnDraw()` → `BrowserViewRenderer::OnDrawSoftware()`。
  4. [P0][AUTO-FIX] L424 — `ANativeWindowBuffer` 类型名错误；Android 12-17 `native_window.h` 定义为 `ANativeWindow_Buffer`。已修正。

### 维度 3：版本差异覆盖
- 评分：4/5（auto-fix 后 5/5）
- 问题数：1
- 具体结论：WebViewUpdateServiceImpl / Impl2 的 Android 14-17 版本边界已更新；正文关于 `SurfaceControl` 子 Surface 不能只按 Android major version 判断的结论仍成立。

## 统计
- P0 事实错误：4 处（均已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5（auto-fix 后 5/5）

## 闭环动作
- 章节内容改动：4 组源码锚点 / 版本边界小修
- frontmatter 标注：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_audit: 2026-06-21`、`last_task9_autofix_at: 2026-06-21`
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
