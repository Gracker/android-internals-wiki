# 深度技术 Review 闲时抽检 · 2026-07-10 06:38

## Review 目标
- 章节：13.15 BufferQueue 阻塞的 Perfetto 识别
- 文件：src/part3-tools/ch13-perfetto/15-bufferqueue-blocking-perfetto.md
- 状态：finalized → task6_pending（auto-fix 后回 Task6）
- 模式：闲时抽检
- 选择说明：`CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检；本节 `last_task9_at=2026-05-28T09:20:00+08:00`，距今超过 21 天。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] frontmatter/source_refs 与「Android 16+/main BUFFER_RELEASE_CHANNEL 待验证」段落 — 章节仍把主源码锚点写成 `android14-release`，且把 Android 16/17 release notify 行为写成待逐行确认；这不符合当前 AIW 基准必须锚定 `android-17.0.0_r1` 的规则。已改为 `android-17.0.0_r1` `BufferQueueProducer.cpp` / `BufferQueueConsumer.cpp` / `BufferQueueCore.cpp` / `BufferQueueCore.h`，并在正文说明 Android 16 为 `BUFFER_RELEASE_CHANNEL` flag 形态，Android 17 已保留 `waitForBufferRelease()` / `notifyBufferReleased()` 路径且最终经 `BufferQueueCore::notifyBufferReleased()` 调用 `mDequeueCondition.notify_all()`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] Android 16/17 边界 — 原文把 Android 16/17 行为保留为待验证项，容易让读者误以为 Android 17 仍没有 release notify 路径结论。已收束为：Android 14/15 作为传统条件变量路径；Android 16/17 作为 release notify 路径；不把 `BUFFER_RELEASE_CHANNEL` flag 名称当成 Android 17 稳定接口。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.0/5

## 闭环动作
- 内容 auto-fix：1 处
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task2b_state=fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_audit=2026-07-10`、`last_task9_autofix_at=2026-07-10`
- Android 版本边界：未使用 Android 18/API 38+；正文主锚点已更新到 `android-17.0.0_r1`。

## 证据锚点
- Perfetto FrameTimeline docs：`https://perfetto.dev/docs/data-sources/frametimeline`，可核到 `Buffer Stuffing` 与 `actual_frame_timeline_slice` 字段。
- AOSP `android-17.0.0_r1`：`frameworks/native/libs/gui/BufferQueueProducer.cpp` 可核到 `waitForBufferRelease()` 与 `mDequeueCondition.wait()` / `wait_for()`。
- AOSP `android-17.0.0_r1`：`frameworks/native/libs/gui/BufferQueueCore.cpp` 可核到 `BufferQueueCore::notifyBufferReleased()` 调用 `mDequeueCondition.notify_all()`。
- AOSP `android-17.0.0_r1`：`frameworks/native/libs/gui/BufferQueueConsumer.cpp` 可核到 release 路径调用 `notifyBufferReleased()`。
- AOSP `android-16.0.0_r1` / `android-15.0.0_r1`：用于版本对比，确认 Android 16 才出现 flag 包裹路径，Android 15 未命中 `BUFFER_RELEASE_CHANNEL`。
