# Task 9 闲时抽检 · 2026-07-08 08:31

## 审计目标
- 章节：11.5 Wakelock 机制与功耗分析
- 文件：src/part2-performance/ch11-power/05-wakelock.md
- 状态：finalized / pass-tech-review
- 模式：闲时抽检
- 审计范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）
- Android 基准：android-17.0.0_r1 / Android 17 / API 37

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] frontmatter 与正文验证标注、参考资料源码路径 — 仍写 AOSP android-17-beta3，且 Android 17 r1 下 `frameworks/base/core/java/android/app/AlarmManager.java`、`frameworks/base/services/core/java/com/android/server/AlarmManagerService.java`、`system/hardware/interfaces/suspend/1.0/default/SystemSuspend.cpp` 均不存在。已验证的 r1 路径分别为 `frameworks/base/apex/jobscheduler/framework/java/android/app/AlarmManager.java`、`frameworks/base/apex/jobscheduler/service/java/com/android/server/alarm/AlarmManagerService.java`、`system/hardware/interfaces/suspend/aidl/default/SystemSuspend.cpp`。
  2. [P0] 第857-865行 IPowerStats HAL API — Android 17 r1 的 `IPowerStats.aidl` 暴露 `getEnergyMeterInfo()` / `readEnergyMeter(in int[] channelIds)`，正文写的 `getRailInfo()` / `getEnergyData()` 未找到。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] 第241-247行 App Standby Bucket 与 wakelock 限制 — 正文写 `enforceWakeLockTimeout()` / `RESTRICTED_WAKELOCK_MAX_TIMEOUT` 是 Android 17 主线的 restricted bucket wakelock 单次超时机制；本轮在 `PowerManagerService.java` 未找到这些符号，在 `AppStandbyController.java` 也未复核到 direct wakelock timeout 入口。需要按 android-17.0.0_r1 重新校准该段机制口径。

## 证据锚点
- `PowerManagerService.java`（android-17.0.0_r1）：`acquireWakeLockInternal`、`WakeLock` 内部类存在；未找到 `RESTRICTED_WAKELOCK_MAX_TIMEOUT` / `enforceWakeLockTimeout`。
- `AlarmManager.java`（android-17.0.0_r1）：实际路径为 `frameworks/base/apex/jobscheduler/framework/java/android/app/AlarmManager.java`，`OnAlarmListener` 与 exact alarm 重载存在。
- `AlarmManagerService.java`（android-17.0.0_r1）：实际路径为 `frameworks/base/apex/jobscheduler/service/java/com/android/server/alarm/AlarmManagerService.java`，`mWakeLock`、`setWorkSource()`、`mWakeLock.acquire()` / `release()` 存在。
- `SystemSuspend.cpp`（android-17.0.0_r1）：实际路径为 `system/hardware/interfaces/suspend/aidl/default/SystemSuspend.cpp`，包含 `wakeup_count` 与 `/sys/power/state` autosuspend 路径。
- `IPowerStats.aidl`（android-17.0.0_r1）：实际路径为 `hardware/interfaces/power/stats/aidl/android/hardware/power/stats/IPowerStats.aidl`，核心方法为 `getEnergyMeterInfo()` 与 `readEnergyMeter()`。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 条，包含 3 个 review_issues
- 更新章节 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无
