# 深度技术 Review · 2026-05-18 02:32（闲时抽检）

## Review 目标
- 章节：1.11 Zygote 机制与启动性能优化
- 文件：src/part1-fundamentals/ch01-architecture/11-zygote-startup.md
- 抽检模式：无 task9_state=pending 候选；抽检 finalized + pass-tech-review 章节
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L75-L76、L117 — `GraphicBufferMapper::preloadHal()` 被写成 IGBP / Ion Buffer Posting HAL，并扩展成 ashmem/dmabuf fd 映射继承；AOSP android-16.0.0_r1 实际链路是 `ZygoteInit.cpp` → `GraphicBufferMapper::preloadHal()` → `Gralloc2/3/4/5Mapper::preload()`，对应 gralloc mapper HAL / mapper 库预加载。
  2. [P0] L104 — `sphal` 被展开成 single pointer hole；Android 文档语境为 Same-Process HAL，且 `sphal_libraries.txt` 是 updatable graphics driver 的 SP-HAL 库列表。
  3. [P2] L175 — 16KB page size 段把页表项/PTE 减少写成 VMA 链表条目减少；VMA 数量由 mmap 区间决定，page size 不会直接减少 VMA 条目。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L68、L117、L376-L377 — `PreloadAppProcessHALs` 标注为 Android 13+ 不成立。跨版本抽查：android-9.0.0_r1 已有 `PreloadAppProcessHALs/nativePreloadAppProcessHALs`；android-10.0.0_r1 已有 `PreloadGraphicsDriver/nativePreloadGraphicsDriver` 与 `cacheNonBootClasspathClassLoaders`。

## 统计
- P0 事实错误：2 处（合并为 1 个 queue 条目）
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5

## 闭环动作
- 写入 queue.json（P95）：1 处，id=`task9-audit-20260518-1.11-zygote-gralloc-version-errors`
- 更新章节 frontmatter：`task9_result=needs-rework`、`pipeline_stage=task2b_pending`、`task2b_state=pending`
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（P2 保留在本抽检日志中，优先闭环 P0/P1）

## 建议修正
1. 把 `nativePreloadAppProcessHALs()` 改成 gralloc mapper HAL / mapper passthrough 库预加载；删除 IGBP、Ion Buffer Posting、ashmem/dmabuf fd 映射继承表述。
2. `sphal` 改为 Same-Process HAL，并锚定 Android linker namespace / SP-HAL 文档。
3. 版本表增加 Android 9/10 的 preload 变化：Android 9 已有 `PreloadAppProcessHALs`，Android 10 已有 `PreloadGraphicsDriver`；Android 16 再单独写 `HttpEngine.preload()` 可选。
4. 16KB page size 段把“VMA 链表条目减少”改成“PTE/page-table 规模下降”，并补充 COW 粒度边界。
