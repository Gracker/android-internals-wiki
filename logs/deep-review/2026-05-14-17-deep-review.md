# 深度技术 Review · 2026-05-14 17:20

## 选择说明
- 选择：当前有效候选不足 3 节；`1.0` 正文 <100 行跳过；`20.1` 今日 08 点已审且仍有 pending 队列，不重复摊薄；`25.4` 为 `task9_state: pending` 且 Task6 pending，按并行规则纳入本轮。
- external-review：`logs/external-review/` 活跃目录为空，本轮无可消费外部审查文件。
- 自动晋升检查：25.4 本轮存在 P1，未晋升 finalized。

## Review 目标
- 章节：25.4 WorkManager 实战与后台任务调度
- 文件：src/part5-app/ch25-power-size/04-workmanager-practice.md
- 状态：ready-for-review → task2b_pending

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：2
- 具体问题：
  1. [P2][源码准确性] L82-L84 — `GreedyScheduler` 当前实现不只处理无约束 work；非 idle、非 content-uri trigger 的 constrained work 会进入 `WorkConstraintsTracker`，约束满足时进程内 `startWork()`，约束失效时 `stopWorkWithReason()`。已写入 suggestions.md。
  2. [P2][源码准确性] L86-L106 — `requiresDeviceIdle()` 与 `setBackoffCriteria()` 不能组合，AndroidX OneTime/Periodic builder 会抛出 `IllegalArgumentException`。已写入 suggestions.md。
- 核验记录：AndroidX `androidx-main` `Schedulers.createBestAvailableBackgroundScheduler()` 创建 `SystemJobScheduler` 并启用 `SystemJobService`；`SystemJobScheduler.scheduleInternal()` 通过 `SystemJobInfoConverter.convert()` 后调用平台 `JobScheduler.schedule()`，expedited quota 失败且策略为 `RUN_AS_NON_EXPEDITED_WORK_REQUEST` 时降级重排；`SystemJobInfoConverter` 在 API 31+ 才设置 `JobInfo.Builder.setExpedited(true)`。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][原理链] L82-L84 — 缺少 `GreedyScheduler` 对普通约束任务的进程内约束跟踪分支，影响“为什么同一个 Work 同时受 JobScheduler 和进程内快速路径影响”的解释。已写入 suggestions.md。

### 维度 3：版本差异覆盖
- 评分：3.5/5
- 问题数：2
- 具体问题：
  1. [P1][版本差异] L179、L193 — UIDT 只从 Android 14/API 34 开始提供公开 `JobInfo.Builder.setUserInitiated(true)`，官方要求 `RUN_USER_INITIATED_JOBS`、network constraint、运行时 notification，并建议低版本用 WorkManager foreground-service path fallback。正文适用 Android 10-16，但未写 API 29-33 分支。已写入 queue.json（P85）。
  2. [P2][版本差异] L197-L201 — `WorkInfo.getStopReason()` / Worker `getStopReason()` 属于 WorkManager 2.9.0+；`JobParameters.getStopReason()` 在 Android 12/API 31 起公开。Android 10-11 或旧 WorkManager 版本需要替代观测字段。已写入 suggestions.md。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：1
- 具体问题：
  1. [P2][知识盲区] L86-L106 — 约束组合缺少“idle + backoff 不兼容”这类实战约束，容易把示例扩展成运行时异常。已写入 suggestions.md。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- P3 记录：L201 的三组守门指标可后续补最小字段模板：WorkManager 版本、API level、App Standby bucket、约束状态、stop reason、运行时长 P50/P95/P99、失败重试率。当前已有指标方向，不构成 P2。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][交叉引用一致性] L179、L193 与 §5.10 UIDT 表不一致。§5.10 已写 Android 14+、`RUN_USER_INITIATED_JOBS`、network constraint、notification 等条件；25.4 的选型表未同步这些边界。该问题已合并进 UIDT P1 queue。
- 核验记录：§25.2、§25.3、§5.10 对应文件存在；`src/SUMMARY.md` 中存在 25.4。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：3 处
- P3 锦上添花：1 处
- 总体技术评分：4.0/5

## 闭环动作
- 写入 queue.json（P85）：1 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：3 处
- 仅日志记录：1 处
- Frontmatter：25.4 更新为 `task9_result: needs-rework`、`task9_state: reviewed`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
