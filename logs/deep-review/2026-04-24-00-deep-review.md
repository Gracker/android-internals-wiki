# 深度技术 Review · 2026-04-24 00:43

## Review 目标
- 章节：2.1 Android 渲染架构全景
  - 文件：src/part1-fundamentals/ch02-rendering/01-rendering-overview.md
  - 状态：ready-for-review
- 章节：2.10 GPU 渲染深入
  - 文件：src/part1-fundamentals/ch02-rendering/10-gpu-rendering.md
  - 状态：finalized
- 章节：2.21 文字渲染性能
  - 文件：src/part1-fundamentals/ch02-rendering/21-text-rendering-performance.md
  - 状态：finalized

## 审查结果

### 2.1 Android 渲染架构全景
- 源码引用准确性：2/5（问题数：6）
- 原理完整性：3/5
- 版本覆盖：2/5
- 知识盲区：3/5
- 数据支撑：4/5
- 交叉引用：4/5
- P0 / P1 问题：
  1. [P0] L124-L129「Layout 过程」调用树 — `ViewGroup.dispatchDraw()` 被放进 Layout 流程，但 `dispatchDraw()` 属于 Draw 阶段；当前调用树会把布局与绘制阶段混在一起。
  2. [P0] L323-L328「BufferQueue 同步机制」Fence 伪代码 — 示例写成 `consumerReleaseFence->signal();`，但 Fence 并不是由消费者手工 signal 的对象；release/acquire fence 的完成通知来自内核 / GPU / 显示硬件。
  1. [P1] L157-L161「VSync 同步」调用树 — `Choreographer.callInputCallbacks()` / `callAnimationCallbacks()` / `callTraversalCallbacks()` 不是 android-16.0.0_r1 的实际方法名；真实实现是 `doFrame()` 内部按 callback type 调 `doCallbacks(...)`。
  2. [P1] L190-L205「SurfaceFlinger 合成 / 显示输出」调用树 — `DisplayHardware composerCallback()` / `DisplayHardware.vsync()` / `DisplayHardware.flip()` 这组类名与方法名不符合现代 SurfaceFlinger / HWC2 主链路，会把读者带回过时模型。
  3. [P1] L261-L263「三缓冲机制」Android 16 扩展段 — 正文继续把 `AsyncBufferQueue` 当成 Android 16 已落地的既定事实，但没有给出可核对的一手锚点；这会把 BLAST / SurfaceControl 事务路径和未证实的新名词混写。
  4. [P1] L489-L559「RenderNode / DisplayList」两段 `[待验证]` — HWUI 内部结构仍以“概念性伪代码”占位，`RenderNode` staging、`DisplayListData`、Skia Pipeline 的 android-16 实际锚点没有落到具体类与路径。
- 结论：存在 P0/P1，已写入 queue.json 并回推 Task 2B。

### 2.10 GPU 渲染深入
- 源码引用准确性：4/5（问题数：3）
- 原理完整性：4/5
- 版本覆盖：4/5
- 知识盲区：4/5
- 数据支撑：3/5
- 交叉引用：4/5
- P2 建议：
  1. [P2] L233-L239「CPU 开销：一个数量级的差距」示例代码 — 把 `glDrawArrays()` 与 `vkQueueSubmit()` 并排比较会把“单个 draw call”和“整批命令提交”混成一层概念。结论方向没错，但代码对照不够对称。
  2. [P2] L506-L510「效果验证」 — 案例给出了 GPU 时间、FPS 和 overdraw 改善区间，但没有补 Trace / AGI 截图、设备型号、分辨率、刷新率与测试条件。
  3. [P2] L267-L271「ANGLE 层的性能影响」 — ANGLE 的 2-5% / 5-10% / 10-20% 只是经验区间，正文已经说明不是系统性基准，但还缺最小测试上下文。
- 结论：无 P0/P1，已自动晋升 `status: finalized` / `pipeline_stage: ready-to-publish`。

### 2.21 文字渲染性能
- 源码引用准确性：4/5（问题数：3）
- 原理完整性：4/5
- 版本覆盖：3/5
- 知识盲区：4/5
- 数据支撑：3/5
- 交叉引用：4/5
- P2 建议：
  1. [P2] L351-L362「版本演进」 — 版本表已经把 PrecomputedText / emoji2 / 16KB page size 这些节点补齐，但还缺 Android 12 的 `FontManager` / updatable fonts（`com.android.fonts` 模块）这一跳，系统字体与 emoji 更新链路仍不完整。
  2. [P2] L223-L314「文字渲染优化实践」 — 优化节已经覆盖 PrecomputedText / BoringLayout / hyphenation，但仍未提 variable font / font variation settings 对 relayout 与 cache key 的影响。
  3. [P2] L322-L349「在 Perfetto 中识别文字渲染瓶颈」 — Perfetto 观察面已经说清了默认 trace 与 atrace 类别的边界，但正文仍然没有放一条最小可复用的实测样例（例如 TextView.onMeasure 的真实耗时区间与采集条件）。
- 结论：无 P0/P1，已自动晋升 `status: finalized` / `pipeline_stage: ready-to-publish`。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：4 处
- P2 建议改进：6 处
- P3 锦上添花：0 处
- 总体技术评分：3.7/5

## 闭环动作
- 写入 queue.json（P95）：1 处（2.1）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：6 处
- 自动晋升 finalized：2 处（2.10、2.21）
