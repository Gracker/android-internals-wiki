# 深度技术 Review · 2026-05-08 12:39

## Review 目标
- 候选说明：当前严格候选池只有 2 个 `task9_state: pending` 章节；1.0 为 README 且正文不足 100 行，按 Task9 规则跳过。本轮审计 1 个内容充分章节。
- 章节：4.3 ART 虚拟机内存管理
- 文件：src/part1-fundamentals/ch04-memory/03-art-memory.md
- 状态：ready-for-review → task2b_pending
- external-review：`logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。

## 审查结果

### 4.3 ART 虚拟机内存管理

#### 维度 1：源码引用准确性
- 评分：3.5/5
- 问题数：P1 1；P2 1（既有 suggestions）
- 具体问题：
  1. [P1][源码错误/版本差异] L297-L303 Android 17 DeliQueue — 正文把“Android 17 的 DeliQueue”写成已在 `android.os.MessageQueue` 层面实现无锁投递。AOSP `android-16.0.0_r1` 与 `main` 公开路径为 `LegacyMessageQueue` / `ConcurrentMessageQueue` / `CombinedMessageQueue`，Concurrent 路径使用 `ConcurrentSkipListSet` 与 CAS stack；未见 `DeliQueue` 命名。Combined 还通过 `Flags.forceConcurrentMessageQueue`、core UID、SystemUI 进程等条件 gated，不是普通应用通用行为。建议：改成公开 AOSP Concurrent/CombinedMessageQueue 路径，并把 DeliQueue/Android 17 命名留作待正式 tag/release note 验证。
  2. [P2][已存在] L140-L143 Large Object Space 实现选择 — FreeListSpace / MapSpace 的默认选择由 `Heap::kDefaultLargeObjectSpaceType` 与 `USE_ART_LOW_4G_ALLOCATOR` 控制，不应直接写成 arm64 / 非 arm64。该 P2 已存在于 `intake/suggestions.md`，本轮不重复追加。

#### 维度 2：原理链完整性
- 评分：3.8/5
- 问题数：P1 1
- 具体问题：
  1. [P1][原理断裂] L297-L305 DeliQueue 与 FinalizerDaemon — 段落先说明 ReferenceQueue/FinalizerDaemon 未证实，又把 MessageQueue 无锁投递作为 Android 17 事实写入 ART GC 性能章节；这会把 Handler/Looper 队列优化与 ART ReferenceQueue 锁竞争混成一条未闭合因果链。建议：若保留，应迁移/交叉引用到 1.13 MessageQueue 专章；本节只保留“ReferenceQueue 接入未证实”的边界。

#### 维度 3：版本差异覆盖
- 评分：3.5/5
- 问题数：P1 1
- 具体问题：
  1. [P1] 同上：AOSP android-16.0.0_r1 已有 Combined/Concurrent MessageQueue 目录；正文却写 Android 17 DeliQueue 且没有正式 tag / API 级别 / flag 边界。

#### 维度 4：知识盲区
- 评分：4.0/5
- 盲区数：0 个新增 P1；P3 1
- 说明：ReferenceQueue 是否会接入并发队列仍是待验证边界，当前已有显式注意块；本轮不新增 research-gaps。

#### 维度 5：数据与案例支撑
- 评分：3.0/5
- 问题数：P1 1；P2 2（既有 suggestions）
- 具体问题：
  1. [P1][Trace 配置错误/数据支撑] L420-L459 Perfetto ART GC track 与 SQL — 正文写 `art_gc` counter track，并用 `track.name LIKE '%art_gc%' AND slice.name LIKE '%GC%'` 统计 GC。AOSP `GarbageCollector::Run()` 通过 `ScopedTrace("%s %s GC")` 产出 GC slice；`TraceGCMetric()` 上报的是 `freed_normal_object_bytes`、`freed_large_object_bytes`、`freed_bytes`，`Heap::TraceHeapSize()` 上报 `Heap size (KB)`。未见通用 `art_gc` track，SQL 可能返回空结果或漏掉真实 GC slice。建议：按真实 trace 样本重写 SQL，slice 与 counter 分开查，并限定 process/thread。
  2. [P2][已存在] L327 ART 8 性能数字、L466-L470 正常/异常 GC 阈值仍缺设备/版本/trace 口径；既有 suggestions 已记录，本轮不重复追加。

#### 维度 6：交叉引用一致性
- 评分：4.0/5
- 问题数：P2 1
- 说明：DeliQueue/MessageQueue 机制更适合引用 1.13 或 Android 17 行为章节；但本轮 P1 会要求重写该段，暂不单独写 P2。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：2 处
- P2 建议改进：3 处（均为已存在/随 P1 回炉处理，本轮不重复追加 suggestions）
- P3 锦上添花：1 处
- 总体技术评分：3.4/5

## 闭环动作
- 写入 queue.json（P85）：1 条（4.3，含 2 个 P1）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条（既有 P2 不重复）
- 自动晋升 finalized：0 个
- 跳过章节：1.0 README（正文不足 100 行）
