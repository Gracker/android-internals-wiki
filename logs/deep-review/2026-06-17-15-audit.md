# Task9 闲时抽检 · 2026-06-17 15:29

## 抽检目标
- 章节：20.9 稳定性治理案例集
- 文件：src/part5-app/ch20-stability/09-stability-case-studies.md
- 状态：finalized / task9_result=pass-tech-review
- 选择原因：当前无 `task9_state: pending` 候选；Task2B backlog 为 0；本节缺少 `last_task9_audit`，且 `last_task9_at=2026-05-28T03:32:12+08:00`。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 边界：仅核对 Android 17/API 37 及以下；本轮未采用 Android 18/API 38+ 资料。

## 复核证据
- AOSP tag：`android-16.0.0_r1` 与 `android-17.0.0_r1` 均存在于 `platform/art`、`platform/frameworks/base`、`platform/system/core`、`platform/bionic`。
- ART 线程创建：`art/runtime/thread.cc` 中 `Thread::CreateNativeThread()` 仍通过 `pthread_attr_setstacksize()` 后调用 `pthread_create()`，失败时抛出 `OutOfMemoryError`，Android 16/17 锚点一致。
- ART Java 堆分配：`Heap::AllocObjectWithAllocator()` 位于 `art/runtime/gc/heap-inl.h`，`AllocateInternalWithGc()` 位于 `art/runtime/gc/heap.cc`，Android 16/17 锚点一致。
- Native crash handler：`system/core/debuggerd/handler/debuggerd_handler.cpp` 注册 `debuggerd_signal_handler`，使用 `SA_RESTART | SA_SIGINFO | SA_ONSTACK`；`bionic/linker/linker_debuggerd_android.cpp` 调用 `debuggerd_init()`，Android 16/17 锚点一致。
- ContentProvider 初始化：`ActivityThread.handleBindApplication()` 先 `installContentProviders(app, data.providers)`，随后 `mInstrumentation.callApplicationOnCreate(app)`；`ComputerEngine.sProviderInitOrderSorter` 对 `ProviderInfo.initOrder` 做降序排序，Android 16/17 锚点一致。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1 个 P2
- 具体问题：
  1. [P2] source anchor 完整性 — 正文使用 `Heap::AllocObjectWithAllocator()` → `AllocateInternalWithGc()` 作为 Java 堆 OOM 特征，但 frontmatter sources 只列 `art/runtime/gc/heap.cc`。前者实际定义在 `art/runtime/gc/heap-inl.h`。正文技术链路正确，属于 source anchor 漏项，不影响读者机制理解。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 说明：本节 frontmatter 适用范围仍为 Android 10/API 29 - Android 16/API 36；本轮补查 Android 17/API 37 tag，关键锚点未发现会推翻正文结论的变化。正文未引用 Android 18/API 38+ 内容。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：1 处（仅日志记录，闲时抽检不扩大 suggestions backlog）
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter：`last_task9_audit: 2026-06-17`
- 自动晋升 finalized：0 个（闲时抽检对象已 finalized）
