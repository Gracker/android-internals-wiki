# 深度技术 Review · 2026-05-05 03:31

## 本轮说明
- `logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。
- 按 frontmatter 实际候选池复核，`task9_state: pending` 的有效章节只有 2 节：9.1、16.4；因此本轮降为 2 节深审。
- 已复核 AOSP / Android 官方锚点：`ActivityManagerService.addErrorToDropBox()` / `processClass()`、Android Vitals ANR docs、`android16-6.12` sched_ext 源码与文档、F2FS checkpoint 触发链、GKI arm64 defconfig。

## Review 目标
- 章节：9.1 ANR 设计思想
- 文件：src/part2-performance/ch09-anr/01-anr-design.md
- 状态：ready-for-review → task2b_pending
- 章节：16.4 Android 17 + Kernel 6.12 系统级性能优化
- 文件：src/part4-system/ch16-aosp/04-android17-kernel612-performance.md
- 状态：ready-for-review → task2b_pending

## 审查结果：9.1 ANR 设计思想
### 维度 1：源码引用准确性
- 评分：3.5/5
- 问题数：1
  1. [P1] L373-L377 Dropbox tag — 正文写 ANR 信息写入 `system_app_anr`。AOSP android-14.0.0_r1 中 `ProcessErrorStateRecord.appNotResponding()` 传入 eventType `anr`，`ActivityManagerService.addErrorToDropBox()` 使用 `processClass(process) + "_" + eventType` 生成 tag；普通应用是 `data_app_anr`，系统应用是 `system_app_anr`，system_server 是 `system_server_anr`。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：前轮 ContentProvider 超时常量、`ApplicationExitInfo.getTraceInputStream()` API 30+、完成通知主线程路径已修正。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

### 维度 5：数据与案例支撑
- 评分：3.5/5
- 问题数：1
  1. [P1] L422-L470 Android Vitals 统计口径 — 正文写“ANR 率按会话计算”。官方口径是 daily active users；user-perceived ANR rate = 单日单设备活跃用户中至少发生一次 user-perceived ANR 的比例，且目前只把 `Input dispatching timed out` 算作 user-perceived。建议改成 DAU 口径，并补 per-device 8% 阈值。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

### 本节统计
- P0 事实错误：0
- P1 重要缺失：2
- P2 建议改进：0
- P3 锦上添花：0
- 总体技术评分：3.8/5

## 审查结果：16.4 Android 17 + Kernel 6.12 系统级性能优化
### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：4
  1. [P0] L74/L76/L350 `kernel/sched/ext/` — android16-6.12 `kernel/sched/` 下实际是 `ext.c` 与 `ext.h`，不存在 `ext/` 目录。
  2. [P0] L104-L111 `SCX_TASK_IN_CUSTODY` — 目标分支 `include/linux/sched/ext.h` / `kernel/sched/ext.c` / `kernel/sched/ext.h` 均无该符号。
  3. [P0] L119-L126 `SCX_DSQ_BYPASS` — 目标分支 `enum scx_dsq_id_flags` 无该内置 DSQ。
  4. [P0] L162-L166 `/sys/kernel/sched_ext/root/events` / `SCX_EV_REFILL_SLICE_DFL` — 目标分支文档只列 state、root/ops、enable_seq；tracepoint 只有 `sched_ext_dump`。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：1
  1. [P1] L176-L184 F2FS Checkpoint Merge — 正文把每次 `fsync()/sync()` 与完整 checkpoint 绑定。`f2fs_do_sync_file()` 只有 `need_do_checkpoint(inode)` 命中时才调用 `f2fs_sync_fs()`；`checkpoint_merge` 合并的是 `f2fs_issue_checkpoint()` 的 `CP_SYNC` 请求，不等于所有 SQLite WAL fsync 都完整 checkpoint。

### 维度 3：版本差异覆盖
- 评分：2.5/5
- 问题数：1
- 同 P0 sched_ext：当前仍混入目标分支不存在的符号 / sysfs 项，应拆成 android16-6.12、后续 mainline、厂商分支三层。

### 维度 4：知识盲区
- 评分：3.5/5
- 盲区数：0

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
  1. [P2][既有 suggestions/queue 已记录] L302-L312 MGLRU 数据 — 本轮确认 `arch/arm64/configs/gki_defconfig` 有 `CONFIG_LRU_GEN=y` / `CONFIG_LRU_GEN_ENABLED=y`，但 `kswapd -40%`、`LMK -85%`、`渲染延迟 -18%` 仍缺来源与测试条件。

### 维度 6：交叉引用一致性
- 评分：3.5/5
- 问题数：0

### 本节统计
- P0 事实错误：4
- P1 重要缺失：2
- P2 建议改进：1
- P3 锦上添花：0
- 总体技术评分：2.8/5

## 统计
- P0 事实错误：4
- P1 重要缺失：4
- P2 建议改进：1
- P3 锦上添花：0
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json：9.1 新增 1 条 pending（priority 85，2 个 P1）；16.4 合并到既有 pending 并提升 priority 95。
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条（16.4 MGLRU P2 已有 suggestions / queue，未重复追加）
- frontmatter：9.1、16.4 均标记 `task9_state: reviewed`、`task9_result: needs-rework`、`pipeline_stage: task2b_pending`
- 自动晋升 finalized：无

## 下一 Review 候选
- 当前 frontmatter 候选池已无新的 `task9_state: pending` 有效章节；等待 Task2B/Task6 推进后再审。
