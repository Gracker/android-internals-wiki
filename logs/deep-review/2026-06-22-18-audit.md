# Task 9 闲时抽检 · 2026-06-22 18:37

## 抽检目标
- 章节：18.4 Android View 混合渲染链路
- 文件：src/part2-performance/ch18-rendering-pipelines/04-android-view-mixed.md
- 状态：finalized
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）
- Android 范围：最高 Android 17 / API 37；未采用 Android 18 / API 38+ 材料

## 维度 1：源码引用准确性
- 结果：通过，无 P0/P1
- 复核证据：
  - AOSP android-17.0.0_r1 `frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp`：`commit()` 中遍历 layer，`hasReadyFrame()` 后调用 `latchBufferImpl()`；`shouldLatchUnsignaled()` 限定单 layer / simple buffer update 等条件。
  - AOSP android-17.0.0_r1 `frameworks/native/libs/gui/Surface.cpp`：存在 `Surface::setBuffersTimestamp(int64_t)`，正文对 native buffer present timestamp 的描述成立。
  - AOSP android-17.0.0_r1 `frameworks/native/libs/gui/include/gui/SurfaceComposerClient.h`：`Transaction::merge(Transaction&&)` 与 `Transaction::setDesiredPresentTime(nsecs_t)` 路径存在。
  - AOSP android-17.0.0_r1 `frameworks/base/core/java/android/view/SurfaceView.java`：类注释和实现包含 SurfaceView hole punching / transparent region / `canvas.punchHole()` 语义，正文没有把机制误写成单一 `clipOut()` 调用。
  - AOSP android-14.0.0_r1 `frameworks/base/core/java/android/window/SurfaceSyncGroup.java`：`add(SurfaceView, Consumer<SurfaceViewFrameCallback>)` 与 `SurfaceViewFrameCallback` 标为 `@hide`，公开入口覆盖 `AttachedSurfaceControl` 与 `SurfaceControlViewHost.SurfacePackage`，正文边界说明成立。

## 维度 3：版本差异覆盖
- 结果：通过，无 P0/P1
- 复核证据：
  - `SurfaceControl.Transaction#addTransactionCommittedListener`：android-12.0.0_r1 / android-12.1.0_r1 未检出公开方法；android-13.0.0_r1 起检出，正文标注 Android 13 / API 33+ 成立。
  - `SurfaceSyncGroup`：android-12.0.0_r1 / android-12.1.0_r1 / android-13.0.0_r1 路径 404；android-14.0.0_r1 起存在，正文标注 Android 14 / API 34+ 成立。
  - Android 17 / API 37 tag 可取到并用于复核；本轮未使用 Android 18 / API 38+ 资料。

## 记录项
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议：0
- P3 备注：1
  - `dumpsys SurfaceFlinger` 字段名在不同版本/视图可能变化；正文中的 `activeBuffer` / `latched buffer` 可作为排查方向，但后续如有专门工具章节可补充 proto/layer trace 的更稳定字段。此次不写入 suggestions。

## 闭环动作
- queue.json：未写入
- research-gaps.md：未写入
- suggestions.md：未写入
- frontmatter：已更新 `last_task9_audit: 2026-06-22`
