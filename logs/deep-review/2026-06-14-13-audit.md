# 深度技术 Review 闲时抽检 · 2026-06-14 13:20

## Review 目标
- 章节：2.15 DMA-BUF、Gralloc 与跨进程图形内存共享
- 文件：src/part1-fundamentals/ch02-rendering/15-dmabuf-gralloc.md
- 状态：finalized
- 模式：闲时抽检，仅复核维度 1（源码引用）与维度 3（版本差异）

## 版本边界
- 本轮未使用 Android 18 / API 38+ 资料。
- 章节正文适用范围保持 Android 12 (API 31) - Android 16 (API 36)。
- AOSP 源码锚点使用 android-16.0.0_r1；未把 main/master 资料作为正文结论。

## 一手资料复核
- AOSP android-16.0.0_r1 `frameworks/native/libs/ui/GraphicBuffer.cpp`：确认 `GraphicBuffer::flatten()` / `unflatten()`、`mTransportNumFds` / `mTransportNumInts`、`free_handle()` 路径存在。
- AOSP android-16.0.0_r1 `hardware/interfaces/graphics/allocator/aidl/.../IAllocator.aidl`：确认 `allocate2()`、`getIMapperLibrarySuffix()` 与 `mapper@4` 兼容说明。
- AOSP android-16.0.0_r1 `hardware/interfaces/graphics/allocator/aidl/.../BufferDescriptorInfo.aidl`：确认 `reservedSize`、`additionalOptions` 与 compression level 示例。
- AOSP android-16.0.0_r1 `hardware/interfaces/graphics/mapper/stable-c/.../IMapper.h`：确认 C-style `AIMapper` API starts at 5。
- AOSP android-16.0.0_r1 `frameworks/native/libs/gui/BufferQueueProducer.cpp`、`BLASTBufferQueue.cpp`：确认 `BUFFER_NEEDS_REALLOCATION`、`requestBuffer()` 与 BLAST `setBuffer()` 路径。
- Android 官方文档 `source.android.com/docs/core/graphics/reduce-consumption`：确认 Android 14 per-layer buffer cache purge 与 Composer HAL 3.2 说明。
- Perfetto 官方文档 `perfetto.dev/docs/analysis/stdlib-docs` 与 `perfetto.dev/docs/getting-started/memory-profiling`：确认 `android_dmabuf_allocs` 与 `dmabuf_heap/dma_heap_stat` 描述。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：2
- 具体问题：
  1. [P1][AUTO-FIX] “Exporter” 列表项将内核 DMA-BUF exporter 的输出直接写成 `buffer_handle_t`，容易混淆内核 `dma_buf_export()` 产物和 Android Gralloc/native_handle 包装层。已改为“内核导出 DMA-BUF fd；Android 图形栈再由 Gralloc 包装进 `buffer_handle_t` / native handle”。
  2. [P2][AUTO-FIX] Android 14 buffer cache purge 的验证路径写成 `source.android.com/docs/core/graphics/bufferqueue — Android 14 Graphics Changes`。官方对应页是 `source.android.com/docs/core/graphics/reduce-consumption`。已修正正文验证注记，并补入 frontmatter sources。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0
- 复核结论：Android 12 ION → DMA-BUF Heaps、Android 13 AutoSingleLayer、Android 14 buffer cache purge、Android 15 16KB page size、Android 16 allocator AIDL / stable-C mapper 边界均能由 AOSP tag 或官方文档支撑。未发现 Android 18 / API 38+ 越界内容。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：1 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- AUTO-FIX：2 处
- frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_audit: 2026-06-14`
