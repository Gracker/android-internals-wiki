# 深度技术 Review · 2026-05-15 05:50

## 本轮说明
- 规范：已先读取 `/Users/gracker/.agents/skills/technical-writing/SKILL.md` 与 `openclaw-tasks/task9-deep-tech-review.md`。
- 选择：本轮 Task9 有效候选仅 20.7 一节；其他 `task9_state: pending` 章节为 draft 或不在目标池内，因此低于默认 3 节。
- external-review：`logs/external-review/` 根目录无活跃文件，本轮未消费 external-review。
- 自动晋升检查：20.7 本轮存在 P0/P1，且 Task6 仍 pending，未晋升 finalized。

## Review 目标
- 章节：20.7 异常处理架构设计
- 文件：src/part5-app/ch20-stability/07-exception-architecture.md
- 状态：ready-for-review → task2b_pending

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L216-L224 WebView renderer 进程模型 — 章节把 WebView 独立进程纳入“每个进程都有 Application、默认 handler”的 App 自有进程模型。AOSP `WebViewClient.onRenderProcessGone()` 的契约是 renderer 退出后通知宿主 App；宿主只能清理受影响 WebView，不能在 renderer 内安装自己的 Crash handler。建议：把 WebView renderer 从普通多进程表中拆出，单独写 `onRenderProcessGone()`、WebView 销毁/重建、页面兜底与上报字段。

### 维度 2：原理链完整性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L120-L148 SafeMode 判定模型 — 只按 `crashStore.recentCrashes()` 和 stack fingerprint 判定，缺少启动状态机。handler 未注册前退出、native crash、ANR、初始化失败、低内存杀进程都可能没有本地 crash envelope。建议补 `launch_started` → 首帧/首页 ready → `launch_succeeded` 的 marker，结合 `ApplicationExitInfo` reason、bootCount/elapsedRealtime、版本号和进程名判断连续启动失败。
  2. [P1] L227 崩溃样本持久化协议 — “临时文件 + rename”只保证目录项切换原子，不保证内容持久化。建议补完整协议：tmp 写入、flush + `fsync(fd)`、同卷 `rename` 到 completed、`fsync(parent dir)`、下次只扫描 completed 并校验 schema/hash/长度。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L102-L104 `ApplicationExitInfo` trace 版本边界 — API 30(Android 11) 提供历史退出原因；源码注释显示 native tombstone trace 通过 `getTraceInputStream()` 返回是 API 31(Android 12)+，且可能因全局环形缓冲覆盖返回 null。建议拆分 API 30 exit reason 与 API 31+ native tombstone trace。

### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P1] SafeMode 崩溃循环判定 — 缺少 launch marker、`ApplicationExitInfo`、WebView renderer 退出、文件持久化协议的一手实现闭环。已写入 `intake/research-gaps.md`。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] L160 Android Vitals 指标口径 — SafeMode 对 user-perceived crash rate 的作用需要加边界。该指标按 DAU 中至少一次前台 crash 的用户计数；SafeMode 主要减少重复崩溃、启动循环和 crash-free sessions 损失，不能抹掉当天已经发生的首次 crash 用户。已写入 `intake/suggestions.md`。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：20.2、20.3、26.2 文件存在；20.7 与 26.2 的“崩溃当下最小记录、下次启动补上下文”方向一致，但 20.7 对 `ApplicationExitInfo` API 30/31 边界和 WebView renderer 模型需要回炉。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：3 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json（P95）：1 条
- 写入 queue.json（P85）：1 条，合并 3 个 P1 issue
- 写入 research-gaps.md：1 条
- 写入 suggestions.md：1 条
- 仅日志记录：0 条

## Top 问题
1. [P0] L216-L224 — WebView renderer 进程不能按 App 自有进程安装 Crash handler，应改为 `onRenderProcessGone()` 恢复路径。
2. [P1] L102-L104 — `ApplicationExitInfo` native tombstone trace 是 API 31+，不能写成 Android 11+ 通用 trace。
3. [P1] L120-L148 — SafeMode 缺 launch marker + exit reason 补偿链路，会漏掉无本地 crash envelope 的启动失败。
