🔬 **Task 9 · 深度技术Review** — 对章节做技术审计（源码准确性、原理链、版本差异、数据支撑），默认写问题单；高置信局部问题可直接修复并回到 Task6 复审。

📋 本轮审计：src/part5-app/ch25-power-size/01-power-diagnosis.md
✅ 自动晋升：无

# 深度技术 Review · 2026-06-22 05:28

## Review 目标
- 章节：25.1 功耗诊断与分析方法
- 文件：src/part5-app/ch25-power-size/01-power-diagnosis.md
- 状态：ready-for-review
- 模式：Task2B 修复后技术复审
- Android 边界：最高 Android 17 / API 37；本轮仅使用 android-17.0.0_r1、android-16.0.0_r1、android-15.0.0_r1 tagged AOSP 与既有官方文档锚点

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：2（均已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] PowerStatsStore 存储描述 — 正文写成 `/data/system/powerstats/log.powerstats.meter.0` + Proto；android-17.0.0_r1 `PowerStatsStore.java` L59-L60 定义目录 `power-stats`、后缀 `.pss`，L162-L166 通过 `PowerStatsSpan.writeXml(out, Xml.newBinarySerializer())` 写入二进制 XML。已修正为 `/data/system/power-stats/` + `.pss` + binary XML。
  2. [P1][AUTO-FIX] 源码行号锚点 — Android 16 `BatteryStatsService` 调用位置应为 L629-L631；Android 17 `WakeupReasonThread` trace 写入点应为 L2928；`getWakeLockStats()` 入口应为 L3534-L3540。已修正文内行号。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：Task2B 已把 Android 15/16/17 功耗归因链拆开；本轮复核确认 Android 16/17 的 `MultiStatePowerAttributor` 委托 `PowerStatsExporter.exportAggregatedPowerStats()`，未再把 `getStateResidencyAsync()` 写成主链。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（已 AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] 版本锚点边界 — 正文仍保留 `frameworks/base master @ 2026-06-16` 作为锚点版本，容易越过 Android 17 / API 37 边界。已改为 `android-17.0.0_r1` 主锚点，`android-16.0.0_r1` / `android-15.0.0_r1` 仅用于版本差异。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：章节已覆盖 Battery Historian / Power Profiler / batterystats / CPU、网络、GNSS、WakeLock 归因；本轮无新增 P1 盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：采集步骤、对比实验条件和异常定位表足够支撑本节任务；未新增数据类问题单。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：§11.1、§11.2、§14.11、§25.2、§25.3、§25.5、§26.3 引用方向与章节职责一致；未发现需要写入 queue 的冲突。

## 一手证据
- `android-15.0.0_r1`：`BatteryStatsService.java` L619-L631 配置 streamlined battery stats 三层 flag；L707-L710 调用 `mPowerStatsScheduler.start(Flags.streamlinedBatteryStats())`；`processor/MultiStatePowerAttributor.java` 不存在。
- `android-16.0.0_r1`：`BatteryStatsService.java` L629-L631 调用 `mPowerStatsScheduler.start(Flags.streamlinedBatteryStats())`；`PowerStatsScheduler.java` L92-L97 为 `start(boolean enablePeriodicPowerStatsCollection)`；`MultiStatePowerAttributor.java` L240-L243 委托 `PowerStatsExporter.exportAggregatedPowerStats()`。
- `android-17.0.0_r1`：`BatteryStatsService.java` L573-L576 调用 `mPowerStatsScheduler.start()`，无 `streamlinedBatteryStats` 命中；`PowerStatsScheduler.java` L91-L94 为无参 `start()`；`BatteryUsageStatsProvider.java` L286-L287 调用 `mPowerAttributor.estimatePowerConsumption()`；`PowerStatsStore.java` L59-L60 / L162-L166 证明存储路径和格式。

## 统计
- P0 事实错误：1 处（已 auto-fix）
- P1 重要缺失：2 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：3 组
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`
- 自动晋升 finalized：无（本轮有 auto-fix，需 Task6 复审）
