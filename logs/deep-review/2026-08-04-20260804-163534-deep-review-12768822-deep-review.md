# AIW Deep Review Evidence · 2026-08-04

- lane: deep-review
- run_id: 20260804-163534-deep-review-12768822
- target: `src/part1-fundamentals/ch05-cpu-power/5.19-ondevice-ai-adpf-intelligent-scheduling.md`
- mode: polish_candidate_ready
- baseline: AOSP `android-17.0.0_r1` / Android common kernel `android17-6.18-2026-06_r6`

## 审计范围

本轮只处理预运行 JSON 指定的既有章节，未新增 `src/**/*.md`，未修改 `src/SUMMARY.md`。材料列表为空，但章节已有较完整源码索引和正文，因此执行源码/版本边界/来源支撑/可读性复核，只做小范围安全补丁。

## 源码核对

- `frameworks/base/core/java/android/os/PerformanceHintManager.java`: 复核 `reportActualWorkDuration()`、`setPreferPowerEfficiency()`、`setThreads()`、hidden `sendHint()` 边界。
- `frameworks/base/core/java/android/os/WorkDuration.java`: 复核 CPU/GPU/total/start timestamp 字段口径。
- `frameworks/native/include/android/performance_hint.h`: 复核 API 36 workload increase/reset/spike 与 setThreads/setPreferPowerEfficiency NDK 面。
- `frameworks/base/core/java/android/content/pm/PackageManager.java`: 复核 `FEATURE_NEURAL_PROCESSING_UNIT = android.hardware.npu`。
- `hardware/interfaces/npu/aidl/android/hardware/npu/SchedulingConfig.aidl`: 复核 UID priority、direct access、归因字段。

## Findings

### P0

- 无。

### P1

- 无。

### P2

1. `[fixed]` `FEATURE_NEURAL_PROCESSING_UNIT` 段落只写“能力判断和安装筛选”，没有显式区分 Android 17 直接访问 NPU 的 manifest feature 声明边界、运行时能力判断和性能保证。已补明：面向 Android 17 且需要直接访问 NPU 的应用要把它作为 manifest feature 声明；运行时仍只能用于能力判断，且不承诺算子覆盖、内存容量、执行延迟、并发数量或 LiteRT delegate 可用。

## 实际补丁

- 更新 frontmatter：`last_verified=2026-08-04`、`last_deep_review_at=2026-08-04`、`last_deep_review_run_id=20260804-163534-deep-review-12768822`。
- 正文收窄 `FEATURE_NEURAL_PROCESSING_UNIT` 语义。
- 增加本轮 deep-review HTML 注释记录。
- 维护 issue pool：`AIW-20260804-5.19-NPU-FEATURE-MANIFEST` 已 add 并 close 为 `fixed`。

## 风险与回流

- `confidence` 保持 `medium`：本轮没有新 materials，不提升置信度。
- `status` 保持 `ready-for-review`：章节正文已有充分源码索引，未发现需要回流重写的 P0/P1。
- 未引入 Android 18/API 38+ 主线结论。

## Validation

待最终校验命令记录在正式报告中。
