# Task 9 闲时抽检 · 2026-06-30 19:29

## 抽检目标
- 章节：22.9 渲染优化排查框架与案例模板
- 文件：src/part5-app/ch22-rendering-practice/09-rendering-case-studies.md
- 状态：finalized / task9_result=pass-tech-review（抽检前后不变）
- 触发：Preflight `CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检；`last_task9_at=2026-05-14T06:32:15+08:00` 超过 21 天，`last_task9_audit=2026-06-08` 超过 21 天，且历史 Task9 留过 FrameTimingMetric P2 指标口径建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮不做完整 6 维度审查。

## 复核资料
- Android Developers Slow rendering：确认 UI Toolkit 慢帧 / frozen frame 口径仍是 `16ms`、`700ms` 分界；FrameTimeline / Perfetto 可用于追踪 slow/frozen frames。
- Android Developers Macrobenchmark metrics：确认 `FrameTimingMetric` 输出 `frameDurationCpuMs` 与 `frameOverrunMs`；`frameOverrunMs` 仅 Android 12 / API 31+；分位数为 P50/P90/P95/P99。
- AndroidX `JankStats` / `PerformanceMetricsState` API reference：确认 `JankStats` 按帧报告耗时、jank 判定和 UI state；`PerformanceMetricsState.getHolderForHierarchy()`、`putState()`、`removeState()` 可用于状态标注。
- Compose performance best practices / codelab：确认 `remember`、Lazy item stable `key`、`contentType`、`LazyRow` / `SubcomposeLayout` 额外成本、composition tracing 等口径仍成立。
- Android 版本边界：未发现 Android 18 / API 38+ 内容；正文主线仍限定 Android 10 - Android 17。

## 抽检结论

### 维度 1：源码引用准确性
- 评分：4.4/5
- P0/P1：0
- 结论：通过。
- 复核点：
  1. L87 对 frame budget、frozen frame、JankStats / Macrobenchmark / Perfetto 的分工描述与官方文档一致；没有把游戏或非 UI Toolkit 渲染纳入 Android Vitals 结论。
  2. L93-L148 对 `JankStats` 状态标签、`FrameTimingMetric`、`TraceSectionMetric("RV OnBindView")`、`Trace.beginSection()` 的用法与 Android Developers / AndroidX API reference 一致。
  3. L152-L184 对 Compose `remember`、stable `key`、`contentType`、Lazy 容器 / `SubcomposeLayout` 成本和 composition tracing 的描述与官方资料一致。

### 维度 3：版本差异覆盖
- 评分：4.0/5
- P0/P1：0
- 结论：通过。
- 复核点：
  1. Frontmatter 已限定 `Android 10 (API 29) - Android 17 (API 37)`，正文未引用 Android 18 / API 38+。
  2. 正文当前使用 `frameDurationCpuMs` 作为验收指标，没有把 `frameOverrunMs` 写成 Android 10/11 可用能力。
  3. 既有 P2 仍成立：后续若补 `frameOverrunMs`，需注明 Android 12 / API 31+，并把 deadline miss 与 Android Vitals slow/frozen frame 口径分开。

## 闭环动作
- P0/P1：0，未写入 `metadata/queue.json`。
- research-gaps.md：0。
- suggestions.md：0（沿用既有 P2 观察，不重复写入）。
- 章节 frontmatter：更新 `last_task9_audit`、`last_task9_audit_at`、`last_task9_audit_log`、`last_task9_audit_result`。

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：1（既有观察，仅日志记录）
- P3 锦上添花：0
- 总体技术评分：4.2/5
