# 闲时抽检 · Task9 深度技术 Review · 2026-06-23 07:26

## Review 目标
- 章节：20.7 异常处理架构设计
- 文件：src/part5-app/ch20-stability/07-exception-architecture.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）
- Android 范围：Android 17 / API 37 及以下；本轮源码锚点使用 android-17.0.0_r1。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：1
- 具体问题：
  1. [P1 · AUTO-FIX] SafeMode 判定示意代码 — 后文源码核验已经指出 `REASON_INITIALIZATION_FAILURE` 应纳入启动失败白名单，但前文 `lastExitIsCrash` 代码只包含 `REASON_CRASH` / `REASON_CRASH_NATIVE` / `REASON_ANR`。android-17.0.0_r1 `frameworks/base/core/java/android/app/ApplicationExitInfo.java:122` 仍定义 `REASON_INITIALIZATION_FAILURE = 7`，本轮已直接补入示意代码。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0
- 具体核验：
  1. android-17.0.0_r1 `ApplicationExitInfo.getTraceInputStream()` 仍存在，签名为 `InputStream getTraceInputStream() throws IOException`；`mAppTraceRetriever` 与 `mNativeTombstoneRetriever` 为空时返回 `null` 的边界仍成立。
  2. android-17.0.0_r1 `AppExitInfoTracker` 仍使用 `APP_EXIT_INFO_PERSIST_INTERVAL = TimeUnit.MINUTES.toMillis(30)`，`core/res/res/values/config.xml` 中 `config_app_exit_info_history_list_size` 仍为 16。
  3. android-17.0.0_r1 `AtomicFile.finishWrite()` 仍只对文件流执行 `FileUtils.sync(str)` 后 rename，未发现父目录 fsync 实现。
  4. android-17.0.0_r1 `DropBoxManagerService` 仍使用 `drop{threadId}.tmp` 临时文件、`renameTo(file)` 完成写入；服务内未调用 `FileUtils.sync`，启动扫描仍清理 `.tmp`。
  5. android-17.0.0_r1 `ActivityManagerService` 仍保留 system_server crash 时 `worker.join(2000)` 的 DropBox 等待逻辑。

## AUTO-FIX
- 修改文件：src/part5-app/ch20-stability/07-exception-architecture.md
- 修改内容：在 `SafeModeController.evaluate()` 的 `lastExitIsCrash` 白名单中加入 `ApplicationExitInfo.REASON_INITIALIZATION_FAILURE`。
- 证据来源：android-17.0.0_r1 `frameworks/base/core/java/android/app/ApplicationExitInfo.java:122`。
- Frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_audit=2026-06-23`、`last_task9_autofix_at=2026-06-23`。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 直接修正文：1 处
- 下一步：Task6 复审该章节的示意代码改动。
