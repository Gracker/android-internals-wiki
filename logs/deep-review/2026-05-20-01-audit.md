# 深度技术 Review 闲时抽检 · 2026-05-20 01:26

## Review 目标
- 模式：闲时抽检（维度 1 源码/资料锚点 + 维度 3 版本差异）
- 章节：12.2 网络性能优化
- 文件：src/part2-performance/ch12-apk-network/02-network-performance.md
- 状态：finalized / pass-tech-review（抽检前）

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] §网络线程的能效分档，首句（原 L239） — 正文写 Android 15 ADPF 提供 `PerformanceManager.setPreferPowerEfficiency(boolean)`。公开 API 中没有 `PerformanceManager` 这个入口；官方 API 是 `android.os.PerformanceHintManager.Session#setPreferPowerEfficiency(boolean)`，Added in API level 35。 建议：把类名/API 改为 `PerformanceHintManager.Session#setPreferPowerEfficiency(boolean)`；同时说明它只作用于 Performance Hint Session 绑定的线程，适用前提是先创建 hint session 并把网络后台线程 tid 加入 session，避免写成全局网络线程调度开关。
  2. [P2] §网络性能指标，验证标注（原 L104） — `developer.android.com/reference/okhttp3/EventListener` 返回 404；OkHttp EventListener 官方文档在 Square 站点，例如 `https://square.github.io/okhttp/5.x/okhttp/okhttp3/-event-listener/`。 建议：把验证标注改成 Square OkHttp EventListener 文档；Android Developers 只保留 Android 平台 API，例如 `ConnectivityManager` / `NetworkCapabilities`。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] §ConnectivityManager.NetworkCallback 段落后（原 L427） — 正文写“Android 16 的 BPF Network Bandwidth Estimator 把 `getLinkDownstreamBandwidthKbps()` 精度提升约 40%”。官方 `NetworkCapabilities` 文档只说明该值是 estimated first hop transport bandwidth；Android 11/12 官方文档有 bandwidth estimation 改进描述，但 Android 16 features/summary/behavior/release notes 未检到 BPF estimator 或 40% 精度提升口径。 建议：补一手来源（AOSP commit / Android 16 release note / 官方文档）后保留；否则删除 Android 16+BPF+40% 这组确定性表述，改为“该值仍是系统估算，只能做粗粒度分档”。如要讲版本线，可改写为 Android 11/12 对 bandwidth estimation 的官方改进。

### 其他记录
- [P3] §0-RTT 安全边界（原 L132） — `addAllowedQuicHost()` 是 QUIC host allowlist；Cronet 文档中 0-RTT 开关是 `QuicOptions.Builder.enableTlsZeroRtt(boolean)`，且跨会话 0-RTT 还依赖 HTTP disk cache/session state。正文当前读起来容易让人把 host allowlist 当作 0-RTT 控制项。 建议：后续可把 QUIC host allowlist、`enableTlsZeroRtt()`、disk cache/session reuse 三个条件拆开。

## 证据摘录
- Android Developers `PerformanceHintManager.Session#setPreferPowerEfficiency(boolean)`：Added in API level 35；说明为“threads can be safely scheduled to prefer power efficiency over performance”。
- Android Developers ADPF 文档：Power Efficiency Mode available in Android 15。
- Android Developers `NetworkCapabilities#getLinkDownstreamBandwidthKbps()`：返回 estimated first hop downstream bandwidth。
- Android 16 features / summary / behavior changes / QPR release notes：未检到 `BPF Network Bandwidth Estimator`、`getLinkDownstreamBandwidthKbps` 或 `40%` 精度提升口径。
- `https://developer.android.com/reference/okhttp3/EventListener`：HTTP 404；Square OkHttp EventListener 文档可访问。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：1 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 处（task9-12.2-network-api-version-audit-20260520）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处
- 更新章节 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
