# 深度技术抽检 · 2026-05-18 07:21

## Review 目标
- 章节：1.8 Activity Manager Service 与性能分析
- 文件：src/part1-fundamentals/ch01-architecture/08-activity-manager.md
- 状态：finalized（闲时抽检；last_task9_at: 2026-04-25T00:29:00+08:00，超过 21 天；曾有 P2 建议）

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- external-review：根目录无活跃文件，本轮未消费 external-review
- 正文处理：不修改正文，仅更新 frontmatter 与问题队列

## 审查结果

### 维度 1：源码引用准确性
- 评分：1.5/5
- 问题数：3
- 具体问题：
  1. [P0] L174 / L189 — `TOP_SLEEPING` 被写成 oom_adj=200；android-16.0.0_r1 `OomAdjuster` 的 top-sleeping 分支实际设置 `adj = FOREGROUND_APP_ADJ`。同段还把 `PERCEPTIBLE_MEDIUM_APP_ADJ` / `CACHED_APP_LMK_FIRST_ADJ` 归为 Android 17 新增，但这些常量在更早 AOSP tag 已存在。
  2. [P0] L285-L311 — Service ANR 代码块标注 android-16.0.0_r1，但 `realStartServiceLocked()` 签名和 `scheduleServiceTimeoutLocked()` 实现均与现行源码不一致；实际超时路径走 `mActiveServiceAnrTimer.start(proc, delay)`。
  3. [P0] L387-L399 — `AppStartInfo` 与 `ActivityManager.getStartRequestOverrideTime()` 不存在；实际 API 是 `ApplicationStartInfo#getStartComponent()`，由 `ActivityManager#getHistoricalProcessStartReasons()` / completion listener 获取。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L189 — `PERCEPTIBLE_MEDIUM_APP_ADJ` 至少 Android 12 已存在，`CACHED_APP_LMK_FIRST_ADJ` 至少 Android 10 已存在；不能写成 Android 17 (Baklava) 新增，且 Baklava 对应 Android 16 口径。
  2. [P1] L526-L528 — `ModernBroadcastQueue.java` 在 android-16.0.0_r1 下不存在；Android 16 应锚定 `BroadcastQueueImpl.java` + `BroadcastProcessQueue.java`，Android 14/15 则需说明 `BroadcastQueueModernImpl.java` 的版本差异。

## 统计
- P0 事实错误：3 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：1.8/5

## 闭环动作
- 写入 queue.json（P95）：1 条（合并 3 个 P0 + 1 个 P1）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 更新章节 frontmatter：`status=ready-for-review`、`pipeline_stage=task2b_pending`、`task9_result=needs-rework`、`task2b_state=pending`、`task2b_result=pending`、`last_task9_audit=2026-05-18`
- 自动晋升 finalized：0 个

## 问题单明细
  1. [P0] L174、L189：进程优先级表与 ProcessList 版本说明 — 正文把 TOP_SLEEPING 写成 oom_adj=200，并把 PERCEPTIBLE_MEDIUM_APP_ADJ=225、CACHED_APP_LMK_FIRST_ADJ=950 写成 Android 17 (Baklava) 新增。AOSP android-16.0.0_r1 的 OomAdjuster 在 top-sleeping 分支把 adj 设为 FOREGROUND_APP_ADJ=0；ProcessList 中 PERCEPTIBLE_MEDIUM_APP_ADJ 至少从 Android 12 起存在，CACHED_APP_LMK_FIRST_ADJ 至少从 Android 10 起存在，且 Baklava 对应 Android 16 口径，不应标成 Android 17 新增。 建议：把 TOP_SLEEPING 从 oom_adj 表中移出或改成“进程状态/importance”语义；版本说明改为“android-16.0.0_r1 已存在”，并补 Android 10/12/14/16 的常量演进边界。
  2. [P0] L285-L311：Service ANR 的 realStartServiceLocked()/scheduleServiceTimeoutLocked() 代码块 — 代码块标注 AOSP android-16.0.0_r1，但实际 ActiveServices.realStartServiceLocked() 签名为 ServiceRecord、ProcessRecord、IApplicationThread、pid、UidRecord、execInFg、enqueueOomAdj、serviceBindingOomAdjPolicy；scheduleServiceTimeoutLocked() 使用 mActiveServiceAnrTimer.start(proc, delay)，不是 mAm.mHandler.obtainMessage(SERVICE_TIMEOUT_MSG) 后 sendMessageDelayed。 建议：改为 android-16.0.0_r1 的真实代码骨架，或明确标注为旧版本/伪代码；保留“onCreate() 位于计时窗口内”的结论，但源码锚点要更新到 AnrTimer 路径。
  3. [P0] L387-L399：冷启动归因 getStartComponent() — 正文写 `ActivityManager.getStartRequestOverrideTime()` 配合 `AppStartInfo`，并把源码锚点写成 android.app.AppStartInfo；AOSP android-16.0.0_r1 不存在 AppStartInfo.java，ActivityManager.java 中也没有 getStartRequestOverrideTime()。实际类为 android.app.ApplicationStartInfo，getStartComponent() 在该类中，ActivityManager 提供 getHistoricalProcessStartReasons() 与 addApplicationStartInfoCompletionListener()。 建议：改为 ApplicationStartInfo#getStartComponent()（API 36 / Android 16），并说明通过 ActivityManager#getHistoricalProcessStartReasons() 或 completion listener 获取；删除 getStartRequestOverrideTime 与 AppStartInfo。
  4. [P1] L526-L528：ModernBroadcastQueue 架构与源码路径 — 正文标注 android-16.0.0_r1 的 ModernBroadcastQueue.java，但该路径不存在；android-16.0.0_r1 下对应的是 BroadcastQueueImpl.java + BroadcastProcessQueue.java。Android 14/15 tag 中现代实现类名又是 BroadcastQueueModernImpl.java，Android 16 才表现为 BroadcastQueueImpl.java。 建议：把源码锚点改成 android-16 的 BroadcastQueueImpl.java / BroadcastProcessQueue.java，并在版本演进里标注 Android 14/15 与 Android 16 的类名差异，避免读者按 ModernBroadcastQueue.java 查源码。
