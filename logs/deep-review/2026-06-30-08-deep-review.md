# 深度技术 Review · 2026-06-30 08:42

## Review 目标
- 章节：1.23 Android Staged Install 与安装原子性性能
- 文件：src/part1-fundamentals/ch01-architecture/23-staged-install-performance.md
- 状态：ready-for-review → task2b_pending
- 章节：1.24 ResourcesManager 与 Configuration 变更性能
- 文件：src/part1-fundamentals/ch01-architecture/24-resourcesmanager-configuration-performance.md
- 状态：ready-for-review → task2b_pending

## 外部 Review
- logs/external-review/ 根目录最近 3 天无活跃文件；本轮未消费 external-review，未归档。

## 审查证据
- Preflight：CANDIDATES 2 / IDLE_AUDIT 331 / TASK2B_BACKLOG 0。
- 已先读取 `/Users/gracker/.agents/skills/technical-writing/SKILL.md` 与 task9-deep-tech-review 规范。
- Android 主线证据均锚定 `android-17.0.0_r1`；仅为版本演进对比读取了 `android-10.0.0_r1`、`android-8.0.0_r1` 的 installd / PackageInstaller 片段，未使用 Android 18/API 38+。
- 关键源码/文档：`PackageInstaller.java#setStaged()`、`PackageInstallerSession#commit()`、`PackageInstallerService#buildSessionDir()` / `restoreAndApplyStagedSessionIfNeeded()`、`StagingManager#commitSession()` / `restoreSessions()` / `resumeSession()`、`Installer#connect()`、`InstalldNativeService`、`ResourcesManager` weak cache、`ActivityTaskManagerService#updateConfigurationLocked()`、`ActivityRecord#ensureActivityConfiguration()` / `shouldRelaunchLocked()`、`DisplayContent` / `WindowToken` FixedRotation 链路、官方 Compose state/state-saving 文档。

## 审查结果

### 1.23 Android Staged Install 与安装原子性性能

#### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：3
- 具体问题：
  1. [P0] L35、L90-L92 — Staged Install 版本与权限边界错误。`setStaged()` 已在 android-10.0.0_r1 存在；Android 17 中仍是 `@SystemApi` + `INSTALL_PACKAGES`，不是普通 `REQUEST_INSTALL_PACKAGES`。
  2. [P0] L63-L82、L165-L184 — `stageSession()`、`installStaged()`、`applyStaged()`、`isRebootRequired()`、`onBootCompleted()` 执行安装等方法/职责不符合 Android 17 源码。真实链路是 `commitSession()` / `restoreSessions()` / `resumeSession()`。
  3. [P0] L79 — staged session 目录写成 `/data/app/staged/vmdl{sessionId}/`；Android 17 `buildSessionDir()` 对 staged/APEX session 使用 `/data/app-staging/session_{id}`。

#### 维度 2：原理链完整性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1] L188-L206 — Cloud Profile、finalize dexopt 与 Staged Install 的关系没有 Android 17 源码闭环；需要重写为 ART/Profile 设备端消费边界。

#### 维度 3：版本差异覆盖
- 评分：1/5
- 问题数：2
- 具体问题：
  1. [P0] L144、L208-L216 — installd Binder 化不是 Android 16/17 新断点；android-8/10 已有 `InstalldNativeService` Binder，且 1.9 节明确不把它写成 Android 16 版本断点。
  2. [P0] L35、L92 — API 版本写成 API 31，应改为 API 29 起的 staged session 历史边界，并说明 Android 17 当前实现。

#### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P1] 缺少 Android 17 staged session 状态机：pre-reboot verification、ready/applied/failed、APEX checkpoint、APK-only 与 APEX 混合 session 的差异。

#### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] L107-L127、L156、L220-L225 — 安装耗时百分比、签名校验耗时、I/O 秒级估算和厂商优化没有可复现实验条件；已合并进本轮 P95 问题单。

#### 维度 6：交叉引用一致性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L144 与 1.9 节 L180 冲突：1.9 明确 `Installer.connect()` 通过 `ServiceManager.getService("installd")` 获取 Binder，不支持本节 Android 16 Binder 化结论。

### 1.24 ResourcesManager 与 Configuration 变更性能

#### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：3
- 具体问题：
  1. [P0] L107-L122、L373-L376 — Android 17 主链路不应锚到 `AMS.updateConfigurationLocked()` / `ActivityManagerService.java`；应锚到 `ActivityTaskManagerService#updateConfigurationLocked()`、`WindowProcessController`、`ActivityRecord#ensureActivityConfiguration()`。
  2. [P0] L130-L138 — FixedRotation 链路中的 `FixedRotationAdjustmentsItem`、`ActivityThread.handleFixedRotationAdjustments()`、`ResourcesManager.overrideTokenDisplayAdjustments()` 在 Android 17 中未命中；真实链路是 `DisplayContent` -> `WindowToken#applyFixedRotationTransform()` -> `ActivityRecord#ensureActivityConfiguration()`。
  3. [P1] L300-L332、L396-L401 — `ResourcesManager#mResourceImpls` 是 `WeakReference<ResourcesImpl>` 并有 ReferenceQueue 清理，不能写成缓存不会自动清理；泄漏要看外部强引用和 GC root。

#### 维度 2：原理链完整性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L284-L292、L403-L411 — Compose 段混淆 recomposition 与 Activity recreation。官方文档明确 `remember` 不跨 configuration changes；跨重建需要 `rememberSaveable` / ViewModel / SavedStateHandle。

#### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L225-L231 — Android 13 density 强制 recreate、Android 14 foldable 强制 recreate 缺少来源且与官方 `android:configChanges="density"` 和 Android 17 `shouldRelaunchLocked()` 逻辑不一致。只保留可验证的 Android 17 targetSdk 37 大屏行为。

#### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P1] 缺少 Android 17 `ActivityRecord#shouldRelaunchLocked()` 的判定模型：`info.getRealConfigChanged()`、PiP density skip、compat policy、resource overlay policy 和 recreate policy 如何共同决定 relaunch。

#### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P2] L163-L177、L326-L332 — recreate 耗时和 ResourcesImpl 内存估算没有设备、布局规模、trace、native heap 条件；已合并进本轮 P95 问题单。

#### 维度 6：交叉引用一致性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L122 source 写 ActivityManagerService 已验证，但源码入口不匹配；FixedRotation 还以 android-12 博客链路标注 android-17 适用，需换成当前 tag 源码链。

## 统计
- P0 事实错误：7 处
- P1 重要缺失：2 处
- P2 建议改进：2 处
- P3 锦上添花：0 处
- 总体技术评分：2.2/5

## 闭环动作
- 写入 queue.json（P95/P85）：2 条（1.23、1.24；P2 合并进同条问题单）
- 写入 research-gaps.md：2 条（staged session 状态机、Activity relaunch 判定模型）
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- 自动晋升 finalized：0 处
- frontmatter：两章均更新为 `task9_result: needs-rework`、`task9_state: reviewed`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_at: 2026-06-30`
