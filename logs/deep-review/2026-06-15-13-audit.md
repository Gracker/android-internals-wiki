# Task 9 闲时抽检 · 2026-06-15 13:20

## 抽检目标
- 章节：3.8 InputFlinger Rust 组件与自适应刷新率协同
- 文件：src/part1-fundamentals/ch03-input/08-inputflinger-rust-arr.md
- 状态：finalized / ready-to-publish
- 范围：闲时抽检，仅覆盖维度 1（源码引用）与维度 3（版本差异）

## 证据复核
- AOSP android-16.0.0_r1 复核通过：`frameworks/native/services/inputflinger/InputManager.cpp` 的 listener 管线包含 `InputFilter`；`InputFilter.cpp` 中 `notifyMotion()` 直接透传，KeyEvent 在 filter 启用时进入 Rust；Rust `input_filter.rs` 通过 `stickyKeysEnabled`、`slowKeysThresholdNs`、`bounceKeysThresholdNs` 组装过滤链。
- AOSP android-16.0.0_r1 复核通过：`InputDispatcher.cpp` 将 touch motion 归类为 `USER_ACTIVITY_EVENT_TOUCH`；`PowerManagerService.java` 发送 `Boost.INTERACTION`；`SurfaceFlinger.cpp` 在 interaction boost 时调用 `Scheduler::onTouchHint()`；`RefreshRateSelector.cpp` 对 `HighHint` / `ExplicitDefault` 的 touch boost 分支存在。
- 官方文档复核通过：Android Developers ARR 文档声明 ARR 支持 Android 15-QPR1 及以上且依赖 HAL；Android 16 features 文档列出 `hasArrSupport()`、`getSuggestedFrameRate(int)`、`getSupportedRefreshRates()` 与 RecyclerView 1.4 ARR 支持。
- AOSP tag 复核：`android-17.0.0_r1` manifest 当前未公开（android.googlesource 404）；正文未写 Android 17-only 行为，版本边界声明符合 AIW Android 17/API 37 上限。

## 维度结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0
- 结论：章节中的核心源码路径、类名、方法名与 android-16.0.0_r1 对齐；android-15.0.0_r36 的 `inputflinger/rust/input_filter.rs` 路径抽查存在。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0
- 结论：frontmatter 将适用范围限定到 Android 15-QPR1 / Android 16；正文只把 Android 17 作为“未核到公开 tag，不写 Android 17-only 行为”的边界说明，未越过 AIW 版本上限。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体抽检评分：4.5/5

## 闭环动作
- 更新章节 frontmatter：`last_task9_audit: "2026-06-15"`
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
