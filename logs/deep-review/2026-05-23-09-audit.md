# 深度技术 Review 闲时抽检 · 2026-05-23 09:31

## 抽检目标
- 模式：闲时抽检（preflight: CANDIDATES=0, IDLE_AUDIT=180）
- 章节：8.1 响应速度原理
- 文件：src/part2-performance/ch08-responsiveness/01-responsiveness-principles.md
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异）
- 选择原因：last_task9_at=2026-05-01T03:20:00+08:00，超过 21 天；上一轮 Task9 曾有 P2 交叉引用建议。

## external-review
- `logs/external-review/` 根目录无 8.1 活跃文件，本轮未消费。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.3/5
- 问题数：1（P2 1）
- 已复核通过：
  1. AOSP android-16.0.0_r1 中 `InputDispatcher::publishMotionEvent()` 位于 `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp`，事件发布链路落到 `InputPublisher::publishMotionEvent()`。
  2. `InputChannel::openInputChannelPair()` 使用 `socketpair(AF_UNIX, SOCK_SEQPACKET)`，`InputChannel::sendMessage()` 使用 `send()` 发送 `InputMessage`；正文“Binder 不承载每个 MotionEvent 分发”的结论成立。
  3. `android.view.MotionPredictor` 在 Android 14/15/16 的 `core/api/current.txt` 中均为 public API，`record()` / `predict(long)` / `isPredictionAvailable(int,int)` 签名与正文一致。
- 具体问题：
  1. [P2][源码准确性] L59 — 大纲仍写 RAIL `Load < 1000ms`；web.dev RAIL 当前口径是首次加载 < 5s、后续加载 < 2s，正文 L133-L135 已写对。已写入 `intake/suggestions.md`。

### 维度 3：版本差异覆盖
- 评分：3.8/5
- 问题数：1（P1 1）
- 已复核通过：
  1. Android Vitals 启动过长阈值仍为 cold ≥5s、warm ≥2s、hot ≥1.5s；TTID 自动上报、TTFD 依赖 `reportFullyDrawn()` 的描述成立。
  2. Slow rendering / frozen frames / games slow sessions 口径与 Android Developers 当前文档一致；90Hz/120Hz 帧预算说明仍有效。
  3. Touch Prediction 的版本边界写成 Android 14 API 34 起提供、Android 16 系统侧变化不做未验证断言，符合 AOSP public API 与正文边界。
- 具体问题：
  1. [P1][版本差异] L210-L214 — “从 Android 12 开始，系统引入 TTID / TTFD”缺官方依据且版本边界错误。当前 Android Developers 文档只说明 Android vitals 使用 TTID、TTFD 由 `reportFullyDrawn()` 标记；AOSP `Activity.reportFullyDrawn()` 在 android-4.4_r1 已存在。需删除 Android 12 归因，把 Android 12 只留给 SplashScreen API。已写入 `metadata/queue.json` P85。

## 统计
- P0 事实错误：0 处
- P1 重要缺失/版本错误：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体抽检评分：4.0/5

## 闭环动作
- 写入 queue.json（P85）：1 处（8.1 TTID/TTFD Android 12 归因）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处（RAIL Load 大纲阈值）
- 更新 frontmatter：8.1 `status: ready-for-review`、`pipeline_stage: task2b_pending`、`task9_result: needs-rework`、`task2b_state: pending`、`last_task9_audit: 2026-05-23`
- 仅日志记录：0 处

## 证据锚点
- Android Developers App Startup Time：TTID 自动上报；TTFD 通过 `reportFullyDrawn()` 标记；Android vitals excessive startup 阈值 cold ≥5s / warm ≥2s / hot ≥1.5s。
- AOSP `Activity.java`：`reportFullyDrawn()` 在 android-4.4_r1 已存在，android-16.0.0_r1 仍保留。
- web.dev RAIL：Load 目标为首次加载 < 5s，后续加载 < 2s。
- AOSP InputTransport：android-16.0.0_r1 `InputChannel::openInputChannelPair()` 使用 `socketpair(AF_UNIX, SOCK_SEQPACKET)`，`InputChannel::sendMessage()` 使用 `send()`。
