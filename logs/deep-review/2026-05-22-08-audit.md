# 深度技术 Review · 2026-05-22 08:40 · 闲时抽检

## Review 目标
- 章节：9.4 特殊场景的 ANR
- 文件：src/part2-performance/ch09-anr/04-special-anr.md
- 状态：finalized / ready-to-publish
- 模式：闲时抽检（无 task9_state=pending 候选；聚焦维度 1 源码引用 + 维度 3 版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] frontmatter sources L22 — `IActivityManager.java` 路径不存在；正确路径是 `IActivityManager.aidl`。
  2. [P0] L146-L150 — `ModernBroadcastQueue` 类名与 App 侧 `onReceive()` 线程模型错误。AOSP Android 14/15 源码是 `BroadcastQueueModernImpl` + `BroadcastProcessQueue`，Android 16 为 `BroadcastQueueImpl`；manifest receiver 仍经 `ActivityThread.H.RECEIVER` 在主线程调用 `onReceive()`。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L109 — Android 16+ Freezer 与 ANR 计时口径过度泛化。`BroadcastQueueImpl`/`AnrTimer` 只能支持广播调度、CPU starvation soft timeout 与 ANR 后冻结处理的限定结论，不能写成所有 ANR 在冻结期间暂停计时。
  2. [P1] L354-L358 — Android 16/16KB page 与 SQLite WAL 写放大的版本边界过宽。官方口径是 Android 15+ 16KB 兼容要求和部分 16KB 设备；SQLite `wal_autocheckpoint` 单位是数据库 page，不能直接等同 kernel memory page size。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：2 处
- P2 建议改进：0 处（既有 GC 数据 P2 本轮不重复处理）
- P3 锦上添花：0 处
- 总体技术评分：2.5/5

## 闭环动作
- 写入 queue.json（P95）：1 条，含 4 个 review_issues
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：`task9_result=needs-rework`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-05-22`

## 关键源码 / 资料锚点
- `frameworks/base/core/java/android/app/IActivityManager.aidl`
- `frameworks/base/core/java/android/app/ActivityThread.java`：`scheduleReceiver()` → `H.RECEIVER` → `handleReceiver()` → `receiver.onReceive(...)`
- `frameworks/base/services/core/java/com/android/server/am/BroadcastQueueModernImpl.java`（Android 14/15）
- `frameworks/base/services/core/java/com/android/server/am/BroadcastQueueImpl.java`（Android 16）
- `frameworks/base/services/core/java/com/android/server/utils/AnrTimer.java`
- Android Developers: Support 16 KB page sizes
- SQLite WAL / PRAGMA page_size docs
