# 深度技术 Review 闲时抽检 · 2026-06-06 14:25

## 抽检目标
- 章节：8.10 ProfilingManager 系统触发式性能追踪
- 文件：src/part2-performance/ch08-responsiveness/08-system-triggered-profiling.md
- 状态：finalized
- 模式：闲时抽检（CANDIDATES=0, TASK2B_BACKLOG=0, IDLE_AUDIT=292）
- 抽检原因：`last_task9_at=2026-04-25T22:29:14+08:00`（已 42 天未复检，>21 天阈值），`last_task9_audit` 缺失；frontmatter 标 `task9_result: needs-rework`，需复核前轮 P1（ANOMALY tag/artifact 闭环）是否已在正文中落地，并扫一遍维度 1（源码引用）与维度 3（版本差异）防止过期。
- 适用范围：Android 16 (API 36) - Android 17 (API 37)，符合 AIW 范围上限。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 范围：最高 Android 17 / API 37；本轮未引入 Android 18 / API 38+ 资料。

## 一手资料核验

### 维度 1：源码路径与 API 名称
- `packages/modules/Profiling/framework/java/android/os/ProfilingManager.java`：Profiling 模块作为 Mainline 模块（自 Android 10 起 APEX 化），公开 API 路径与官方文档 `developer.android.com/reference/android/os/ProfilingManager` 一致。
- `packages/modules/Profiling/framework/java/android/os/ProfilingTrigger.java` / `ProfilingResult.java`：与官方 `ProfilingTrigger`、`ProfilingResult` API 参考页面对齐。
- `packages/modules/Profiling/service/java/com/android/os/profiling/ProfilingService.java`：服务端实现路径，承载 trigger 注册、结果回传、artifact 落盘职责。
- 公开 API 名称核对：
  - `registerForAllProfilingResults(Executor, Consumer<ProfilingResult>)` —— Android 16+ 公开 API ✓
  - `addProfilingTriggers(List<ProfilingTrigger>)` —— Android 16+ 公开 API ✓
  - `ProfilingTrigger.Builder` / `setRateLimitingPeriodHours(int)` —— 与公开 API 一致 ✓
  - `ProfilingResult#getTriggerType()` / `getResultFilePath()` / `getTag()` —— 与公开 API 一致 ✓
- 关联 API：
  - `Activity.reportFullyDrawn()` —— Activity 公开 API ✓
  - `ApplicationStartInfo.getStartType() == START_TYPE_COLD` —— `ApplicationStartInfo` 自 Android 15 (API 35) 起公开 ✓
  - `ApplicationExitInfo.REASON_EXCESSIVE_RESOURCE_USAGE` —— `ApplicationExitInfo` 公开常量 ✓
- 源码版本标注：所有 AOSP 引用均标 `AOSP main`，符合"未进入 android-17.0.0_r1 的资料需明确标注"的规则（已标注，非正文结论级断言）。

### 维度 3：版本差异
- 触发器常量映射（与公开文档一致）：
  - API 36：`APP_FULLY_DRAWN=1`、`ANR=2` ✓
  - extension 36.1：`APP_REQUEST_RUNNING_TRACE=3`、`KILL_FORCE_STOP=4`、`KILL_RECENTS=5`、`KILL_TASK_MANAGER=6` ✓（扩展 gating 通过 `SdkExtensions.getExtensionVersion(36) >= 1`）
  - API 37：`OOM=7`、`ANOMALY=8`、`KILL_EXCESSIVE_CPU_USAGE=9`、`COLD_START=10`、`APP_COMPAT=11` ✓
- ProfilingManager 引入时间线：Android 15 (API 35) 引入 `ProfilingManager.requestProfiling()`，Android 16 (API 36) 扩展 `addProfilingTriggers()`，与公开 changelog 一致 ✓
- device_config 命名空间版本差异：
  - Android 16+：`profiling_testing.delete_temporary_results.disabled`（同时保留 redacted 与 unredacted）✓
  - Android 15：`profiling_testing.delete_unredacted_trace.disabled`（仅保留 unredacted）✓
  - testing 模式：`profiling_testing.system_triggered_profiling.testing_package_name` ✓
- `APP_FULLY_DRAWN` vs `COLD_START` 区别：API 36 的 APP_FULLY_DRAWN 是 `reportFullyDrawn()` 调用之后的 snapshot；API 37 的 COLD_START 是从冷启动尽早阶段开始录制，到 `reportFullyDrawn()` 或默认 ~5 秒停止，使用 discard buffer 保留早期事件。两者语义、采样窗口、常量值均不同，正文描述成立 ✓
- OOM 触发边界：`TRIGGER_TYPE_OOM` 处理 `OutOfMemoryError`，返回 Java heap dump，与 `LowMemoryKiller` / `lmkd` 路径分离，配套要求自定义 `UncaughtExceptionHandler` 仍需委托默认 handler ✓

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.8/5
- 问题数：0 个 P0/P1
- 发现：
  1. [P3] ANOMALY 段落（"ANOMALY：在进程终止之前拿到现场"）的 `[已验证]` 标签指向 `packages/modules/Profiling/service/java/com/android/os/profiling/ProfilingService.java`，但 ANOMALY 触发逻辑的实际驱动服务是 `AnomalyDetectionService`（位于 `frameworks/base/services/` 下的 `com.android.server.anomaly` 包），ProfilingService 负责结果交付。属于 source attribution 不完整而非引用错误，AIW 范围内不阻断；建议在后续 P2 轮次把 AnomalyDetectionService 路径补进 frontmatter `sources`。
  2. [P3] 所有 AOSP 引用统一标 `AOSP main`，未指定具体 tag；当前轮次未发现 Android 17 r1 已公开且与本章结论冲突的源码行为变化，暂不构成 P0/P1。
- 结论：API 名称、类名、方法签名、源码路径与公开文档一致；前轮 P0/P1 无回归。

### 维度 3：版本差异覆盖
- 评分：4.8/5
- 问题数：0 个 P0/P1
- 结论：触发器常量表、Android 15→16→16.1→17 的能力分层、`SdkExtensions` gating、`device_config` 命名空间差异、`COLD_START` 默认停止时间、OOM 与 LMK 的边界都成立。版本演进表与正文小节互相对齐，无遗漏。
- 观察（非问题）：Android 17 `android-17.0.0_r1` tag 当前在本轮抽检中未在本机 AOSP 镜像命中；本轮未把任何 Android 17 源码细节升级为正文结论，符合 AIW 范围上限。

## 历史 P1 复核
- 2026-04-25 22:29 留下的 P1：ANOMALY 触发器 tag 驱动分流、MemoryLimiter/ProfilingService 证据链、多包同 UID 注册时不保证返回产物的边界。
- 复核结果：当前正文 "ANOMALY：在进程终止之前拿到现场" 一节已包含以下闭环：
  - `ProfilingResult#getTag()` 携带异常分类信息（如 `MemoryLimiter`） ✓
  - artifact 动态性（`.hprof` 走 heap dump 工具链，`.perfetto-trace` 走 Perfetto UI） ✓
  - MemoryLimiter 场景的排查顺序（`ApplicationExitInfo.getReason()` → `getTag()` → heap dump） ✓
  - 多包同 UID 注册时不保证每进程都收到产物的边界 ✓
  - 公开 API 未列出 `AnomalyDetectionService` 全部判定规则的边界 ✓
- 闭环结论：内容层面 P1 已落地。frontmatter `task9_result: needs-rework` 仍为旧值（属于"内容已修复但 frontmatter 未随之更新"的元数据漂移），按"闲时抽检不改 frontmatter 业务状态"的原则留待 Task2B / Task6 链路处理；本轮仅以 P3 在日志中标注。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：2 处（AnomalyDetectionService 路径补全、AOSP tag 细化）
- 总体技术评分：4.8/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 修改正文：0 处
- 更新 frontmatter：`last_task9_audit: "2026-06-06"`、`last_task9_at: "2026-06-06T14:25:00+08:00"`
