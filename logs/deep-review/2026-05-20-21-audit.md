# 深度技术 Review 抽检 · 2026-05-20 21:20

## Review 目标
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- 章节：18.14 Camera 渲染管线
- 文件：src/part2-performance/ch18-rendering-pipelines/14-camera-pipeline.md
- 状态：finalized → task2b_pending（发现 P0/P1，写入 queue）
- 选择原因：无 pending 候选；该章节 `last_task9_at=2026-04-27`，超过 21 天，且上一轮有 Stream Use Case 相关 P2 建议。
- 外部输入：logs/external-review/ 根目录无 18.14 最近 3 天活跃文件

## 抽检证据
- AOSP `frameworks/base/core/java/android/hardware/camera2/params/OutputConfiguration.java`：android-13.0.0_r1 / android-14.0.0_r1 / android-15.0.0_r1 / android-16.0.0_r1 均无 `isConfigurationValid()`；`setStreamUseCase()` 对非法公开范围值抛 `IllegalArgumentException` 后再设置 `mStreamUseCase`。Android 13 的 max public use case 是 `VIDEO_CALL`，Android 14+ 是 `CROPPED_RAW`。
- AOSP android14-release `CameraMetadata.java`：`SCALER_AVAILABLE_STREAM_USE_CASES_CROPPED_RAW = 0x6`，`SCALER_AVAILABLE_STREAM_USE_CASES_VENDOR_START = 0x10000`。
- AOSP android14-release `MandatoryStreamCombination.java`：`sStreamUseCaseCombinations` 从 L1099 开始；正文 L130-L134 的 5 行组合对应 L865 开始的 `sConcurrentStreamCombinations`，属于 concurrent camera mandatory combination 口径。
- AOSP android14-release `CameraCharacteristics.java`：`SCALER_MANDATORY_USE_CASE_STREAM_COMBINATIONS` 与 `SCALER_MANDATORY_CONCURRENT_STREAM_COMBINATIONS` 是两个独立 key，不能互换。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L107-L120 `OutputConfiguration.setStreamUseCase()` 源码片段 — 正文给出的 android14-release 片段使用不存在的 `isConfigurationValid()`，且把非法 use case 的行为写成条件设置；实际 AOSP 会按公开值上限和 vendor range 校验，非法公开范围值抛 `IllegalArgumentException`，随后直接设置 `mStreamUseCase`。

### 维度 3：版本差异覆盖
- 评分：2.5/5
- 问题数：1
- 具体问题：
  1. [P1] L96-L134 Stream Use Case 常量与 Guaranteed Stream Combination — Android 14+ 已有 `CROPPED_RAW=0x6` 与 vendor range；正文同时把 `sConcurrentStreamCombinations` 误写成 `REQUEST_AVAILABLE_CAPABILITIES_STREAM_USE_CASE` 的 guaranteed combinations。真实 stream-use-case mandatory list 应对应 `SCALER_MANDATORY_USE_CASE_STREAM_COMBINATIONS` / `sStreamUseCaseCombinations`。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2.25/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（task9-audit-18.14-camera-stream-use-case-source-mismatch）
- 更新 frontmatter：task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-20
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
