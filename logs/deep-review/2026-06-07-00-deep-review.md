# 深度技术 Review · 2026-06-07 00:24

## Review 目标
- 章节：13.9 Android Tracing 基础设施:atrace、ftrace 与 Perfetto 数据采集原理
- 文件：src/part3-tools/ch13-perfetto/09-tracing-infrastructure.md
- 状态：ready-for-review

## 版本边界
- 本轮未使用 Android 18 / API 38+ 材料。
- `git ls-remote` 对 frameworks/base、frameworks/native、system/core、external/perfetto 的 `refs/tags/android-17*` 均无返回；本轮源码锚点仍以 Android 16.0.0_r1 / Android 15.0.0_r1 / Android 14.0.0_r1 为准，并保留 Android 17 待公开 tag 复核边界。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：2
- 具体问题：
  1. [P0 · AUTO-FIX] L169-L173、L208-L218、L256、L471-L475 — Task2B 已补 Android 15+ `libtracing_perfetto`，但正文仍把 Android 15+ Java Trace 图示/小结写成单一路径，遗漏 `atrace_begin()` fallback；本轮按 Android 15 / Android 16 源码补齐 TrackEvent 与 atrace fallback 双路径。
  2. [P0 · AUTO-FIX] L334 — `ATRACE_*` 宏位置误写为 `libcutils/Trace.h`；实际路径为 `system/core/libcutils/include/cutils/trace.h`。
- 证据：
  - AOSP android-14.0.0_r1 `frameworks/base/core/jni/android_os_Trace.cpp`: `nativeTraceBegin` 直接调用 `atrace_begin()`。
  - AOSP android-15.0.0_r1 `frameworks/base/core/jni/android_os_Trace.cpp`: `nativeTraceBegin` 调用 `tracing_perfetto::traceBegin()`。
  - AOSP android-15.0.0_r1 `frameworks/native/libs/tracing_perfetto/tracing_perfetto.cpp`: 通过 `toPerfettoCategory()` 在 Perfetto TrackEvent 与 `atrace_begin()` 之间分流。
  - AOSP android-16.0.0_r1 `frameworks/native/libs/tracing_perfetto/tracing_perfetto.cpp` / `tracing_perfetto_internal.cpp`: `shouldPreferAtrace()` 处理并发 atrace 兼容优先级。
  - AOSP android-16.0.0_r1 `system/core/libcutils/include/cutils/trace.h`: 定义 `ATRACE_BEGIN` / `ATRACE_END` / `ATRACE_CALL`。

### 维度 2：原理链完整性
- 评分：4.5/5
- 问题数：0
- 结论：ftrace → traced_probes → traced service 与 Java/C++ 用户空间 tag 的分流链路已闭合；本轮补齐了图示和小结中的 fallback 分支，避免正文结论与前文说明冲突。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：1
- 具体问题：
  1. [P1 · AUTO-FIX] L169-L171 — Android 15.0.0_r1 尚未使用 `shouldPreferAtrace()`；Android 16.0.0_r1 才引入该并发兼容判断。正文已改为 Android 15 使用 `toPerfettoCategory()`、Android 16 使用 `shouldPreferAtrace()` 的分版本表述。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 备注：`TracingManager` Java 层自定义 Data Source 仍标注为待验证，未作为正文结论。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 备注：开销数字已保持待验证边界，本轮不新增性能结论。

### 维度 6：交叉引用一致性
- 评分：4.5/5
- 问题数：0
- 结论：§14.10、§13.3、§9.1/9.2 等引用未发现新增技术冲突。

## AUTO-FIX 记录
- 修改 Java `android.os.Trace` Android 15+ 分流描述：区分 Android 15 `toPerfettoCategory()` 与 Android 16 `shouldPreferAtrace()`。
- 修改数据流图和小结：加入 Java Trace 的 `atrace fallback -> trace_marker -> ftrace ring buffer -> traced_probes` 分支。
- 修改 App 层 Data Source 描述：不再把 `android.os.Trace` / `androidx.tracing` 统一写成 `trace_marker`。
- 修正 C/C++ `ATRACE_*` 宏源码路径为 `system/core/libcutils/include/cutils/trace.h`。
- Frontmatter 已更新为 `task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`。

## 统计
- P0 事实错误：2 处（均已 auto-fix）
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.4/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- auto-fix：3 处，章节回到 Task6 复审
