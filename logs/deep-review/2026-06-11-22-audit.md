# 深度技术 Review 闲时抽检 · 2026-06-11 22:20

## 抽检目标
- 章节：21.8 启动监控与度量
- 文件：src/part5-app/ch21-startup/08-startup-monitoring.md
- 状态：finalized
- 选择原因：本轮 `CANDIDATES=0`、`TASK2B_BACKLOG=0`，进入闲时抽检模式；本节 `last_task9_audit=2026-05-17`，距今超过 21 天，且当前无 pending queue 条目。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 版本边界：只覆盖到 Android 17 / API 37；`android-17.0.0_r1` AOSP tag 当前不可取，源码锚点退到 `android-16.0.0_r1`，未采用 Android 18 / API 38+ 内容。

## 复核证据
- Android Developers `ApplicationStartInfo`：`ApplicationStartInfo` added in API 35；`STARTUP_STATE_FIRST_FRAME_DRAWN` 表示启动到首帧，`START_TIMESTAMP_FULLY_DRAWN` 表示应用调用 `reportFullyDrawn()` 的 monotonic 时间戳。
- Android Developers `ActivityManager.addApplicationStartInfoCompletionListener()`：completion callback 以 first frame drawn 为完成边界，不等待 `Activity.reportFullyDrawn()`；如需 `START_TIMESTAMP_FULLY_DRAWN`，应在 `reportFullyDrawn()` 后读取 `ApplicationStartInfo` 副本。
- AOSP `android-16.0.0_r1`：`frameworks/base/core/java/android/app/ApplicationStartInfo.java` 行 90-95、194-205 与上述状态和时间戳语义一致；`frameworks/base/core/java/android/app/ActivityManager.java` 行 4373-4380 明确 completion listener 与 `reportFullyDrawn()` 的边界。
- Android Developers App startup time：Android Vitals 慢启动阈值仍为 cold >= 5s、warm >= 2s、hot >= 1.5s，且 Vitals 使用 TTID；TTFD 需要应用调用 `reportFullyDrawn()`。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0 个 P0/P1
- 结论：正文 L225-L233 对 `ApplicationStartInfo`、`getHistoricalProcessStartReasons(int)`、`addApplicationStartInfoCompletionListener()`、`getStartupTimestamps()`、`START_TIMESTAMP_FULLY_DRAWN` 的描述与 Android Developers 和 AOSP `android-16.0.0_r1` 一致。正文 L94-L111 对 `reportFullyDrawn()` 作为 TTFD 入口的描述准确。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0 个 P0/P1；1 个 P2
- 具体问题：
  1. [P2][版本补充] L227 — Android 16 / API 36 为 `ApplicationStartInfo` 增加 `getStartComponent()` 与 `START_COMPONENT_ACTIVITY/BROADCAST/CONTENT_PROVIDER/SERVICE/OTHER`，可用于区分进程启动由 activity、service、broadcast 或 content provider 触发。正文把 `getReason()`、`getStartType()`、`getStartupState()`、`getStartupTimestamps()` 列为核心字段，未列 `getStartComponent()`。这不影响现有结论，但后续可在 Android 15+ 平台启动信息段补一句 API 36 组件维度。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：1 处（仅日志记录，未重复写入 suggestions.md）
- P3 锦上添花：0 处
- 总体技术评分：4.4/5

## 闭环动作
- 更新 frontmatter：`last_task9_audit: "2026-06-11"`
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节正文改动：0 处
