# 深度技术 Review 闲时抽检 · 2026-06-12 05:20

## 抽检目标
- 章节：3.3 手势导航与系统交互
- 文件：src/part1-fundamentals/ch03-input/03-gesture-navigation.md
- 状态：finalized / task9_result=pass-tech-review
- 模式：闲时抽检，仅覆盖维度 1（源码引用准确性）与维度 3（版本差异覆盖）

## 抽检结论
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议：0
- 处理动作：记录日志；更新 frontmatter `last_task9_audit: "2026-06-12"` 与 `last_task9_audit_log`。

## 维度 1：源码引用准确性
- 评分：4.6/5
- 结论：通过。
- 复核点：
  1. AOSP `android-17.0.0_r1` 对本轮检查的 frameworks/base 与 frameworks/native 路径请求返回 404，本轮不采用 Android 17 tag 作为正文结论。
  2. AOSP `android-16.0.0_r1` 中存在 `EdgeBackGestureHandler.java`、`BackPanelController.kt`、`BackPanel.kt`、`InputMonitorCompat.java`、`InputDispatcher.cpp`、`View.java`、`ViewRootImpl.java`、`DisplayContent.java` 等正文列出的关键锚点。
  3. `EdgeBackGestureHandler.triggerBack()` 在 `mBackAnimation == null` 时注入 `KEYCODE_BACK`，否则调用 `mBackAnimation.setTriggerBack(true)`；正文对 legacy / predictive 两条路径的拆分成立。
  4. `InputMonitorCompat` 调用 `InputManagerGlobal.monitorGestureInput(name, displayId)`，并暴露 `pilferPointers()`；`InputDispatcher.cpp` 中存在 `pilferPointersLocked()` 与 `CANCEL_POINTER_EVENTS` 相关路径；正文关于 monitor 旁路观察与 pilfer 后取消原目标 pointer stream 的描述成立。
  5. `DisplayContent.calculateSystemGestureExclusion()` 使用 `mSystemGestureExclusionLimit` 约束左右侧排除区域；`View.setSystemGestureExclusionRects()` 与 `ViewRootImpl.reportSystemGestureExclusionChanged()` 路径存在。

## 维度 3：版本差异覆盖
- 评分：4.5/5
- 结论：通过。
- 复核点：
  1. `core/api/current.txt` 复核：Android 13 tag 有 `OnBackInvokedCallback`，无 `OnBackAnimationCallback` 与 `PRIORITY_SYSTEM_NAVIGATION_OBSERVER`；正文 API 33 commit callback 边界成立。
  2. Android 14 tag 出现 `OnBackAnimationCallback`；正文 API 34 progress callback 边界成立。
  3. Android 16 tag 出现 `PRIORITY_SYSTEM_NAVIGATION_OBSERVER`；正文 API 36 observer-only 边界成立。
  4. 正文未写入 Android 18 / API 38+ 内容；未发现 targetSdk 37+ 且无法证明属于 Android 17/API 37 的结论。

## 证据索引
- AOSP android-16.0.0_r1 `EdgeBackGestureHandler.java`：`triggerBack()`、`updateIsEnabledInner()`、`pilferPointers()`、`dispatchToBackAnimation()`。
- AOSP android-16.0.0_r1 `InputMonitorCompat.java`：`monitorGestureInput()` / `pilferPointers()`。
- AOSP android-16.0.0_r1 `InputDispatcher.cpp`：`pilferPointersLocked()` / `CANCEL_POINTER_EVENTS`。
- AOSP android-16.0.0_r1 `DisplayContent.java`：`calculateSystemGestureExclusion()` / `mSystemGestureExclusionLimit`。
- AOSP `core/api/current.txt` tags：android-13.0.0_r1、android-14.0.0_r1、android-16.0.0_r1。

## 闭环动作
- 写入 queue.json：0
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- Auto-fix：0
