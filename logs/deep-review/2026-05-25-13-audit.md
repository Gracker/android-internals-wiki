# 深度技术 Review 闲时抽检 · 2026-05-25 13:20

## Audit 目标
- 章节：19.24 崩溃与 ANR 捕获机制
- 文件：src/part3-tools/ch19-apm/24-crash-anr-internals.md
- 状态：finalized / task9_result=pass-tech-review（抽检前）
- 抽检范围：维度 1 源码引用准确性 + 维度 3 版本差异覆盖
- 触发原因：无 task9 pending 候选；该章节 last_task9_at=2026-05-06，且曾出现 Task9/P2 历史建议。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：4
- 具体问题：
  1. [P0] L416-L435 — `sa_sigaction` 函数签名和旧 handler 转发方式错误。`sa_handler` 只能按一参数调用，`SA_SIGINFO` handler 才接收 `(int, siginfo_t*, void*)`。
  2. [P0] L450-L460 — Android 14 `ProcessList` OOM adj 常量值错误，正文片段与 AOSP `android-14.0.0_r1` 不一致。
  3. [P0] L475 — `/data/anr/` 文件命名写成 `traces_text.txt`，与当前 AOSP `StackTracesDumpHelper` 的 `anr_` 前缀生成逻辑不一致。
  4. [P0] L574-L585 — `ProfilingTrigger` 常量和 `ProfilingManager` 注册 API 写错：不存在 `TRIGGER_TYPE_APP_REQUESTED` / `TRIGGER_TYPE_CRASH` / `registerTrigger(...)`。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2（ProfilingTrigger 与维度 1 第 4 条合并计为同一 P0）
- 具体问题：
  1. [P0] L513 — 把 `FrozenStateChangeCallback` 写成 API 30+ LMK 事件监听不成立。`isLowMemoryKillReportSupported()` 是 API 30 的 LMK 历史退出报告能力查询；`IBinder.FrozenStateChangeCallback` 是 API 36 的 Binder 远端进程冻结状态通知。
  2. [P0] L570-L578 — Android 16/17 ProfilingTrigger 分层缺失：API 36 有 ANR / fully drawn 触发器，36.1 有 running trace，API 37 再增加 OOM、cold start、kill/anomaly/app-compat 等触发器；不能写成“crash 触发器（推测）”。

## 统计
- P0 事实错误：5 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：1 条，合并 5 个 review_issues
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`status=ready-for-review`、`pipeline_stage=task2b_pending`、`task9_result=needs-rework`、`task2b_state=pending`

## 复核依据
- AOSP `ProcessList.java` android-14.0.0_r1：`CACHED_APP_MAX_ADJ=999`、`CACHED_APP_MIN_ADJ=900`、`SERVICE_B_ADJ=800`。
- AOSP `StackTracesDumpHelper.java` main：`ANR_TRACE_DIR=/data/anr`、`ANR_FILE_PREFIX=anr_`，`createAnrDumpFile()` 生成 `anr_时间戳` 文件。
- Android Developers：`ApplicationExitInfo.getTraceInputStream()` API 30 起，API 31 起 native tombstone protobuf；`ActivityManager.isLowMemoryKillReportSupported()` API 30；`IBinder.FrozenStateChangeCallback` API 36；`ProfilingTrigger` / `ProfilingManager` 官方常量与注册方法。
