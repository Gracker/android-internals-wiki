# AIW Deep Review Log · 2026-08-04

- run_id: `20260804-123504-deep-review-9e41e213`
- mode: `deep-review`
- target: `src/part1-fundamentals/ch05-cpu-power/5.23-android17-background-audio-hardening-leaudio-power-source.md`
- baseline: `android-17.0.0_r1` / Android 17 API 37
- materials: none from pre-run; review used chapter-declared AOSP anchors only

## 审计结论

章节已有充分正文与 AOSP source anchors，未作为薄占位稿回流。复核重点放在 hardening Java/native 分层、LE Audio scanner suspend 边界、LC3 HOST/ADSP codec location 边界与 Android 17 版本边界。

## P0

- 无。未发现会导致错误主线结论、Android 18/API38+ 外推或新增章节违规的问题。

## P1

- 修复 LE Audio suspend 描述：把“提交停止并注销当前 scanner”改为明确说明 `stopBackgroundScan()` 将 stop/unregister 投递到 scan thread，不是同步硬件停止屏障。
- 修复 LC3/HOST 表述：将“软件配置”限定为“系统侧软件配置”，避免读者误解为应用进程 codec pipeline。

## P2

- 更新 frontmatter 审计时间、run_id 与 last_verified。
- 在章节 NOTE 中补充本次复核边界：不使用 Android 18/API38+ 主线行为外推，不把 LE Audio offload 设备相关收益写成 AOSP 保证。

## 实际补丁

- 修改 1 个既有 `src/**/*.md` 章节。
- 未新增章节。
- 未修改 `src/SUMMARY.md`。
- 未维护 review-findings issue pool：本轮未创建新的持久 P0/P1 findings，仅做 bounded 文案/边界修复。

## Evidence

本轮临时抽查源码来自 android.googlesource.com `refs/tags/android-17.0.0_r1`：

- `HardeningEnforcer.java`: `blockFocusMethod()` 中 `USAGE_ALARM` + exact alarm、`BLUETOOTH_CONNECT` 降级到 partial、`CINNAMON_BUN` 分支。
- `Tracks.cpp`: `getHardeningDecision()` 对 `ENABLE/THROW` 映射 FULL、Offload/MMAP permission callback 的 `kBroadcastDelay = 40ms`、`maybeLogPlaybackHardening()` 单 track 守门。
- `LeAudioService.java`: `setSystemSuspended()`、`isScannerNeeded()`、`stopBackgroundScan()` 的 scan-thread 投递与 scanner id 状态更新。
- `codec_manager.cc`: offload property/HAL gate、默认 HOST、`IsLc3ConfigMatched()` 四个 LC3 维度、software-session fallback TODO。
