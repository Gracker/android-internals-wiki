# 深度技术 Review · 2026-05-07 20:24

## Review 目标
- 章节：4.1 Android 内存模型全景
  - 文件：src/part1-fundamentals/ch04-memory/01-memory-overview.md
  - 状态：ready-for-review → task2b_pending

## 候选说明
- 本轮只有 4.1 一个 `task9_state: pending` 候选；其余章节无 Task9 pending。
- 4.1 在 19:32 已审过一次，但 19:48 Task2B 完成技术修复、20:08 Task6 复审后重新置为 `task9_pending`，本轮按“修复后复审闭环”处理。
- `logs/external-review/` 根目录无活跃文件；未消费 external-review 活跃文件。
- `memory_search` 不可用：embedding provider `zai` 未注册；本轮以仓库文件、官方网页抓取、Perfetto 本地源码/文档为准。

## 审查结果

### 4.1 Android 内存模型全景
- 源码引用准确性：3.5/5
- 原理链完整性：4.0/5
- 版本差异覆盖：4.0/5
- 知识盲区：4.0/5
- 数据与案例支撑：3.5/5
- 交叉引用一致性：4.0/5
- 具体问题：
  - [P1] [源码准确性/工具数据源] L170、L458 Perfetto 内存趋势 / 内存面板数据源 — 正文仍把 `android.process_meminfo` 写成 Perfetto 内存趋势/内存面板数据源，并写成可周期性采集 PSS/RSS。Perfetto 官方 memory-counters 文档与本地源码显示，进程内存轮询数据源是 `linux.process_stats`，常见计数器为 `mem.rss`、`mem.rss.anon`、`mem.rss.file`、`mem.swap`、`mem.rss.watermark`；系统 meminfo/vmstat 走 `linux.sys_stats`；Java heap dump 走 `android.java_hprof` / `android.java_hprof.oom`。默认 Perfetto 进程内存时间线不能直接等同于 `dumpsys meminfo` 的 PSS 口径。建议：把 L170 改成“如果采集的是 dumpsys/smaps/PSS 快照口径才按 PSS 解释；Perfetto `linux.process_stats` 默认主要解释 RSS/anon/file/swap”；把 L458 改成 `linux.process_stats` + `linux.sys_stats` 的 TraceConfig 说明。
  - [P2] [数据缺失/Trace 观察点] L221、L272、L460、L526 — dumpsys meminfo 真机输出、Graphics 内存、Perfetto 内存曲线和内存压力 Trace 仍是 `[待补充]`，缺设备版本、page size、TraceConfig 数据源、采样周期与操作步骤。建议补同设备、同包名的 `dumpsys meminfo` 前后快照 + Perfetto TraceConfig，并标注观察点。

## 外审/历史项复核
- 关闭 1 条 4.1 历史 external-review pending：源码锚点、Android 17 MemoryLimiter、MTE 开销、16KB page PSS 修正均已在正文覆盖；剩余 Perfetto 数据源问题另开本轮 P1。
- 19:32 发现的 Android 17 MemoryLimiter P0 已修正：正文已写 `REASON_OTHER` + `getDescription()` 包含 `MemoryLimiter:AnonSwap`，未再写成独立 reason code。
- 19:32 发现的 Perfetto Track 主段已部分修正：L512-L514 已改成 `linux.process_stats` / `linux.sys_stats` / `android.java_hprof`；但 L170、L458 遗留旧口径。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.8/5

## 闭环动作
- 写入 queue.json：1 个 P85 pending 条目（4.1 Perfetto 数据源口径）
- 关闭 queue.json 历史 external-review pending：1 条
- 写入 suggestions.md：1 条 P2
- 写入 research-gaps.md：0 条
- 更新章节 frontmatter：task9_state=reviewed，task9_result=needs-rework，task2b_state=pending，pipeline_stage=task2b_pending
- 自动晋升 finalized：0 个（本轮存在 P1）

## 复核依据
- Android 17 behavior changes：app memory limits 命中后 `ApplicationExitInfo.getReason()` 为 `REASON_OTHER`，`getDescription()` 包含 `MemoryLimiter:AnonSwap`，可结合 `TRIGGER_TYPE_ANOMALY` 触发 heap dump。
- Perfetto `docs/data-sources/memory-counters.md`：进程内存时间线使用 `linux.process_stats`，配置 `process_stats_config.proc_stats_poll_ms`；示例 counter 为 `mem.rss`、`mem.rss.anon`、`mem.rss.file`、`mem.swap`、`mem.rss.watermark`。
- Perfetto 本地源码：`src/traced/probes/ps/process_stats_data_source.cc` 注册 `linux.process_stats`；`src/traced/probes/sys_stats/sys_stats_data_source.cc` 注册 `linux.sys_stats`；`src/traced/service/builtin_producer.cc` 注册 `android.java_hprof` / `android.java_hprof.oom`。
