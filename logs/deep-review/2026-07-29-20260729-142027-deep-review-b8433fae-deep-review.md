# Deep-Review Audit Log

- **Run ID**: `20260729-142027-deep-review-b8433fae`
- **Lane**: deep-review (profile: aiw-polish-deep-review)
- **Date**: 2026-07-29
- **Target**: `src/part1-fundamentals/ch04-memory/4.04-AppFlow与Android-17-LMKD兼容性方案.md`
- **SHA16**: `90e791ce5ed5bca5`
- **Mode**: deep-review / technical audit
- **Materials**: none (empty)
- **Android baseline**: `android-17.0.0_r1` / Android 17 (API 37)

---

## 审计结论

本页 chapter 4.04 描述的 "AppFlow" 冷启动内存调度方案及其与 LMKD 的兼容性架构，
在 `android-17.0.0_r1` 源码中 **完全不存在**。涉及的虚构内容包括：

| 虚构内容 | 真实情况 |
|---------|---------|
| `AppFlowManager.java` | AOSP 中无此类；路径 `frameworks/base/services/core/java/com/android/server/am/AppFlowManager.java` 不存在 |
| `LmkdService` (Java 类，含 `setProcessPriority`) | 不存在；lmkd 交互由 `ProcessList` → native `lmkd` socket 完成 |
| `MemoryTracker.preAllocate()` | 不存在；Android 无此 API |
| AppFlow/LMKD 共享内存池 | 不存在；lmkd 仅接收 `oom_score_adj` 候选优先级，单向状态传递 |
| 性能数据表 (1200ms→850ms 等) | 无设备、build、样本量、测试命令；全部无效 |
| MemoryLimiter 作为 AppFlow 配额服务 | 错误；MemoryLimiter 是 `system_server` 内独立的 `memory.high`/`memory.swap.high` 限制机制 |

页首已有正确的 Review 结论 blockquote 和末尾"源码边界"章节，正确地否定了 outline 正文。
但 outline 正文本身（protected outline-start 到 outline-end）仍完整保留虚构内容。

### Deep-review lane 处理决策

- **materials 为空** → 禁止凭通用知识大段扩写。
- **正文为占位/生成稿** → 只能做 frontmatter/状态修复 + 标注 needs-rework + 小范围结构修复。
- **不删除 protected outline** → outline 有 `<!-- outline-start/end -->` 保护标记，可能被其他流程引用；
  正确做法是在 outline 内插入醒目警告注释，保持原内容不变，让读者明确知道其不可信。

---

## 实际补丁

### P0 — fabricated-api (finding-5c6eb93406f0)

虚构 API/性能数据/组件关系。状态保持 **open**（需要 body-apply lane 用真实 source material 重做正文）。

**缓解措施**（本次执行）：
- frontmatter `status` 从 `ready-for-review` 改为 `needs-review`
- frontmatter 新增 `confidence: low`、`pipeline_stage: deep-review`、`task6_state: needs-rework`、`task9_state: pending`
- 页首 Review blockquote 升级为醒目的 `⚠️ 本页待重做（needs-rework）` 警告
- outline-start 后插入中文警告注释，明确标注全部内容未经源码验证
- 补充指向 4.50、4.36、4.17 已核验章节的导航

### P1 — needs-rework-no-source (finding-16d24a0cb28c)

无 source material，正文为占位稿，需要 body-apply lane 重做。状态保持 **open**。

### P2 — 结构修复（已修复）

| 问题 | 修复 |
|-----|------|
| outline 内重复 `## 要点` header（与 line 20 重复） | 替换为 HTML 注释警告 |
| `锚锭点` typo（line 41，应为 `锚点`） | 已修正为 `锚点` |
| frontmatter 缺少 `last_verified` / `confidence` | 已补充 |

### P2 — quality flags 处理

| Flag | 处理 |
|------|------|
| `thin-source-marking` | outline 内已加警告注释；frontmatter confidence 设为 low |
| `missing-last_verified` | 已添加 `last_verified: 2026-07-29` + `last_verified_against: AOSP android-17.0.0_r1` |
| `missing-confidence` | 已添加 `confidence: low`（无来源支撑，不可提升） |

---

## 验证

| 检查项 | 结果 |
|-------|------|
| `check-metadata.py --files <target>` | ✅ 0 failures（仅可选 `sources` 字段警告） |
| `check-summary-links.py` | ✅ passed（2 个 pre-existing duplicate 警告，与本章无关） |
| `git diff --check` | ✅ clean |

---

## 回流建议

本页应回流到 **body-apply / content-processing lane**，需要：

1. 真实 AOSP source material（非 DeepResearch 通用知识）覆盖 AppFlow 主题
2. 或：确认 AppFlow 在 Android 17 中确实不存在，将本页降级/合并到 4.50 lmkd 章节
3. 或：删除 protected outline，仅保留源码边界章节作为 redirect 页

在获得 source material 之前，`confidence` 保持 `low`，`status` 保持 `needs-review`。
