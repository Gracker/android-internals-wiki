# Task 9 闲时抽检 · 2026-06-29 18:34

## Audit 目标
- 章节：22.1 布局优化策略
- 文件：src/part5-app/ch22-rendering-practice/01-layout-optimization.md
- 状态：finalized / task6_pending（本轮 auto-fix 后回到 Task6 复审）
- 触发原因：无 task9_state: pending 候选；Task2B backlog=0；该章 last_task9_at=2026-05-13T06:25:00+08:00，超过 21 天，且上次 Task9 记录有 P2 建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 边界：仅使用 Android 17 / API 37 及以下材料；未使用 Android 18 / API 38+ 内容。本轮 `platform/frameworks/base` 已命中 `android-17.0.0_r1` tag。

## 一手核对锚点
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/view/ViewRootImpl.java`：`performTraversals()` 存在，内部进入 `performMeasure()` / `performLayout()`；`performMeasure()` trace 名为 `measure`，`performLayout()` trace 名为 `layout`。章节对 View 遍历路径和 trace 观察点的描述成立。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/view/View.java`：`requestLayout()` 设置 `PFLAG_FORCE_LAYOUT` 并向父节点传播 layout request。章节对触发路径的引用成立。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/view/LayoutInflater.java`：存在 `TAG_INCLUDE` / `TAG_MERGE` 与 `parseInclude()` 分支，章节对 `<include>` / `<merge>` 的源码锚点成立。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/view/ViewStub.java`：`inflate()` / `setVisibility()` 会用目标布局替换 `ViewStub`，章节对延迟创建和不能重复 inflate 的描述成立。
- Android Developers Blog 2017 ConstraintLayout benchmark：20 秒 Systrace、80 次 expensive measure/layout alerts、`FrameMetrics.LAYOUT_MEASURE_DURATION` 约 40% 改善的引用与原文一致；正文已保留“不能当成固定收益”的边界。
- AndroidX `asynclayoutinflater:1.1.0` source/docs：后台 inflate 要求 parent `generateLayoutParams(AttributeSet)` 线程安全，View 构造不能依赖 `Looper.myLooper()` / `Handler`，失败时回退主线程；章节边界描述成立。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] frontmatter 与正文验证锚点 — 章节适用范围已到 Android 17 / API 37，但 `last_verified_against` 仍停在 AOSP android-16.0.0_r1，正文 AOSP 锚点未显式写 tag。已基于 android-17.0.0_r1 复核并更新锚点。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 结论：本章讨论的是 View 布局优化、`ViewStub` / `<merge>` / `<include>` / `AsyncLayoutInflater` 使用边界，未发现 Android 17 相比 Android 16 会推翻正文结论的版本差异。未引入 Android 18/API 38+ 内容。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处新增
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：更新章节 `last_verified` / `last_verified_against`、AOSP inline 验证 tag、Task9 frontmatter 状态；回到 Task6 复审。
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：0 处
