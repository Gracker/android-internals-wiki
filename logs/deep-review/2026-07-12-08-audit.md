# Task 9 闲时抽检 · 2026-07-12 08:25

## 抽检目标
- 章节：18.4 Android View 混合渲染链路
- 文件：src/part2-performance/ch18-rendering-pipelines/04-android-view-mixed.md
- 状态：finalized
- 选择说明：Preflight `CANDIDATES=0`、`TASK2B_BACKLOG=0`、`IDLE_AUDIT=342`，进入闲时抽检模式。本节 `last_task9_at=2026-05-09T00:37:58+08:00`，超过 21 天；历史 Task9 曾保留 dumpsys/Perfetto 字段样例与交叉引用类 P2，适合作为源码引用与版本边界抽检目标。
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）
- Android 范围：最高 Android 17 / API 37；源码锚点只使用 `android-17.0.0_r1` 及以下 tag，未使用 Android 18 / API 38+ 内容。

## 维度 1：源码引用准确性
- 结果：通过，无 P0/P1
- 复核证据：
  - AOSP `android-17.0.0_r1` `frameworks/native/libs/gui/SurfaceComposerClient.cpp` 存在 `SurfaceComposerClient::Transaction::merge(Transaction&&)` 与 `Transaction::setDesiredPresentTime(...)`，正文跨 Surface 同步原语表的 native Transaction 锚点成立。
  - AOSP `android-17.0.0_r1` `frameworks/native/libs/gui/Surface.cpp` 存在 `Surface::setBuffersTimestamp(int64_t)`，正文把它限定为 native/EGL 层 per-buffer timestamp 能力，未误写成 Java `Surface` 公共 API。
  - AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/view/SurfaceView.java` 仍包含 transparent region / `canvas.punchHole(...)` / `syncNextFrame(...)` 相关实现，正文没有把挖洞机制固化为单一 `clipOut()` 调用。
  - AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/window/SurfaceSyncGroup.java` 中 `add(SurfaceView, Consumer<SurfaceViewFrameCallback>)` 标记 `@hide`，`add(AttachedSurfaceControl, ...)` 与 `add(SurfaceControlViewHost.SurfacePackage, ...)` 路径存在；正文对 SurfaceView internal 路径和受控 Surface 边界的说明成立。
  - AOSP `android-17.0.0_r1` `frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp` 在 commit 阶段按 layer 检查 `hasReadyFrame()` / `willReleaseBufferOnLatch`，再调用 `latchBufferImpl(...)`；正文“per-layer 推进、无新 buffer 时沿用上一帧”的主线与源码一致。

## 维度 3：版本差异覆盖
- 结果：通过，无 P0/P1
- 复核证据：
  - `SurfaceControl.Transaction#addTransactionCommittedListener` 在 `android-12.0.0_r1` / `android-12.1.0_r1` 未检出，在 `android-13.0.0_r1` 与 `android-17.0.0_r1` 存在；正文标注 Android 13 / API 33+ 成立。
  - `SurfaceSyncGroup.java` 在 `android-13.0.0_r1` 路径 404，在 `android-14.0.0_r1` 与 `android-17.0.0_r1` 存在；正文标注 Android 14 / API 34+ 成立。
  - 章节 `applicable_versions: Android 11 (API 30) - Android 17 (API 37)` 未把 Android 18 / API 38+ 主线行为写入正文结论。

## 记录项
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议：0 新增
- P3 备注：1
  - 既有维护项仍成立：`dumpsys SurfaceFlinger` 中 `activeBuffer` / `latched buffer` 以及 `GLES` / `HWC` / `OVERLAY` 字段口径会随版本和 dump 视图变化；正文只把它作为排查方向，本轮不升级为 P1，也不重复写入 suggestions.md。

## 闭环动作
- queue.json：未写入
- research-gaps.md：未写入
- suggestions.md：未写入
- frontmatter：已更新 `last_task9_audit: 2026-07-12`
