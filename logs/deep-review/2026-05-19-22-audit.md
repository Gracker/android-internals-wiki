# 深度技术 Review 闲时抽检 · 2026-05-19 22:20

## 抽检目标
- 章节：1.2 系统启动全流程
- 文件：src/part1-fundamentals/ch01-architecture/02-boot-process.md
- 状态：finalized
- 抽检原因：无 task9_state: pending 候选；该章 last_task9_audit 不存在，且历史 Task9 曾保留 bootstat/GBL、Zygote slice、Pixel 8 数据、dm-verity 版本边界等 P2 建议。
- 抽检范围：闲时模式，仅复核维度 1（源码/API 引用准确性）与维度 3（版本差异）。

## 复核依据
- AOSP android-16.0.0_r1 `system/core/bootstat/bootstat.cpp`：`ShowHelp()` 中 `-l/--log` 定义为 “Log all metrics to logstorage”，`-p/--print` 定义为 “Dump the boot event records to the console”；`main()` 中 `case 'l'` 调用 `LogBootEvents()`，`case 'p'` 调用 `PrintBootEvents()`。
- AOSP android-16.0.0_r1 `frameworks/base/config/preloaded-classes`：去注释和空行后共 18431 条，正文中的现代分支数量口径成立。
- AOSP android-16.0.0_r1 `frameworks/base/core/java/com/android/internal/os/ZygoteInit.java`：`PreloadClasses`、`PreloadResources` trace 名称存在；`PreloadSharedLibraries` / `PreloadOpenGL` 在当前文件中未命中，沿用既有 P2。
- AOSP android-16.0.0_r1 `frameworks/base/services/java/com/android/server/SystemServer.java`：`startBootstrapServices(t)` → `startCoreServices(t)` → `startOtherServices(t)` → `startApexServices(t)` 顺序成立。
- AOSP 对比：`startApexServices()` 在 `android-12.0.0_r34` / `android-12.1.0_r27` 中不存在，在 `android-13.0.0_r1` 起存在，正文 Android 13 版本边界成立。
- AOSP android-16.0.0_r1 `system/core/rootdir/init.rc`：`on zygote-start` 下存在 `wait_for_prop odsign.verification.done 1`、`start zygote`、`start zygote_secondary`，正文 zygote-start 描述成立。

## 审查结果

### 维度 1：源码引用准确性 / API 名称准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0][源码/API 使用错误] L245-L249 — 正文把 `adb shell bootstat -l` 写成“快速看分段耗时”的入口；但 AOSP `bootstat.cpp` 中 `-l/--log` 是写入 logstorage，`-p/--print` 才会把 boot event records 打到控制台。建议把示例命令改为 `adb shell bootstat -p`，并按需要补充 `getprop` 读取 bootloader 上报项的说明。
- 复核通过项：preloaded-classes 18431 条、SystemServer 四阶段顺序、init.rc zygote-start/odsign 等源码锚点与正文一致。
- 既有 P2：L255 `PreloadSharedLibraries` / `PreloadOpenGL` 不是 android-16.0.0_r1 `ZygoteInit.java` 当前核心 trace 名称，本轮仅确认仍存在，不重复写入 suggestions.md。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0（无新增 P0/P1）
- 复核结论：
  1. `startApexServices()` 的 Android 13 引入边界经 AOSP tag 对比确认成立。
  2. Android 16 分支 preloaded-classes 数量口径成立。
  3. `zygote-start` 等 init 阶段节点按 android-16.0.0_r1 描述，未发现会影响版本判断的新增阻断项。
- 既有 P2：GBL/bootstat 上报口径、dm-verity/AVB 版本边界、Pixel 8 冷启动分段数据来源仍属于历史建议项，本轮不升级为 P0/P1。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：4 处（历史 P2，未新增写入 suggestions.md）
- P3 锦上添花：0 处
- 闲时抽检技术评分：3.5/5

## 闭环动作
- 写入 queue.json：1 条（`task9-1.2-bootstat-flag-error`，P95）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（P0 已写入 queue；历史 P2 不重复追加）
- 更新 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: "2026-05-19"`
- 自动晋升 finalized：无
