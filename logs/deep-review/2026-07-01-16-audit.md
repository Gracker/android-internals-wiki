# Task 9 闲时抽检 · 2026-07-01 16:29

## 抽检目标
- 章节：19.26 混合栈与跨平台 APM (WebView / Flutter)
- 文件：src/part3-tools/ch19-apm/26-hybrid-apm.md
- 状态：finalized
- 选择原因：无 task9 pending 候选；Task2B backlog=0；该章节 last_task9_audit=2026-06-09，超过 21 天，且历史 Task9 留有 P2 建议。
- 范围：闲时抽检，仅复查维度 1（源码引用准确性）与维度 3（版本差异）。
- Android 版本边界：主线锚定 android-17.0.0_r1 / Android 17(API 37)；未使用 Android 18/API 38+ 资料。
- external-review：根目录最近 3 天无活跃文件，未消费 external-review。

## 证据源
- AOSP android-17.0.0_r1 `frameworks/base/graphics/java/android/view/PixelCopy.java`：`request(Window, Rect, Bitmap, listener, Handler)` 存在，Window backing surface / DecorView 条件与正文 PixelCopy 边界一致。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/WebViewClient.java`：`onPageCommitVisible()`、`onRenderProcessGone()` 存在；`onRenderProcessGone()` 对受影响的每个 WebView 回调，参数 WebView 不可继续使用。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/webkit/WebView.java`：`postVisualStateCallback()`、`evaluateJavascript()`、`addJavascriptInterface()`、`setWebViewRenderProcessClient()` 存在。
- 本地 Android SDK sources android-28 / android-29：android-28 无 `WebViewRenderProcessClient.java`，android-29 有 `WebViewRenderProcessClient` 与 unresponsive/responsive 回调，正文 API 29+ 边界成立。
- Flutter API docs：`FrameTiming.buildDuration` / `rasterDuration` / `totalSpan` 为时长指标；`timestampInMicroseconds(FramePhase)` 文档说明 epoch 不保证匹配 `DateTime`。
- 本书交叉引用：§2.11、§18.12 已将 Flutter 移动端主线线程模型写为 Main(UI+Platform) / Raster / IO，并把独立 UI thread 限定为旧版或定制 Embedder。
- Flutter upstream issue #150525：记录 Android/iOS stable 默认合并 UI 与 Platform 线程的上游进展；本轮仅作为 Flutter 版本差异证据，不涉及 Android 18/API 38+。

## 抽检结论
- P0：0
- P1：1
- P2：1（既有建议级问题，本轮不扩大 suggestions）
- 处理：未直接改正文。Flutter merged model 的精确版本口径需要与 §2.11 / §18.12 一并收敛，已写入 queue.json；章节 frontmatter 标记 `task2b_pending`。

## 维度 1：源码引用准确性
- 评分：4/5
- P0/P1：0
- 复核结果：正文涉及的 Android API 名称与主要签名可在 android-17.0.0_r1 / SDK sources 中核对：`PixelCopy.request(Window, Rect, ...)`、`WebViewClient.onRenderProcessGone()`、`WebViewRenderProcessClient`、`WebView.postVisualStateCallback()`、`evaluateJavascript()` 均未发现源码路径或 API 名称错误。
- P2 观察：L160 对 `onPageCommitVisible()` 的“新内容首次绘制到屏幕”描述略强。AOSP 文档更精确地说它保证旧导航内容不再绘制，下一次 draw 可能是背景色或部分新页面内容；这不影响当前白屏采样主链路，保留为建议级边界。

## 维度 3：版本差异覆盖
- 评分：3/5
- P1：1
- 具体问题：
  1. [P1][版本差异/交叉引用一致性] L66 / L86 / L231 / L233 — 19.26 仍按“Flutter 自己的 UI 线程 / Dart UI 线程 + Raster 线程”描述 APM 观察入口，缺少 current Flutter merged UI+Platform 线程模型边界。§2.11 与 §18.12 已明确 current 主线应看 Main(UI+Platform) / Raster / IO，旧版或定制 Embedder 才单独看 `1.ui` / `io.flutter.ui`。该不一致会让读者在 Android 端 APM/Perfetto 中把 Dart Build/Layout/Paint 错归到不存在的独立 UI thread。
- WebView 版本复核：API 23+ `onPageCommitVisible()` / `postVisualStateCallback()`、API 26+ `PixelCopy` 与 `onRenderProcessGone()`、API 29+ `WebViewRenderProcessClient` 的正文边界仍成立。
- Flutter 时间戳复核：`FrameTiming` 时长与 raw timestamp 边界仍成立；正文禁止把 raw timestamp 直接换算为 `elapsed_realtime_ms` 的结论成立。

## 闭环动作
- 写入 queue.json：1 处（P85，19.26 Flutter merged UI+Platform 线程模型边界）
- 写入 research-gaps.md：0
- 写入 suggestions.md：0
- 修改章节正文：0
- 更新 frontmatter：1（`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-07-01`）
