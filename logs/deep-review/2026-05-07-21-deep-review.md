# 深度技术 Review · 2026-05-07 21:27

## Review 目标
- 章节：19.13 androidx.tracing（Tracing SDK）
  - 文件：src/part3-tools/ch19-apm/13-tracing-sdk.md
  - 状态：ready-for-review → task2b_pending

## 候选说明
- 本轮有效 `task9_state: pending` 章节只有 19.13；19.27 正文为空壳，按任务规则跳过；其余章节无 Task9 pending。
- 19.13 在 19:32 已审过一次，但 19:48 queue 中 Task9 P95 条目被标记 completed 后，章节 frontmatter 仍处于 `task9_state: pending`，本轮按“修复后复审闭环”处理。
- `logs/external-review/` 根目录无活跃文件；未消费 external-review 活跃文件。
- `memory_search` 不可用：embedding provider `zai` 未注册；本轮以仓库文件、AndroidX 官方 release 页面、Google Maven metadata、AndroidX/AOSP 源码抓取为准。

## 审查结果

### 19.13 androidx.tracing（Tracing SDK）
- 源码引用准确性：2.5/5
- 原理链完整性：3.0/5
- 版本差异覆盖：3.0/5
- 知识盲区：4.0/5
- 数据与案例支撑：2.5/5
- 交叉引用一致性：4.0/5
- 具体问题：
  - [P0] [源码错误/版本差异] L166-L178、L235 AndroidX tracing 开销与 API31+ 路径 — 正文仍把 `androidx.tracing` 1.2+/API 31+ 写成“内联路径/跳过 JNI/减少 JNI overhead”。复核 AndroidX tracing-android 1.3.0 源码，`Trace.beginSection(label)` 只是调用 `android.os.Trace.beginSection(label.truncatedTraceSectionLabel())`；AOSP `Trace.beginSection()` 仍经 `isTagEnabled(TRACE_TAG_APP)` 与 `nativeTraceBegin()`（@FastNative）。AndroidX 1.2 release note 的变化是 lazy string/cookie 的 `trace()` / `traceAsync()` 与 begin 失败时跳过 end，不是 API31+ begin/end 内联优化。建议：删除这条版本断言，改为 AndroidX 包装层、平台 Trace、native atrace enabled/disabled path 分层说明。
  - [P1] [数据缺失/数据准确性] L162-L178、L191 tracing 开销表与帧预算结论 — 表格继续给出 200-500ns、500-1000ns、<50ns、每帧 <0.5% 等精确数据，但没有可复现 benchmark、commit、采样次数、trace config、是否启用 atrace app tag、字符串长度分布。且这些数字建立在“API31+ 跳过 JNI”的错误源码模型上。建议：删除精确 ns 表，或补完整 `androidx.benchmark` 复测条件并标为项目实测。
  - [P1] [原理断裂/代码示例错误] L305-L325 协程 async trace 修正示例 — 修正示例仍在 `viewModelScope.launch` 中用同步 `trace("Home#fetchData") { repository.fetchData() }` 包住 `fetchData()`。如果 `fetchData()` 是 suspend 或内部 `withContext(Dispatchers.IO)`，同步 slice 仍会跨挂起点；如果它是阻塞 I/O，则示例没有 `withContext(Dispatchers.IO)`，所谓 “I/O 线程” 不成立。建议：把线程内同步 slice 放进明确 dispatcher；suspend 跨度用 async trace / `traceAsync` / 2.0 alpha `traceCoroutine`。
  - [P1] [原理断裂/异常路径] L342-L361 executor + mainHandler async trace 示例 — 成功路径把 `endAsyncSection()` 放在 `mainHandler.post { ... }` 末尾，posted runnable 内 `submitList()` 抛异常、`post()` 返回 false、或 `execute()` 提交失败都会遗留 async span。建议：`execute()` 提交失败纳入 try/catch；posted runnable 内用 `try/finally` 关闭 async span，并处理 `post()` 失败。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：3 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.2/5

## 闭环动作
- 写入 queue.json：1 个 P95 pending 条目（19.13 tracing 源码路径/协程 async 示例/开销数据）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 更新章节 frontmatter：task9_state=reviewed，task9_result=needs-rework，task2b_state=pending，pipeline_stage=task2b_pending
- 自动晋升 finalized：0 个（本轮存在 P0/P1）

## 复核依据
- Google Maven metadata：`androidx.tracing:tracing` latest/release 为 `2.0.0-alpha07`；`tracing-perfetto` / `tracing-perfetto-binary` latest/release 为 `1.0.1`；稳定线仍为 `1.3.0`。
- AndroidX release notes：1.2.0 重点为 lazy string/cookie 的 `trace()` / `traceAsync()` 与 begin 失败时跳过 end；1.3.0 为 Trace API 转 Kotlin、`tracing-ktx` 合并；2.0.0-alpha07 为当前 alpha。
- AndroidX tracing-android 1.3.0 源码：`Trace.beginSection(label)` 调 `android.os.Trace.beginSection(label.truncatedTraceSectionLabel())`；`trace(lazyLabel)` 只在 `Trace.isEnabled()` 时构造 label；`traceAsync` 使用 begin/end async 的 try/finally。
- AOSP `frameworks/base/core/java/android/os/Trace.java`：`beginSection()` 先 `isTagEnabled(TRACE_TAG_APP)`，再 `nativeTraceBegin(TRACE_TAG_APP, sectionName)`；不是 AndroidX 1.2+/API31 的“内联跳过 JNI”路径。
