# 深度技术 Review · 2026-07-11 08:29

## Review 目标
- 章节：8.18 Binder Trace 驱动的 Activity 冷启动性能分析
- 文件：src/part2-performance/ch08-responsiveness/18-binder-trace-cold-start-analysis.md
- 状态：ready-for-review
- 模式：正常 Review（task9_state: pending）
- external-review：未发现近 3 天活跃文件

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：4
- 具体问题：
  1. [P0][AUTO-FIX] §一 / §4.2 — `getApplicationInfo()` 误归到 `IActivityManager` / `ActivityManager`。Android 17 `IActivityManager.aidl` 无该方法；`IPackageManager.aidl` 定义 `getApplicationInfo(String packageName, long flags, int userId)`。已改为 PackageManager / IPackageManager 归因。
  2. [P0][AUTO-FIX] §4.3 — 首窗路径写成 `IWindowSession.addToDisplay()`，但 Android 17 `ViewRootImpl.setView()` 调用 `IWindowSession.addToDisplayAsUser()`，服务端走 `Session.addToDisplayAsUser()` → `WindowManagerService.addWindow()`。已修正。
  3. [P0][AUTO-FIX] §4.3 — `relayout()` 被写成 WMS → App 回调，并出现不存在的 `donerelayout()`。Android 17 `IWindowSession.relayout()` 是 App → WMS 同步调用，首帧完成后上报为 `finishDrawing()` oneway。已修正。
  4. [P0][AUTO-FIX] §2.4 — `PerfettoTrace.beginSection()` 不是本节已验证的 AndroidX tracing API。已改为同一小节已导入的 `androidx.tracing.Trace.beginSection()` / `endSection()`。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][AUTO-FIX] §5.5 — `BR_FROZEN_REPLY` 归因成客户端或 system_server worker 被冻。android17-6.18 `binder_proc_transaction()` 明确判定对象是目标进程/目标线程；同步事务打到 frozen 目标时返回 `BR_FROZEN_REPLY`。已改为 target-process 语义。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：Android 17 / API 37 边界整体明确，关键 Perfetto 与 Binder kernel 结论未引用 Android 18+。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：本轮未发现需要写入 research-gaps.md 的高优盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：经验数值均带有条件边界；本轮未新增 P2 数据建议。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：相关章节引用未发现阻断性错误。

## 统计
- P0 事实错误：5 处（均已 AUTO-FIX）
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：5 处
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`

## 证据来源
- AOSP android-17.0.0_r1 `core/java/android/app/IActivityManager.aidl`
- AOSP android-17.0.0_r1 `core/java/android/content/pm/IPackageManager.aidl`
- AOSP android-17.0.0_r1 `core/java/android/view/IWindowSession.aidl`
- AOSP android-17.0.0_r1 `core/java/android/view/ViewRootImpl.java`
- AOSP android-17.0.0_r1 `services/core/java/com/android/server/wm/WindowManagerService.java`
- kernel/common android17-6.18 `drivers/android/binder.c`
