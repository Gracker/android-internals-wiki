# 深度技术 Review · 2026-05-06 20:37

## Review 目标
- 章节：11.1 Android 功耗模型
- 文件：src/part2-performance/ch11-power/01-power-model.md
- 状态：ready-for-review / task9_state: pending → reviewed / pipeline_stage: task2b_pending

## external-review 输入
- `logs/external-review/` 根目录本轮无活跃文件，只有 `archive/`。
- 本轮是 Task6 今日复审后触发的技术风险复核；11.1 虽不足默认 3 节，但当前无足够未审候选，17.2 当天已被 Task9 审过，故按实际候选处理 1 节。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0][源码错误/API 入口错误] PowerMonitor API 段仍把入口写成 `PowerManager`，并写成订阅式更新；AOSP android-15/16 入口为 `android.os.health.SystemHealthManager.getSupportedPowerMonitors()` / `getPowerMonitorReadings()`，`PowerMonitorReadings` 返回 since boot 累积能量 snapshot。
  2. [P0][源码错误] GPU 段写 `gpu.active` 是 power_profile 标准键；android-16.0.0_r1 `power_profile.xml`、`PowerProfile.java`、`BatteryConsumer` 标准组件、`EnergyConsumerType` 均不支持标准 GPU power component。

### 维度 2：原理链完整性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1][原理断裂] PAS/ADPF 与 ODPM 动态反馈段把 PowerStats/ODPM、Power HAL headroom、PerformanceHint、kernel sched EM/PAS 串成闭环，但未给公开 AOSP/官方调用链；本轮复核也未在 android-16.0.0_r1 中找到桥接链路。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P1][版本差异] Android 15/16 版本表仍保留“PAS 订阅 ODPM 修正 EM”“ADPF 利用 ODPM 微秒级预防性降频”的平台级断言；应降级为待验证/OEM 扩展，或删除。

### 维度 4：知识盲区
- 评分：3/5
- 盲区数：1
- 具体问题：
  1. [P1][知识盲区] 缺少 PAS/ADPF/CPU-GPU headroom 与 PowerStats/ODPM 公开边界说明；已写入 research-gaps.md。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2][数据缺失] Perfetto power data source 配置示例仍标待验证；参考链接 `https://perfetto.dev/docs/data-sources/power` 复核为 404。

### 维度 6：交叉引用一致性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1][交叉引用一致性] 与 §5.2 / §5.9 的关系把待验证机制写成确定性版本演进，需与相关章节统一边界。

## 关键证据
- AOSP android-15.0.0_r1 / android-16.0.0_r1：`core/java/android/os/health/SystemHealthManager.java` 中 `getSupportedPowerMonitors()`、`getPowerMonitorReadings()`；`core/api/current.txt` 中 `PowerMonitor` / `PowerMonitorReadings` / 两个 SystemHealthManager 方法均为 `@FlaggedApi("com.android.server.power.optimization.power_monitor_api")`。
- AOSP android-16.0.0_r1：`PowerMonitorReadings.getConsumedEnergy()` 注释为 since boot、单位 microwatt-seconds；无订阅语义。
- AOSP android-16.0.0_r1：`hardware/interfaces/power/stats/aidl/.../IPowerStats.aidl` 只提供 `getEnergyConsumed()` / `readEnergyMeter()` 等读取接口；`hardware/interfaces/power/aidl/.../IPower.aidl` 的 `getCpuHeadroom()` / `getGpuHeadroom()` 注释为 based on past history。
- AOSP android-16.0.0_r1：`HintManagerService` 调用 `mPowerHal.getCpuHeadroom()` / `getGpuHeadroom()`；`PerformanceHintManager` 公开 target/actual work duration 与 hint session；未见 PowerStats/ODPM 到 ADPF/PAS 的公开桥接。
- AOSP android-16.0.0_r1：`PowerProfile.java`、`power_profile.xml`、`BatteryConsumer.java`、`EnergyConsumerType.aidl` 未定义标准 GPU power component / `gpu.active`。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5

## 闭环动作
- 写入 queue.json（P95）：合并 1 条 11.1 pending entry，更新 3 个 P0/P1 review_issues
- 写入 research-gaps.md：1 处
- 写入 suggestions.md：1 处
- 仅日志记录：0 处
- 更新 frontmatter：11.1 → `task9_state: reviewed`、`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
- 更新 progress.json：11.1 section 状态同步
- 自动晋升 finalized：无（本轮有 P0/P1，且 task6_result=needs-rework）

## 下一 Review 候选
- 当前本轮有效候选不足；17.2 今日已审，不应重审。下一轮需重新扫描 `task9_state: pending` 且未在 7 天内审过的章节。
