# Task 9 闲时抽检 · 2026-07-09 18:30

## Review 目标
- 章节：5.2 EAS 能量感知调度
- 文件：src/part1-fundamentals/ch05-cpu-power/02-eas.md
- 状态：finalized → auto-fixed，回到 Task6 复审
- 模式：闲时抽检
- 范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5（auto-fix 后）
- 问题数：2
- 具体问题：
  1. [P0][AUTO-FIX] `find_energy_efficient_cpu()` 决策流程第四步 — 原文写成“能耗差异在阈值范围内就倾向选择性能更好的 CPU”。复核 Linux v6.6/v6.12 与 Android common kernel `android17-6.18` 的 `kernel/sched/fair.c` 后，未发现这个阈值逻辑；实际比较是候选 CPU fit 程度以及相对 `prev_cpu` 的 `compute_energy()` 增量，二者都 fit 且能耗不更低时通常保持 `prev_cpu`。
  2. [P0][AUTO-FIX] `Android 中的 uclamp 使用` — 原文把 Android 17 `setProcessGroup()` JNI 路径概括为 `SetProcessProfiles()`。复核 `frameworks/base/core/jni/android_util_Process.cpp` @ `android-17.0.0_r1` 后，线程分组路径是 `SetTaskProfiles()`，进程分组路径是 `SetProcessProfilesCached()`；冻结 / 解冻等 profile 路径才直接调用 `SetProcessProfiles()`。

### 维度 3：版本差异覆盖
- 评分：4/5（auto-fix 后）
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] Android 17 覆盖不足 — 原文版本表只到 GKI 6.12 / Android 16。复核 Android common kernel `android17-6.18` 后，`find_energy_efficient_cpu()` 已增加 `sync` 参数，并在同步唤醒、当前 CPU 只有当前任务运行、cpumask 允许且 `task_fits_cpu()` 通过时直接返回当前 CPU；同时 UClamp 仍是 max/bucket 聚合。正文已补齐 Android 17 差异与引用边界。

## AUTO-FIX 摘要
- 更新正文：修正 EAS “阈值选择性能核”的错误表述，改为源码中的 fit + energy delta 比较。
- 更新正文：把 Android 17 JNI 调用链修正为 `SetTaskProfiles()` / `SetProcessProfilesCached()`，并保留 `SetProcessProfiles()` 的实际使用边界。
- 更新正文：补充 `android17-6.18` 的 overutilized 调用点、同步唤醒 fast path 与 UClamp max/bucket 边界。
- 更新参考资料：AOSP 平台源码锚定 `android-17.0.0_r1`，kernel 锚定 Android common kernel `android17-6.18`。
- 更新 frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_audit: 2026-07-09`。

## 证据来源
- AOSP platform/system/core `libprocessgroup/profiles/task_profiles.json` @ `android-17.0.0_r1`。
- AOSP platform/frameworks/base `core/jni/android_util_Process.cpp` @ `android-17.0.0_r1`。
- Android common kernel `kernel/sched/fair.c` @ `android17-6.18`（当前 refs/heads/android17-6.18: c7dfe9e17c62fb4e7c4fcee7005e726d4a27a772）。
- Android common kernel `kernel/sched/core.c` @ `android17-6.18`。
- Linux mainline `kernel/sched/fair.c` v6.6 / v6.12，用于历史差异对照。

## 统计
- P0 事实错误：2 处（均已 auto-fix）
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- queue.json：0 条
- research-gaps.md：0 条
- suggestions.md：0 条

## 闭环动作
- 未写入 queue.json：本轮问题均满足高置信、小范围 auto-fix 条件。
- 未消费 external-review：最近 3 天未发现与 5.2 / EAS 匹配的活跃 external-review 文件。
- 章节回到 Task6：等待文风/格式复审。
