# 深度技术 Review · 2026-06-16 19:29

## Review 目标
- 章节：14.4 dumpsys 系列命令
- 文件：src/part3-tools/ch14-other-tools/04-dumpsys.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码/命令引用 + 维度 3 版本差异）

## 审查结果（Idle Audit - 维度 1 & 3）

### 维度 1：源码引用准确性
- 评分：5/5
- 问题数：0
- 具体问题：未发现 P0/P1。章节中的 `dumpsys`、`meminfo`、`cpuinfo`、`gfxinfo`、WindowManager 焦点、SurfaceFlinger Frontend / HWC / latency 相关源码锚点与 AOSP android-16.0.0_r1 对齐。

### 维度 3：版本差异覆盖
- 评分：5/5
- 问题数：0
- 具体问题：未发现 P0/P1。`android-17.0.0_r1` 在本轮检查的 `frameworks/native`、`frameworks/base` 远端 tag 中不存在；未使用 main/master 结论。章节继续保持 `last_verified_against: AOSP android-16.0.0_r1`，不升级到 Android 17。

### 维度 2、4、5、6
- 未检查：闲时抽检仅覆盖维度 1 与维度 3。

## 证据
- `frameworks/native/cmds/dumpsys/dumpsys.cpp`：`Dumpsys::listServices()` 调用 ServiceManager `listServices()`；`Dumpsys::startDumpThread()` 中调用目标 service 的 `dump()`。
- `frameworks/base/services/core/java/com/android/server/am/ActivityManagerService.java`：`dumpsys activity exit-info` 分发到 `mAppExitInfoTracker.dumpHistoryProcessExitInfo()`。
- `frameworks/base/services/core/java/com/android/server/am/ProcessList.java`：Android 16 仍定义 `FOREGROUND_APP_ADJ=0`、`VISIBLE_APP_ADJ=100`、`PERCEPTIBLE_APP_ADJ=200`。
- `frameworks/base/services/core/java/com/android/server/am/ActivityManagerService.java`：`meminfo dump options` 包含 `-a/-d/-c/-s/--oom/--local/--package/--checkin/--proto/--logstats/--unreachable`，未包含 `--slab`。
- `frameworks/base/core/java/com/android/internal/os/ProcessCpuTracker.java`：进程行拆 user/system，`TOTAL` 行包含 iowait/irq/softirq。
- `frameworks/base/libs/hwui/FrameInfo.h`：Android 12-15 已有 `FrameDeadline` / `FrameInterval`；Android 16 新增 `WorkloadTarget`。
- `frameworks/base/libs/hwui/JankTracker.cpp` 与 `libs/hwui/service/GraphicsStatsService.cpp`：deadline missed 统计和 `Number Frame deadline missed` 输出仍存在。
- `frameworks/base/services/core/java/com/android/server/wm/DisplayContent.java`：`mCurrentFocus` 与 `mFocusedApp` 是 DisplayContent 维度状态，`dump()` 中输出。
- `frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp`：公开 dumper 参数包含 `--frontend`、`--hwclayers`、`--latency`；`dumpFrontEnd()` 输出 `Composition list` / `Input list`。
- `frameworks/native/services/surfaceflinger/FrontEnd/LayerHierarchy.h`：`LayerHierarchyBuilder` 存在于该文件；`RequestedLayerState.h` 的 `Changes` 位掩码到 `BufferUsageFlags = 1u << 20`，`kMustComposite` 包含 `Buffer`。
- `frameworks/native/services/surfaceflinger/FrontEnd/LayerSnapshotBuilder.h`：`tryFastUpdate(const Args& args)` 仍是快速路径入口。
- Android 官方 Battery Historian 文档仍标注 Battery Historian 不再积极维护，并建议优先考虑 system tracing、Macrobenchmark power metric 或 Power Profiler；章节中“不再积极维护”的边界成立。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：5/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：更新 `last_task9_audit: 2026-06-16`、`last_task9_audit_log: logs/deep-review/2026-06-16-19-audit.md`
