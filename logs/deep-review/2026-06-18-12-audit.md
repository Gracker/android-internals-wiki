# Task 9 闲时抽检 · 2026-06-18 12:34

## 抽检目标
- 章节：7.10 图片加载与 Bitmap 性能优化
- 文件：src/part2-performance/ch07-smoothness/10-image-bitmap-performance.md
- 当前状态：finalized / ready-to-publish
- 触发原因：本轮 `CANDIDATES=0`、`TASK2B_BACKLOG=0`、`IDLE_AUDIT=306`，进入闲时抽检模式；该章 `last_task9_at=2026-05-06T12:42:00+08:00`，超过 21 天，历史 Task9 曾有 P2 建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- 本轮只做高风险快速复核，不做完整 6 维深审。

## 核对来源
- AOSP `android-17.0.0_r1` 已存在：`platform/frameworks/base`、`platform/libcore`、`platform/frameworks/native` tags 均可解析。
- AOSP `platform/frameworks/base` android-17.0.0_r1 `graphics/java/android/graphics/Bitmap.java`：`NativeAllocationRegistry` 登记 native allocation；`Config.HARDWARE` 像素访问抛 `IllegalStateException`；`copy()` / `sameAs()` 等慢路径保留 `StrictMode.noteSlowCall()`；`hasGainmap()` / `getGainmap()` / `setGainmap()` 存在；`prepareToDraw()` 仍调用 `nativePrepareToDraw()`。
- AOSP `platform/frameworks/base` android-17.0.0_r1 `libs/hwui/jni/BitmapFactory.cpp`：`inSampleSize` 进入 `SkAndroidCodec::getSampledDimensions()`；fine scale 分支仍存在；`javaBitmap != nullptr` 时执行 `bitmap::reinitBitmap()` 后返回同一个 `javaBitmap`；Gainmap 解码仍经 `getGainmapAndroidCodec()` / `decodeGainmap()`。
- AOSP `platform/frameworks/base` android-17.0.0_r1 `libs/hwui/pipeline/skia/SkiaGpuPipeline.cpp`：`prepareToDraw()` 仅在 `!bitmap->isHardware()` 时执行 `PinAsTexture()` / `UnpinTexture()` / `flushAndSubmit()`。
- AOSP `platform/frameworks/base` android-17.0.0_r1 `graphics/java/android/graphics/ImageDecoder.java`：`setTargetSize()`、`setTargetSampleSize()`、`ALLOCATOR_HARDWARE`、`setAllocator()` 仍要求在 `OnHeaderDecodedListener` 路径配置。

## 审计结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- P0/P1：0
- 结论：本轮未发现阻塞级源码错误。
- 已复核：`BitmapFactory.cpp` 的 `inBitmap` 返回对象语义与正文一致；sample size / fine scale 路径与正文一致；`SkiaGpuPipeline::prepareToDraw()` 的 hardware bitmap no-op 分支与正文一致；`Bitmap.java` 的 Hardware Bitmap 像素访问异常、slow call、Gainmap API 与正文一致。
- 仅记录：正文仍保留 Gainmap 显存倍率、AVIF fallback 实现名、Perfetto 调用栈采集条件等 `[待验证]` / 历史 P2 方向。本轮未发现会影响正文结论的 P0/P1。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- P0/P1：0
- 结论：版本口径可保留到 Android 17 / API 37，未发现 Android 18 / API 38+ 内容。
- 已复核：`Bitmap.Config.HARDWARE` API 26、`ImageDecoder` API 28、AVIF Android 12 引入、Ultra HDR / Gainmap Android 14+、Bitmap 像素内存三段式边界与正文方向一致；android-17.0.0_r1 关键源码锚点未推翻现有描述。
- 仅记录：后续若处理 P2，可继续把 “Android 14 对部分新设备要求支持 AV1 硬件解码” 拆细为平台支持、CDD handheld 要求和设备 SoC 能力三个口径，但这不是 P0/P1。

## 闭环动作
- queue.json：0
- research-gaps.md：0
- suggestions.md：0
- frontmatter：标注 `last_task9_audit: 2026-06-18`、`last_task9_audit_log: logs/deep-review/2026-06-18-12-audit.md`
- 日志：logs/deep-review/2026-06-18-12-audit.md

## 统计
- P0 事实错误：0
- P1 重要缺失：0
- P2 建议改进：0（未新增，历史 P2 保持）
- 抽检结论：pass-idle-audit
