# AIW Deep Review Log · 2026-07-31

- lane: deep-review
- run_id: `20260731-083556-deep-review-977cb49d`
- target: `src/part5-app/ch25-power-size/29-dex-size-optimization.md`
- baseline: Android 17 / API 37 / `android-17.0.0_r1`
- mode: patch applied (outline-body 一致性修复 + frontmatter 状态推进)

## 审计结论

本章（25.29 DEX 体积优化实战）正文成熟度高（651 行），结构完整：优化对象定义 → DEX 结构与体积构成 → 构建路径 → R8 → 依赖与生成代码 → D8 → Debug 信息 → Kotlin Metadata → 字符串/资源/注解 → Multidex → Baseline Profile/ART → DFM → 测量流程 → 回归排查 → Android 17 源码边界 → 常见错误 → 版本演进 → 延伸阅读。已标注 13 条一手资料来源。

本轮 deep-review 重点：源码/版本边界、来源支撑、交叉引用准确性、outline 与 body 一致性、中文可读性、frontmatter 状态。

### 源码/版本边界 ✅

- 版本锚定 `android-17.0.0_r1` / API 37，明确区分平台能力（ART reader）与构建工具输出（D8/R8/AGP），正确。
- DEX header 112 字节（DEX 040 及更早）vs 120 字节（DEX 041）与 AOSP `dex_file.h` 一致。
- `method_ids` 65,536 上限、`field_ids` 同级 16 位索引，正确。
- DEX 035—041 版本演进表与 `standard_dex_file.cc` 一致。
- `DexPathList.java` 不承诺"并行加载"性能契约，正确声明。
- 未引入 Android 18/API 38+ 主线结论 ✅。
- R8 Full Mode 自 AGP 8.0 默认，正确。
- AGP 8.3 DEX layout 默认启用、AGP 8.8 `r8.json` 可检查 Startup Profile 标记，正确。
- 构建 SDK 相关声明（AGP 9.1 class repackaging、AGP 9.3 optimization DSL）属于章节声明的"2026 年 7 月 Android Developers 文档语义"范围，在平台边界内。

### 交叉引用 ✅

5 条延伸阅读链接逐一核对：

- 25.6 → `ch25-power-size/06-apk-analysis.md` ✅
- 25.7 → `ch25-power-size/07-r8-resource-optimization.md`（title: "R8 与资源优化"）✅
- 25.8 → `ch25-power-size/08-app-bundle-delivery.md`（title: "App Bundle 与按需分发"）✅
- 21.12 → `part5-app/ch21-startup/12-startup-profile-dex-layout.md`（title: "Startup Profile 与 DEX Layout 启动优化"，finalized）✅
- 1.57 → `part1-fundamentals/ch01-architecture/57-android17-art-boot-image-memory-mapping-startup.md`（title: "ART Boot Image 内存映射与启动性能"）✅

注：21.12 位于 `part5-app/ch21-startup/` 而非 `part2-performance/ch21-startup/`；从 `ch25-power-size/` 的相对路径 `../ch21-startup/12-startup-profile-dex-layout.md` 正确解析。

### Outline 与 Body 一致性（本轮重点修复）

本章 outline（要点 + 扩展）是 task2a 生成时的原始大纲，body 是后续深度扩写。逐条比对发现 4 处 outline 条目与 body 技术口径冲突，本轮已全部修复：

1. **`LineNumberTable` 术语错误**：outline 写"调试信息（LineNumberTable、LocalVariableTable）"，body（§"DEX 里没有 JVM 的 LineNumberTable"）明确指出这些是 class 文件属性，DEX 中对应 `debug_info_item`。→ 已改为 `debug_info_item`。
2. **`-strip-debug` 描述误导**：outline 写"`-strip-debug`：移除 LineNumberTable 对崩溃堆栈的影响"暗示推荐使用，body 明确指出"无需用一个非标准的 `-strip-debug` 规则手工破坏栈信息"。→ 已改为描述现代 R8 自行处理。
3. **`@StringRes` 与 keep "冲突"措辞**：outline 写"`@stringRes` 注解与 R8 keep 的冲突"，body 明确指出类型提示注解不自动形成 keep 入口，不存在"冲突"。→ 已改为正确语义。
4. **`--print-usage` / `--print-seeds` 非标准记法**：outline 使用双连字符 `--print-usage`，ProGuard/R8 实际选项为 `-printusage` / `-printseeds`（单连字符、无中间连字符）。body 使用 AGP 产物名 `usage.txt` / `seeds.txt`。→ 已统一为标准选项名并标注对应 AGP 产物。

### 章节结构 ✅

- 逻辑递进清晰：从"确定优化对象"（代理指标 vs 用户结果）到 DEX 结构、构建路径、R8/D8 职责、依赖治理、可追溯性、Multidex、Baseline Profile 边界、DFM、测量流程和回归排查。
- "常见错误"节（11 条）与正文结论一致，无矛盾。
- 版本演进表（Android × AGP 双轴）正确说明二者不可按行一一对应。

### 中文可读性 ✅

- 专业术语使用准确（ULEB128 差分编码、MUTF-8、VDEX/OAT/App Image、dexopt 等）。
- 代码块标注语言（kotlin/proguard/bash/mermaid）完整。

### Frontmatter（本轮修正）

- `task6_state: pending` / `task9_state: pending` → `reviewed`（本章已通过 draft-polish + deep-review，pending 状态过期）。
- `pipeline_stage: task6_pending` → `deep-review`（反映当前实际阶段）。
- `last_verified: 2026-07-30` → `2026-07-31`。
- 补 `last_deep_review_at` / `last_deep_review_run_id` 跟踪字段。
- `confidence: medium-high` 维持不变（不提升至 high）：正文技术结论可靠且与 AOSP 源码一致，但本 lane materials 为空、无新增独立来源材料，不应升级。

## P0 / P1 / P2

### P0

- 无。正文无未隔离的危险错误结论，源码/版本/格式口径均正确。

### P1

- 无。本轮无正文级重要缺陷。交叉引用全部解析成功。

### P2（本轮已修复）

- **已修复**：outline `LineNumberTable` 术语与 body 冲突（§DEX 文件结构与体积构成）。
- **已修复**：outline `-strip-debug` 描述与 body 反 R8 最佳实践冲突。
- **已修复**：outline `@stringRes` 与 keep "冲突"措辞与 body 矛盾。
- **已修复**：outline `--print-usage` / `--print-seeds` 非标准选项记法。
- **已修复**：frontmatter `task6_state`/`task9_state` pending 过期、`pipeline_stage` 过期。
- **已修复**：frontmatter 补 deep-review 跟踪字段。

## 实际补丁

仅修改既有章节：`src/part5-app/ch25-power-size/29-dex-size-optimization.md`：

outline 修正（4 处）：
1. 调试信息 `LineNumberTable、LocalVariableTable` → `debug_info_item`：行号、参数名、局部变量事件
2. `-strip-debug` 描述 → 现代 R8 自行处理行号映射
3. `@stringRes` 冲突 → `@StringRes` / `@DrawableRes` 不自动形成 keep 入口
4. `--print-usage` / `--print-seeds` → `-printusage` / `-printseeds` + AGP 产物标注

frontmatter 修正：
1. `task6_state: pending` → `reviewed`
2. `task9_state: pending` → `reviewed`
3. `pipeline_stage: task6_pending` → `deep-review`
4. `last_verified: 2026-07-30` → `2026-07-31`
5. 新增 `last_deep_review_at` / `last_deep_review_run_id`

- 未新增 `src/**/*.md` 章节。
- 未修改 `src/SUMMARY.md`。
- 未引入 Android 18/API38+ 结论。
- 未做无来源正文扩写。

## 证据来源

- 目标章节正文逐节核对：DEX header/ID 表/data item 结构、method_ids 65,536 限制、DEX 035—041 版本演进、R8 Full Mode、AGP 版本表。
- 交叉引用目标文件 frontmatter title 实读核对（5 个链接全部存在且解析正确）。
- AOSP `standard_dex_file.cc` / `dex_file.h` / `DexPathList.java` @ `android-17.0.0_r1`（URL 在正文中提供）。

## 校验结果

- `python3 scripts/check-metadata.py --files src/part5-app/ch25-power-size/29-dex-size-optimization.md`：✅ 通过（1 文件，0 错误，1 个 pre-existing sources 可选字段警告）
- `python3 scripts/check-summary-links.py`：✅ 通过（164 链接，2 个 pre-existing 重复警告，均与本章无关）
- `git diff --check`：✅ 无空白错误
