# 深度技术 Review · 2026-05-08 04:31

## Review 目标
- 章节：1.17 IPC 全景：Android 进程间通信机制对比与性能选型
- 文件：src/part1-fundamentals/ch01-architecture/17-ipc-panorama.md
- 状态：ready-for-review → finalized / ready-to-publish
- 章节：4.7 16KB Page Size 与 Android 性能
- 文件：src/part1-fundamentals/ch04-memory/07-16kb-page-size.md
- 状态：ready-for-review → finalized / ready-to-publish
- 章节：10.2 内存泄漏
- 文件：src/part2-performance/ch10-memory-perf/02-memory-leak.md
- 状态：ready-for-review → finalized / ready-to-publish

## 选择说明
- 本轮审计当前 3 个 `task9_state: pending` 且 Task6 已通过的章节：1.17、4.7、10.2。三节均为 Task2B 修复后复审，queue.json 中无 pending 条目。
- `logs/external-review/` 根目录无活跃 external-review 文件，本轮未消费外部 review。
- 本轮无 P0/P1；P2 仅写入/保留 suggestions.md，不阻塞自动晋升。

## 审查结果

### 1.17 IPC 全景：Android 进程间通信机制对比与性能选型

#### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0 个 P0/P1；P2 见具体问题

#### 维度 2：原理链完整性
- 评分：4.5/5

#### 维度 3：版本差异覆盖
- 评分：4.0/5

#### 维度 4：知识盲区
- 评分：4.5/5

#### 维度 5：数据与案例支撑
- 评分：4.0/5

#### 维度 6：交叉引用一致性
- 评分：4.5/5

#### 具体问题
1. [P2] L144/L360-L370 AF_VSOCK / RpcBinder 数据量与拷贝次数 — 表格把 AF_VSOCK / RpcBinder 的数据量写成“与 Binder 同量级”、拷贝次数写成 1，容易让读者误以为 Binder driver 的进程级 transaction buffer 约束也适用于 RpcBinder over vsock。AVF 文档把 host↔VM 主通道描述为 virtio-socket/vsock，数据上限与性能边界应按 vsock/libbinder_rpc framing、socket buffer 和协议实现单独说明。 建议：把该行改成“无 Binder transaction buffer 约束，受 vsock/socket buffer 与 RpcBinder framing 影响；大 payload 仍应另走共享内存/文件/流式协议”，拷贝次数改为“取决于 vsock/virtio 路径，需实测”。
2. [P2] L274-L291 Parcel::writeBlob 零拷贝表述 — 当前源码链已修正为 16KB 阈值与 ashmem-compatible fd，但标题和说明直接写“实现零拷贝”。AOSP `Parcel::writeBlob()` 对大 blob 是把 fd 写入 Binder transaction，并让调用方写入映射区域；它避免 payload 经过 Binder buffer，不等于所有场景端到端零拷贝。 建议：改成“对 Binder 事务而言只传 fd，不把大 payload 塞进 Parcel buffer；调用方仍可能需要把已有数据写入/拷入共享区域”。

### 4.7 16KB Page Size 与 Android 性能

#### 维度 1：源码引用准确性
- 评分：4.0/5
- 问题数：0 个 P0/P1；P2 见具体问题

#### 维度 2：原理链完整性
- 评分：4.0/5

#### 维度 3：版本差异覆盖
- 评分：4.0/5

#### 维度 4：知识盲区
- 评分：4.0/5

#### 维度 5：数据与案例支撑
- 评分：4.0/5

#### 维度 6：交叉引用一致性
- 评分：4.5/5

#### 具体问题
1. [P2] L126-L139 内部碎片的 5KB 对象例子 — 正文用“App 分配 5KB 对象，16KB 页浪费 11KB”解释内部碎片，容易把 Java/Native 小对象说成直接占一个内核页。ART、malloc/scudo 会在 page/span 内做子分配，小对象通常不会一对象一页；页大小带来的浪费主要体现在页粒度映射、allocator span/class、mmap 大小与 RSS 结算上。 建议：把例子限定为“单独 mmap 或页级分配区域”，并补一句：普通 Java/Native 小对象会被运行时/allocator 打包到页内，内存增长要用 RSS/PSS 和 allocator class 实测。
2. [P2] L263-L271 NDK r27 mprotect alignment bug 口径 — 正文写“NDK r27 链接器生成的 ELF 文件 p_align=4096”，口径过宽。官方迁移文档说明 NDK r27 及更低版本可通过 `-z,max-page-size=16384` 与 `-z,common-page-size=16384` 生成 16KB-aligned 产物；如果引用 android/ndk#2026，应限定为未正确设置 flags 或特定 r27 linker/RELRO 场景。 建议：改成“NDK r27 及以下默认/未加完整 flags 的产物可能仍是 4KB alignment；android/ndk#2026 对应的 mprotect 失败需按 issue 的复现条件描述”。
3. [P2] L224-L229 控制接口总览（既有 P2，未重复追加） — `pm.16kb.app_compat.disabled` 仍被归类为“安装时预检”。此前 suggestions.md 已记录：该项应按设备级 backcompat 控制属性/强制关闭 compat 来写，并补 Android 17 `fatal` 模式边界。 建议：沿用既有 suggestions.md 条目修正，不重复新增。

### 10.2 内存泄漏

#### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0 个 P0/P1；P2 见具体问题

#### 维度 2：原理链完整性
- 评分：4.0/5

#### 维度 3：版本差异覆盖
- 评分：4.0/5

#### 维度 4：知识盲区
- 评分：4.5/5

#### 维度 5：数据与案例支撑
- 评分：4.0/5

#### 维度 6：交叉引用一致性
- 评分：4.5/5

#### 具体问题
1. [P2] L121-L124 LeakCanary retained 与 leak confirmation — 流程把“GC 后 WeakReference 没进 ReferenceQueue”直接写成“发生了泄漏”。LeakCanary 更精确的状态是对象被判定 retained；最终 leak 结论依赖 heap dump 后从 GC Root 到 watched object 的引用链分析。`Runtime.getRuntime().gc()` 也是触发请求而非规范保证。 建议：把第三步改成“标记为 retained / suspected leak”，第四步再写“Heap Dump 中找到有效 GC Root 引用链后确认泄漏”。
2. [P2] L233-L236 libmemunreachable 零开销表述 — `libmemunreachable` 被写成“零开销的泄漏检测”。它没有常驻插桩开销，但触发扫描时仍有暂停、遍历和误报/漏报成本；更适合作为 on-demand native leak sweep，而不是生产环境持续零成本检测。 建议：改成“无持续插桩开销；触发时有扫描成本，结果是不精确的 suspected unreachable blocks”。
3. [P2] L301 heapprofd Java heap allocation 版本边界 — 正文写 heapprofd 可通过 `heaps: "com.android.art"` 采 Java heap allocations，但版本边界“待核”。Perfetto 文档已经标注 Java allocation profiling available on Android 12 or higher，并说明它是 allocation samples，不是 heap dump/retention graph。 建议：补 Android 12+ 边界，并明确它只能看到对象创建调用栈样本，不能证明对象仍被引用；泄漏确认仍要 Heap Dump/LeakCanary/Shark。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：8 处（其中 1 处为既有 suggestions.md 条目，本轮未重复追加）
- P3 锦上添花：0 处
- 总体技术评分：4.2/5

## 闭环动作
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：7 处；保留既有 P2：1 处
- 自动晋升 finalized：1.17、4.7、10.2
- Review 日志：logs/deep-review/2026-05-08-04-deep-review.md
