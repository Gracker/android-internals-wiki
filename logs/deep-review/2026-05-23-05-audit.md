# 深度技术 Review 抽检 · 2026-05-23 05:20

## 抽检目标
- 章节：3.3 手势导航与系统交互
- 文件：src/part1-fundamentals/ch03-input/03-gesture-navigation.md
- 状态：finalized
- 模式：闲时抽检（无 pending 候选；聚焦维度 1 源码引用 + 维度 3 版本差异）
- 选择原因：last_task9_at=2026-04-30T16:20:00+08:00，超过 21 天；此前 Task9 通过但记录 P2 2 处；本文件此前未记录 last_task9_audit。

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- P0/P1：0
- 抽检证据：
  1. AOSP android-16.0.0_r1 中确认存在 `EdgeBackGestureHandler.java`、`BackPanelController.kt`、`BackPanel.kt`、`InputMonitorCompat.java`、`InputDispatcher.cpp`、`OnBackInvokedCallback.java`、`OnBackAnimationCallback.java`、`OnBackInvokedDispatcher.java`。
  2. `EdgeBackGestureHandler.triggerBack()` 与正文一致：`mBackAnimation == null` 时注入 `KEYCODE_BACK` down/up；否则走 `mBackAnimation.setTriggerBack(true)`。
  3. legacy path 中 `pilferPointers()` 只在 `mBackAnimation == null` 分支执行；`InputDispatcher.cpp` 中 `pilferPointersLocked()` 与 `CANCEL_POINTER_EVENTS` 路径存在，正文“App 收到 cancel 结尾”的描述可成立。
  4. `DisplayContent.calculateSystemGestureExclusion()`、`mSystemGestureExclusionLimit`、`ViewRootImpl.reportSystemGestureExclusionChanged()` 与 `View.setSystemGestureExclusionRects()` 均可在 android-16.0.0_r1 对上。

### 维度 3：版本差异覆盖
- 评分：5/5
- P0/P1：0
- 抽检证据：
  1. Android Developers reference 确认 `OnBackInvokedCallback` Added in API level 33，`OnBackAnimationCallback` Added in API level 34。
  2. Android Developers reference 确认 `OnBackInvokedDispatcher.PRIORITY_SYSTEM_NAVIGATION_OBSERVER` Added in API level 36；AOSP android-16.0.0_r1 源码中该常量存在并标注 `@FlaggedApi(Flags.FLAG_PREDICTIVE_BACK_PRIORITY_SYSTEM_NAVIGATION_OBSERVER)`，正文“API 36 observer-only callback”口径成立。
  3. Predictive Back animations 文档确认 opted-in app 显示 back-to-home、cross-activity、cross-task 动画；章节把 Android 15+ 动画默认展示范围与 opt-in 条件拆开，未发现把 targetSdk 写成唯一开关的问题。
  4. Gesture navigation 文档确认 Android 10 引入 `View.setSystemGestureExclusionRects()` 用于左右边缘 back gesture 的选择性排除；mandatory system gesture insets 不能按 back gesture 一样 opt out，正文边界成立。

## 问题单
- P0：0
- P1：0
- P2：0（本轮仅抽检高风险源码/版本差异；未新增建议项）
- P3：0

## 闭环动作
- 更新章节 frontmatter：`last_task9_audit: "2026-05-23"`
- 写入抽检日志：`logs/deep-review/2026-05-23-05-audit.md`
- queue.json：未写入
- research-gaps.md：未写入
- suggestions.md：未写入

## 结论
本轮未发现源码引用错误或版本差异 P0/P1。章节保持 `status: finalized` / `pipeline_stage: ready-to-publish` / `task9_result: pass-tech-review`。
