# Task 9 闲时抽检 · 2026-06-29 21:29

## 抽检目标
- 章节：21.6 延迟初始化与按需加载
- 文件：src/part5-app/ch21-startup/06-lazy-initialization.md
- 状态：finalized → ready-for-review（Task9 auto-fix 后回到 Task6 复审）
- 选择原因：CANDIDATES=0、TASK2B_BACKLOG=0，进入闲时抽检；本节 last_task9_at=2026-05-13T01:43:00+08:00，last_task9_audit=2026-06-07，超过 21 天未复核。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 版本边界：仅使用 Android 17 / API 37 及以下资料；源码锚定 AOSP android-17.0.0_r1，未采用 Android 18 / API 38+ 或 main/master 资料作为正文结论。

## 复核证据
- AOSP android-17.0.0_r1：`frameworks/base/core/java/android/os/MessageQueue.java` 已不再是可用路径；`core/java/android/os/` 下存在 `LegacyMessageQueue/MessageQueue.java`、`CombinedDeliMessageQueue/MessageQueue.java`、`CombinedMessageQueue/MessageQueue.java`。
- AOSP android-17.0.0_r1：`LegacyMessageQueue/MessageQueue.java` 仍保留旧版 `mMessages`、`mIdleHandlers`、`mPendingIdleHandlers` 与 `next()` 中复制 idle handlers、调用 `queueIdle()`、返回 false 后移除的流程。
- AOSP android-17.0.0_r1：`CombinedDeliMessageQueue/MessageQueue.java` 保留 `IdleHandler` API 与返回值语义，但 `targetSdkVersion >= 37` 的 DeliQueue 路径走 `nextDeliQueue()`，使用 `mStack.hasMessages(...)` 判断可分发消息，并用 `mIdleHandlersLock` 管理 idle handlers；不能继续把 Android 17 主线描述成旧版 `synchronized (this) + mMessages` 单链表。
- Android Developers Android 17 behavior changes / lock-free MessageQueue blog：Android 17 targetSdk 37+ 应用会收到新的 lock-free `android.os.MessageQueue` 实现；旧 target 保持兼容路径。

## 抽检结果
- P0 事实错误：0
- P1 重要缺失：1（AUTO-FIX 后无待处理）
- P2 建议改进：0
- P3 记录：0

## AUTO-FIX
- [P1][版本差异/源码锚点] frontmatter 与正文 IdleHandler 小节仍以 android-15.0.0_r1 `frameworks/base/core/java/android/os/MessageQueue.java` 作为 Android 10-17 主线锚点，遗漏 Android 17 DeliQueue / LegacyMessageQueue 路径拆分。
- 修复内容：将 frontmatter `last_verified_against` 与 sources 更新到 android-17.0.0_r1 `LegacyMessageQueue/MessageQueue.java` + `CombinedDeliMessageQueue/MessageQueue.java`；补充 targetSdk 37 DeliQueue 边界；把 `next()` 流程限定为旧实现路径。
- 修改范围：src/part5-app/ch21-startup/06-lazy-initialization.md（局部版本边界与源码锚点修正）。
- Frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`task2b_state=fixed`、`last_task9_autofix_at=2026-06-29`。

## 闭环动作
- 写入 queue.json：0 处（高置信局部 auto-fix，不再写问题单）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节回到 Task6 复审：1 处
