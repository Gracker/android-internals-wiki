# 深度技术 Review 抽检 · 2026-05-17 14:20

## Review 目标
- 模式：闲时抽检（CANDIDATES=0，IDLE_AUDIT=192）
- 章节：2.5 MainThread 与 RenderThread 协作
- 文件：src/part1-fundamentals/ch02-rendering/05-main-render-thread.md
- 状态：finalized → needs-rework / task2b_pending
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）
- external-review：近 3 天无活跃文件，本轮未消费 external-review

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：2
- 具体问题：
  1. [P1] L351-L355、L404 — `syncFrameState` 长耗时被直接归因为“上一帧 GPU 未完成”。AOSP `DrawFrameTask::run()` 在 `syncFrameState(info)` 后会先 `unblockUiThread()`，之后才进入 `CanvasContext::draw()`；该 slice 不能单独判定 GPU 完成状态。
  2. [P1] L185-L191、L522 — DeliQueue 被写成替代 RenderThread 内部队列。官方 Android 17 DeliQueue 是 `android.os.MessageQueue` 行为变化；AOSP HWUI `WorkQueue.h` 在 main/android-16 仍是 `std::mutex` 保护的 WorkQueue。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L225-L229、L515-L521 — RenderThread ADPF hint session 被写成 Android 16+。AOSP android-14/15/16 `CanvasContext.cpp` 均已有 `HintSessionWrapper` 与 `reportActualWorkDuration()`；Android 16 headroom API 需要单独拆开。

## 证据锚点
- AOSP android-16.0.0_r1 `libs/hwui/renderthread/DrawFrameTask.cpp`：`postAndWait()` 向 `mRenderThread->queue().post()`，`run()` 中 `syncFrameState(info)` 后先 `unblockUiThread()`，随后才 `context->draw()`。
- AOSP main / android-16.0.0_r1 `libs/hwui/thread/WorkQueue.h`：`WorkQueue` 仍包含 `std::mutex& mLock` 与 `std::vector<WorkItem> mWorkQueue`。
- Android Developers Blog《Under the hood: Android 17’s lock-free MessageQueue》：DeliQueue 是 targetSdk 37+ app 的 `MessageQueue` 新实现。
- AOSP android-14.0.0_r1 / android-15.0.0_r1 / android-16.0.0_r1 `libs/hwui/renderthread/CanvasContext.cpp`：均存在 `HintSessionWrapper`、`updateTargetWorkDuration()`、`reportActualWorkDuration()`。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：3 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3/5

## 闭环动作
- 写入 queue.json（P85）：1 条（合并 3 个 P1 问题）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 更新 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-05-17`
- 自动晋升 finalized：0 章（抽检发现 P1）
