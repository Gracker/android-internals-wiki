# 深度技术 Review 闲时抽检 · 2026-06-23 05:31

## Audit 目标
- 章节：13.11 Perfetto 时间跨度关联：SPAN_JOIN 与窗口函数
- 文件：src/part3-tools/ch13-perfetto/11-perfetto-span-join-window-functions.md
- 状态：finalized
- 模式：闲时抽检
- 范围：维度 1（源码引用）+ 维度 3（版本差异）
- 选择原因：该章 `status=finalized`、`task9_result=pass-tech-review`，此前没有 `last_task9_audit`；历史 Task9 曾修过 frame 边界裁剪和 GC pause window 合并问题，属于 SQL 语义容易回退的章节。

## 审查依据
- Android 17 / API 37 边界：本轮只使用 `android-17.0.0_r1` 与本地 Perfetto v49 对照资料，未采用 Android 18 / API 38+ 结论。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/intrinsics/operators/span_join_operator.cc` 存在；`Column()` 按 `max(t1.ts, t2.ts)` 与 `min(raw_ts_end)` 计算输出 `ts/dur`，`Cursor::FindOverlappingSpan()` / `FindEarliestFinishQuery()` 负责按分区与时间推进。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/prelude/after_eof/cpu_scheduling.sql` 中 `sched` 暴露 `cpu` 与 `ucpu`，其中 `cpu` 是 `ucpu AS cpu` 的单机 trace 便利列；多 machine trace 需要按 `ucpu` 关联 `cpu` 表。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/sched/thread_executing_span_with_slice.sql` 包含 `_slice_flattened`、`thread_state` 与 `SPAN_LEFT_JOIN` 的组合，正文对 flattened slice + thread_state 的说明成立。
- AOSP `android-17.0.0_r1`：`external/perfetto/src/trace_processor/perfetto_sql/stdlib/linux/cpu/frequency.sql` 通过 `counter_leading_intervals`、`cpu_counter_track` 与 `cpu` 表生成 `cpu_frequency_counters`，正文的 counter → span 思路与版本内标准库方向一致。
- Perfetto docs / 本地 v49：`SPAN_JOIN` 的整数分区、同分区 span 不得重叠、`SPAN_LEFT_JOIN` / `SPAN_OUTER_JOIN` 语义与正文一致。

## 审查结果

### 维度 1：源码引用准确性
- 评分：5/5
- P0/P1：0
- 说明：frontmatter 中列出的 `span_join_operator.cc`、`span_join_operator.h`、`thread_executing_span_with_slice.sql` 路径在 Android 17 tag 下均可定位；正文对 `Column()` 输出公式、整数分区错误、同分区重叠约束、标准库 flattened slice 路径的描述与源码一致。

### 维度 3：版本差异覆盖
- 评分：4/5
- P0/P1：0
- 说明：正文适用范围写到 Android 17 / API 37，未引用 Android 18/API 38+ 内容。`sched.cpu` 在 Android 17 prelude 中仍存在，但源码注释限定其只对 single-machine trace 有意义；移动端 Android trace 通常满足这个前提，本轮不判 P0/P1。

## 低风险观察
1. [P3] `sched.cpu` 与 `cpu_counter_track.cpu` 的分区写法适合单机 Android trace；若后续将查询模板扩展到 multi-machine trace，建议改用 `ucpu` 或显式关联 `cpu` 表，避免 CPU index 与 unique CPU id 混淆。本轮只记录，不写入 suggestions。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：1 处
- 总体技术评分：4.5/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter：`last_task9_audit=2026-06-23`、`last_task9_audit_result=pass-idle-audit`
- 章节正文：未改动
