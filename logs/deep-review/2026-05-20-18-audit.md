# 深度技术 Review 抽检 · 2026-05-20 18:20

## Review 目标
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- 章节：13.8 Perfetto 输入延迟 SQL 深度分析
- 文件：src/part3-tools/ch13-perfetto/08-input-latency-sql.md
- 状态：finalized → task2b_pending（发现 P0，写入 queue）
- 外部输入：logs/external-review/ 根目录无 13.8 最近 3 天活跃文件

## 抽检证据
- Perfetto 当前 `android/input.sql`：`android_input_events` 的 latency 字段、`android_motion_events`、`android_key_events`、`android_input_event_dispatch` 与正文主体表述基本一致。
- AOSP `frameworks/native/services/inputflinger/dispatcher/InputDispatcher.cpp`（main，2026-05-20 复核）：`traceOutboundQueueLength()` 统计 `connection.outboundQueue`，`traceWaitQueueLength()` 统计 `connection.waitQueue`；`startDispatchCycleLocked()` publish 成功后把 entry 从 outboundQueue 移入 waitQueue；`doDispatchCycleFinishedCommand()` 从 waitQueue 按 seq 移除已 finish 的事件；`isConnectionResponsive()` 遍历 waitQueue 判断超时。

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] §InputDispatcher 延迟分解 → 输入队列长度追踪 — 正文把 `oq` 解释为“已发送等待 ACK”，把 `wq` 解释为“等待窗口获取焦点”。AOSP 源码显示 `oq` 是 connection outboundQueue（待 publish / 尚未成功发到 input channel），`wq` 是 connection waitQueue（已发给 App，等待 App finish/ACK，并参与 dispatch timeout/ANR 响应性判断）。当前表述会把 App 处理反压误判成窗口焦点等待。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：本轮只复核当前 AOSP main 与 Perfetto main；未发现新的 Android 17/Perfetto stdlib 版本差异阻断项。`android.input.inputevent` debuggable build 边界仍与 proto 注释一致。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（task9-audit-13.8-inputdispatcher-oq-wq-semantics）
- 更新 frontmatter：task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-20
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
