# 深度技术 Review 闲时抽检 · 2026-05-17 22:27

## Review 目标
- 模式：闲时抽检（无 task9_state: pending 候选；优先抽检 finalized/pass-tech-review 且未记录 last_task9_audit 的章节）
- 章节：1.1 Android 分层架构
- 文件：src/part1-fundamentals/ch01-architecture/01-layered-architecture.md
- 状态：finalized → ready-for-review（P0 命中后进入 task2b_pending）
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L260 — 正文称 `/dev/binder`、`/dev/vndbinder`、`/dev/hwbinder` “各自独立 Context Manager 和 AVC 实例”。抽检复核 android16-6.12 `drivers/android/binder.c` 与 `security/selinux/avc.c`：三路 binder 设备有独立 binder context/context manager；但 SELinux AVC 是单个全局 `static struct selinux_avc selinux_avc`，缓存键基于 `ssid/tsid/tclass/perm`，不是每个 binder 设备一套独立 AVC cache。该句把 Binder context 隔离误写成 AVC 实例隔离。
- 建议修正：改为“三路 binder 设备隔离提供独立 binder context/context manager，并通过 SELinux type/权限边界限制跨域访问；SELinux AVC 本身是全局访问向量缓存”。删除“各自独立 AVC 实例”和“因此降低 AVC 检查频率”的因果。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 抽检结论：Android 8+ Treble、Android 13 HIDL deprecated、Android 15/16 16KB page size、Android 15 起 VNDK-less 这些版本边界在正文中基本有交代。本轮未发现足以构成 P0/P1 的版本差异遗漏。

## 源码/资料复核锚点
- AOSP kernel/common `android16-6.12`: `security/selinux/hooks.c` — `selinux_binder_transaction()` 调用 `avc_has_perm()` 检查 `BINDER__CALL` / `BINDER__IMPERSONATE`。
- AOSP kernel/common `android16-6.12`: `security/selinux/avc.c` — 全局 `static struct selinux_avc selinux_avc`，未见按 binder 设备拆分的 AVC 实例。
- AOSP kernel/common `android16-6.12`: `drivers/android/binder.c` — binder context/context manager 属于 binder 设备/上下文隔离范畴。
- source.android.com HAL 文档：Android 13 起 HIDL deprecated，新 HAL 迁移到 AIDL；既有 HIDL 继续兼容。
- source.android.com VNDK 文档：Vendor NDK 适用于 <= AOSP 14；Android 15 起 VNDK-less 相关属性退出。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json（P95）：1 处（task9-1.1-selinux-avc-instance-error）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- 更新 frontmatter：task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-17
