# Task 9 闲时抽检 · 2026-05-21 12:24

## 抽检目标
- 章节：19.05 LeakCanary
- 文件：src/part3-tools/ch19-apm/05-leakcanary.md
- 状态：finalized
- 触发原因：无 `task9_state: pending` 候选；本章节 `last_task9_audit` 缺失，`last_task9_at=2026-04-26T23:27:38+08:00`，超过 21 天复检优先线。

## 抽检范围
- 维度 1：源码 / 官方文档引用准确性
- 维度 3：版本差异与过期风险
- 本轮不做完整 6 维深审；只复核高风险源码锚点与版本边界。

## 复核资料
- LeakCanary Fundamentals: How LeakCanary works（保留对象、5 秒等待、阈值、heap dump、Shark、Application / Library Leak）
- LeakCanary Getting Started（2.14 当前安装方式与默认观察对象）
- LeakCanary Recipes（release retained instances、referenceMatchers、自定义配置）
- LeakCanary UI Tests（`leakcanary-android-instrumentation`、`LeakAssertions.assertNoLeaks()`、`DetectLeaksAfterTestSuccess`）
- LeakCanary Changelog（2.6 `RootViewWatcher` / `ServiceWatcher`；2.7 root view 默认策略；2.9.1 移除旧 RunListener；2.14 稳定版；3.0 alpha heap-growth APIs）

## 抽检结论
- P0：0
- P1：0
- P2：1
- P3：2
- 结果：无阻塞性源码错误或关键版本断裂；不写入 `queue.json`，仅记录抽检日志并标注 `last_task9_audit: "2026-05-21"`。

## 维度 1：源码 / 官方文档引用准确性
- 评分：4/5
- 通过项：
  1. L92-L99 对 retained object 的流程描述与官方 Fundamentals 一致：生命周期对象交给 `ObjectWatcher`，弱引用等待 5 秒并触发 GC，达到可见 5 / 不可见 1 阈值后 dump heap，再由 Shark 分析。
  2. L115-L117 Application Leak / Library Leak 分类与官方文档一致。
  3. L125-L129 debug 使用 `leakcanary-android`、release 只保留 `leakcanary-object-watcher-android` 计数信号，与 Recipes 的 release retained instances 说明一致。
  4. L158-L160 `ServiceWatcher` 从 2.6 起默认安装、通过灰名单反射观察 Service 生命周期，与 changelog 2.6 一致。

## 维度 3：版本差异与过期风险
- 评分：4/5
- 问题：
  1. [P2][版本差异] L151-L160 默认观察对象把 `detached root View` 写成 LeakCanary 2.x 的一般默认项，但 changelog 2.7 已调整 `RootViewWatcher` 默认策略：继续检测 toast root view，默认忽略 PopupWindow root view 和 dialog root view，可用 `leak_canary_watcher_watch_dismissed_dialogs=true` 打开 dismissed dialog 观察。建议后续补一句版本边界，避免读者误以为所有 detached root view 在 2.7+ 都会默认报警。
  2. [P3][版本差异] L250-L260 UI / instrumentation 测试段落应把当前 2.14 主路径写实：`LeakAssertions.assertNoLeaks()` + `DetectLeaksAfterTestSuccess`；旧的 `FailTestOnLeakRunListener` 系列在 2.9.1 已移除。正文当前“版本对应的 RunListener”没有构成错误，但容易让读者继续找旧 API。
  3. [P3][版本边界] frontmatter `applicable_versions: Android 8 - Android 17` 对工具章节意义有限；正文实际验证对象是 LeakCanary 2.14 stable，并提到 3.0 alpha 的 heap-growth / test artifacts 变化。建议下次常规维护时补 `verified_tool_version: LeakCanary 2.14` 或在正文参考资料前标注工具版本。

## 闭环动作
- `queue.json`：0 条（未发现 P0/P1）
- `research-gaps.md`：0 条
- `suggestions.md`：0 条（闲时抽检无 P0/P1 时仅日志记录）
- frontmatter：已标注 `last_task9_audit: "2026-05-21"`
