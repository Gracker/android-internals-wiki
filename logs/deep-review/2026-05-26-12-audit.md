# 深度技术 Review 闲时抽检 · 2026-05-26 12:24

## Review 目标
- 章节：7.11 WebView 渲染性能与优化
- 文件：src/part2-performance/ch07-smoothness/11-webview-performance.md
- 模式：闲时抽检
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L522-L524 — Renderer 退出调用链写成 `AwBrowserTerminator.ProcessTerminationStatus()` + `SyncSocket pipe`。当前 Chromium HEAD 的 `android_webview/browser/aw_browser_terminator.cc` / `.h` 没有该方法或 SyncSocket 路径；实际链路是 `AwBrowserTerminator::OnChildExit(const crash_reporter::ChildExitObserver::TerminationInfo& info)` 读取 `info.is_crashed()`，随后投递 `OnRenderProcessGone(..., info.pid, info.is_crashed())`，再进入 `AwRenderProcessGoneDelegate::OnRenderProcessGone(int, bool)`、Java `AwContents.onRenderProcessGone(int, boolean)` 和框架 `WebViewClient.onRenderProcessGone()`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2] L655-L662 — Renderer 模型表把 Android 7-8 (API 24-26) 写成 in-process，同时下一行又把 Android 8-10 (API 26-29) 写成 out-of-process，API 26 重叠。前文 L102 已给出更准确口径：Android 8.0 起引入 out-of-process renderer，O-Q 低内存 32-bit 设备仍可能回退 in-process，Android 11+ 才是全部设备默认 OOP。

## 复核资料
- AOSP `frameworks/base/core/java/android/webkit/WebViewClient.java`：`onRenderProcessGone()` 默认返回 false，未处理时 crash/killed 分支符合章节口径。
- AOSP `frameworks/base/core/java/android/webkit/WebViewFactoryProvider.java`：`getWebViewFactoryClassName()` 在 `Flags.useBEntryPoint()` 下返回 `WebViewChromiumFactoryProviderForB`，章节 L571 口径可保留。
- Chromium `android_webview/docs/architecture.md`：browser code、GPU service、Network Service in-process；renderer 独立进程与 sandbox 口径符合章节主体描述。
- Chromium `android_webview/browser/aw_browser_terminator.cc` / `.h`、`aw_render_process_gone_delegate.h`、`AwContents.java`：用于校正 Renderer 退出调用链。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json（P95）：1 条（7.11，含 P0 + P2 两个 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处（P2 已并入本次 queue issue，随 P0 一并交给 Task2B）
- 更新 frontmatter：`task9_result=needs-rework`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-05-26`
