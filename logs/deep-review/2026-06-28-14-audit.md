# 深度技术 Review 闲时抽检 · 2026-06-28 14:27

## Review 目标
- 章节：1.26 DeliQueue 无锁队列源码解析（AOSP android-16.0.0_r1）
- 文件：src/part1-fundamentals/ch01-architecture/01.26-messagqueue-deliqueue-optimization.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：1
- 具体问题：
  1. [P0] §1.26.2-§1.26.5、§1.26.7 — 正文主干引用 `android-16.0.0_r1` 的 `ConcurrentMessageQueue/MessageQueue.java`、`ConcurrentSkipListSet<MessageNode>`、`STACK_NODE_*`、`drainStack()`。AOSP `android-17.0.0_r1` 中 `ConcurrentMessageQueue/MessageQueue.java` 不存在；实际锚点是 `CombinedDeliMessageQueue/MessageQueue.java`、`MessageStack.java`、`MessageHeap.java`、`Message.java`。

### 维度 3：版本差异覆盖
- 评分：1/5
- 问题数：1
- 具体问题：
  1. [P1] frontmatter、§1.26.6、参考资料 — 章节明确写本节源码全部来自 `android-16.0.0_r1` 且 `android-17.0.0_r1` 尚未源码验证，违反 AIW Android 17/API 37 主线基准。Android 16 可作为历史 rollout 对比，不能作为正文主线结论。

## 一手核验证据
- `https://android.googlesource.com/platform/frameworks/base/+/refs/tags/android-17.0.0_r1/core/java/android/os?format=JSON`：存在 `CombinedDeliMessageQueue/`、`CombinedMessageQueue/`、`LegacyMessageQueue/`、`MessageStack.java`、`MessageHeap.java`；不存在 `ConcurrentMessageQueue/`。
- `CombinedDeliMessageQueue/MessageQueue.java`：`USE_NEW_MESSAGEQUEUE` changeId 标注 `@EnabledAfter(targetSdkVersion = android.os.Build.VERSION_CODES.BAKLAVA)`；`computeUseDeliQueue()` 使用 `CompatChanges.isChangeEnabled(USE_NEW_MESSAGEQUEUE)` 与 `Flags.useConcurrentMessageQueueInApps()`。
- `MessageStack.java`：`pushMessage()` 通过 `VarHandle.weakCompareAndSetRelease` CAS 入栈；`heapSweep()` 将未处理消息加入堆。
- `MessageHeap.java`：使用 `Message[] mHeap`，注释为 `A min-heap of Message objects. Used by MessageQueue.`，排序由 `Message.compareMessages()` 的 `when + insertSeq` 决定。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：1/5

## 闭环动作
- 写入 queue.json（P95）：1 条（含 2 个 review_issues）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-06-28`
- auto-fix：未执行。原因：需要把正文主线从 Android 16 `ConcurrentMessageQueue` 重写到 Android 17 `CombinedDeliMessageQueue + MessageStack + MessageHeap`，超过局部 15 行修复范围。
