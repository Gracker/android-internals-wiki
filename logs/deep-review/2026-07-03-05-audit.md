# 闲时抽检 · 2026-07-03 05:26

## 抽检目标
- 章节：2.8 过度绘制
- 文件：src/part1-fundamentals/ch02-rendering/08-overdraw.md
- 状态：finalized
- 模式：Task 9 闲时抽检；只覆盖维度 1（源码引用）与维度 3（版本差异）

## 一手资料核对
- AOSP `android-17.0.0_r1`：`frameworks/base/libs/hwui/Properties.h` 仍声明 `PROPERTY_DEBUG_OVERDRAW = debug.hwui.overdraw`，并保留 `show`、`show_deuteranomaly`、`count` 注释口径。
- AOSP `android-17.0.0_r1`：`frameworks/base/libs/hwui/Properties.cpp` 当前读取 `debug.hwui.overdraw` 后只处理 `show` / `show_deuteranomaly` 分支，未启用旧 counter 分支。
- AOSP `android-17.0.0_r1`：`frameworks/base/libs/hwui/pipeline/skia/` 目录仍以 `SkiaOpenGLPipeline` / `SkiaVulkanPipeline` / `SkiaGpuPipeline` 等文件为主，未见 Graphite 默认后端或设备白名单入口。
- AOSP `android-17.0.0_r1`：`frameworks/base/graphics/java/android/graphics/Canvas.java` 中 `quickReject(RectF, EdgeType)` 仍 deprecated，`quickReject(RectF)` 存在；`clipRect(..., Region.Op)` 注释仍限定 P 起只允许 `INTERSECT` / `DIFFERENCE`。
- Android Developers Overdraw 文档当前仍说明系统优化在很多情况下会避免 app 内部 overdraw，例如 Fragment background 覆盖 window background；该点延续前轮 P2 建议，不构成 P0/P1。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.8/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] frontmatter 与参考资料仍把主线核验锚点写成 `android-16.0.0_r1`，不符合 2026-06-27 Android 17 基准。已复核 `android-17.0.0_r1` 对应路径和行为后，直接改为 Android 17 锚点。

### 维度 3：版本差异覆盖
- 评分：4.7/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] “Android 16（API 36）” Graphite 边界只写到 android-16，正文适用范围却到 Android 17。已改为 “Android 16-17（API 36-37）”，并把“截至”锚点更新为 `android-17.0.0_r1`。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：1 处（沿用前轮 Window 背景文档口径建议，仅日志记录）
- P3 锦上添花：0 处
- 总体技术评分：4.75/5（抽检范围评分）

## 闭环动作
- AUTO-FIX：3 处版本/源码锚点小改动
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_autofix_at=2026-07-03`
