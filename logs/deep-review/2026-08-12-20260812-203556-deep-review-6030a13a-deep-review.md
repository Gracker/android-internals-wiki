# AIW Deep Review Evidence — 20260812-203556-deep-review-6030a13a

- lane: `aiw-polish-deep-review`
- target: `src/part1-fundamentals/ch01-architecture/37-logd-logging-system-performance.md`
- baseline: `android-17.0.0_r1`
- preflight HEAD: `acb2ae1903f3f947c76c31bae85fa74bb3bb64d9`
- preflight target sha16: `0675cfa62a9a1383`
- final target sha16: `0fbbb2408c603ad4`

## Finding formed

- ID: `AIW-1.37-LOGD-OFFICIAL-SOURCE-METADATA-20260812`
- Severity: P3
- Type: source-metadata
- Claim: the reader-visible prose cites two official documents, but frontmatter `sources` did not list them.
- Evidence: §5.2 cites Android Developers R8 Additional rule types for `-maximumremovedandroidloglevel` and assumption warning boundaries; §5.4 cites Android Developers Log Info Disclosure for `READ_LOGS`, privileged reader, and production log-removal guidance. Frontmatter previously listed AOSP logd/liblog/JNI and NDK logging only.
- Target range: frontmatter `sources` list; reader-visible official citations in §5.2 and §5.4.
- Recommended action: add both official documents to frontmatter `sources`, keep Android 17 source boundary, and refresh deep-review timestamps.
- Status: fixed and closed in `metadata/review-findings.json`.

## Source checks

- `frameworks/base/core/java/android/util/Log.java` android-17.0.0_r1: `Log.d()` calls `println_native(LOG_ID_MAIN, DEBUG, tag, msg)`; long-message helper `printlns()` budgets against `LOGGER_ENTRY_MAX_PAYLOAD` and line breaks.
- `frameworks/base/core/jni/android_util_Log.cpp` android-17.0.0_r1: `println_native` validates `LOG_ID_MAX` and uses `GetStringUTFChars()` for tag/message before calling liblog.
- `system/logging/liblog/logger_write.cpp` android-17.0.0_r1: `__android_log_buf_write()` calls `__android_log_is_loggable()` before dispatching through `__android_log_write_log_message()` / `LogdWrite()`.
- `system/logging/liblog/logd_writer.cpp` android-17.0.0_r1: non-security buffers use the nonblocking socket, `LOG_ID_SECURITY` uses the blocking socket, payload is clipped to `LOGGER_ENTRY_MAX_PAYLOAD`, `EAGAIN` increments the dropped counter, and event tag `1006 liblog` reports drops on a later writable path.
- `system/logging/liblog/include/android/log.h` and `liblog/include/log/log.h` android-17.0.0_r1: log IDs `main` through `kernel` are defined and `LOGGER_ENTRY_MAX_PAYLOAD` is 4068.
- `system/logging/logd/main.cpp` android-17.0.0_r1: default `logd.buffer_type` is `serialized`; alternatives are `serialized` and `simple`; logd listens on `/dev/socket/logdr` and `/dev/socket/logdw`.
- `system/logging/logd/LogListener.cpp` and `logd.rc` android-17.0.0_r1: `logdw` is `dgram+passcred`; `LogListener` chooses io_uring only when `use_iouring()` and initialization both pass; synchronous path uses `recvmsg()` and reads `SCM_CREDENTIALS`.
- `system/logging/logd/SerializedLogBuffer.cpp`, `SerializedLogChunk.cpp`, and `CompressionEngine.cpp` android-17.0.0_r1: serialized chunks are appended, finished chunks are Zstd-compressed, readers are notified/skipped on pruning, and buffer size is enforced by pruning old chunks.
- `system/logging/logd/LogSize.h` / `LogSize.cpp` android-17.0.0_r1: default 256 KiB, min 64 KiB, max 256 MiB; low-RAM non-debuggable devices use minimum size; property override is gated by debuggable and allowed device type.
- `system/logging/logcat/logcat.cpp` android-17.0.0_r1: `--pid` is a command/filter parameter, `android_log_shouldPrintLine()` and `std::regex_search()` run in logcat, and `-g` reports max entry/payload.
- `packages/modules/StatsD/lib/libstatssocket/statsd_writer.cpp` android-17.0.0_r1: native stats socket uses `/dev/socket/statsdw`, `SOCK_DGRAM | SOCK_NONBLOCK`, `writev()`, and `EAGAIN` overload semantics.
- Android Developers R8 Additional rule types (fetched 2026-08-12): documents `-maximumremovedandroidloglevel` and warns that `-assumenosideeffects` assumptions can force optimizations and require testing.
- Android Developers Log Info Disclosure (fetched 2026-08-12): states that since Android 4.1/API 16 only privileged system apps can be granted `READ_LOGS`, discourages direct production logging of sensitive data, and recommends automated removal with tools such as R8.
- Android Developers NDK logging reference (fetched 2026-08-12): documents `__android_log_is_loggable()` and that `LOG_ID_MAIN` is the only log buffer available to apps.

## Changes applied

1. Added `last_deep_review_at` and `last_deep_review_run_id` to frontmatter.
2. Updated `last_verified` to `2026-08-12` and `task6_state` to `reviewed`; status remains `ready-for-review`, confidence remains `medium`.
3. Added the two cited official docs to frontmatter `sources`.
4. Reduced two Gracker Writing structural-meta hits in reader-visible prose without changing technical claims.
5. Added and closed the stable finding in `metadata/review-findings.json`.

## Validation

- `python3 scripts/check-metadata.py --files src/part1-fundamentals/ch01-architecture/37-logd-logging-system-performance.md` passed.
- `python3 scripts/check-summary-links.py` passed.
- `git diff --check` passed.
- Reader-visible Gracker Writing hard gate passed after scanning the body with frontmatter excluded.
