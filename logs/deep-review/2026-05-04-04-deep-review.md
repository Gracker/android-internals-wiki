# 深度技术 Review · 2026-05-04 04:33

## 本轮选择说明
- external-review 活跃文件：0 个。
- 本轮按实际 task9 pending / 已过 7 天冷却且 Task2B 已修复的章节选择：1.16、9.3、18.10。
- 只写问题单，不改章节正文；本轮仅更新 frontmatter 状态、metadata、queue、intake 与日志。

---

## Review 目标
- 章节：1.16 Audio Pipeline 延迟与性能
- 文件：src/part1-fundamentals/ch01-architecture/16-audio-pipeline-performance.md
- 状态：ready-for-review / task6_pending → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：3.0/5
- 问题数：1
- 具体问题：
  1. [P0] L459-L460 游戏音频最佳实践（回调模式 / 音频回调线程） — 正文写“回调模式由 AudioFlinger 在需要数据时主动拉取”，并进一步写“音频回调运行在 AudioFlinger 的高优先级线程上”。App 回调代码不应被描述为运行在 audioserver/AudioFlinger 线程内；AAudio/Oboe 的 data callback 在应用进程侧的回调线程执行，系统可提高该线程调度优先级，AudioFlinger/AAudioService 通过共享内存、timing model 与唤醒机制消费数据。当前写法会误导进程隔离、锁等待、文件 I/O 和崩溃影响面的判断。 建议：改成“使用 AAudio/Oboe data callback 可减少 app 侧主动阻塞 write 带来的调度不确定性；callback 运行在应用进程的高优先级音频线程中，必须避免锁、I/O、分配和重计算”。不要写成 AudioFlinger 线程执行应用回调。

### 维度 2：原理链完整性
- 评分：3.5/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：3.0/5
- 问题数：1
- 具体问题：
  1. [P1] L237-L243 / L372 / L418 AAudio Power Saving Offloaded — 正文把 `AAUDIO_PERFORMANCE_MODE_POWER_SAVING_OFFLOADED` 写成 Android 16 已确定引入，并断言“音频解码工作完全交给 DSP”“实测功耗可降低约 75%”；同一段落又保留“最终 API 入口、最小 API level、支持格式范围待验证”。本轮复核本地 NDK r29 `aaudio/AAudio.h` 只说明该模式会走 offloaded audio path、可向 hardware buffer 写入数秒数据、让 framework data pipe 暂停并允许 CPU sleep；它不能支撑 75% 量化收益，也不足以把所有 payload 写成“解码完全交给 DSP”。 建议：先把结论收窄为“请求省电型 offloaded output path，最终模式需用 `AAudioStream_getPerformanceMode()` / `dumpsys audio` / output profile 验证”。补 Android 16/17 API 文档、支持格式矩阵、设备能力与功耗测试条件后，再保留具体版本和 75% 数字。

### 维度 4：知识盲区
- 评分：3.5/5
- 问题数：0

### 维度 5：数据与案例支撑
- 评分：3.0/5
- 问题数：1
- 具体问题：
  1. [P1] L237-L243 / L372 / L418 AAudio Power Saving Offloaded — 正文把 `AAUDIO_PERFORMANCE_MODE_POWER_SAVING_OFFLOADED` 写成 Android 16 已确定引入，并断言“音频解码工作完全交给 DSP”“实测功耗可降低约 75%”；同一段落又保留“最终 API 入口、最小 API level、支持格式范围待验证”。本轮复核本地 NDK r29 `aaudio/AAudio.h` 只说明该模式会走 offloaded audio path、可向 hardware buffer 写入数秒数据、让 framework data pipe 暂停并允许 CPU sleep；它不能支撑 75% 量化收益，也不足以把所有 payload 写成“解码完全交给 DSP”。 建议：先把结论收窄为“请求省电型 offloaded output path，最终模式需用 `AAudioStream_getPerformanceMode()` / `dumpsys audio` / output profile 验证”。补 Android 16/17 API 文档、支持格式矩阵、设备能力与功耗测试条件后，再保留具体版本和 75% 数字。

### 维度 6：交叉引用一致性
- 评分：4.0/5
- 问题数：0

## 详细问题单
1. [P0] L459-L460 游戏音频最佳实践（回调模式 / 音频回调线程） — 正文写“回调模式由 AudioFlinger 在需要数据时主动拉取”，并进一步写“音频回调运行在 AudioFlinger 的高优先级线程上”。App 回调代码不应被描述为运行在 audioserver/AudioFlinger 线程内；AAudio/Oboe 的 data callback 在应用进程侧的回调线程执行，系统可提高该线程调度优先级，AudioFlinger/AAudioService 通过共享内存、timing model 与唤醒机制消费数据。当前写法会误导进程隔离、锁等待、文件 I/O 和崩溃影响面的判断。
   - 建议：改成“使用 AAudio/Oboe data callback 可减少 app 侧主动阻塞 write 带来的调度不确定性；callback 运行在应用进程的高优先级音频线程中，必须避免锁、I/O、分配和重计算”。不要写成 AudioFlinger 线程执行应用回调。
2. [P1] L237-L243 / L372 / L418 AAudio Power Saving Offloaded — 正文把 `AAUDIO_PERFORMANCE_MODE_POWER_SAVING_OFFLOADED` 写成 Android 16 已确定引入，并断言“音频解码工作完全交给 DSP”“实测功耗可降低约 75%”；同一段落又保留“最终 API 入口、最小 API level、支持格式范围待验证”。本轮复核本地 NDK r29 `aaudio/AAudio.h` 只说明该模式会走 offloaded audio path、可向 hardware buffer 写入数秒数据、让 framework data pipe 暂停并允许 CPU sleep；它不能支撑 75% 量化收益，也不足以把所有 payload 写成“解码完全交给 DSP”。
   - 建议：先把结论收窄为“请求省电型 offloaded output path，最终模式需用 `AAudioStream_getPerformanceMode()` / `dumpsys audio` / output profile 验证”。补 Android 16/17 API 文档、支持格式矩阵、设备能力与功耗测试条件后，再保留具体版本和 75% 数字。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.3/5

## 闭环动作
- 写入 queue.json（priority 95）：1 个条目，覆盖 2 个 P0/P1 问题
- 写入 research-gaps.md：1 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：task9_state=reviewed、task9_result=needs-rework、task2b_state=pending、pipeline_stage=task2b_pending
- 自动晋升 finalized：0 个

---

## Review 目标
- 章节：9.3 ANR 分析方法
- 文件：src/part2-performance/ch09-anr/03-anr-analysis.md
- 状态：ready-for-review / task6_pending → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：4.0/5
- 问题数：0

### 维度 2：原理链完整性
- 评分：3.5/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：4.0/5
- 问题数：0

### 维度 4：知识盲区
- 评分：4.0/5
- 问题数：0

### 维度 5：数据与案例支撑
- 评分：3.0/5
- 问题数：2
- 具体问题：
  1. [P1] L143-L145 Android 14+ traces.txt 可信度“90% 以上”与 2 秒阈值 — 正文把 Android 14+ 的主线程堆栈可信度写成“90% 以上”，并给出“dumpStackTraces 与 ANR 触发时间差超过 2 秒才警惕”的硬阈值。AOSP android-16.0.0_r1 `AnrLatencyTracker` 可验证的是 `anrRecordPlacedOnQueue`、`anrProcessingStarted`、`dumpStackTracesStarted/Ended` 等 Perfetto/StatsLog 计时点；源码本身不提供 90% 可信度或 2 秒通用阈值。当前写法把可观测时延指标升级成未经证明的诊断概率。 建议：删除“90% 以上”或补内部样本统计。把 2 秒改成经验阈值并绑定证据：列出应读取的 AnrLatencyTracker slice/StatsLog 字段、ANR trigger time、firstPids dump start/end 与 bugreport 时间线；在系统高负载、多进程 dump、目标进程已恢复时仍要降级 traces.txt 结论。
  2. [P2] L183-L192 Perfetto 抓取与 am_anr 定位 — 正文建议在 Perfetto 搜索 `am_anr`，但没有给出必须采集 EventLog/logcat 或 `am` atrace 的可复现 trace config。只抓 sched/binder/input/view 等数据源时，Perfetto 里可能没有 `am_anr` 事件，读者会按文档操作失败。 建议：补最小抓取配置或 fallback：trace config 中明确包含 android.log/eventlog 或 atrace `am`，否则从 bugreport / `logcat -b events` 中定位 `am_anr` 时间，再跳回 Perfetto。

### 维度 6：交叉引用一致性
- 评分：4.0/5
- 问题数：0

## 详细问题单
1. [P1] L143-L145 Android 14+ traces.txt 可信度“90% 以上”与 2 秒阈值 — 正文把 Android 14+ 的主线程堆栈可信度写成“90% 以上”，并给出“dumpStackTraces 与 ANR 触发时间差超过 2 秒才警惕”的硬阈值。AOSP android-16.0.0_r1 `AnrLatencyTracker` 可验证的是 `anrRecordPlacedOnQueue`、`anrProcessingStarted`、`dumpStackTracesStarted/Ended` 等 Perfetto/StatsLog 计时点；源码本身不提供 90% 可信度或 2 秒通用阈值。当前写法把可观测时延指标升级成未经证明的诊断概率。
   - 建议：删除“90% 以上”或补内部样本统计。把 2 秒改成经验阈值并绑定证据：列出应读取的 AnrLatencyTracker slice/StatsLog 字段、ANR trigger time、firstPids dump start/end 与 bugreport 时间线；在系统高负载、多进程 dump、目标进程已恢复时仍要降级 traces.txt 结论。
2. [P2] L183-L192 Perfetto 抓取与 am_anr 定位 — 正文建议在 Perfetto 搜索 `am_anr`，但没有给出必须采集 EventLog/logcat 或 `am` atrace 的可复现 trace config。只抓 sched/binder/input/view 等数据源时，Perfetto 里可能没有 `am_anr` 事件，读者会按文档操作失败。
   - 建议：补最小抓取配置或 fallback：trace config 中明确包含 android.log/eventlog 或 atrace `am`，否则从 bugreport / `logcat -b events` 中定位 `am_anr` 时间，再跳回 Perfetto。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：1 处
- P3 锦上添花：0 处
- 总体技术评分：3.8/5

## 闭环动作
- 写入 queue.json（priority 85）：1 个条目，覆盖 1 个 P0/P1 问题
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：1 处
- 更新章节 frontmatter：task9_state=reviewed、task9_result=needs-rework、task2b_state=pending、pipeline_stage=task2b_pending
- 自动晋升 finalized：0 个

---

## Review 目标
- 章节：18.10 SurfaceControl API 深入
- 文件：src/part2-performance/ch18-rendering-pipelines/10-surface-control-api.md
- 状态：ready-for-review / task6_pending → task2b_pending

## 审查结果
### 维度 1：源码引用准确性
- 评分：2.5/5
- 问题数：2
- 具体问题：
  1. [P0] L90 / L270-L276 `ASurfaceControl_fromJava()` — 正文把 `ASurfaceControl_fromJava()` 写成 API 35+ 可用，并给出 JNI 示例。本轮复核 AOSP android-16.0.0_r1 与 main 的 `frameworks/native/include/android/surface_control.h`、本地 NDK r29 `android/surface_control.h`，均未发现 `ASurfaceControl_fromJava` 符号。该示例无法编译，且会误导读者以为 NDK 可直接从 Java `SurfaceControl` 转句柄。 建议：删除 `ASurfaceControl_fromJava()` 路径与示例。保留可复核路径：`Surface` → `ANativeWindow_fromSurface()` → `ASurfaceControl_createFromWindow()`，或在 Java 层使用 `SurfaceControl.Transaction` 完成对应操作；若未来平台新增桥接 API，需补真实头文件签名和 API level。
  2. [P0] L263-L270 `ASurfaceControl_writeToParcel()` / `ASurfaceControl_readFromParcel()` — 正文声称 Android 15+ NDK 公开 `ASurfaceControl_writeToParcel()` / `ASurfaceControl_readFromParcel()`，可实现全 C++ 跨进程图层迁移。本轮复核 AOSP android-16.0.0_r1/main 与本地 NDK r29 `surface_control.h` 均未找到这两个函数。NDK 当前不能按正文示例直接 parcel `ASurfaceControl*`。 建议：删除全 C++ Parceling 结论与代码。跨进程场景按 Java `android.view.SurfaceControl` Parcelable、WindowManager/Shell 托管或系统服务转交来讲，并标清普通应用可用性、权限边界和版本。

### 维度 2：原理链完整性
- 评分：4.0/5
- 问题数：0

### 维度 3：版本差异覆盖
- 评分：3.0/5
- 问题数：0

### 维度 4：知识盲区
- 评分：3.5/5
- 问题数：0

### 维度 5：数据与案例支撑
- 评分：4.0/5
- 问题数：0

### 维度 6：交叉引用一致性
- 评分：4.0/5
- 问题数：0

## 详细问题单
1. [P0] L90 / L270-L276 `ASurfaceControl_fromJava()` — 正文把 `ASurfaceControl_fromJava()` 写成 API 35+ 可用，并给出 JNI 示例。本轮复核 AOSP android-16.0.0_r1 与 main 的 `frameworks/native/include/android/surface_control.h`、本地 NDK r29 `android/surface_control.h`，均未发现 `ASurfaceControl_fromJava` 符号。该示例无法编译，且会误导读者以为 NDK 可直接从 Java `SurfaceControl` 转句柄。
   - 建议：删除 `ASurfaceControl_fromJava()` 路径与示例。保留可复核路径：`Surface` → `ANativeWindow_fromSurface()` → `ASurfaceControl_createFromWindow()`，或在 Java 层使用 `SurfaceControl.Transaction` 完成对应操作；若未来平台新增桥接 API，需补真实头文件签名和 API level。
2. [P0] L263-L270 `ASurfaceControl_writeToParcel()` / `ASurfaceControl_readFromParcel()` — 正文声称 Android 15+ NDK 公开 `ASurfaceControl_writeToParcel()` / `ASurfaceControl_readFromParcel()`，可实现全 C++ 跨进程图层迁移。本轮复核 AOSP android-16.0.0_r1/main 与本地 NDK r29 `surface_control.h` 均未找到这两个函数。NDK 当前不能按正文示例直接 parcel `ASurfaceControl*`。
   - 建议：删除全 C++ Parceling 结论与代码。跨进程场景按 Java `android.view.SurfaceControl` Parcelable、WindowManager/Shell 托管或系统服务转交来讲，并标清普通应用可用性、权限边界和版本。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json（priority 95）：1 个条目，覆盖 2 个 P0/P1 问题
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新章节 frontmatter：task9_state=reviewed、task9_result=needs-rework、task2b_state=pending、pipeline_stage=task2b_pending
- 自动晋升 finalized：0 个

## 本轮汇总
- P0 事实错误：3 处
- P1 重要缺失：2 处
- P2 建议改进：1 处
- queue.json 新增/合并：3 个章节
- research-gaps.md：1 处（1.16 AAudio offloaded playback 边界）
- suggestions.md：1 处（9.3 Perfetto am_anr 可复现配置）
- 自动晋升 finalized：0 个
