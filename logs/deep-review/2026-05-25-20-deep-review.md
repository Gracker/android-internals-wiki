# 深度技术 Review · 2026-05-25 20:22

## Review 目标
- 章节：18.8 OpenGL ES 渲染链路
- 文件：src/part2-performance/ch18-rendering-pipelines/08-opengl-es.md
- 原状态：ready-for-review / task9_state=pending（Task2B 修复后复审）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：0
- 说明：复核 `GLSurfaceView` 创建/唤醒/render loop、`EGL_ANDROID_native_fence_sync` 用法、`Settings.Global` ANGLE 切换键、`BufferQueueProducer::dequeueBuffer()` 与 `BLASTBufferQueue::acquireNextBufferLocked()`。上一轮 P0 的 native fence sync type / flush / FD 导出边界已修正：示例已拆成 CPU wait 与 FD 导出两类，并补 `glFlush()` 与 `EGL_SYNC_FLUSH_COMMANDS_BIT_KHR`。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：GLThread → EGLSurface → BufferQueue/BLAST → SurfaceFlinger/HWC 的主链路完整。上一轮 P1 的 `dequeueBuffer` 归因已改为“可用 slot / outstanding buffer / 返回 fence”分层判断，避免把所有长耗时直接等同于 release fence。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：Android 9-11 传统 BufferQueue 与 Android 12+ SurfaceView BLAST adapter / BLASTBufferQueue 的版本边界、Android 15+ Vulkan/ANGLE 策略边界均未发现 P0/P1。ANGLE 部分保持“可选路径 / OEM 策略”口径，没有再写成强制默认。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：EGLContext 共享、GLSurfaceView vs 原生 EGL、ANGLE Trace 差异、Buffer/Fence 观察点已有覆盖；本轮未新增 research-gaps。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：历史 P2“GLES BufferQueue 通常配置为 3 个 Slot”的口径建议已在 `intake/suggestions.md`（2026-05-20）记录，本轮不重复追加。正文当前 P0/P1 修复点不依赖新增 benchmark。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：18.9 Vulkan、18.6 SurfaceView、2.14 图形 API 演进、2.13 BufferQueue 目标文件存在；本章 ANGLE 与 Vulkan 的边界未发现和 2.14 的当前已知风险相冲突的新问题。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：0 处（历史 P2 已在 suggestions.md，未重复新增）
- P3 锦上添花：0 处
- 总体技术评分：4.0/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：历史 P2 1 处未重复新增
- 更新章节 frontmatter：`task9_result=pass-tech-review`、`task9_state=reviewed`、`status=finalized`、`pipeline_stage=ready-to-publish`
- 自动晋升 finalized：18.8（Task6 已通过，queue.json 中 18.8 无 pending 条目）

## 核验锚点
- AOSP android-16.0.0_r1 `frameworks/base/opengl/java/android/opengl/GLSurfaceView.java`：`setRenderer()`、`guardedRun()`、`readyToDraw()`、`eglSwapBuffers()` 调用点
- AOSP android-16.0.0_r1 `frameworks/native/libs/gui/BufferQueueProducer.cpp`：`waitForFreeSlotThenRelock()`、`dequeueBuffer()`、returned fence / slot / max dequeued 边界
- AOSP android-16.0.0_r1 `frameworks/native/libs/gui/BLASTBufferQueue.cpp`：`acquireNextBufferLocked()`、`Transaction::setBuffer()`、acquire fence 传递
- AOSP android-16.0.0_r1 `frameworks/base/core/java/android/provider/Settings.java`：`angle_gl_driver_selection_pkgs` / `angle_gl_driver_selection_values`
