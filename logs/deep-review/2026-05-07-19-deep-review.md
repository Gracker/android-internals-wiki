# 深度技术 Review · 2026-05-07 19:32

## Review 目标
- 章节：4.1 Android 内存模型全景
  - 文件：src/part1-fundamentals/ch04-memory/01-memory-overview.md
  - 状态：ready-for-review → task2b_pending
- 章节：8.4 其他响应速度场景
  - 文件：src/part2-performance/ch08-responsiveness/04-other-scenarios.md
  - 状态：ready-for-review → task2b_pending
- 章节：19.13 androidx.tracing（Tracing SDK）
  - 文件：src/part3-tools/ch19-apm/13-tracing-sdk.md
  - 状态：ready-for-review → task2b_pending

## 外部 Review 输入
- logs/external-review/ 根目录无活跃文件；本轮未消费 external-review 文件。
- memory_search 不可用（embedding provider `zai` 未注册），本轮以仓库文件、官方网页抓取、AndroidX/AOSP/Perfetto 源码拉取为准。

## 审查结果

### 4.1 Android 内存模型全景
- 源码引用准确性：2.5/5
- 原理链完整性：3.5/5
- 版本差异覆盖：3.0/5
- 知识盲区：4.0/5
- 数据与案例支撑：3.0/5
- 交叉引用一致性：4.0/5
- 具体问题：
  - [P0] [版本差异/源码准确性] L118 Android 17 MemoryLimiter — 正文写“应用 PSS 超过系统配额会被直接终止，ApplicationExitInfo 记录 MemoryLimiter 原因”。Android 17 官方行为变更说明的诊断契约是：命中 app memory limits 时 ApplicationExitInfo.getReason() 为 REASON_OTHER，getDescription() 包含 “MemoryLimiter:AnonSwap”；同时可用 ProfilingTrigger.TRIGGER_TYPE_ANOMALY 触发 heap dump。正文把触发口径写成 PSS，并把 MemoryLimiter 写成 exit reason，和公开 API/诊断口径不一致。 建议：改为 Android 17 app memory limits / AnonSwap 口径：写清 REASON_OTHER + getDescription() 字符串；不要写成独立 MemoryLimiter reason；如果要解释配额指标，补 AOSP/官方文档锚点，区分 PSS、RSS、AnonSwap。
  - [P1] [源码准确性/工具数据源] L510-L514 Perfetto 内存相关 Track — 正文列出 `android.process_meminfo`、`linux.counter` / `linux.process_counter`、`android.memory_snapshot`。Perfetto 本地源码/文档中进程内存轮询数据源是 `linux.process_stats`，系统 meminfo/vmstat 是 `linux.sys_stats`，Java heap dump 数据源是 `android.java_hprof` / `android.java_hprof.oom`；SQL 表侧通常读 `counter` + `process_counter_track`，不是上述名称。 建议：把该段改成 TraceConfig data source 与 SQL/UI 名称分层说明：`linux.process_stats` 采集 mem.rss/mem.swap 等 per-process counters，`linux.sys_stats` 采集 /proc/meminfo，Java Heap Dump 使用 `android.java_hprof`；不要把 UI track/table 名称写成 data source。

### 8.4 其他响应速度场景
- 源码引用准确性：3.0/5
- 原理链完整性：3.5/5
- 版本差异覆盖：4.0/5
- 知识盲区：4.0/5
- 数据与案例支撑：3.5/5
- 交叉引用一致性：4.0/5
- 具体问题：
  - [P1] [源码准确性/Trace 观察点] L125、L247 FragmentManager trace slice — 正文写 AndroidX Fragment 1.3.0+ 可在 Perfetto 看到 `FragmentManager:commit`、`FragmentManager:execPendingActions`、`FragmentManager:moveToState`，并建议 Tab 切换时搜索 `FragmentManager:` 前缀。复核 AndroidX `androidx-main` 的 FragmentManager/BackStackRecord/FragmentStateManager/SpecialEffectsController 源码，未发现 `Trace.beginSection` 或这些 slice 名称；源码里的 `FragmentManager:` 主要是 ActivityResultRegistry key 前缀，不是 Perfetto slice。 建议：删除“自动可见 FragmentManager:* slice”的确定性说法；改为业务入口自行 `Trace.beginSection("FragmentTransaction")`，并结合主线程 slice、inflate、RecyclerView layout/prefetch、FrameTimeline 观察。如果确有特定 Fragment 版本/工具链会生成 slice，需要补源码 commit 或真实 trace 截图。
  - [P2] [示例代码边界] L196-L214 Lifecycle + ViewModel 懒加载示例 — 示例只在 `viewLifecycleOwner.lifecycleScope.launch` 中 collect `uiState`，没有触发首次加载，也没有 `repeatOnLifecycle` 限制可见状态；与“只在首次可见时加载”的说明没有闭环。 建议：补 `onResume`/`repeatOnLifecycle(Lifecycle.State.RESUMED)` 触发 + ViewModel 幂等 guard，或者把示例改成只展示状态收集，不宣称它完成懒加载。
  - [P2] [Trace 观察点] L407 搜索响应网络请求观察 — 正文写网络请求可以在 `HttpURLConnection` 或 OkHttp 的 trace 中观察到，容易让读者理解为 Perfetto 默认有稳定 OkHttp/HttpURLConnection slice。实际需要 OkHttp EventListener、自定义 `Trace.beginSection`、Network Inspector 或更底层 socket/syscall/ftrace 线索。 建议：改成“默认 Perfetto 不保证有应用层 HTTP client slice；需要在 repository/OkHttp EventListener 外层补稳定 trace 名称，或结合 socket/线程状态侧证”。

### 19.13 androidx.tracing（Tracing SDK）
- 源码引用准确性：2.0/5
- 原理链完整性：3.0/5
- 版本差异覆盖：2.5/5
- 知识盲区：3.5/5
- 数据与案例支撑：2.0/5
- 交叉引用一致性：4.0/5
- 具体问题：
  - [P0] [代码示例错误] L304-L316 协程 async trace 修正示例 — “修正”示例中 `val data = repository.fetchData()` 定义在 `trace("Home#fetchData") { ... }` lambda 内，随后在 `withContext(Dispatchers.Main)` 中使用 `data`，超出作用域，Kotlin 代码无法编译。 建议：把 `val data = trace("Home#fetchData") { repository.fetchData() }` 写成返回值，或在 trace 外声明变量；同时保留 I/O 同步 slice 与主线程 render slice。
  - [P1] [原理断裂/异常路径] L300-L353 async trace 示例 — 两个 async trace 示例都在启动时 `beginAsyncSection`，只在成功路径 `endAsyncSection`。如果 `fetchData()`、解析、`submitList()`、executor 任务或协程被取消/抛异常，async span 会遗留未闭合，正好制造本文警告的 trace 失配。 建议：协程示例用 `try/finally` 或 `invokeOnCompletion` 保证 end；executor 示例也要在异常路径关闭 async span，必要时在主线程 post 前后分别处理失败和取消。
  - [P1] [数据错误/数据缺失] L162-L188 tracing 开销表与帧预算换算 — 正文给出 200-500ns、100-300ns、per-frame <0.5% 等精确数值，但没有设备、Android 版本、trace enabled/disabled、字符串长度和测量方法。L175 还把 1-2μs 写成占 120Hz 帧预算 0.8%-1.6%；按 8.33ms 帧预算计算，1-2μs 约为 0.012%-0.024%，数量级错误。 建议：删除或降级这些精确数字，补 microbenchmark 条件；帧预算换算重新计算。若保留开销段，至少拆 tracing disabled fast path、enabled write path、动态字符串分配三类。
  - [P1] [版本差异/源码准确性] L232-L246 AndroidX tracing 版本表 — 正文写生产包推荐 1.2 稳定版，并把 1.2 归因于 API 31+ begin/end 内联优化。AndroidX tracing release notes 显示最新 stable 为 1.3.0（2025-04-23），1.2.0 的重要变化是 lazy string/cookie 的 `trace()` / `traceAsync()` 与 begin 失败时跳过 end，1.3.0 是 Trace 转 Kotlin、ktx 合并；“1.2 修正 API31+ 内联路径”缺少 release note 支撑。 建议：把推荐版本更新为 1.3.0 stable（若项目要锁 1.2 需说明原因）；按官方 release notes 重写 1.2/1.3/2.0-alpha 差异，不要把未证实的“API31+ 内联路径”写成版本事实。
  - [P2] [版本边界] L232 API 31+ app tracing 默认可见性 — 正文写 API 31+ app tracing 在所有应用里默认开启，但 AndroidX Trace 源码注释还列出 `<profileable enabled=false/>` 或 `<profileable shell=false/>` 例外。 建议：补 profileable manifest 例外，避免把“默认开启”写成无条件成立。

## 统计
- P0 事实错误：2 处
- P1 重要缺失：5 处
- P2 建议改进：3 处
- P3 锦上添花：0 处
- 总体技术评分：3.0/5

## 闭环动作
- 写入 queue.json：3 个 section 级 pending 条目（4.1 P95，8.4 P85，19.13 P95）
- 写入 suggestions.md：3 条 P2
- 写入 research-gaps.md：0 条
- 更新章节 frontmatter：task9_state=reviewed，task2b_state=pending，pipeline_stage=task2b_pending
- 自动晋升 finalized：0 个（本轮存在 P0/P1）

## 复核依据
- Android 17 behavior changes: App memory limits；ApplicationExitInfo.getDescription 包含 `MemoryLimiter:AnonSwap`，reason 为 `REASON_OTHER`，可配合 `TRIGGER_TYPE_ANOMALY`。
- AndroidX Fragment androidx-main 源码：FragmentManager/BackStackRecord/FragmentStateManager/SpecialEffectsController 未发现相关 `Trace.beginSection` slice；`FragmentManager:` 命中处为 ActivityResultRegistry key prefix。
- Perfetto 本地源码/文档：builtin producer 注册 `android.java_hprof` / `android.java_hprof.oom`；memory counters 文档使用 `linux.process_stats`、`linux.sys_stats`。
- AndroidX tracing release notes/API：stable 1.3.0；1.2.0 重点是 lazy string/cookie trace/traceAsync 等；Trace.forceEnableAppTracing 文档说明 API 31+ 默认开启及 profileable 例外。
