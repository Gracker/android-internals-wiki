# 深度技术 Review 闲时抽检 · 2026-05-22 21:20

## Review 目标
- 模式：闲时抽检（无 pending 候选，抽检 finalized/pass-tech-review 章节）
- 章节：1.13 MessageQueue 机制与 DeliQueue 无锁优化
- 文件：src/part1-fundamentals/ch01-architecture/13-messagequeue-deliqueue.md
- 状态：finalized → task2b_pending（发现 P0 后回炉）
- 抽检范围：维度 1（源码引用准确性）+ 维度 3（版本差异覆盖）

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：1
- 具体问题：
  1. [P0] L506-L641 — `CombinedDeliMessageQueue/MessageQueue.java`、`DeliQueue/MessageQueue.java`、`MessageStack.java`、`MessageHeap.java` 被写成 AOSP mainline / Android 17 API 37 阶段源码锚点，但 android.googlesource `platform/frameworks/base` 的 `main` 分支和 `android-16.0.0_r1` 均无法访问这些路径（404）。当前 public main tree 可见的是 `CombinedMessageQueue/`、`ConcurrentMessageQueue/`、`LegacyMessageQueue/`、`LockedMessageQueue/`、`SemiConcurrentMessageQueue/`。
  2. [P0] L640-L643 — `SemiConcurrentMessageQueue` 被写成 ROM fork、非 AOSP 主线；但 2026-05-22 抽检 AOSP main tree 下已存在 `core/java/android/os/SemiConcurrentMessageQueue/`。需要按 ref 区分：android-16 tag 下不存在，当前 main 下存在，不能笼统写“非 AOSP 主线”。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] L506-L641 — 正文前半段基于 `android-16.0.0_r1`，正确写出 `CombinedMessageQueue` / `ConcurrentMessageQueue` 的 rollout；补充段又把 LineageOS 镜像/概念性 DeliQueue 结构写成 AOSP mainline Android 17 源码路径。Android Developers Blog 可以支撑“Treiber stack + min-heap”的概念描述，Android 17 behavior guidance 可以支撑 `mMessages` always null、targetSdk 37 默认启用；但不能支撑这些具体 public AOSP 文件路径。

## 证据锚点
- `https://android.googlesource.com/platform/frameworks/base/+/android-16.0.0_r1/core/java/android/os/?format=JSON`：仅见 `CombinedMessageQueue`、`ConcurrentMessageQueue`、`LegacyMessageQueue`、`MessageQueue_ravenwood.java`。
- `https://android.googlesource.com/platform/frameworks/base/+/main/core/java/android/os/?format=JSON`：见 `CombinedMessageQueue`、`ConcurrentMessageQueue`、`LegacyMessageQueue`、`LockedMessageQueue`、`SemiConcurrentMessageQueue`，未见 `CombinedDeliMessageQueue`、`DeliQueue`、`MessageStack.java`、`MessageHeap.java`。
- `CombinedMessageQueue/MessageQueue.java`（main / android-16 tag）：命中 `ConcurrentSkipListSet`、`mPriorityQueue`、`mAsyncPriorityQueue`；未命中 `MessageStack`、`MessageHeap`、`DeliQueue`。
- Android Developers behavior guidance：Android 17 保留 `mMessages` 字段但新实现中 always null；Espresso 需 3.7.0+，Robolectric 需 4.17+；`USE_NEW_MESSAGEQUEUE` 可用于兼容测试。
- Android Developers Blog 2026-02-17：可作为 DeliQueue 概念、性能数字、Treiber stack + min-heap 的来源，但不等同于上述 AOSP 文件路径存在性。

## 闭环动作
- 写入 queue.json（P95）：1 处
- 更新章节 frontmatter：`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`、`last_task9_audit: 2026-05-22`
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：无（发现 P0）

## 建议修正方向
1. 删除或降级 `CombinedDeliMessageQueue` / `MessageStack.java` / `MessageHeap.java` 的 AOSP mainline 源码断言；若保留，必须补可访问官方 ref/commit。
2. Android 16 tag 源码段只锚 `CombinedMessageQueue` / `ConcurrentMessageQueue` / `LegacyMessageQueue`，并说明 public tag 里的队列结构是 `ConcurrentSkipListSet` + 两组 priority queue。
3. Android 17 DeliQueue 段改用官方 blog / behavior guidance 的概念来源，避免把 blog 概念结构写成不可验证的 AOSP 文件路径。
4. `SemiConcurrentMessageQueue` 按具体 ref 改写：android-16 tag 不存在；当前 AOSP main 存在；若讨论 ROM fork，需另列 fork URL 与 commit。

## 统计
- P0 事实错误：1 处
- P1 重要缺失：0 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5
