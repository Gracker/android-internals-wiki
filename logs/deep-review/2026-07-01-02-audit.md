# Task9 闲时抽检 · 2026-07-01 02:28

## Review 目标
- 章节：6.5 SharedPreferences/DataStore 性能与 ANR 优化
- 文件：src/part1-fundamentals/ch06-storage/05-sharedpreferences-datastore.md
- 状态：finalized
- 模式：闲时抽检，只检查维度 1（源码引用）和维度 3（版本差异）
- Android 基准：android-17.0.0_r1（Android 17 / API 37）；未使用 Android 18 / API 38+ 资料。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：1（已 auto-fix，无开放 P0/P1）
- 具体问题：
  1. [P0][AUTO-FIX] §MultiProcess DataStore 源码级验证 — 正文把 `libdatastore_shared_counter.so` 写成来自单独的 `androidx.datastore:datastore-multiprocess` AAR，并要求显式添加该依赖，同时保留了旧版 `ShadowSharedCounter` 降级代码片段。Google Maven 未发布该 artifact；1.1.7 中 native library 位于 `androidx.datastore:datastore-core-android` AAR 的 `jni/<abi>/` 目录，`datastore-core` / `datastore` 通过 POM 依赖带入；`SharedCounter.android.kt` 公开 source jar 中没有 `ShadowSharedCounter` 降级路径。
  2. [P1][AUTO-FIX] frontmatter 与正文源码标签仍以 `android-16.0.0_r1` 作为主线锚点。已复核 Android 17 源码后把 `SharedPreferencesImpl`、`QueuedWork`、`ActivityThread`、`BroadcastReceiver` 验证标签重锚到 `android-17.0.0_r1`。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0（开放）
- 具体结论：
  1. AOSP android-17.0.0_r1 `SharedPreferencesImpl.java` 897 行，`sLoadExecutor`、`awaitLoadedLocked()`、`enqueueDiskWrite()`、`mWritingToDiskLock`、`MAX_FSYNC_DURATION_MILLIS = 256` 均存在，正文 SP 读写路径成立。
  2. AOSP android-17.0.0_r1 `QueuedWork.java` 318 行，`waitToFinish()` 仍先 `processPendingWork()` 再 drain `sFinishers`。
  3. AOSP android-17.0.0_r1 `ActivityThread.java` 仍保留 pre-Honeycomb pause、现代 Activity stop、Service start/stop 的 `QueuedWork.waitToFinish()` 路径；`BroadcastReceiver.java` 仍在 `QueuedWork.hasPendingWork()` 为真时 queue `sendFinished(mgr)`。
  4. AOSP android-17.0.0_r1 `SharedPreferences.java` javadoc 仍明确建议新数据存储需求不要使用 SharedPreferences，转向 DataStore / Room。

## 一手核验证据
- Gitiles AOSP `platform/frameworks/base` tag `android-17.0.0_r1`：`core/java/android/app/SharedPreferencesImpl.java`、`QueuedWork.java`、`ActivityThread.java`、`core/java/android/content/BroadcastReceiver.java`、`SharedPreferences.java`、`app/ContextImpl.java` 均可定位。
- Google Maven `androidx.datastore:datastore-core-android:1.1.7` AAR：包含 `jni/arm64-v8a/libdatastore_shared_counter.so`、`jni/armeabi-v7a/libdatastore_shared_counter.so`、`jni/x86/libdatastore_shared_counter.so`、`jni/x86_64/libdatastore_shared_counter.so`。
- Google Maven metadata：`androidx.datastore:datastore-multiprocess` 返回 404；`datastore-core` POM 依赖 `datastore-core-android:1.1.7`。
- `datastore-core-android-1.1.7-sources.jar`：`MultiProcessDataStoreFactory.android.kt`、`MultiProcessCoordinator.android.kt`、`SharedCounter.android.kt`、`MulticastFileObserver.android.kt` 与修正后的多进程协调链路一致；`MultiProcessCoordinator` 在 lazy 初始化 `.version` counter 时调用 `SharedCounter.loadLib()`。

## 统计
- P0 事实错误：0 处（开放；1 处已 auto-fix）
- P1 重要缺失：0 处（开放；1 处已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.5/5

## 闭环动作
- 修改章节：src/part1-fundamentals/ch06-storage/05-sharedpreferences-datastore.md
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 更新 frontmatter：`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-07-01`、`last_task9_audit: 2026-07-01`
- 后续闭环：回到 Task6 复审 auto-fix 后正文质量。
