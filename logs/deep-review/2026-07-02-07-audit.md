# 深度技术 Review 闲时抽检 · 2026-07-02 07:24

## 抽检目标
- 章节：21.8 启动监控与度量
- 文件：src/part5-app/ch21-startup/08-startup-monitoring.md
- 状态：finalized
- 选择原因：Preflight `CANDIDATES=0`、`TASK2B_BACKLOG=0`、`IDLE_AUDIT=336`，进入闲时抽检模式；本节 `last_task9_audit=2026-06-11`，且上一轮抽检留有 API 36 `getStartComponent()` P2 版本补充线索，适合复查源码引用与版本差异。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- Android 版本边界：主线锚定 `android-17.0.0_r1`；未引用 Android 18 / API 38+ 内容。

## 复核证据
- AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/app/ApplicationStartInfo.java`：`STARTUP_STATE_FIRST_FRAME_DRAWN`、`START_TIMESTAMP_LAUNCH/FORK/APPLICATION_ONCREATE/BIND_APPLICATION/FIRST_FRAME/FULLY_DRAWN`、`getReason()`、`getStartType()`、`getStartupState()`、`getStartupTimestamps()` 均存在；源码注释说明 `START_TIMESTAMP_FULLY_DRAWN` 不保证存在，因为依赖应用调用 `reportFullyDrawn()`。
- AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/app/ActivityManager.java`：`getHistoricalProcessStartReasons(int)` 与 `addApplicationStartInfoCompletionListener(Executor, Consumer<ApplicationStartInfo>)` 均存在；completion listener 注释明确当前回调不会返回 `START_TIMESTAMP_FULLY_DRAWN`，需要在 `Activity.reportFullyDrawn()` 后读取 `ApplicationStartInfo`。
- AOSP `android-17.0.0_r1` `frameworks/base/core/java/android/app/Activity.java`：`reportFullyDrawn()` 仍是系统启动诊断的 fully drawn 上报入口。
- Android Developers App startup time / Android Vitals 文档：慢启动阈值仍为 cold >= 5s、warm >= 2s、hot >= 1.5s；TTFD 需要应用调用 `reportFullyDrawn()`。

## 抽检结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0 个 P0/P1
- 结论：正文对 `Activity.reportFullyDrawn()`、`ApplicationStartInfo`、`getHistoricalProcessStartReasons(int)`、`addApplicationStartInfoCompletionListener()`、`getStartupTimestamps()` 与 `START_TIMESTAMP_FULLY_DRAWN` 的描述与 `android-17.0.0_r1` 源码和官方文档一致。Vitals 冷/温/热启动阈值口径复核通过。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0 个 P0/P1；1 个既有 P2
- 具体问题：
  1. [P2][版本补充][既有] Android 16 / API 36 已在 `ApplicationStartInfo` 中提供 `getStartComponent()` 与 `START_COMPONENT_ACTIVITY/BROADCAST/CONTENT_PROVIDER/SERVICE/OTHER`，可补充启动来源组件维度。正文只列 `getReason()`、`getStartType()`、`getStartupState()`、`getStartupTimestamps()`，不影响当前 Android 15+ 平台启动信息主结论，继续作为发布前可选补充记录。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：0 处
- P2 建议改进：1 处（既有 P2，仅日志记录，不重复写入 suggestions.md）
- P3 锦上添花：0 处
- 总体技术评分：4.4/5

## 闭环动作
- 更新 frontmatter：`last_task9_audit: "2026-07-02"`
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节正文改动：0 处
