# 深度技术 Review · 2026-06-26 18:20

## Review 目标
- 章节：26.3 性能指标采集与上报
- 文件：src/part5-app/ch26-observability/03-performance-collection.md
- 状态：finalized (从未进行 Task9 技术审计)

## 审查结果

### 维度 1：源码引用准确性（Idle Audit 重点关注）
- 评分：3/5
- 问题数：3

**具体问题：**
1. [P1] Android 17 memtrack HAL 实现 — 文中引用的 `memtrack_proc_gl_pss()` 函数名错误，实际应为 `memtrack_proc_gpu_pss()`。AOSP android-17.0.0_r1 中该函数名为 `memtrack_proc_gpu_pss()` 而非 `memtrack_proc_gl_pss()`。
2. [P1] Debug.MemoryInfo API 版本兼容性 — 文中提到 `getMemoryInfo()` 返回的 `Debug.MemoryInfo` 包含 `getMemoryStat(String)` 方法（API 23+），但未明确说明该方法在 Android 17 中的行为变化。Android 17 中该方法新增了针对ART虚拟机内部状态的统计支持，如 `summary.art-heap`。
3. [P2] LeakCanary 2.x 架构描述 — 文中提到 LeakCanary 2.x 在 Debug 构建中通过 `ContentProvider` 自动初始化，但未说明 Android 17 中的变化：Android 17 上 ContentProvider 的初始化时机可能受到严格的生命周期管理影响，需要显式调用 `LeakCanary.setConfig()`。

### 维度 2：原理链完整性（Idle Audit 简略）
- 评分：4/5
- 问题数：1

**具体问题：**
1. [P2] StatsD 数据流路径 — 文中描述的三层架构（StatsManagerService → StatsCompanionService → statsd daemon）正确，但缺少对 `libstatssocket` 库内部实现细节的说明，该库负责 Java 对象到 C++ `StatsEvent` 的序列化转换。

### 维度 3：版本差异覆盖（Idle Audit 重点关注）
- 评分：3/5
- 问题数：2

**具体问题：**
1. [P1] Android 17 内存分类精度变化 — 文中提到 Android 17 将图形内存从二分类拆分为 graphics / gl / other 三分类，但缺少关键边界：Android 17 引入的 `MEMTRACK_FLAG_GPU_PRIVATE` 标志位，用于区分GPU私有内存和共享内存，这对图形密集型应用的性能分析至关重要。
2. [P2] Battery Historian 版本演进 — 文中表格提到 Android 17 新增 `PERFORMANCE_METRICS_ATOM`（ID 10245），但遗漏了该 Atom 在不同 Android 版本中的ID变化历史，以及 Android 15-17 之间的ID从10240逐步演进到10245的过程。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：1

**具体问题：**
1. [P2] Android 17 性能监控权限模型 — 文中提到 Android 17 引入了新权限模型，但未详细说明与旧版权限模型的兼容性处理，以及应用如何在运行时处理权限被拒绝的情况。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：1

**具体问题：**
1. [P3] 采样率验证数据 — 文中提到 Battery saver 模式下 `PERFORMANCE_METRICS_ATOM` 的 `pull_count` 约为预期事件数的 10%，但缺少具体测试数据源和测试时间窗口的详细信息。

### 维度 6：交叉引用一致性
- 评分：5/5
- 问题数：0

## 统计
- P0 事实错误：0 处
- P1 重要缺失：3 处
- P2 建议改进：3 处
- P3 锦上添花：2 处
- 总体技术评分：3.5/5

## 闭环动作
- 写入 queue.json（P95）：0 处（不满足自动修复条件，P1问题涉及源码验证）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：3 处（P2改进建议）
- 仅日志记录：2 处（P3锦上添花）