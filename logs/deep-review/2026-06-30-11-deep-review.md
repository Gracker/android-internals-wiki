# 深度技术 Review · 2026-06-30 11:44

## Review 目标
- 章节：1.24 ResourcesManager 与 Configuration 变更性能
- 文件：src/part1-fundamentals/ch01-architecture/24-resourcesmanager-configuration-performance.md
- 状态：ready-for-review → task6_pending
- 模式：Task2B 修复后复审
- Android 版本边界：android-17.0.0_r1；未使用 Android 18 / API 38+ 资料。

## 外部 Review
- logs/external-review/ 根目录最近 3 天无该章节活跃文件；本轮未消费 external-review，未归档。

## 审查证据
- 已先执行 preflight：CANDIDATES 1 / IDLE_AUDIT 333 / TASK2B_BACKLOG 1。
- 已先读取 `/Users/gracker/.agents/skills/technical-writing/SKILL.md` 与 task9-deep-tech-review 规范。
- 一手源码锚点：`android-17.0.0_r1` 的 `ResourcesManager.java`、`ConfigurationController.java`、`ActivityThread.java`、`ActivityManagerService.java`、`ActivityTaskManagerService.java`、`WindowProcessController.java`、`ActivityRecord.java`、`DisplayContent.java`、`WindowToken.java`。
- 官方文档锚点：Android 17 behavior changes（大屏方向/resize/aspect ratio 限制、IME visibility）、manifest `activity` / `configChanges`、Per-app language preferences。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：2
- 具体问题：
  1. [P0][AUTO-FIX] L114-L130 — 传播图把 `ResourcesManager.applyConfigurationToResources()` 放在 ATMS 服务端决策链中。Android 17 中进程级配置由 `WindowProcessController.dispatchConfiguration()` 发送 `ConfigurationChangeItem`，客户端 `ActivityThread.handleConfigurationChanged()` / `ConfigurationController.handleConfigurationChanged()` 才调用 `ResourcesManager.applyConfigurationToResources()`。已改成进程级配置派发 + Activity 级 relaunch 判定两条路径。
  2. [P0][AUTO-FIX] L134-L148 — FixedRotation 描述仍使用 `DisplayAdjustments` 口径，并把 `ActivityRecord.ensureActivityConfiguration()` 写成 `WindowToken.applyFixedRotationTransform()` 的直接下一步。Android 17 直接链路是 `DisplayContent#setFixedRotationLaunchingApp()` → `startFixedRotationTransform()` → `WindowToken.applyFixedRotationTransform()` → `onFixedRotationStatePrepared()` → `WindowProcessController.registerActivityConfigurationListener()`。已修正正文和源码锚点。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P0][AUTO-FIX] L354-L357 — “多窗口拖拽 resize 不再触发 recreate”过度断言。Android 17 `ActivityRecord.shouldRelaunchLocked()` 仍按 `configChanges` mask 判定；未处理 `screenSize` / `windowConfiguration` 等变化时仍可能发送 `ActivityRelaunchItem`，resize-only 场景只是 preserve window 或推迟 relaunch。已改成条件化描述。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] L344-L349 — Per-app language 段把应用级 locale 写成每个 Activity 可独立设置。官方 `LocaleManager.setApplicationLocales()` / `AppCompatDelegate.setApplicationLocales()` 是应用级语言偏好；只有额外 override Context 才会形成多 locale ResourcesKey。已修正边界。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：上一轮要求补充的 `ActivityRecord.shouldRelaunchLocked()` 判定模型已在正文中落到 `info.getRealConfigChanged()`、PiP density skip、compat/resource overlay/recreate policy 等条件，未新增 P1 盲区。

### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P2] recreate 耗时和 ResourcesImpl 内存估算仍是工程量级，正文已明确“数据待验证”，本轮不重复写 suggestions；建议后续用 Perfetto + heap dump 替换为实测表。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：22.1、22.3、4.7 章节存在且主题匹配；本轮未发现章节号级交叉引用错误。

## Queue / 元数据修复
- [AUTO-FIX] `metadata/queue.json` 原本不是合法 JSON：1.23 pending placeholder 在 `material_paths` 后有非法尾逗号，且 1.23 staged install 问题错挂到 1.24；已恢复 JSON 可解析，并把明显 `status: completed` 的旧 1.24 Task9 条目移出 pending 列表。
- 1.24 仍有 Task6 写作 pending 条目，因此本轮不自动晋升 finalized。

## 统计
- P0 事实错误：3 处（均 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：2 处（1 处 auto-fix，1 处仅日志）
- P3 锦上添花：0 处
- 总体技术评分：3.8/5

## 闭环动作
- 章节正文 auto-fix：4 组（传播路径、FixedRotation、多窗口 resize、Per-app language）
- frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-06-30`
- 写入 queue.json：0 条新增；执行 1 次 JSON / 错挂条目修复
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：1 处（数据待验证仍保留）
- 自动晋升 finalized：0 章（queue 中仍有 1.24 Task6 pending）
