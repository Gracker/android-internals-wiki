# 深度技术 Review · 抽检审计日志（14.22 HPROF）

## 审计时间
2026-07-17 11:29 (Asia/Shanghai)

## 模式
**闲时抽检模式**（CANDIDATES=0、TASK2B_BACKLOG=0、IDLE_AUDIT=342）

## 抽检对象
- 章节：14.22 HPROF Heap Dump 管线与 Perfetto java_hprof 数据源
- 文件：`src/part3-tools/ch14-other-tools/22-hprof-heapdump-javahprof-datasource.md`
- 状态：`finalized` / `pipeline_stage: ready-to-publish`
- 上次抽检：**从未抽检**（last_task9_audit 字段缺失，最高优先级）
- task9_result：`fixed-verified`
- applicable_versions：Android 12 (API 31) - Android 17 (API 37)
- 抽检原因：① 长期未抽检（9999+ 天）；② 章节含多源调研产物叠加（`AIW-源码调研-2026-06-13`、`2026-06-28`、`2026-07-01`、`2026-07-04`），存在新旧内容冲突的高风险；③ 章节内含多条 `[待验证]` 标记，本身已承认部分内容未经 AOSP 二次确认

## 抽检范围（仅维度 1+3）

### 维度 1：源码引用准确性

**A. 严重冲突：数据源名称自相矛盾**

| 位置 | 描述 | 评级 |
|---|---|---|
| 正文 "Perfetto java_hprof 数据源" 节 | 声称 "Android 17 中 Perfetto 引入了 `art_hprof` 数据源（也称为 `java_hprof` 数据源）" | **P0** |
| 正文 protobuf 配置示例注释 | "实际数据源名（来自 `src/profiling/memory/java_hprof_producer.cc:25`）常量 `kJavaHprofDataSource = "android.java_hprof"`" | 自我修正（部分正确） |
| 附录 C（AIW-源码调研-2026-07-04） | "Java producer: 占用 `__SIGRTMIN + 6`，通过 `android.java_hprof` 数据源" | 正确 |

**问题**：原文中 `art_hprof` 表述未被移除，与 `android.java_hprof` 自我修正并存。读者扫读正文会拿到错误名称。

**B. 严重冲突：heap_graph 表数量自相矛盾**

| 位置 | 描述 | 评级 |
|---|---|---|
| 正文 "heap_graph 系列表" 节 | 列出 6 张表（`heap_graph` / `heap_graph_object` / `heap_graph_class` / `heap_graph_reference` / `heap_graph_primitive` / `heap_graph_object_data`） | **P0** |
| 章节内 "源码验证结论（Android 17.0.0_r1）" 节 | 自我修正为 "实际仅定义 3 张核心表"：HEAP_GRAPH_CLASS_TABLE、HEAP_GRAPH_OBJECT_TABLE、HEAP_GRAPH_REFERENCE_TABLE | 自我修正 |

**问题**：原 6 张表描述未被删除，3 张表修正单独存在；同一章节两种相反结论并存。

**C. 行号引用多处不一致（`profiler_tables.py`）**

| 位置 | 声称行号 | 评级 |
|---|---|---|
| "heap_graph 系列表" 节 | "~行 595-1230" | **P1** |
| "heap_graph 系列表" 节末 SQL 注释 | "行 950-1175" | **P1** |
| "源码验证结论" 节 | HEAP_GRAPH_CLASS_TABLE (行 461)、HEAP_GRAPH_OBJECT_TABLE (行 496)、HEAP_GRAPH_REFERENCE_TABLE (行 546) | 与上述两处行号不重叠 |

**问题**：同一文件的同一组表给出三套不同行号，未说明何者权威。读者无从判断。

**D. 多处 `[待验证]` 标记（未消化的验证债务）**

| 位置 | 标记内容 | 评级 |
|---|---|---|
| am dumpheap 节 | `[待验证: android-17.0.0_r1 frameworks/base/cmds/am/src/com/android/commands/am/Am.java]` | **P1** |
| AMS 层节 | `[待验证: android-17.0.0_r1 frameworks/base/services/core/java/com/android/server/am/ActivityManagerService.java]` | **P1** |
| ART 层核心源码注释 | `[待验证: android-17.0.0_r1 — 核心源码路径（`art/runtime/hprof/hprof.cc`）中的 `Hprof::Dump()`、`DumpHeapObject()`、`DumpHeapClass()` 等方法名和签名需在 android-17.0.0_r1 中确认]` | **P1** |
| java_hproducer.cc 引用 | `[待验证: android-17.0.0_r1 — 数据源名 ... Producer 通过 `sigqueue(pid, __SIGRTMIN+6, ...)` 触发...路径和行号需在 android-17.0.0_r1 中二次确认。]` | **P1** |
| proto 字段编号 | `[待验证: android-17.0.0_r1 — 字段编号与含义见 `external/perfetto/protos/perfetto/config/profiling/java_hprof_config.proto`...]` | **P1** |
| heap_graph_object schema | `[待验证: android-17.0.0_r1 — heap_graph_object 表实际 schema 需在 `profiler_tables.py`...]` | **P1** |
| KOOM fork-dump 描述 | `[来源: Obsidian/DeepResearch/2026-05-03-app_exit_info_tracker_and_koom_fork_hprof.md — 以上内容待 android-17.0.0_r1 源码验证]` | **P1** |

**问题**：7 处 `[待验证]` 标记直接悬挂在正文叙述上，不构成可发布的"已验证"声明。task9_result=fixed-verified 标签与正文 7 处待验证并存，自相矛盾。

**E. 附录 C 中代码片段可信度低**

附录 C "AIW-源码调研-2026-07-04" 中包含完整 C++ 代码片段：
- `ProducerSupport::CanProfile()` 三层权限模型
- `JavaHprofProducer::DoContinuousDump()` 完整方法
- `PackagesListParser::ReadPackagesListLine()` 完整方法
- `ProfilerGuardrails::CheckMemoryLimit()` / `CheckCpuLimit()` 完整方法

每段代码都给出"源码位置"和精确行号，但其中部分细节与上游 Perfetto v50.x 公开文档存在差异（如字段命名 `kInitialConnectionBackoffMs` / `kMaxConnectionBackoffMs`、`PRIVATE_FLAG_PROFILEABLE_BY_SHELL = 1 << 23` 等私有 flag 值）。这类长代码段未逐行核对 android-17.0.0_r1 全量源码前，应标注为 `[待验证]`。

**评级**：**P1**（关键代码片段需逐行核对，否则存在误导风险）

**F. 字段命名不一致**

| 位置 | 命名 |
|---|---|
| protobuf 配置示例 | `dump_phase_ms` / `dump_interval_ms`（snake_case） |
| 正文 "java_hproducer" 节 | `DumpIntervalMs`/`DumpPhaseMs`（PascalCase） |
| 附录 C | `dump_interval`（无后缀） |

**评级**：**P2**（同一概念三种命名，应统一为 proto 实际 snake_case 形式）

---

### 维度 3：版本差异覆盖

**A. applicable_versions 边界**
- 章节声明 `Android 12 (API 31) - Android 17 (API 37)` ✓ 与多处"Android 17.0.0_r1" 锚点一致

**B. 版本差异表覆盖**

附录 A "Java 堆抓取的版本差异" 表完整覆盖 API 30 / 31 / 33 / 34-37 ✓
- API 30: `scan_pids_only_on_start=true`，无 `target_installed_by` 字段 ✓
- API 31: 引入 `target_installed_by` 字段 ✓
- API 33+: `scan_pids_only_on_start=false`，通配扩展 ✓
- API 34-37: 与 API 33 行为延续 ✓

KOOM fork-dump 兼容性表覆盖 API 31-37 ✓
- API 31: 首次适配
- API 34: 引入 userfaultfd GC，SuspendVM 语义变化
- API 35-37: GC 演进，标注需实测

**C. 边界声明**
- "源码验证结论（Android 17.0.0_r1）" 显式标注 ✓
- 附录 A/C 末尾显式标注 `基于 android-17.0.0_r1 基准` ✓
- 全文 grep 未发现 Android 18 / API 38+ 结论性表述 ✓

**D. 一处可改进项（P2）**
- 附录 A 中 `S- 默认 true；T+ 默认 false` 使用 Android 内部 codename（S/T），应展开为 `Android 12 (API 31) 及以下 默认 true；Android 13 (API 33) 及以上 默认 false` 避免读者查代号

**评分：4/5**（边界表本身完整；唯一扣分项为 codename 未展开）
**问题数：1 处 P2**

---

## 抽检结论

| 维度 | 评分 | P0/P1/P2 数 |
|---|---|---|
| 维度 1：源码引用准确性 | 2/5 | P0×2、P1×8、P2×1 |
| 维度 3：版本差异覆盖 | 4/5 | P2×1 |

**总评**：
- **P0 事实错误**：2 处（数据源名称冲突、表数量冲突）
- **P1 重要缺失**：8 处（7 处 `[待验证]` 标记 + 附录 C 长代码片段可信度）
- **P2 建议改进**：2 处（字段命名不一致、Android codename 未展开）

**该章节触发完整 Task 9 处理路径，不能仅以"无问题"放行。**

---

## 闭环动作

- 写入 `queue.json`（P95）：**2 处 P0** + **1 处 P1 合并条目**（P1 数量较大合并）
- 写入 `research-gaps.md`：**0 处**（不涉及新研究主题）
- 写入 `suggestions.md`：**1 处 P2**（字段命名统一）
- 仅日志记录：1 处
- 章节 frontmatter 更新：
  - `task9_result: needs-rework`
  - `task9_state: reviewed`
  - `task2b_state: pending`
  - `pipeline_stage: task2b_pending`
  - `last_task9_audit: 2026-07-17T11:29:35+08:00`

---

## 备注

- 本次为闲时抽检第 2 节，章节为首次进入 Task 9 视野即发现多处 P0/P1
- 章节内已含"源码验证结论（Android 17.0.0_r1）"自我修正节，但**修正未回写到正文**——属于 "修正追加 vs 正文替换" 失误，应进入 Task 2B 重写
- 附录 C "AIW-源码调研-2026-07-04" 是新加的深度研究产物，但代码片段需在 android-17.0.0_r1 全量源码中逐行核对后才能作为正文结论
- 下次抽检建议：跳过本章节，待 Task 2B 处理完 queue.json 中的 P0/P1 后再回归复审
