# 深度技术 Review · 2026-07-02 21:29

## Review 目标
- 章节：15 Android 性能优化研究方法论
- 文件：src/ch15-methodology.md
- 状态：ready-for-review
- Android 基准：android-17.0.0_r1（Android 17 / API 37）
- 本轮模式：Task2B 修复后复审；仅使用 Android 17/API 37 及以下资料

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：3
- 具体问题：
  1. [P1][AUTO-FIX] §3.2 Android 14-17 命令说明 — 正文把 `perfetto -d` 写成“后台 detach 模式”，并给出 `perfetto --attach --stop`。android-17.0.0_r1 `external/perfetto/src/perfetto_cmd/perfetto_cmd.cc` 显示 `-d/--background` 是后台运行，`--detach=<key>` / `--attach=<key>` 是单独的 detached session，且 `--detach` 与 `--background` 互斥；`--attach` 需要 key。已修正语义和示例。
  2. [P1][AUTO-FIX] §4.3 主线程 Binder SQL — `thread_track.utid` 被错误关联到 `thread.id`。Perfetto schema 中线程表 join key 是 `thread.utid`；已改为 `JOIN thread ON t.utid = thread.utid`，并用 `is_main_thread` / `tid = pid` 判断主线程。
  3. [P1][AUTO-FIX] §4.3 heapprofd SQL — `heap_profile_allocation.callsite_id` 被直接 join 到 `stack_profile_frame.callsite_id`，但 Android 17 Perfetto schema 中 `callsite_id` 指向 `stack_profile_callsite`，再由 `frame_id` 指向 `stack_profile_frame`。已改为 allocation → callsite → frame，并用 `SUM(a.count)` 统计分配次数。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P1][AUTO-FIX] §4.3 帧内阶段 SQL — 原 SQL 用 `track.name GLOB '*Choreographer*'` 查帧内阶段，但 `Choreographer#doFrame` 是 `slice.name`，不是 thread track 名称；Perfetto `slice` 表已有 `parent_id` 表示父子 slice。已改成先取 `frame_id`，再用 `child.parent_id = frame.id` 展开直接子阶段。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 说明：Android 8/9/10-13/14-17 的 tracing 边界基本成立；本轮未使用 Android 18/API 38+ 资料。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0
- 说明：方法论章节覆盖问题分类、工具选择、trace_processor SQL、根因分析、验证与知识沉淀；没有新增 P1 盲区。

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：Task2B 已移除疑似编造案例数字；本轮未发现新的未标注性能数据断言。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] §3.1 工具表 — `ftrace trail` 不是稳定术语，已修为 `ftrace 事件`，与 Perfetto/AIW 其他章节口径一致。

## 统计
- P0 事实错误：0 处
- P1 重要缺失/技术错误：4 处（已 auto-fix）
- P2 建议改进：1 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：§3.1、§3.2、§4.3 共 5 处局部修正
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- Frontmatter：`task9_result=auto-fixed`、`task9_state=reviewed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_autofix_at=2026-07-02`

## 证据来源
- AOSP android-17.0.0_r1 `external/perfetto/src/perfetto_cmd/perfetto_cmd.cc`：`-d/--background`、`--detach=<key>`、`--attach=<key> [--stop]` 参数定义与互斥检查。
- AOSP android-17.0.0_r1 `external/perfetto/perfetto.rc`：`traced`、`traced_relay`、`traced_probes` 默认 disabled；`persist.traced.enable=1` 启动 traced/traced_probes。
- AOSP android-17.0.0_r1 `external/perfetto/src/trace_processor/tables/profiler_tables.py`：`heap_profile_allocation.callsite_id` → `stack_profile_callsite.id` → `stack_profile_frame.id`；`stack_profile_frame` 无 `callsite_id` 字段。
- Perfetto SQL prelude `slice` 表：`parent_id` 表示当前 slice 的直接父 slice，可用于展开 `Choreographer#doFrame` 子阶段。
