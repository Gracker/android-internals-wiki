# 深度技术 Review 闲时抽检 · 2026-05-23 01:30

## Preflight
- CANDIDATES: 0
- IDLE_AUDIT: 181
- 模式：闲时抽检（源码引用 + 版本差异快速扫描）
- external-review 活跃文件：无

## 抽检目标
- 章节：14.4 dumpsys 系列命令
- 文件：src/part3-tools/ch14-other-tools/04-dumpsys.md
- 状态：finalized → task2b_pending（发现 P0，写入 queue）
- 抽检原因：`last_task9_at=2026-04-30T18:33:37+08:00`，超过 21 天；上一轮 Task9 曾记录 P2 建议；文件当前无 pending queue 条目。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 抽检证据
- AOSP android-16.0.0_r1 `frameworks/native/services/surfaceflinger/FrontEnd/` 目录包含 `LayerHierarchy.h` / `LayerHierarchy.cpp`，不包含 `LayerHierarchyBuilder.h`；android16-qpr1-release 与 main 同样不包含该文件。
- `LayerHierarchy.h` 中定义 `class LayerHierarchyBuilder`，并提供 `update(LayerLifecycleManager&)`、`getHierarchy()`、`getOffscreenHierarchy()` 等接口。
- `SurfaceFlinger.cpp` 在 commit 路径中调用 `mLayerHierarchyBuilder.update(mLayerLifecycleManager)`，并将 `mLayerHierarchyBuilder.getHierarchy()` 传给 `LayerSnapshotBuilder::Args`；调用链存在，但正文给出的头文件路径错误。
- 其他关键源码锚点抽检通过：`frameworks/native/cmds/dumpsys/dumpsys.cpp` 存在；`SurfaceFlinger.cpp` 暴露 `--frontend`、`--hwclayers`、`--latency`，默认 dump 会输出 visible FrontEnd 后再追加 HWC minidump；`GraphicsStatsService.cpp` 输出 `Number Frame deadline missed`；`ProcessList.java` 中 Android 16 的 `FOREGROUND_APP_ADJ=0`、`VISIBLE_APP_ADJ=100`、`PERCEPTIBLE_APP_ADJ=200` 口径通过。

## 审查结果

### 维度 1：源码引用准确性
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P0] FrontEnd 架构深扒表格 / 源码索引 — `frameworks/native/services/surfaceflinger/FrontEnd/LayerHierarchyBuilder.h` 在 AOSP android-16.0.0_r1、android16-qpr1-release、main 均不存在。实际应为 `FrontEnd/LayerHierarchy.h`（定义 `class LayerHierarchyBuilder`）与 `LayerHierarchy.cpp`（实现）。
- 既有 P2 观察：上一轮记录的 `tryFastUpdate()` “0.5-1ms/帧”收益仍缺设备/trace/benchmark 条件，本轮为源码/版本抽检，不重复写 suggestions。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0
- 结论：Android 16 SurfaceFlinger dump 参数、default visible FrontEnd 输出、Frame deadline missed 统计、AMS/ProcessList 基础版本口径未发现新的 P0/P1。`LayerHierarchyBuilder.h` 是源码路径错误，不是单一 Android 16 版本差异；main 与 android16-qpr1-release 也同样不存在该文件。

## 统计
- P0 事实错误：1
- P1 重要缺失：0
- P2 建议改进：0（既有 P2 未重复登记）
- P3 锦上添花：0
- 总体技术评分：3.5/5（抽检范围评分）

## 闭环动作
- 写入 queue.json（P95）：1 条（task9-audit-20260523-14.4-layerhierarchybuilder-source-path）
- queue 动作：新增 pending 条目（task9-audit-20260523-14.4-layerhierarchybuilder-source-path）
- 更新 frontmatter：status=ready-for-review；task9_result=needs-rework；task2b_state=pending；pipeline_stage=task2b_pending；last_task9_audit=2026-05-23
- 写入 research-gaps.md：0
- 写入 suggestions.md：0（P0 已进入 queue；既有 P2 不重复写入）
- 自动晋升 finalized：无
