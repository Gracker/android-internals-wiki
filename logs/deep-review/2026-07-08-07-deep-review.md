# 深度技术 Review · 2026-07-08 07:30

## Review 目标
- 章节：2.30 Android 17 FrameTimeline GPU/CPU 合成边界判定机制
- 文件：src/part2-rendering/ch02-rendering/2.30-android17-frametimeline.md
- 状态：ready-for-review → task6_pending（AUTO-FIX 后回 Task6 复审）
- Android 基线：AOSP android-17.0.0_r1 / API 37，未使用 Android 18+ 资料

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：4（均 AUTO-FIX）
- 具体问题：
  1. [AUTO-FIX/P0] L123-L130 — FrameTimeline data source 写成 `surfaceflinger.frametimeline`，且把 `view` 写成独立 Perfetto data source。已改为 `android.surfaceflinger.frametimeline`、`android.surfaceflinger.frame` 与 `view/gfx` atrace category 三路信号。
  2. [AUTO-FIX/P0] L480-L488 — `createSurfaceFrameForToken(token)`、`setSfPresent(token, ...)`、`onCommitNotComposited(token)`、`finalizeCurrentDisplayFrame(token)` 与 Android 17 方法签名不一致。已按 `Scheduler/FrameTimeline.{h,cpp}` 修正。
  3. [AUTO-FIX/P1] L74-L88 — `GraphicsFrameEvent.BufferEventType` 表遗漏 `MODIFY/DETACH/ATTACH/CANCEL`，同时把 `HWC_COMPOSITION_QUEUED` 写成 Android 17 必然发射事件。已补齐 proto 枚举并标注 Android 17 未找到活跃发射点。
  4. [AUTO-FIX/P1] L109-L113 / L496 — App 侧 `vsyncId` 不是由 `Choreographer` 分配，而是从 `DisplayEventReceiver.VsyncEventData` 接收并选择 preferred timeline。已修正链路。

### 维度 2：原理链完整性
- 评分：4.3/5
- 问题数：2（均 AUTO-FIX）
- 具体问题：
  1. [AUTO-FIX/P1] L217-L230 — `HWC_COMPOSITION_QUEUED` 原段写成 `Output::present()` 为每个 HWC layer 发射该事件；android-17.0.0_r1 中 `FrameTracer.cpp` / `Layer.cpp` / `CompositionEngine/` 未检出活跃发射点。已改为事件边界说明，并给出 `FALLBACK_COMPOSITION`、`gpu_composition`、Layer trace / HWC dump 三类判断证据。
  2. [AUTO-FIX/P1] L234-L240 — release fence 原段写成 `hwc2_display.present()` 返回 release fence。已改为 `Display::presentFrame()` → `presentAndGetReleaseFences()` → `getLayerReleaseFence()`，并说明 GPU/client composition 时与 client target acquire fence 合并。

### 维度 3：版本差异覆盖
- 评分：4.2/5
- 问题数：0（Android 17 主线已校准）
- 说明：本轮只使用 android-17.0.0_r1；旧版本演进表未引入 Android 18/API 38+ 内容。`Scheduler/FrameTimeline.{h,cpp}` 已明确作为 Android 17 主线实现。

### 维度 4：知识盲区
- 评分：4.1/5
- 盲区数：0
- 说明：App vsyncId → SurfaceFrame → DisplayFrame 的主链路、SF gpu composition done fence、release fence 回收链路已闭合；OEM/GPU 差异仍保留为待实测边界，不写入 research-gaps。

### 维度 5：数据与案例支撑
- 评分：4.0/5
- 问题数：2（均 AUTO-FIX）
- 具体问题：
  1. [AUTO-FIX/P1] L311-L315 / L374 — trace 抓取只写 `gfx view`，会让读者误以为不需要显式 FrameTimeline data source。已补 `android.surfaceflinger.frametimeline` 与 `android.surfaceflinger.frame` 配置边界。
  2. [AUTO-FIX/P1] L393-L408 — SQL 把 `jank_type = 1/2` 解释成 App/SF jank，但 Android 17 proto 中 `jank_type` 是 bitmask，Perfetto SQL 表会展开为可读标签。已改为按 `jank_type` / `present_type` 直接分组。

### 维度 6：交叉引用一致性
- 评分：4.4/5
- 问题数：0
- 说明：2.4 / 2.6 的引用方向成立。本轮修复后，本节不再向其他章节传播错误 data source、方法签名或 release fence 口径。

## 统计
- P0 事实错误：0 处剩余（AUTO-FIX 2 处）
- P1 重要缺失：0 处剩余（AUTO-FIX 6 处）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.25/5

## 闭环动作
- AUTO-FIX：8 处，已直接修改章节正文
- 写入 queue.json（P95/P85）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task2b_state: fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`
- 自动晋升 finalized：否；本轮发生 AUTO-FIX，需回 Task6 复审

## 证据锚点
- AOSP android-17.0.0_r1：`frameworks/native/services/surfaceflinger/Scheduler/FrameTimeline.{h,cpp}`
- AOSP android-17.0.0_r1：`frameworks/native/services/surfaceflinger/FrameTracer/FrameTracer.{h,cpp}`
- AOSP android-17.0.0_r1：`frameworks/native/services/surfaceflinger/Layer.cpp`
- AOSP android-17.0.0_r1：`frameworks/native/services/surfaceflinger/SurfaceFlinger.cpp`
- AOSP android-17.0.0_r1：`frameworks/native/services/surfaceflinger/CompositionEngine/src/Display.cpp`
- AOSP android-17.0.0_r1：`external/perfetto/protos/perfetto/trace/android/graphics_frame_event.proto`
- AOSP android-17.0.0_r1：`external/perfetto/protos/perfetto/trace/android/frame_timeline_event.proto`
- Perfetto 官方文档：`https://perfetto.dev/docs/data-sources/frametimeline`
