# Task9 闲时抽检 · 2026-06-29 19:26

## Review 目标
- 章节：5.4 DVFS 与功耗管理
- 文件：src/part1-fundamentals/ch05-cpu-power/04-dvfs.md
- 状态：finalized
- 模式：闲时抽检（维度 1 源码引用 + 维度 3 版本差异）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.4/5
- 问题数：1
- 具体问题：
  1. [P1][版本锚点过期] 章节尾部“游戏调度框架与 Power HAL 协同”仍声明 AOSP `android-17.0.0_r1` tag 未发布，并把 GameManagerService / PowerManagerService / Power HAL AIDL 锚点停在 `android-16.0.0_r1`。2026-06-29 复核 `platform/frameworks/base` 与 `platform/hardware/interfaces` 均已发布 `android-17.0.0_r1`，该说明已违反当前 Android 17 基线。

### 维度 3：版本差异覆盖
- 评分：4.2/5
- 问题数：1
- 具体问题：
  1. [P1][Android 17 基线缺口] framework / Power HAL 路径可直接升级到 `android-17.0.0_r1`；kernel/common 未命中 `android17-6.12` 分支或 `android-17.0.0_r1` tag，因此 schedutil / SCMI 代码段保留 ACK 分支边界，不写成 Android 17 新增行为。

## AUTO-FIX
- 更新 frontmatter：`last_verified`、`last_verified_against`、`sources`、`task9_result: auto-fixed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at`、`last_task9_audit`、`last_task9_review_log`。
- 正文中将 GameManagerService、PowerManagerService、Power HAL AIDL 的源码锚点从 `android-16.0.0_r1` 升级为 `android-17.0.0_r1`。
- 修正 JNI 路径为 `frameworks/base/services/core/jni/com_android_server_power_PowerManagerService.cpp`，补充 Power HAL AIDL `Mode.aidl` 路径。
- 删除“Android 17 tag 未公开 / 待 tag 公开后复核”的过期结论，改为 Android 17 已复核 + kernel ACK 分支边界说明。

## 证据来源
- `git ls-remote --tags https://android.googlesource.com/platform/frameworks/base refs/tags/android-17.0.0_r1` 命中 `23149ba144e850fe494a91ffa13f8019455c0804`。
- `platform/frameworks/base @ android-17.0.0_r1`：`GameManagerService#setGameState()` / `notifyGraphicsEnvironmentSetup()` 仍通过 `PowerManagerInternal.setPowerMode(Mode.GAME_LOADING, ...)` 下发 loading mode。
- `platform/frameworks/base @ android-17.0.0_r1`：`PowerManagerService#setPowerModeInternal()` → JNI `nativeSetPowerMode()` → Power HAL controller `setMode()` 路径存在。
- `platform/hardware/interfaces @ android-17.0.0_r1`：`power/aidl/android/hardware/power/Mode.aidl` 存在 `GAME`、`GAME_LOADING`、`CAMERA_STREAMING_LOW/MID/HIGH/SECURE`；`IPower.aidl` 暴露 `setMode(in Mode type, in boolean enabled)`。
- `git ls-remote https://android.googlesource.com/kernel/common refs/heads/android17-6.12 refs/tags/android-17.0.0_r1` 未命中；kernel 段落保留 android15-6.6 / android16-6.12 ACK 边界。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.3/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- AUTO-FIX：1 处
- 回到 Task6 复审：是
