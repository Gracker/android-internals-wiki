# 深度技术 Review · 2026-06-22 13:35

## Review 目标
- 章节：20.12 SafeMode 崩溃循环判定与启动补偿链路
- 文件：src/part5-app/ch20-stability/12-safemode-crash-loop-recovery.md
- 状态：finalized（Task6 13:14 已晋升；本轮 Task9 复审后因 auto-fix 回到 task6_pending）
- external-review：无（logs/external-review 根目录本轮未发现活跃文件）

## 审查结果

### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：2（均已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] `ApplicationExitInfo.timestamp` 时间口径 — AOSP android-17.0.0_r1 将 `mTimestamp` 标注为 `@CurrentTimeMillisLong`，正文却建议统一只写 `elapsedRealtime()` marker，和表格/附录的 `started_wall_time_ms` 匹配逻辑冲突。已改为同时保存 elapsed/wall，AOSP 匹配走墙钟，elapsed 只用于本地过期判断。
  2. [P2][AUTO-FIX] `tombstoned` 提交流程表述 — android-17.0.0_r1 `rename_tombstone_fd()` 是先 `unlinkat` 清旧路径，再 `linkat` 提交 `O_TMPFILE` inode。正文多处写成 `linkat + unlink`，已统一为 `unlinkat` + `linkat`。

### 维度 2：原理链完整性
- 评分：4/5
- 问题数：0
- 说明：SafeMode marker、退出证据补偿、WebView Renderer gone、文件持久化边界和 AppExitInfoTracker 状态机链路闭合。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1（已 AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] `REASON_USER_STOPPED` 版本边界 — 正文写 Android 13 已有该常量，容易误导为 API 33 才可用；AOSP android-11.0.0_r1 已存在 `REASON_USER_STOPPED`，`REASON_PACKAGE_STATE_CHANGE` 则在 Android 14 引入。已修正为 Android 11(API 30) / Android 14 的边界。

### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0
- 说明：实践案例和 AOSP 源码锚点足够支撑当前机制设计；OEM timestamp 口径已改为有 AOSP 边界的经验提示。

### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0
- 说明：20.7、20.8、20.10、26.2 等交叉引用未发现阻断性错误。

## 统计
- P0 事实错误：2 处（均已 auto-fix）
- P1 重要缺失：0 处
- P2 建议改进：1 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4/5

## 闭环动作
- AUTO-FIX：3 处
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 仅日志记录：0 处
- frontmatter：task9_result=auto-fixed，task9_state=reviewed，task6_state=revisiting，pipeline_stage=task6_pending
- 自动晋升 finalized：0（本轮有 auto-fix，需 Task6 复审）

## 一手证据
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/app/ApplicationExitInfo.java`：`mTimestamp` 标注 `@CurrentTimeMillisLong`。
- AOSP android-11.0.0_r1 / android-17.0.0_r1 `ApplicationExitInfo.java`：`REASON_USER_STOPPED` 在 Android 11 已存在；`REASON_PACKAGE_STATE_CHANGE` 在 Android 14+ 存在。
- AOSP android-17.0.0_r1 `system/core/debuggerd/tombstoned/tombstoned.cpp`：`rename_tombstone_fd()` 先 `unlinkat()`，再对 `/proc/self/fd/<fd>` 调用 `linkat()`。
