# Task 9 闲时抽检 · 2026-07-10 05:29

## Audit 目标
- 章节：21.3 ContentProvider 启动治理
- 文件：src/part5-app/ch21-startup/03-contentprovider-optimization.md
- 状态：finalized
- 抽检原因：无 `task9_state: pending` 候选，Task2B backlog=0；本节 `last_task9_at=2026-05-13T08:40:35+08:00` 超过 21 天，且历史 Review 曾有 remote Provider 进程语义 P2。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 一手资料复核
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/app/ActivityThread.java`：`data.info.makeApplicationInner(...)` 位于 L8260，`installContentProviders(app, data.providers)` 位于 L8293，`mInstrumentation.callApplicationOnCreate(app)` 位于 L8308。
- AOSP android-17.0.0_r1 `frameworks/base/core/java/android/view/ViewTreeObserver.java`：`removeOnDrawListener()` 在 `mInDispatchOnDraw` 时构造 `IllegalStateException`；`targetSdkVersion >= Build.VERSION_CODES.O` 时抛出，否则记录日志。
- AndroidX Startup 1.2.0 source jar：`InitializationProvider.onCreate()` 调用 `AppInitializer.getInstance(context).discoverAndInitialize(getClass())`；`AppInitializer` 通过 `Initializer.dependencies()` 递归初始化并检测 cycle，`initializeComponent()` 支持手动初始化。
- 未使用 Android 18 / API 38+ 资料。

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：2
- 具体问题：
  1. [P1][AUTO-FIX] frontmatter / L81 — 本节适用 Android 17，但 `last_verified_against` 与正文验证锚点仍停在 `android-16.0.0_r1`。已改为 `android-17.0.0_r1`，并补入 ActivityThread 行号。
  2. [P2][AUTO-FIX] 表格“跨进程唤醒” — 原表述容易读成主进程冷启动会自动安装 remote Provider。已改为 remote Provider 被同步访问或目标进程启动时才拉起子进程，并把治理方向收窄为避免主进程同步访问触发预热。

### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：1
- 具体问题：
  1. [P2][AUTO-FIX] AndroidX Startup 来源未固定 artifact 版本。已将 source 锚点固定到 `androidx.startup:startup-runtime:1.2.0`，避免后续源码漂移。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（已 auto-fix）
- P2 建议改进：2 处（已 auto-fix）
- P3 锦上添花：0 处
- 总体技术评分：4.5/5（auto-fix 后）

## 闭环动作
- 正文 / frontmatter auto-fix：3 处
- 更新 frontmatter：`task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`、`last_task9_autofix_at: 2026-07-10`、`last_task9_audit: 2026-07-10`
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 自动晋升 finalized：0 处（auto-fix 后回到 Task6 复审）
