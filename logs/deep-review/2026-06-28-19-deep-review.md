# 深度技术 Review · 2026-06-28 19:30

## Review 目标
- 章节：26.12 Android 版本化线上诊断能力：ApplicationExitInfo、ProfilingManager 与 ProfilingTrigger
- 文件：src/part5-app/ch26-observability/12-versioned-diagnostics.md
- 状态：ready-for-review
- 章节：1.26 DeliQueue 无锁队列源码解析（AOSP android-17.0.0_r1）
- 文件：src/part1-fundamentals/ch01-architecture/01.26-messagqueue-deliqueue-optimization.md
- 状态：ready-for-review

## 审查结果

### 26.12 Android 版本化线上诊断能力

#### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：3（AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] ProfilingTrigger 表格仍写 API 37 trigger “AOSP 源码未命中”。已按 android-17.0.0_r1 `packages/modules/Profiling/framework/java/android/os/ProfilingTrigger.java` 修正为常量可见、feature flag gate。
  2. [P0][AUTO-FIX] `isValidRequestTriggerType()` 代码块写成 `return triggerType >= TRIGGER_TYPE_NONE`。源码实际为 `APP_FULLY_DRAWN` / `ANR` 直接放行，其余 trigger 逐项按 feature flag 判定。已替换为 android-17.0.0_r1 逻辑。
  3. [P1][AUTO-FIX] `REASON_APPLICATION_SPECIFIC_ERROR` 与待复核项仍引用 AOSP main / master。已改为 android-17.0.0_r1 结论，并标注该常量未进入 Android 17 公开 reason 列表。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：1（AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] `TRIGGER_TYPE_ANOMALY` 与 MemoryLimiter 的映射写得过满。官方 features 只确认 OS-defined memory limits / binder spam 示例，具体归因需结合 `ProfilingResult#getTag()`、文件类型与 `ApplicationExitInfo` 描述。已补边界。

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0（auto-fix 后通过）

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：1（仅日志）
- 说明：MemoryLimiter 的设备样本、description/subreason 组合仍需后续素材补证，不在本轮改写。

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：0

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：1（AUTO-FIX）
- 具体问题：
  1. [P2][AUTO-FIX] frontmatter `related_chapters` 引号结构不可解析。已改为标准数组字面量，避免后续自动化读取失败。

### 1.26 DeliQueue 无锁队列源码解析

#### 维度 1：源码引用准确性
- 评分：4/5
- 问题数：3（AUTO-FIX）
- 具体问题：
  1. [P0][AUTO-FIX] Tombstone 机制写成 `markRemoved()` 设置 `FLAG_IN_USE`。android-17.0.0_r1 `Message.java` 实际设置 `FLAG_REMOVED`。已修正。
  2. [P0][AUTO-FIX] Perfetto `message_queue_receive` 被写成可观察 `heapSweep()` / `mStack.peek()` 子阶段。android-17.0.0_r1 `Looper.loopOnce()` 在 `mQueue.next()` 返回后才 begin slice，slice 包住后续 `dispatchMessage()`。已修正诊断方法。
  3. [P1][AUTO-FIX] FAQ 写 `mMessages` 字段不复存在并可能 `NoSuchFieldException`。android-17.0.0_r1 `CombinedDeliMessageQueue/MessageQueue.java` 仍保留 legacy `mMessages` 字段；DeliQueue 模式下它不是权威队列数据源。已修正。

#### 维度 2：原理链完整性
- 评分：4/5
- 问题数：2（AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] 删除流程写成 `heapSweep()` 是唯一遍历并重构链路的路径。源码实际为 `moveMatchingToFreelist()` 标记、`drainFreelist()` / `mStack.remove()` unlink，`heapSweep()` 建 backlink 并过滤 removed message。已改。
  2. [P2][AUTO-FIX] 入队段落写“无失败路径”。源码中 `pushMessage()` 在 quitting 时返回 false，唤醒状态也有 `mWaitState` CAS 循环。已补边界。

#### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：0（本轮锚定 android-17.0.0_r1，Android 16 仅作历史对照）

#### 维度 4：知识盲区
- 评分：4/5
- 盲区数：0

#### 维度 5：数据与案例支撑
- 评分：4/5
- 问题数：1（AUTO-FIX）
- 具体问题：
  1. [P1][AUTO-FIX] Perfetto SQL 解释把 dispatch slice 当作队列迁移耗时。已改为“message dispatch slice，不等同于取队列耗时”，并说明 heapSweep 需队列长度监控或临时插桩。

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

## 统计
- P0 事实错误：4 处（均 auto-fixed）
- P1 重要缺失/错误边界：5 处（均 auto-fixed）
- P2 建议改进：2 处（均 auto-fixed 或仅日志）
- P3 锦上添花：1 处
- 总体技术评分：4/5

## 闭环动作
- 写入 queue.json（P95）：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 直接 auto-fix：2 个章节
- frontmatter：2 个章节更新为 `task9_result: auto-fixed`、`task9_state: reviewed`、`task6_state: revisiting`、`pipeline_stage: task6_pending`
- 自动晋升 finalized：0 个
