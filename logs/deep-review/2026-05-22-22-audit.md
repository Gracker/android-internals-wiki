# Task 9 闲时抽检 · 2026-05-22 22:20

## 抽检目标
- 章节：2.2 帧率与刷新率
- 文件：src/part1-fundamentals/ch02-rendering/02-framerate.md
- 状态：finalized / task9_result=pass-tech-review（抽检前）
- 模式：闲时抽检（聚焦维度 1 源码引用 + 维度 3 版本差异）
- external-review：最近 3 天未发现匹配 2.2 / 帧率与刷新率 / 02-framerate 的活跃文件

## 审查结果

### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P0] L263 — `LayerHistory::getSnapshot()` 在 AOSP android-16.0.0_r1 中不存在；当前实现是 `LayerHistory::summarize()`，由 `Scheduler::chooseRefreshRateForContent()` 调用后进入 policy。
  2. [P0] L682-L692 — `LayerVoteType 有 7 种类型` 与 Android 16 源码不一致；`RefreshRateSelector.h` 当前有 9 类，漏掉 `ExplicitGte` 与 `ExplicitCategory`。

### 维度 3：版本差异覆盖
- 评分：3/5
- 问题数：1
- 具体问题：
  1. [P1] L691-L695 — `ExplicitExact` 被写成固定 `divisor==1`；Android 16 `calculateLayerScoreLocked()` 在 `supportsAppFrameRateOverrideByContent()` 为 true 时允许 `divisor > 0`。

## 证据锚点
- `frameworks/native/services/surfaceflinger/Scheduler/LayerHistory.cpp`（android-16.0.0_r1）：`LayerHistory::summarize()` L192-L229 汇总 active layer votes。
- `frameworks/native/services/surfaceflinger/Scheduler/Scheduler.cpp`（android-16.0.0_r1）：`Scheduler::chooseRefreshRateForContent()` L747-L756 调用 `mLayerHistory.summarize(...)` 并写入 policy。
- `frameworks/native/services/surfaceflinger/Scheduler/RefreshRateSelector.h`（android-16.0.0_r1）：`LayerVoteType` L152-L166 共 9 类，包含 `ExplicitGte` / `ExplicitCategory`。
- `frameworks/native/services/surfaceflinger/Scheduler/RefreshRateSelector.cpp`（android-16.0.0_r1）：`calculateLayerScoreLocked()` L449-L458 对 `ExplicitExact` 的判定受 `supportsAppFrameRateOverrideByContent()` 影响。
- Android Developers docs：`View.setRequestedFrameRate(float)` / `Window.setFrameRatePowerSavingsBalanced(boolean)` 均为 API 35，说明 Android 15+ category vote 是当前章节必须覆盖的版本口径。

## 闭环动作
- 写入 queue.json（P95）：1 条（含 P0 2 处、P1 1 处）
- 更新章节 frontmatter：`task9_result=needs-rework`、`task9_state=reviewed`、`task2b_state=pending`、`pipeline_stage=task2b_pending`、`last_task9_audit=2026-05-22`
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处

## 统计
- P0 事实错误：2 处
- P1 重要缺失：1 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2.5/5
