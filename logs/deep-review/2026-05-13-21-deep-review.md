# 深度技术 Review · 2026-05-13 21:57

## Review 目标
- 章节：2.19 刷新率切换与帧率适配性能
- 文件：src/part1-fundamentals/ch02-rendering/19-refresh-rate-switching.md
- 状态：ready-for-review（Task9 needs-rework，回到 Task2B）
- 章节：2.21 文字渲染性能
- 文件：src/part1-fundamentals/ch02-rendering/21-text-rendering-performance.md
- 状态：ready-for-review（Task9 needs-rework，回到 Task2B）
- 章节：3.1 Input 事件分发全流程
- 文件：src/part1-fundamentals/ch03-input/01-input-dispatch.md
- 状态：ready-for-review（Task9 needs-rework，回到 Task2B）

## external-review 输入
- `logs/external-review/` 根目录无活跃文件；本轮未消费 external-review，未执行归档。

## 审查结果

### 2.19 刷新率切换与帧率适配性能

#### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：3
  1. [P0] L197-L205 HWC 4.0 预判式切换 — `expectedPresentTime` 字段实际在 `hardware/interfaces/graphics/composer/aidl/.../DisplayCommand.aidl`，android-13.0.0_r1 已存在；正文写成 Android 16 强制 AIDL V4 且锚到 `IComposerClient.aidl`，版本和文件都不对。 建议：改为“Composer AIDL 的 DisplayCommand.expectedPresentTime 允许 SurfaceFlinger 传递目标 present 时间”；如要讨论 Android 15/16，再单独核对 `IComposerClient.notifyExpectedPresent` / VRR early hint，不要把二者合并。
  2. [P0] L304-L312 / L384 VsyncModulator 原子化相位切换 — AOSP android-16.0.0_r1 `VsyncModulator.cpp` 中 `setVsyncConfigSet()` 仍是 `std::lock_guard<std::mutex>` 后按值写入 `mVsyncConfigSet`；未见 `std::atomic<std::shared_ptr<VsyncConfigSet>>`、`atomic_load/store` 路径。 建议：删除“Android 16 原子化相位切换”“纳秒级确定性”等断言，改写为当前源码可验证的 mutex 更新模型；若引用未来分支，必须标注具体 commit/tag。
  3. [P1] L316-L328 FrameRateParams 表格 — `Surface.FrameRateParams` / `setFrameRate(FrameRateParams)` 在 android-16.0.0_r1 带 `@FlaggedApi(FLAG_ARR_SETFRAMERATE_API)`；正文把它写成 Android 15-QPR1+ 的通用 App 侧用法，缺少 flag/SDK 可用性边界。 建议：表格补“flagged API/设备与 SDK 开关”说明；普通三方 App 的稳定路径仍以 `Surface.setFrameRate(float,int[,int])`、`View.setRequestedFrameRate()`、`Display.getSuggestedFrameRate()` 等公开 API 为主。

#### 维度 2：原理链完整性
- 评分：3/5
- 问题数：0（本轮主要命中源码/版本/数据层错误）

#### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
  1. [P0/P1] L197-L205 HWC 4.0 预判式切换 — `expectedPresentTime` 字段实际在 `hardware/interfaces/graphics/composer/aidl/.../DisplayCommand.aidl`，android-13.0.0_r1 已存在；正文写成 Android 16 强制 AIDL V4 且锚到 `IComposerClient.aidl`，版本和文件都不对。 建议：改为“Composer AIDL 的 DisplayCommand.expectedPresentTime 允许 SurfaceFlinger 传递目标 present 时间”；如要讨论 Android 15/16，再单独核对 `IComposerClient.notifyExpectedPresent` / VRR early hint，不要把二者合并。
  2. [P0/P1] L316-L328 FrameRateParams 表格 — `Surface.FrameRateParams` / `setFrameRate(FrameRateParams)` 在 android-16.0.0_r1 带 `@FlaggedApi(FLAG_ARR_SETFRAMERATE_API)`；正文把它写成 Android 15-QPR1+ 的通用 App 侧用法，缺少 flag/SDK 可用性边界。 建议：表格补“flagged API/设备与 SDK 开关”说明；普通三方 App 的稳定路径仍以 `Surface.setFrameRate(float,int[,int])`、`View.setRequestedFrameRate()`、`Display.getSuggestedFrameRate()` 等公开 API 为主。

#### 维度 4：知识盲区
- 评分：3/5
- 盲区数：0（无独立 research-gap，问题可由 Task2B 按源码核对修复）

#### 维度 5：数据与案例支撑
- 评分：3/5
- 问题数：0

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

### 2.21 文字渲染性能

#### 维度 1：源码引用准确性
- 评分：2/5
- 问题数：3
  1. [P0] L147-L149 / L394 HarfBuzz 10.x 性能数字 — AOSP android-16.0.0_r1 `external/harfbuzz_ng/NEWS` 对应 10.2.0；未找到“阿拉伯语 Nastaliq 45%”“AAT 60%”这组结论。NEWS 中相近数字分别指 Noto Duployan shaping、SourceHanSans-VF subsetting 或 AAT short words up to 4%，正文把对象和比例混写。 建议：删除或重写具体百分比；只保留可从 HarfBuzz NEWS 直接核对的条目，并写清 shaping / subsetting / loading 的不同口径。
  2. [P0] L330-L336 / L394 Variation Axes 缓存 — 正文宣称 Android 16 Minikin 为 Variation Axes 中间结果引入缓存、动态字重开销降低 40%，但当前可核验材料只显示 Minikin 支持 variation family/axis；HarfBuzz 4.4 NEWS 有 variable fonts shaping cache，未指向 Android 16 Minikin，也没有 40% 结论。 建议：把该段降级为 `[待验证]` 或删除“Android 16 / Minikin / 40%”断言；若保留，需要补具体 AOSP commit、benchmark 条件和指标。
  3. [P0] L395 / L404-L419 Android 17 排版 API — Android Developers `StaticLayout.Builder` 文档显示 `setUseBoundsForWidth(boolean)`、`setShiftDrawingOffsetForStartOverhang(boolean)` Added in API 35；正文写成 Android 17/API 37，且正文列表中的方法名漏掉 `set` 前缀。 建议：把版本改为 Android 15/API 35，并统一使用真实方法名；“android-17.0.0_r1 已验证”锚点需要删除或替换为可访问 tag/官方 API 文档。

#### 维度 2：原理链完整性
- 评分：3/5
- 问题数：0（本轮主要命中源码/版本/数据层错误）

#### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：1
  1. [P0/P1] L395 / L404-L419 Android 17 排版 API — Android Developers `StaticLayout.Builder` 文档显示 `setUseBoundsForWidth(boolean)`、`setShiftDrawingOffsetForStartOverhang(boolean)` Added in API 35；正文写成 Android 17/API 37，且正文列表中的方法名漏掉 `set` 前缀。 建议：把版本改为 Android 15/API 35，并统一使用真实方法名；“android-17.0.0_r1 已验证”锚点需要删除或替换为可访问 tag/官方 API 文档。

#### 维度 4：知识盲区
- 评分：3/5
- 盲区数：0（无独立 research-gap，问题可由 Task2B 按源码核对修复）

#### 维度 5：数据与案例支撑
- 评分：2/5
- 问题数：2
  1. [P1] L147-L149 / L394 HarfBuzz 10.x 性能数字 — AOSP android-16.0.0_r1 `external/harfbuzz_ng/NEWS` 对应 10.2.0；未找到“阿拉伯语 Nastaliq 45%”“AAT 60%”这组结论。NEWS 中相近数字分别指 Noto Duployan shaping、SourceHanSans-VF subsetting 或 AAT short words up to 4%，正文把对象和比例混写。 建议：删除或重写具体百分比；只保留可从 HarfBuzz NEWS 直接核对的条目，并写清 shaping / subsetting / loading 的不同口径。
  2. [P1] L330-L336 / L394 Variation Axes 缓存 — 正文宣称 Android 16 Minikin 为 Variation Axes 中间结果引入缓存、动态字重开销降低 40%，但当前可核验材料只显示 Minikin 支持 variation family/axis；HarfBuzz 4.4 NEWS 有 variable fonts shaping cache，未指向 Android 16 Minikin，也没有 40% 结论。 建议：把该段降级为 `[待验证]` 或删除“Android 16 / Minikin / 40%”断言；若保留，需要补具体 AOSP commit、benchmark 条件和指标。

#### 维度 6：交叉引用一致性
- 评分：4/5
- 问题数：0

### 3.1 Input 事件分发全流程

#### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：5
  1. [P0] L155-L205 / L253-L263 / L1078-L1130 InputFlinger Rust 架构 — android-16.0.0_r1 确有 `services/inputflinger/rust/`，但它是 InputFilter 的 Rust 实现及 bounce/slow/sticky keys 等辅助输入过滤器；`InputReader`、`InputDispatcher` 仍是 C++。正文写成 InputFlinger 核心从 C++ 到 Rust、`InputDispatcher::notifyKey(..., KeyEntry&)` 调 `mInputFilter->filter(entry)`，在 `InputDispatcher.cpp` 中找不到该接口/调用。后文 `services/core/inputflinger/`、`InputDispatcher::predictiveBackHandling()` 也不存在。 建议：删除“核心重构/替代传统 C++”叙述，改为“Android 16 引入 Rust InputFilter wrapper”；保留 Rust 内容时只写 `InputFilter.cpp/.h`、`rust/input_filter.rs`、accessibility filter 的真实边界。
  2. [P0] L376-L382 Android 16 AOT 返回键预判拦截 — android-16.0.0_r1 `InputDispatcher.cpp` 中没有 `OnBackInvokedCallback`、`BackNavigationController`、`predictiveBackHandling` 等符号；把返回 callback 适配写成 InputDispatcher 分发前检查目标窗口 callback，会误导读者。 建议：按平台实际返回分发链重写：区分 KeyEvent BACK、预测性返回/OnBackInvokedDispatcher 的 framework/window 路径和 InputDispatcher 的按键分发边界；未核实前标 `[待验证]`。
  3. [P0] L520-L522 Android 17 DeliQueue 对输入响应的加速 — 段落宣称 DeliQueue 让输入事件入队/出队走无锁路径并减少 15% 锁等待、掉帧率下降 4%，但未给出 AOSP tag/commit、类名、trace 指标或 benchmark 条件；该段直接影响 App 侧 InputChannel/Looper 原理判断。 建议：删除具体百分比和“已实现”语气；若保留，补 `MessageQueue`/`Looper`/`WindowInputEventReceiver` 的真实源码锚点和对照 trace。
  4. [P0] L820-L840 AnrTracker 的 O(1) 优化（Android 14+） — `services/inputflinger/dispatcher/AnrTracker.cpp` 在 android-12.0.0_r1 已存在；实现是按 timeout 排序的容器，提供 `insert/erase/eraseToken/firstTimeout/firstToken`，不是 Android 14+ 新增，也没有正文暗示的 `findExpired()` API。 建议：改成 Android 12+ 已有 AnrTracker，并说明它用最早 timeout 驱动下一次 ANR 检查；不要写 O(1) 全流程优化或 Android 14+ 引入。
  5. [P0] L1067 WOULD_BACK — `WOULD_BACK` 不是 InputTransport/InputDispatcher 返回码；正文上方源码使用的是 `WOULD_BLOCK`。 建议：统一改为 `WOULD_BLOCK`，并说明这是 socket/pipe 满时的 backpressure 信号。

#### 维度 2：原理链完整性
- 评分：2/5
- 问题数：0（本轮主要命中源码/版本/数据层错误）

#### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
  1. [P0/P1] L376-L382 Android 16 AOT 返回键预判拦截 — android-16.0.0_r1 `InputDispatcher.cpp` 中没有 `OnBackInvokedCallback`、`BackNavigationController`、`predictiveBackHandling` 等符号；把返回 callback 适配写成 InputDispatcher 分发前检查目标窗口 callback，会误导读者。 建议：按平台实际返回分发链重写：区分 KeyEvent BACK、预测性返回/OnBackInvokedDispatcher 的 framework/window 路径和 InputDispatcher 的按键分发边界；未核实前标 `[待验证]`。
  2. [P0/P1] L820-L840 AnrTracker 的 O(1) 优化（Android 14+） — `services/inputflinger/dispatcher/AnrTracker.cpp` 在 android-12.0.0_r1 已存在；实现是按 timeout 排序的容器，提供 `insert/erase/eraseToken/firstTimeout/firstToken`，不是 Android 14+ 新增，也没有正文暗示的 `findExpired()` API。 建议：改成 Android 12+ 已有 AnrTracker，并说明它用最早 timeout 驱动下一次 ANR 检查；不要写 O(1) 全流程优化或 Android 14+ 引入。

#### 维度 4：知识盲区
- 评分：3/5
- 盲区数：0（无独立 research-gap，问题可由 Task2B 按源码核对修复）

#### 维度 5：数据与案例支撑
- 评分：1/5
- 问题数：1
  1. [P1] L520-L522 Android 17 DeliQueue 对输入响应的加速 — 段落宣称 DeliQueue 让输入事件入队/出队走无锁路径并减少 15% 锁等待、掉帧率下降 4%，但未给出 AOSP tag/commit、类名、trace 指标或 benchmark 条件；该段直接影响 App 侧 InputChannel/Looper 原理判断。 建议：删除具体百分比和“已实现”语气；若保留，补 `MessageQueue`/`Looper`/`WindowInputEventReceiver` 的真实源码锚点和对照 trace。

#### 维度 6：交叉引用一致性
- 评分：3/5
- 问题数：0

## 一手核对锚点
- HWC：`DisplayCommand.aidl` 的 `expectedPresentTime` 在 android-13.0.0_r1 已存在；android-15/16 的 `IComposerClient.aidl` 另有 `notifyExpectedPresent` 早期提示接口。
- SurfaceFlinger：android-16.0.0_r1 `VsyncModulator::setVsyncConfigSet()` 仍使用 `std::lock_guard<std::mutex>`，未见 atomic shared_ptr。
- Text：Android Developers `StaticLayout.Builder#setUseBoundsForWidth` / `setShiftDrawingOffsetForStartOverhang` 均为 API 35；HarfBuzz 10.2.0 NEWS 未支撑正文写的 45%/60% 组合。
- Input：android-16.0.0_r1 `services/inputflinger/rust/` 是 InputFilter/辅助输入过滤器；`InputDispatcher.cpp` 无 `mInputFilter->filter(entry)`、`OnBackInvokedCallback`、`predictiveBackHandling` 符号；`AnrTracker.cpp` Android 12 已存在。

## 统计
- P0 事实错误：9 处
- P1 重要缺失：2 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：2/5

## 闭环动作
- 写入 queue.json（P95）：3 条（2.19、2.21、3.1）
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- frontmatter：3 个章节均更新为 `status: ready-for-review`、`task9_state: reviewed`、`task9_result: needs-rework`、`task2b_state: pending`、`pipeline_stage: task2b_pending`
- 自动晋升 finalized：无（3 个章节均存在 P0/P1）
- external-review 归档：无活跃文件，未执行归档

## 下一 Review 候选
- 5.4 DVFS 与功耗管理
- 5.9 ADPF 自适应性能框架
- 21.5 Splash Screen 与感知启动速度
