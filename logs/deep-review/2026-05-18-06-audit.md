# 深度技术抽检 · 2026-05-18 06:20

## Review 目标
- 章节：20.3 Native Crash 分析与治理
- 文件：src/part5-app/ch20-stability/03-native-crash-governance.md
- 状态：finalized（闲时抽检；last_task9_at 缺失；曾有 P2/DeepResearch 待处理建议）

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖
- external-review：根目录无活跃文件，本轮未消费 external-review
- 正文处理：不修改正文，仅更新 frontmatter 与问题队列

## 审查结果

### 维度 1：源码引用准确性
- 评分：1/5
- 问题数：4
- 具体问题：
  1. [P0] frontmatter sources / SignalChain 与 debuggerd 引用 — `art/runtime/signal_chain.cc`、`system/core/debuggerd/tombstone.cpp`、`bionic/linker/debuggerd/handler.cpp` 在 AOSP android-16.0.0_r1 下不是有效锚点；正确路径应改为 `art/sigchainlib/sigchain.cc`、`art/runtime/fault_handler.cc`、`system/core/debuggerd/handler/debuggerd_handler.cpp`、`system/core/debuggerd/libdebuggerd/tombstone*.cpp`。
  2. [P0] 线上获取 tombstone — `ApplicationExitInfo.getProcessExitInfo()` 不存在；Android 16 源码提供的是 `ApplicationExitInfo.getTraceInputStream()`。
  3. [P0] 崩溃收集流程 — Native crash 信息不会通过 `ActivityManagerService` 回调到应用层 `UncaughtExceptionHandler`；`crash_dump.cpp` 是向 `/data/system/ndebugsocket` 写入 AM 通知。
  4. [P1] Breakpad 符号化段落 — `external/google-breakpad/docs/symbol_file_format.md` 路径无效，实际文档为 `docs/symbol_files.md`；MODULE id/目录查找协议不能写成“16 字符 / 截取前 16 字符”。

### 维度 3：版本差异覆盖
- 评分：2/5
- 问题数：2
- 具体问题：
  1. [P1] ApplicationExitInfo API 边界 — API 30 引入历史退出查询，但 Android 16 `ApplicationExitInfo.java` 注释写明 native tombstone trace 从 API 31 开始通过 `getTraceInputStream()` 返回，且全局循环缓冲区覆盖时可能为 null。
  2. [P1] Android 16 crash_dump 启动链路 — 现行链路是 handler pseudothread `execle(CRASH_DUMP_PATH, ...)` 启动 `/apex/com.android.runtime/bin/crash_dump64`，不应继续写成 debuggerd 守护进程 fork 出 crash_dump 的旧模型。

## 统计
- P0 事实错误：3 处
- P1 重要缺失：2 处
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：1.5/5

## 闭环动作
- 写入 queue.json（P95）：1 条（合并 3 个 P0 + 2 个 P1）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：0 条
- 更新章节 frontmatter：`status=ready-for-review`、`pipeline_stage=task2b_pending`、`task9_result=needs-rework`、`task2b_state=pending`、`task2b_result=pending`
- 自动晋升 finalized：0 个
