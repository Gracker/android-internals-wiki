# 深度技术 Review · 2026-05-08 09:27

## Review 目标
- 章节：7.1 卡顿的定义与分类
- 文件：src/part2-performance/ch07-smoothness/01-jank-definition.md
- 状态：ready-for-review → task2b_pending
- 章节：8.4 其他响应速度场景
- 文件：src/part2-performance/ch08-responsiveness/04-other-scenarios.md
- 状态：ready-for-review → finalized / ready-to-publish

## 选择说明
- 严格候选池仅剩 2 个 `task9_state: pending` 章节：7.1、8.4；默认 3 章规则因候选不足降为 2 章。
- `logs/external-review/` 根目录只有 archive 目录，无本轮可消费 active external-review 文件。
- 7.1 是 07:30 P1 后的 Task2B/Task6 回归复审；8.4 是 Fragment trace P1 修复后的回归复审。

## 审查结果

### 7.1 卡顿的定义与分类

#### 维度 1：源码引用准确性
- 评分：4.3/5
- 问题数：0 个 P0/P1；`JankInfo.h`、`FrameTimeline.cpp`、Perfetto binder stdlib 入口复核通过。

#### 维度 2：原理链完整性
- 评分：3.4/5
- 问题数：P1 1
- 具体问题：
  1. [P1][原理断裂/SQL 归因] L281-L319 Perfetto SQL 佐证 — 当前查询用 `actual_frame_timeline_slice` 锁定 `AppDeadlineMissed` 帧并限定 `client_upid`，但没有把 `android_binder_txns.client_utid` 收窄到该帧的 UI 线程 / RenderThread（`doFrame` / `DrawFrame`）关键路径。同一进程后台线程在同一帧窗口内的 Binder 事务仍可能被误当作 AppDeadlineMissed 因果证据。时间条件也只检查 `client_ts` 落在窗口内，未使用区间重叠；第二段 breakdown 查询单独执行时找不到上一段 CTE `frame_binder`。
  - 建议：用 FrameTimeline token 或 `android.frames.timeline` 视图定位对应 `doFrame` / `DrawFrame` / RenderThread `utid`，再用 `client_utid` + 区间重叠条件匹配 Binder；breakdown 放进同一 WITH 链路或落临时表。

#### 维度 3：版本差异覆盖
- 评分：4.5/5
- 问题数：0

#### 维度 4：知识盲区
- 评分：4.0/5
- 盲区数：0 个新增；Binder 关键线程约束归入 P1 回炉，不单独写 research-gaps。

#### 维度 5：数据与案例支撑
- 评分：3.5/5
- 问题数：P1 1（同上，SQL 证据仍可能误归因）

#### 维度 6：交叉引用一致性
- 评分：4.0/5
- 问题数：既有 P2 1（§2.3 “第十一节源码补充”引用层级不一致，已在 07:30 suggestions 记录，本轮不重复写入）

### 8.4 其他响应速度场景

#### 维度 1：源码引用准确性
- 评分：4.1/5
- 问题数：0 个 P0/P1；ActivityTaskManager / ClientTransaction、`DEFAULT_MAX_BINDER_THREADS=15`、`BINDER_VM_SIZE`、InputChannel `socketpair(AF_UNIX, SOCK_SEQPACKET)`、ViewPager2 `LinearLayoutManagerImpl` / `OFFSCREEN_PAGE_LIMIT_DEFAULT` 复核通过。

#### 维度 2：原理链完整性
- 评分：4.0/5
- 问题数：P2 1
- 具体问题：
  1. [P2] L287-L290 Ripple 与 onClick 阻塞 — Ripple 在 `ACTION_DOWN` 设置 pressed state，但首帧仍依赖主线程返回 Looper 并完成下一次 traversal；如果快速 tap 的 `ACTION_UP` 触发 `onClick` 后立即阻塞主线程，Ripple 首帧可能被一起延迟。已写入 suggestions.md。

#### 维度 3：版本差异覆盖
- 评分：4.3/5
- 问题数：0 个 P0/P1

#### 维度 4：知识盲区
- 评分：4.0/5
- 盲区数：0 个新增

#### 维度 5：数据与案例支撑
- 评分：3.8/5
- 问题数：P2 1
- 具体问题：
  1. [P2] L356 Flow `distinctUntilChanged()` — 它只过滤连续相同 query；“删除再重输入相同内容”属于非连续重复时仍会触发新搜索。已写入 suggestions.md。

#### 维度 6：交叉引用一致性
- 评分：4.0/5
- 问题数：既有 P2 2（`BackStackRecord.execute()` 调用链、默认 OkHttp/HttpURLConnection trace 可见性已在 earlier suggestions 记录，本轮不重复写入）

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处
- P2 建议改进：5 处（新增 2；既有 3 不重复写入）
- P3 锦上添花：0 处
- 总体技术评分：4.0/5

## 闭环动作
- 写入 queue.json（P85）：1 条（7.1 Binder SQL 关键线程/区间重叠约束）
- 写入 research-gaps.md：0 条
- 写入 suggestions.md：2 条（8.4 Ripple 首帧条件；Flow `distinctUntilChanged()` 连续去重语义）
- 仅日志记录 / 既有 suggestions 不重复：3 条
- 更新 frontmatter/progress：7.1 → `task2b_pending`；8.4 → `pass-tech-review` 并自动晋升 `finalized` / `ready-to-publish`
- 自动晋升 finalized：8.4

## 复核依据
- Perfetto main `stdlib/android/binder.sql`：`android_binder_txns` 暴露 `client_upid`、`client_utid`、`client_ts`、`client_dur`、`binder_txn_id`。
- Perfetto main `stdlib/android/binder_breakdown.sql`：`android_binder_client_breakdown` 以 `binder_txn_id` 关联事务拆分。
- AndroidX `FragmentStateAdapter`：非 primary Fragment 通过 `setMaxLifecycle(..., STARTED)`，primary 设为 `RESUMED`。
- AndroidX `ViewPager2.java`：内部创建 `LinearLayoutManagerImpl`，`setOffscreenPageLimit()` 只接受 `-1` 或 `>=1`。
- AOSP android-16.0.0_r1 `InputTransport.cpp` / `ProcessState.cpp` / `LaunchActivityItem.java`。
