# 深度技术 Review 闲时抽检 · 2026-06-06 11:20

## 抽检目标
- 章节：8.4 其他响应速度场景
- 文件：src/part2-performance/ch08-responsiveness/04-other-scenarios.md
- 状态：finalized
- 抽检原因：无 pending 候选；该章 last_task9_audit 缺失，且 2026-05-08 复审曾留下 P2 建议。

## 抽检范围
- 维度 1：源码引用准确性
- 维度 3：版本差异覆盖

## 审查结果

### 维度 1：源码引用准确性
- 评分：4.5/5
- 问题数：0 个 P0/P1
- 复核结论：AOSP android-16.0.0_r1 下 `Activity.java`、`ActivityThread.java`、`ActivityStarter.java`、`View.java`、`InputDispatcher.cpp`、`InputTransport.cpp`、`InputManagerService.java`、`ProcessState.cpp` 路径均存在；`Activity.startActivity()` 经 `Instrumentation.execStartActivity()`，内部入口使用 `ActivityTaskManager.getService()`；AndroidX ViewPager2 1.1.0 保持 `OFFSCREEN_PAGE_LIMIT_DEFAULT=-1`、`setOffscreenPageLimit()` 仅允许 `-1` 或 `>=1`，内部 `LinearLayoutManagerImpl` 与 `FragmentStateAdapter#setMaxLifecycle()` 口径成立；AndroidX Fragment 1.8.9 的 `androidx.fragment.app` 源码未命中 `Trace.beginSection`。

### 维度 3：版本差异覆盖
- 评分：4/5
- 问题数：1 个 P1，已 AUTO-FIX
- 具体问题：
  1. [P1][版本差异] L101 / L448-L455 — 章节版本边界停在 Android 16，但 Android 17（API 37）已公开 targetSdk 37 配置变更默认不重启 Activity 的行为：keyboard、keyboardHidden、navigation、touchscreen、colorMode 以及仅 `UI_MODE_TYPE_DESK` 切换的 uiMode 默认分发 `onConfigurationChanged()`，需要重启时使用 `android:recreateOnConfigChanges`。这会直接影响“页面突然重建”和配置变化后的响应延迟判断。

## AUTO-FIX
- 修改 `applicable_versions` 到 Android 17 / API 37；刷新 `last_verified` / `last_verified_against`。
- 在 Activity 跳转版本差异段补 targetSdk 37 分界。
- 在版本演进补 Android 17 条目。
- 在参考资料和 sources 增加 Android Developers Blog: The First Beta of Android 17。
- 更新 frontmatter：`task9_result=auto-fixed`、`task6_state=revisiting`、`pipeline_stage=task6_pending`、`last_task9_audit=2026-06-06`、`last_task9_autofix_at=2026-06-06`。

## 证据来源
- AOSP android-16.0.0_r1：platform/frameworks/base、platform/frameworks/native 对应源码路径。
- AndroidX Maven sources：viewpager2 1.1.0、fragment 1.8.9。
- Android Developers Blog, 2026-02-13, The First Beta of Android 17。

## 统计
- P0 事实错误：0 处
- P1 重要缺失：1 处（AUTO-FIX）
- P2 建议改进：0 处
- P3 锦上添花：0 处
- 总体技术评分：4.2/5

## 闭环动作
- 写入 queue.json：0 处
- 写入 research-gaps.md：0 处
- 写入 suggestions.md：0 处
- 章节 auto-fix：1 处，回到 Task6 复审
