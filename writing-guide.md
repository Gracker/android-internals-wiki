# Android Internals & Performance 技术文章写作规范

> 本规范用于指导 OpenClaw 在"知识加工"task 中产出的章节草稿质量。
> 核心标准：**读完这篇文章的人，能独立分析同类问题。**

---

## 一、写作的根本原则

你写的不是百科词条，不是 API 文档，不是知识点罗列。你写的是**一个有经验的工程师在给另一个工程师讲一件事**——他会从"为什么要了解这个"开始，带你走一遍真实的技术脉络，走完之后你脑子里有一张清晰的图，下次遇到问题知道从哪里下手。

所以每一篇内容必须回答三个问题：
1. **这个东西为什么存在？**（它解决什么问题，没有它会怎样）
2. **它是怎么工作的？**（机制、流程、关键代码路径）
3. **我在实际工作中怎么用这个知识？**（Trace 中怎么看、出问题怎么查、优化怎么做）

---

## 二、文章结构模板

根据内容类型，分为三种结构。不是每篇都要死板套用，但必须包含核心要素。

### 类型 A：机制原理篇（如 "VSync 机制"、"Binder IPC"）

```
## 开头：为什么要了解 XX
- 用 1-2 段话说清楚：这个机制解决什么问题，没有它会怎样
- 给读者一个动机：了解这个之后，你能做什么之前做不到的事
- 如果可能，用一个具体的现象引入（"你在 Perfetto 中看到的 XX 那行绿色方块，就是本文要讲的东西"）

## 核心机制
- 用连贯的叙述（不是列表！）把机制讲清楚
- 遵循"从宏观到微观"的顺序：先讲整体架构/数据流，再深入单个环节
- 关键处贴源码，但不是贴一整个函数——只贴决定行为的那几行，加上你的注释
- 每引用一段源码，前后必须有文字说明"这段代码在做什么"和"对我们分析问题意味着什么"
- 复杂流程用图示辅助（流程图/时序图/架构图），图不是装饰，是必须的

## 在 Perfetto/工具 中的表现
- 这个机制在 Trace 中长什么样？对应哪个 Track？
- 正常情况什么样，异常情况什么样
- 如果可能，给出截图或描述具体的 Trace 片段

## 与其他机制的关系
- 这个机制和上下游的关联（如 VSync → Choreographer → MainThread/RenderThread）
- 交叉引用其他章节

## 版本演进（如适用）
- 在不同 Android 版本中的变化
- 标注每个变化的影响

## 常见问题与误区
- 新手容易误解的点
- 面试常问但容易答错的点

## 参考资料
- AOSP 源码路径（精确到文件和关键函数）
- 官方文档链接
- 其他高质量参考
```

### 类型 B：性能分析实战篇（如 "滑动卡顿分析"、"启动速度优化"）

```
## 开头：问题现象描述
- 描述用户/测试反馈的现象
- 明确属于哪个性能分类（流畅性/响应速度/ANR/内存/功耗）
- 交代分析环境（设备、Android 版本、复现条件）

## 分析思路
- 在动手之前，先讲清楚你的分析思路：为什么从这里切入？
- 有经验的工程师面对这类问题，脑子里的决策路径是什么？
- 这一段是最有价值的——它教的不是"答案"，而是"怎么找到答案"

## 抓取与定位
- 用什么工具/配置抓取 Trace
- 在 Trace 中怎么定位到问题区域

## 逐步分析
- 像侦探破案一样，一步步展开分析过程
- 每一步：看到了什么 → 说明了什么 → 下一步往哪里看
- 关键发现要截图或贴 Trace 片段
- 中间的弯路和排除过程也要保留（读者需要知道"为什么不是那个原因"）

## 根因与结论
- 明确的根因分析
- 如果涉及源码，指出具体的代码路径

## 修复/优化方案
- 针对根因的具体修复
- 如果是系统问题，说明 App 端的规避方案
- 如果是 App 问题，说明最佳实践

## 举一反三
- 这个案例背后的通用规律是什么？
- 下次遇到类似现象，可以用同样的方法吗？

## 参考资料
```

### 类型 C：工具使用篇（如 "Perfetto Trace 抓取"、"MAT 使用"）

```
## 开头：这个工具解决什么问题
- 不是"这个工具是什么"，而是"没有这个工具你会多痛苦"
- 跟其他工具的关系和定位差异

## 基本使用
- 从零开始，能跑通的完整步骤
- 每个步骤截图或命令行示例
- 标注常见的坑（"第一次用经常卡在这里"）

## 进阶用法
- 高级配置/参数
- 结合具体场景的使用技巧

## 实战示例
- 用一个真实案例演示完整的使用流程
- 从打开工具到得出结论的全过程

## 参考资料
```

---

## 三、写作手法要求

### 1. 叙述为主，列表为辅

**错误写法（知识点罗列）：**
```
VSync 机制的关键点：
- HW VSync 由 HWC 产生
- DispSync 将其分发为 VSYNC_APP 和 VSYNC_SF
- VSYNC_APP 驱动 Choreographer
- VSYNC_SF 驱动 SurfaceFlinger
- 三缓冲可以减少掉帧
```

**正确写法（连贯叙述）：**
```
VSync 信号的源头是显示硬件。HWC（Hardware Composer）在每次屏幕
刷新周期开始时发出一个硬件 VSync 中断，以 120Hz 屏幕为例，
这个中断每 8.33ms 触发一次。

但系统不会把这个原始信号直接交给 App 和 SurfaceFlinger 使用。
原因是 App 的渲染和 SurfaceFlinger 的合成在时间上有先后关系——
App 需要先画好一帧，SurfaceFlinger 才能拿去合成。如果两者同时
收到信号同时开始工作，SurfaceFlinger 合成的时候 App 可能还
没画完，就会浪费一个 VSync 周期。

为了解决这个问题，DispSync 将硬件 VSync 分发为两个独立的信号：
VSYNC_APP 和 VSYNC_SF，它们之间有一个精心计算的时间差
（offset）。VSYNC_APP 先到，触发 Choreographer 通知 App 开始
这一帧的 measure/layout/draw；等 App 渲染完成后，VSYNC_SF
到来，触发 SurfaceFlinger 开始合成。

在 Perfetto 中，你可以在 SurfaceFlinger 进程里看到这两个信号
（VSYNC-app 和 VSYNC-sf），它们的间距就是 offset 值...
```

注意区别：正确写法**有因果关系、有上下文、有"为什么"**。读者跟着走一遍，脑子里就有一张时序图了。

### 2. 源码引用的规范

**不要这样：**
```
下面是 Choreographer 的 scheduleFrameLocked 方法：

// 贴 50 行源码

上面的代码展示了 Vsync 的申请过程。
```

**要这样：**
```
当 App 需要更新 UI 时（比如调用了 View.invalidate()），最终会走到
Choreographer.scheduleFrameLocked()。这个方法做的核心事情只有一件：
向系统申请下一个 VSync-app 信号。

```java
// frameworks/base/core/java/android/view/Choreographer.java
// @ AOSP android-17-beta3
private void scheduleFrameLocked(long now) {
    if (!mFrameScheduled) {
        mFrameScheduled = true;  // 标记：已经申请了，避免重复申请
        if (USE_VSYNC) {
            mDisplayEventReceiver.scheduleVsync(); // 关键：向 SurfaceFlinger 注册回调
        }
    }
}
```

这里有两个细节值得注意：

第一，mFrameScheduled 这个标志位保证了在一个 VSync 周期内，
无论调用多少次 invalidate()，只会申请一次 VSync。这就是为什么
你在 Trace 中看到的 doFrame 是每个 VSync 周期最多一次，而不是
每次 invalidate() 一次。

第二，scheduleVsync() 是一个"订阅"操作，不是"立即触发"操作。
调用后 App 会继续执行其他代码，直到下一个 VSync-app 信号到来，
才会回调到 Choreographer.doFrame() 开始真正的渲染工作。
```

**原则：贴代码是为了证明你的说法，不是为了展示代码本身。** 每段代码前要说"这段代码在做什么"，代码后要说"这意味着什么/读者应该注意什么"。

### 3. 图文配合

技术文章中，以下场景**必须**配图或配 Trace 截图描述：
- 架构图：涉及多个组件交互的机制（如渲染管线、Input 分发链路）
- 时序图：有时间先后关系的流程（如一帧的渲染过程）
- Trace 截图描述：在 Perfetto 中的实际表现
- 对比图：优化前后、正常vs异常

图的要求：
- 必须是说明性的，不是装饰性的
- 图中的文字要清晰、用中文
- 如果是 Trace 截图描述，标注关键区域并解释
- OpenClaw 生成草稿时可以用文字描述图的内容，标注 `[图：{描述}]`，由高爷后续补充实际截图

### 4. 由浅入深、层层递进

文章的信息密度应该是前松后紧：
- **开头**：读者还没进入状态，用现象或问题引入，给他一个"为什么要看这篇"的理由
- **中段**：核心机制讲解，信息密度最高，但每个知识点之间有过渡衔接
- **深入**：源码级分析、边界情况、版本差异——不是每个读者都会看到这里，但看到的人需要这些
- **收尾**：总结关键点、与其他知识的关联、实操建议

### 5. 语言风格

- **用"我们"而非"你"**：我们来看、我们注意到、我们可以发现——这是一起探索的语气
- **口语化但不随意**：可以说"简单来说"、"说白了"，但不要说"大家都知道"（读者不一定知道）
- **精准的技术措辞**：不说"数据传给了下一层"，说"App 通过 queueBuffer() 将 GraphicBuffer 提交给 BufferQueue"
- **承认复杂性**：不要假装一切都很简单。如果某个机制确实复杂，直说"这部分比较复杂，我们分步拆解"
- **适当使用类比**：帮助理解抽象概念，但类比之后要回到精确的技术描述

---

## 四、内容质量检查清单

每篇草稿完成后，OpenClaw 自检以下项目：

### 结构检查
- [ ] 开头是否回答了"为什么要了解这个"？
- [ ] 是否有连贯的叙述主线，而非知识点罗列？
- [ ] 是否做到了由浅入深、层层递进？
- [ ] 是否有收尾（总结、关联、下一步）？

### 深度检查
- [ ] 是否讲了"为什么"，而不只是"是什么"？
- [ ] 源码引用是否精准（标注文件路径、分支、关键函数）？
- [ ] 源码前后是否有解释性文字？
- [ ] 是否给出了在 Perfetto/工具中的实际表现？
- [ ] 是否有版本演进说明（如适用）？

### 准确性检查
- [ ] 技术措辞是否精准（不含糊、不笼统）？
- [ ] 源码路径是否经过验证？
- [ ] 数值是否准确（超时时间、帧周期等）？
- [ ] 是否标注了验证状态？

### 可读性检查
- [ ] 段落是否过长？（单段不超过 8 行）
- [ ] 是否在应该配图的地方标注了图？
- [ ] 专业术语是否在第一次出现时解释？
- [ ] 关键结论是否突出（不埋在段落中间）？

### 实用性检查
- [ ] 读完这篇，读者能独立分析同类问题吗？
- [ ] 是否提供了"怎么在实际工作中用这个知识"？
- [ ] 是否有常见误区或注意事项？
- [ ] 案例集部分是否有真实案例而非假设？

---

## 五、反面教材：坚决不要写成这样

### 反面 1：百科词条式
```
## Choreographer

Choreographer 是 Android 中负责协调渲染时机的组件。它基于 VSync
信号工作，主要功能包括：

- 接收 VSync 回调
- 执行 Input 事件处理
- 执行动画计算
- 执行 View 的 measure/layout/draw

Choreographer 在 Android 4.1 引入，位于
frameworks/base/core/java/android/view/Choreographer.java。
```

这种写法的问题：读完之后你知道 Choreographer 是什么，但不知道它为什么这样设计、怎么工作的、出了问题怎么查。

### 反面 2：源码堆砌式
```
## Choreographer 的实现

首先我们看 Choreographer 的构造函数：
[贴 30 行代码]

然后看 scheduleFrameLocked：
[贴 20 行代码]

接着看 doFrame：
[贴 40 行代码]

以上就是 Choreographer 的核心逻辑。
```

这种写法的问题：读者淹没在代码中，不知道哪些是重要的、为什么这样写、跟他的日常工作有什么关系。

### 反面 3：概述式
```
## 2.4 Choreographer 与渲染流水线

Choreographer 是 Android 渲染流水线的核心协调者。它基于 VSync
信号驱动，负责调度 Input、Animation、Traversal 三类回调。

主要流程：
1. App 调用 invalidate()
2. Choreographer 向系统申请 VSync
3. VSync 到来时执行 doFrame
4. 依次执行 Input → Animation → Traversal 回调
5. 主线程完成后通知 RenderThread

关键类：Choreographer.java、ViewRootImpl.java
```

这种写法的问题：看起来很"全"，但每个点都只有一句话，等于什么都没说。读者看完了觉得"嗯，我好像知道了"，但其实什么都做不了。

---

## 六、好文章的标准：Gracker 的写法

参考高爷（Gracker）在 androidperformance.com 上的系列文章，提炼出的写作特点：

1. **现象驱动**：从读者能看到的东西出发（Trace 中的某一行、用户感知到的卡顿），而不是从抽象概念出发
2. **图文交织**：每讲一段机制，就配一张 Trace 截图或流程图，让读者对照着看
3. **代码点到为止**：不贴完整函数，只贴关键的几行，但每行都有解释
4. **不断回扣到 Trace**："在 Systrace 中，这个过程对应的就是 XX track 中的 XX 区域"
5. **经验之谈**：穿插实战经验（"在实际分析中，我们经常发现 XX 情况"）
6. **前后呼应**：每篇文章都告诉你"看这篇之前需要了解什么"和"看完之后可以去看什么"
7. **认知差异**：主动指出不同角色（用户 / App 开发 / 系统开发 / 测试）看同一个问题的差异

**OpenClaw 在写作时应该以这种风格为标杆**，而不是写成教材或文档。

---

## 七、关于篇幅

- 一个小节（如 2.3 VSync 机制）的目标篇幅：**3000-8000 字**
- 如果内容确实很多（如 Choreographer 机制），可以拆成多篇
- 宁可一个小节写深写透，也不要在一篇里蜻蜓点水地覆盖多个小节
- 案例集部分，每个案例 **1500-3000 字**

---

## 八、给 OpenClaw 的特别说明

你在加工草稿的时候，记住以下几点：

1. **你不是作者，你是编辑助理和研究员。** 高爷的文章有他自己的表达风格和技术判断，你的工作是帮他整理结构、补充引用、验证准确性，而不是用你的"AI 味"改写他的内容。

2. **高爷原创的内容是最高优先级素材。** 当你加工他的博客文章或笔记时，保留他的核心表达。你可以调整段落顺序、补充上下文衔接、添加源码引用，但不要改变他的技术观点和叙述方式。

3. **你没有实机验证能力。** 涉及到"在 Trace 中表现为 XX"的描述，如果素材中有就保留，如果没有就标注 `[待补充：Trace 截图]` 而不是自己编一个描述。

4. **对于你不确定的技术细节，标注而非猜测。** 写 `[待验证：XX 在 Android 17 中是否仍然适用]` 比写一个可能错误的陈述好一万倍。

5. **写完之后，用"五、反面教材"中的三种错误写法对照检查你的草稿。** 如果你的某一段读起来像百科词条、像源码堆砌、或像概述式罗列，就重写那一段。
