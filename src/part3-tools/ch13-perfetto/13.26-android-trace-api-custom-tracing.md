---
title: "android.os.Trace API 深度解析与应用级自定义追踪"
chapter: "13.26"
status: ready-for-review
pipeline_stage: task6_pending
drafted_date: "2026-07-06"
applicable_versions: "Android 10 (API 29) - Android 17 (API 37)"
last_verified: "2026-07-06"
last_verified_against: "android-17.0.0_r1"
confidence: high
sources:
  - type: aosp
    path: "frameworks/base/core/java/android/os/Trace.java"
  - type: aosp
    path: "frameworks/base/core/jni/android_os_Trace.cpp"
  - type: aosp
    path: "system/core/libcutils/trace-dev.cpp"
  - type: official
    path: "developer.android.com/reference/android/os/Trace"
tags: [trace, systrace, perfetto, tracing, debugging, custom-trace]
related_chapters: ["13.1", "13.3", "13.5", "13.17", "14.27"]
created_by: "task2a-knowledge-gap"
created_date: "2026-07-05"
drafted_by: "task2a-content-processing"
task6_state: reviewed
task6_result: pass-light-edit
task9_result: pass-tech-review
task9_state: reviewed
last_task9_reviewed_at: 2026-07-13
reviewed_by: openclaw-task6
reviewed_date: 2026-07-13
pipeline_stage: ready-to-publish
deepseek_cn_review_state: done
last_deepseek_cn_review_at: 2026-07-13
---

# 13.26 android.os.Trace API 深度解析与应用级自定义追踪

在 Android 上做性能埋点，`android.os.Trace` 是最直接的入口。一行 `beginSection`，就能在 Perfetto trace 时间线上留下自定义 Slice；`setCounter` 能打数值点。它兼容 API 18 至今所有版本，开销极低，而且和系统级 trace 事件天然对齐到同一时间轴。

本节从版本演进、底层写入路径、最佳实践和常见陷阱四个角度，把 `android.os.Trace` 的完整用法梳理清楚。

## 1. Trace API 演进与版本差异

### 1.1 核心 API 总览

`android.os.Trace` 提供四种类型的 API，覆盖 Android 4.3（API 18）至今的全部版本：

| API | 引入版本 | 用途 | 线程约束 |
|------|---------|------|----------|
| `beginSection(String sectionName)` / `endSection()` | API 18 | 同步区域标记 | 必须在同一线程 begin/end 配对 |
| `beginAsyncSection(String name, int cookie)` / `endAsyncSection(String name, int cookie)` | API 18 | 异步区域标记 | 可跨线程配对，需提供 cookie |
| `setCounter(String name, long value)` | API 18 | 计数器/计量值 | 任意线程调用 |
| `setAppTracingAllowed(boolean)` | API 26 | 应用级 tracing 总开关 | 全局生效 |


### 1.2 同步区域：beginSection / endSection

`beginSection` 在当前线程的 trace 栈中压入一个 begin 事件，`endSection` 弹出栈顶事件。两者必须在同一线程严格嵌套调用——不允许线程 A begin、线程 B end。Perfetto 采集后，这些事件在 Thread Track 上显示为带颜色的 Slice 条。

```kotlin
// 正确：同线程嵌套
Trace.beginSection("loadUserData")
try {
    Trace.beginSection("dbQuery")
    val users = userDao.getAll()
    Trace.endSection() // 弹出 dbQuery

    Trace.beginSection("mapToUi")
    val uiModels = users.map { it.toUiModel() }
    Trace.endSection() // 弹出 mapToUi
} finally {
    Trace.endSection() // 弹出 loadUserData
}
```

section name 在 trace_marker 写入时被截断到 127 字节。这个限制来自 `Trace.java` 内部常量 `MAX_SECTION_NAME_LEN`（实际值 127）。过长的 section name 不会报错，但会在 Perfetto UI 中显示为截断后的字符串。


### 1.3 异步区域：beginAsyncSection / endAsyncSection

异步区域允许跨线程配对，通过 `cookie`（int 类型的唯一标识）关联 begin 和 end。典型用途是追踪一个在工作线程发起、在主线程回调完成的异步操作：

```kotlin
private val asyncCookie = AtomicInteger(0)

fun trackAsyncWork() {
    val cookie = asyncCookie.incrementAndGet()
    Trace.beginAsyncSection("asyncImageLoad", cookie) // 主线程 begin

    backgroundScope.launch {
        // 工作线程执行
        val bitmap = loadBitmapFromNetwork()
        withContext(Dispatchers.Main) {
            Trace.endAsyncSection("asyncImageLoad", cookie) // 主线程 end
        }
    }
}
```

cookie 必须保证在同一 name 的并发实例中唯一。如果两个线程同时用相同 name + cookie begin/end，Perfetto 中的 Slice 会错乱交叉。

### 1.4 计数器：setCounter

`setCounter` 向 trace 中写入一个带名称的数值点，Perfetto 采集后在 Counter Track 上以面积图呈现。典型用途包括追踪缓存命中率、队列长度、自定义内存指标：

```kotlin
// 在数据处理循环中定期打点
Trace.setCounter("queueSize", queue.size().toLong())
Trace.setCounter("cacheHitRate", (hits * 100 / total).toLong())
```

底层实现上，`setCounter` 调用 native `nativeTraceCounter`，最终通过 `ATRACE_INT` 路径写入 trace_marker。不同于 `beginSection` 的压栈/弹栈模型，counter 写入是完全独立的，不需要配对。


### 1.5 Android 版本演进要点

- **API 18 (Android 4.3)**：Trace API 首次引入，含 beginSection/endSection/beginAsyncSection/endAsyncSection/setCounter
- **API 23 (Android 6.0)**：native 层 `ATRACE_BEGIN`/`ATRACE_END` 宏对从 `<cutils/trace.h>` 迁移到 `<android/trace.h>`，统一 NDK 接口
- **API 26 (Android 8.0)**：新增 `setAppTracingAllowed(false)`，允许应用在敏感时段关闭 tracing
- **API 29 (Android 10)**：Perfetto system backend（`traced`/`traced_probes`）成为默认采集后端，Trace API 写入的 marker 从此被 Perfetto 而非 Systrace 采集
- **API 30 (Android 11)**：Trace.java 内部重构，属性检查从每次调用的 `__system_property_get` 改为 TLS 缓存 + sse 屏障刷新，显著降低高频 tracing 的 CPU 开销 [待验证: 具体提交 hash]
- **Android 12 (API 31)**：FrameTimeline 集成，`Choreographer#doFrame` 的 Trace marker 自动关联帧分类信息
- **Android 15 (API 35)**：`ProfilingManager` 允许 App 主动请求系统采集 trace（参见 13.1），但 Trace API 本身无变化
- **Android 17 (API 37)**：Trace API 无新公开接口变更，但底层 trace_marker 写入路径优化了内存屏障开销 [待验证: 具体优化细节]

## 2. Trace 底层实现机制

### 2.1 从 Java 到 trace_marker 的完整调用链

当应用调用 `Trace.beginSection("MyTag")` 时，执行路径如下：

```
Trace.beginSection("MyTag")                      [Java]
  → nativeBeginSection(name)                    [JNI: android_os_Trace.cpp]
    → atrace_begin_body(ATRACE_TAG_APP, name)  [libcutils: trace-dev.cpp]
      → write(trace_marker_fd, "B|<pid>|MyTag", len)  [syscall]
```

具体来说：

1. **Java 层** (`Trace.java`)：首先检查 TLS 变量 `sIsTracingEnabled`（见 2.2）。如果为 false，直接 return，开销约为一次分支判断。
2. **JNI 层** (`android_os_Trace.cpp`)：`nativeBeginSection` 调用 `atrace_begin_body(ATRACE_TAG_APP, sectionName)`。`ATRACE_TAG_APP` 是值为 `(1 << 13)` 的标签位。
3. **libcutils 层** (`trace-dev.cpp`)：检查 `atrace_is_tag_enabled(ATRACE_TAG_APP)`。如果标签未启用，直接返回。
4. **系统调用**：`write()` 向 trace_marker 文件描述符写入一行格式化文本：`B|<pid>|<sectionName>\n`。内核 ftrace 将这行文本记录到 ring buffer。

`endSection` 类似，写入格式为 `E|<pid>\n`。async section 写入 `S|<pid>|<name>|<cookie>\n`（begin）和 `F|<pid>|<name>|<cookie>\n`（end）。counter 写入 `C|<pid>|<name>|<value>\n`。


### 2.2 Tracing 启用检测：属性 + TLS 缓存

Trace API 在没抓 trace 时必须是零开销的——不然没人敢在生产代码里埋点。Trace.java 用三层机制做到这一点：

1. **属性检查**：系统启用 atrace 时把 `debug.atrace.app_number` 设为非零。Trace.java 初始化时通过 `__system_property_get` 读这个值。
2. **TLS 缓存**：属性值缓存到线程本地变量 `sIsTracingEnabled`（Android 11+ 优化），后续调用直接读 TLS，不走系统调用。
3. **屏障刷新**：每 64 次 TLS 读取做一次 `__system_property_get` 刷新，保证 tracing 开关时 TLS 能及时跟上。

当 tracing 未启用时，一次 `Trace.beginSection()` 的实际开销是一次 TLS 读取 + 一次条件分支跳转，约 2-5 ns。当 tracing 启用时，开销主要是一次 `write()` 系统调用（约 1-3 μs），加上字符串格式化和拷贝。


### 2.3 trace_marker 文件描述符

trace_marker 的文件描述符（fd）在进程首次调用 Trace API 时懒加载。libcutils 的 `trace-dev.cpp` 通过 `open()` 打开 `/sys/kernel/tracing/trace_marker`（Android 9+）或 `/sys/kernel/debug/tracing/trace_marker`（旧版）。fd 被缓存在全局变量中，后续调用直接复用。

在 SELinux 限制下，非系统进程通过 `/__metadata__/trace_marker` 路径访问（Android 10+ 对 untrusted_app 域开放了此路径）。这确保了普通应用可以写 trace marker，但不能读取或修改内核 trace 配置。


### 2.4 ATRACE_TAG 分类

每个 trace marker 写入时都带一个标签（ATRACE_TAG），用于在 Perfetto UI 中分类和着色。应用层始终使用 `ATRACE_TAG_APP`，其值为 `(1 << 13) = 8192`。

Framework 内部代码可以使用更多标签：

| 标签 | 值 | 用途 |
|------|-----|------|
| ATRACE_TAG_APP | 1<<13 | 应用自定义事件 |
| ATRACE_TAG_VIEW | 1<<4 | View 系统（measure/layout/draw） |
| ATRACE_TAG_INPUT | 1<<6 | 输入事件分发 |
| ATRACE_TAG_WM | 1<<5 | WindowManager |
| ATRACE_TAG_AM | 1<<7 | ActivityManager |
| ATRACE_TAG_GRAPHICS | 1<<1 | 图形/渲染管线 |
| ATRACE_TAG_SCHED | 1<<11 | CPU 调度信息 |

普通应用只能使用 ATRACE_TAG_APP，无法切换标签。这也意味着应用写入的 Trace marker 在 Perfetto 中都属于 "Atrace" category，颜色和过滤行为一致。


## 3. 应用层 Trace 最佳实践

### 3.1 关键路径埋点策略

埋点不是越多越好，集中在性能关键路径才有效。推荐按以下优先级来：

**第一优先：帧级渲染路径**
- 首屏渲染：从 `Application.onCreate` 到第一帧 drawn 的全链路
- Feed 滑动：每帧的 `onBindViewHolder` / `onCreateViewHolder`
- 自定义 View 的 `onDraw` / `onLayout`

**第二优先：用户感知延迟**
- 页面跳转：从 `startActivity` 到目标页首帧
- 网络请求：从 `enqueue` 到 `onResponse`（用 async section）
- 图片加载：从 `load()` 到 `onResourceReady`

**第三优先：后台耗时操作**
- 数据库迁移、文件 IO、JSON 解析

不推荐在循环体内逐次打点（频率 > 1MHz），原因见 3.3。

### 3.2 使用 Compose Recomposition Trace

Jetpack Compose 1.2.0 起内置了 Recomposition 追踪。抓 trace 时 Compose runtime 自动为每次重组写 Trace marker，包含：

- 函数名和 source position（如 `"MyApp.Background:<position>"`）
- Recomposition 原因（state changed / forced）
- 跳过重组的 @Composable 函数

开发者无需手动埋点，只需在抓 trace 时确保 Compose 版本 >= 1.2.0 且启用了 `recomposition` atrace category。在 Perfetto UI 中，这些 marker 出现在 MainThread Track 上，颜色为 Compose 专属色。

对于更细粒度的追踪，可以在 @Composable 函数内手动加 `Trace.beginSection`：

```kotlin
@Composable
fun FeedItem(item: FeedData) {
    Trace.beginSection("FeedItem:${item.id}") // name 包含动态信息
    // ... compose content ...
    Trace.endSection()
}
```

section name 带了动态值会导致 Perfetto 中出现大量独立 Slice 而非聚合结果。如果目的是统计 FeedItem 的平均重组耗时，用固定 name `"FeedItem"`，聚合交给 Perfetto SQL。

### 3.3 避免高频 beginSection 的高开销

当 tracing 启用时，每次 `beginSection` 至少触发一次 `write()` 系统调用（~1-3 μs）。在热路径中高频调用会引入可观的开销：

| 调用频率 | 单线程开销估计 | 影响 |
|---------|-------------|------|
| < 1000 次/秒 | < 3ms CPU | 可忽略 |
| 1000-10000 次/秒 | 3-30ms CPU | 轻微，需评估 |
| > 10000 次/秒 | > 30ms CPU | 显著，会导致帧延迟 |

建议的策略：
- 在循环体内只对整轮循环打一个 beginSection（如 `"processBatch"`），而非逐元素打点
- 对高频回调（如 `onScrollChanged`），使用 `setCounter` 追踪数值变化而非 begin/end Section
- 使用 `Trace.setAppTracingAllowed(false)` 在确定不需要 trace 的热路径段临时关闭

```kotlin
// ❌ 不推荐：在 RecyclerView 的 onBindViewHolder 中逐 item 打点
override fun onBindViewHolder(holder: VH, position: Int) {
    Trace.beginSection("bind:$position") // position 每次不同，且频率高
    // ...
    Trace.endSection()
}

// ✅ 推荐：在 Batch 回调中打一个点
override fun onBindViewHolder(holder: VH, position: Int) {
    // 不打 Trace，用 setCounter 追踪绑定量
    Trace.setCounter("bindCount", (position + 1).toLong())
    // ...
}
```

### 3.4 协程与 Trace 集成

Kotlin 协程的线程切换会使传统的同线程 begin/end 模型失效。推荐使用 `CoroutineContext.Element` 封装 Trace 逻辑：

```kotlin
class TracingElement(private val name: String) : CoroutineContext.Element {
    companion object Key : CoroutineContext.Key<TracingElement>
    override val key = Key

    // 在 resumeWith 中自动 end section
}
```

或者更简洁地使用 async section 配合 cookie：

```kotlin
suspend fun <T> tracedAsync(name: String, block: suspend () -> T): T {
    val cookie = name.hashCode() // 简化，生产环境需唯一
    Trace.beginAsyncSection(name, cookie)
    try {
        return block()
    } finally {
        Trace.endAsyncSection(name, cookie)
    }
}

// 使用
val data = tracedAsync("fetchUserData") {
    userRepository.getUser()
}
```

async section 在线程切换时不会断开，因为它是通过 name+cookie 而非线程栈来关联的。

## 4. Perfetto SDK vs android.os.Trace

### 4.1 能力对比

| 维度 | android.os.Trace | Perfetto SDK (C++17) | androidx.tracing.perfetto (Jetpack) |
|------|-----------------|---------------------|----------------------------------|
| 引入版本 | API 18 | Perfetto v30+ | Jetpack 1.2.0 |
| 语言 | Java/Kotlin | C++17 | Java/Kotlin |
| 事件类型 | Slice + Counter | 完全自定义 protobuf | Slice + Counter（封装层） |
| 数据通道 | trace_marker (ftrace) | Perfetto shared memory | Perfetto shared memory |
| 系统依赖 | 无（始终可用） | 需 system backend | 需 system backend 或 in-process |
| 性能开销 | ~1-3 μs/次 | ~200-500 ns/次 | ~300-800 ns/次 |
| 自定义参数 | 不支持 | 支持（typed args） | 不支持 |
| 定位 | 最广兼容性 | 最高性能 + 最大灵活性 | Jetpack 友好 |


### 4.2 选型建议

**android.os.Trace**（绝大多数应用的默认选择）：
- 追踪关键业务流程耗时，和系统级事件天然对齐
- 兼容性最广（API 18+），团队无额外学习成本

**Perfetto SDK (C++17)**（引擎或大型应用）：
- 需要 event 带结构化参数（如 imageLoad 附带 URL、尺寸、格式）
- 需要自定义 track（如独立的 "Network Pool"）
- tracing 事件量极大（每秒万级），对开销极度敏感
- 已有 C++ native 层的性能监控需求

**androidx.tracing.perfetto**（Jetpack 技术栈）：
- 已走 Jetpack 依赖管理，想要 Perfetto SDK 的部分能力
- Java/Kotlin API，兼容 API 21+

## 5. Trace 与 Perfetto SQL 查询的配合

### 5.1 自定义 section 在 slice 表中的位置

应用层 Trace 写入的 begin/end section 在 Perfetto 中被采集为 `slice` 行。同步 section（beginSection）的 `category` 为 `atrace`，`thread_name` 为调用线程名。

```sql
-- 查询某自定义 section 的延迟分布
SELECT
    name,
    COUNT(*) as count,
    ROUND(AVG(dur)/1000000.0, 2) as avg_ms,
    ROUND(MIN(dur)/1000000.0, 2) as min_ms,
    ROUND(MAX(dur)/1000000.0, 2) as max_ms
FROM slice
WHERE name LIKE 'MyTag:%' AND category = 'atrace'
GROUP BY name
ORDER BY avg_ms DESC;
```

### 5.2 使用 PERCENTILE 计算长尾

Perfetto Trace Processor 支持 `PERCENTILE` 聚合函数：

```sql
-- 计算 P50/P90/P99 延迟
SELECT
    name,
    ROUND(PERCENTILE(dur, 0.5)/1000000.0, 2) as p50_ms,
    ROUND(PERCENTILE(dur, 0.9)/1000000.0, 2) as p90_ms,
    ROUND(PERCENTILE(dur, 0.99)/1000000.0, 2) as p99_ms
FROM slice
WHERE name = 'loadUserData' AND category = 'atrace'
GROUP BY name;
```

### 5.3 Async Section 的查询

Async section 在 Perfetto 中以 `name + cookie` 标识。它们的 `category` 也是 `atrace`，但 `thread_name` 可能不同（begin 和 end 可以在不同线程）：

```sql
-- 查询 async section 的 begin-end 间隔
SELECT
    b.name,
    b.ts as begin_ts,
    e.ts as end_ts,
    (e.ts - b.ts)/1000000.0 as duration_ms
FROM slice b
JOIN slice e ON b.name = e.name
    AND e.ts > b.ts
    AND e.ts - b.ts < 60000000000 -- 60s 内
WHERE b.name LIKE 'asyncImageLoad%'
    AND b.category = 'atrace'
ORDER BY duration_ms DESC
LIMIT 20;
```

async section 在 Perfetto UI 中按 name 分组显示在独立的 Async Track 上，不出现在线程 Track。

### 5.4 Counter 数据查询

`setCounter` 写入的数值点在 Perfetto 的 `counter` 表中：

```sql
-- 查询自定义 counter 的时序变化
SELECT
    name,
    ts,
    value
FROM counter
WHERE name = 'queueSize'
ORDER BY ts;

-- 计算平均值和峰值
SELECT
    name,
    ROUND(AVG(value), 2) as avg_value,
    MAX(value) as peak_value,
    MIN(value) as min_value
FROM counter
WHERE name LIKE 'MyCounter%'
GROUP BY name;
```

## 6. 常见陷阱与性能影响

### 6.1 begin/end 不匹配

最常见的问题。当 begin 和 end 的次数不一致时，Perfetto 中的 Slice 栈会错位——后续的 endSection 可能弹出错误的事件，导致整个线程的 trace 时间线混乱。

**根因场景**：
- 异常路径遗漏 endSection（没有 try-finally 保护）
- 异步回调中 begin/end 在不同线程

**修复模式**：
```kotlin
Trace.beginSection("critical")
try {
    // ... 可能抛异常的代码 ...
} finally {
    Trace.endSection() // 确保无论如何都 end
}
```

### 6.2 section name 过长被截断

section name 超过 127 字节时被静默截断，不会报错。这会导致 Perfetto 中出现意料之外的短 name，SQL `WHERE name = ...` 查询匹配失败。

**建议**：section name 保持简洁、结构化，如 `"loadUser:id=$userId"`（动态信息附在后缀）。如果需要传递更多信息，使用 Perfetto SDK 的 typed arguments 而非拼接超长字符串。

### 6.3 高频 tracing 导致 buffer 溢出

Perfetto 的 trace buffer 默认大小为几 MB 到几十 MB（取决于配置）。高频 begin/end section 会迅速填满 buffer，导致早期事件被覆盖。

**症状**：抓取 10 秒 trace，但只能看到最后 1-2 秒的数据。

**解决**：
- 增大 buffer size（`TraceConfig.buffers[].size_kb`）
- 降低应用层打点频率
- 使用 `ring_buffer` 模式（只保留最新数据）而非 `stop_on_full`

### 6.4 多线程 async section 的 cookie 冲突

当多个线程同时用相同的 name 和 cookie 调用 beginAsyncSection 时，Perfetto 无法区分哪个 begin 对应哪个 end，Slice 会交叉。

**解决**：使用全局唯一的 cookie 生成器（如 `AtomicInteger`）而非硬编码值或 `name.hashCode()`。在进程内保证 name+cookie 组合唯一。

### 6.5 Trace.isEnabled() 不可靠

`Trace.isEnabled()` 在 Java 层返回的是 TLS 缓存值，可能比实际状态滞后最多 64 次调用（`CACHE_REFRESH_RATE`）。在 tracing 刚开始或刚结束的边界上，此方法可能返回错误的值。

**影响**：如果你用 `Trace.isEnabled()` 来决定是否执行某些只为 trace 服务的辅助逻辑（如拼接 section name 字符串），在边界条件下可能产生少量无效写入或遗漏有效写入。

**建议**：不要将 `Trace.isEnabled()` 用于安全敏感的决策，只用于性能优化（避免在 tracing 关闭时拼接字符串）。

## 扩展

### 🔸 全链路追踪：从 App Trace 到系统 Trace

大型应用做全链路追踪的进阶思路是用统一 ID 串起各层事件：

1. 在应用层用 async section 打一个带唯一 ID 的 "requestTrace" marker
2. 在同一个请求路径经过的系统组件中（如 OkHttp Interceptor、Room DAO 的 `@Trace` 注解），用相同 ID 打 begin/end section
3. 在 Perfetto 中通过 SQL 关联不同 track 上的 slice，构建完整的调用链路

对于跨进程场景（如 App → SystemServer → SurfaceFlinger），async section 的 name + cookie 可以穿透进程边界，因为 trace_marker 是全局的（写入同一个内核 ftrace buffer）。

与 `androidx.tracing.perfetto` 的嵌套 trace 集成：Perfetto SDK 的自定义数据源可以和应用层 Trace marker 在同一 trace 中共存，SDK 事件出现在专用 track 上，atrace 事件出现在线程 track 上。两者通过时间戳天然对齐。

<!-- AIW-源码调研-2026-07-14 -->

## 7. 跨应用追踪：Android 17 源码级实现

> 以下内容基于 `android-17.0.0_r1` 源码直接调研。

### 7.1 跨进程追踪的三层基础设施

Android 17 的跨应用 Trace 体系建立在三层基础设施上，全部从源码可直接验证：

**第一层：全局共享的 trace_marker fd**

```c
// system/core/libcutils/trace-dev.cpp @ android-17.0.0_r1:32-46
static void atrace_init_once() {
    atrace_marker_fd = open("/sys/kernel/tracing/trace_marker",
                            O_WRONLY | O_CLOEXEC);
    if (atrace_marker_fd == -1) {
        atrace_marker_fd = open("/sys/kernel/debug/tracing/trace_marker",
                                O_WRONLY | O_CLOEXEC);
    }
    ...
}
```

每个进程独立 `open` trace_marker fd，但底层是**同一个内核 ftrace ring buffer**（写入路径：`trace_write` → `trace_seq_printf` → `ring_buffer_write`）。App A 写入的 `S|pidA|...` 与 App B 写入的 `S|pidB|...` 落在同一时间轴上。Perfetto `traced` 通过 `/sys/kernel/tracing/trace_pipe` 统一读出——这就是跨进程追踪的物理基础。

**第二层：SELinux 策略对 App 域写权限的授予**

```
# system/sepolicy/private/domain.te @ android-17.0.0_r1:275-280
# Almost all processes log tracing information to
# /sys/kernel/debug/tracing/trace_marker
# The reason behind this is documented in b/6513400
allow domain debugfs:dir search;
allow domain debugfs_tracing:dir search;
allow domain debugfs_tracing_debug:dir search;
allow domain debugfs_trace_marker:file w_file_perms;
```

`w_file_perms` 授予**所有域**（包括 `untrusted_app`、`platform_app`、`priv_app`）对 trace_marker 的写权限。读权限不在此授予——由 `traced.te` 单独控制 `traced_probes` 等系统服务读取。这避免了应用越权读取其他进程的 trace 事件。

**第三层：Binder AIDL 自动埋点（关键发现）**

`frameworks/native/libs/binder/Binder.cpp` 的 `BBinder::transact` 对每次 AIDL 调用自动包 trace：

```cpp
// Binder.cpp @ android-17.0.0_r1:484-501
__attribute__((noinline)) bool BBinder::startTrace(uint32_t code) {
    char traceSectionName[TRACE_BUFFER_SIZE];   // 512 字节
    status_t result = getTraceName(code, traceSectionName, TRACE_BUFFER_SIZE);
    if (result != OK) return false;
    trace_begin(ATRACE_TAG_AIDL, traceSectionName);
    return true;
}

status_t BBinder::transact(uint32_t code, const Parcel& data,
                            Parcel* reply, uint32_t flags) {
    bool tracingEnabled = get_trace_enabled_tags() & ATRACE_TAG_AIDL;
    if (tracingEnabled) {
        tracingEnabled = startTrace(code);
    }
    scope_guard guard = make_scope_guard([&]() {
        if (tracingEnabled) trace_end(ATRACE_TAG_AIDL);
    });
    // ... 实际 transact ...
}
```

trace 名称格式（`getTraceName`，Binder.cpp 第 739-806 行）：

```
AIDL::CPP::<descriptor>::<methodName>::server    // C++ backend
AIDL::JAVA::<descriptor>::<methodName>::server    // Java backend
```

例如：`AIDL::CPP::android.media.IMediaCodec::configure::server`。

> **值得注意的 gap**：`BpBinder::transact`（`frameworks/native/libs/binder/BpBinder.cpp:394-430`）未发现对应的 `trace_begin(ATRACE_TAG_AIDL, ...)` 调用。AIDL 的 server 端有完整 trace 段，client 端在 native 层没有对称埋点。这意味着在 Perfetto UI 上，AIDL 调用的"server 端执行时长"可见，但"client 发起 → server 收到"之间的网络/Binder 驱动开销不可见。需应用层用 `beginAsyncSection` 自行补齐 client 端。

### 7.2 Trace.java 在 API 37 的新能力

#### asyncTraceForTrackBegin / asyncTraceForTrackEnd

```java
// Trace.java @ android-17.0.0_r1:399-410
@SystemApi
@FlaggedApi(Flags.FLAG_ASYNC_TRACE_FOR_TRACK)
public static void asyncTraceForTrackBegin(
        @NonNull String trackName, @NonNull String methodName, int cookie) {
    asyncTraceForTrackBegin(TRACE_TAG_APP, trackName, methodName, cookie);
}
```

对比 `beginAsyncSection`：track 版本在**同 trackName + 同 cookie 上可嵌套**，而旧版 async section 显式禁止嵌套（`Trace.java:268` "asynchronous events cannot be not nested"）。跨进程场景下，track 版本对"父任务跨进程调度子任务"的层级展示更友好。

`@FlaggedApi(Flags.FLAG_ASYNC_TRACE_FOR_TRACK)` 意味着默认未启用——需要 aconfig 打开。**注意**：未开 flag 时调用此 API 会被运行时拒绝。

#### instant / instantForTrack

```java
// Trace.java @ android-17.0.0_r1:466-481
public static void instant(long traceTag, String methodName) { ... }
public static void instantForTrack(long traceTag, String trackName, String methodName) { ... }
```

写入格式（`trace-dev.cpp:89-95`）：

```
I|<pid>|<name>           // instant，单字符行，无持续时间
N|<pid>|<track>|<name>   // instantForTrack
```

`I` 行（instant）不带持续时间，但因为 ftrace ring buffer 是单调时钟写入，仍能作为跨进程同步点使用——例如"权限校验完成"、"数据预取完成"等瞬时事件。`<pid>` 字段让 Perfetto 能区分是哪个进程触发的同步点。

#### registerWithPerfetto：双 backend 共存

```java
// Trace.java @ android-17.0.0_r1:588-597
public static void registerWithPerfetto() {
    if (android.os.Flags.perfettoSdkTracingV3()) {
        com.android.internal.dev.perfetto.sdk.PerfettoTrace.register(
                false /* isBackendInProcess */);
        PerfettoCategories.registerCategories();
    }
}
```

启用后，`Trace.beginSection` 同时向 **atrace（kernel trace_marker）** 与 **Perfetto SDK（共享内存）** 写入。对跨应用追踪的意义：双 backend 让使用 `android.os.Trace` 的 App 与使用 Perfetto SDK 的 App 在同一 trace 中对齐时间线。

### 7.3 完整调用链：Java 到 trace_marker

```
Trace.beginAsyncSection("req-42", 1234)              [Trace.java:530]
  → asyncTraceBegin(TRACE_TAG_APP, "req-42", 1234)   [Trace.java:271]
    → nativeAsyncTraceBegin(tag, name, cookie)       [JNI: android_os_Trace.cpp:78]
      → tracing_perfetto::traceAsyncBegin(...)       [tracing_perfetto.cpp:51]
        → if shouldPreferAtrace():
            atrace_async_begin(tag, name, cookie)    [libcutils: trace.h]
              → atrace_async_begin_body(name, cookie) [trace-dev.cpp:77]
                → WRITE_MSG("S|%d|", "|%d", "", name, cookie)
                                                     [trace-dev.inc:187]
                  → write(atrace_marker_fd, buf, len)
                                                     [kernel sys_write]
```

每一层职责清晰：Java 短路判断 → JNI UTF-8 转换 → tracing_perfetto 决定 backend → atrace 格式化字符串 → WRITE_MSG 宏 snprintf + write。

### 7.4 跨进程追踪的实操模式

模式 A：AIDL 自动 + 应用 cookie 关联

```kotlin
// App A 发起跨进程请求
val cookie = AtomicInteger().incrementAndGet()
Trace.beginAsyncSection("cross_app_request:$cookie", cookie)
val result = binderService.doWork()  // server 端会写 AIDL::CPP::...::server
Trace.endAsyncSection("cross_app_request:$cookie", cookie)

// 在 Perfetto 中通过 SQL 用 name + cookie 把
// App A 的 begin/end 与 SystemServer 的 AIDL slice 对齐
```

模式 B：instant 作为同步信号

```kotlin
// App B 收到广播后写入 instant
override fun onReceive(ctx: Context, intent: Intent) {
    Trace.instant(Trace.TRACE_TAG_APP, "BroadcastReceived:myEvent")
    // 没有任何 duration 信息，只是一个时间点标记
}
```

模式 C：track 嵌套表达父子任务跨进程

```kotlin
// 父任务 token = 100
Trace.asyncTraceForTrackBegin("MyTaskTrack", "parent", 100)
// 调用子任务，子任务在另一个进程也写同一个 track + cookie = 100
val subToken = 100
Trace.asyncTraceForTrackBegin("MyTaskTrack", "child", subToken)
doSubWork()
Trace.asyncTraceForTrackEnd("MyTaskTrack", subToken)
Trace.asyncTraceForTrackEnd("MyTaskTrack", 100)
```

> 模式 C 要求 `FLAG_ASYNC_TRACE_FOR_TRACK` 已开启，否则会在 `isTagEnabled` 检查后被静默忽略——可以通过 `Trace.isEnabled()` + flag 双重确认。

### 7.5 跨进程追踪的 Perfetto SQL 查询范式

```sql
-- 查询某次跨进程请求的全链路 slice（系统 AIDL 自动埋点 + 应用自定义）
SELECT
    s.ts,
    s.dur,
    s.name,
    s.thread_name,
    proc.name AS process_name
FROM slice s
JOIN thread_track tt ON s.track_id = tt.id
JOIN thread t ON tt.utid = t.utid
JOIN process_track pt ON s.track_id = pt.id
JOIN process proc ON pt.upid = proc.upid
WHERE (s.name LIKE 'AIDL::%' OR s.name LIKE 'cross_app_request:%')
  AND proc.name IN ('com.app.a', 'com.app.b', 'system_server')
ORDER BY s.ts
LIMIT 200;

-- async section 跨进程 begin/end 配对
SELECT
    b.ts AS begin_ts,
    e.ts AS end_ts,
    (e.ts - b.ts) / 1e6 AS dur_ms,
    proc_b.name AS begin_proc,
    proc_e.name AS end_proc
FROM slice b
JOIN slice e
  ON b.name = e.name
 AND ABS(e.ts - b.ts) < 60e9  -- 60s 窗口
LEFT JOIN process_track pt_b ON b.track_id = pt_b.id
LEFT JOIN process proc_b ON pt_b.upid = proc_b.upid
LEFT JOIN process_track pt_e ON e.track_id = pt_e.id
LEFT JOIN process proc_e ON pt_e.upid = proc_e.upid
WHERE b.name LIKE 'cross_app_request:%'
  AND b.category = 'atrace'
  AND e.category = 'atrace';
```

### 7.6 性能影响（来自源码观察）

| 维度 | 开销 |
|------|------|
| tracing 未启用时 `beginAsyncSection` | ~2-5 ns（仅一次 `nativeIsTagEnabled` syscall） |
| tracing 启用时单次写 | ~1-3 μs（一次 `write(trace_marker_fd, ...)` 系统调用） |
| 跨进程追踪额外开销 | 零——AIDL 自动埋点已包含在 Binder 事务中 |
| fd 复用 | `atrace_marker_fd` 通过 `pthread_once` 只 open 一次（`trace-dev.cpp:27`） |
| 字符串拷贝 | `WRITE_MSG` 宏（`trace-dev.inc:187-216`）每次 snprintf 到 `ATRACE_MESSAGE_LENGTH` 栈 buffer |

### 7.7 局限性

1. **跨进程信息只通过时间轴对齐**：所有关联都靠「同一名字 + 同一 cookie」在 trace 时间轴上对齐，不存在进程间协议传递 trace context
2. **AIDL client 端 native 层无对称埋点**：服务端时长可见，但"客户端发起 → 服务端收到"之间存在不可见区域，需应用层补齐
3. **cookie 是 int 类型**：跨进程追踪时必须保证进程内单调递增的 int（如 `AtomicInteger.incrementAndGet()`），`name.hashCode()` 在多进程场景有碰撞风险
4. **ATRACE_TAG_AIDL 默认不启用**：抓取 trace 时需要 `-t aidl` 或在 Perfetto config 中显式开启
5. **section name 长度限制 127 字节**：`MAX_SECTION_NAME_LEN`（`Trace.java:115`），超长会被截断且 `beginSection` 会抛 `IllegalArgumentException`（`Trace.java:507-510`）

### 7.8 信息源

- [一手] `frameworks/base/core/java/android/os/Trace.java` @ `android-17.0.0_r1`（599 行全量）
- [一手] `frameworks/base/core/jni/android_os_Trace.cpp` @ `android-17.0.0_r1`（171 行）
- [一手] `frameworks/native/libs/tracing_perfetto/tracing_perfetto.cpp` @ `android-17.0.0_r1`（151 行）
- [一手] `system/core/libcutils/trace-dev.cpp` @ `android-17.0.0_r1`（116 行）
- [一手] `system/core/libcutils/trace-dev.inc` @ `android-17.0.0_r1`（`WRITE_MSG` 宏第 187-216 行）
- [一手] `frameworks/native/libs/binder/Binder.cpp` @ `android-17.0.0_r1`（关键段：第 60-69、480-505、735-810 行）
- [一手] `frameworks/native/libs/binder/BpBinder.cpp` @ `android-17.0.0_r1`（973 行；未发现 client-side AIDL trace）
- [一手] `frameworks/native/cmds/atrace/atrace.cpp` @ `android-17.0.0_r1`（ATRACE_TAG_AIDL 分类定义于第 127 行）
- [一手] `system/sepolicy/private/domain.te` @ `android-17.0.0_r1`（trace_marker SELinux 规则第 275-280 行）

## 参考资料

### Android 17 跨应用 Trace API 实践指南
- 来源：/Users/gracker/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian/DeepResearch/2026-07-14-android17-cross-app-trace-api.md
- 类型：DeepResearch 调研结果
- 摘要：Android 17 跨应用 Trace 体系基于三层基础设施：统一 ftrace ring buffer、Binder AIDL 自动埋点（ATRACE_TAG_AIDL）、应用层 asyncTraceForTrackBegin 叠加语义 ID。核心 API 包括 asyncTraceForTrackBegin/End 的嵌套追踪、instant/instantForTrack 跨进程同步点、registerWithPerfetto 双 backend 共存。关键限制是跨进程只读语义信息共享，不共享 TLS 或内存状态——关联靠「同一名字+同一 cookie」在 trace 时间轴上对齐。
- 注入时间：2026-07-14
- 价值：填补跨应用 trace 链路追踪的源码级空白，对 §13.26 的 API 深度解析提供了 Android 17 最新实现细节
