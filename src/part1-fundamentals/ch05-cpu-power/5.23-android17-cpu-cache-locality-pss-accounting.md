---
title: "Android 17 CPU 缓存局部性与 PSS 内存核算源码机制"
chapter: "5.23"
status: deprecated
applicable_versions: "Android 17 (API 37) - Android 17 (API 37)"
tags: [Android17, CPU缓存, PSS核算, 缓存局部性, 内存管理]
related_chapters: ["4.35", "5.21"]
created_by: "task2a-knowledge-gap"
created_date: "2026-07-07"
gap_source: "素材驱动"
---

# 5.23 Android 17 CPU 缓存局部性与 PSS 内存核算源码机制

<!-- outline-start -->
## 要点

### 🔹 三段式 PSS 内存核算架构
- 形成三种 cache 局部性不同的数据流
- Debug.MemoryInfo 新增的 4 个 Linux kernel 字段
- 不同缓存层次的数据结构差异分析

### 🔸 ART biased card table 的 cache 优化技巧
-biased card table 的设计原理
- 指令级预取优化策略
- 缓存行对齐的最佳实践

### 🔸 MemUsage 结构增强
- THP (Transparent Huge Page) 字段支持
- shmem_pmd_mapped 映射状态追踪
- 共享内存的精确核算机制

### 🔸 Scudo 分配器 page size 探测
- 动态 page size 切换算法
- 内存碎片优化的自适应策略
- 分配器选择与配置优化

### 🔸 生产级性能调优
- CPU 缓存局部性的监控指标
- PSS 内存核算的精度提升
- 大内存设备的特殊优化

## 扩展

### 🔸 多核 CPU 的缓存一致性
- Cache 一致性协议 (MESI) 优化
- 跨核心数据共享的性能影响
- NUMA 架构的内存访问优化

### 🔸 内存访问模式分析
- 缓存命中率监控工具
- 预取算法的改进策略
- 内存局部性的自动检测

<!-- outline-end -->

> [!WARNING]
> 本页是已废弃的重复选题，不再扩写。规范内容已迁移到 [5.18 Android 17 CPU 缓存局部性与 PSS 内存核算源码机制](5.18-android17-cpu-cache-locality-pss-accounting.md)。

## 为什么保留这个文件

该文件由历史 gap-mining 任务生成，标题与 5.18 相同，但没有形成正文。保留文件可以让旧链接和 Hermes/OpenClaw 的状态记录继续找到原目标；新增修订应进入 5.18，避免两篇同题文章出现不同源码结论。

## 已迁移到 5.18 的内容

5.18 已按 Android 17 / API 37 / `android-17.0.0_r1` 完成统一核验，覆盖：

- kernel `smaps`、`smaps_rollup` 对 RSS、PSS、USS、SwapPss 的核算边界；
- `libmeminfo` 对 rollup、完整 smaps 与 `StatusVmRSS()` 的选择；
- `Debug.getMemoryInfo()`、`Debug.getPss()`、`Debug.getRss()` 与 `ActivityManager.getProcessMemoryInfo()` 的差异；
- Android 17 `AppProfiler` 的 RSS/PSS 采样策略；
- CPU cache 指标与进程页核算不能互相替代的验证方法；
- ART TLAB、CardTable、cpuset 等容易被误读的实现细节。

## 本页 outline 中需要留意的旧假设

上方 outline 是历史规划记录，不作为技术结论。其中“Debug.MemoryInfo 新增 4 个 kernel 字段”“三段式 PSS 架构”“动态 page size 切换算法”“NUMA 局部内存优化”等说法缺少对应锚点源码，不能直接用于 Android 17 结论。

后续若发现新的 cache locality 或 PSS 证据，请先判断它属于 kernel 页核算、Android 用户态采集、ART heap 还是 CPU 微架构，再更新 5.18 的相应小节。
