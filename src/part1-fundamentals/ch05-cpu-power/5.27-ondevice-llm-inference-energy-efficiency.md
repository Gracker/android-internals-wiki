---
title: "Android 17 CPU 缓存局部性与 PSS 计量优化"
chapter: "5.27"
status: deprecated
applicable_versions: "Android 17 (API 37) - Android 17 (API 37)"
tags: [Android 17, 系统优化, 性能调试]
related_chapters: []
created_by: "task2a-knowledge-gap"
created_date: "2026-07-06"
gap_source: "研究素材"
---

# 5.27 Android 17 CPU 缓存局部性与 PSS 计量优化

<!-- outline-start -->
## 要点

### 🔹 GPU调试工具链架构
Android 17通过Perfetto集成的FrameTracer提供跨进程帧追踪，FPSReporter实现分层FPS监控，JankTracker进行卡顿分类检测，TimeStats提供详细的帧时间统计。硬件层的直接集成（如grallocusage用法转换）和用户态SurfaceFlinger中的GPU调试开关系统支持实时性能数据收集和分析。

### 🔹 SurfaceFlinger GPU调试系统
SurfaceFlinger初始化阶段加载大量GPU调试开关，包括回压控制、采样策略、预测策略等核心属性。系统通过debug.sf.enable_gl_backpressure等属性控制GPU合成行为，支持实时的性能调试与优化。

### 🔹 FPS监控架构
FPSReporter作为跨进程FPS监控的核心组件，通过LayerHierarchy分发层级FPS信息，支持多监听器注册与移除机制。系统设计考虑了进程间通信的稳定性与性能开销。

### 🔹 GPU回压控制机制
Android 17引入了基于时间戳的GPU回压控制策略，通过CompareByTargetRunTime排序算法和精确的时间等待机制，实现GPU合成任务的智能调度，避免了传统FIFO调度的性能瓶颈。

### 🔸 扩展：GPU调试工具的生产环境部署
探讨如何在生产环境中部署GPU调试工具，包括性能开销控制、数据采样策略、以及与现有监控系统的集成方案。

<!-- outline-end -->

> [!WARNING]
> 本页已废弃，不再生成正文。文件名、frontmatter 和 protected outline 分别指向三个不同主题，任何一项都不能代替另一项的技术结论。

## 冲突来源

- 文件名 `ondevice-llm-inference-energy-efficiency` 指向端侧大模型推理能效；
- frontmatter 与 H1 指向 CPU cache locality 和 PSS 核算；
- protected outline 指向 SurfaceFlinger/GPU 调试；
- 历史 gap-mining 曾为同一路径写入 MoE/量化草稿，随后 cleanup 将其标记为 `deprecated`。

原文声称“status 已改为 outline-broken”，但当前 frontmatter 的有效历史状态是 `deprecated`。本页保留该状态，避免 Hermes/OpenClaw 把错误选题重新送回正文加工。

## 内容应分别进入哪里

### CPU cache 与 PSS

请使用 [5.18 Android 17 CPU 缓存局部性与 PSS 内存核算源码机制](5.18-android17-cpu-cache-locality-pss-accounting.md)。该章已区分 CPU cache 指标、kernel 页核算与 Android 用户态采集 API。

### 端侧模型推理与能效

按问题类型使用以下章节：

- [5.11 端侧 ML 推理性能](11-ondevice-ml-inference-performance.md)：模型加载、runtime 边界、profiling 与应用侧优化；
- [5.13 移动端 LLM 的 DVFS 与能耗](13-mobile-llm-dvfs-energy.md)：prefill/decode 阶段、并发、温控和能量测量；
- [5.14 Android 17 ML runtime 与 NPU 能力边界](14-android17-ml-runtime-npu-boundary.md)：平台 API、vendor runtime 与 NPU 可用性的证据边界。

MoE、量化位宽和算子 offload 都依赖具体模型、runtime、delegate 和硬件能力，不能由本页文件名自动推出。

### GPU 调试

上方 outline 是误写模板，不在本页展开。涉及 FrameTimeline、SurfaceFlinger composition、GPU counter 或 vendor profiler 时，应进入对应渲染/GPU 章节，并以 Android 17 源码和目标 GPU 工具实测为依据。

## 保留文件的目的

本文件只承担旧链接与历史任务状态的兼容。后续自动任务不应修改 protected outline 之外的内容来“补齐”任一主题，也不应把本页恢复为 draft；应直接更新上面列出的规范章节。
