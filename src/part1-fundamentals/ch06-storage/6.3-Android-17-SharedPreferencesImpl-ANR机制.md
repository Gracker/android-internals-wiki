---
title: "Android 17 SharedPreferencesImpl ANR机制"
chapter: "6.3"
status: deprecated
applicable_versions: "Android 17 (API 37)"
tags: ['SharedPreferences', 'ANR', 'fsync', '存储性能', 'Android17', '文件系统']
related_chapters: []
created_by: "task2a-knowledge-gap"
created_date: "2026-07-02"
gap_source: "存储优化"
---
> ⚠️ **本节已隔离**：文件名与 SharedPreferences 主题相符，但受保护 outline 是 Activity 配置变更的错稿，且选题与 6.03、6.2 重复。为保留 Hermes/OpenClaw 的识别信息，本文件不删除，也不改写 outline；请勿把 outline 当作 SharedPreferences 资料引用。


# 6.3 Android 17 SharedPreferencesImpl ANR机制

<!-- outline-start -->
## 要点

### 🔹 配置变更判定模型
Android 17 中 ActivityRecord#shouldRelaunchLocked() 已被重构为基于配置差分和 Shell Transitions 的新判定模型。配置变更不再立即触发全局 relaunch，而是通过 TransitionController 创建 TRANSIT_CHANGE 过渡动画实现原子性配置更新。

### 🔹 精确触发条件
新增 RELAUNCH_REASON_WINDOWING_MODE_RESIZE 和 RELAUNCH_REASON_FREE_RESIZE 两种精确触发条件，使用 deferWindowLayout/continueWindowLayout 嵌套锁避免冗余布局计算。

### 🔹 6层掩码策略
skipRelaunchConfigMask 实现7层叠加：CONFIG_RESOURCES_UNUSED 直接 return false 后，(a)~(g) 六层叠加。DisplayCompatPolicy 是双路径策略，PiP skip CONFIG_DENSITY 是显式无条件豁免。

## 扩展

### 🔸 跨应用配置同步
配置变更在多应用场景下的同步机制与性能影响分析。

### 🔸 低内存条件下的配置优化
在内存受限情况下，配置变更的处理策略和性能调优。

<!-- outline-end -->

## 审阅结论

上方 outline 中的 `ActivityRecord#shouldRelaunchLocked()`、Shell Transitions、窗口模式 resize 和配置掩码均属于 Activity/WindowManager 方向，与 `SharedPreferencesImpl`、`QueuedWork`、XML 写盘或 ANR 机制没有技术关系。本文件不再扩写这些内容，也不把错稿迁移到其他 SharedPreferences 章节。

Android 17 / API 37 下的正确阅读入口如下：

- [6.03 Android 17 SharedPreferencesImpl ANR 机制](./6.03-Android-17-SharedPreferencesImpl-ANR机制.md)：短篇排查入口，覆盖首次加载等待、`commit()`、`apply()` 和生命周期收尾；
- [6.2 Android 17 SharedPreferencesImpl ANR 深度机制](./6.2-sharedpreferences-anr-optimization.md)：以 `android-17.0.0_r1` 为锚点解释锁、写盘代际和 `QueuedWork.waitToFinish()`；
- [6.5 SharedPreferences/DataStore 性能与 ANR 优化](./05-sharedpreferences-datastore.md)：讨论选型、迁移和故障边界。

上述章节已分别核对 AOSP `android-17.0.0_r1` 的 `SharedPreferencesImpl.java`、`QueuedWork.java`、`ActivityThread.java`，以及当前采用的稳定版 AndroidX DataStore 源码。
