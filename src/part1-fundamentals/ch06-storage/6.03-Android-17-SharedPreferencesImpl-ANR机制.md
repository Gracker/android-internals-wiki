---
title: "Android 17 SharedPreferencesImpl ANR机制"
chapter: "6.03"
status: finalized
applicable_versions: "Android 17 (API 37)"
tags: ['android', 'performance', 'system', 'sharedpreferences', 'anr', 'storage']
created_by: "task2a-knowledge-gap"
created_date: "2026-07-01"
gap_source: "DeepResearch + daily-info hot topic"
sources:
  - "DeepResearch/2026-07-01-sharedpreferences-impl-anr-mechanism.md"
last_verified: "2026-07-26"
reviewed_date: "2026-07-26"
reviewed_by: hermes-aiw-review-finalize-apply
confidence: high
pipeline_stage: finalized
task6_state: reviewed
task9_state: reviewed
last_deep_review_at: "2026-07-26T16:35:45+08:00"
last_deep_review_run_id: "20260726-163545-deep-review-fb0c2133"
last_review_finalize_at: "2026-07-26T18:07:19+08:00"
last_review_finalize_run_id: "20260726-180558-8fc790e7"
last_idle_audit_at: "2026-07-26T18:35:01+08:00"
last_idle_audit_run_id: "20260726-183501-idle-audit-fb0c2133"
last_idle_audit_log: "logs/audit/2026-07-26-20260726-183501-idle-audit-fb0c2133-idle-audit.md"
idle_audit_result: passed-with-minor-source-marking-fix
---

# 6.03 Android 17 SharedPreferencesImpl ANR 机制

> 本节锚定 AOSP `android-17.0.0_r1` / Android 17（API 37），源码范围包括 `SharedPreferencesImpl.java`、`QueuedWork.java`、`ActivityThread.java`、`SharedPreferences.java` 与 `FileUtils.java`。6.2 讲完整状态机，6.5 讲 SharedPreferences 与 DataStore 的选型；本节保留为 ANR 路径的短篇排查入口。

---

## 要点

### 1. SharedPreferencesImpl 的锁与文件状态

Android 17 中，`SharedPreferencesImpl` 仍是 `SharedPreferences` 的核心平台实现。它的性能风险来自“内存先行、磁盘异步或同步落盘”的设计，而不是 Android 17 新增了一套特殊 ANR 机制：

- **内存状态**：解析后的键值对保存在 `mMap`，编辑器通过 `commitToMemory()` 先合并到内存，再决定是否写盘。
- **文件状态**：主文件 `mFile` 与备份文件 `mBackupFile` 组成简单的崩溃恢复协议；写入前可能把旧 `.xml` rename 为 `.bak`，成功写入后删除 `.bak`，失败时保留备份供下次 `loadFromDisk()` 恢复。
- **锁边界**：源码采用多个 monitor 分工，不属于通用“读写锁分离”模型。`mLock` 保护加载/内存状态，`mWritingToDiskLock` 串行化磁盘写入，`EditorImpl` 还使用 `mEditorLock` 保护本次编辑的修改集。
- **加载线程**：构造 `SharedPreferencesImpl` 后通过 `startLoadFromDisk()` 把解析任务投递到静态 `sLoadExecutor`；调研材料记录该 executor 为 0-1 核心的串行线程池，空闲约 10 秒后回收线程。

### 2. 读路径风险：`awaitLoadedLocked()` 会让调用线程等待

首次访问 SharedPreferences 时，真实 XML 解析虽然在后台加载线程上执行，但调用方读值仍可能在主线程等待加载完成：

```text
ContextImpl.getSharedPreferences()
  -> new SharedPreferencesImpl(...)
  -> startLoadFromDisk()

UI thread: getString()/getInt()/edit()
  -> synchronized (mLock)
  -> awaitLoadedLocked()
  -> while (!mLoaded) mLock.wait()

sLoadExecutor:
  -> loadFromDisk()
  -> XmlUtils.readMapXml(...)
  -> synchronized (mLock) { mLoaded = true; mLock.notifyAll(); }
```

两个容易误判的点：

1. `awaitLoadedLocked()` 在等待前会调用 `BlockGuard.getThreadPolicy().onReadFromDisk()`。这是为了让 StrictMode 能把“主线程等待读盘结果”识别为读盘违规，即使实际文件读取发生在后台线程。
2. SharedPreferences 没有定义固定的 5 秒或 10 秒 ANR 门槛；最终是否形成 ANR 取决于调用场景对应的系统超时与同时发生的其他阻塞。SharedPreferences 在这里提供的是“主线程可能长期等待”的条件。

### 3. 写路径风险：`commit()` 同步等待，`apply()` 受生命周期收尾约束

`commit()` 与 `apply()` 的区别主要体现在写盘线程和结果返回语义：

- **`commit()`**：调用 `commitToMemory()` 后等待 `writtenToDiskLatch`，再返回成功或失败。当 `mDiskWritesInFlight == 1` 时，`enqueueDiskWrite()` 直接在调用线程执行 `writeToFile()`；已有写任务时，它把本次写入立即排入 `QueuedWork`，调用线程仍会等待 latch。主线程调用 `commit()` 既可能亲自执行 `FileUtils.sync()`，也可能等待前序任务和本次写盘。
- **`apply()`**：先把变更合并到内存并立即返回，不暴露写盘失败信号；写盘任务通过 `QueuedWork.queue()` 放到 `queued-work-looper` 后台线程串行执行。
- **关键风险**：`apply()` 返回后会向 `QueuedWork` 注册 finisher。组件生命周期收尾调用 `QueuedWork.waitToFinish()` 时，主线程会先调用 `processPendingWork()`；若任务尚未被后台线程取走，XML 写入与 fsync 可以直接在主线程执行。随后主线程运行 finisher，并通过 latch 等待已经在其他线程执行的写入结束。

下面的调用链用于区分“主线程执行写盘”和“主线程等待后台写盘”两种情况：

```text
Activity A: editor.apply()
  -> queued-work-looper 正在 fsync
用户切到 Activity B / Activity A stop
  -> ActivityThread.handleStopActivity()
  -> QueuedWork.waitToFinish()
  -> 主线程等待 finisher / 写盘完成
慢存储或队列堆积
  -> 主线程长时间无响应，可能演化为 ANR
```

`waitToFinish()` 并非只等待一个后台队列：它允许当前线程接手尚未执行的 work。因而，ANR trace 可能落在 `FileUtils.sync()`，也可能落在 `CountDownLatch.await()`；两种栈都符合 `apply()` 在生命周期边界反压主线程的机制。

### 4. `QueuedWork.waitToFinish()` 的 Android 17 调用点

调研材料在 `ActivityThread.java`（`android-17.0.0_r1`）中定位到 4 个 `QueuedWork.waitToFinish()` 主线程调用点：

| 场景 | 调用点含义 | 风险说明 |
|---|---|---|
| `handleServiceArgs()` | `onStartCommand()` / `onTaskRemoved()` 返回后收尾 | Service 回调中的 apply 可能反压主线程 |
| `handleStopService()` | `onDestroy()` 与资源清理后收尾 | 停止 Service 时处理未完成写盘 |
| `handlePauseActivity` | legacy / pre-Honeycomb Activity 路径 | 旧兼容路径，不是现代 Activity 的主要触发点 |
| `handleStopActivity` | Activity 停止时收口 | 常见：页面跳转、退后台、配置变化后等待 apply 写盘 |

`QueuedWork` 内部维护等待耗时 histogram，并使用 `MAX_WAIT_TIME_MILLIS = 512ms` 作为慢等待告警阈值。需要注意：512ms 是 waitToFinish 的慢等待记录/告警阈值，不是 EditorImpl 的操作超时，也不是“512ms 后自动取消写盘”。

### 5. fsync histogram 只负责观测

`SharedPreferencesImpl.writeToFile()` 在 XML 内容写入后调用 `FileUtils.sync(FileOutputStream)`，后者包装 `stream.getFD().sync()`。源码调研材料记录写盘结束后会把 fsync 耗时写入 `mSyncTimes`：

```java
long fsyncDuration = fsyncTime - writeTime;
mSyncTimes.add((int) fsyncDuration);
mNumSync++;
if (DEBUG || mNumSync % 1024 == 0 || fsyncDuration > MAX_FSYNC_DURATION_MILLIS) {
    mSyncTimes.log(TAG, "Time required to fsync " + mFile + ": ");
}
```

- `MAX_FSYNC_DURATION_MILLIS = 256ms`：单次 fsync 超过 256ms 会触发日志输出。
- `ExponentiallyBucketedHistogram(16)`：用于记录 fsync 耗时分布。
- **审计修正**：histogram 只在 fsync 完成后观测耗时，不会主动阻塞业务，也不是 ANR 触发器；它也不直接暴露 `QueuedWork` 队列长度。

---

## 工程建议

### 1. 主线程读路径：避免首次访问即等待 XML 解析

- 不要在启动首帧、`ContentProvider.onCreate()`、首个 `Activity.onCreate()` 的关键路径上首次读取大 SharedPreferences 文件。
- 若仍需使用 SharedPreferences，可在可控时机提前触发小文件加载；但这只是把等待移动到更安全的窗口，不改变底层 XML 解析成本。
- SharedPreferences 每次持久化的是整个 map 对应的 XML。大对象、列表和高频状态不适合继续累积在同一个文件中。

### 2. 写路径：减少生命周期收口时的 apply 堆积

- UI 生命周期回调（尤其 `onPause()` / `onStop()`）中避免批量 `apply()` 多个文件。
- 把高频计数、埋点缓存、会话状态与低频配置拆开，避免每次页面切换都触发同一 SharedPreferences 文件重写。
- 对必须知道持久化结果的关键配置，不能简单把 `commit()` 机械替换为 `apply()`；应在后台线程执行同步写入并显式处理失败。

### 3. 迁移策略：DataStore 是新实现优先项

Android 17 的 `SharedPreferences.java` javadoc 已更明确列出 SharedPreferences 的缺陷：UI 阻塞、`apply()` 可能触发生命周期等待、缺少错误信号、内存状态可能领先磁盘状态。新代码优先考虑 Jetpack DataStore；老代码迁移时建议：

1. 先识别启动关键路径和生命周期回调中的 SharedPreferences 读写。
2. 使用 `SharedPreferencesMigration` 在 DataStore 首次访问前完成一次性迁移；迁移成功后不要长期保留两个可写数据源。
3. 迁移完成后让业务只从 DataStore 读取。需要回滚时，应设计明确的数据版本与回退流程，避免按“哪个值非空就用哪个”的双读逻辑制造新旧值竞争。

### 4. 诊断方法

- **StrictMode**：用于暴露主线程首次读取 SharedPreferences 的风险；`awaitLoadedLocked()` 已主动标记 read-from-disk。
- **日志与源码 histogram**：关注 `Time required to fsync ...` 相关日志，尤其是超过 256ms 的 fsync。
- **ANR trace / Perfetto**：从主线程栈中确认是否卡在 `QueuedWork.waitToFinish()`、`CountDownLatch.await()` 或 SharedPreferences 加载等待；Perfetto 侧应使用通用调度、block I/O、binder/lifecycle 等信号交叉判断。不要假设存在 `sharedpreferences_read` / `sharedpreferences_write` 这类 AOSP ftrace 事件。

---

## 边界说明

- 所有平台源码结论限制在 `android-17.0.0_r1`。
- `@RavenwoodKeepWholeClass` 是测试环境相关注解，不能推导为 SharedPreferences 的应用侧性能变化。
- 设备端 fsync 延迟高度依赖闪存、文件系统、压力与厂商内核策略；文中不使用未实测的固定耗时作为普遍结论。
- Perfetto 可辅助定位调度和 I/O 等待，但本节不声明 Android 17 平台提供专属 SharedPreferences ftrace 事件。

## 参考资料

### Android 17 SharedPreferencesImpl 性能与 ANR 优化机制

- 来源：`DeepResearch/2026-07-01-sharedpreferences-impl-anr-mechanism.md`
- 类型：DeepResearch 源码调研结果
- 源码锚点：AOSP `android-17.0.0_r1` 的 `frameworks/base/core/java/android/app/SharedPreferencesImpl.java`、`QueuedWork.java`、`ActivityThread.java`、`frameworks/base/core/java/android/content/SharedPreferences.java`、`frameworks/base/core/java/android/os/FileUtils.java`。
- 摘要：首次加载会使调用 `getXxx()` / `edit()` 的线程在 `awaitLoadedLocked()` 等待 `sLoadExecutor`。`commit()` 会同步等待写盘；`apply()` 通常由后台线程写盘，但 `QueuedWork.waitToFinish()` 可以在 Activity/Service 生命周期收尾时由主线程处理待执行 work 或等待 finisher。`MAX_WAIT_TIME_MILLIS=512ms` 是 waitToFinish 慢等待告警阈值；fsync 超过 `MAX_FSYNC_DURATION_MILLIS=256ms` 会记录到 `mSyncTimes` 指数 histogram。
- 注入时间：2026-07-04
- 价值：补齐 SharedPreferences ANR 调用链、fsync 监控和 Android 17 版本边界。
