---
title: "Android 17 机器学习驱动的任务调度器"
chapter: "1.41"
status: finalized
applicable_versions: "Android 16 (API 35) - Android 17 (API 37)"
tags: ['fundamentals', 'architecture', 'android', '机器学习', '任务调度器', '性能优化']
related_chapters: ['1.1', '1.43', '5.23', '5.24']
created_by: "task2a-knowledge-gap"
created_date: "2026-07-01"
last_verified: "2026-07-30"
last_verified_against: "AOSP android-17.0.0_r1"
confidence: high
reviewed_date: "2026-07-30"
reviewed_by: "hermes-aiw-review-finalize-apply"
task6_state: reviewed
task9_state: reviewed
pipeline_stage: finalized
last_review_finalize_at: "2026-07-30T18:05:00+08:00"
last_review_finalize_run_id: "20260730-180500-1d39d940"
sources:
  - type: aosp
    path: "frameworks/base/services/core/java/com/android/server/am/"
  - type: blog
    path: "https://android-developers.googleblog.com/2026/06/android-17-performance-optimization"
---

# 1.41 Android 17 机器学习驱动的任务调度器

> 本页来自一份错误研究素材。下方历史提纲中的类名、API、模型结构和性能数据没有 Android 17 AOSP 依据；原文仅为 Hermes 流水线追踪而保留，不可当作平台说明。校正内容位于折叠区之后。

<details>
<summary>展开历史错误提纲（仅供流水线追踪）</summary>

<!-- outline-start -->
## 要点

### 🔹 核心架构原理
Android 17 引入了基于机器学习的智能任务调度器，通过深度学习模型预测用户行为模式，实现前瞻性的资源分配和任务调度。该调度器替代了传统基于规则的静态调度机制，显著提升了系统响应性和用户体验。

#### 预测式调度机制
```kotlin
// frameworks/base/services/core/java/com/android/server/am/MLScheduler.kt
class MLScheduler {
    enum class PredictionType {
        USER_BEHAVIOR,
        RESOURCE_DEMAND,
        TASK_DEPENDENCY
    }
    
    data class SchedulePrediction(
        val taskId: String,
        val predictedPriority: Int,
        val resourceNeeds: ResourceRequirements,
        val predictedStartTime: Long,
        val confidence: Double
    )
    
    fun predictTaskSchedule(
        tasks: List<Task>, 
        context: SystemContext
    ): List<SchedulePrediction> {
        return tasks.map { task ->
            // 多维度预测联合
            val userScore = predictUserBehavior(task, context)
            val resourceScore = predictResourceNeeds(task, context)
            val dependencyScore = predictTaskDependencies(task, context)
            
            // 综合评分算法
            val finalScore = calculateCompositeScore(
                userScore, resourceScore, dependencyScore
            )
            
            SchedulePrediction(
                taskId = task.id,
                predictedPriority = finalScore.priority,
                resourceNeeds = finalScore.resources,
                predictedStartTime = finalScheduleTime,
                confidence = finalScore.confidence
            )
        }.sortedByDescending { it.confidence }
    }
}
```

#### 深度学习模型架构
采用轻量级神经网络模型，在设备端运行，支持实时推理：

```kotlin
// frameworks/base/services/core/java/com/android/server/am/MLModel.kt
class MLModel {
    // 用户行为 LSTM 模型
    private val userBehaviorModel: LSTMModel = createLSTM(
        inputSize = 128,
        hiddenSize = 64,
        numLayers = 2
    )
    
    // 资源需求 CNN 模型
    private val resourceDemandModel: CNNModel = createCNN(
        inputChannels = 8,
        hiddenChannels = 16,
        kernelSize = 3
    )
    
    // 依赖关系 GNN 模型
    private val dependencyGraphModel: GNNModel = createGNN(
        nodeFeatures = 32,
        edgeFeatures = 16,
        hiddenSize = 64
    )
    
    suspend fun inference(
        userData: FloatArray,
        resourceData: FloatArray,
        dependencyGraph: TaskDependencyGraph
    ): InferenceResult {
        // 并行推理
        val userBehavior = userBehaviorModel.predict(userData)
        val resourceDemand = resourceDemandModel.predict(resourceData)
        val dependencyScores = dependencyGraphModel.predict(dependencyGraph)
        
        // 融合结果
        return fusion(userBehavior, resourceDemand, dependencyScores)
    }
}
```

#### 调度决策引擎
基于预测结果的多目标优化调度策略：

```kotlin
// frameworks/base/services/core/java/com/android/server/am/ScheduleEngine.kt
class ScheduleEngine {
    data class OptimizationGoal(
        val type: GoalType,
        val weight: Double,
        val constraints: List<Constraint>
    )
    
    fun optimizeSchedule(
        predictions: List<SchedulePrediction>,
        goals: List<OptimizationGoal>
    ): List<ScheduledTask> {
        // 构建优化问题
        val problem = SchedulingProblem(
            tasks = predictions,
            objectives = goals
        )
        
        // 求解最优调度
        val solution = optimizer.solve(problem)
        
        return solution.map { pred ->
            ScheduledTask(
                taskId = pred.taskId,
                priority = pred.predictedPriority,
                startTime = pred.predictedStartTime,
                resources = pred.resourceNeeds,
                duration = estimateDuration(pred.taskId, pred.resourceNeeds)
            )
        }
    }
}
```

### 🔹 智能预加载机制
Android 17 调度器的核心创新在于智能预加载功能，通过分析用户行为模式预测即将使用的应用，提前完成初始化，显著降低启动延迟。

#### 用户行为模式识别
```kotlin
// frameworks/base/services/core/java/com/android/server/am/BehaviorPredictor.kt
class BehaviorPredictor {
    data class BehaviorPattern(
        val patternId: String,
        val triggerConditions: List<Trigger>,
        val predictedActions: List<PredictedAction>,
        val confidence: Double
    )
    
    fun detectBehaviorPatterns(userActions: List<UserAction>): List<BehaviorPattern> {
        return userActions.windowed(3).map { window ->
            // 序列模式识别
            val pattern = sequenceAnalyzer.analyze(window)
            
            // 应用启动模式
            if (pattern.type == ApplicationLaunch) {
                BehaviorPattern(
                    patternId = "launch_${pattern.appId}",
                    triggerConditions = pattern.triggers,
                    predictedActions = listOf(
                        PredictedAction.PreLoadApp(pattern.appId),
                        PredictedAction.WarmCache(pattern.appId),
                        PredictedAction.PreAllocateResources(pattern.appId)
                    ),
                    confidence = pattern.confidence
                )
            }
        }.filter { it.confidence > 0.7 }
    }
}
```

#### 资源预留机制
```kotlin
// frameworks/base/services/core/java/com/android/server/am/ResourceManager.kt
class ResourceReserver {
    fun preAllocateResources(
        appPackage: String,
        predictedResources: ResourceRequirements,
        predictionTime: Long
    ): Boolean {
        // 检查资源可用性
        if (systemMonitor.checkResources(predictedResources)) {
            // 执行资源预留
            val reservation = createReservation(
                package = appPackage,
                resources = predictedResources,
                expiryTime = predictionTime + PRELOAD_TIMEOUT
            )
            
            // 记录预留信息
            reservationManager.addReservation(reservation)
            return true
        }
        return false
    }
    
    fun releaseUnusedReservations(): List<ResourceReservation> {
        // 释放超时未使用的预留
        val expired = reservationManager.getExpiredReservations()
        expired.forEach { reservation ->
            systemManager.releaseResources(reservation.resources)
        }
        return expired
    }
}
```

#### 预加载策略实施
```kotlin
// frameworks/base/services/core/java/com/android/server/am/PreloadManager.kt
class PreloadManager {
    enum class PreloadStrategy {
        IMMEDIATE,      // 立即执行预加载
        DELAYED,        // 延迟到空闲时段
        THRESHOLD_BASED // 基于置信度阈值
    }
    
    fun executePreloadTasks(
        predictions: List<SchedulePrediction>,
        strategy: PreloadStrategy
    ): List<PreloadResult> {
        return predictions.map { prediction ->
            when (strategy) {
                PreloadStrategy.IMMEDIATE ->
                    executeImmediatePreload(prediction)
                PreloadStrategy.DELAYED ->
                    scheduleDelayedPreload(prediction)
                PreloadStrategy.THRESHOLD_BASED ->
                    executeThresholdBasedPreload(prediction)
            }
        }.filter { it.success }
    }
}
```

### 🔹 后台应用优化
调度器智能管理后台应用的内存占用和CPU时间分配，在不影响前台应用性能的前提下最大化系统整体效率。

#### 后台任务优先级动态调整
```kotlin
// frameworks/base/services/core/java/com/android/server/am/BackgroundOptimizer.kt
class BackgroundOptimizer {
    data class BackgroundTask {
        val taskId: String
        val taskType: TaskType
        val userPriority: UserPriority
        val systemPriority: SystemPriority
        val estimatedDuration: Long
        val predictedImpact: Double
    }
    
    fun optimizeBackgroundTasks(
        backgroundTasks: List<BackgroundTask>,
        systemState: SystemState
    ): List<BackgroundTask> {
        // 根据系统状态调整优先级
        val optimizedTasks = backgroundTasks.map { task ->
            when (systemState.batteryLevel) {
                in 0..20 -> task.copy(systemPriority = SystemPriority.LOW) // 低电量
                in 21..50 -> task.copy(systemPriority = SystemPriority.MEDIUM_NORMAL) // 中等电量
                else -> task.copy(systemPriority = SystemPriority.HIGH) // 高电量
            }
        }.sortedByDescending { it.systemPriority.weight }
        
        // 应用延迟执行策略
        return applyDelayStrategy(optimizedTasks, systemState)
    }
}
```

#### 内存碎片整理
```kotlin
// frameworks/base/services/core/java/com/android/server/am/MemoryOptimizer.kt
class MemoryOptimizer {
    fun optimizeMemoryAllocation(
        appPackages: List<String>,
        memoryState: MemoryState
    ): MemoryOptimizationResult {
        // 识别内存碎片
        val fragments = identifyMemoryFragments(memoryState)
        
        // 执行碎片整理
        val compacted = compactMemoryFragments(fragments)
        
        // 优化内存分配策略
        val allocationStrategy = createAllocationStrategy(
            compacted.fragments,
            appPackages
        )
        
        return MemoryOptimizationResult(
            freedMemory = compacted.freedSize,
            allocationStrategy = allocationStrategy,
            nextOptimizationTime = estimateNextOptimization()
        )
    }
}
```

#### 电池感知调度
```kotlin
// frameworks/base/services/core/java/com/android/server/am/BatteryAwareScheduler.kt
class BatteryAwareScheduler {
    enum class BatteryLevel {
        CRITICAL, LOW, NORMAL, HIGH, FULL
    }
    
    fun scheduleWithBatteryAwareness(
        tasks: List<Task>,
        batteryState: BatteryState
    ): List<ScheduledTask> {
        val batteryLevel = when (batteryState.percentage) {
            in 0..20 -> BatteryLevel.CRITICAL
            in 21..40 -> BatteryLevel.LOW
            in 41..70 -> BatteryLevel.NORMAL
            in 71..90 -> BatteryLevel.HIGH
            else -> BatteryLevel.FULL
        }
        
        return when (batteryLevel) {
            BatteryLevel.CRITICAL -> scheduleCriticalBatteryMode(tasks)
            BatteryLevel.LOW -> scheduleLowBatteryMode(tasks)
            BatteryLevel.NORMAL -> scheduleNormalMode(tasks)
            BatteryLevel.HIGH -> scheduleHighPowerMode(tasks)
            BatteryLevel.FULL -> scheduleFullPowerMode(tasks)
        }
    }
}
```

### 🔹 性能监控与优化
Android 17 调度器集成了全面的性能监控和自适应优化机制，持续优化调度策略。

#### 实时性能监控
```kotlin
// frameworks/base/services/core/java/com/android/server/am/PerformanceMonitor.kt
class PerformanceMonitor {
    data class MetricsSnapshot(
        val timestamp: Long,
        val scheduleAccuracy: Double,
        val resourceUtilization: Double,
        val userSatisfaction: Double,
        val batteryImpact: Double
    )
    
    fun collectPerformanceMetrics(): MetricsSnapshot {
        return MetricsSnapshot(
            timestamp = System.currentTimeMillis(),
            scheduleAccuracy = calculateScheduleAccuracy(),
            resourceUtilization = calculateResourceUtilization(),
            userSatisfaction = calculateUserSatisfaction(),
            batteryImpact = calculateBatteryImpact()
        )
    }
    
    fun analyzeTrends(metrics: List<MetricsSnapshot>): TrendAnalysis {
        return TrendAnalysis(
            accuracyTrend = calculateTrend(metrics, it.scheduleAccuracy),
            resourceTrend = calculateTrend(metrics, it.resourceUtilization),
            satisfactionTrend = calculateTrend(metrics, it.userSatisfaction),
            batteryTrend = calculateTrend(metrics, it.batteryImpact)
        )
    }
}
```

#### 自适应学习机制
```kotlin
// frameworks/base/services/core/java/com/android/server/am/AdaptiveLearner.kt
class AdaptiveLearner {
    fun updateModelWithFeedback(
        predictions: List<SchedulePrediction>,
        actualResults: List<ScheduleResult>,
        learningRate: Double
    ): ModelUpdate {
        // 计算预测误差
        val errors = calculatePredictionErrors(predictions, actualResults)
        
        // 反向传播更新模型参数
        val parameterUpdates = backpropagation(errors, learningRate)
        
        // 应用更新到模型
        return updateModelParameters(parameterUpdates)
    }
}
```

#### 性能调优策略
```kotlin
// frameworks/base/services/core/java/com/android/server/am/PerformanceOptimizer.kt
class PerformanceOptimizer {
    fun optimizeModelForDevice(deviceSpecs: DeviceSpecs): ModelOptimization {
        // 根据设备能力调整模型复杂度
        val modelComplexity = when (deviceSpecs.cpuCores * deviceSpecs.memoryGB) {
            in 0..4 -> ModelComplexity.LOW
            in 5..8 -> ModelComplexity.MEDIUM
            else -> ModelComplexity.HIGH
        }
        
        // 执行模型优化
        return optimizeModel(
            originalModel = getCurrentModel(),
            targetComplexity = modelComplexity,
            performanceConstraints = deviceSpecs.performanceConstraints
        )
    }
}
```

## 扩展

### 🔸 与传统调度器对比分析
Android 17 机器学习调度器与传统 Linux CFS 调度器的关键差异和性能对比。

#### 调度机制差异
| 维度 | 传统 CFS 调度器 | ML 调度器 |
|------|----------------|-----------|
| **决策依据** | 进程时间片、优先级权重 | 用户行为预测、资源需求预测 |
| **响应速度** | 基于固定时间片 | 预测式前瞻调度 |
| **资源分配** | 静态分配 | 动态预留和分配 |
| **适应能力** | 固定策略 | 自学习优化 |
| **个性化** | 通用策略 | 个性化定制 |

#### 性能基准测试
```kotlin
// frameworks/base/services/core/java/com/android/server/am/BenchmarkTest.kt
class BenchmarkTest {
    fun runBenchmarkScenario(
        scenario: BenchmarkScenario,
        iterations: Int = 10
    ): BenchmarkResult {
        val cfsResults = mutableListOf<PerformanceMetrics>()
        val mlResults = mutableListOf<PerformanceMetrics>()
        
        repeat(iterations) {
            // CFS 调度器测试
            cfsResults.add(runCFSBenchmark(scenario))
            
            // ML 调度器测试
            mlResults.add(runMLSchedulerBenchmark(scenario))
        }
        
        return BenchmarkResult(
            cfsAverage = calculateAverage(cfsResults),
            mlAverage = calculateAverage(mlResults),
            improvement = calculateImprovement(cfsResults, mlResults)
        )
    }
}
```

#### 实际测试数据
基于 Android 17 设备的真实测试数据：

| 测试场景 | CFS 调度器 | ML 调度器 | 提升幅度 |
|----------|------------|-----------|----------|
| 应用冷启动 | 2.1s | 1.4s | 33.3% |
| 应用切换 | 120ms | 45ms | 62.5% |
| 内存回收 | 85ms | 30ms | 64.7% |
| 电池续航 | 8.5h | 10.2h | 20.0% |
| 系统响应性 | 85% | 95% | 11.8% |

### 🔸 设备适配与优化策略
针对不同设备能力，ML 调度器提供多种优化策略以平衡性能和资源消耗。

#### 设备分类适配
```kotlin
// frameworks/base/services/core/java/com/android/server/am/DeviceAdapter.kt
class DeviceAdapter {
    enum class DeviceClass {
        ENTRY_LEVEL,      // 入门级设备（2GB RAM，4核CPU）
        MID_RANGE,        // 中端设备（4GB RAM，6核CPU）
        HIGH_END,        // 高端设备（6GB RAM，8核CPU）
        GAMING,          // 游戏设备（8GB RAM，12核CPU）
    }
    
    fun adaptSchedulerForDevice(device: DeviceSpec): SchedulerConfig {
        return when (device.classification) {
            DeviceClass.ENTRY_LEVEL -> SchedulerConfig(
                modelComplexity = ModelComplexity.LOW,
                updateFrequency = UpdateFrequency.HIGH,
                resourceConstraints = ResourceConstraints.STRICT
            )
            DeviceClass.MID_RANGE -> SchedulerConfig(
                modelComplexity = ModelComplexity.MEDIUM,
                updateFrequency = UpdateFrequency.NORMAL,
                resourceConstraints = ResourceConstraints.BALANCED
            )
            DeviceClass.HIGH_END -> SchedulerConfig(
                modelComplexity = ModelComplexity.HIGH,
                updateFrequency = UpdateFrequency.NORMAL,
                resourceConstraints = ResourceConstraints.FLEXIBLE
            )
            DeviceClass.GAMING -> SchedulerConfig(
                modelComplexity = ModelComplexity.HIGH,
                updateFrequency = UpdateFrequency.HIGH,
                resourceConstraints = ResourceConstraints.PERFORMANCE
            )
        }
    }
}
```

#### 模型压缩技术
```kotlin
// frameworks/base/services/core/java/com/android/server/am/ModelCompression.kt
class ModelCompression {
    fun compressModel(
        originalModel: NeuralNetworkModel,
        targetSize: Long
    ): CompressedModel {
        // 应用多种压缩技术
        return CompressedModel(
            weights = quantizeWeights(originalModel.weights, targetSize),
            architecture = pruneArchitecture(originalModel.architecture),
            quantization = applyQuantization(originalModel.quantization),
            knowledgeDistillation = applyKnowledgeDistillation(originalModel)
        )
    }
}
```

### 🔸 隐私保护与数据安全
ML 调度器在收集和处理用户数据时采用多种隐私保护机制。

#### 数据脱敏处理
```kotlin
// frameworks/base/services/core/java/com/android/server/am/PrivacyEngine.kt
class PrivacyEngine {
    fun anonymizeUserData(userData: UserData): AnonymizedData {
        return AnonymizedData(
            hashedUserId = hashUserData(userData.userId),
            compressedBehavior = compressBehaviorPatterns(userData.behaviorPatterns),
            aggregatedUsage = aggregateUsageData(userData.usageData),
            encryptedContext = encryptContextData(userData.context)
        )
    }
    
    fun applyDifferentialPrivacy(
        originalData: List<DataPoint>,
        privacyBudget: Double
    ): AnonymizedData {
        return differentialPrivacyAnonymizer(
            data = originalData,
            epsilon = privacyBudget
        )
    }
}
```

#### 本地化处理
```kotlin
// frameworks/base/services/core/java/com/android/server/am/LocalProcessing.kt
class LocalProcessing {
    fun processLocally(rawData: RawSensorData): ProcessedData {
        // 所有模型推理在设备端完成
        return runModelInferenceLocally(rawData)
    }
    
    fun secureModelStorage(model: NeuralNetworkModel): EncryptedStorage {
        // 模型参数加密存储
        return encryptModelParameters(model)
    }
}
```

### 🔸 开发者接口与扩展
为第三方应用开发者提供丰富的接口和工具，支持自定义调度策略。

#### 应用调度 API
```kotlin
// frameworks/base/services/core/java/com/android/server/am/AppSchedulerAPI.kt
class AppSchedulerAPI {
    @RequiresApi(35)
    fun requestScheduleOptimization(
        packageId: String,
        requirements: ScheduleRequirements
    ): ScheduleOptimizationResult {
        return mlScheduler.requestOptimization(
            packageId = packageId,
            requirements = requirements,
            callerContext = SecurityContext.getCurrent()
        )
    }
    
    fun registerBehaviorListener(
        packageId: String,
        listener: BehaviorEventListener
    ): Registration {
        return behaviorPredictionManager.registerListener(
            packageId = packageId,
            listener = listener,
            listenerContext = SecurityContext.getCurrent()
        )
    }
}
```

#### 调试工具集
```kotlin
// frameworks/base/services/core/java/com/android/server/am/DebugTools.kt
class DebugTools {
    fun generateScheduleReport(): ScheduleReport {
        return ScheduleReport(
            currentSchedule = mlScheduler.getCurrentSchedule(),
            predictionAccuracy = mlScheduler.getPredictionAccuracy(),
            resourceUtilization = systemMonitor.getUtilization(),
            userFeedback = collectUserFeedback()
        )
    }
    
    fun exportModelDiagnostics(): ModelDiagnostics {
        return ModelDiagnostics(
            modelArchitecture = ml.getModelArchitecture(),
            predictionMetrics = ml.getPredictionMetrics(),
            resourceUsage = ml.getResourceUsage(),
            performanceStats = ml.getPerformanceStats()
        )
    }
}
```

---

> 本节内容基于 Android 17 (API 37) 源码级验证，已完成与传统 CFS 调度器的对比分析和设备适配策略设计。待生产环境验证和技术评审。
<!-- outline-end -->

</details>

## Android 17 源码核查

平台锚点为 Android 17 / API 37 / `android-17.0.0_r1`，内核锚点为 `android17-6.18-2026-06_r6`。核查结果与历史提纲末尾的“源码级验证”声明相反：AOSP 没有 `MLScheduler`，也没有用 LSTM、CNN 和 GNN 统一安排应用启动、后台任务、内存回收与 CPU runnable task 的 framework 组件。

`frameworks/base/services/core/java/com/android/server/am/` 在该标签下有 140 个直接目录项，没有 Kotlin 文件。提纲声称位于这里的 19 个 `.kt` 文件均不存在，包括：

- `MLScheduler.kt`、`MLModel.kt`、`ScheduleEngine.kt`；
- `BehaviorPredictor.kt`、`PreloadManager.kt`、`ResourceManager.kt`；
- `BackgroundOptimizer.kt`、`MemoryOptimizer.kt`、`BatteryAwareScheduler.kt`；
- `AdaptiveLearner.kt`、`AppSchedulerAPI.kt`、`DebugTools.kt`。

这些代码块还引用了没有定义的 `Task`、`ResourceRequirements`、`NeuralNetworkModel` 等类型，属于演示性质的伪代码。文件路径注释和 `@RequiresApi(35)` 不会把伪代码变成 Android API。

## Android 17 中对应职责由谁承担

| 关注对象 | AOSP 机制 | 能决定的范围 |
|---|---|---|
| 应用后台工作 | JobScheduler controllers、quota、App Standby bucket | Job 是否满足约束、是否在 quota 内、何时进入可执行集合 |
| 进程重要性 | AMS、OomAdjuster、process state、task profile | `oom_score_adj`、进程状态、回收与资源分组依据 |
| 周期性线程负载 | ADPF、`PerformanceHintManager`、Power HAL | 应用提交 target/actual duration 后，设备策略怎样响应 |
| CPU runnable entity | Linux fair scheduler / EEVDF | 根据权重、lag、eligibility 与 virtual deadline 选择任务 |
| 产品私有策略 | OEM service、vendor hook、私有模型 | 只对已验证的设备、版本和实现成立 |

这些机制可以先后影响同一个应用，但没有提纲所写的统一 `SchedulePrediction`、置信度排序或 `ResourceReservation` 对象。

### JobScheduler 与 App Standby

JobScheduler 根据网络、充电、idle、deadline、quota、App Standby bucket 等约束管理应用提交的 Job。`QuotaController` 按 effective standby bucket 维护执行时长与次数等额度，源码中没有预测模型训练或反向传播。

Android 17 的 App Standby 允许拥有 `CHANGE_APP_IDLE_STATE` 的特权组件提交 bucket，部分调用会标记为 `REASON_MAIN_PREDICTED`。这个 reason 只记录来源类别，不证明调用方使用机器学习。12 小时 prediction timeout、权限和观测字段见 [1.31 App Standby 预测与 ML 调度边界](./1.31-Android-17-机器学习驱动任务调度器.md)。

### OomAdjuster

OomAdjuster 根据 Activity、Service、Provider、Broadcast、绑定关系和进程状态计算进程重要性。它会推动 `oom_score_adj`、process state 和进程组更新，但不预加载预测应用，也不在内核 runqueue 中挑选下一条线程。

提纲中的 `MemoryOptimizer.compactMemoryFragments()` 不是 AOSP API。Android 的进程 compaction、LMKD 和内核内存回收有各自触发条件，不能改写成一个 ML scheduler 的方法。

### ADPF

`PerformanceHintManager` 接收应用显式报告的目标工作时长与实测工作时长。它没有收集提纲中的用户满意度，也不公开 CPU、NPU 或内存预留接口。设备策略怎样响应 hint 取决于 Power HAL 和产品实现。

ADPF 的 API 与线程约束见 [ADPF 自适应性能框架](../ch05-cpu-power/09-adpf.md)。

### EEVDF

`android17-6.18-2026-06_r6` 的 fair scheduler 使用 EEVDF 选择可运行实体。Framework 可以通过 nice、cpuset、cgroup weight、uclamp 和 task profile 改变约束，内核接口中没有 `predictedPriority`、模型置信度或用户行为特征。

CPU 调度细节见 [Android 17 EEVDF 调度器](../ch05-cpu-power/31-android17-eevdf-scheduler.md)。

## 开发者 API 核查

历史提纲宣称第三方应用可以调用 `AppSchedulerAPI.requestScheduleOptimization()` 并注册 `BehaviorEventListener`。Android 17 SDK 和对应 AOSP 包中没有这些类与方法，也没有与之匹配的 system service、AIDL 或权限。

版本号也被写错：API 35 对应 Android 15，API 36 对应 Android 16，API 37 对应 Android 17。伪代码中的 `@RequiresApi(35)` 不能作为 Android 17 能力证据。

应用能够使用的调度入口仍是职责明确的公开 API，例如 JobScheduler、WorkManager、AlarmManager 与 `PerformanceHintManager`。每个入口都有自己的权限、约束和失败语义，不接受提纲中的通用模型配置。

## 性能数据核查

冷启动 2.1 秒降到 1.4 秒、应用切换 120 毫秒降到 45 毫秒、续航 8.5 小时增到 10.2 小时等数字没有设备型号、构建版本、工作负载、样本量、温度、功耗模式、统计分布或 trace。对应 `BenchmarkTest.kt` 也不存在。

这组数字不能标成 Android 17 测试结果。若产品确有预测式扩展，至少应给出：

1. 系统服务、包名、模型或推理库；
2. 模型输出进入 JobScheduler、task profile、uclamp 或其他执行机制的调用路径；
3. 固定 build fingerprint 与设备状态；
4. 冷热缓存、温度、电量和输入序列一致的对照组；
5. 原始 Perfetto、功耗与内存数据，以及 P50/P90/P99 或置信区间。

没有这些材料时，只能记录为待验证的 OEM 假设。

## 来源问题

frontmatter 中的 `android-17-performance-optimization` 博客地址在本次审阅时返回 404。可访问的 [Android 17 官方发布说明](https://android-developers.googleblog.com/2026/06/Android-17.html) 没有发布提纲所述的 ML Scheduler、开发者 API 或基准表。

同题事实核查与源码阅读入口见 [1.59 Android 17「ML 驱动任务调度器」事实核查](./1.59-android17-ml-task-scheduler-comprehensive.md)。后续材料若继续使用“ML scheduling”一词，应写明它指 App Standby 的 privileged prediction input、JobScheduler prefetch、ADPF，还是某个产品私有模型。

## 源码锚点

- [Android 17 AMS 源码目录](https://android.googlesource.com/platform/frameworks/base/+/android-17.0.0_r1/services/core/java/com/android/server/am/)
- [Android 17 `JobSchedulerService.java`](https://android.googlesource.com/platform/frameworks/base/+/android-17.0.0_r1/apex/jobscheduler/service/java/com/android/server/job/JobSchedulerService.java)
- [Android 17 `QuotaController.java`](https://android.googlesource.com/platform/frameworks/base/+/android-17.0.0_r1/apex/jobscheduler/service/java/com/android/server/job/controllers/QuotaController.java)
- [Android 17 `AppStandbyController.java`](https://android.googlesource.com/platform/frameworks/base/+/android-17.0.0_r1/apex/jobscheduler/service/java/com/android/server/usage/AppStandbyController.java)
- [Android 17 `OomAdjuster.java`](https://android.googlesource.com/platform/frameworks/base/+/android-17.0.0_r1/services/core/java/com/android/server/am/psc/OomAdjuster.java)
- [Android 17 `PerformanceHintManager.java`](https://android.googlesource.com/platform/frameworks/base/+/android-17.0.0_r1/core/java/android/os/PerformanceHintManager.java)
- [Android common kernel `fair.c`](https://android.googlesource.com/kernel/common/+/refs/tags/android17-6.18-2026-06_r6/kernel/sched/fair.c)
