---
title: "Android 17 Binder 优先级继承机制详解"
chapter: "1.48"
status: finalized
drafted_date: "2026-07-02"
applicable_versions: "Android 17 (API 37)"
last_verified: "2026-08-08"
last_verified_against: "AOSP android-17.0.0_r1"
confidence: high
reviewed_date: "2026-08-08"
reviewed_by: "hermes-aiw-review-finalize-apply"
task6_state: reviewed
task9_state: reviewed
pipeline_stage: finalized
last_review_finalize_at: "2026-08-08T22:06:30+08:00"
last_review_finalize_run_id: "20260808-220550-a065c244"
last_rework_at: "2026-08-08T21:35:46+08:00"
last_rework_run_id: "20260808-213546-rework-3b3755ed"
sources:
  - type: aosp
    path: "frameworks/native/libs/binder/ProcessState.cpp"
  - type: aosp
    path: "frameworks/native/libs/binder/Binder.cpp"
  - type: aosp
    path: "frameworks/native/libs/binder/IPCThreadState.cpp"
  - type: aosp
    path: "frameworks/native/libs/binder/Parcel.cpp"
  - type: aosp
    path: "kernel/common/drivers/android/binder.c"
  - type: aosp
    path: "kernel/common/drivers/android/binder_internal.h"
  - type: aosp
    path: "include/uapi/linux/android/binder.h"
  - type: deepresearch
    path: "DeepResearch/2026-07-11-android17-binder-ipc-priority-inheritance-deepdive.md"
  - type: deepresearch
    path: "DeepResearch/2026-07-12-android17-binder-priority-inheritance-ipcthreadstate.md"
  - type: deepresearch
    path: "DeepResearch/2026-07-13-android17-binder-priority-inheritance-ipcthreadstate-propagation.md"
tags: ["Binder", "优先级继承", "Android 17", "IPC优化", "性能提升"]
related_chapters: ["1.47"]
---

# 1.48 Android 17 Binder 优先级继承机制详解


## 版本与概念边界

源码基线为 Android 平台 `android-17.0.0_r1` 与 Android common kernel `android17-6.18-2026-06_r6`。Binder 调度优先级、调用身份和 WorkSource 是三组独立状态，需要分别分析。

## Android 17 不包含的接口与策略

Android 17 平台和 kernel r6 没有以下类型或接口：

- `ProcessPriority`、`TransactionType`、`TransactionBatch`；
- `setDefaultThreadPriority()`、`setBatchWindowSize()`、`setMaxBatchSize()`；
- `priority_inheritance_threshold`、`priority_boost_factor` 等参数；
- 按进程前台/可见/cached 状态映射 Binder transaction queue 的固定表；
- 基于历史数据、机器学习或固定时间窗口重排 Binder transaction 的调度器。

Binder 也没有“oneway 自动批量合并、sync 永不合并、死亡通知优先级最高”的四级事务队列。oneway 与同步事务在是否等待 reply、优先级候选值、async 队列和错误反馈上有差异，但没有固定的高、中、低、最高分类。

所有 20%–80% 的性能提升、统一 4–64 线程范围、100 ms 慢事务阈值和 100 MB Binder 内存阈值都缺少测试环境与源码依据，不能作为 Android 17 平台指标。

## 调度优先级的有效输入

一次事务的候选优先级由 driver 在创建 `binder_transaction` 时选择：

| 条件 | 候选值 |
|---|---|
| 同步事务，Binder 支持调用线程的调度策略 | 调用线程的 policy 与 priority |
| oneway | 目标进程的 `default_priority` |
| 同步事务，Binder 不支持调用线程的调度策略 | 目标进程的 `default_priority` |

Binder 接受 `SCHED_NORMAL`、`SCHED_BATCH`、`SCHED_FIFO` 和 `SCHED_RR`。目标 node 的 `min_priority` 与 `sched_policy` 可以提高最低处理优先级；`inherit_rt` 决定 caller 的 FIFO/RR 候选值能否保留。缺少 RT 继承许可时，driver 把 RT 候选值降到 `SCHED_NORMAL`、nice 0。

应用到目标线程前还要经过 `CAP_SYS_NICE`、`RLIMIT_RTPRIO` 和 `RLIMIT_NICE` 校验。fair policy 可能只调用 `set_user_nice()`；policy 变化或使用 RT policy 才进入 `sched_setscheduler_nocheck()`。“一次继承必定执行一次 scheduler syscall”并不成立。

目标事务结束或失败时，driver 使用事务保存的 `saved_priority` 恢复线程。完整的候选值、node 最低策略、等待线程选择和恢复路径见后文源码入口与 `binder.c` / `binder_internal.h`。

## node 调度属性怎样进入内核

`Parcel::flattenBinder()` 把本地 `BBinder` 转成 `flat_binder_object`。`BBinder.setMinSchedulerPolicy()` 的 policy/priority 和 `setInheritRt()` 会编码进 `flags`，driver 在创建 `binder_node` 时解析为：

- `node->sched_policy`；
- `node->min_priority`；
- `node->inherit_rt`。

没有显式最低策略时，native libbinder 默认把 `SCHED_NORMAL`、nice 19 编入 node；调用 `IPCThreadState::disableBackgroundScheduling(true)` 后，默认 sched bits 变成 0，对应 `SCHED_NORMAL`、nice 0。它不会直接读取“接收方自己的默认优先级”。具体 Binder object 显式设置的最低策略仍可覆盖默认 sched bits。

这些 flags 描述 node 的最低处理策略和 RT 门禁。同步 caller 的候选优先级由 transaction 路径另行取得，两组输入在 `binder_transaction_priority()` 中比较。

## `SET/PENDING/ABORT` 保护恢复竞态

kernel r6 的 `binder_prio_state` 只有 `SET`、`PENDING` 和 `ABORT`。嵌套同步事务回复时，旧事务准备恢复优先级，会把状态设为 `PENDING` 并把目标值存入 `prio_next`。

如果另一笔新 transaction 在恢复窗口选中同一目标线程，新的优先级处理会：

1. 把新事务的 `saved_priority` 设为 `prio_next`；
2. 把线程状态改为 `ABORT`；
3. 应用新事务计算出的调度优先级。

旧恢复随后看到 `ABORT`，放弃写回已经过期的值。新事务结束时仍可借助自己的 `saved_priority` 恢复到正确状态。这是目标线程上的 restore 竞态处理，不代表“内层事务总是更紧迫”，也不要求事务发往同一 Binder node。

## Calling identity 不传递 CPU 调度优先级

driver 创建 transaction 时保存 `sender_euid`，并用内部的 `from_pid`、`from_tid` 记录发送进程和提交线程。向用户空间填充 `binder_transaction_data` 时：

- `sender_euid` 转换到当前 user namespace；
- 同步请求仍能取得 `t->from` 时，`sender_pid` 写入发送进程的 TGID；
- oneway 与 reply 等没有 `t->from` 的路径把 `sender_pid` 写为 0。

服务端 `IPCThreadState` 处理 `BR_TRANSACTION` 时，把这些字段放入 `mCallingPid`、`mCallingUid`，执行 `onTransact()` 后恢复此前的线程局部调用身份。内核内部的 `from_tid` 不在 `binder_transaction_data` 中公开给服务实现。

`clearCallingIdentity()` 返回的 64-bit token 保存：

```text
[ calling uid: 32 bits | pid sign: 1 bit |
  hasExplicitIdentity: 1 bit | pid remainder: 30 bits ]
```

bit 30 用于保存 `mHasExplicitIdentity`，bit 31 保留 PID 符号。该格式至少已出现在 `android-14.0.0_r1`，不能列为 Android 17 新设计。`mHasExplicitIdentity` 记录当前线程是否显式清除了 Binder caller identity，供嵌套 clear/restore 正确还原；它不代表“系统注入 PID”，也不参与 scheduler priority 计算。

WorkSource 还有独立的 `mWorkSource` 与 `mPropagateWorkSource`。它用于工作归因，AIDL stub 通常在 `Parcel.enforceInterface()` 处理中读取；Calling UID/PID、WorkSource 和目标线程调度优先级之间没有一一绑定关系。

## `binder_transaction_data` 的字段边界

面向用户空间的核心字段包括 `target`、`cookie`、`code`、`flags`、`sender_pid`、`sender_euid`、data size、offsets size 与数据指针。启用 transaction security context 的 node 使用 `binder_transaction_data_secctx`，在基本结构后附加 secctx 指针。

`from_pid`、`from_tid`、`saved_priority` 和 `set_priority_called` 属于 kernel 内部的 `binder_transaction`，不在 uAPI transaction data 中。分析日志或 BPF/tracepoint 时，要区分内部 transaction 对象和传给 libbinder 的 uAPI 结构。

## 可依赖的排查方法

- 用 Perfetto `android.binder` 模块的 `android_binder_txns` 找到 client/server 事务和 transaction id。
- 关联 `binder_set_priority`、`sched_switch`、目标线程 policy/nice/RT priority，确认优先级何时改变。
- 检查目标 `BBinder` 是否调用 `setMinSchedulerPolicy()`、`setInheritRt()`，以及进程是否设置全局 global inherit RT。
- 用服务端日志验证 `getCallingPid()` / `getCallingUid()` 时，区分同步请求、oneway、reply 与 `clearCallingIdentity()` 后的语义。
- 大事务、async 预算和 buffer 分配失败见 [1.44 Binder 4 MiB 映射上限、异步预算与优先级状态](1.44-android17-binder-sz4m-kernel-buffer-pool.md)。

## 源码入口

- [IPCThreadState.cpp @ android-17.0.0_r1](https://android.googlesource.com/platform/frameworks/native/+/android-17.0.0_r1/libs/binder/IPCThreadState.cpp)：Calling identity、WorkSource 与 transaction 用户态执行。
- [Parcel.cpp @ android-17.0.0_r1](https://android.googlesource.com/platform/frameworks/native/+/android-17.0.0_r1/libs/binder/Parcel.cpp)：node 调度 flags 的编码。
- [Binder.cpp @ android-17.0.0_r1](https://android.googlesource.com/platform/frameworks/native/+/android-17.0.0_r1/libs/binder/Binder.cpp)：`BBinder` 的最低策略与 RT 配置。
- [binder.c @ android17-6.18-2026-06_r6](https://android.googlesource.com/kernel/common/+/android17-6.18-2026-06_r6/drivers/android/binder.c)：transaction 候选值、优先级状态机和 sender 字段。
- [binder.h uAPI @ android17-6.18-2026-06_r6](https://android.googlesource.com/kernel/common/+/android17-6.18-2026-06_r6/include/uapi/linux/android/binder.h)：`binder_transaction_data` 与 flags。
