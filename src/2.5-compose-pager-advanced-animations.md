---
title: "Compose Pager 从基础到高级动画"
chapter: "2.5"
status: finalized
applicable_versions: "Android 14+ (API 34) - Android 17 (API 37)"
tags: ["Jetpack Compose", "动画", "Pager", "性能"]
related_chapters: ["2.1", "2.3"]
created_by: "task2a-knowledge-gap"
created_date: "2026-07-09"
gap_source: "素材驱动"
confidence: medium
last_verified: "2026-07-23"
sources:
  - "https://developer.android.com/develop/ui/compose/layouts/pager"
  - "https://developer.android.com/reference/kotlin/androidx/compose/foundation/pager/package-summary"
  - "https://developer.android.com/develop/ui/compose/animation"
reviewed_by: "hermes-aiw-review-finalize-apply"
reviewed_date: "2026-07-23"
task6_state: reviewed
task9_state: reviewed
pipeline_stage: finalized
last_review_finalize_at: "2026-07-23T19:06:04+08:00"
last_review_finalize_run_id: "20260723-190536-4cc75b06"
---

# 2.5 Compose Pager 从基础到高级动画

<!-- outline-start -->
## 要点

### 🔹 Pager 基础概念
Jetpack Compose `HorizontalPager` / `VerticalPager` 的基础架构和状态管理模式

### 🔹 页面切换动画
基于 `PagerState`、`currentPageOffsetFraction` 与 `graphicsLayer` 的页面变换

### 🔹 性能优化策略
页面切换中的重组控制、预加载边界与状态派生原则

### 🔸 高级边界与反模式
页面可见性降级、帧时间读取与自定义动画控制器的适用边界

## 扩展

### 🔸 页面可见性降级
用显式可见性控制重型内容，不把未公开机制写成 Pager 能力

### 🔸 Choreographer 与帧时间
优先使用 Compose 动画和 `withFrameNanos`；直接接入 `Choreographer` 需谨慎

### 🔸 自定义动画控制器
业务层动画开关的封装方法和性能考量

<!-- outline-end -->

## 🔹 Pager 基础概念

Jetpack Compose Foundation 的 Pager 组件为构建分页界面提供了声明式 API。与传统 `ViewPager` / `ViewPager2` 相比，Compose Pager 更强调：状态由 `PagerState` 暴露，页面内容由 Composable 按需组合，动画和视觉变换通过 Modifier 或 Compose 动画 API 叠加。

> 版本边界：本文面向 Android 14+ 到 Android 17 设备上的 Compose 应用实践；Pager 本身属于 Jetpack Compose Foundation 库能力，不应把库版本特性直接等同为 Android 平台版本特性。

### 基础架构

在较新的 Compose Foundation Pager API 中，页数通常交给 `rememberPagerState(pageCount = { ... })`，`HorizontalPager` / `VerticalPager` 接收 `state` 并通过尾随 lambda 构建页面：

```kotlin
@Composable
fun BasicPager(items: List<PageItem>) {
    val pagerState = rememberPagerState(pageCount = { items.size })

    HorizontalPager(
        state = pagerState,
        key = { page -> items[page].id }
    ) { page ->
        PageContent(items[page])
    }
}
```

关键点：

- **pageCount**：总页数由 `PagerState` 持有，影响布局、滚动目标和边界计算。
- **state**：管理当前页、目标页、滚动偏移和布局信息。
- **key**：为页面提供稳定标识，减少列表变化时的状态错配。
- **pageContent**：每个页面的 Composable 内容；应避免在其中执行重型同步计算。

### 状态管理

`PagerState` 是 Pager 的状态中心，常用字段包括：

- **currentPage**：当前最接近吸附位置（snap position）的页面。
- **settledPage**：滚动或动画完成后稳定停留的页面。
- **targetPage**：正在滚动或动画过程中预计到达的页面。
- **currentPageOffsetFraction**：当前页相对吸附位置的偏移比例，可用于视觉变换。
- **isScrollInProgress**：当前是否处于滚动状态。
- **layoutInfo**：当前可见页面与布局信息，适合做调试和轻量策略判断。

这些字段足以覆盖大多数「页码显示、Tab 同步、滚动状态反馈、页面变换」场景。

## 🔹 页面切换动画

ViewPager 到 Compose Pager 的迁移不仅是组件替换，也会改变动画实现位置：传统 ViewPager 常通过 PageTransformer 或 View 属性动画实现；Compose Pager 更常通过页面内容的 `Modifier.graphicsLayer`、`Modifier.alpha`、`Modifier.scale` 和 Compose Animation API 实现。

### 基于偏移量的页面变换

官方文档推荐使用 `currentPageOffsetFraction` 计算当前页面与目标页面的相对偏移，再映射到透明度、缩放、位移等视觉属性：

```kotlin
@Composable
fun AnimatedPager(items: List<PageItem>) {
    val pagerState = rememberPagerState(pageCount = { items.size })

    HorizontalPager(state = pagerState) { page ->
        val pageOffset = (
            (pagerState.currentPage - page) + pagerState.currentPageOffsetFraction
        ).absoluteValue

        Card(
            modifier = Modifier
                .fillMaxWidth()
                .graphicsLayer {
                    val scale = lerp(
                        start = 0.85f,
                        stop = 1f,
                        fraction = 1f - pageOffset.coerceIn(0f, 1f)
                    )
                    scaleX = scale
                    scaleY = scale
                    alpha = lerp(
                        start = 0.5f,
                        stop = 1f,
                        fraction = 1f - pageOffset.coerceIn(0f, 1f)
                    )
                }
        ) {
            PageContent(items[page])
        }
    }
}
```

使用这类动画时应注意两点：

1. 变换计算应保持轻量，避免在滚动帧中做 I/O、复杂分配或大对象创建。
2. 视觉变换适合放在 Modifier 层；业务状态变更应尽量由 `settledPage` 或显式事件驱动，避免在每帧滚动偏移变化时触发昂贵副作用。

### 与 TabRow 同步

典型做法是用 `pagerState.currentPage` 或 `settledPage` 驱动 Tab 选中态，点击 Tab 时调用 `animateScrollToPage`：

```kotlin
@Composable
fun PagerWithTabs(tabs: List<String>) {
    val pagerState = rememberPagerState(pageCount = { tabs.size })
    val scope = rememberCoroutineScope()

    TabRow(selectedTabIndex = pagerState.currentPage) {
        tabs.forEachIndexed { index, title ->
            Tab(
                selected = pagerState.currentPage == index,
                onClick = {
                    scope.launch { pagerState.animateScrollToPage(index) }
                },
                text = { Text(title) }
            )
        }
    }

    HorizontalPager(state = pagerState) { page ->
        Text(text = tabs[page])
    }
}
```

## 🔹 性能优化策略

页面切换中的性能优化核心不是「手写调度器」，而是控制组合范围、避免每帧昂贵计算，并为 Pager 提供稳定状态和可预测页面内容。

### 使用 derivedStateOf 过滤高频状态

如果 UI 只关心从滚动状态派生出的低频布尔值或简单枚举，应使用 `derivedStateOf` 降低无意义重组：

```kotlin
@Composable
fun PagerChrome(pagerState: PagerState) {
    val showPrevious by remember {
        derivedStateOf { pagerState.currentPage > 0 }
    }
    val showNext by remember {
        derivedStateOf { pagerState.currentPage < pagerState.pageCount - 1 }
    }

    NavigationButtons(
        showPrevious = showPrevious,
        showNext = showNext
    )
}
```

### 隔离页面内部状态

避免把会高频变化的顶层状态一路传递到每个页面。每个页面只接收自己需要的数据，并在页面内部缓存可复用计算：

```kotlin
@Composable
fun IsolatedPagerItem(item: Item) {
    val title = remember(item.id, item.title) { item.title.trim() }

    Box(
        modifier = Modifier
            .fillMaxSize()
            .background(item.backgroundColor)
    ) {
        ItemContent(
            title = title,
            content = item.content
        )
    }
}
```

### 控制预加载和重型内容

Pager 可以通过 `beyondViewportPageCount` 影响可见视口外页面的组合数量，通过 `pageSpacing` 调整页面间距；二者不要混为一谈。「更大的预加载」不总是更快。实践上应：

- 当前页和相邻页优先保障流畅；远离当前页的内容延后加载。
- 图片、视频、WebView 等重型资源跟随可见性启停，不要只依赖 Composable 是否存在。
- 用 Macrobenchmark、Perfetto/JankStats 等工具验证实际掉帧，而不是凭主观动画观感下结论。

> 版本边界：不要把 Compose Foundation 库 API 写成 Android 17 平台新增能力。若要讨论特定 Compose 版本的 Pager 行为，应同时标明 Compose Foundation 版本和来源。

## 🔸 高级边界与反模式

以下内容适合作为高级实践边界说明：优先使用公开 Compose API 和可验证性能数据，不把内部实现或实验性方向写成应用可依赖的 Pager 行为。

### 页面可见性降级

如果页面内包含视频、地图、WebView 或高频动画，业务代码更稳妥的做法是基于页面可见性显式降级重型内容，而不是依赖未公开的组合暂停机制：

```kotlin
@Composable
fun VisiblePagerItem(
    item: Item,
    isVisible: Boolean,
    content: @Composable () -> Unit
) {
    if (isVisible) {
        content()
    } else {
        PlaceholderContent(title = item.title)
    }
}
```

这种写法只表达业务层资源降级策略；它不声明 Pager 自动暂停组合，也不依赖 Android 17 平台新增行为。

### Choreographer 与帧时间

`Choreographer` 是 Android 平台的帧调度入口，应用可通过 `Choreographer.getInstance().postFrameCallback { frameTimeNanos -> ... }` 获取帧回调；但 Compose 动画一般不需要直接操作它，优先使用 Compose 的动画 API 或协程帧时钟，例如 `withFrameNanos`。

```kotlin
LaunchedEffect(Unit) {
    withFrameNanos { frameTimeNanos ->
        // 读取帧时间戳，执行轻量级计算或记录
        recordFrame(frameTimeNanos)
    }
}
```

如果确实需要直接接入平台 Choreographer，应至少保证：

- 使用正确 API：`Choreographer.getInstance()`，不是 `Choreger`。
- 识别 VSYNC（不是 “VSENS”）与帧时间戳的含义。
- 回调中只做轻量工作；重型计算应提前完成或移到后台线程。
- 不把未验证的 deadline/帧预算能力写成 Pager 公开行为。

### 自定义动画控制器

高级动画控制器适合封装业务层动画状态，而不是替代 Compose 内置滚动和动画系统：

```kotlin
class PagerAnimationController {
    private val activePages = mutableSetOf<Int>()

    fun onVisiblePagesChanged(pages: List<Int>) {
        activePages.clear()
        activePages.addAll(pages)
    }

    fun shouldRunHeavyAnimation(page: Int): Boolean {
        return page in activePages
    }
}
```

性能考量：

1. **内存管理**：不要为每帧滚动创建大量临时对象。
2. **计算复杂度**：滚动路径上只保留 O(1) 或很小 O(n) 的轻量计算。
3. **帧率稳定**：动画复杂度必须用实际 trace/benchmark 验证。

## 复查结论

本轮复查已移除未证实的 Android 17 Pager 智能缓存、Pausable Composition 直连 Pager、Choreographer Deadline 等平台级断言；保留内容限定在 Android Developers 文档可支撑的 Compose Pager API、页面变换示例、帧时间读取边界和性能验证建议。本文可作为 Android 14+ 到 Android 17 设备上的 Compose Pager 实践章节进入 `finalized`，但后续若新增 Compose Runtime / Foundation 内部机制，仍需补充对应版本和源码证据。

## 参考资料

### Android Developers — Pager in Compose
- 来源：https://developer.android.com/develop/ui/compose/layouts/pager
- 类型：官方文档
- 摘要：介绍 `HorizontalPager`、`VerticalPager`、`rememberPagerState`、页面状态读取和页面变换示例。

### Android Developers — Compose Foundation Pager API reference
- 来源：https://developer.android.com/reference/kotlin/androidx/compose/foundation/pager/package-summary
- 类型：官方 API reference
- 摘要：提供 Compose Foundation Pager 包的 API 入口和签名参考。

### Android Developers — Animation in Compose
- 来源：https://developer.android.com/develop/ui/compose/animation
- 类型：官方文档
- 摘要：介绍 Compose 动画 API 的常规使用边界。

### Compose Pager 深度教程 — 从基础 HorizontalPager/VerticalPager 到 PagerState 编程控制
- 来源：https://juejin.cn/post/7618433588125007923
- 类型：实战教程
- 摘要：从基础 HorizontalPager/VerticalPager 到 PagerState 编程控制、页面切换动画、嵌套滚动等进阶用法；可作为实践参考，但不足以单独支撑 Android 17 平台级结论。
- 入库时间：2026-07-15
- 评分：12/20
