---
title: ANR 治理策略
chapter: '20.4'
status: ready-for-review
applicable_versions: Android 10 (API 29) - Android 17 (API 37)
tags:
- app
- stability
- anr
- governance
created: '2026-06-23'
updated: '2026-06-25'
---

# 20.4 ANR 治理策略

> 本文件是流水线误生成的重复入口，不在 `SUMMARY.md` 中。ANR 治理的唯一正文是 [04-anr-governance.md](04-anr-governance.md)。

主章节已经以 Android 17 / API 37 / `android-17.0.0_r1` 为锚点，覆盖以下内容：

- 输入分发、广播、Service、ContentProvider 与前台服务的计时边界；
- 主线程、Binder、锁竞争和组件回调的治理方法；
- 应用 Watchdog、API 37 ANR warning、profiling trigger 与 `ApplicationExitInfo` 的证据关联；
- 前后台 ANR、Google Play 指标口径和系统负载分桶；
- AOSP 源码锚点、公开 API 示例与回归验证流程。

本副本原有正文因技术内容不可继续传播而移除，与篇幅和表达无关：

- 把 Choreographer 帧调度描述成系统 ANR 检测器，并配有不存在的 `predictNextVSync()`、`reportFrameJank()` 代码；
- 虚构 `ActivityManagerService.checkForAnr()` 轮询线程、500 ms 预警和 `InputReader` 分发超时实现；
- 声称 Android 17 引入不存在的 `DeliQueue` 无锁消息队列；
- 把输入、广播、Service、provider 等不同计时器压成固定时限，忽略设备配置、CPU 饥饿扩展和计时起止点；
- 使用无来源的业务损失数字、效果百分比和统一目标值；
- 推荐已废弃的 `LocalBroadcastManager`、`JobIntentService`，并给出不能编译或生命周期错误的示例。

后续修改、引用和 Hermes 审核都应落在 [04-anr-governance.md](04-anr-governance.md)。保留这个文件只为兼容可能存在的历史路径，不再承载独立正文。
